# I2Cスレーブ 非同期ロジック回路 設計ノート

参照仕様: NXP `UM10204` *I2C-bus specification and user manual*, Rev. 5.0J（2012年、日本語版）

> **v2アーキテクチャへの更新（本ノート全体に影響）**: `i2c_slave_async.v` は
> Yosysでの論理合成不能（`ERROR: Found non-synthesizable event list!`）を機に
> ゲート合成可能な構成へ全面的に書き直した（以下 v2）。本ノートの §2/§3 は
> v2の実装に合わせて更新済み。v2の合成結果・検証方法は §7.5 を参照。

## 1. 設計方針

本設計にはシステムクロック（`clk`）が一切ない。すべての状態遷移は SCL / SDA
そのもののエッジによって駆動される（バスタイムド／自己タイミング回路）。これは
仕様書が定義しているプロトコルの構造そのものと一致する。

| 仕様書の規定 | 対応する回路動作 |
|---|---|
| 3.1.3 データの有効性 — SCL=HIGH中はSDA安定、変化はSCL=LOW中のみ | ビットのサンプルはSCL立上りエッジ、出力の変更はSCL立下りエッジで行う |
| 3.1.4 START/STOPコンディション — SCL=HIGH中のSDA遷移で定義 | SDAのエッジをSCLレベルでゲートして検出（NANDラッチ相当、図5） |
| 3.1.5 バイトフォーマット — 8bit・MSBファースト | 8bitシフトレジスタ、ビットカウンタでMSBファーストを保証 |
| 3.1.6 ACK/NACK — 9番目のクロックでレシーバがSDAをLOWに保持 | ACK/NACK専用ステート（ADDR_ACK / DATA_WR_ACK / DATA_RD_ACK） |
| 3.1.9 クロックストレッチ（オプション） | 未実装（本スレーブはSCLを駆動しない）。実装する場合は §5 参照 |

## 2. ステートマシン（v2: `phase` エンコーディング）

```
PH_ADDR --[8bit]--> PH_ADDR_ACK --+--(addr match, R/W=0)--> PH_DATA_WR --[8bit]--> PH_DATA_WR_ACK --> PH_DATA_WR ...
                                   +--(addr match, R/W=1)--> PH_DATA_RD --[8bit]--> PH_DATA_RD_ACK --(master ACK)--> PH_DATA_RD ...
                                   +--(no match)------------> PH_IGNORE                              --(master NACK)--> PH_IGNORE
任意のphase --(start_pulse)--> PH_ADDR（非同期RESET, §3参照）
busy=0（STOP後）--> sda_oeドメインも非同期リセット
```

- `PH_ADDR`（=リセット値 `3'b000`）: アドレス+R/Wビット（8bit）をSCL立上りで
  シフトイン。8bit目でアドレス一致判定（`addr_ok`）とR/Wビット（`rw_bit`）を
  確定し `PH_ADDR_ACK` へ。
- `PH_ADDR_ACK` / `PH_DATA_WR_ACK`: SCL立下りで`sda_oe`をLOWに駆動（ACK）。
  アドレス不一致時は駆動せず`PH_IGNORE`へ（NACK、STOP待ち）。
- `PH_DATA_WR`: マスタ→スレーブのデータ8bitをシフトイン。8bit目で`rx_data`
  確定、`rx_valid`は`phase==PH_DATA_WR_ACK`の間アサート。
- `PH_DATA_RD`: スレーブ→マスタ。SCL立下りごとに`tx_data`（アプリ側が用意
  した値）をMSBファーストでシフトアウト。
- `PH_DATA_RD_ACK`: マスタが返すACK/NACKをSCL立上りでサンプルし、ACK
  （`sda_in==0`）なら`PH_DATA_RD`へ戻って次バイト送出、NACKなら
  `PH_IGNORE`へ（仕様3.1.6の条件5と一致）。
- `PH_IGNORE`: アドレス不一致／NACK後、次のSTOPまたはSTARTを待つだけの状態。

`phase`の値を`PH_ADDR = 3'b000`（＝レジスタのリセット値そのもの）に選んで
あるのがv2の設計上の要点で、これにより START検出時は「非同期RESETを1本
かけるだけ」で `PH_ADDR` に戻せる（§3参照、DFFRが非同期SETを持たないため）。

## 3. 実装上の重要な注意（v2: 非同期部と同期部の分離）

v1（旧版）は START/STOP検出とビット処理を単一の`always`ブロックに統合し、
SCL/SDAの1つ前の値をシャドウレジスタとして保持するレベル比較方式だったが、
Yosysはこれを合成不能と判定した（`always @(scl or sda_in or negedge rst_n)`
のようにレベル感度とエッジ感度が混在する`always`は、純粋な`@*`組合せ回路
としても単一エッジの同期回路としても認識されないため）。

v2ではこれを次の3つのドメインに明確に分離した:

1. **非同期のSTART/STOP検出**（クロックなし）: `DEL1`遅延セルでSDAの
   遅延コピー`sda_d`を作り、`scl & sda_d & ~sda_in`（SDA 1→0 かつ SCL=1）
   / `scl & ~sda_d & sda_in`（SDA 0→1 かつ SCL=1）の組合せ論理だけで
   `start_pulse`/`stop_pulse`を検出する。これはUM10204 3.1.4の定義その
   ものであり、レジスタを一切使わない。
2. **`busy`ラッチ**: `NOR2`を2個クロスカップルしたSRラッチ
   （`start_pulse`でSET、`stop_pulse | ~rst_n`でCLEAR）。ゲートレベルで
   非同期回路として素直に書ける形。**旧`logic_cells_mapping.md`のNAND2案
   から、実装ではNOR2クロスカップルに変更されている**（両方式とも
   UM10204図5相当のSRラッチだが、極性の都合でNOR2を採用）。
3. **SCLエッジ駆動の同期部**: `phase`/`bit_cnt`/`shreg`/`addr_ok`/`rw_bit`
   /`rx_data`はSCL立上りエッジ（`posedge scl`）で駆動される`DFFR`
   （非同期RESET付きD-FF）バンク。`sda_oe`/`txreg`はSCL立下りエッジ
   （`INV_X1`で`scl`を反転した`scl_n`の`posedge`）で駆動される別の
   `DFFR`バンク。SCLそのものが「クロック」であり、これはバスタイムド
   設計の本質と一致する。START時は`rst_scl_domain = ~rst_n | start_pulse`
   で`PH_ADDR`（＝リセット値）へ非同期に戻し、STOP後（`busy`落下）は
   `rst_sdaoe_domain = ~rst_n | ~busy`で`sda_oe`側をリセットする。

この分離により、各`always`ブロックは「純粋な非同期組合せ」または「単一
エッジ+単一非同期リセットのDFFRパターン」のいずれかとなり、Yosysの
`proc`/`dfflegalize`/`dfflibmap`/`abc`パスで問題なく合成できることを
確認済み（§7.5）。

## 4. タイミング仕様との関係（表10, Standard/Fast/Fast-mode Plus）

本FSMの正しさはクロック周波数に依存しない（イベント駆動のため）。しかし
実際にゲート/ラッチで実装する際は、以下の値より内部遅延（ゲート伝搬遅延の
合計）が十分短いことを確認する必要がある。

| パラメータ | Standard-mode (最小) | Fast-mode (最小) | Fast-mode Plus (最小) |
|---|---|---|---|
| tHD;STA（START保持時間） | 4.0 µs | 0.6 µs | 0.26 µs |
| tLOW（SCL LOW期間） | 4.7 µs | 1.3 µs | 0.5 µs |
| tHIGH（SCL HIGH期間） | 4.0 µs | 0.6 µs | 0.26 µs |
| tSU;DAT（データセットアップ） | 250 ns | 100 ns | 50 ns |
| tSU;STA / tSU;STO | 4.7 µs / 4.0 µs | 0.6 µs / 0.6 µs | 0.26 µs / 0.26 µs |

特に重要: ACK駆動（SCL立下り→`sda_oe`変化）の内部遅延は、次のSCL立上り
（マスタがACKをサンプルする瞬間）までに収まっていなければならない
（`tSU;DAT`/`tVD;ACK`相当）。

## 5. パワーオンリセット必須（重要）

このFSMは`clk`を持たないため、内部レジスタ（`state`, `sda_oe`, `scl_q`,
`sda_q`, `busy`など）が確定値を持つ経路は`if (!rst_n)`分岐のみである。
実機・実シミュレーションでは全レグが不定値（X）で起動するため、**`rst_n`の
ローパルスを必ず与えること**。これを怠ると`scl_q`/`sda_q`がXのまま伝播し、
STARTすら検出できず全機能が停止する（4-state Verilogシミュレータで実際に
発生し、`i2c_slave_async_tb.v`で修正済み）。

MyHDLモデル側で最初の検証がこの問題を検出できなかったのは、MyHDLの
`Signal(...)`がPython側で明示的な初期値を持ち、Xに相当する不定状態が
存在しない2値（état）シミュレーションだったため。実チップでは相当する
パワーオンリセット（POR）回路が必須であることを`i2c_slave_async.v`利用時の
前提条件として明記する。

## 6. 既知の制限と拡張ポイント

- 7bitアドレッシングのみ（10bitアドレッシング未対応、§3.1.11参照）
- クロックストレッチ未対応（`scl`は入力のみ、スレーブはSCLを駆動しない）
- データ受信は常にACKする設計（アプリ側でNACKしたい場合は`DATA_WR_ACK`の
  `sda_oe`駆動を外部フラグでゲートする）
- マルチマスタ調停（§3.1.7/3.1.8）はスレーブロジックの範囲外のため未実装
- SDA/SCL入力にはUM10204 §3.1.2のスパイクフィルタ（50ns未満を抑制）を
  本モジュールの手前に別途置くことを推奨（本モジュールはすでに整形された
  信号を受け取る前提）

## 7. 検証方法

開発時のサンドボックスには `iverilog` / `Verilator` が無かったため、同一の
ステートマシンを MyHDL（Python）で2種類実装し、Pythonのイベント駆動
シミュレータ上でバス機能モデル（マスタBFM）と接続してまず検証した。

- `i2c_slave_async_model.py` : 複数always-block構成での初期モデル
  （`test_i2c_slave_async.py`, `test_i2c_slave_async_negative.py` で検証）
- `i2c_slave_async_model_v2.py` : `i2c_slave_async.v` と同一の単一プロセス
  ／シャドウレジスタ構成（実際に納品するRTLの構造そのもの）
  （`test_v2_positive.py`, `test_v2_negative.py` で同一シナリオを再検証）
- 両モデルとも以下すべてPASS:
  - 書き込み: START→ADDR+W→ACK→0xA5→ACK→STOP、`rx_data`確認
  - 読み出し: START→ADDR+R→ACK→0x3Cシフトアウト→マスタNACK→STOP
  - アドレス不一致: START→誤アドレス→NACK確認、`addr_match`が立たないこと

その後 `i2c_slave_async_tb.v` をローカル環境の`iverilog`で実行したところ、
全項目FAILとなった。原因は**テストベンチが`rst_n`のリセットパルスを
一度も発行していなかったこと**（§5参照）。MyHDLは2値シミュレーションの
ため問題が表面化しなかったが、4-state Verilogでは全レジスタがXのまま
デッドロックし、STARTすら検出できなかった。テストベンチにリセットパルスを
追加して修正。

リセットパルス追加後も、STARTに依存する項目のみ再びFAILした（`busy`が
一度も1にならない等）。原因は`i2c_slave_async.v`側の**レースコンディション**:
`start_cond`/`stop_cond`/`scl_rise`/`scl_fall`を独立した`wire = ...`の連続
代入として宣言していたが、その`always`ブロック自体も同じ`scl`/`sda_in`の
変化で起動するため、「wireの再評価」と「always内でそのwireを読む」の間の
実行順序はIEEE Verilogの仕様上保証されない。一部のシミュレータ（iverilogを
含む）では、always側が遷移前（stale）の値を読んでしまい、START/STOP検出が
恒常的に失敗する。MyHDLモデルはこれらの条件を`@always`関数内でPythonの
ローカル変数として直接計算していたため、この種のレースが最初から存在せず、
再現できなかった。

**修正**: `start_cond`等の外部wireを廃止し、`scl_q`/`sda_q`/`scl`/`sda_in`を
always内で直接読んでインライン評価するように変更（同じプロセスが所有する
レジスタ／トリガ信号を直接読むだけなので、レース条件が原理的に発生しない）。

この2件はいずれも実際に`iverilog`で実行して初めて判明したバグであり、
MyHDL（2値・Python内オブジェクト直接評価）だけでは検出できなかった。
修正後、ユーザー環境（実機`iverilog`）で以下コマンドを実行し、
**14項目全てPASS**を確認済み（write/read/アドレス不一致の3シナリオ全て）。

```
cd src
iverilog -o sim i2c_slave_async.v i2c_slave_async_tb.v && vvp sim
```

MyHDLモデルはPythonの2値シミュレーションであるため、今後この設計を変更する
際はMyHDLでの動作確認だけでなく、**必ず実際のVerilogシミュレータ（iverilog等）
で再実行して確認すること**。X不定値の伝播やイベント順序に依存するレース
条件は2値シミュレーションでは原理的に再現できない。

## 7.5 v2: Yosys論理合成とその検証

`i2c_slave_async.v`（v2）を実スタンダードセル（`TR1um_5_stdcell`）へ
論理合成できることを、`yowasp-yosys`（WebAssembly版Yosys、`pip install
yowasp-yosys`でネットワーク制限下でも導入可能）を用いて確認した。

```
yowasp-yosys -p "
  read_verilog i2c_slave_async.v
  hierarchy -top i2c_slave_async -keep_portwidths
  proc; opt
  techmap; opt
  dfflegalize -cell \$_DFF_PP0_ 0
  dfflibmap -liberty TR1um_5_stdcell.lib
  abc -liberty TR1um_5_stdcell.lib
  write_verilog i2c_slave_async_net.v"
```

- `techmap; opt` を `dfflegalize`/`dfflibmap` の前に挟む必要がある。
  Yosysの`proc`直後は粗粒度の`$adff`/`$adffe`セルのままで、`dfflegalize`
  /`dfflibmap`は細粒度の`$_DFF_*`セルしか見ないため、techmapで一度
  細粒度化しないと`DFFR`へのマッピングが（エラーなく）失敗する。
- ABC (`abc -liberty ...`) はYosys本体の`read_liberty`より厳格なLiberty
  パーサを持ち、`intrinsic_rise`/`intrinsic_fall`のようなスカラー遅延
  記法を受け付けない。`lu_table_template`＋`cell_rise`/`cell_fall`/
  `rise_transition`/`fall_transition`のテーブル形式で書き直す必要が
  あった（`TR1um_5_stdcell.lib`参照）。
- 合成結果（`i2c_slave_async_net.v`, 1604行）: **33×DFFR**（`phase`/
  `bit_cnt`/`shreg`/`addr_ok`/`rw_bit`/`rx_data_r`/`sda_oe_r`/`txreg`の
  合計ビット数と一致）、組合せ部は7×AND2_X1・30×NAND2・6×NAND3・
  2×NAND4・18×NOR2・3×NOR3・5×NOR4・6×OR2・2×OR3・1×OR4・19×MUX2・
  11×INV_X1、および元RTLで直接インスタンス化していた1×DEL1・2×NOR2
  （busyラッチ）・1×INV_X1。ラッチ（インファード）や未マッピングセルは
  ゼロ。XOR2/XNOR2/BUF_X1/DFFSは今回未使用。
- **ABCが生成した144個のセルインスタンスには`.VDD`/`.GND`接続が一切付与
  されない**（ABCのブール最適化は電源ピンを意識しないため）。これは
  Pythonの正規表現post-processスクリプトで全インスタンスに
  `.VDD(VDD), .GND(GND)`を追記して補完済み（`i2c_slave_async_net.v`は
  補完後の版）。
- `TR1um_5_stdcell.lib` は**タイミング未特性化のプレースホルダ**
  （固定値の2x2テーブル）。論理・接続の正しさの確認が目的で、STA/
  タイムクロージャ/テープアウト用途には使えない。実SPICE特性化後に
  差し替えが必要。
- 形式的等価性検証（合成前後のSAT/帰納法によるequivalenceチェック、
  Yosysの`equiv_make`/`equiv_induct`）はサンドボックスのbashツールが
  45秒のハードタイムアウトを持ち、かつバックグラウンドプロセスが
  ツール呼び出しをまたいで維持できない制約のため、**未実施・断念**。
  代わりに以下の2点を合成の妥当性の根拠とする:
  1. v2アーキテクチャと同一構造のMyHDLモデル
     （`script/i2c_slave_async_model_v3.py`）による機能検証
     （`script/test_v3_positive.py` / `test_v3_negative.py`、write/read/
     アドレス不一致の3シナリオ全てPASS）。
  2. Yosys合成がエラー・ラッチ推論・未マッピングセルなしでクリーンに
     完了していること。
- ローカル`iverilog`での実4値シミュレーション確認は、v2の
  `i2c_slave_async.v`本体に対しても**実施済み**（14項目全PASS、§7.6）。
  v1同様、MyHDLの2値シミュレーションでは検出できない実バグ（今回は
  `DEL1`/`NOR2`遅延が同値だったことによるSETパルス競合、§7.6）が実
  `iverilog`実行で初めて発覚しており、この種の確認がこの設計にとって
  引き続き重要であることが再確認された。実行するには
  `src/stdcell_behavioral_stubs.v`（DEL1/NOR2/INV_X1/DFFR等の
  ビヘイビアモデル、タイミング精度なし）を`i2c_slave_async.v`・
  `i2c_slave_async_tb.v`と一緒にコンパイルする:
  ```
  cd src
  iverilog -o sim i2c_slave_async.v stdcell_behavioral_stubs.v i2c_slave_async_tb.v && vvp sim
  ```
  同じ`stdcell_behavioral_stubs.v`を使えば、合成後の`i2c_slave_async_net.v`
  自体をiverilogで直接シミュレートすることも可能（ゲートレベル確認）。

## 7.6 v2: iverilog実機シミュレーションで発覚したbusyラッチのパルス競合

`stdcell_behavioral_stubs.v`（全セル一律 `#1` 遅延）で `i2c_slave_async.v`
（v2）を実際に`iverilog`実行したところ、以下5項目がFAILした
（addr_match/rw/rx_data等は全てOK）:

```
FAIL: busy asserted after START
FAIL: slave ACKed matching address (write)
FAIL: slave ACKed data byte
FAIL: slave ACKed matching address (read)
FAIL: read byte == 0x3C
```

**根本原因**: `start_pulse`/`stop_pulse`は`DEL1`の遅延時間ぶんだけの幅を
持つワンショットパルスである（`sda_d`が`sda_in`に追いつくまでの間だけ
`scl & sda_d & ~sda_in`等が真になる）。このパルスが`busy`ラッチ
（`NOR2`2個の交差結合、`u_lat_qn`→`u_lat_q`で1往復＝ゲート2段）を
確実にSET/RESETするには、**パルス幅がラッチの帰還ループ遅延より
十分広い**必要がある。ところがスタブモデルは`DEL1`も`NOR2`もどちらも
`#1`と同じ遅延値にしていたため、パルス幅（=DEL1遅延）とラッチ内部の
1ゲート遅延がちょうど一致し、Verilogのinertial delay（幅がdelay値と
同程度以下のパルスは出力に伝播しないことがある）により**SETパルスが
ラッチに届く前に消えてしまい、`busy`が一度もHIGHにならない**という
競合が発生した。

`busy`がHIGHにならないと、`rst_sdaoe_domain = ~rst_n | ~busy`が
トランザクション中ずっとアサートされたままになり、`sda_oe`/`txreg`用の
DFFRバンク（ACK駆動・読み出しデータ駆動を担当）が常時非同期リセット
状態に固定される。一方`phase`/`shreg`/`addr_ok`等のSCL(posedge)ドメインは
`busy`に依存しないため正常に動作し続けた——これが「addr_match/rw/rx_data
は正しいのに、ACK駆動と読み出しデータ駆動だけが軒並み失敗する」という
FAILパターンの説明になる（`busy cleared after STOP`等のチェックは、
`busy`が最初から一度もHIGHにならないため見かけ上OKになっただけで、
実際にはSET/CLEAR動作を検証できていなかった）。

**対策**: `stdcell_behavioral_stubs.v`の`DEL1`遅延を`#1`→`#4`
（`DEL2`/`DEL4`もそれぞれ`#8`/`#16`に）拡大し、パルス幅をラッチの
ゲート遅延（`#1`のまま）に対して十分（4倍）広く確保した。これはRTL
（`i2c_slave_async.v`）自体の構成は変更せず、シミュレーション用スタブの
遅延値のみを実際の設計意図（ヘッダコメント「real DEL1 is intentionally
slower than a plain buffer」）に合わせて修正したもの。

**確認**: 修正後、ユーザー環境の`iverilog`で`i2c_slave_async.v` +
`stdcell_behavioral_stubs.v` + `i2c_slave_async_tb.v`を再実行し、
**write/read/アドレス不一致の3シナリオ・14項目全てPASS**を確認済み
（`busy asserted after START`を含む、上記でFAILしていた5項目も全てOK
に変わったことを確認）。これによりv2 RTLはMyHDL(model_v3)・Yosys合成
（クリーン、ラッチ推論なし）・実iverilog4値シミュレーションの3方面
すべてで検証済みとなった。

**重要（実シリコンへの申し送り事項）**: これは単なるシミュレーション上の
アーティファクトではなく、**実際の物理設計でも成立させる必要がある
タイミング制約**である——`DEL1`の実遅延は、`busy`ラッチを構成する
`NOR2`2段の帰還ループ遅延に対して十分なマージン（目安: 数倍以上）を
持つ必要がある。`TR1um_5_stdcell.lib`が実SPICE特性値に置き換わった際、
`DEL1`と`NOR2`の実遅延比を確認し、このマージンが確保されているかを
再検証すること。不足していれば`DEL1`のインスタンスを`DEL2`/`DEL4`に
変更するなどして対応する。

## 8. ファイル一覧

- `i2c_slave_async.v` — 納品RTL本体（v2: 非同期SR部＋SCLエッジ同期部）
- `i2c_slave_async_net.v` — Yosys+ABC合成済み構造Verilogネットリスト
  （`TR1um_5_stdcell.lib`ベース、VDD/GND補完済み）
- `i2c_slave_async_tb.v` — ローカルシミュレータ用テストベンチ（VDD/GND配線済み）
- `stdcell_behavioral_stubs.v` — iverilogで`i2c_slave_async.v`/
  `i2c_slave_async_net.v`をコンパイルするためのセルビヘイビアモデル
  （非タイミング精度）
- `../TR1um_5_stdcell.lib` — プレースホルダLiberty（未特性化、論理合成用）
- `i2c_slave_async_model.py` / `i2c_slave_async_model_v2.py` — 旧版（v1）検証用MyHDLモデル
- `i2c_slave_async_model_v3.py` — v2アーキテクチャに対応するMyHDLモデル
- `test_i2c_slave_async.py` / `test_i2c_slave_async_negative.py` — model.py用テスト（実行済み・全PASS）
- `test_v2_positive.py` / `test_v2_negative.py` — model_v2.py用の同一テスト（実行済み・全PASS）
- `test_v3_positive.py` / `test_v3_negative.py` — model_v3.py用の同一テスト（実行済み・全PASS）
- `../schematic/i2c_slave_async_net.sch` — `i2c_slave_async_net.v`から自動生成したxschem回路図、ネットラベル方式（§9参照）
- `gen_schematic.py` — 上記（ラベル方式）回路図の生成スクリプト
- `../schematic/i2c_slave_async_net_routed.sch` — 同、実配線版（§10参照）
- `gen_schematic_routed.py` — 上記（配線版）回路図の生成スクリプト
- `gen_liberty.py` — `../TR1um_5_stdcell.lib`の生成スクリプト。セルごとの
  面積・遅延・論理関数を`COMB_CELLS`/`FF_CELLS`のテーブルで管理しており、
  実SPICE特性化後は該当セルの数値を書き換えて`python3 script/gen_liberty.py
  > TR1um_5_stdcell.lib`を再実行すれば反映できる（現行の`.lib`と
  バイト単位で一致することを確認済み）。
- `verify_spice.py` — xschemが書き出したSPICEネットリストと
  `i2c_slave_async_net.v`の接続を機械的に突き合わせる検証スクリプト（§11参照）。
  `python3 script/verify_spice.py`（パス3箇所がハードコードされているため
  環境が変わった場合は書き換えが必要。デフォルトの参照先は
  `schematic/i2c_slave_async_net.spice`）。
- `../schematic/i2c_slave_async_net.spice` — 接続検証済みのSPICE
  ネットリスト。KLayout LVSの正解ネットリストとして再利用できる（§12参照）。

## 9. xschem回路図の自動生成（P&R環境が無いため手作業レイアウト用）

P&Rツールが無い環境のため、レイアウトは手作業で進める前提。その参照用に、
合成済みネットリスト `i2c_slave_async_net.v` から xschem 形式の回路図
（`schematic/i2c_slave_async_net.sch`）を `script/gen_schematic.py` で自動生成した。

- **配線方式**: 見た目の引き回し（ratsnest配線）は行わず、**ネットラベル方式**
  を採用。各セルインスタンスの各ピンに20単位の短いスタブ配線を生やし、
  そこへ `{lab=NETNAME}` を付与する。同じ`lab`を持つ配線はxschem上で
  電気的に同一ネットとして扱われる——これは`TR-1um_5_stdcell`ライブラリ
  自身の各セルの内部`.sch`（例: `NOR2.sch`）で使われているのと同じ手法で、
  VDD/GNDの电源網も含め全て`lab=`一致だけで正しく接続される。見た目には
  引き回されていないが、接続情報としては完全に正しい。
  手動レイアウト時の「このピンとこのピンは同じネットか」の確認に使うことを
  想定しており、美しく整った回路図ではない点に注意。
- **セル配置**: `i2c_slave_async_net.v`中のインスタンス順に12列のグリッドへ
  機械的に配置しただけで、論理的なまとまり（例: SCLドメインのDFFR群）を
  意識したレイアウトにはなっていない。手動レイアウトの参考にする際は、
  design_notes.md §2/§3のブロック構造（非同期SR部／SCL posedgeドメイン／
  SCL negedgeドメイン）を併読することを推奨する。
- **ネット名の解決**: Yosysが生成した`i2c_slave_async_net.v`には
  `assign _040_ = bit_cnt[0];`のような大量のネットエイリアス（同一の電気的
  ネットに複数のVerilog上の名前が付いている）が含まれる。スクリプトは
  Union-Findでこれらを一つのネットにまとめた上で、可能な限り
  `phase[0]`/`addr_ok`/`rst_n`のような意味のある名前を優先して表示し、
  該当がない場合のみYosysの`_NNN_`形式の名前にフォールバックしている。
  なお、どのセルにも接続されていない未使用の内部バス（`_322_`等、`case`文
  合成の副産物でロジックには寄与しないdead net）は回路図から除外済み。
- **セルシンボルの参照パス**: 各スタンダードセルは絶対パス
  （`/Users/okamura/Dropbox/91_OpenPDK/TR-1um/libs.tech/xschem/TR-1um_5_stdcell/*.sym`）
  で参照している。I/Oピン（`devices/ipin.sym`等）はxschem標準ライブラリを
  相対参照しており、`TR-1um_5_stdcell`内の既存`.sch`ファイルと同じ書き方
  （§2冒頭で確認したNOR2.schの記法）に合わせてある。
- **未検証事項**: このリポジトリのサンドボックスにはxschem本体が無いため、
  生成した`.sch`ファイルを実際にxschemで開いて構文・接続の妥当性を
  確認する作業は**未実施**。まずユーザー環境のxschemで開き、エラーが
  出ないこと、`i2c_slave_async_net.v`と同じ144セル＋4個の手動配置セル
  （`u_del_sda`/`u_inv_scl`/`u_lat_q`/`u_lat_qn`）が過不足なく存在すること、
  ネットリスト書き出し（xschem→spice/verilog netlist機能）で元の
  `i2c_slave_async_net.v`と接続が一致することを確認してから、レイアウト
  作業のリファレンスとして使うことを推奨する。
- ネットリスト（`i2c_slave_async_net.v`）を将来再合成した場合は、
  `python3 script/gen_schematic.py`を再実行すれば`schematic/`以下の
  `.sch`を再生成できる（パスはこのリポジトリ構成前提でスクリプト内に
  ハードコードされているため、環境が変わった場合は`STDCELL_ABS_DIR`等の
  定数を書き換える必要がある）。

## 10. 配線済み回路図（`i2c_slave_async_net_routed.sch`）

§9のラベル方式に加え、実際に配線を引いた版を`script/gen_schematic_routed.py`
で追加生成した（`schematic/i2c_slave_async_net_routed.sch`）。VDD/GNDのみ
§9と同様ラベル方式のままとし（電源網は一般的なスキーマティックでも配線せず
ラベルで表現するのが通例のため）、信号ネットは実際にドライバピンから
各レシーバピンまで直交（L字/コの字）配線で結線している。

- **セル配置アルゴリズム**: ネットリスト中の接続関係からセル間の
  依存グラフを構築し、各セルに「論理段数（レベル）」を割り当てて
  左→右にレベル順で配置する簡易Sugiyama式階層レイアウトを実装した
  （列内の並び順は前後列との接続本数を見たbarycenter法で4回反復し、
  交差をある程度低減）。
  - 重要な設計判断: **`DFFR`の`Q`/`QB`出力は、下流の組合せ論理段数を
    数える際は常にレベル0（一次入力と同様の「新しい起点」）として扱う**。
    これを入れないと、`phase`/`bit_cnt`等の相互に依存し合うFSMレジスタ群
    を素朴に辿った際、実際には1クロックで並列に決まる論理が見かけ上
    最大49段もの直列チェーンに積み上がってしまう不具合が最初に発生した
    （レジスタの出力を「前クロックの結果」として扱わず、そのレジスタ自身の
    D入力側の論理段数をそのまま下流に伝播させてしまっていたのが原因）。
    修正後は最大論理段数9段（レベル1〜9に144セルが分布）という、この
    回路の規模として妥当な深さに収まった。
  - `u_lat_q`/`u_lat_qn`（busyラッチの交差結合NOR2）は`DFFR`を介さない
    真の組合せフィードバックループのため、レベル計算時にDFS的な
    サイクル検出で該当エッジをスキップして無限再帰を回避している
    （配線自体はスキップせず正しく結線される。レベル計算だけの特例）。
- **信号配線**: 各ネットについて、ドライバから各レシーバへ3線分（横→縦→横）
  の直交経路を生成。複数レシーバを持つ高ファンアウト信号（`scl`/`rst_n`/
  `phase[i]`等、最大20〜30本規模）は、経路の折れ曲がり位置をネットごとに
  少しずつずらして完全な重なりは避けているが、**144セル規模の平坦な
  ゲートレベル回路を1枚のシートに収めている以上、高ファンアウト信号の
  交差密集は避けられない**。実用的な"読める"回路図というよりは、
  「どのピンとどのピンが実際に線で結ばれているかを目で追える」ことを
  目的とした参考図として扱ってほしい。手作業レイアウトでは、design_notes.md
  §2/§3のブロック単位（非同期SR部／SCL posedge・negedgeドメイン）で
  領域を分けて進める方が実務的に見通しが良い。
- §9同様、xschem本体でのオープン確認・接続整合性の確認は**未実施**。
  まずお手元のxschemで開いてエラーが無いことを確認してほしい。
- 再生成: `python3 script/gen_schematic_routed.py`
  （こちらもパスがハードコードされているため、環境が変わった場合は
  スクリプト冒頭の定数を書き換えること）。

## 11. 重要な修正: ネットラベル方式の接続が実際には機能していなかった

§9/§10の初版は、配線されていないワイヤ（`N`行）に`{lab=NETNAME}`という
プロパティを直接付与すれば、同じ`lab`値を持つワイヤ同士が電気的に同一
ネットとして扱われる、という前提で実装していた。ユーザーが実際に
`i2c_slave_async_net.sch`（ラベル方式版）をxschemで開き、SPICEネット
リストを書き出して共有してくれたところ、この前提は**誤りだった**ことが
判明した。

- 検証: `script/verify_spice.py`で、xschemが書き出したSPICEネットリスト
  （各セルインスタンスの接続ピン→net番号）と、元の`i2c_slave_async_net.v`
  から機械的に導出した「本来同一ネットであるべきピンの組」を突き合わせた。
  結果、**820個の(インスタンス,ピン)組が820個全く別々のnet番号に
  分解されていた**（本来190個の電気的ネットにまとまるはずが、1個も
  マージされず、全ピンが孤立ノードになっていた）。
- 原因: xschemの接続認識は幾何学的な「ワイヤ／ピンが実際に接触しているか」
  のみに基づいており、ワイヤに付けた`lab=`プロパティ自体はネットリスタが
  読まない（公式マニュアル "XSCHEM ELEMENTS" 参照:
  "XSCHEM recognizes connection of wires and uses this information to
  build up the circuit connectivity"）。物理的に接続されていない配線同士を
  同名で電気的に短絡させる正式な機能は、専用コンポーネント
  **`devices/lab_pin.sym`**であり、これは「同じ`lab`値を持つ
  `lab_pin.sym`インスタンスは配置場所によらず全て電気的に短絡される」と
  公式マニュアル("COMPONENT INSTANTIATION")に明記されている。
  参考にしていた`TR-1um_5_stdcell`側の`.sch`ファイル（例: `NOR2.sch`）が
  たまたま`lab=`タグ付きワイヤ**かつ**物理的にも全て接続されていたため、
  「`lab=`だけで繋がる」という誤った推測をしてしまっていた。
- 修正: `gen_schematic.py`（ラベル方式）は全820ピンの接続を、
  `gen_schematic_routed.py`（配線方式）はVDD/GND（288ピン）の接続を、
  いずれもワイヤ端に`devices/lab_pin.sym`インスタンス（`lab=NETNAME`）を
  配置する方式に修正した。配線方式のSIGNAL配線（ドライバ→レシーバの
  実配線、ゲート-ゲート間の物理的に繋がったポリライン）はこの不具合の
  影響を受けていない（xschemの基本機能である「接触しているワイヤは
  繋がる」だけで正しく成立するため）。
- **確認済み**: 修正版`i2c_slave_async_net.sch`をユーザー環境のxschemで
  開き直してSPICEネットリストを再書き出ししてもらい、`script/verify_spice.py`
  で`i2c_slave_async_net.v`と突き合わせたところ、**820個の(インスタンス,
  ピン)組すべてについて期待される接続と完全に一致（mismatched groups: 0）**
  することを確認した。`lab_pin.sym`方式への修正で、ラベル方式回路図の
  接続がYosysが生成した元のネットリストと電気的に完全に等価であることが
  裏付けられた。配線方式（`i2c_slave_async_net_routed.sch`）のVDD/GND側
  修正は同じ仕組みを流用しているため妥当と考えられるが、そちらは個別には
  未検証（信号配線自体は最初から実配線のため元々問題なし）。
  このとき確認に使ったSPICEネットリストは`schematic/i2c_slave_async_net.spice`
  として保存されている（当初`src/schematic/`にネストして置かれていたが、
  `schematic/`直下に移動済み）。今後LVS用の正解ネットリストとして
  再利用できる。

## 12. 物理実装環境（KLayout配置配線フェーズ）

P&Rツールが無いため、KLayoutでの手動配置配線に移行する。使用する物理環境は
以下（`~/Dropbox/91_OpenPDK/TR-1um/libs.tech/klayout`、新たに接続）:

- **セルGDSライブラリ**: `libraries/TR-1um_STDCELL.gds` — 本設計が使う
  14セル型（INV_X1, DEL1, MUX2, NOR2/3/4, NAND2/3/4, AND2_X1, OR2/3/4,
  DFFR）を含む105セル収録の統合GDS。行高62.6umで統一、ピン位置は
  テキストラベル（GDSレイヤ48/0）で埋め込み済み。`FILL1`/`FILL1TAP`/
  `DECAP*`等のフィラー・デカップリングセルも収録されており、行の隙間
  埋めに使える。**`STDLIB/LogicCells/gds`の個別セルGDSではなく、こちらを
  正とする**（前者はRSラッチ等未完成セルを含む雑多な集合、こちらは
  正式なセルライブラリ）。
  - 参考寸法（回路合成結果144インスタンス基準）: 幅合計 約7435um
    （うちDFFR33個で約3320um、全体の45%）。10〜12行程度でコアが
    概ね680×690um角に収まる計算。
  - 電源ピンラベルはGDS上`vdd`/`gnd`と小文字（回路図/ネットリスト側は
    `VDD`/`GND`）。LVSツールの大文字小文字設定に注意。
- **DRCルールデック**: `tech/drc/run.drc`（`00_Layers.drc`/`01_Basics.drc`/
  `02_Device.drc`/`03_Electrical.drc`を読み込むKLayout Ruby DRCマクロ一式）。
- **LVSルールデック**: `tech/lvs/run.lvs`（`01_Extract.lvs`/`02_Extract.lvs`/
  `03_Combiner.lvs`/`04_Custom.lvs`/`05_Compare.lvs`）。LVSの正解
  ネットリストには、xschemで接続検証済みの
  `schematic/i2c_slave_async_net.spice`を使う想定。
- **テクノロジファイル**: `tech/TR-1um.lyt`（レイヤ定義） /
  `tech/TR-1um.lyp`（表示プロパティ）。

配置は§10で提案したブロック分け（非同期フロントエンド／SCL posedge
ドメイン／SCL negedgeドメイン）に沿って進め、DRC・LVSは144セル一括では
なくブロック・行単位でこまめに回すことを推奨する。

## 13. スタンダードセル配置（GDSII生成、`Layout/i2c_slave_async_layout.gds`）

`TR-1um_STDCELL.gds`の実寸法を使い、以下の制約で配置案を作成・GDSII化した:

- **配置領域**: 幅1800um固定、縦方向最小化。
  **[修正済み]** 当初`cell.bbox()`（全レイヤ込みのbbox）をセル幅・行高の
  基準にしていたが、これは誤り。実際のセル突き付け境界は**prBoundary
  レイヤ（235/0）**であり、`cell.bbox()`は左右に均等6.3um・上下に非対称
  1.3um/6.3umのガードバンド（隣接セル/行に重なる想定の余白）を含む。
  VDD/GNDピンラベルの座標がprBoundaryの上端(y=55.0)・下端(y=0.0)に厳密に
  一致することからも、prBoundaryが正しい基準と確認した。
  prBoundary基準では総セル幅（144インスタンス）は**約5621.0um**（旧計算
  より約1814um少ない）、1800um幅では**4行が最小**（3行では幅不足）。
  行高**55.0um**（prBoundary基準、INV_X1/NOR2/NAND2で共通）×4行＝
  **コア高さ220.0um**（旧: 313.0um/5行から縮小）。
- **ピン配置**: `scl`/`sda_in`/`sda_oe`/`rst_n`をブロック**左端**の境界ピン
  （高さ方向に等間隔）、`tx_data[7:0]`/`rx_data[7:0]`/`rx_valid`/
  `addr_match`/`rw`/`busy`/`VDD`/`GND`を**右端**に配置（ユーザー指示により
  当初案から左右反転）。
- **セルの行内配置順**: 各セルについて、上記の左端ポート群・右端ポート群
  それぞれからのインスタンス隣接グラフ上のBFS距離を計算し、
  `dist_left - dist_right`のスコアで並べ替え（負に大きい＝左寄り、
  正に大きい＝右寄り）。5行に均等（目標1487um/行）に振り分け。
  結果、行1・3にDFFRが集中（左のSCL/RST_Nに近いFF群）、行4にMUX2が集中
  （右のtx_data/rx_dataに近いデータ選択ロジック）という、意図通りの
  左右分布になった。ただしスコアの分解能は粗く（144セル規模のため
  グラフ直径が小さい）、行内の細かい並び順は叩き台に過ぎない。
- **行の反転（電源レール共有）**: 奇数行（行1・3）をX軸ミラー（`M0`変換）
  で上下反転し、隣接行の電源レールが同極性で向き合うようにした
  （偶数行の上端＝VDD、奇数行の下端＝VDD、で共有；奇数行の上端＝GND、
  偶数行の下端＝GND、で共有）。生成後のGDSを実際に読み戻し、各セルの
  `vdd`/`gnd`ピンラベル座標を検証——行境界（y=55, 110, 165, 220、
  prBoundary基準の55umピッチ）それぞれでVDD-VDD、GND-GNDが境界を挟んで
  対称位置に来ることを確認済み（`script/gen_gds_placement.py`のコメントに
  手法を記載）。
- **各行内は完全に隙間なく突き付け配置**（セル間ギャップ0）。
  行の右端には未使用ギャップが残る（行0: 463.5um, 行1: 397.5um,
  行2: 469.0um, 行3: 249.0um）。**フィラー/デカップリング
  セルは未挿入**——`TR-1um_STDCELL.gds`には`FILL1`/`FILL1TAP`/
  `FILL1TAPNM`/`DECAP1`/`DECAPx*`があるが全て幅29.1um固定の1種類のみで、
  上記ギャップは29.1umの整数倍にならない（例: 336.7/29.1=11.57）ため、
  端数が残る。密度ルール・ウェルタイ要件を満たすフィラー挿入は今後の
  課題とする。
- **付加情報（非量産レイヤ）**: GDSレイヤ48/0（`tech/TR-1um.lyp`定義では
  "TXM1"＝ライブラリ自体のピン名テキストレイヤ）に左右端の境界ピン
  マーカー＋テキストラベルを追加。レイヤ250/0に各インスタンスの元の名前
  （`_336_`等、`i2c_slave_async_net.v`/`schematic/i2c_slave_async_net.spice`
  と対応）を注記。レイヤ251/0にコア外形（0,0)-(1800,220)を記載。
  **[修正済み]** 当初はインスタンス名注記に199/0、コア外形に200/0を
  使っていたが、`tech/TR-1um.lyp`で199/0が実PDKレイヤ"COVER"に割り当て
  られていたため、250/0・251/0（未使用）に変更した。250/0と251/0は
  製造レイヤではないため、テープアウト前に削除/無視すること。
- **検証済み**: 生成後のGDSを読み戻し、(1) 配置インスタンス数144個・
  インスタンス名の集合が元netlistと完全一致、(2) 各行内でセル間の
  すき間・重なりがゼロ（正確に突き付け）、(3) 電源レール共有が意図通り
  であることを確認済み。**配線（M1/M2/poly）は含まれていない**——
  これは次工程（手動配線）で行う。
- 生成スクリプト: `script/plan_placement.py`（配置案の計算、
  `compute_rows()`。セル幅・行高はprBoundaryレイヤ基準）＋
  `script/gen_gds_placement.py`（GDSII生成、
  `python3 script/gen_gds_placement.py`で`Layout/i2c_slave_async_layout.gds`
  を再生成）。両方ともパスがハードコードされているため環境が変わった
  場合は書き換えが必要。

## 14. M1横配線エリアの概算

配線（M1/M2、必要に応じてpoly短接続）を入れる前に、必要なM1横方向配線
本数からチャネル面積の目安を見積もった（`script/estimate_m1.py`）。

- **前提モデル**: VDD/GND（各行専用レールに直結、配置済み）を除いた
  信号ネット（188本）について、行ごとに「その行内にピンを持つネット」の
  水平方向の広がり（そのネットが行内に持つインスタンスx座標の
  min〜max。行内ピンが1個だけのネットは幅ゼロのスタブ扱い）を求め、
  区間重なりの最大同時本数（＝古典的なチャネル配線密度の下界）を、
  その行に必要な最小M1トラック本数とみなした。**これは理想的な配線が
  前提の理論下界であり、実際の2層配線（ドッグレッグの自由度が乏しい）
  ではこれより多くのトラックが必要になる可能性が高い。**
- M1ピッチはDRCルール（`M1.W1`最小幅1.8um、`M1.S1`最小間隔1.4um）から
  3.2um/トラック、実装マージンを見込んだ目安として4.0um/トラックの
  2通りで換算。
- **結果**（行高は現行55.0um/行）:

  | 行 | ネット数 | 必要トラック数（下界） | 必要チャネル高さ（DRC最小/実用目安） |
  |---|---|---|---|
  | 0 | 73 | 27 | 86.4um / 108.0um |
  | 1 | 92 | 12 | 38.4um / 48.0um |
  | 2 | 59 | 7  | 22.4um / 28.0um |
  | 3 | 94 | 14 | 44.8um / 56.0um |
  | 合計 | — | — | 192.0um / 240.0um |

  現行コア高さ220.0um（55um×4行、行間ギャップ0）に対し、上記チャネル
  高さの合計（192〜240um）はほぼ同等〜それ以上——**現在の行間ギャップ
  ゼロの詰め配置のままでは、必要なM1横配線を収める余地がほぼ無い**こと
  を意味する。
- 行0が突出して大きい（27トラック）理由を調査したところ、単一の巨大
  ネットではなく、`bit_cnt[0:3]`・`phase[1:2]`・`scl`など、行の広い範囲
  に散らばった多数のネット（行0だけで最大1146.8umの広がりを持つネットが
  複数）が同時に重なっているため。これは現行配置（左右端ポートからの
  BFS距離のみでスコアリングし、行内の局所性は最適化していない）の
  副作用であり、**配置の局所性を改善すれば必要トラック数は相応に減る
  見込み**。
- 対応方針は今後の課題（ユーザー指示待ち）。候補: (a) 行内配置順を
  ネット局所性重視に改良、(b) 行間に実配線チャネル（ギャップ）を確保
  （現行の電源レール共有前提の詰め配置を見直す必要あり）、
  (c) 行数を増やして1行あたりの幅・ネット数を減らす、など。
- 生成スクリプト: `script/estimate_m1.py`（`python3 script/estimate_m1.py`
  で再計算。`plan_placement.py`の`compute_rows()`に依存）。

## 15. M1配線チャネル（FILLセル）の物理実装、M2配線領域の概算

§13の4行構成（論理行0=最下段〜論理行3=最上段）に対し、ユーザー指示により
以下のフロアプランを採用した（呼称は画面表示の見た目に合わせた
「1列目＝最上段（論理行3）／2列目＝論理行2／3列目＝論理行1／
4列目＝最下段（論理行0）」に対応）：

- **論理行0・論理行3（外側の行）**は、それぞれブロック外周に面しているため
  各自専用のM1チャネル（マージン）を確保。
- **論理行1・論理行2（内側の行）**は隣接する外側の行との境界を電源レール
  共有の詰め配置（ギャップ0）のまま維持し、内側同士の境界（論理行1と
  論理行2の間）だけを共有のM1チャネルとする。

§14の概算をこの3グループで再計算（実用ピッチ4.0um/トラック基準）:

| 配置 | 対象行 | ネット数 | 必要トラック数 | 必要高さ（実用目安） |
|---|---|---|---|---|
| 下マージン（4列目の下） | 論理行0 | 73 | 27 | 108.0um |
| 上マージン（1列目の上） | 論理行3 | 94 | 14 | 56.0um |
| 中央チャネル（2列目・3列目の間） | 論理行1+2（合算） | 132 | 29 | 116.0um |

FILLセルの高さは55.0um固定（prBoundary基準、標準セル行と同じ）のため、
各チャネルは55um単位に切り上げて**FILL1セルの行として実際に埋めた**:
下マージン=55um×2行=110um、上マージン=55um×2行=110um、
中央チャネル=55um×3行=165um（いずれも上表の実用目安を上回るマージンを
確保）。

**物理行スタック**（下から上、各55.0um、`script/gen_gds_placement.py`の
`PHYSICAL_ROWS`）:

```
filler x2 (下マージン, 110um)
論理行0
論理行1
filler x3 (中央チャネル, 165um)
論理行2
論理行3
filler x2 (上マージン, 110um)
```
合計11物理行 × 55.0um = **コア高さ605.0um**（§13時点の220.0umから拡大）。

電源レール共有のミラー反転は論理行番号ではなく**物理行インデックス
（0〜10）の奇偶**で決定するよう変更——これによりFILL行を挟んだ場合も
含め、全11境界でVDD-VDD/GND-GND共有が連続することを、生成後のGDS読み戻し
（vdd/gndピンラベルy座標: VDDが55,165,275,385,495,605、GNDが
0,110,220,330,440,550の交互配置）で確認済み。

FILLセルは`FILL1`（prBoundary幅16.5um）を使用。1800um幅に対し
1800/16.5=109.09のため1行あたり109個（1798.5um、端に1.5umの余り）を
配置。フィラー自体はダミー（ウェルタイ/密度ルール充足用）で信号配線は
持たないため、配線チャネル自体を塞ぐことはない想定。

**M2（縦方向トランク）配線領域の概算**: 上記3グループそれぞれについて、
「そのグループに1ピン以上を持つネット数」×M2ピッチ（DRC最小 3.0+2.0=
5.0um/トラック）を**全トランクが同時に別列を必要とする最悪ケース**として
算出し、各行の未使用幅（行内セル詰め配置後の右端の余り）と比較した:

| グループ | ネット数 | 最悪ケースM2幅 | 利用可能な行内余白 |
|---|---|---|---|
| 下マージン（論理行0） | 73 | 365.0um | 463.5um（余裕あり） |
| 中央チャネル（論理行1+2） | 132 | 660.0um | 866.5um（余裕あり） |
| 上マージン（論理行3） | 94 | 470.0um | 249.0um（**不足**） |

論理行3（1列目・最上段）だけは最悪ケース想定で行内余白を超過する。
ただしこの「最悪ケース」は全ネットが同時に専用の縦列を必要とすると
仮定した非常に保守的な上限（実際はM1側のピーク同時本数＝14本程度が
実質的な上限に近いと考えられる）であり、実配線時に個別確認が必要な
箇所として明記するに留める。

- **検証済み**: 生成後のGDSを読み戻し、(1) 総インスタンス数（本節時点で907、
  §16のフィラー分散後は998）、(2) 電源レール共有が全11物理行境界で
  連続していること、(3) コアbbox が (0,0)-(1800,605) であることを
  確認済み。**配線（M1/M2/poly）はまだ含まれていない**——引き続き
  次工程（手動配線）で行う。
- 生成スクリプト: `script/gen_gds_placement.py`（`PHYSICAL_ROWS`で
  チャネル位置・段数を定義。変更時は再実行で`Layout/i2c_slave_async_layout.gds`
  を再生成）、`script/estimate_m2.py`（M2概算、任意のグループ分けに対して
  再計算可能）。

## 16. 行内フィラーの分散配置（M2縦配線コリドーの分散）

§13〜15時点では、論理行内の未使用幅（行0: 463.5um・行1: 397.5um・
行2: 469.0um・行3: 249.0um）は右端に1個のまとまった空きとして残していた。
これだと右端に近いセルしかM2縦トランクへ短距離でアクセスできない
（左端のセルは行全体を横切ってM1で引き回す必要がある）ため、ユーザー
指示によりこの空きを**複数のコリドーに分割し、行内に分散配置**するよう
変更した（`script/gen_gds_placement.py`の`distribute_row_fillers()`）。

- 目安として配置済みセル幅200umあたり1コリドーを作成し、各コリドーを
  `FILL1`（幅16.5um）で埋める。ただし1コリドーあたりのフィラー本数が
  1本未満に細分化されて無駄が増えないよう、コリドー数は
  `未使用幅 // (2×フィラー幅)`を上限にクリップしている。
- 結果（分散後）:

  | 行 | コリドー数 | コリドーあたりFILL1数 | 埋めた幅 | 残り端数 |
  |---|---|---|---|---|
  | 0 | 7 | 4 | 1798.5um | 1.5um |
  | 1 | 7 | 3 | 1749.0um | 51.0um |
  | 2 | 7 | 4 | 1793.0um | 7.0um |
  | 3 | 7 | 2 | 1782.0um | 18.0um |

  端数（1.5〜51.0um）はコリドー未満の余りとして残るが、いずれも十分小さく
  実用上問題ない想定。
- 総インスタンス数は907→**998**に増加（論理行内の分散フィラー91個を追加）。
- **検証済み**: 生成後のGDSを読み戻し、電源レール共有が変更後も全11物理行
  境界で連続していること（VDDが55,165,275,385,495,605、GNDが
  0,110,220,330,440,550、§15と同一）を再確認済み。行内の突き付け配置
  （セル間ギャップ・重なりゼロ）もコリドー挿入後の並びで維持されている
  （カーソルベースの配置ロジック自体は変更していないため）。

## 17. ペア行間でのM2コリドーX位置整列

§16のコリドーは行ごとに独立して配置しており、電源レール共有ペア
（論理行0+1、論理行2+3）の間でX位置が揃っていなかった。ユーザー指示
により、**ペア内の2行で同じX位置にコリドーが来るよう**変更した
（`distribute_paired_row_fillers()`）。

- 手法: ペアの2行に対して共通のX閾値（等間隔）を設定し、両方の行を
  同時に左から走査——各閾値に達するまで実セルを両行それぞれ消費し、
  閾値ごとに**同数のFILL1**（コリドーサイズはペアの2行のうち小さい方の
  余白に合わせてクリップ）を両行に挿入する。コリドー本数・サイズは両行で
  完全に一致させたが、実セルの内容が行ごとに異なるため、閾値到達時に
  「消費中だった最後の1セル分」だけ両行でずれうる——**完全な1画素単位の
  一致ではなく、概ね数十um以内の一致**（下表参照）。余白が大きい方の行
  には、ペアでは使い切れない残り余白を末尾に非整列の追加コリドーとして
  埋めている。
- ミラー反転される行（論理行1・論理行3）は描画時に左右反転されるため、
  整列計算も「反転後の実際の描画順」に対して行うよう修正
  （`physical_order()`）——反転前の順序のまま計算すると、片方だけ左右反転
  されて整列が崩れるため。
- **結果（初版）**: ずれは概ね20〜80um程度だった。

### 17.1 追加のずれ補正（プレパディング）

ユーザーから「これ以上の調整は可能か」との確認があり、各X閾値で共有
コリドーを挿入する**前に**、遅れている方の行へ数個（最大3個、
`filler_w`未満に収まるまで）のFILL1を個別に追加してからコリドーを揃える
処理を追加した（"pre-pad"、`distribute_paired_row_fillers()`内）。

- 最初の実装では、このプレパディングが「その行の残り実セル幅」しか
  考慮しておらず、**まだ配置していない残りのコリドー分の予算**を
  考慮していなかったため、論理行3（ペアの中で余白が最も小さい行、
  249um）でプレパディングが後続コリドー用の予算を食いつぶし、
  行合計が1800umを超過するバグが発生した（1848.0um、要求仕様違反）。
  各行の合計幅が`ROW_WIDTH_UM`を超えないことを`assert`で検証する
  ガードを追加し、プレパディングの予算計算に「残り実セル幅＋残り
  コリドー予算」を含めるよう修正して解消した。
- **結果（プレパディング後、コリドー中心X、生成後のGDS読み戻しで確認）**:

  | ペア | 行 | コリドー中心X (um) |
  |---|---|---|
  | 論理行0+1 | 行0 | 261, 481, 718, 982, 1207, 1433, 1716 |
  | | 行1 | 256, 487, 707, 968, 1202, 1427, 1653, (1782=非整列の余り分) |
  | 論理行2+3 | 行2 | 280, 490, 715, 940, 1150, 1446, 1678 |
  | | 行3 | 280, 490, 737, 932, 1150, 1386, 1617 |

  ずれは論理行0+1ペアで概ね5〜15um（末尾のみ63um）、論理行2+3ペアで
  0〜60um程度に縮小。論理行2+3ペアの後半で改善が小さいのは、ペアの
  共有余白が論理行3（余白249um、7コリドー×2本=231um使用）でほぼ
  使い切られており、プレパディングに回せる予備がほとんど残っていない
  ため——**これは配置全体のトレードオフ（論理行3の余白そのものが
  タイト）に起因する限界であり、アルゴリズムだけでは今以上の改善は
  難しい**。さらに詰めるには、論理行3自体の余白を増やす（コリドー数を
  減らす、コリドー幅を絞る、または論理行3のセル配置自体を組み替える）
  必要がある。
- **検証済み**: 生成後のGDSを読み戻し、(1) 電源レール共有が全11物理行
  境界で連続（§16と同一のVDD/GND y座標）、(2) 総インスタンス数1002、
  (3) コアbboxが変わらず(0,0)-(1800,605)、(4) 全4論理行の合計幅が
  1800umを超えていないことを確認済み。

## 18. ファンアウト（FO）解析とバッファ挿入

配置が完了したことを受け、配線（M1/M2/poly）に入る前段階として、
ネットのファンアウトを調べ、高FOネットにバッファを挿入した。

### 18.1 RTLレベルの概算（`script/fanout_rtl.py`）

まず`src/i2c_slave_async.v`（ゲート合成前のRTL）に対し、各信号がモジュール
内で「読まれている」回数（インスタンス入力ピン・assign右辺・if/case条件・
always感度リストでの参照数）を数える簡易スクリプトを作成した。これは
実際のゲートレベルFOではなく、合成後の負荷本数の目安（プロキシ）に過ぎない
点に注意——実装時、除外リスト（exclude）の扱いに`bit_cnt <= bit_cnt + 1`
のような自己参照インクリメントの読み出しを誤って除外するバグがあり、
（scl=4, sda_in=5, bit_cnt=8, phase=3 の手計算による相互チェックで発見・
修正）。FO≥5はbit_cnt(FO=8)とsda_in(FO=5)の2つのみだった。

### 18.2 ゲートレベルの正式なFO解析（`script/fanout_net.py`）

続いて、実際に配置・検証に使っている合成後ネットリスト
`src/i2c_slave_async_net.v`に対し、正式なゲートレベルFOを算出する
スクリプトを作成した。Yosysの`assign a = b;`エイリアス（スカラー・
バス両対応）をUnion-Findで正規化した上で、各正準ネットが実際に駆動する
入力ピン数（VDD/GND除く）を数える——`verify_spice.py`/`plan_placement.py`
で確立済みの正規化ロジックを再利用。

設計全体でFO≥5のネットは12本:

| ネット | FO | 備考 |
|---|---|---|
| scl | 27 | プライマリ入力（SCLドメインのクロック） |
| _126_ | 24 | = rst_scl_domain駆動元（OR2 `_435_`.Y） |
| _070_ | 9 | MUX2セレクト線 |
| _073_ | 9 | MUX2セレクト線 |
| scl_n | 9 | u_inv_scl出力（SCL_nドメインのクロック） |
| _127_ | 9 | = rst_sdaoe_domain駆動元（NAND2 `_432_`.Y） |
| sda_in | 7 | プライマリ入力 |
| bit_cnt[0] | 6 | DFFR `_463_`.Q |
| bit_cnt[1] | 6 | DFFR `_464_`.Q |
| _046_ | 5 | 内部組み合わせ信号（フェーズデコード） |
| _055_ | 5 | 内部組み合わせ信号（フェーズデコード） |
| _058_ | 5 | 内部組み合わせ信号（フェーズデコード） |

RTLレベル概算のbit_cnt(FO=8)は、ゲートレベルでは4本の独立ネット
（bit_cnt[0]=6, [1]=6, [2]=3, [3]=4）に分かれることも確認済み
（`script/fanout_net.py "bit_cnt[0]" "bit_cnt[1]" "bit_cnt[2]" "bit_cnt[3]"`）。

### 18.3 バッファ挿入（`script/insert_buffers.py`）

ユーザー指示により、以下の4ネットにバッファを挿入した（他のFO≥5ネットは
そのまま）:

| 対象ネット | 方式 | 内容 |
|---|---|---|
| scl (FO=27) | 分岐 | 2分岐、各branchにBUF_X16を1個ずつ（`scl_buf0`/`scl_buf1`） |
| sda_in (FO=7) | 分岐 | 2分岐、各branchにBUF_X2を1個ずつ（`sda_in_buf0`/`sda_in_buf1`） |
| _126_ (FO=24) | 分岐 | 2分岐、各branchにBUF_X4を1個ずつ（`_126_buf0`/`_126_buf1`） |
| _127_ (FO=9) | 分岐 | 2分岐、各branchにBUF_X2を1個ずつ（`_127_buf0`/`_127_buf1`） |

**[修正]** scl/sda_inは当初、直列3段チェーン（BUF_X2→BUF_X8→BUF_X16）で
提案・実装したが、ユーザーからの再検討依頼を受けて全て分岐方式に変更した。
理由: チェーンは駆動力を1点で段階的に上げるだけで、全負荷は結局
最終段1個のバッファに残ったまま（そのバッファのFOは変わらない）。
一方、分岐は負荷をグループに分割し別々のバッファが駆動するため、
**バッファ1個あたりの実効FO・配線長そのものを下げられる**。scl・sda_in
とも配置済みフロアプラン全体（4行・605um高さ）に負荷が物理的に散らばって
おり、特にsclは事実上のクロック（DFFRのCKに直結）でスキューが問題に
なりうるため、チェーンよりも分岐が適切と判断した。
サイズ選定はscl（最大FO・最重要）に最大サイズのBUF_X16、sda_in（FO=7、
_127_のFO=9と同規模）に_127_と同じBUF_X2を割り当てている。

分岐後の実際のFO（`script/fanout_net.py`で生成後ネットリストを再検証）:

| 分岐ネット | FO | 元ネット |
|---|---|---|
| scl_buf0 | 14 | scl（27を2分岐） |
| scl_buf1 | 13 | scl（27を2分岐） |
| sda_in_buf0 | 4 | sda_in（7を2分岐） |
| sda_in_buf1 | 3 | sda_in（7を2分岐） |
| _126_buf0 | 12 | _126_（24を2分岐） |
| _126_buf1 | 12 | _126_（24を2分岐） |
| _127_buf0 | 5 | _127_（9を2分岐、5/4に分割） |
| _127_buf1 | 4 | _127_（9を2分岐、5/4に分割） |

分岐する負荷（DFFRの.CK/.RSTピンや組み合わせゲート入力）はインスタンス
出現順に交互（round-robin）で2グループに振り分けている——特定の物理的
近さ等は考慮していない簡易な均等分割で、フロアプラン確定後に近接性を
考慮した再分割の余地は残る。

- **実装方法**: 各対象ネットの正準形と全シンク（inst,pin）リストを
  `fanout_net.py`と同じUnion-Find解決で求め、シンクごとに該当インスタンス
  ブロックをテキスト上で特定し、該当ピンの接続のみを個別に書き換える
  （ブラインドな全文置換はしていない）。挿入したバッファインスタンスは
  `endmodule`直前にまとめて追加、新規ワイヤ宣言は既存ワイヤ宣言ブロックの
  末尾（最初のインスタンス行の直前）に追加。scl/sda_inをチェーンから
  分岐に変更した際は、`.bak`（バッファ挿入前のオリジナル）から復元して
  から全4ネット分を一括で再適用し、混在した中間状態が残らないようにした。
- **副作用**: 元のエイリアスネット（`_138_`=scl、`rst_scl_domain`=_126_等、
  すべての読み出し元がバッファ経由に付け替わったことで宣言はあるが
  読み出されない“死んだ”ワイヤになったものがある）はそのまま残置——
  シミュレーション上は無害（未使用ワイヤの警告が出る程度）。
- **検証済み（構造）**: 変更後のネットリストを`fanout_net.py`で再解析し、
  (1) scl/sda_in/_126_/_127_の元ネットのFOがいずれも2（新設した2分岐の
  バッファ入力のみ）に減ったこと、(2) 各バッファの出力ネットが元の
  シンク集合と完全に一致すること（負荷の付け替え漏れ・重複なし）、
  (3) インスタンス数が145→153（+8、4ネット×2分岐）に増えたことを
  確認済み。**機能シミュレーション（iverilog）による最終検証はユーザー側
  で実施**。
- 新規セル`BUF_X2`/`BUF_X4`/`BUF_X8`/`BUF_X16`の振る舞いモデルを
  `src/stdcell_behavioral_stubs.v`に追加（既存`BUF_X1`と同じ`#1`固定
  遅延のプレースホルダー——本ファイルの全セル共通の注意点として、
  SPICE特性抽出前の暫定値であることは変わらない）。
- 生成スクリプト: `script/fanout_rtl.py`（RTL概算）、
  `script/fanout_net.py`（ゲートレベル正式FO、引数なしでFO≥5一覧、
  ネット名を渡すとそのネットのシンク詳細を表示）、
  `script/insert_buffers.py`（バッファ挿入、
  `src/i2c_slave_async_net.v`を書き換え、`.bak`に元ファイルを保存）。

### 18.4 ネットリスト用テストベンチ（`src/i2c_slave_async_net_tb.v`）

ユーザーが`i2c_slave_async_net.v`を`i2c_slave_async_tb.v`（RTL用TB）と
組み合わせて実行したところ、
`parameter SLAVE_ADDR not found in i2c_slave_async_tb.dut`でエラーになった。
原因はYosys合成時に`parameter [6:0] SLAVE_ADDR`が定数畳み込みされ、
`i2c_slave_async_net.v`のモジュール宣言自体にはparameterが残らないため
（ネットリスト内でSLAVE_ADDRは既に7'h50として直接ハードコードされている）
——RTL用TBの`i2c_slave_async #(.SLAVE_ADDR(SLAVE_ADDR)) dut (...)`という
パラメータ上書き付きインスタンス化が、パラメータを持たないネットリスト
モジュールに対しては不正になる。合成後ネットリストがparameterを保持しない
のは合成一般の正常な挙動であり、今回のバッファ挿入による副作用ではない。

対策として`src/i2c_slave_async_net_tb.v`を新規作成した。内容は
`i2c_slave_async_tb.v`と同一（同じ3シナリオ）で、DUTインスタンス化から
`#(.SLAVE_ADDR(...))`のパラメータ上書きを削除しただけ（`localparam
SLAVE_ADDR = 7'h50`はネットリスト側のハードコード値と一致するため、
上書きを外しても等価）。

実行方法:
```
iverilog -o sim_net i2c_slave_async_net.v stdcell_behavioral_stubs.v i2c_slave_async_net_tb.v && vvp sim_net
```
サンドボックスにiverilogが無いため、この実行自体はユーザー側で行う。

## 19. ネットリスト版テストベンチのレースコンディション修正（scl/sda_in同時遷移によるグリッチ）

§18.4で作成した`i2c_slave_async_net_tb.v`を用い、バッファ挿入前のオリジナル
`i2c_slave_async_net.v`をiverilogシミュレーションしたところ、14項目中
**7項目がFAIL**した（addr_match/rw/rx_data/読み出しバイト関連、いずれも
`addr_ok`/`rw_bit`がリセット値0のまま更新されない症状）。

**根本原因**: 設計（RTL/ネットリスト）のロジックバグではなく、**テスト
ベンチの刺激パターンがゲートレベル特有の組み合わせハザードを誘発していた**
ことが原因だった。START/STOPパルス検出経路（`start_pulse`→
`rst_scl_domain`）は、Yosys合成後、sda_in経由（NOR2 `_434_`の1段で
`_148_`に到達）とscl経由（NAND2 `_433_`（`_105_`経由）→NOR2 `_434_`の
2段で`_148_`に到達）とで経路長が異なっていた。テストベンチの`send_bit`/
`read_ack`/`read_byte`/`send_ack`各タスクは`#T scl = 0; m_oe = ~bitval;`
のようにsclとm_oe（→sda_in）を**同一シミュレーション時刻に同時に**変化
させていたため、経路の短いsda_in側の効果が先に反映され、scl側（`_105_`
経由）の訂正が1ns遅れて追いつくまでの間、`_148_`/`rst_scl_domain`が
1ns幅だけ誤って"1"にグリッチする、という古典的なstatic hazardが発生
していた。このグリッチはDFFRの非同期リセット入力に対して有効なposedge
として認識され、`bit_cnt`/`phase`/`shreg`を毎ビット部分的にリセットして
しまうため、8ビット目で`bit_cnt==7`に到達できず、`addr_ok`/`rw_bit`の
更新イネーブル（`phase==PH_ADDR && bit_cnt==7`）が一度も成立しなかった。

RTL版（`i2c_slave_async_tb.v`によるシミュレーション）ではこの不具合は
現れない。RTLでは`start_pulse`が1本の連続代入で計算され、この経路長
ミスマッチによる中間段が存在しないため。すなわちゲートレベル合成後にしか
顕在化しないタイプの不具合であり、実機でもSCLとSDAが完全に同一タイミング
で遷移することは通常ない（配線遅延がある）ため、テストベンチの理想化
された刺激が生んだレースと考えられる。

デバッグには、サンドボックスにiverilogが無いため`stdcell_behavioral_stubs.v`
相当のセル遅延・機能を再現する独自のPython製ゲートレベル離散イベント
シミュレータを一時的に構築して解析に用いた（最終検証はユーザー環境の
実iverilogで実施）。また`dut.phase`/`dut.bit_cnt`/`dut.shreg`/
`dut.addr_ok`/`dut.rw_bit`を直接モニタする診断用テストベンチ
`src/i2c_slave_async_net_tb_debug.v`を作成し、実iverilogでの信号トレース
から`rst_scl_domain`のグリッチ（t=342ns付近、1ns幅の誤トリガ）を直接
確認した。

**修正**: `src/i2c_slave_async_net_tb.v`と`src/i2c_slave_async_tb.v`の
両方で、sclとm_oe（sda_in）が同時に変化する箇所全てに2nsのステージング
（scl変化→2ns待つ→m_oe変化、合計待ち時間は変更前と同じになるよう後続の
待ち時間を2ns短縮）を追加した：`send_bit`/`read_ack`/`read_byte`/
`send_ack`各タスク、およびSTOP準備コード3箇所（`m_oe = 1; scl = 1; #T;`
→ `scl = 1; #2 m_oe = 1; #(T-2);`）。設計ファイル（`i2c_slave_async.v`、
`i2c_slave_async_net.v`）自体は変更していない。あくまでテストベンチの
刺激タイミングの修正である。

**確認済み**: 修正後、ユーザーが実iverilogで再検証し、RTL版・ネットリスト版
（バッファ挿入前のオリジナル）とも**14項目全てPASS**したことを確認済み。

**今後の検証**: 次はバッファ挿入版（scl/sda_in/_126_/_127_にBUF_X2/X4/X16
等を分岐挿入したもの、§18.3・`script/insert_buffers.py`で生成）を再度
適用し、同じ修正済みテストベンチで検証する予定。

## 20. バッファ挿入版のxschem回路図再生成とSPICE接続検証

§18のバッファ挿入（scl/sda_in/_126_/_127_へのBUF_X16×2/BUF_X2×2/BUF_X4×2/
BUF_X2×2、計152インスタンスに増加）を受け、§9/§11で確立したラベル方式
（`devices/lab_pin.sym`）の回路図生成フローを、バッファ挿入後のネット
リストに対して再実行した。

- `script/gen_schematic.py`を再実行し、`src/i2c_slave_async_net.v`
  （152インスタンス: 元144 + 新規BUF_Xセル8個）から`schematic/i2c_slave_async_net.sch`
  を再生成・上書き保存した。BUF_X2/BUF_X4/BUF_X16のセルシンボルは
  `TR-1um_5_stdcell`ライブラリに既存のものをそのまま参照でき、追加の
  シンボル作成は不要だった。
- ユーザーがこの`.sch`をお手元のxschemで開き、エラーが無いことを確認した
  上でSPICEネットリストを書き出し、`schematic/i2c_slave_async_net.spice`
  を上書き保存した。
- `script/verify_spice.py`で、書き出されたSPICEネットリストと
  `i2c_slave_async_net.v`から機械的に導出した接続関係を突き合わせた結果:
  - ground-truth instances: 152、(inst,pin)ペア: 852
  - spice instance lines: 152、(inst,pin)ペア: 852
  - missing/extra keys: 0/0
  - expected distinct nets: 198、actual distinct nets: 198
  - mismatched groups: 0
- **結果: PERFECT MATCH**——152インスタンス・852ピン・198ネット全てに
  ついて、xschem回路図から書き出したSPICEネットリストがVerilogネット
  リストと電気的に完全に一致することを確認した。§11で導入した
  `lab_pin.sym`方式が、バッファ挿入によるインスタンス数増加後も引き続き
  正しく機能することが裏付けられた。

## 21. バッファ挿入版のKLayoutセル配置（GDSII）再生成

続けて、§13〜17で確立した配置フロー（`script/plan_placement.py`の
`compute_rows()` + `script/gen_gds_placement.py`、prBoundaryレイヤ基準の
実セル幅、4論理行構成、電源レール共有のための行ミラーリング、行内
フィラー分散配置、ペア行間でのM2コリドーX位置整列）を、コード変更なしで
バッファ挿入後のネットリストに対して再実行した。両スクリプトとも
ネットリストを都度パースする設計のため、インスタンス数のハードコードは
無く、152インスタンスへの増加はそのまま吸収された。

### 21.1 配置案の計算（`script/plan_placement.py`）

- 総セル幅: 5962.0um（バッファ挿入前は5621.0um。新規BUF_Xセル8個分、
  約341um増加）。
- 行数: 4行（1800um幅で従来通り最小）、行高55.0um、コア高さ220.0um
  （論理行のみの場合）。
- 各行のセル内訳（一部）: 行0に新規BUF_X2×2/BUF_X16×2、行2に新規
  BUF_X2×2/DEL1、行3に新規BUF_X4×2が配置された。左右配置スコア
  （BFS距離）のロジック自体は変更していないため、各バッファは元の
  ネット（scl/sda_in/_126_/_127_）の隣接セルの近くに自然に配置されて
  いる。

### 21.2 GDSII生成と検証（`script/gen_gds_placement.py`）

`Layout/i2c_slave_async_layout.gds`を再生成・上書きした。

- 物理11行構成（下から: フィラーmargin×2、論理行0、論理行1、共有
  チャネル用フィラー×3、論理行2、論理行3、フィラーmargin×2）は変更
  なし、コア高さ605.0um（§17時点と同一）。
- ペア行フィラー整列: 論理行0+1ペアは7コリドー×FILL1 2個ずつ（整列前
  ドリフト最大38.5um）、論理行2+3ペアは4コリドー×FILL1 2個ずつ（整列前
  ドリフト最大11.0um）。
- 総配置インスタンス数（実セル+フィラー）: 988個。

**検証済み**（生成後のGDS読み戻しによる確認）:
1. アノテーションレイヤ（250/0）から実セルインスタンス名を抽出し、
   ネットリストの152実インスタンスと完全一致（missing: 0, extra: 0）
   することを確認。
2. コア外形（レイヤ251/0）が(0,0)-(1800.0, 605.0)umであり、バッファ
   挿入前と同じ物理エンベロープを維持していることを確認。
3. 全prBoundary形状の合成bboxが(0,0)-(1798.5, 605.0)umとなり、コア
   全域（幅方向は1.5umの端数を除きほぼ全域）にわたって突き付け配置
   されていることを確認。
4. 各行の配置時、`gen_gds_placement.py`内の行幅超過アサーション
   （`assert final_a <= ROW_WIDTH_UM`等）がエラー無く通過しており、
   全11物理行が1800um幅を超過していないことを確認。

電源レール共有の仕組み（物理行インデックスの偶奇によるミラーリング）は
セル内容に依存しないロジックのため、§15〜17で確認済みの整合性がそのまま
維持されている（構造自体は変更していない）。

**結論**: バッファ挿入によるインスタンス数増加（144→152）は、既存の
配置フローに変更を加えることなく吸収でき、配置結果は構造的に健全
（実セル欠落・重複無し、コアエンベロープ不変、行オーバーフロー無し）。
配線（M1/M2/poly）は依然として未実施（次工程）。

## 22. 上マージンチャネル（論理行3）のパイロット配線

配線（M1/M2）工程に入る前に、3チャネル中もっとも自己完結度が高い
「論理行3の上マージンチャネル」1本だけを使って配線手法を検証する
パイロットを実施した（`script/route_channel_pilot.py`、新規）。対象は
論理行3の全40インスタンスのうち、ピンが全て論理行3内に閉じている
（他行へのM2引き回しが不要な）32ネットのみ。

### 22.1 配線方式

1. **M1トランク**: チャネル内に32ネット分の水平M1トランクを、区間
   スケジューリング法（左端でソートし、空いている最初のトラックに
   詰める古典的手法）でトラック割り当て。実際に使用したのは12トラック
   （チャネル高110umに対し最大16トラックの余地）。
2. **ピンからトランクへのエスケープ**: 論理行3内に埋もれたセルピンから
   M1で直接トランクまで引くと、電源レールや無関係な他セルのM1形状を
   横切ってしまう（実測: 幅違反26件・間隔違反61件）ため、実務の
   スタンダードセル配線と同じ「エスケープルーティング」を採用: ピンの
   M1 → V1 → M2（縦走、M1とは無干渉のためレール等を安全に横断） →
   V1 → トランクのM1、という構成にした。

### 22.2 発見した5つの実体的な問題と対策

パイロットの過程で、事前の想定が誤っていた実装上の問題を5つ発見し、
いずれも「その場しのぎの補正」ではなく根本原因を修正する形で解決した。

1. **論理行3の実配置はセルリストの単純な再計算とは一致しない**:
   `gen_gds_placement.py`は各行を`distribute_paired_row_fillers()`に
   通し、コリドー整列用のFILL1／`corrB3_*`セルを行の途中に挿入して
   いる（§16/17）。これを踏まえずに`plan_placement.compute_rows()`の
   生セルリストからカーソルX座標を再計算すると、フィラー挿入点以降の
   実セルすべてで絶対X座標がずれる。対策: 各インスタンスの実X座標を
   再計算せず、`gen_gds_placement.py`自身が描画したインスタンス名
   アノテーション（レイヤ250/0）から生成済みGDSを読み戻して直接取得
   する方式に変更（配置ロジックの独立再実装をやめ、正解を生成物から
   逆引きする方針に統一）。
2. **実ピンのM1形状は矩形とは限らない（櫛形・ノッチ形状）**: ライブラリ
   セルの一部ピン（特に内部信号ピン）は、隣接ピンとの間隔を詰めるため
   矩形ではなく屈曲した櫛形形状で描かれている。単純なバウンディング
   ボックス中心にビアを置くと、実際には金属が無い「切り欠き」部分に
   ビアが乗ってしまう。対策: ピンの実ポリゴンをKLayoutの`Region.sized()`
   による収縮演算で解析し、ビア＋V1包含マージン（1.7um）を安全に確保
   できる内部点を探索。収縮結果が空（＝ネイティブ形状に余裕がゼロ、
   実測ではほぼ全ての内部ピンがDRC最小値ちょうどに設計されていた）の
   場合のみ、実ポリゴン自体を最小限だけ膨張させて着地点を作る
   （バウンディングボックスへの一律パディングではなく、実形状の膨張＋
   その形状をそのままM1パッチとして描画）。
3. **チャネル自体もM1的に完全には空ではない**: チャネルを構成する
   FILL1フィラー行も、他の行と同様に自分自身の行境界にフル幅の電源
   レールを持つ。110umのチャネルはレール（チャネル中央、幅2.6um）で
   実質2分割されており、これを考慮せずトラックを敷くとレールを横切る
   トランクが生じる（実測: 間隔違反238件）。対策: レール帯を生成済み
   GDSから実測し、レールを避けた2つの区間（99.7〜145.3um、154.7〜
   200.3um、チャネル下端からの相対値）にのみトラックを配置。
4. **行内のM2も完全には空ではない**: 論理行3内のFILL1フィラー他が、
   独自のM2パターン（密度・タップ関連と推定）を持っていることが判明。
   これを無視してM2エスケープを直進させると、既存M2との間隔違反が
   発生する。対策: 生成済みGDSから既存M2形状を実測し、直進経路が
   干渉する場合のみ、ピン側のパッド高さでM2をジグザグに迂回させてから
   垂直走行に戻す（ピン・ビア位置自体は動かさず、M1側のDRC安全性を
   崩さない）。
5. **DRC間隔チェックだけでは短絡を検出できない**: 上記4を素朴に
   ネットごとに独立して解決すると、2つの異なるネットが「事前に存在した
   M2」は避けつつ、互いの新規M2同士は避けないまま重なってしまうケースが
   発生した。重なった形状同士は幾何的に「間隔ゼロ」ではなく「同一形状に
   併合」されるため、`space_check`は0件のまま短絡を見逃す
   （実際に7ペア・8ネットでこの短絡を検出）。対策(a): ネットを処理する
   たびに、そのネットが新規に描いたM2形状を「既存障害物」集合に追加し、
   後続ネットのジグザグ迂回判定に反映（早い者勝ちの逐次配線）。
   対策(b): 上記だけでは不十分なため、DRCとは別に、配線済みGDSの実際の
   M1/V1/M2形状からUnion-Findで連結成分を再構成し、ネットリスト上の
   ピン対応と突き合わせる独立した接続検証スクリプト
   （`verify_pilot_connectivity.py`、`/outputs`配下、新規）を作成し、
   本パイロットの最終検証として実行した。

### 22.3 最終結果

- **DRC**（`tech/drc/run.drc`の数値基準をKLayout Region APIで再現）:
  M1幅・間隔、M2幅・間隔、V1間隔、V1-M1包含、V1-M2包含のいずれも
  **0件**（全項目クリーン）。
- **接続検証**（`verify_pilot_connectivity.py`、DRCとは独立に、配線後
  GDSの実形状から再構成した接続関係とネットリストを突き合わせ）:
  32ネット・65ピン中、**24ネットは完全に接続され短絡も無いことを確認**。
  残り**8ネットで、同一スタンダードセルインスタンス内の隣接ピン
  （出力Yと入力A/B、物理距離7〜10um程度）同士のM2エスケープ経路が
  干渉し、ジグザグ迂回の探索範囲内（±100um）では解消できなかった**
  （該当: `_018_`/`rx_data_r[6]`、`_019_`/`rx_data_r[5]`、
  `_020_`/`rx_data_r[4]`、`addr_ok`/`_115_`、`tx_data[7]`/`_137_`、
  `_086_`/`tx_data[7]`、`_037_`/`rx_data_r[7]`、`_039_`/`addr_ok`の
  各ペア）。DRC自体はこれらのケースでもクリーンなままである点に注意
  （§22.2の5番目の問題そのもの — 見た目上のDRCクリーンだけでは
  この種の短絡は検出できない）。

**結論**: M1トランク＋M1→V1→M2→V1→M1エスケープルーティングという
配線方式そのものは妥当であることを、DRC・接続検証の両面から確認できた。
一方で、単純な「1ネットずつ独立にジグザグ回避」という戦略には、
同一セル内の隣接ピン同士が競合する場合に限界があることも分かった
（対策候補: 競合するピン同士を意図的に異なるトラックへ十分離して割り
当てる、あるいは片方を反対側のチャネルへ逃がすなど、ネット単独ではなく
ペア／グループ単位での配線順序・トラック割り当てが必要）。他の2
チャネル（下マージン＝論理行0、共有チャネル＝論理行1/2）および行を
跨ぐM2トランク配線へ本格展開する前に、この隣接ピン競合への対処方針を
決めるのが次のステップ。

### 22.4 実機KLayout DRC（`tech/drc/run.drc`本体）による追加検出とその修正

§22.3の「DRC 0件」は、`run.drc`本体（193ルール）ではなく、その一部を
KLayout Region APIで独自再現した`drc_check.py`（M1/M2幅・間隔、V1間隔、
V1-M1/M2包含の5項目のみ）による結果だった。ユーザーが実際にKLayout GUI
で`run.drc`本体を走らせ、その結果（`Layout/DRC_Error.lyrdb`）を提供して
くれたところ、**`V1.GC:V1-GC Smin < 1.2`（V1とゲートコンタクト層GC
[レイヤ8/1]の間隔が1.2um未満）が6件**検出された。該当は
`_137_`(`_345_.Y`)、`_075_`(`_395_.Y`)、`_076_`(`_400_.B`)、
`_082_`(`_404_.Y`)、`_086_`(`_410_.Y`)、`qn`(`u_lat_qn.Y`)の6ピン。

原因は`drc_check.py`がそもそもGCレイヤを一切検証対象にしていなかった
こと（自前チェックのカバレッジ不足であり、配線ロジック自体の欠陥では
ない）。ピンのビア着地点探索（§22.2の2番目の対策）はM1包含の安全性
のみを見ており、セル内部のゲートコンタクト形状との干渉は考慮して
いなかった。

**修正**: `abs_pin_anchor()`のピン安全領域計算に、GC形状をV1.GC
最小間隔（1.2um）分だけ膨張させた「立入禁止領域」を追加し、ビア中心が
そこに入らないよう収縮演算の対象に含めた（M1包含用の収縮結果から
GC立入禁止領域を差し引く）。ネイティブ形状に安全点が無い場合は既存の
成長ロジック（§22.2の2番目の対策と同じ仕組み）でそのまま吸収される
ため、新規のフォールバック機構は不要だった。修正後、該当6ピンの着地点
は0.7〜1.4um程度シフトし、実際のGC形状に対する間隔違反はKLayout Region
APIでの再検証で0件（`v1.separation_check(gc, 1.2um)` → 0件）。M1/M2幅・
間隔、V1間隔、V1-M1/M2包含も含め全項目クリーンな状態を維持しており、
§22.3で見つかった8件の隣接ピン短絡リスクにも変化はない（GC対応とは
独立した別の問題のため）。

**教訓**: 自前のDRC再現スクリプトは対象ルールを恣意的に選んでいるため
「クリーン」と言っても部分的な保証に過ぎない。実機の`run.drc`本体を
定期的に走らせて突き合わせることが、次の2チャネル・行間配線に進む前の
必須ステップ。

### 22.5 隣接ピン競合への対処（方向強制割り当て）と残存2件

§22.3で見つかった8件の隣接ピン短絡疑いに対し、以下3段階の対策を
`route_channel_pilot.py`に実装した。

1. **共有障害物検出＋グループ化**: 各ピンのM2エスケープ経路が塞がれる
   原因となる既存M2障害物（プレースドセル/フィラーの内部形状）を
   事前パスで検出し、同一障害物を共有するピン、同一インスタンス内で
   X距離20um以内のピン、インスタンスを問わずX20um・Y2um以内で近接する
   ピンをUnion-Findでグループ化。
2. **交互方向割り当て＋オフセットランク**: 各グループ内でX座標順に
   ソートし、左右(L/R)を交互に強制割り当て。同じ側に複数ピンが並ぶ
   場合は`forced_rank`により`_find_jog_x`側の検索開始オフセットを
   ずらし（`skip_rank`）、同一の最近接オフセットに収束しないようにした。
3. **処理順の優先制御**: 強制方向を持つピンを含むネットを先に処理し、
   その迂回経路が周辺の空きスペースを先に確保できるようにした。

**結果**: 8件 → **2件**まで削減（`addr_ok`/`_395_.A` vs `_115_`、
`_076_`/`_396_.Y` vs `_137_`）。DRCは全項目クリーンのまま。

**残り2件の根本原因**: `_find_jog_x`内の早期リターン
（「まっすぐ配線して既存形状に衝突しなければジグザグ不要」という
判定）が`preferred_dir`（強制方向）のチェックより先に評価されていた。
ネット処理はアルファベット順のため、強制ペアの片方（例:
`_404_.B`、net `_115_`）が先に処理される時点では、もう片方
（`_404_.A`、net `addr_ok`）はまだ描画されておらず「まっすぐでも
クリア」に見えてしまい、強制方向の割り当てが無視される。後から処理
される側が衝突を避けようとしても、周辺（行3のx≈1400〜1770の列）が
非常に混雑しており±100umの探索範囲内に空きが見つからず、結局両者
ともほぼ同じX帯にまっすぐ配線され、間隔ゼロ（＝形状がマージされる）
のためDRCには現れない短絡となる。

この早期リターンを「強制方向を持つピンはショートカット禁止」に修正
して再検証したところ、該当ペアの短絡は解消されたが、**同じ混雑
コリドー内で別の組み合わせが新たに衝突し、結果は2件→3件へ悪化**
した（モグラ叩き状態）。単純な貪欲法（各ピンが独立に最近接の空きを
探す方式）は、この密度のコリドーでは限界に達していると判断し、
この修正はrevertして2件残存の状態を維持することとした。

**判断**: ユーザーと協議の上、この2件は既知の制約として設計ノートに
記録し、パイロットの最終状態として受け入れる方針とした（実配線への
反映はまだ行っていないため、実害はない）。真の解消には貪欲法ではなく
多段階/迷路探索型のジョグ配置（優先度付きキュー、経路のバックトラック
などを伴う本格的なチャネルルータ相当のロジック）が必要と考えられる。
他の2チャネル（下マージン＝論理行0、共有チャネル＝論理行1/2）や
行を跨ぐM2トランク配線へ展開する際は、この制約を踏まえて（a）同一
コリドーへの配線密度を下げる配置上の工夫、または（b）将来的な迷路配線
アルゴリズムの導入を検討する。

### 22.6 対策候補としてのセルXミラー反転（試行・不採用）

残存2件のうち、`_400_`（NOR4、net `_137_`のA pin、経路探索が
±100um以内で失敗しフォールバックで直配線されているピン）について、
配置そのものをX方向にミラー反転すれば衝突を避けられないか試した。

**事前検討**: ライブラリGDS（`STDLIB/LogicCells/gds/NAND2.gds`等）を
確認したところ、NAND2のA/B、AND2_X1のA/B、NOR4のA/B・C/Dはいずれも
セル内でX座標が完全に同一（縦積み配線）だった。これは§22.3/22.5の
「同一インスタンス内ピン競合」（例: `_404_`のA/B）についてはXミラーで
解決しないことを意味する（ミラーは同座標のペアを丸ごと別位置へ
移動させるだけで、両者のX一致自体は解消されない）。一方`_400_.A`の
衝突相手は別インスタンス（`_396_`）の配線であり、たまたま近接した
X座標が原因なので、ミラーで`_400_.A`単独のXをずらせば効果がある
可能性があった。

**実装**: `gen_gds_placement.py`に`X_MIRROR_INSTANCES`集合を追加し、
指定インスタンスに対して既存の行Y反転（電源レール共有用、
`db.Trans.R0`/`M0`の切り替え）と独立にX反転を合成
（`R0→M90`、`M0→R180`）できるようにした。フットプリントの
X範囲（隣接セルとのアバットメント）は変えず、セル内部形状とピン
位置だけを反転する変換式を導出して適用。

**結果**: `_400_.A`は(1738.0, 467.5)→(1765.5, 467.5)へ移動したが、
再配線・DRC・接続検証を行ったところ **悪化**した。
DRCでM1間隔違反が新規に2件発生（ミラーされたNOR4の内部M1が
隣接セルとの境界で新たな間隔問題を起こした）、接続検証の短絡疑いも
2件→4件に増加（`_386_`/`_137_`、`tx_data[7]`/`_076_`、
`_410_`/`_073_`など、別の組み合わせで新規発生）。ミラーによる
ピン位置の移動が、たまたま今度は別の密集箇所に着地してしまった
ためと考えられる。

**判断**: この試行は不採用としてrevertした
（`gen_gds_placement.py`をgit checkoutで復元、配置GDSをバックアップ
から復元、パイロット配線を再実行してDRCクリーン・残存2件の状態に
戻したことを確認済み）。§22.5の結論のとおり、単純な貪欲法の限界に
起因する問題であり、個別セルのミラー反転のような局所的な対策では
モグラ叩きになりやすい。将来的な迷路探索型ジョグ配置の実装まで、
この2件は既知の制約として保留する。

### 22.7 同一X pinの強制ジョグ前処理（採用）

ユーザー提案: 「PINのXが同一の座標のときは、特別に前処理で座標を
ズラす処理を入れてから配線するのはどうか」。§22.6のミラー反転が
効果薄だった一方、これは§22.5で特定した根本原因（`_find_jog_x`の
早期リターンが`preferred_dir`より先に評価され、兄弟ピンがまだ
描画されていない処理順では強制分割が無視される）を、より狭い範囲
だけに絞って直接叩く方式であり、有望と判断し実装した。

**実装**: 全ピンをX座標で厳密突き合わせし、完全に同一のXを持つ
ペアのみ（このパイロットでは`_404_`・`_410_`・`_430_`の各A/B、
いずれもNAND2/NOR4系ゲートの縦積み入力ピンで、ライブラリ設計上X
座標が完全一致すると確定しているケース）を`must_jog`として
マーク。`_find_jog_x`に`must_jog`引数を追加し、該当ピンは
「まっすぐで既にクリア」の早期リターンを無条件にスキップして必ず
実際のジグザグ探索を行うようにした。§22.5で試みた「`preferred_dir`
を持つ全ピンに早期リターン禁止を適用」という広い修正は、密集コリ
ドー内で不要な迂回を増やし別の衝突を誘発して2件→3件に悪化させた
ため、今回は完全同一X（衝突が数学的に確実なケース）のみに範囲を
絞り込んだ。実際に対象となったのは3ペア6ピンのみ。

なお分離後の間隔が本当にDRC上安全かは、`_find_jog_x`が呼ぶ
`_box_clear`/`_m2_run_clear`が既存M2形状をM2最小間隔（2.0um）分
膨張させてから交差判定する既存の仕組みにそのまま乗っており、
先に処理された側の実際に描画済みの形状を見て後発側が十分な距離
まで探索を伸ばすため、狭すぎる分離（例: 両者オフセット2umずつで
合計4um、コア間隙0.9um未満）は自動的に棄却される。追加の間隔計算は
不要だった。

**結果**: `_404_.A`/`_404_.B`間の衝突（既知の残存2件の1つ、
`addr_ok`/`_115_`）は完全に解消（該当ペアの「ジョグ失敗」警告が
消失）。DRCは全項目クリーンのまま変化なし。ただし接続検証の
問題件数は**2件のまま変化せず**、内容だけ入れ替わった
（新: `addr_ok`/`_395_.A` vs `_137_`、`_076_`/`_396_.Y` vs
`addr_ok`）。原因は`_404_`まわりのM2クラッタが変化したことで
コリドー内の別の場所（`_429_.A`が新たに「ジョグ失敗」）で別の
偶然の近接衝突が発生したため——§22.5で指摘した「密集コリドーでは
一つ直すと別の場所で衝突が湧く」モグラ叩き構造が、ここでも確認
された形。

**判断**: 採用する。件数は変わらないが、`_404_`/`_410_`/`_430_`の
同一X衝突という「セル設計上必ず起きる」既知バグを、副作用なく
（DRC非劣化・他ネットへの影響なし）確実に修正できた点は明確な
改善であり、他チャネル展開時にも同一メカニズムの衝突を自動的に
防止できる。残る2件（コリドー混雑起因の偶然近接衝突）は§22.5と
同じ性質の未解決課題として引き続き保留する。

生成物: `Layout/i2c_slave_async_layout_routed_pilot.gds`
（配置済みGDSにパイロット配線のM1/V1/M2形状を追加したもの。本配線は
まだfab用レイアウトには未反映・未マージ）。

## 23. 残り2チャネル（下マージン＝論理行0、共有チャネル＝論理行1/2）の行内配線

§22で論理行3のパイロットが確立した手法を、残り2チャネルに展開した。
ユーザーとの合意により今回のスコープは**各行の「行内完結ネット」の
配線のみ**とし、行をまたぐネットやブロック境界I/Oピンへの配線・
複数チャネルの単一GDSへの統合は対象外（今後の課題）とした。

### 23.1 `route_channel_pilot.py`の一般化（`script/route_channel.py`）

論理行3専用にハードコードされていた部分をパラメータ化した
`route_row_channel()`関数を新設（`script/route_channel.py`）:

- `phys_row_index`（物理行インデックス）、`mirrored`（電源レール共有の
  ためのY反転有無）、`channel_bottom_y`/`channel_height`（チャネルの
  絶対Y範囲）、`escape_dir`（`'up'`=チャネルが行の上、`'down'`=行の下）
  を引数化。
- ピンの絶対座標変換（`abs_pin_anchor`相当）に、`mirrored=True`の場合の
  M0変換（ローカルY座標を反転してから配置原点を加算）を追加——論理行0・
  論理行3は非反転（`gen_gds_placement.py`のPHYSICAL_ROWS上、物理行
  インデックス2・8はどちらも偶数で非反転）だが、論理行1・2は反転
  （物理行インデックス3・7はどちらも奇数）のため、後続の共有チャネル
  対応に向けて必要な一般化。
- 障害物探索・ジグザグ探索の上下方向を`escape_dir`で切り替え
  （`_m2_run_clear`/`_box_clear`にY座標のmin/max正規化を追加し、
  どちら向きでも同じ関数で動くようにした）。M2コア/パッドの描画は
  元々`y0,y1=min/max`で書かれていたため無改造でどちら向きにも対応
  していた。

**回帰チェック**: `route_row_channel(**ROW3_PILOT)`で論理行3を
再実行し、既存パイロットと完全に同じ結果（DRC全項目クリーン、
接続検証の残存2件も内容含め同一）になることを確認してから、
論理行0の配線に着手した。

**一般化作業中に発見・修正したバグ**: 初期実装では「行内の既存M2
障害物」を初回スキャンする範囲（`_row_scan_box`、行自体＋チャネル）
と、各ネット描画後に障害物集合へ畳み込む際の再スキャン範囲
（誤って元のパイロット用の狭い`_m2_scan_box`＝チャネルのみを流用）
が不一致だった。この不一致により、1つ目のネットを描画した時点で
「行自体の既存M2障害物」情報が接続検証の障害物集合から脱落し、
2つ目以降のネットがそれを見落として衝突——結果、DRC M2間隔違反14件・
接続検証5件という明確な悪化として検出された。両方のスキャンに
同一の`_row_scan_box`を使うよう統一して解消、回帰チェックが
パスすることを確認済み。

### 23.2 論理行0（下マージンチャネル）配線

`route_row_channel(**ROW0_CHANNEL)`（物理行2、非反転、
`escape_dir='down'`、チャネルY=[0,110]）を実行。

- 行内完結ネット: 26本（構成48インスタンス中）
- 使用トラック: 11（利用可能16スロット中）
- DRC: 全項目クリーン（M1/M2幅・間隔、V1間隔、V1-M1/M2包含、
  V1-GC間隔いずれも0件）
- 接続検証: 26ネット・53ピン中、**3件**の短絡疑いを検出
  （`_100_`/`_426_.Y`↔`_113_`、`_101_`/`_427_.Y`↔`_093_`、
  `_107_`/`_438_.Y`↔`_106_`）。§22.5で確立済みの「密集コリドーでの
  偶然の近接衝突」と同じ性質の既知の制約——論理行0は`_425_`〜`_427_`
  にかけて8ピン連鎖のshared-obstacleグループが存在するなど、
  論理行3より配線密度の高い区間があり、残存件数もやや多い。

生成物: `Layout/i2c_slave_async_layout_routed_row0.gds`

### 23.3 論理行1/2（共有チャネル）配線

論理行1・2は互いの共有境界（物理行3・7、いずれも電源レール共有の
ため反転配置）だけがM1配線用に開放されており、チャネルを両側
（行1は上向き、行2は下向き）から使う点が論理行0・3と異なる。この
ため新たに`script/route_channel_shared.py`（`route_shared_channel()`）
を実装した。単一行版との違いは:

- **両行のピン座標計算を`mirrored=True`で実施**——route_channel.pyで
  追加したM0変換をそのまま利用。
- **トラック（Y方向スロット）割当を両行のネット全体で一括実施**——
  行ごとに独立してトラックを割り当てると、行1のネットと行2のネットが
  X方向で重なりつつ同じトラックYを取ってしまう可能性があり
  （§22.5と同種の「見えない短絡」を新たに生み得る）、これを避ける
  ため左端法によるトラック詰め込みを両行のネット区間を混ぜた単一の
  ソート済みリストに対して行う。
- **障害物探索の上下方向をピンごとに**行1は上向き・行2は下向きで
  個別に判定（`escape_dir`をピン単位のタプルとして保持）。
- shared-obstacle/same-instance/exact-Xの方向強制グルーピングは
  両行のピンを合わせた集合に対して行う（行をまたいだ偶然の近接は
  Y座標差が数百um単位になるため、既存のY<2um・X<20umのゲート条件で
  自然に行内のみにリンクが限定される——行1・行2間の誤リンクは
  発生しない設計）。

実行結果（物理行3=論理行1、物理行7=論理行2、チャネルY=[220,385]、
3フィラー行分165um）:

- 行内完結ネット: 論理行1=11本、論理行2=20本、計31本
- 使用トラック: 9（利用可能24スロット中）
- DRC: 全項目クリーン
- 接続検証: 31ネット・48ピン中、**4件**の短絡疑い
  （`_088_`/`_414_.Y`↔`_087_`、`_090_`/`_416_.Y`↔`_089_`、
  `_095_`/`_421_.Y`↔`_094_`、`_097_`/`_423_.Y`↔`_096_`）。
  同じく密集コリドー起因の既知の制約。

行1・2という物理的に大きく異なる配置（反転・両側エスケープ・トラック
共有）でも実装・DRCともに一発で破綻なく動作した点は、§22.1〜22.7で
確立した設計（GC回避付きビアアンカー探索、M2ジグザグエスケープ、
左端法トラック詰め込み、同一X/隣接ピンの方向強制）が論理行3固有の
偶然ではなく、この標準セルライブラリ・配置方式に対して概ね汎用的な
手法であることを裏付けている。

生成物: `Layout/i2c_slave_async_layout_routed_row12.gds`

### 23.4 3チャネル合計まとめ

| チャネル | 行内ネット数 | 使用トラック | DRC | 接続検証の残存短絡疑い |
|---|---|---|---|---|
| 上マージン（論理行3、§22） | 32 | 12/16 | クリーン | 2/65ピン |
| 下マージン（論理行0、§23.2） | 26 | 11/16 | クリーン | 3/53ピン |
| 共有チャネル（論理行1/2、§23.3） | 31 | 9/24 | クリーン | 4/48ピン |
| **合計** | **89** | — | **クリーン** | **9件** |

3チャネルとも実機DRC相当のチェック（M1/M2幅・間隔、V1間隔、V1-M1/M2
包含、V1-GC間隔）はゼロ件を達成した。一方、接続検証でのみ検出できる
「密集コリドーでの偶然の近接衝突」（§22.5で根本原因を特定した、
DRC上は間隔ゼロではなく形状マージのため検出不能なショート）は
3チャネル合計9件残存しており、単純な貪欲法ベースの方向強制では
解消しきれない既知の制約として引き続き保留する（真の解消には迷路
探索型のジョグ配置アルゴリズムが必要——§22.5参照）。

**今回のスコープ外（今後の課題）**:
- 行をまたぐネットの配線（論理行0↔1↔2↔3を接続するM2縦トランク）
- ブロック境界I/Oピン（scl/sda/tx_data/rx_data等）への配線
- 3チャネル＋パイロットの配線結果を単一の最終GDSへ統合
- 残存9件の短絡疑いの解消（迷路探索型アルゴリズムの検討）

### 23.5 行をまたぐネットの分析と行0↔行1境界配線の試作（否定的結果）

3チャネルの行内配線完了後、残るスコープ（行をまたぐネット、ブロック
I/O、単一GDSへの統合）のうち、行をまたぐネットの配線に着手した。

**分析**: ネットリスト全体を走査したところ、複数の論理行にピンを持つ
ネット（電源VDD/GND除く）は**107本**——3チャネル合計の行内ネット
（89本）より多い。内訳は隣接しない行同士（行0↔行2で30本、行1↔行3で
7本等）が大半を占めた。さらに構造上、論理行0/1と論理行2/3はそれぞれ
電源レール共有のためギャップ0で直接隣接しており、その境界には配線用
の空きチャネルが存在しない（論理行1/2の間だけが実チャネルを持つ）。
実際に使われているDRCデック（`tech/drc/run.drc`）にはM3層・M2-M3間
ビアの定義が無く、M1/M2/V1のみでの配線継続が前提となった。

**試作**（`script/route_cross_row.py`）: ギャップ0境界を跨ぐ配線として
「M1トランクの代わりにM2そのものを連続シェイプとして使う」新方式
（ピンごとにM2で境界近傍までエスケープし、水平のM2バーで合流、ビアは
ピン側1個のみ）を実装し、行0↔行1のみの10ネットで試験。

- 初回: トラック割当が単純な「最初に空いているスロット」方式だった
  ため、ピンによっては境界から50〜80um離れた、反対側の行の奥深くまで
  ジグザグ経路を強いられ、ジグザグ探索失敗15/28ピン
- トラックを境界に近い順に優先する方式へ修正 → 12/28ピン失敗（約
  43%）に改善したが依然高い
- ジグザグ失敗時に「まっすぐ配線してフォールバック」する従来の挙動は、
  空きチャネルと違い密なセル本体では失敗の連鎖汚染を招く（ある
  ネットの失敗フォールバック形状が後続ネットの探索を妨害）ことが
  判明。「未接続のまま開放（オープン）にする」方式に変更したが、
  それでも失敗率は同水準
- DRC: M2間隔違反2件。接続検証: 10ネット中10ネットに問題（意図した
  未接続によるNET SPLITに加え、**新規の電気的短絡が4件**）

**結論**: 行内チャネル配線で確立した「ピンごと独立ジグザグ＋トラック
詰め込み」方式は、空きチャネルが無い高密度領域を直接横切る配線には
不十分。この試作はコミットしていない（`Layout/`への出力ファイルは
スクラッチ領域のみ）。ユーザーと相談の上、根本原因（行をまたぐネット
がそもそも多すぎる＝配置起因）に対処する方針へ転換した（§24）。

生成物一覧（本セッション）: `script/route_channel.py`、
`script/route_channel_shared.py`、
`Layout/i2c_slave_async_layout_routed_row0.gds`、
`Layout/i2c_slave_async_layout_routed_row12.gds`。

## 24. 配置最適化によるクロス行ネット削減（FMハイパーグラフ分割）

§23.5の結果を受け、ユーザー提案により「配線アルゴリズムを強化する」
のではなく「そもそも行をまたぐネットの本数を配置の最適化目標にする」
方針へ転換した。物理構造（配線チャネル＋2段積み＋配線チャネル＋2段
積み＋配線チャネル、各行1800um幅、論理行0+1と論理行2+3はギャップ0の
電源共有ペア、論理行1/2間だけが実チャネル）は一切変更していない。

### 24.1 現状分析: 配置アルゴリズムの問題

`plan_placement.py`の`compute_rows()`は、全セルに「LEFT系ポートから
のBFS距離 − RIGHT系ポートからのBFS距離」という単一スコアを付け、
スコア順に1列へ並べて幅がtargetに達するごとに行0→1→2→3へ機械的に
輪切りにしていた。回路の接続関係（どのセルが同じ行にまとまるべきか）
を一切考慮しない、1次元順序の単純な等幅分割である。

この方式による行をまたぐネット数（電源除く、165ネット中）:

| 境界 | 本数 | 配線難易度 |
|---|---|---|
| 合計 | 108 | — |
| 行0↔行1（ギャップ0） | 44 | 困難（§23.5で失敗率43%を実測） |
| 行2↔行3（ギャップ0） | 23 | 困難 |
| 行1↔行2（実チャネルあり） | 19 | 容易（既存route_channel_shared.pyで対応可） |

### 24.2 FM法による階層的ハイパーグラフ分割

Fiduccia-Mattheyses法（バランス制約付き2分割ハイパーグラフ最小カット、
`_fm_bipartition()`として`plan_placement.py`に実装）を、物理構造に
合わせて階層的に適用する:

1. 全セルを「下半分＝{行0,行1}」vs「上半分＝{行2,行3}」に2分割
   （容量上限=2×1800um）——このカットは実チャネルを持つ行1↔行2
   境界に対応するため、多少カットが発生しても配線コストは低い。
2. 下半分をさらに行0 vs 行1に2分割（容量上限=1800um each）——
   このカットはギャップ0の困難な境界なので最小化を優先。
3. 上半分をさらに行2 vs 行3に2分割（同様に最小化を優先）。

各分割はゲイン最大のセルを貪欲に移動→ロック→パス内最良点まで
ロールフォワードする標準的なFMパスを最大20パス反復。行内の左右
配置順序は、行の割当とは独立に、従来通りのL/RスコアでBFS距離順に
ソートする（ブロック境界I/Oピンへの近さは維持）。

### 24.3 効果測定と実配置への適用

実際に`gen_gds_placement.py`を再実行して生成された配置GDS（アノテー
ション層からインスタンス→行の対応を読み戻して検証、乱数シードは
固定しているがPythonのハッシュ順序に依存する部分がありパスごとに
多少結果が変動する——後述）:

| 境界 | 本数 | 削減率 |
|---|---|---|
| 合計 | 29 | **-73%** |
| 行0↔行1 | 16 | -64% |
| 行2↔行3 | 6 | -74% |
| 行1↔行2 | 6 | — |

各行の実セル幅は行0=1309um、行1=1738um、行2=1749um、行3=1166um
（フィラーセルで1800umまで埋められる、既存の仕組みをそのまま利用）
——いずれも1800um上限に収まっている。

**注記（既知の非決定性）**: `_fm_bipartition()`内の集合反復順序が
Pythonの文字列ハッシュランダム化の影響を受けるため、乱数シードを
固定していても実行ごとに部分的に異なる分割結果になる（実測で合計
29〜34本の範囲で変動、いずれも良好な局所解）。再現性が必要な場合は
`PYTHONHASHSEED`を固定するか、集合をソート済みリストに置き換える
改修が今後の課題。

**判断**: 採用する。物理構造を変えずに、配線が困難な境界を跨ぐ
ネット数を67本→22本（-67%）まで削減できた。これにより§23.5で
確認された「密なセル本体を直接横切る配線」の必要量が大幅に減り、
残る少数のネットであれば§23.5の新方式（M2連続シェイプ＋境界近傍
トラック）でも現実的に対応できる可能性が高い。

**次のステップ**: 新しい配置に対して3チャネルの行内配線
（`route_channel.py`/`route_channel_shared.py`）を再実行し直す必要が
ある（配置が全面的に変わったため、`Layout/i2c_slave_async_layout_
routed_pilot.gds`等の既存ルーティング結果はこの新配置に対して
無効・再生成待ち）。その後、削減された行をまたぐネット（29〜34本
規模）に対して§23.5の新方式を再適用する。

生成物: `Layout/i2c_slave_async_layout.gds`（新配置で上書き済み。
配線は未実施の配置のみの状態）。`script/plan_placement.py`に
`_fm_bipartition()`と新しい`compute_rows()`ロジックを実装。

## 25. 決定性バグ修正・チャネル高さ拡大・配線再生成

### 25.1 非決定性バグの修正

§24.3で「既知の非決定性」として保留していた問題を修正した。
`_fm_bipartition()`内の`nodes = list(node_ids)`が、ハッシュランダム
化されたPythonの`set`をそのままイテレートしていたため、同じ
`compute_rows()`呼び出しでもプロセスを跨ぐと（`gen_gds_placement.py`
の内部呼び出しと`route_channel.py`が別プロセスで独自に呼ぶ場合など）
異なる分割結果になり得た。実際に`route_channel.py row3`実行時に
`RuntimeError: 53 row3 instances have no annotation match`として
顕在化。`nodes = sorted(node_ids)`に修正し、3回の独立呼び出しで
MD5ハッシュが完全一致すること、`gen_gds_placement.py`の再実行結果が
アノテーション層988インスタンス中0件の差分であることを確認した。
この修正に伴い配置を再生成（セルの並びは意味的に同一だが、
FMの内部タイブレークが変わったため一部インスタンスの行内順序が
変化している可能性がある——ユーザーには実施前に報告済み）。

### 25.2 チャネル容量不足とチャネル高さの拡大

修正後の配置で3チャネルの行内配線を実行したところ、行3チャネル
（上マージン、2フィラー行=110um=16トラック枠）が22トラック、
行1/2共有チャネル（3フィラー行=165um=24トラック枠）が46トラック
を必要とし、いずれも容量超過（`AssertionError`）。FM最適化が
狙い通り行内ネットを増やした（＝行をまたぐネットを減らした）直接の
副作用で、固定サイズのチャネルでは収まらなくなった。行0チャネル
（2フィラー行）は13/16で収まり変更不要。

対応方針をユーザーに確認し、「配線＋2段積み＋配線＋2段積み＋配線」
の物理構造・シーケンスは変えず、該当チャネルの高さ（フィラー行数）
のみを増やす方針を採用（`gen_gds_placement.py`の`PHYSICAL_ROWS`）。

トラック枠数はフィラー行1行あたり約8枠（レール帯の間隔55um、
実測: 2行→16枠）で線形にスケールすることを確認し、以下に変更:

| チャネル | フィラー行数 | 変更前 | 変更後 | 必要数 | 余裕 |
|---|---|---|---|---|---|
| 行3(上マージン) | 2→4 | 16枠 | 32枠 | 22 | 10 |
| 行1/2共有 | 3→7 | 24枠 | 56枠 | 46 | 10 |
| 行0(下マージン) | 2 | 16枠 | 16枠 | 13 | 3(変更なし) |

行1/2共有チャネルのフィラー行数は、物理行インデックスの偶奇
（`phys_idx % 2`でミラー方向が決まる）を保つため**奇数を維持**する
制約があり（3→7、+4は偶数増分なので条件を満たす）、行3側は
制約なし（後続の行が無いため任意の値で良い、2→4）。

この変更に伴いコア高さは605um→935um（+330um、約1.5倍）に増加。
行2の物理行インデックスは7→11、行3は8→12にシフト（ミラー方向は
両方とも変化なし＝偶奇保存を確認）。`route_channel.py`の
`ROW3_PILOT`と`route_channel_shared.py`の`ROW12_SHARED`の
`phys_row_index`/`channel_bottom_y`/`channel_height`を追従修正。

再生成後、3チャネルとも容量内に収まることを確認（行0:13/16、
行3:22/32、行1/2共有:46/56）。

### 25.3 ジョグ失敗時のフォールバック汚染バグ修正（route_cross_row.pyからの逆輸入）

チャネル高さ拡大後の初回配線・DRC結果は行1/2共有チャネルで
M2スペース違反70件、ショート疑い64件と大幅に悪化した（行0・行3は
従来通りDRC clean、残存ショートも6件・9件で変化なし）。

原因は§23.5で`route_cross_row.py`について特定済みだった「ジョグ
探索失敗時のフォールバック汚染」バグが、実は`route_channel.py`/
`route_channel_shared.py`本体にも同じ形で存在していたこと
（`top_x = x`として衝突が確定している未ジョグ形状をそのまま描画
→`_existing_m2`に取り込まれ後続ネットの探索も連鎖的に失敗）。
従来の行0・行3単体チャネル（ネット数20・36）では顕在化しにくかった
密度だが、行1/2共有チャネルがFM最適化でネット数31→109に急増した
ことで一気に表面化した。

`route_cross_row.py`で実証済みの修正（失敗ピンは未配線のまま残し
`continue`、その旨を`unrouted_pins`に記録）を両ファイルに逆輸入。
結果、ショート疑いは全て「ネット分断（オープン）」に変わり
（ショート→オープンは安全side、後から個別に手直しできる）、
M2スペース違反は70→4件、V1スペース違反は2→1件まで減少。

| チャネル | 配線ネット数 | 残存オープン | 残存M2/V1 DRC | 備考 |
|---|---|---|---|---|
| 行0 | 20 | 6 | 0/0 | DRC clean |
| 行3 | 36 | 9 | 0/0 | DRC clean |
| 行1/2共有 | 109 | 73 | 4/1 | 高密度による限界 |

行1/2共有チャネルのオープン73件（265ピン中27.5%）は、FM最適化で
意図的に集約した高密度トラフィックに対して現在の貪欲ジョグ
アルゴリズムの限界が露呈した結果。DRC違反4件は§22.5で既知とした
「密な回廊内での偶然の衝突」と同種で新規性なし。

### 25.4 現状のステータス

- 物理構造・配置は確定（DRC clean、決定性あり）。
- 3チャネルとも配線はDRC違反ほぼゼロ（行1/2共有のみM2×4/V1×1の
  既知パターン残存）で、危険な誤結線（ショート）は0件。
- 未配線（オープン）ピンが計88件（6+9+73）残っており、特に行1/2
  共有チャネルの73件は手直しまたはルーターの改良が必要。
- 行をまたぐネット配線（§23.5〜24の29〜34本）は未着手のまま。

## 26. ミラー配置廃止・5行チャネル方式への全面再設計

### 26.1 動機

§25までの構造（4論理行、行0↔行1・行2↔行3はゼロギャップの
ミラー隣接ペアで電源レールを共有、行1↔行2のみ実チャネル）では、
ゼロギャップ境界を跨ぐネットの配線が根本的に困難だった
（§22.5・§23.5: `route_cross_row.py`試作でピン失敗率43〜50%、
DRC違反、新規ショート）。FM配置最適化でその境界を跨ぐネット数
自体を減らす方向で対処してきたが（§24〜25）、ゼロギャップ境界を
跨ぐネットの配線自体は最後まで未解決のまま残っていた。

ユーザー指示により方針を転換: 「配線＋スタセル1段＋配線」を1単位
として縦に5単位積む構造に変更し、スタセルの上下ミラー配置を廃止。
全ての行境界を実チャネル（§22で実証済みの行1/2共有チャネルと同じ
方式）にすることで、ゼロギャップ境界そのものをなくす。ミラーは
電源レール共有のためだけに使われていたため、実チャネルが常に
存在するなら不要（全行R0/無ミラーで配置）。

### 26.2 新しい物理構造

```
N_TM  x フィラー行   -- 上マージンチャネル(行4専用)
論理行4
N_34  x フィラー行   -- 行3/行4共有チャネル
論理行3
N_23  x フィラー行   -- 行2/行3共有チャネル
論理行2
N_12  x フィラー行   -- 行1/行2共有チャネル
論理行1
N_01  x フィラー行   -- 行0/行1共有チャネル
論理行0
N_BM  x フィラー行   -- 下マージンチャネル(行0専用)
```

行数は4→5に変更（`plan_placement.py`の`NROWS`）。理由: 実チャネルを
両側に持たせる構造ではミラーによる面積節約がなくなるため、1行あたり
の目標幅に余裕を持たせたほうが配置・配線双方に有利（5621um総セル幅
÷5行=1124um/行、1800um上限に対して十分な余裕）。

各行は必ず2つの隣接チャネル（両端の行0・行4は片方が専用マージン
チャネル）に接し、ミラー方向はもう使わないため偶奇制約も消滅——
どのチャネルの高さも他のチャネルに影響せず独立に調整できる（§25で
あった「奇数を維持」制約はもう存在しない）。

### 26.3 配置アルゴリズム: 5行チェーン対応FM分割

`_fm_bipartition()`自体は変更なし。`compute_rows()`に`nrows==5`用の
分岐を追加し、物理的な行の並び（チェーン）に正確に対応する4回の
階層的2分割を行う:

1. 全体を{行0,行1} vs {行2,行3,行4}に分割 → 境界(行1|行2)に対応
2. {行0,行1}を行0 vs 行1に分割 → 境界(行0|行1)
3. {行2,行3,行4}を行2 vs {行3,行4}に分割 → 境界(行2|行3)
4. {行3,行4}を行3 vs 行4に分割 → 境界(行3|行4)

旧4行版は「ゼロギャップ境界(高コスト) vs 実チャネル境界(低コスト)」
という質的な違いがあったため片方を優先的に最小化する木構造だったが、
新版は全境界が等価な実チャネルなので、そのような優先順位付けは不要
——単純にチェーンの物理的な隣接関係と一致する木構造にするだけで、
各カットが対応する境界の交差ネット数の近似的な最小化になる（複数行
をスキップするネットは最上位でカットされた時点でしか数えられない、
という既知の近似——概要はコード内docstring参照）。

容量制約は当初row_width_um(1800um)基準で緩すぎて行1が0セルになる
という不具合が発生した（カット数最小化だけを目的関数にすると、
容量上限に余裕がある限りFMは片側を空にしてでもカットを減らそうと
する）。目標行幅（総セル幅/5行=1124um）基準にスラック1.28倍
（旧4行版で実際に機能していた比率）を掛けた容量に締め直して解消。

実行結果（真の最終境界交差数、階層カットの近似値ではなく実際の行
割当から算出）:

| 境界 | 交差ネット数 |
|---|---|
| 行0\|行1 | 7 |
| 行1\|行2 | 15 |
| 行2\|行3 | 19 |
| 行3\|行4 | 36 |
| 合計 | 77 |

各行の実利用率: 行0=19%, 行1=84%, 行2=61%, 行3=85%, 行4=83%
（行0が小さめに偏っているが、容量制約は満たしており機能上の問題は
ない——配線量最小化が主目的でバランスは副次的）。

### 26.4 チャネルルーターの一般化: ネット分類と実クロス行ネット配線

新設`route_all_channels.py`が全体を統括する。各ネットを、それが
接する行の集合で分類:

- **1行のみ（行内ネット）**: その行が接する2つのチャネルに交互に
  振り分け（同じネットを2チャネルで二重配線しないため）。
- **隣接2行に跨る（クロス行ネット）**: その2行が共有するチャネルへ
  割り当てる。**これは§23.5以来ずっと未解決だった「行をまたぐ
  ネットの実配線」が、実チャネルの存在によって初めて解けたケース**
  ——`route_channel_shared.py`のネット分類を「各行単独で完結する
  ネット」から「2行の和集合内で完結するネット」へ拡張し、各ピンを
  それが属する行自身のミラー/原点/エスケープ方向で個別にアンカー
  する（`inst_meta`辞書に統一）ことで、同じ枠組みで両方を扱えるよう
  一般化した。
- **非隣接または3行以上に跨る（マルチホップ）**: 現状未対応
  （後述）。

両ルーター関数に`allowed_nets`引数を追加し、`route_all_channels.py`
が計算したネット集合を明示的に渡すことで、二重配線を防止。

ネット分類結果: 行内148本、隣接クロス行33本、マルチホップ15本
（計196本、VDD/GND除く）。

### 26.5 チャネル高さの実測イテレーション

§25と同じ方法論（一様な初期値→実測→不足分を増量）を適用。
初期値（全チャネル3フィラー行=24枠）ではch3_4が32本要求に対し
24枠で不足。実測に基づき個別に調整:

| チャネル | フィラー行数 | 枠数 | 使用トラック数 | 利用率 |
|---|---|---|---|---|
| 下マージン(行0専用) | 1 | 8 | 1 | 13% |
| 行0/行1 | 2 | 16 | 7 | 44% |
| 行1/行2 | 3 | 24 | 14 | 58% |
| 行2/行3 | 4 | 32 | 22 | 69% |
| 行3/行4 | 5 | 40 | 32 | 80% |
| 上マージン(行4専用) | 2 | 16 | 10 | 63% |

コア高さ: 1210um（5行×55um + 17フィラー行×55um）。

### 26.6 DRC・接続性検証

| チャネル | 配線ネット数 | DRC(M2space/V1space) | オープン | ショート疑い |
|---|---|---|---|---|
| 下マージン | 2 | 0/0 | 0 | 0 |
| 行0/行1 | 20 | 0/0 | 1 | 0 |
| 行1/行2 | 33 | 0/0 | 2 | 0 |
| 行2/行3 | 36 | 0/0 | 2 | 0 |
| 行3/行4 | 68 | 3/0 | 19 | 1 |
| 上マージン | 22 | 0/0 | 2 | 0 |
| **合計** | **181** | **3/0** | **26** | **1** |

旧アーキテクチャ（§25.3修正後、行1/2共有チャネルのみで109ネット・
オープン73件・M2違反4件）と比較して、181ネット・オープン26件・
M2違反3件・ショート1件と、ネット数が増えたにもかかわらず絶対数・
比率とも大幅に改善——実チャネルによりクロス行ネットも同じ枠組みで
配線できるようになったことに加え、各チャネルの密度が旧版の
行1/2共有チャネル（109ネット集中）ほど偏らなくなったことが大きい。
残るショート1件・M2違反3件はch3_4（最多68ネット）に集中しており、
§22.5で既知とした「密な回廊内での偶然の衝突」と同種。

### 26.7 現状のステータスと残課題

- 物理構造・配置・6チャネル配線とも生成済み、DRCはほぼ完全クリーン
  （ショート1件のみ、既知パターン）。
- オープン26件は手直し対象（危険な誤結線ではない）。
- **マルチホップネット15本が未配線**（非隣接行または3行以上を跨ぐ
  高ファンアウト系: `scl`/`scl_n`関連バッファ、`addr_ok`、
  `shreg[0]`等）。複数チャネルを直列に通す配線方式の実装、または
  配置側でこれらのネットが跨る行数をさらに減らす追加最適化が今後の
  課題。
- 各チャネル出力GDSは個別ファイル（`i2c_slave_async_layout_routed_
  ch*.gds`）のままで、1つのGDSへの統合は引き続きスコープ外
  （将来のマージ作業として保留）。

### 26.8 統合レイアウトの生成と、チャネル間の見落とし衝突の修正

ユーザー要望により全チャネルを1つのGDSにまとめて出力することに
した。当初は各チャネルの出力GDS（いずれも同じ配置のみGDSを起点に
独立生成）から配線レイヤ（M1/V1/M2/ネット注釈）だけをトップセルに
直接コピーして単純合成する`merge_channels.py`を作成したが、統合後
にDRC・接続性を再検証したところ、M2スペース違反19件・ショート疑い
約52件と、個別チャネルごとの検証結果（M2違反3件、ショート疑い1件、
残りはオープン）よりはるかに悪化した。

原因: 各チャネルルーターは配置のみGDS（配線前の同一ベース）を
毎回独立に読み込み、自チャネル内の衝突だけを確認していたため、
同じ行を挟んで隣り合う2つのチャネル（例: 行2は`ch1_2`と`ch2_3`
の両方に接する）が互いの配線結果を一切見ずに配線していた。行2に
属する行内ネットは2チャネルに振り分けられ、クロス行ネットも両側の
チャネルへ別々に配線されるため、行の同じ領域近辺で2つの独立した
配線処理が互いに気づかずニアミス・接触することがあり、統合して
初めて顕在化した。

対策（`route_channel.py`/`route_channel_shared.py`に`in_gds`引数を
追加、`route_all_channels.py`で活用）: 6チャネルを物理的な並び順
（下マージン→行0/1→行1/2→行2/3→行3/4→上マージン）に**逐次**配線し、
各チャネルは直前のチャネルの出力GDS（＝それまでの全チャネルが
累積された状態）を入力として読み込む。こうすることで、後から
配線するチャネルは同じ行を共有する先行チャネルの配線済み形状を
障害物として正しく認識できる。最後のチャネル（上マージン）の出力が
そのまま全チャネル統合済みの最終レイアウトになる（別途マージ処理
不要）。

修正後の統合レイアウト（`i2c_slave_async_layout_routed_all.gds`）
検証結果:

| 指標 | 単純合成(誤り) | 逐次累積配線(修正後) |
|---|---|---|
| M2スペース違反 | 19件 | 5件 |
| ショート疑い | 約52件 | 1件 |
| ネット分断(オープン) | 少数 | 55件 |
| V1スペース違反 | 0件 | 0件 |

ショート疑い1件（net=`_003_`/`_059_`）は個別チャネル検証でも同一
箇所で検出されていた既知パターン（§22.5「密な回廊内での偶然の
衝突」）で、チャネル統合によって新たに生じたものではない。オープン
件数は26→55に増えているが、これは危険な誤結線を避けて安全側
（未配線）に倒した結果であり、劣化ではなく修正の直接的な帰結
——先行チャネルの形状が見えるようになったことで、以前は（他チャネル
の存在に気づかず）誤って成功していたジョグが、正しく「衝突するので
失敗」と判定されるようになったため。

生成物: `Layout/i2c_slave_async_layout_routed_all.gds`（6チャネル
統合済み最終レイアウト、181ネット配線、DRC違反5件・ショート疑い1件
のみ）。

## 27. 上下ミラー配置の復活（全物理行）

ユーザーがKLayoutで実際のDRCデック（`tech/drc/run.drc`）を実行し、
多数のDRCエラーを確認。§26で「電源レール共有はゼロギャップ境界
専用の仕組みで、実チャネル方式では不要」と誤って判断しミラー配置を
全廃していたが、これは誤りだった。標準セルの電源レール（VDD/GNDが
prBoundaryの上端/下端、y=55/y=0に配置）だけでなくNウェル/Pウェルの
バンディングも上下ミラー交互配置を前提にしており、ミラーなし
（全行R0）では行境界でウェル系のDRCルールに違反する
——本セッションで使ってきた簡易チェックスクリプト
（`drc_check_full.py`）はM1/M2/V1のみ検証しウェル/タップ層を
一切見ていなかったため、この種の違反を検出できていなかった。

このサンドボックス環境には実際のKLayout DRCデックを実行できる
Rubyマクロエンジンが無く（pip版`klayout`パッケージはC++コア
バインディング（db/tl/rdb等）のみでDRC DSLランタイムを含まない）、
`run.drc`をこちらで直接再実行して確認することはできない
——引き続きユーザー側のKLayoutでの確認に依存する。

対応: `gen_gds_placement.py`で、実セル行・フィラー行（チャネル）の
**両方**を含む全物理行に対し、物理行インデックスの偶奇による上下
ミラー交互配置（`phys_idx % 2 == 1`→M0、それ以外→R0）を復活。
5行+6チャネルという§26の物理構造自体は変更なし——ミラーの有無は
配置構造そのものとは独立な話だった。`route_all_channels.py`の
各チャネル呼び出しも、ハードコードしていた`mirrored=False`を
`phys_row_index % 2 == 1`から動的算出するよう修正。

配置・配線を再生成した結果、行内ネット/隣接クロス行ネットの分類
やチャネルごとの必要トラック数は完全に不変（行のX配置はY方向の
ミラーに依存しないため）——全チャネルとも同じトラック数で収まった。
統合レイアウトの簡易DRC・接続性検証結果:

| 指標 | ミラーなし(§26.8) | ミラー復活後 |
|---|---|---|
| M2スペース違反 | 5件 | 7件 |
| ショート疑い | 1件 | 0件 |
| ネット分断(オープン) | 55件 | 61件 |

M1/M2/V1観点では大きな悪化はない（今回の主目的はウェル系DRCの
解消であり、それは簡易スクリプトでは検証できない）。ユーザーには
実際のKLayoutでの再確認を依頼する。

## 28. 実DRC結果に基づく残課題の解消

ユーザーが実際のKLayout DRC（`tech/drc/run.drc`使用、TR-1um
プロセスの完全なルールセット）を`i2c_slave_async_layout_routed_
all.gds`に対して実行し、結果（`DRC_Error.lyrdb`）を共有。実エラーは
15件で、内訳はM2最小間隔違反（`M2.S1:M2 Smin < 2.0`）7件——本
セッションの簡易チェックスクリプトでも同数検出していた既知パター
ン——と、V1がCO（コンタクト、layer 11/0、poly/diffusion-M1間の
コンタクト層）に重なる違反（`V1.CO:V1 overlap CO`）8件——こちらは
簡易スクリプトがCO層を一切見ていなかったため完全に見逃していた
新種。ユーザーの指示（修正する）を受けて両方に対応した。

### 28.1 V1-CO重なりの修正

Via anchor探索（`abs_pin_anchor()`）は既にGC層（ゲートコンタクト
キープアウト）を回避する仕組みを持っていたが、CO層は対象外
だった。同じ仕組みをCO層にも適用（`cell_co_forbidden()`を追加し、
via半径+1.0um分だけ`sized()`で膨らませた禁止領域をGC禁止領域と
合成、安全なpin anchor候補から差し引く）。修正後、統合レイアウトで
V1-CO重なり・1.0um未満の分離とも0件を確認（自前のRegion演算による
検証、実DRCデックでの再確認はユーザー側に依頼）。

### 28.2 M2最小間隔違反の削減

残る7件のうち何が原因か調査するため座標を`pin_map`と突き合わせた
ところ、2つの異なる原因が判明した:

1. **同一ネット内の兄弟ピン同士の衝突**: `_find_jog_x()`によるジョグ
   探索は、ネット全体のピン位置を先に確定してから一括描画していた
   ため、同じネットの2本目以降のピンが1本目のピン（まだ描画されて
   おらず`_existing_m2`に未反映）と衝突しても検出できなかった。
   ピンごとに配線・`_existing_m2`更新を即座に行う（ネット単位では
   なくピン単位でインターリーブする）よう`route_channel.py`/
   `route_channel_shared.py`を修正。
2. **同一インスタンスの異なるピンが別チャネルに分かれる**: 1行の
   行内ネットをその行が接する2つのチャネルへ交互に振り分ける方式
   （§26.4）が、同じセルインスタンスの異なるピン（別々のネットに
   属する）を別々のチャネルへ送ってしまうことがあり、2つのチャネル
   は互いに配線結果を見ないため（逐次累積はチャネル間のみで、同じ
   行内の別ピン同士は元々想定外）、物理的に数um しか離れていない
   2本のM2形状が衝突することがあった。`route_all_channels.py`の
   振り分けロジックを、ネット単位の単純な交互割当から「同じ
   インスタンスに触れるネットをUnion-Findでグループ化してから
   グループ単位で交互割当」に変更し、同一セルの複数ピンが常に同じ
   チャネルに収まるようにした。

この変更でチャネルごとのネット数バランスが変化した（例:
`ch0_1`が20→34ネット、`ch4_tm`が22→4ネットなど）ため、チャネル
高さを再調整（`ch0_1`を2→4フィラー行、ミラー偶奇を保つため偶数
増分）。

結果、統合レイアウトの簡易DRC・接続性検証:

| 指標 | §27修正直後 | CO修正後 | 同一インスタンス
グループ化後 |
|---|---|---|---|
| M2スペース違反 | 7件 | 8件 | **3件** |
| V1-CO違反 | (未検証) | 0件 | 0件 |
| ショート疑い | 0件 | 0件 | 0件 |

残る3件のM2違反は上記2つの原因のいずれにも当てはまらない別の
密な回廊での偶然衝突で、§22.5以来繰り返し観測されている既知
パターンの範囲内と判断し、これ以上の追い込みは行わない
（181ネット・2897形状規模に対して3件は十分小さい）。

生成物: `Layout/i2c_slave_async_layout_routed_all.gds`（181ネット
配線、簡易DRC違反3件・ショート疑い0件・V1-CO違反0件）。ユーザーの
実KLayoutでの最終確認待ち。

## 29. 未配線（オープン）解消の試行と根本原因の特定

「未配線を潰します」の指示を受け、残課題（15件のマルチホップ
ネット、67ネット・88ピンのオープン）のうち、まず単一チャネル内で
発生しているピン単位のオープン（88件）に着手した。

### 29.1 パラメータ調整では効果なしを確認

まず、既存の単一ベンド（L字）ジョグ探索アルゴリズムのパラメータを
2方向で緩めて効果を確認した。

- ジョグ探索の最大振れ幅`mags`を100umから800umへ拡大
- 最も密なチャネル（`ch2_3`, `ch3_4`）のフィラー行数を増やしトラック
  スロット数・クリアYスパンを大幅に拡大

いずれも、オープンになる88ピンのリストは1件も変化しなかった
（拡大前後で完全に同一の88ピン）。これは、これらの失敗がトラック
容量や探索範囲の不足によるものではないことを示している。フィラー行
増加は面積コストのみが増えて得るものがなかったため、`N_23=4,
N_34=5`（既知良好値）に戻した。

### 29.2 2ベンド（Zシェイプ）ジョグフォールバックの実装

単一ベンドの限界（ピン→トラックの垂直区間全体が、どのXにジョグ
しても経路上のどこかで衝突する）を回避するため、`route_channel.py`
/`route_channel_shared.py`の両方に2段ベンドのフォールバック経路を
実装した。

- `_find_jog_x(vx, y_center, track_y, ...)`を`_find_jog_x_range(vx,
  y0, y1, ...)`に一般化（任意の(y0,y1)区間に対して単一ベンド探索を
  行えるように）
- `_find_jog_path(...)`を新設。まず通常の単一ベンド経路を試し、
  失敗した場合は中間Y（ピン→トラック間の0.25/0.4/0.5/0.6/0.75の
  各分割点）を5通り試し、各分割点で「ピン→中間点」の短距離
  ローカル退避ジョグ（最大24um、後に100umまで拡大して再検証）→
  「中間点→トラック」のフルレンジジョグ、の2段構えで経路を探索
- ピン単位描画ループを、単一の`top_x`前提から任意長のウェイポイント
  列（`path`）をたどってM2セグメント＋中間ベンドパッドを順に描画
  する方式に書き換え

### 29.3 検証結果: オープン件数は不変（88件のまま）

配置再生成→全チャネル再配線を実行した結果、Zシェイプ経路が実際に
使われた件数は**0件**、オープン件数も**88件のまま変化なし**
だった。簡易DRC（M2スペース3件、V1-CO 0件、ショート疑い0件）は
§28時点と同一で、Zシェイプ配線の追加による新規違反もなかった
（退行なし）。

### 29.4 根本原因の再診断: ジョグ経路の問題ではなくピン自身の
アンカー位置の衝突

なぜZシェイプでも1件も救えなかったのかを、実際に失敗している
ピンの座標と既存M2形状を直接調べて特定した。

例: `ch4_tm`チャネルで失敗した`_388_.Y`ピン（x=1457.5,
y=1182.5）を調査すると、そのピンの実via anchor位置から半径3um以内
に、**他のネットが既に描画したM2形状**（x=[1448.95,1470.05],
y=[1180.80,1184.20]という横長のジョグボックス）が存在し、
ピン自身のvia/pad設置に必要なクリアランスの時点で既に衝突して
いた。つまりこのピンは、

- 単一ベンド探索: 直進区間も横移動区間も「すでにその場に別ネットの
  配線がある」ために全方向で失敗
- Zシェイプのローカル退避（ピン位置から上方向へ数um動くだけの
  区間）: そもそも出発点であるピン自身の位置が既に塞がれている
  ため、退避の起点そのものが成立しない

という状態であり、**ジョグ経路の形状をどれだけ賢くしても解決
しない**種類の失敗である。

88件全件についてこの仮説を機械的に検証したところ（最終統合GDS上で
各失敗ピンの座標±1.7um以内に既存M2形状があるかを判定）、

| 分類 | 件数 |
|---|---|
| ピン自身のアンカー位置が既存M2と衝突 | **70件 (80%)** |
| 衝突なし（別要因、未特定） | 18件 |

という結果になった。すなわち88件の大半（8割）は「経路探索の
限界」ではなく、**ネット処理順序に起因する資源競合**——先に処理
された別ネットのジョグ配線が、まだ処理されていない後続ネットの
ピン自身の設置場所を塞いでしまう——という、これまでのジョグ
探索改善（探索範囲拡大、チャネル拡大、多段ベンド）のいずれでも
原理的に対処できない問題だった。

### 29.5 今後の対応方針（未着手）

この問題を正面から解決するには、以下のいずれか（またはその組合せ）
が必要で、いずれも今回のジョグ探索改善より侵襲的な変更になる。

1. **動的障害物を考慮したvia anchor配置**: `abs_pin_anchor()`は
   現在、セルライブラリ由来の静的なGC/CO禁止領域のみを避けて
   pin中心を選んでいるが、実行時に`_existing_m2`（他ネットが
   既に描画した配線）も考慮し、衝突する場合はピン自身のポリゴン内
   で別の安全な位置にオフセットする、または先着ネット側の配線を
   ピンの直近を避けるよう調整する
2. **ネット処理順序の見直し**: 現在は`net_priority`（強制方向
   グループを優先）→ネット名のソート順で処理しているが、空間的に
   競合するネット同士（同じX帯域・Y帯域に密集するピン群）を
   検出し、"場所を塞ぎやすい"ネットを後回しにする、または近接
   ピン同士をまとめて同時に解決する
3. 上記いずれも実装コストと再検証コストが大きく、既存の3件のM2
   違反同様「既知の残課題」として一旦許容し、必要な67ネット分は
   手動配線（またはPRの後工程）に委ねる、という判断も選択肢

Zシェイプ実装自体は退行を生んでいない（簡易DRC・接続性は§28
時点と同一）ため、コードは維持し、将来的により大きなチャネル高さ
や異なる密度の設計で単一ベンドが真に不足する局面が来た場合には
引き続き有効に働く可能性がある。

生成物: `Layout/i2c_slave_async_layout_routed_all.gds`（更新版、
181ネット中166ネット完全配線・88ピン/一部15マルチホップネットが
オープン、簡易DRC違反3件・ショート疑い0件・V1-CO違反0件、§28時点
から数値上の変化なし）。

## 30. チャネル負荷の偏り診断とネット数ベースの貪欲バランス化

ユーザーから「トップ配線チャネル（`ch4_tm`）が使われない理由は？」と
質問を受け調査したところ、§28.2で導入した「同一インスタンスに触れる
ネットをUnion-Findでグループ化してから、行が接する2つの候補チャネルへ
グループ単位で交互（round-robin）に割り当てる」ロジック
（`route_all_channels.py`）に見落としがあることが判明した。

調査結果: 論理行4は行内完結ネットを45本持つが、これらをUnion-Find
グループ化すると、同一インスタンスを介した推移的な結合（ネットAと
Bが同じセルを共有→BとCも別のセルを共有→…という連鎖）により
**わずか5グループに収束**し、うち1グループだけで37本（全体の82%）を
占めていた（残り4グループ: 3, 3, 1, 1本）。グループを**個数**で
交互割当していたため、たまたま37本の巨大グループが偶数番目
（`ch3_4`側）に入り、`ch4_tm`には残りの小さいグループ合計4本しか
回らなかった（「ネット単位で半分ずつ」のつもりが「グループ単位で
半分ずつ」になっており、グループサイズが極端に偏ると配分も偏る）。

### 30.1 修正: ネット数ベースの貪欲バランス

`route_all_channels.py`の割当ロジックを、グループをネット数の
多い順にソートし、その時点でネット数が少ない方の候補チャネルへ
逐次割り当てる貪欲法に変更した（`cand_load`で各候補チャネルの
累計ネット数を追跡）。

### 30.2 結果: チャネル利用は改善、未配線数は不変

配置・全チャネル再配線を実行した結果:

| 指標 | 変更前(§29時点) | 変更後 |
|---|---|---|
| `ch4_tm`割当ネット数 | 4 | 8 |
| `ch3_4`割当ネット数 | 67 | 63 |
| 未配線ピン | 88件 | 89件 |
| M2スペース違反 | 3件 | 4件 |
| V1-CO違反 | 0件 | 0件 |
| ショート疑い | 0件 | 0件 |

`ch4_tm`の利用ネット数は倍増し、最も混雑していた`ch3_4`の負荷は
多少減ったが、未配線ピン数・M2違反数はほぼ横ばい（誤差レベルの
微増）だった。これは§29.4の診断（未配線の主因はチャネル容量では
なく、ピン自身のvia anchor設置位置が先に処理された別ネットの配線と
衝突する、ネット処理順序に起因する資源競合）と整合する結果であり、
チャネル間の負荷バランスを改善しても、この種の衝突自体は解消
しないことが再確認された。

ユーザー判断により、このネット数ベース貪欲バランスは**そのまま
採用**（今後`ch4_tm`のような余剰容量チャネルをより有効活用できる
ため、それ自体に価値がある）。真の未配線解消には引き続き§29.5の
対応（動的障害物を考慮したvia anchor配置、またはネット処理順序の
見直し）が必要で、未着手のまま。

生成物: `Layout/i2c_slave_async_layout_routed_all.gds`（更新版、
181ネット中165ネット完全配線・89ピン/15マルチホップネットが
オープン、簡易DRC違反4件・ショート疑い0件・V1-CO違反0件）。

## 31. 2パス方式のvia anchor予約によるM2違反の完全解消

§29.5で挙げた対策候補のうち「動的障害物を考慮したvia anchor配置」を、
「予約→配線」の2パス方式として実装した。

### 31.1 実装

`route_channel.py`/`route_channel_shared.py`両方で、各チャネルの
配線対象ネット（`net_stub_pts`/`net_pin_names`確定直後、実際の配線
開始前）について、**全ピンの via/pad アンカー位置に`M2_PAD_SIZE`の
小さな占有ボックスを先に一括で確保**する処理を追加した
（`_reserved_region_all`、`_pin_own_box`）。

ジョグ探索で使う`_m2_run_clear()`/`_box_clear()`を、既存の
`_existing_m2`（実際に描画済みのM2形状）に加えて、この予約領域
（処理中の自分自身のピンだけは除外——`_current_reserved_excl`を
ピンごとに1回だけ計算し、除外込みの領域をクロージャ経由で参照）も
障害物として扱うよう拡張した。これにより、ある時点でまだ配線して
いない後続ネットのピンであっても、その"将来の設置場所"へ他ネットの
ジョグ経路が入り込むことを未然に防げる——§29.4で診断した「先着
ネットのジョグ配線が後続ピンの設置場所を塞ぐ」という資源競合の
根本原因に直接対処する変更。

### 31.2 結果

配置再生成・全6チャネル再配線を実行した結果:

| 指標 | §30時点 | 2パス予約後 |
|---|---|---|
| 未配線ピン | 89件 | **86件** |
| M2スペース違反 | 4件 | **0件** |
| V1-CO違反 | 0件 | 0件 |
| ショート疑い | 0件 | 0件 |

**M2スペース違反が§22.5以来初めて0件になった**（これまで3〜8件の
間で増減を繰り返していた既知の残課題）。未配線ピンの削減幅は89→86
の3件と小さいが、これはある意味予想通りで——予約の効果は「後続
ピンの場所を塞がない」ことであり、複数のピン自身の設置位置同士が
物理的に近すぎて（セル配置由来の間隔がM2最小間隔未満）予約領域
同士が最初から重なってしまうケース（例: `ch4_tm`の`_388_.Y`/
`_388_.A`/`_391_.Y`のような、隣接インスタンスの近接ピン群）は
予約だけでは解決できない——この場合は forced_dir 側の方向分離
ロジック（§11/28.2）や、配置そのものの調整が必要になる。

M2違反の完全解消は、2パス予約が「他ピンの場所を塞がない」ことに
加えて「自分自身のジョグ経路が他ピンの予約領域とも一定間隔を
保つ」ことを間接的に強制した結果と考えられる（予約ボックスへの
`_m2_run_clear`/`_box_clear`のクリアランス判定が、実質的に全ピン
アンカー間の最小間隔を保証する形になったため）。

生成物: `Layout/i2c_slave_async_layout_routed_all.gds`（更新版、
181ネット中165ネット完全配線・86ピン/15マルチホップネットが
オープン、簡易DRC違反**0件**・ショート疑い0件・V1-CO違反0件）。

## 32. アンカー予約のチャネル横断グローバル化（負の結果、ロールバック）

§31の残り86件を再診断したところ、23件（27%）は§31の予約が
**チャネル単位でしか閉じていない**ことに起因する見落としだった:
1行は必ず2つのチャネルに接するが、予約領域は「そのチャネルが
実際に配線するネット（`allowed_nets`でフィルタ済みの
`net_stub_pts`）」のピンしか含んでいなかったため、片方のチャネルが
先に配線した際、もう片方のチャネル行きのピンの設置場所を知らずに
塞いでしまうケースが残っていた（§26.8で一度解決した「チャネル間の
見落とし衝突」問題の変種）。

### 32.1 実装と結果

`route_channel.py`/`route_channel_shared.py`の予約対象を、
`net_stub_pts`（ネットフィルタ済み）ではなく、**その行に属する
全インスタンスの全ピン**（`sym_pins[typ]`で列挙、`allowed_nets`に
関係なく）に拡張した。ピンの物理アンカー位置はどちらのチャネルが
配線するかに依存しないため、行の両チャネルが同じ行の全ピンを予約
すれば、チャネル間の調整をグローバルに行わなくても自然に一致する
という狙い。

配置再生成・全チャネル再配線を実行した結果:

| 指標 | §31時点 | グローバル化後 |
|---|---|---|
| 未配線ピン | 86件 | **104件（悪化）** |
| M2スペース違反 | 0件 | 0件 |

未配線ピンが86→104件に**悪化**した。予約対象を「そのチャネルが
実際に配線するネットのピン」から「行内の全ピン（無関係なネットの
ピンも含む）」へ大幅に広げたことで、各ジョグ経路が回避すべき障害物
が急増し、以前は通れていた経路まで塞いでしまったと考えられる
（M2スペース違反は0件を維持しており、"より安全だが、より多くが
未配線になる"という副作用そのものは一貫している）。

### 32.2 判断: ロールバック

27%の見落とし解消という狙った効果より、副作用による新規未配線の
増加が大きく上回ったため、この変更は**採用せず§31時点の実装に
ロールバック**した（`git checkout`でスクリプトを復元、配置・配線を
再生成して86件・M2違反0件の状態を再確認済み）。

チャネル横断の見落とし（23件）に対処する場合は、今回のような
「予約範囲を広げる」単純な拡張ではなく、既に配線済みの他チャネルの
ピン**のみ**を予約する（未配線・無関係なピンは予約しない）、
または実際に衝突が起きた場合にのみピンペアを検出して個別に
`forced_dir`分離を適用する、といったより選択的なアプローチが
必要と考えられる（未着手）。

生成物: `Layout/i2c_slave_async_layout_routed_all.gds`（§31時点の
状態に復元、181ネット中165ネット完全配線・86ピン/15マルチホップ
ネットがオープン、簡易DRC違反0件・ショート疑い0件・V1-CO違反0件）。

## 33. マルチホップネット(15件)専用の最終配線パス（新規スクリプト）

「マルチホップネットの対応をしてください」の指示を受け、15件の
マルチホップネット（3行以上、または非隣接行にまたがるネット——
`route_all_channels.py`のチャネル配線では原理的に扱えない）向けに、
新規スクリプト`script/route_multihop.py`を実装した。既存の6チャネル
配線が完了した統合レイアウト（`i2c_slave_async_layout_routed_all
.gds`）を読み込み、そこへ追加配線してそのまま上書きする、真の最終
パスとして設計した。

### 33.1 方式

チャネル配線とは異なり、M1トランク＋チャネルトラックの概念を使わず、
各マルチホップネットのピンをY座標順に並べ、隣接するピン同士を
M2の直接パスで結ぶ「チェーン配線」方式を採用した（既存のM1を跨いで
良いM2の性質を利用——旧`route_cross_row.py`と同じ原理、
design_notes.md section 23.5参照）。

最初に単純な2〜3セグメントのエルボー/Zシェイプ（既存チャネル
ルーターと同系統のジョグ探索を、チップ全体スケールに拡張したもの）
を試したが、**100%失敗**した。原因を調査したところ、チップ全体で
見ると実際に「全高さ分クリアな列」が900候補中14列だけ存在するには
存在するが、そこへ到達するための水平方向の橋渡し（ピン自身のY高さ
での長距離水平走行）が、行の端で最も混雑している帯域を横切る必要が
あり、単純な固定高さの水平ブリッジでは途中のどこかで必ず衝突する
ことが分かった——§29で見つかった「単一ベンドは経路上のどこかの
障害物を避けられない」問題が、チャネル規模ではなくチップ規模で
再現した形。

### 33.2 真の解決: 有界迷路探索（Lee/BFSグリッド）の実装

§22.5以来繰り返し「真の解消には迷路探索型のアルゴリズムが必要」と
記録してきた通り、対象がわずか15ネット・約100セグメントに限られる
ことを踏まえ、粗いグリッド（6um間隔、約300×220ノード）上での
Lee/BFS探索を実装した。既存M2形状（クリアランス分だけ`sized()`で
膨らませたもの）をポリゴンのbboxベースでグリッドにラスタライズし、
各セグメントごとにBFSで最短経路を求める。

最初の実装（生の格子経路をそのまま箱で描画）はM2スペース違反14件を
新たに作り込んでしまった。原因を切り分け、2段階で修正した:

1. **同一ネット内の複数セグメントが互いを見ずに計画される問題**:
   ネット内の全セグメントを一括で計画してから描画していたため、
   §28.2と同種の兄弟セグメント同士の衝突が発生。セグメントごとに
   即座に描画し`_existing_m2`を更新するインターリーブ方式に変更。
2. **ジグザグ格子経路の角の接続不良**: 6um刻みで頻繁に曲がる
   生の格子経路をそのまま箱の集合として描くと、角で微小な
   ノッチ（クリアランス未満の隙間）が生じることがあった。
   「文字列引っ張り(string pulling)」による経路単純化——生の格子
   経路の各中間点について、正確な（グリッド近似でない）クリアランス
   判定を使い、より遠くの点へ直接ショートカットできないか貪欲に
   試す——を追加し、実際に描画するセグメント数を大幅に削減。
   これによりM2違反は14→1件に減ったが、残り1件は経路単純化の
   フォールバック（ショートカット候補が見つからない場合に生の
   格子ステップをそのまま採用する経路）が正確なクリアランス
   再検証を経ていなかったために発生していた（グリッドの
   bbox近似が境界付近でわずかに楽観的だったケース、実測で
   0.25um不足）。単純化後の経路全体を最後にもう一度正確な
   クリアランス判定で再検証し、失敗した場合はそのセグメント全体を
   未配線として扱う（描画しない）よう変更し、**M2違反0件**を達成。

### 33.3 結果

| 指標 | 値 |
|---|---|
| 対象マルチホップネット数 | 15 |
| 完全配線（全セグメント成功） | 1 |
| 部分配線（一部セグメントのみ成功） | 8 |
| 全失敗 | 6 |
| 総セグメント数 | 101 |
| 成功セグメント数 | 10 |
| M2スペース違反 | 0件（新規0、既存分も0） |
| V1-CO違反 | 0件 |
| ショート疑い | 0件（簡易DRCのspace_check 0件から間接確認） |

セグメント成功率は約10%と低いが、**新規DRC違反ゼロを保ったまま**
達成した点を重視した（違反を出してでも配線本数を増やすくらいなら、
確実にクリーンな部分改善の方が実務上の価値が高いという、本
プロジェクト一貫の判断基準——§25.3, §29の"unrouted over colliding
fallback"方針をこのスケールでも踏襲）。

「部分配線」（8件）は、ネットの一部のピア間だけが電気的に接続され、
残りは孤立したままである点に注意——単純に「ましになった」とは
言えず、ネットが複数の孤立したグループに分かれている状態は、
全く配線されていないのとは異なる種類の未完了である（一部の
ピンだけがつながる分、デバッグ時に接続グラフを見て
誤解を招く可能性がある）。

### 33.4 残課題

- 6件が完全に未着手のまま（`scl_buf0`, `scl_n`の一部、`addr_ok`
  など、密なチェーン構造のネットで、BFSが経路を見つけられなかった
  セグメントが多い）。
- グリッド解像度（6um）は経験的に選んだ値で、より細かくすれば
  精度は上がるが探索コストが増える——15ネット規模では現状で十分
  高速（1秒未満）だが、対象ネット数が増える場合は要再検討。
- 真にすべてのマルチホップネットを解消するには、BFSの障害物
  ラスタライズ精度向上（bbox近似ではなく正確なポリゴン形状での
  ラスタライズ）や、ネット処理順序の最適化（密なチェーンネットを
  優先的に処理する等）が必要と考えられるが、今回は未着手。

生成物: `Layout/i2c_slave_async_layout_routed_all.gds`（マルチホップ
ネット10/101セグメント追加配線、簡易DRC違反0件・ショート疑い0件・
V1-CO違反0件——§31時点からの新規劣化なし）。

## 34. FO=1ネットのクラスタ配置

### 34.1 動機と実装

181ネット中FO=1（受信ピン1個）のネットが98件（約半数）を占める
ことが判明（`route_multihop._parse_nets()`のdirection情報を利用して
カウント）。未配線86ピンのうち38件（58%）、マルチホップ15ネット中
2件（`_172_`, `_115_`——いずれも最も離れた行0↔行4）がFO=1ネット
だったことから、ユーザー提案により「FO=1ネットの出力セルと入力
セルを1配置単位（クラスタ）としてまとめる」方針を採用。

`plan_placement.py`に実装:

- `_build_fo1_pairs()`: 各ネットのdirection情報から、出力ピン1個・
  入力ピン1個のネット（真の一対一接続）だけを抽出し
  (driver_inst, receiver_inst)ペアのリストを作る。
- `_cluster_fo1_pairs()`: このペアグラフをUnion-Findでクラスタ化。
  連鎖（あるセルが片方のペアの受信側かつ別ペアの送信側）で
  グループが際限なく大きくなるのを防ぐため、`FO1_CLUSTER_CAP=4`
  （ユーザー指定）でマージを打ち切る決定的な実装。
- FM分割（行割当）はクラスタ単位のノード（重み=メンバーの
  セル幅合計）で実行、クラスタは必ず同じ行に入る。
- 行内の左右順序も、クラスタ単位でグループ化してから並べ、
  クラスタメンバーが常に隣接するようにした。

実装結果: 152インスタンス中91組の単純ペアが見つかり、
39クラスタ（サイズ2:23件、3:4件、4:12件）、85配置ノードに集約。

### 34.2 5行構成での検証結果と副作用

クラスタ導入直後、FM分割の容量スラック（従来1.28、152インスタンス
粒度で調整済み）をそのまま85クラスタノードに適用したところ、
行0が0インスタンスになる退行が再発（§30と同種の「片側に全部
寄る」現象——クラスタ化でノードが粗くなり、同じスラック比率では
バランスが効かなくなった）。SLACKを0.95〜1.28の範囲で実測比較し、
**SLACK=1.05**が行バランス（1067〜1248um、目標1192um比）と真の
総境界越え数（117、他の値では128〜193）の両方で最良と判明、
採用。

5行構成・実配線での結果:

| 指標 | クラスタ化前 | クラスタ化後(上限4) |
|---|---|---|
| チャネル内オープンピン | 86 | **42**（改善） |
| マルチホップネット数 | 15 | **34**（悪化） |
| マルチホップ完全配線 | 1 | 0 |
| マルチホップ完全失敗 | 6 | 25 |
| マルチホップ未配線セグメント | 91 | 142 |
| M2スペース違反 | 0件 | **2件**（新規） |
| 総未配線アイテム(チャネル+マルチホップ) | 177 | 184（悪化） |

原因: FO=1ペアの一方が同時にクロック分配ネット
（`scl_buf0`/`scl_n`等）や他の多ピンネットにも接続している場合、
クラスタ化がその配置を強制的に引きずり、元々多行にまたがって
いなかった`txreg`/`bit_cnt`/`phase`系のネットまで新たにマルチホップ
化させてしまった。ローカルな改善が、無関係な他ネットの配置を
悪化させる典型的なトレードオフ。

### 34.3 単一行構成での効果判定実験

ユーザー提案により、「マルチホップが原理的に発生しない」状態で
FO=1クラスタ化単体の効果を切り分けるため、行境界そのものを
なくす実験を実施（`NROWS=1`, `ROW_WIDTH_UM=6600`——全セル幅
5962umが1行に収まる幅、`gen_gds_placement.py`のPHYSICAL_ROWSも
下マージン+行0+上マージンの2チャネルのみに縮小）。1行のみなら
すべてのネットが自動的に"row-only"となり、マルチホップ・
隣接ペアという分類自体が発生しない。

実験中に新たな問題が発覚: 全196ネットが1行に入ると、
`route_all_channels.py`の「同一インスタンス経由のUnion-Find
グルーピング」（§30由来、片方のチャネルへの偏り防止策）が
全ネットを1つの巨大グループに収束させてしまい（196ネットが
1グループに）、貪欲バランサーがグループを分割できず片方の
チャネルに196、もう片方に0という極端な偏りが発生。対策として
`SAME_INST_GROUP_CAP`（§34.1のクラスタ上限と同じ発想）を追加し、
マージがこの上限を超える場合は同一インスタンスの異なるピンが
別チャネルに分かれることを許容するようにした（チャネルは
逐次累積配線されるため後段チャネルは前段のM2を障害物として
認識でき、§31の2パス予約もアンカー衝突リスクを別途カバーして
いるため、許容範囲と判断）。上限を8/12/16/24/32で実測比較し、
**16**が最良（オープン83、DRC違反0、チャネル間98:98で完全均衡）。

検証中に**PYTHONHASHSEEDによる非決定性**も発見: 同一コード・
同一入力でも`PYTHONHASHSEED`未固定だと未配線ピン数が83〜113、
M2違反が0〜2件の間で実行のたびに変動した（`route_channel.py`系の
どこかで、setやdictの反復順序がハッシュランダム化の影響を受ける
箇所が残っている——`_fm_bipartition`の`sorted()`化コメントで
言及されている過去の教訓と同種の問題が未修正の場所に残存して
いる可能性）。`PYTHONHASHSEED=0`で完全に再現性のある結果が
得られることを確認。今後の全ての回帰比較は`PYTHONHASHSEED=0`で
実行すべき、という運用上の教訓として記録。

最終結果（`PYTHONHASHSEED=0`, `SAME_INST_GROUP_CAP=16`）:

| 構成 | オープンピン | マルチホップ | M2違反 | ショート |
|---|---|---|---|---|
| 5行（クラスタ化前） | 86 | 15ネット/91セグメント未配線 | 0件 | 0件 |
| 5行+FO=1クラスタ化(上限4) | 42 | 34ネット/142セグメント未配線 | 2件 | - |
| 1行+FO=1クラスタ化+グループ上限16 | **83** | **0(構造上発生しない)** | **0件** | **0件** |

1行構成は総オープン数（83、マルチホップ皆無のため）で他の2構成を
大きく上回り、DRC・ショートともゼロを達成——プロジェクト全体で
最良の配線結果。ただし6600um×1375umという非現実的なアスペクト
比のダイであり、実チップ設計としては採用できない、あくまで
「FO=1クラスタ化はマルチホップという副作用さえなければ強く有効」
ことを実証する診断実験と位置づける。

### 34.4 現在の採用状態

ユーザー判断により、実装方針の統合（5行への戦略適用、中間行数の
探索等）は本セッションでは行わず、実験結果の記録のみとした。

- `plan_placement.py`: FO=1クラスタ化（`FO1_CLUSTER_CAP=4`）と
  SLACK=1.05は**本番(5行)構成に適用済み・コミット対象**。
  `NROWS=5`, `ROW_WIDTH_UM=1800.0`（本番値）に復元済み。
- `gen_gds_placement.py`: 5行チェーン構造（N_BM=1, N_01=4, N_12=3,
  N_23=4, N_34=5, N_TM=2）に復元済み。単一行構成はgit履歴に
  保存（コメントで参照方法を明記）。
- `route_all_channels.py`: `SAME_INST_GROUP_CAP`機構は追加した
  ままだが、本番(5行)では実質無効な値（9999）に設定——§30時点の
  挙動（最大37/45ネットのグループ、片側偏重は発生しない規模）を
  変えないため。単一行実験でのみ16を使用。
- 本番(5行)構成での最終確認値: チャネル内オープン42件、
  マルチホップ34ネット中完全配線0・部分配線9・完全失敗25
  （142/155セグメント未配線）、M2スペース違反2件、V1-CO/ショート
  疑い0件。クラスタ化前（86件オープン、DRC0件）からは総合的には
  悪化（§34.2参照）——**次回セッションで、この2件のM2違反の
  原因調査、または5行構成へのクラスタ化適用の是非（ロールバック
  も含む）を判断する必要がある**。

### 34.5 行内FILLコリドーの効果確認

ユーザー質問「FILLをなくすとどうなるか」を受け、単一行構成
（§34.3の環境）で`distribute_row_fillers()`のコリドー分散配置
（約200um間隔）を無効化し、余り幅638umを行末に1ブロックへ
まとめる比較を実施（`NO_CORRIDORS=1`環境変数でトグル、
`gen_gds_placement.py`）。

| | コリドーあり | コリドーなし |
|---|---|---|
| オープンピン | 83 | 91 |
| DRC違反 | 0件 | 0件 |

コリドー撤去でオープンピンが8件増加（DRC違反はどちらも0件）。
コリドーが「セル近傍のM1/M2に一切干渉されない縦方向の逃げ道」
として機能しているというdocstringの説明を裏付ける結果。壊滅的
な差ではないが、コリドー配置は明確にプラスの効果があることを
確認した。レイアウトサイズ（行幅・行高）自体はコリドーの有無に
関わらず不変（ROW_WIDTH_UM固定のため、余り幅の配分方法が違うだけ）
——真にFILLなしでセルを密着させ行幅を実セル幅5962umまで縮める
場合は別途検証が必要（未実施）。

### 34.6 6行構成の試行

ユーザー要望により行数を6に増やす試行を実施。`plan_placement.py`
のFM分割を汎用化（`nrows`が4/5以外の場合に対応する再帰的
チェーン分割`split_chain()`を新規実装——行インデックス列を
中間点で再帰的に2分し、各分割が物理的な行境界1つに対応するように
した。クラスタ粒度のFM二分割を使う点は§34.1と同じ）。この汎用化は
§34.3の単一行（nrows=1）ケースも自動的にカバーする（分割不要の
基底ケースとして正しく動作）。

本番同様`ROW_WIDTH_UM=1800`のまま行数のみ6に変更（1行あたり目標幅
994um、5行時の1192umより小さい）。チャネル高さは一律4（暫定値、
5行構成のような個別チューニング未実施）から開始。

| 指標 | 5行(本番) | 6行(試行) |
|---|---|---|
| チャネル内オープンピン | 42 | 55 |
| マルチホップネット数 | 34 | 30 |
| マルチホップ完全配線/部分/完全失敗 | 0/9/25 | 0/9/21 |
| マルチホップ未配線セグメント | 142 | 136 |
| M2スペース違反 | 2件 | 1件 |
| コア高さ | 1320um | 1650um（+25%） |

最もオープンが集中したch1_2（29/55件、49ネット担当）のチャネル
高さを4→8に倍増して再試行したが、オープン数は55件のまま変化
せず——§29.4で確立した「オープンの大半はチャネル容量ではなく
自分のアンカーが既存M2に塞がれる資源競合問題」という診断が、
行数を変えても同じく成り立つことを追加確認した形。

総合すると6行はDRC違反がわずかに少ない（1件 vs 2件）ものの、
オープン・未配線セグメントは5行と同水準かやや悪く、ダイ高さは
25%増加——明確な優位性は見られなかった。行数を増やすほど
チャネル数（≒資源分散の機会）は増えるが、1行あたりのネット数が
減る分、行内・チャネル内の絶対的な混雑は必ずしも改善しない
ことを示唆する結果。試行後は本番(5行)構成に復元済み
（`plan_placement.py`のNROWS=5, `gen_gds_placement.py`の
`PHYSICAL_ROWS`は5行チェーンのまま——6行チェーン構造は
`USE_6ROW=1`環境変数で再現可能なコードとして残置）。

### 34.7 チャネル内オープン42件の原因内訳

ユーザー質問を受け、本番(5行)構成の42件のチャネル内オープンピンを、
§29.4と同じ手法（最終統合GDS上でピン座標±1.7um以内に既存M2
形状があるか）で分類した:

| 分類 | 件数 |
|---|---|
| ピン自身のアンカー位置が既存M2と衝突 | 16件 (38%) |
| 衝突なし（別要因） | 26件 |

§29.4時点（クラスタ化前、88件中70件=80%がアンカー衝突）と比べ、
アンカー衝突の割合が大きく下がった——FO=1クラスタ化による配置
変更で、この種の資源競合パターンが以前ほど支配的ではなくなった
ことを示す。

「別要因」26件の内訳を調査するため、`route_channel.py`/
`route_channel_shared.py`に`ROUTE_DEBUG_CAUSE=1`環境変数で
有効化される診断コードを追加した。各失敗ピンについて、
(1) 自分のアンカー位置が既存M2または2パス予約（§31）で
塞がれているか、(2) 塞がれていない場合、ピンからトラックまでの
垂直経路（`_m2_run_clear`によるstraight run）のどこが塞がれて
いるかを2um刻みでスキャンして報告する。

実測の結果:

- 42件中25件は、自分のアンカー位置（既存M2・予約とも）は
  完全にクリアであることを確認（§29.4流の±1.7um判定26件との
  差1件は判定手法のクリアランス値の違い——2.0um固定 vs
  ±1.7um矩形——による誤差）。
- これら25件は例外なく、ピンからトラックへの垂直経路の途中
  （ピンから10〜20um程度離れた地点が多い）で塞がれていた。
  例: `_037_`ネットの`_412_.Y`ピン（y_center=82.50,
  track_y=11.10）はy=[69.1,73.1]付近で塞がれており、実際に
  その場所には**幅23um・高さ4.2umの横長M2形状**
  （x=[1152.5,1175.5], y=[71.5,75.7]）が存在した——別ネットが
  既に描いた水平トレース（トラックへ到達するための横引き部分）
  である。
- オフセットジョグ探索（±2〜800umの47候補）も全て失敗しており、
  近傍の複数列も同種の混雑で塞がっていることを示唆する。

**結論**: 「別要因」の正体は、§29.4の「自分のアンカー位置が直接
衝突」パターンの派生形で、**衝突点がアンカーではなく経路の途中
（他ネットの水平トレース）にある**という違いだけであり、根本原因
は同じ「ネット処理順序に起因する資源競合」に分類できる。すなわち
42件全体（16件+26件）が、程度の差はあれ同一クラスの問題——
現在の単一ベンド/Zシェイプ・ジョグ探索アルゴリズムの構造的限界
（経路上の障害物を迂回する能力がオフセット探索の範囲に限られる）
に起因する。真の解消には route_multihop.py で採用したような
迷路探索（Lee/BFS）ベースのアプローチをチャネル内配線にも
適用する必要があると考えられるが、今回は未着手（診断のみ）。

診断コード（`ROUTE_DEBUG_CAUSE=1`）は両ルータースクリプトに
残置——デフォルト無効のため本番動作に影響なし。

### 34.8 オープンピン/未接続ネット/接続先のレイヤー分離可視化

ユーザー要望「オープンピンと未接続ネットを別レイヤーで、オープン
ピンの接続先も別レイヤーで表示」を受け、`script/gen_err_report2.py`
を新規作成。従来の`gen_err_report.py`（route_all_channels.py/
route_multihop.pyの警告ログをパースする方式）とは異なり、**最終
統合GDS自体から接続性を直接再構築**する方式に変更した:

1. 全196ネットの全ピンについて、via anchor位置を
   `route_multihop.py`と同じロジック（セルpinポリゴン＋GC/CO
   キープアウト回避＋段階的growthフォールバック）で計算。
2. 統合GDS全体（チャネル境界に関係なく）のM1/V1/M2形状から
   Union-Findで連結成分を構築（`verify_channel_connectivity.py`
   と同じ手法だが、チャネルごとのスキャン範囲制限なし）。
3. ネットごとに、そのネットの各ピンがどの連結成分に属するかを
   照合し、
   - 全ピンが1つの成分にまとまっていれば「完全配線」（表示なし）
   - 2つ以上の成分に分かれ、かつ最大の成分が2ピン以上あれば、
     その最大成分（本体）に属さないピンを**オープンピン**
     （(252,0)）、接続先はその本体側の最近傍ピン
   - 全ピンがそれぞれ孤立した1ピンずつの成分（＝ネット内で
     どのペアも一切繋がっていない）なら、そのネット全体を
     **未接続ネット**（(252,1)）とし、各ピンの接続先はネット内の
     最近傍の他ピン

結果（本番5行構成）:

| 分類 | 件数 |
|---|---|
| オープンピン（ネットは部分的に接続済み） | 70ピン |
| 未接続ネット | 49ネット・166ピン |

チャネル内オープン42件・マルチホップ関連未配線と単純合算した
数字とは一致しない点に注意——本レポートはピン単位ではなく
**ネット全体の実接続性**（M1/M2/V1を辿った連結成分）で再判定
しているため、「チャネルルーターは一応ジョグを描けたが、その
ジョグが実は他ネットと繋がるべき場所まで届いていない」
「同じネットの複数ピンがそれぞれ別のオープンで、ネット全体としては
1つも接続されていない」といった、ログベースの集計では見えない
ケースも拾っている。特に未接続ネット49件は、§34.2以降で報告してきた
「マルチホップ完全失敗25件」より多く、チャネル内配線のみで完全に
孤立したネットが追加で存在することを示している。

生成物: `Layout/i2c_slave_async_layout_with_err2.gds`
（(252,0)=オープンピン、(252,1)=未接続ネットのピン、
(252,2)=各オープン/未接続ピンの接続先マーカーと接続線、
非fab・参照専用、メインのrouted_all.gdsは別ファイルのため
無変更）。手作業修正の優先順位づけに使う想定。

### 34.9 FILL常時挿入＋行幅拡張の試行（5行構成）

§34.7までの「セル間に必ず1つ以上FILL1を入れる」方針を、本番
5行構成（ROW_WIDTH_UM=1800）にそのまま適用したところ、行3・4で
「必要な隙間(gap)がFILL幅×(セル数-1)に足りない」というassertion
エラーで配置自体が失敗した。1800umでは全セル+必須FILLを収める
横幅が不足していたため、幅を拡張して再試行:

| ROW_WIDTH_UM | チャネル内オープン | M2 space viol |
|---|---|---|
| 1800（不可・配置失敗） | - | - |
| 2050 | 31 | 9 |
| 2200 | 31 | 1 |

2200umはDRC違反が1800um本番の2件から1件に改善し、2050umより
明らかに良好だったため、この時点では2200umを暫定的な改善候補と
した（本番採用は保留、面積+22%）。

### 34.10 route_multihop.pyの無条件パッド描画バグ発見と修正

6行(1700um)＋FILL常時挿入の試行（後述34.11）でM2最小間隔違反が
7件に急増したため原因調査したところ、`route_multihop.py`の
タッチ済みピンのvia/パッド描画ループが、このプロジェクトの他の
全描画箇所と異なり**クリアランスチェックなしで無条件に**via/M2
パッドを描画していたことが判明した。5行構成ではたまたま衝突が
顕在化していなかった（他原因の残存違反に紛れていた）だけで、
実際には潜在バグだった。

**修正試行1**: 描画直前に、その時点の最新`_existing_m2`（他ネット
分＋自ネットが直前までに描画した分すべてを含む累積M2）に対して
クリアランスチェックを行い、衝突する場合は0.5〜4umの局所オフセット
を4方向×6段階で試し、それでも駄目ならパッド未描画（オープン）に
する、という方式を実装。6行試行でのM2違反は7→1に減少したが、
これは**誤検出**を含んでいた：自ネット自身がそのvia/パッドの直前に
描いたばかりの幹線（トランク）にパッドが重なるのは意図した接続
そのものだが、`_existing_m2`は既にその幹線を含んでいるため
「衝突」と誤判定され、必要以上に多くのパッドが未描画のまま
スキップされていた。本番5行構成で検証したところ、DRC違反数は
2件のまま変化しなかったが、`gen_err_report2.py`による真の接続性は
悪化していた（オープン70→16に見えるが未接続ネット側166→238に
シフトし、合計236→254ピンに悪化）。

**修正試行2（採用）**: `_existing_m2`をそのまま使うのではなく、
各ネットの処理ループの**先頭**（そのネットが何も描く前）の時点で
`_other_nets_m2`としてスナップショットを取り、パッドのクリアランス
判定はこのスナップショットに対して行うよう変更。これにより自ネット
自身の幹線との意図した重なりは正しく許容しつつ、他ネットとの衝突は
従来通り検出できる。本番5行構成で再検証: オフセット12件・未描画
スキップ0件・M2 space viol=2（元の基準値と一致）・
`gen_err_report2.py`の真の接続性も元の基準値と完全一致
（オープン70ピン・未接続166ピン/49ネット）。挙動としては
「何も調整が要らない場合は元のコードと完全に同じ」という意味で
中立であることを確認した。

ところがこの検証run で、これまで一度も出たことのなかった
**「V1 enclosed by M1<1.0 viol: 48」**という新種のDRC違反が出現した。
`git stash`で該当3ファイル（route_multihop.py/route_channel.py/
route_channel_shared.py）を元のコミット済み版に戻して再生成すると
この違反は0件に戻ることから、この違反が今回のセッションでの
変更に起因することは確定したが、パッド描画自体は「スキップ0件・
オフセット0件」という報告（誤り、後述）と矛盾するように見え、
一旦原因不明のまま持ち越しとなっていた。

セッション再開後、標準出力だけでなく標準エラー出力も含めて
確認し直したところ、実際には**オフセットが12件発生していた**
（`DEBUG OFFSET`ログはstderrに出力されており、以前の検証では
stdoutのみをgrepしていたため見落としていた）。この12件はいずれも
`_126_buf0`/`_126_buf1`/`_127_buf0`/`_127_buf1`（リセット系高
ファンアウトネット）の`.RST`ピンで、行境界Y座標(y≈88, 572, 858,
1188)付近に集中していた。

根本原因: `abs_pin_anchor()`はvia中心を選ぶ際、ピン自身のM1
ポリゴンを`VIA_MARGIN_UM`分だけ内側に侵食した「安全領域」内から
選んでいるため、**元の位置(vx,vy)ではV1-M1包含(1.0um以上)が
自動的に保証される**。しかし0.5〜4umのローカルオフセットで
via/M2パッドを新しい位置(placed_vx,placed_vy)にずらした際、
その新しい位置に対応するM1形状（ピン自身のM1、または
patch_polysによる拡張パッチ）を描画していなかったため、
オフセット先でV1-M1包含要件を満たせなくなっていた
（V1-M2包含は既にM2パッド自体を新位置に描き直していたため
問題なかった）。

**修正**: オフセットが適用された場合に限り、via/M2パッドと同じ
新位置(placed_vx,placed_vy)にM2_PAD_SIZE（=VIA_SIZE+2.0、
V1-M1包含の最小要件ちょうど）サイズのM1保証パッドを追加描画する
処理を追加。本番5行構成で再検証: オフセット12件（変わらず）・
スキップ0件・M2 space viol=2（変化なし、元の基準値）・
**V1 enclosed by M1<1.0 viol=0**（48→0に解消）・
`gen_err_report2.py`の真の接続性も元の基準値と完全一致。
これで修正が正しく機能していることを確認し、コミットした。

### 34.11 6行構成の行幅スイープ（修正済みルーターでの再評価）

§34.9で保留していた「6行構成の可能性」を、§34.10で修正した
route_multihop.pyを使って改めて評価した。6行・FILL常時挿入方針
のまま、ROW_WIDTH_UMを振って全パイプライン
（plan_placement→gen_gds_placement→route_all_channels→
route_multihop→drc_check_full→gen_err_report2、いずれも
PYTHONHASHSEED=0固定）を再実行した結果:

| ROW_WIDTH_UM | チャネル内オープン | M2 space viol | V1系viol | 真の接続性（オープン+未接続/ネット数） |
|---|---|---|---|---|
| 1700 | 29 | 7 | 0 | 97+100 / 35net = 197 |
| 1900 | 29 | 4 | 0 | 89+108 / 35net = 197 |
| 2050 | 27 | 8 | 0 | - |
| 2075 | 29 | **0** | 0 | 82+120 / 38net = **202** |
| 2100 | 28 | 1 | 0 | 86+114 / 37net = 200 |
| 2125 | 28 | 2 | 0 | - |
| 2150 | 28 | 1 | 0 | - |
| 2200 | 31 | 7 | 0 | 93+107 / 36net = 200 |

（V1系viol＝V1 space/V1-M1包含/V1-M2包含/V1-GC間隔、全幅で0件）

5行本番（ROW_WIDTH_UM=1800, 修正済みルーターで再検証）との比較:

| 構成 | 面積 | M2 space viol | 真の接続性合計 |
|---|---|---|---|
| 5行 @ 1800um（本番） | 1800×275=495,000um² | 2 | 70+166=236 |
| 6行 @ 2075um | 2075×330=684,750um² | **0（DRC完全クリーン）** | **202（本番比-14%）** |

5行本番の1800umでは幅不足によりFILL常時挿入方針自体が適用できず
（§34.9）、5行でこの方針を活かすには2200um（面積+22%）が必要
だった。6行構成は2075umでDRC完全クリーンかつ真の接続性も本番より
良好（問題ピン合計202 vs 236）という結果が得られたが、面積は
+38.3%とより大きい。また幅に対する感度が非単調（2075で0件だが
隣接する2050/2100/2125/2150では1〜8件）であり、配置のFM分割
結果がわずかな幅変化で大きく変わるため、この最適点はやや
繊細（fragile）である可能性がある点に注意。

現時点では本番構成（5行・1800um）は変更せず、上記6行@2075umの
結果は参考記録として残す。採用可否はユーザー判断待ち。
関連バックアップ: `/tmp/pp_6row_2075_final.py`,
`/tmp/ggp_6row_2075_final.py`, GDS一式は`/tmp/trial_6row_2075_gds/`。

### 34.12 マルチホップネットのM1チャネルスイッチ方式への変更

ユーザー指摘「セル間をM2で直接接続しているネットが問題です。
セル間の接続は必ずM1にチャネル領域でスイッチしてから接続して
ください」を受け、`route_multihop.py`のルーティングモデルを変更した。

**背景**: チャネルルーター（route_channel.py/route_channel_shared.py）
は元々、各ピンからM2で最寄りのトラックまでジョグした後、複数ピンを
つなぐ水平バスは**M1トランク**として描画しており（`top.shapes(m1_idx)
.insert(trunk)`）、この点ではユーザーの要望を既に満たしていた。
一方`route_multihop.py`（非隣接行をまたぐ15〜30ネット）は、コース
グリッド上のLee/BFS迷路探索による経路を**全てM2**で描画しており
（セル行の上をM1と電気的接続なしに素通りする設計、docstring内で
「no via = no electrical connection, so this is safe」と明記）、
これがユーザーの指摘する「M2直結」の実体だった。

**変更内容**:
- `PHYSICAL_ROWS`からチャネル帯（Noneが連続する物理行区間）のY範囲を
  算出（`_channel_bands`）。
- 迷路探索グリッドをレイヤー認識型に変更: 垂直移動は常時許可・M2判定
  （セル行を電気的接続なしに通過）、水平移動は**チャネル帯のY座標に
  いる場合のみ**許可・M1判定。両方向とも既存M1/M2の
  クリアランス（それぞれM1_MIN_SPACE=1.4um/M2_MIN_SPACE=2.0um）で
  ブロック判定。
- `_simplify_path`（直線化ショートカット）・最終クリアランス再検証も
  同様に、水平区間はチャネルY判定+M1クリアランス、垂直区間はM2
  クリアランスに変更。
- 経路の折れ点（水平M1区間と垂直M2区間の境界）は全て真のレイヤー
  遷移になるため、各折れ点にV1ビア＋M1/M2保証パッド
  （M2_PAD_SIZE、V1包含要件ちょうど）を新規描画する処理を追加。

**発生した問題と対処**:
1. 最初の実装では**全セグメントが経路探索失敗**（30ネット中フル
   ルート0、部分ルートわずか1）。原因調査の結果、GRID_STEP=6.0umの
   粗いグリッドでは、ピン自身のvia位置の直上・直下マス（垂直移動の
   唯一の脱出路、水平移動はチャネル外で禁止済みのため）が近傍の
   他ピンM2パッドとのクリアランスで頻繁に「ブロック」判定され、
   探索が1歩も進めず即詰みになっていた（旧来の単一レイヤー方式では
   水平方向への迂回で回避できていたが、新方式ではその迂回自体を
   禁止しているため露呈）。
2. 対処1: `goal`セルに元々あった「1歩だけクリアランス判定を免除する」
   措置を`start`セル（ピン自身のアンカー）にも対称的に適用。
3. 対処2: GRID_STEPを6.0→3.0umに縮小（探索は遅くなるが解像度が
   上がり、密集領域の狭い隙間を発見しやすくなる）。2.0umも試したが
   3.0umの方がDRC結果が良好だったため3.0umを採用。

**6行構成(2075um・修正済みルーター)での結果**（両対処適用後）:

| 指標 | M1チャネルスイッチ前 | M1チャネルスイッチ後 |
|---|---|---|
| セグメント: フル/部分/完全失敗 | 0/11/19 | 0/15/15 |
| M1 space viol | 0 | 1 |
| M2 space viol | 0 | 1 |
| V1 space viol | 0 | 6 |
| 真の接続性（オープン+未接続/ネット数） | 82+120/38net=202 | 83+113/35net=**196** |

セグメントレベルの成功数はむしろ改善し（部分ルート11→15、完全失敗
19→15）、真の接続性も僅かに改善した（202→196）が、代わりに
軽微なDRC違反が8件（M1/M2各1件、V1間隔6件、いずれも折れ点の
保証パッド同士が僅かに近接するケース）発生し、DRC完全クリーンでは
なくなった。セル間の直接M2接続は解消され、水平移動は全てチャネル
帯のM1経由になった。

この8件の詳細な原因調査・解消は未着手（時間の都合で今回は方針の
妥当性確認までとした）。方針自体は有効に機能している
（セグメント成功率・接続性はむしろ改善）ため、次のステップとして
残存DRC違反の解消を検討する価値はある。

### 34.13 M2到達範囲をセル間FILLに限定する方針への精緻化

ユーザーが方針を再定義: 「M2は水平方向にはセル間のFILL領域までしか
延ばさない。M2だけでセルとセルを直接結ぶことは禁止するが、M2を
使うこと自体は禁止しない（=M1経由の乗り継ぎを挟めば長距離接続は
可）。チャネル領域外＝スタセル上でのM2横配線は左右隣にあるFILLまで
の延長として、それ以上の接続はM2でチャネル方向に縦方向に延長して
M1で横方向にチャネル領域でつなげる」。

§34.12はチャネル帯の**外**（セル行の上）では水平移動を一切禁止して
いたが、この方針はより精密で、セル行内でも**隣接する実セル間の
FILLギャップの範囲内**でのみM2の水平延長を許可する（ギャップを
越えて別の実セルの上に踏み込むことは禁止）。

**実装**: `route_multihop.py`に、配置済み全実セル（フィラーではない
もの）の絶対X範囲を`compute_rows()`の行順序とアノテーションレイヤー
から算出する処理を追加し（`_cell_x_extent`）、行ごとに隣接セル間の
ギャップ範囲を事前計算（`_gap_ranges_by_row`）。水平区間の可否判定
関数`_h_leg_layer(y, x0, x1)`を新設し、yがチャネル帯なら'M1'
（X制限なし）、yがセル行内かつ[x0,x1]全体が単一のギャップに収まって
いれば'M2'、それ以外はNone（禁止）を返す。迷路探索グリッドも
`_h_layer_grid`として全マスを事前分類（0=禁止/1=M1チャネル/2=M2
ギャップ）し、隣接マス同士が同じ分類の場合のみ水平移動を許可する
よう変更。折れ点のビア挿入も、隣接する区間の実際のレイヤーが異なる
場合のみ（すなわち垂直M2区間とM2ギャップ区間が連続する場合はビア
不要、M1チャネル区間との境界のみビアが必要）という判定に修正した。

**結果**（6行構成、修正済みルーター）:

| 構成 | セグメント(全/部分/失敗) | DRC違反合計 | 真の接続性合計 |
|---|---|---|---|
| §34.12 M1限定(2075um) | 0/11/19 | 8(M1:1,M2:1,V1:6) | 202 |
| §34.13 FILLギャップ限定(2075um) | 0/15/15 | **2**(M1:1,M2:1) | 196 |
| §34.13 FILLギャップ限定(1700um) | 0/15/15 | **2**(M2:2) | 195 |

FILLギャップ限定に精緻化したことで、同じ2075umでもDRC違反が8→2に
大幅改善した（特にV1間隔違反6件が0件に）。さらに、ユーザー指定の
横幅1700umでも同等の結果（DRC違反2件・問題ピン195個）が得られ、
§34.11で同じ1700um幅がM2違反7件だったこと、§34.9で5行構成では
1800umでもFILL常時挿入自体が成立しなかったことと比べても、明確な
改善である。1700um構成の面積は1700×330=561,000um²で、5行本番
(1800×275=495,000um²)比+13.3%に留まる（§34.11の2075um案は
+38.3%だった）。

残存DRC違反（2075um: M1 1件・M2 1件、1700um: M2 2件）はいずれも
局所的な僅かな間隔不足で、原因の詳細調査は未着手。

まだ本番採用はしていない。次のステップとしてユーザーに結果を報告し、
残存2件のDRC違反解消を続けるか、この時点で一旦記録に留めるかを
確認する。

### 34.14 低利用率チャネルの高さ半減

ユーザー指摘「チャネル０−１と２−３、３−４は縦方向を半分に出来ますね」
を受け、route_all_channels.pyのトラック使用率ログ（§34.13の1700um
構成）を確認したところ:

| チャネル | 使用トラック数/利用可能数 |
|---|---|
| chbm_0 | 3/16 |
| ch0_1 | **7/32** |
| ch1_2 | 20/32 |
| ch2_3 | **5/32** |
| ch3_4 | **7/32** |
| ch4_5 | 16/32 |
| ch5_tm | 3/16 |

ch0_1・ch2_3・ch3_4は32スロット中5〜7しか使っておらず、16スロットに
半減しても十分な余裕がある。ch1_2(20/32)・ch4_5(16/32)は利用率が
高くそのまま維持し、`gen_gds_placement.py`のN_016/N_236/N_346を
4→2（フィラー行数、220um→110um）に変更した。

**結果**: コア高さ1650um→**1320um（-20%）**。ただし:

| 指標 | 変更前(§34.13) | 変更後(§34.14) |
|---|---|---|
| セグメント(全/部分/失敗) | 0/15/15 | 0/13/17 |
| DRC違反合計 | 2(M1:1,M2:1) | 6(M1:5,M2:1) |
| 真の接続性合計 | 195 | 199 |

DRC違反の内訳を確認したところ、5件のM1違反のうち3件は**ch1_2**
（高さを変更していないチャネル）内、2件がch3_4（半減したチャネル）
内で発生していた。ch1_2側の違反は、他チャネルの高さ変更に伴う
route_multihop.pyの経路選択の変化（他ネットの占有パターンが変わり、
連鎖的にch1_2の混雑が増した）による副作用とみられる。

面積は20%削減できるが、DRC違反・接続性ともにやや悪化するという
トレードオフが生じている。本番採用はまだ判断保留、ユーザーに報告し
判断を仰ぐ。

### 34.15 折れ点ビアパッドのクリアランス未チェックバグ修正

ユーザーから「M1スペースのDRCエラーはroute_multihop.pyがM1の配線幅
として2umを採用しているのに対して、通常のroutingのM1配線幅が4um
なことが原因だと思われます」との指摘。実際に§34.14のM1違反5件の
座標を調べたところ、原因は幅の不一致そのものではなく、より直接的な
バグだった:

各違反箇所には共通して、(1) チャネルルーターの4um幅M1トランク
（またはセル自身のM1ピン形状）、(2) route_multihop.pyのM1脚
（2um幅）、(3) 折れ点（M1⇔M2レイヤー遷移点）に描画される
3.4×3.4umの保証パッド、の3種類の形状が近接していた。実測すると
脚同士のクリアランスは十分（1.9um等）だったが、**保証パッドと
既存M1との間隔が1.0〜1.2umしかなく**、M1_MIN_SPACE=1.4um未満に
なっていた。この保証パッド（§34.12で導入、M1⇔M2遷移のビア接続用）
は、他のあらゆる描画箇所と異なり**クリアランスチェックなしに
無条件描画**されていた——§34.10で発見・修正したタッチ済みピンの
パッド描画バグと全く同じクラスの見落としだった。

ユーザーの「M1幅2um vs 4umの不一致」という指摘は、直接の原因では
なかったものの、正しい方向を示していた: 保証パッド（3.4um角、脚の
2umより幅広）が脚本体より外側にはみ出す分だけ、隣接する4um幅の
トランクに接触しやすくなっていた。

**修正**: `_bend_pad_clear(x,y)`を新設し、M1⇔M2遷移が起きる全ての
折れ点について、保証パッドが`_existing_m1`(M1_MIN_SPACE)・
`_existing_m2`(M2_MIN_SPACE)の両方に対してクリアであることを、
実際に描画する**前**に検証するよう変更。他の脚のクリアランス検証と
統合し、いずれかが不可なら区間全体を未配線のまま残す（この
プロジェクト全体で一貫している「衝突する可能性のある形状は
描かない」方針に合わせた）。

**結果**（6行/1700um、§34.14のチャネル半減構成のまま再検証）:

| 指標 | 修正前(§34.14) | 修正後(§34.15) |
|---|---|---|
| セグメント(全/部分/失敗) | 0/13/17 | 0/12/18 |
| DRC違反合計 | 6(M1:5,M2:1) | **1(M2のみ)** |
| 真の接続性合計 | 199 | 201 |

M1違反は完全に0件になった。M2違反が1件（x≈436,y≈562-570、
チャネル半減前の§34.13時点から一貫して残っている別要因のもの）
残るが、DRC品質は大きく改善した。セグメント成功数・接続性は
わずかに悪化（保証パッドが置けない折れ点を含む区間は保守的に
未配線扱いにするため）したが、軽微な範囲。

### 34.16 チャネルルーターのピンパッド未検証バグ修正

§34.15後も残っていたM2違反1件（x≈436,y≈562-570）を調査したところ、
route_multihop.pyではなく**route_channel_shared.py**（隣接ペア
チャネル配線）側の問題と判明した。route_multihop.py実行**前**
（route_all_channels.py出力時点）で既にこの3形状が存在していた
ことをGDS比較で確認。

原因: 隣接する2本のピン（それぞれ別ネット）が同じチャネル(ch1_2)
で独立に配線される際、片方のピン（ジョグ不要でまっすぐ縦に伸びる
経路が見つかった方）が先に描画され、もう片方のピンの**自分自身の
M2パッド**（ピンの物理位置に固定、`top.shapes(m2_idx).insert(pad(x,
y_center, M2_PAD_SIZE))`）が、そのすぐ隣（0.75um）に無条件で
描画されていた。ジョグ経路自体（`_find_jog_path`）はクリアランス
検証されているが、**ピン自身のパッドはその経路探索の対象範囲に
必ずしも含まれない**（今回のケースでは相手ピンの経路が反対方向へ
伸びていたため、パッド位置を一度も検証していなかった）。
route_multihop.pyで§34.10・§34.15と2度発見・修正した「パッド無条件
描画」と全く同じ問題のクラスが、実はroute_channel_shared.py /
route_channel.pyにも最初から存在していたことになる。

**修正**: 各ピンの配線処理の冒頭（ジョグ探索の前）で、ピン自身の
M2パッド位置が`_existing_m2`に対してクリアかどうかを`_box_clear`で
検証。ブロックされていればジョグ探索を試みる意味がない（接点自体が
使えない）ため、即座に未配線（オープン）として扱う。
route_channel_shared.py・route_channel.pyの両方に同一の修正を適用。

**結果**（6行/1700um、§34.14のチャネル半減構成）:

| 指標 | 修正前(§34.15) | 修正後(§34.16) |
|---|---|---|
| チャネル内オープン | 29 | **64** |
| DRC違反合計 | 1(M2のみ) | **0（完全クリーン）** |
| 真の接続性合計 | 201 | 248 |

DRC違反は完全に0件になったが、チャネル内オープンが29→64、真の
接続性の問題ピン合計が201→248と大きく悪化した。今回の修正は
「パッド位置がブロックされていたら即座に諦める」という保守的な
実装で、§34.10のタッチ済みピンパッドで行ったような**局所オフセット
再試行**（0.5〜4um範囲でパッド位置を少しずらして再検証）は未実装。
オフセットを試みればDRCクリーンを保ったまま接続性の悪化をかなり
回復できる可能性が高いが、パッド位置をずらすと同時に描画される
ジョグ経路の始点ともズレが生じうる（経路側は0.3umしかマージンの
余裕がない箇所がある）ため、安全に実装するには経路の始点も
連動して調整する追加作業が必要——これは今回未着手。

DRC完全クリーンという成果と、接続性悪化という代償のトレードオフ
について、ユーザーに報告し方針を確認する。

**ユーザー確定（2026-08-09）**: 「現状のまま確定」との回答を得た。
DRC違反0件・真の接続性問題ピン248件（6行/1700um構成）を本方針の
最終状態として確定する。オフセット再試行による接続性回復は今回は
実施しない（将来必要になった場合の拡張候補として本節に記録を残す）。

## 35. 次世代スタンダードセル・配線チャネル設計方針（構想）

§18〜§34の一連の試行を通じて、配置配線側の複雑さ・バグの大半（§34.10/
34.15/34.16で3回発見した「パッド未検証描画」バグ、§34.12/34.13の
M1チャネルスイッチ方式、§17のフィラーコリドー整列問題等）は、根本的には
**スタンダードセルのピン位置が固定の配線グリッドに乗っていない**ことに
起因していた。次期スタセルライブラリでは、これをセル自体の設計段階で
解消する方針とする。本節はその方針の記録（実装は未着手、次期検討）。

### 35.1 グリッド方針

ユーザー提示のDRCサマリ（`TR-1um_DRC_summary.pdf`）に基づく:

- **セル高さ**: 5.4umの整数倍（N行）。5.4umはM2の縦トラックピッチと
  共通のグリッドで、これによりセル行境界をまたいでもM2トラックが
  連続する。
- **セル幅**: 5.4um×信号ピン数。各信号ピンに専用のXトラックを1本ずつ
  割り当て、同一Xに複数ピンを置かない（"ピンは同じXには置かない"）。
- **チャネル内グリッド**: X=5.4um（M2ピッチ、セルと共通）、
  Y=4.0um（M1トラックピッチ）。M1はチャネル内の横方向配線専用、
  M2はセル上も含めた縦方向トランク配線用として5.4umピッチの固定
  トラック上を走る。

### 35.2 寸法（DRC最小値との整合確認済み）

| 項目 | 値 | 備考 |
|---|---|---|
| M1最小幅/最小間隔 | 1.8 / 1.4um | `drc_check_full.py`のプロジェクト既存値と一致 |
| M2最小幅/最小間隔 | 3.0 / 2.0um | 同上 |
| VIA(V1)サイズ | 1.4um | `route_channel.py`の`VIA_SIZE`と一致 |
| M1/M2ビアエンクロージャ | 1.0um | 同上 |
| M2トラックピッチ | 5.4um (幅3.4+間隔2.0) | 幅は最小値+0.4のマージン、間隔は最小値ちょうど |
| ビアパッドサイズ | 3.4um (=VIA1.4+エンクロージャ1.0×2) | 既存`M2_PAD_SIZE=VIA_SIZE+2.0`と一致 |
| M1トランク幅 | 1.8um（最小値ちょうど） | 行ピッチ4.0はパッドとトランクが最接近するケースで最小間隔1.4umちょうどになるよう算出 |

**全ての寸法が最小値ちょうど、マージンゼロ**である点に注意。手作業
スクリプトで幾何を生成する以上、丸め誤差で違反になり得るため、位置は
浮動小数点umではなく**5.4/4.0グリッドの整数トラック番号**で内部管理し、
GDS書き出し時にのみum変換する実装とすることを推奨する。

### 35.3 電源（VDD/GND）

信号ピン用の5.4グリッド（35.1のセル幅算出）には含めない。専用の
タップセルを用意し、配置時に指示した位置へ挿入、M2縦ストラップで
上下方向に接続する。副次的な利点として、タップセル（フィラーの一種）
のM2ストラップX座標をグローバル固定グリッド上で決められるため、
§16/17で対応に苦労した「行ペア間でフィラーコリドーのX位置を揃える」
問題がそもそも発生しなくなる（§16/17は各行のセル配置カーソルから
逆算していたためズレが生じていた）。

### 35.4 OBS（使用済みトラック宣言）とセル上M2素通り

「スタンダードセル上もM2で配線可能にする」方針を安全に実現するため、
LEFのOBS（配線禁止領域）と同じ発想で、セルごとに**内部配線が使用して
いるM2トラック番号を整数リストとして宣言**する。素通り配線（他ネットが
セルの上を通過するM2）は、このOBSリストに無いトラックのみ使用できる。
これが無いと、セル内部配線と素通りネットがショートする問題が形を変えて
再発する（§34.12〜34.13のM1チャネルスイッチ方式を導入した理由そのもの）。

まずはセル全高を単純にブロックする整数集合（Y範囲を持たない）で開始し、
面積が厳しくなった段階でY範囲付き（部分ブロック）に拡張する、という
段階的な実装を想定する。タップセル自体のM2ストラップが使うトラックは、
そもそもタップセルの位置に信号ピンが存在しないため自動的に空くという
扱いとする（信号ピン用トラックとは別枠での予約は不要）。

### 35.5 期待される効果

この方針を整数トラックグリッドで一貫実装した場合、配置さえ正しければ
幾何的に衝突しようがない設計になり、本セッションで繰り返し発生した
以下の問題が構造的に排除される見込み:

- パッド・経路の未検証描画バグ（§34.10/34.15/34.16、3回発見）
- ジョグ経路探索・オフセット再試行のヒューリスティック（クリアランスの
  事後チェックに依存していたための脆さ）
- チャネル間・行ペア間のフィラーコリドー位置ズレ（§16/17）
- M1チャネルスイッチ方式のような複雑な回避策（§34.12/34.13）

**次のステップ（未着手）**: 上記グリッド仕様を元にした個々のスタセルの
ピン配置表（トラック番号ベース、LEF相当のJSON等）の設計、および
配置・配線スクリプトの整数トラックベースへの書き直し。

### 35.6 実装決定事項（INV_X1試作を経て確定）

§35.1〜35.5の構想を実際にINV_X1のGDSで試作する過程で、以下を確定した:

- **ミラー反転は行わない**。全行を同一向きで配置する。§13以来の
  「奇数行をミラーしてVDD/GNDレールを共有する」方式は廃止し、代わりに
  **チャネル＞スタセル＞チャネルのサンドイッチ配置**（全ての行が上下
  両方に専用チャネルを持つ）を採用する。副次効果として、
  route_channel_shared.py等にあった「mirrored=True時のピン座標変換」
  ロジックが丸ごと不要になる。
- **via/M2パッドはセル自身に焼き込む**（ルータ側での都度描画はしない）。
  ただし、M2をセル上下端まで貫通させる（縦に伸ばす）ことはしない——
  ピンの位置はLEFのオフセット情報から一意に計算できる（探索不要）ため、
  貫通しなくても位置合わせの問題は起きない。加えて、各ピンは専用の
  Xトラックを排他的に持つ設計なので、チャネル境界からピンのY座標まで
  他ネットが存在することは構造的にあり得ず、ルータは衝突チェックなしで
  最短距離の接続を安全に描画できる。**セル上のM2はX方向配線には使わず、
  縦方向の貫通接続にのみ使う**という方針も別途確認済み（貫通させない
  今回の実装は、あくまで「ピンパッドをセル境界まで無条件に延長しない」
  という意味で、X方向配線の解禁とは無関係）。
- **PINレイヤーをM1用・M2用で分離**した。既存の`48/1`（元は"pin"の
  汎用マーカー）はM1PIN専用とし、新たに`49/1`をM2PIN用として追加
  （`49/0`=TXM2という既存の対応パターンに倣った）。分離前は同一レイヤーに
  M1ピン（VDD/GNDレール）とM2ピン（信号パッド）が両方マークされており、
  M2をセル上に貫通させた場合にVDD/GNDレールとマーカーが重複してどちらの
  層のピンか判別できなくなる問題があった。分離後は`tech/TR-1um.lyp`への
  `M2PIN`表示定義の追記が必要（未実施）。

### 35.7 LEF生成スクリプトとライブラリ進捗

`script/gen_lef.py`を新規作成した。GDSの実ジオメトリから座標を自動抽出して
LEF MACROを生成する（手入力座標を持たない）:

- SIZE：prBoundary(235/0)から。
- PIN：M1PIN(48/1)・M2PIN(49/1)マーカーの矩形をそのまま使用、
  TXM1/TXM2のテキスト位置とマーカー内包判定でピン名を対応付け
  （vdd/gnd等の小文字ラベルはVDD/GNDへ正規化、§12の大小文字問題を踏襲）。
- OBS：実際のM1/M2形状からピン領域を差し引いた残り（内部配線）を
  多角形のまま自動出力。
- SITE：幅5.4um（M2トラックピッチと同一グリッド、意図的に同じ数値）
  ×セル高さ。
- ピンの方向(INPUT/OUTPUT)・USE(SIGNAL/POWER/GROUND)は幾何から判定
  できないため、セルごとの手入力テーブル`PIN_META`で管理
  （`script/gen_liberty.py`のCOMB_CELLS定義と整合するよう作成）。

**ライブラリ進捗（`LEF/TR-1um_STDCELL.gds` / `LEF/TR-1um_STDCELL.lef`）**:
合成後ネットリストで使用する17種類のうち14種類が完成済み
（INV_X1, DEL1, NAND2/3/4, NOR2/3/4, AND2_X1, OR2/3/4, MUX2, DFFR）。
全macro、DRC最小値（M1/M2幅・間隔、V1エンクロージャ・間隔）違反ゼロ、
ピンマーカーと実ジオメトリの対応も検証済み。作業中にOR4のVDDピン
マーカーがセル幅を5.4um（1トラック分）超えて描画されている不整合を
発見・報告し、修正済みであることを確認した。

DFFR（§35.8の20トランジスタ構成）はSIZE 64.8×64.8um（12×12トラック）、
5信号ピン(D, CK, RST, Q, QB)全てM2、via本数5（各ピン1個）で、幾何・DRCとも
他セルと同じ手順で検証済み。CKはLEF上`USE CLOCK`として区別している。

**17/17種類、完成**。最後の3種類（BUF_X2/X4/X16、計8インスタンス）は
ユーザー方針により専用の物理セルを描かず、新規作成した`BUF_X1`の
物理セルを流用する。LEF側は、`FOREIGN`文でMACRO名と実体GDSセル名を
分離できるLEFの標準機能を使い、`MACRO BUF_X2`/`BUF_X4`/`BUF_X16`それぞれ
`FOREIGN BUF_X1`として同一ジオメトリを参照する形でエイリアス化した
（`gen_lef.py`の`CELL_ALIASES`辞書）。ネットリスト側の変更は不要。

**未解決の注意点**: `script/gen_liberty.py`のCOMB_CELLSテーブルには
BUF_X1のみ定義されており、BUF_X2/X4/X16のタイミングエントリが無い。
LEFのエイリアス化はP&R（配置配線）には十分だが、STA（静的タイミング
解析）ツールはBUF_X2/X4/X16という名前のセルをlibertyから見つけられず
エラーになる可能性がある。対応案: (a) gen_liberty.pyにBUF_X2/X4/X16を
追加し、駆動力の違いを反映しない場合はBUF_X1と同じプレースホルダ値を
複製する、または (b) ネットリスト側のインスタンスを全てBUF_X1に
リネームしてしまう。どちらも未着手。

### 35.9 電源タップセル(TAP2/TAP3)

§35.3で決めた「VDD/GNDは専用タップセルのM2縦ストラップで供給する」
方針の実セルとして、TAP2（2トラック幅=10.8um）・TAP3（3トラック幅=
16.2um）が作成された。信号ピンを持たず、VDD/GND各1トラックを占有する:

- 各トラックのM2は**セル最下端から最上端まで全高で貫通**している
  （信号ピンとは異なり、局所パッドではなくフル高さのストラップ）。
  電源は経路計算ではなく「どこからでも触れられる」冗長性が重要なため、
  信号ピンで見送った貫通方式（§35.6）をここでは採用している。
- M1レール（VDD=上端, GND=下端、セル全幅）とM2ストラップを繋ぐビアは、
  各ネットにつき1個のみ。GND用ビアはGNDトラックの下端（M1のGNDレール側）、
  VDD用ビアはVDDトラックの上端（M1のVDDレール側）に配置。
- 結果として、GNDのM2ストラップはVDDのM1レール領域を、VDDのM2ストラップ
  はGNDのM1レール領域を、それぞれビア無しで物理的に素通りする（異なる
  層なのでショートしない、§34.16以前から確認済みの通常のスタック構造）。
  M1PINとM2PINマーカーはこの素通り箇所でXY平面上重なるが、レイヤーが
  異なる（M1PINはM1層、M2PINはM2層）ため、ピン識別上の曖昧さは無い
  （§で分離した48/1・49/1の効果はレイヤー単位で効いている）。

**LEF生成側の修正**: 1つのピン名（VDD/GND）が複数の断片
（M1レール1枚＋M2ストラップの両端キャップ2枚）に分かれるケースが
初めて発生したため、`gen_lef.py`を修正——同名の断片を`PIN`単位でまとめ、
1つの`PORT`内に複数の`LAYER`/`RECT`を並べる形に変更した（修正前は
同名の`PIN`ブロックが複数出力される不正なLEFになっていた）。
TAP2/TAP3ともDRC（M1/M2幅・間隔、V1エンクロージャ・間隔、ピンマーカー
とのカバレッジ）検証済み、他15セルへの影響なし（回帰確認済み）。

これで物理セル17種類（DFFR, BUF_X1, TAP2, TAP3含む）全てGDS+LEF完成。
BUF_X2/X4/X16のLEFエイリアスを合わせ、LEFは計20 MACRO。

### 35.10 幅違いフィラー(FILL2/FILL3)

以前のスタセル改良アドバイス（「フィラーセルの種類がFILL1(固定幅)
1種類しかなく、行の端数埋めで無駄な余りが常に発生する」）を受けて、
FILL2（2トラック幅=10.8um）・FILL3（3トラック幅=16.2um）が追加された。
信号ピンなし、VDD/GND M1レールのみ（M2・viaは無し）——FILL1と同じ役割の
幅違いバリエーション。`gen_lef.py`にPIN_META追加（VDD/GND、TAP2/TAP3と
同じ`_PWR`のみ）で対応、既存セル一式の回帰確認込みで全19物理セル＋
BUF_X2/X4/X16エイリアス計22 MACRO、DRC違反ゼロ。

## 36. 新セルライブラリでの初回P&R試行（1行構成）

§35で確定したグリッド仕様・LEFを使い、実際に合成済みネットリスト
（`src/i2c_slave_async_net.v`, 152インスタンス）を配置配線できるかを
検証した。まずは最小構成として**1行のみ**（チャネル＞スタセル＞
チャネルのサンドイッチ、両端＋約600um間隔でTAP2を挿入）で試行し、
「この方式で本当に配線が成立するか」を確認することを目的とした。

### 36.1 新規スクリプト一式

これまでのroute_channel.py等（浮動小数点座標＋事後クリアランス
チェック）とは異なり、LEFのピン座標表から幾何を直接計算し、探索や
クリアランス再検証を行わない実装とした（§35の設計目標そのもの）:

- `script/lef_parser.py`: `LEF/TR-1um_STDCELL.lef`を読み、MACRO毎の
  SIZE・FOREIGN（実体GDSセル名）・PIN毎のDIRECTION/USE/PORT矩形を
  辞書化する。以降の全スクリプトはGDSではなくこのLEF表を正とする。
- `script/netlist_parser.py`: 構造Verilog（named-port方式）をパースし
  インスタンスリストと{ピン:ネット}を返す。**Union-Findによる
  assignエイリアス解決**を実装（後述36.2）。
- `script/gen_placement_row.py`: 152インスタンスをネットリスト順に
  1行へ配置。TAP2をTAP_INTERVAL_UM=600um間隔＋両端に自動挿入。
  各ピンの絶対座標（行ローカルY基準）を`LEF/placement_row.json`に出力。
- `script/estimate_channel_tracks.py`: §14/estimate_m1.pyと同じ手法
  （ネットのX区間の最大同時重なり=必要トラック数）を、新方式
  （M1=4.0umピッチ・全水平配線はチャネル内M1のみ）に合わせて実装。
  上下2チャネルで分割した場合のトラック数を見積もる。
- `script/gen_placement_gds.py`: 見積もったチャネル高さで実際の配置
  GDSを生成（LEFのFOREIGN経由で実体セルを配置、TAP2含む）。
- `script/route_row_channels.py`: 新チャネルルータ本体。ネット毎に
  区間グラフの貪欲彩色（first-fit interval coloring、証明可能に
  最小トラック数になる古典解法）でレーンを割り当て、上下チャネルに
  分割。各ピンからチャネルのトラックYまでM2で直線的に延長し、
  トラックとの交点にビア＋M1ローカルパッド（3.4um、エンクロージャ
  1.0um×2の式通り）を配置。探索・事後チェックは一切行わない。

### 36.2 assignエイリアス解決バグの発見・修正

初回実装では`netlist_parser.py`がセルインスタンスの`.PIN(net)`接続
のみを読み、`assign A = B;`によるネットの別名付け（§9で既出、
`rw`/`addr_match`/`rx_data`等の主要出力ポートや`_005_ = _169_`の
ような内部リネームが115件存在）を解決していなかった。この結果、
本来2ピン以上を持つはずのネットが誤って「行内1ピンのみ＝スタブ」と
判定され、対象ネット数が実際より過大（268件）、配線対象が過小
（128件）に出た。

`gen_schematic.py`（§9）で既に検証済みの方式——`{`や`'`を含む複雑な
assign（バス結線・定数代入、ロジックに寄与しないdead net）は除外し、
単純なスカラーエイリアスのみUnion-Findで併合——を`netlist_parser.py`
に移植して解決。修正後、対象ネット数196・配線165・スタブ31
（うち7本はtx_data入力ピン、約20本は未使用のDFFR QB出力、残りは
`sda_oe_r`等の主要出力ポート直結——いずれも1行構成でチップ境界I/O
パッドを未実装なこの試行では妥当なスタブ）に修正され、必要チャネル
トラック数の見積もりも50→88（片チャネル25→44、+マージン2で46
トラック=184um/チャネル）に更新された。

### 36.3 配置・配線結果

- コアサイズ: 4719.6um × 432.8um（下チャネル184.0 + 行64.8 +
  上チャネル184.0）。161インスタンス（実セル152＋TAP2×9）、重なり
  ゼロを確認。
- DRC: M1/M2幅・間隔、V1エンクロージャ・間隔——**全違反ゼロ**
  （探索を伴わない決定的な幾何生成のため、これまでのセッションで
  3回発見したような「未検証パッド描画」バグのクラスがそもそも
  発生しなかった）。
- 接続性: 165ネット全て（2ピン以上を持つネットの100%）が実際に
  M1/M2/V1経由で電気的に接続されていることを確認。

### 36.4 検証手法上の教訓（レイヤーを区別しない接続性チェックは誤検出する）

初回の接続性検証スクリプトは`M1+M2+V1`をレイヤー区別なく単純に
マージしたRegionでUnion-Findを取っていたところ、**ネットのほぼ
全て（150件超）が1つの巨大な塊に併合される**という誤った結果が出た。
調査の結果、これはルーティングのバグではなく検証スクリプト自体の
誤りと判明——M1とM2は別レイヤーであり、ビア（V1）が無い限り
物理的に重なっていても電気的には繋がらない（他社の配線が意図的に
上を素通りするのは正常）。ところが単純マージはこの区別をせず、
「M1とM2が座標的に重なっていれば接続」とみなしてしまうため、
チャネル内を上下に長く伸びるM2スタブが、経路上の無関係な他ネットの
M1トランクと座標的に交差するたびに誤って短絡と判定していた。

**正しい検証手法**: M1同士・M2同士はそれぞれ独立にUnion-Findで
連結成分を求め、**ビア(V1)の位置でM1側の成分とM2側の成分だけを
橋渡しする**（ビアが無い交差はそのまま別成分のまま）。この方式で
再検証したところ、短絡ゼロ・全165ネット完全接続を確認した。
今後のこのプロジェクトの接続性検証スクリプト（gen_err_report2.py
含む）は元々レイヤーを意識した実装だったため影響なし——今回新規に
書いたこの検証専用スクリプトだけの問題だったが、次世代方式の
検証では同じ誤りを繰り返さないよう、ここに明記しておく。

### 36.5 まとめ

1行構成でのチャネル＞スタセル＞チャネル方式は、新セルライブラリ
（LEFピン表・整数グリッド・貫通M2の電源タップ）と組み合わせて
**探索なし・事後クリアランスチェックなしでDRCクリーン・完全接続の
配線が成立する**ことを確認できた。次のステップ（未着手）は複数行
構成への拡張、行間でのネット横断配線、および主要I/O（チップ境界
ピン）の扱い。

### 35.8 DFFRのトランジスタレベル回路提案（承認済み）

DFFRの面積削減のため、伝送ゲート(TG)ベースの静的マスタ・スレーブ
D-FFを提案し、ユーザーの承認を得た。**合計20トランジスタ**:

| ブロック | 機能 | T数 |
|---|---|---|
| TG1 | マスタ入力パスゲート(D→M) | 2 |
| INV1 | マスタ保持段(M→Mb) | 2 |
| TG2 | マスタ帰還(Mb→M、CLK反対位相) | 2 |
| TG3 | マスタ→スレーブ(Mb→S) | 2 |
| NAND2(S, RSTB) | スレーブ保持段＋非同期リセット統合(→QB) | 4 |
| TG4 | スレーブ帰還(QB→S、CLK反対位相) | 2 |
| INV_Q | 出力バッファ(QB→Q) | 2 |
| INV_CLK | ローカルCLK→CLKB生成 | 2 |
| INV_RST | ローカルRST→RSTB生成 | 2 |

設計判断:

- リセットは**スレーブ段（QB/Qを直接駆動する側）にのみ**組み込む。
  スレーブ段の保持インバータをNAND2(S, RSTB)に置き換えるだけで
  RST=1時にQB=1(Q=0)を強制できる。マスタ段まで両方リセットする実装も
  可能（+2T）だが機能的には不要——リセット解除後は次のクロックエッジで
  正しくDを再取込みするため、マスタ段の値がリセット中に不定でも出力の
  正しさに影響しない。
- **RSTはアクティブハイ**という前提で設計。Yosys合成結果が
  `$_DFF_PP0_`（クロック正エッジ・リセット正論理）にマッピングされて
  いたことから確認済み（§7.5参照）。QB=NAND(S, RSTB)＝NOT(RSTB・S)
  ＝RST + NOT(S)という関係になり、RST=1で強制的にQB=1になる。
- CLK/RSTのローカルインバータ（DFFRインスタンスごとに1個ずつ、
  合計4T）は、外部からCLKB/RSTBを共有ネットとして配布できれば
  DFFR33個×4T=132T削減できる可能性があるが、セルのピン仕様
  （現状CK/D/RST/Q/QBのみ）を変更する、より踏み込んだ最適化になるため
  今回は見送り、ローカル生成のままとする。

**次のステップ（未着手）**: この回路を実際のトランジスタ配置（GDS）に
起こし、via/M2パッド・PIN層分離・DRC検証を経てLEF化する
（INV_X1等で確立した手順と同じ）。

## 37. 2行構成トライアル（チャネル＞行1＞共有チャネル＞行2＞チャネル）

1行構成（§36）の成功を受け、2行スタック構成を試行した。ユーザー
指示：「単純分割で試す」「TAPは各行のX座標で同一位置に並べる。先に
TAP配置、残りにスタセル、空き地はFILL2/3」。

### 37.1 ネット分割・TAP配置（完了・成功）

`gen_placement_2row.py`: 累積幅による単純2分割（行1=108セル、
行2=44セル、DFFR偏在のため行2の方が実質重い）。TAP列は両行で
共通のXグリッド（TRACK_UM=5.4、TAP_INTERVAL_TRACKS=111=599.4um）
に先置きし、両行が同じgap数（5）になるよう `_gaps_needed()` の
ドライランで揃えた。結果：両行とも幅3061.8umで完全一致、TAP X座標
`[0.0, 610.2, 1220.4, 1830.6, 2440.8, 3051.0]`。

### 37.2 3チャネルのトラック見積もり（完了・重要な知見）

`estimate_channel_tracks_2row.py`: ネットを行1専用／行2専用／
行間横断（cross-row）に分類し、行1専用は下マージンchまたは中央ch、
行2専用は中央chまたは上マージンch、**行間横断ネットは中央chのみ**
使用可能とした。

結果：行1専用=82net(ピーク30)、行2専用=7net(ピーク4)、
**行間横断=76net(ピーク56)**、スタブ31net。
下マージン=17トラック(68um)、**中央=75トラック(300um)**、
上マージン=4トラック(16um)。

単純分割では行間横断ネットが多数発生し、中央チャネルが
マージンチャネルの4〜19倍の規模になることが数値で確認された。
これは分割前に懸念していたリスクの実証であり、より高度な
局所性考慮の分割（FM法など）が必要かどうかの判断材料となる。

### 37.3 配置GDS・ルータ拡張（完了、DRCクリーン）

`gen_placement_gds_2row.py` で3チャネル構成の配置GDSを生成。
`route_channels_2row.py` は1行トライアルのルータを拡張し、
中央チャネルを3つの独立レーン割当結果（cross・行1側半分・行2側
半分）のインデックスをオフセットして共存させた。

実装中に2種類の見落としを発見・修正：

1. **中央チャネルのインデックス重複バグ**：3グループのレーン番号
   オフセットが1箇所抜けており、異なるネットが同一トラックに
   割り当てられていた（DRCは無反応、接続性検証で発覚）。
2. **Y方向隣接トラックのビアパッド近接**：M1ビアパッド(3.4um角)が
   トラックピッチ4.0umに対し過大で、異なるネットのビアXが
   一致・近接すると隙間0.6um（M1最小間隔1.4um未満）になりDRC
   スペース違反。空きトラックへの退避で解消（13ネット）。

最終的にDRC完全クリーン（M1/M2幅・間隔、V1間隔・エンクロージャ、
V1-GC間隔すべて0違反）を確認。中央チャネルの実使用86トラックは
予算90トラック（=360um、当初300umから拡張）以内に収まった。

### 37.4 レイヤー考慮接続性検証：未解決の実配線バグを発見

§36.4のレイヤー考慮Union-Find検証（M1同士・M2同士を別成分とし
V1位置でのみ橋渡し）を2行構成に適用したところ、**28件の真の短絡**
が見つかった。DRCはクリーンなのに実際は繋がっている、というのは
§36.4と同じ「隙間がなければDRCは黙る」パターン。

**原因**：中央チャネルの深いトラック（最大86段）に到達するには、
行の端からトラックまでM2スタブを長距離（最大250um超）まっすぐ
延ばす必要がある。ピンX座標は5.4umセルグリッドの倍数であるため、
全く無関係な2つのセルインスタンスのピンが偶然同じXに一致すること
は珍しくない（実際に136組の一致を確認）。Y範囲が重なる2本の
スタブが同じXにあれば、ただ1本のポリゴンに融合し電気的に短絡する
——これはDRCのスペースチェックでは検出不可能（隙間ゼロで「違反」
にすらならない）。

根本原因は、section 35の「ピンは専用トラックを持つので事後
チェック不要」という設計方針が、**1セル内のピン配置にしか
当てはまらない**こと。多数のセルインスタンスが同じ5.4umグリッド
上に置かれる以上、セルをまたいだピンX座標の一致は本質的に避けられ
ない。

**対策の試行と結論**：ユーザーとの相談の上「スタブ衝突検出+ジョグ」
を選択し実装を試みたが、スケールしないことが判明した：

- 中央チャネルだけで384本のピンスタブが存在し、そのY範囲の
  ピーク同時重複は229（下マージンchでは122／122＝ほぼ全数）。
  つまりほとんど全てのスタブが互いにY方向で重なっており、
  「近い空きXを探して逃がす」場当たり的なジョグでは、逃避先への
  接続経路自体が他のスタブと交差してしまう。
- 探索半径を広げる、経路全体の衝突をチェックするなど複数回の
  改良を試みたが、いずれも本質的にはマルチレベルのドッグレッグ
  （複数箇所での迂回）を要する詳細配線（メイズルータ）問題であり、
  このトライアルのスコープを超えると判断し中断。

**現状**：`route_channels_2row.py` はDRCクリーンな配線を生成するが、
上記の長距離スタブ交差による28件の短絡は未解決のまま
（`script/pin_map_2row.json` に記録された座標で再現可能）。
これは1行構成の設計（§35〜36）自体の欠陥ではなく、チャネルが
深くなった場合の「探索なし単純ルータ」の限界として記録する。

### 37.5 今後の選択肢（検討時点の記録）

1. **detailed/mazeルータの実装**：複数レベルでのドッグレッグに
   対応した本格的なチャネルルータへの投資（相応の追加工数）。
2. **FM法など局所性考慮の行分割**：cross-row net数を減らし中央
   チャネルの規模を縮小すれば、上記の衝突頻度も比例して下がり
   単純ジョグで足りる可能性がある。§37.2の数値（cross-row=76、
   ピーク56）が分割前の見積もり材料になる。
3. 現状の記録をもって2行構成トライアルを一旦区切りとする。

ユーザーは最終的に多段構成（横幅1700um目標）へ進む方針のため、
選択肢2（FM分割）を先行させることにした。多段化するほど行境界の
数が増え、境界ごとに単純分割ではcross-row netが積み上がる懸念が
あり、分割アルゴリズムの改善は全ての境界に効くレバレッジの高い
投資と判断。detailed/mazeルータ（選択肢1）は、分割改善後もまだ
チャネルが深すぎる場合の最終手段として温存する。

### 37.6 FM法による行分割の改善（実施・大幅改善を確認）

`fm_partition.py`：ネットリストをハイパーグラフとして扱い
（ノード=セルインスタンス、重み=セル幅、ハイパーエッジ=ネット）、
古典的Fiduccia-Mattheyses法でmin-cut二分割を実施。初期解は既存の
累積幅分割（既に幅バランスは良好：2338.2 vs 2284.2um、差1.2%）。
各パスで、幅バランス制約（目標の±5%）を満たす範囲で最大ゲインの
ノードを貪欲に選び、パス終了後に最良の累積ゲイン地点まで巻き戻す
標準的なFM実装。152ノード・167ネットで実行時間0.4秒。

**結果**：cut net数 78→16（4パスで収束）。幅バランスは
w0=2230.2um / w1=2392.2um（目標2311.2umに対し±3.5%、許容範囲内）。

この分割で配置・チャネル見積もりをやり直したところ
（`gen_placement_2row_fm.py`, `estimate_channel_tracks_2row.py`を
`placement_2row_fm.json`に向けて再実行）:

| 指標 | 単純分割(§37.1-37.4) | FM分割 |
|---|---|---|
| cross-row net数 | 76 (ピーク56) | 14 (ピーク7) |
| 行1専用/行2専用 | 82/7 | 83/68 |
| 中央チャネル必要トラック数 | 73〜86 | 49〜53 |
| コア高さ | 573.6um | 631.6um(下記参照) |
| §37.4の実配線短絡数 | 28件 | **7件** |

(行1専用/行2専用の内訳が大きく変わったのは、FMが幅だけでなく
接続性でも分割するため。コア高さは今回下/上マージンchの見積もりに
+20%程度の安全マージンを大きめに取ったため単純分割時より増えて
いるが、これはマージン設定の違いであり本質的な改善とは無関係。)

`route_channels_2row_fm.py`（§37.3のルータをFM分割の配置・
チャネル予算に向けて再実行、ロジックは同一）を実行した結果、
DRCは完全クリーン（0違反）。レイヤー考慮の接続性検証
（`verify_connectivity_2row_fm.py`）では**短絡7件**（単純分割時の
28件から75%削減）。

**残る7件を閉じる追加の試行と結論**：case 1（トラック隣接ビア
パッド近接）と同じ「空きトラックへの退避」機構を、trackインデックス
に依存しない汎用衝突判定（ピンのスタブ全体のX近接＋Y範囲重複、
チャネル内の全ピン対に対して判定）に拡張して case 2 も一括で解消
できないか試したが、退避先のtrack_yが元の行から離れるほどスタブの
Y範囲が伸び、それが別のスタブとの新たな衝突を誘発する連鎖
（§37.4で単純分割時に経験したのと同じ暴走パターン）が発生し
タイムアウト。安全のため撤回し、case 1のみを解消する既存のシンプル
な退避ロジックに戻した。

**まとめ**：FM分割はcross-row net・チャネル深さ・実短絡数のいずれも
大幅に改善したが（短絡28→7）、根本原因（case 2：長距離M2スタブの
偶然のX一致）は分割改善だけでは完全には消えない。残り7件は
`pin_map_2row_fm.json`の座標で再現可能な既知の制限として記録する。
多段化を進める際は、各行境界でFM分割を適用しつつ、この種の残存
短絡が段数に対して線形かそれ以下で増えるかを継続的に確認する
必要がある。

## 38. 4段構成（横幅1700um目標）への拡張

ユーザー指示：「４段で進めてください。FILLセルは適当に分散して
配置して、上下のチャネルのM2での配線領域として活用してください。」
X=1700umに収める段数見積もり（4段案：各行natural幅の平均
約1155um、TAP込みで1436.4um、余裕あり）を受けて、2行FM分割トライ
アル（§37.6）を4段に一般化した。

### 38.1 4-way FM分割・TAP-first配置・分散FILL配置

`fm_partition.py`に`fm_multiway_partition()`（再帰的二分割：全体を
FM二分割→各半分をさらにFM二分割、n_rowsは2のべき乗が必要）と
`classify_multirow_nets()`を追加。4分割の結果：行幅
1155.6/1074.6/1166.4/1225.8um（目標2311.2umの半分の半分に対し
最大±6%程度）、ネット分類はrow_only=163, adjacent_pair=24,
spanning(3行以上またはpair以外)=11。

`gen_placement_nrow_fm.py`（新規）：`gen_placement_2row_fm.py`を
N行に一般化。TAP_INTERVAL_TRACKS=130（702.0um、今回の行幅に合わせて
ローカルに再設定——`gen_placement_2row.py`の同名グローバル
（111トラック=599.4um、2行トライアルの遥かに広い行向け）を
importして使うと`_gaps_needed`がそちらのグローバルを見てしまい
オーバーシュートするバグを発見・修正、行幅1841.4um→1436.4umに）。

FILL配置をユーザー指示に沿って変更：`pack_row`（TAPギャップの末尾に
FILLを1ブロックでまとめる）から`pack_row_distributed`に置き換え。
各TAPギャップ内で実セル6個ごと（`INSERT_PERIOD=6`）に挿入点を設け、
`split_fill_evenly()`でギャップの余りトラックを挿入点数に応じて
分散（1トラックのみの端数は隣接チャンクから借りて解消）。狙いは
FILL2/FILL3（M1電源レールのみでM2を一切持たない、§35.10）のX座標を
行全体に散らし、後述のspanning net用パススルー配線の「実ピンが
存在しないことが保証されたX候補」として使えるようにすること。

出力：`LEF/placement_nrow_fm.json`（行幅1436.4um、4行、各行の実セル
+TAP2+FILL内訳: 行0=46+3+17, 行1=36+3+22, 行2=45+3+15, 行3=25+3+12）。

### 38.2 5チャネル（下マージン/ch1/ch2/ch3/上マージン）の配置GDS

`gen_placement_gds_nrow_fm.py`（新規、`CH_HEIGHTS`リストで任意行数に
一般化）と`estimate_channel_tracks_nrow_fm.py`（新規、row-only/
adjacent-pair/spanningの3分類をNチャネルに一般化した見積もり。
spanning netは合法チャネル`range(r_min, r_max+1)`の中で暫定的に
ネット数最少のチャネルへ貪欲割当）で、下記のチャネル高さを決定：

`CH_HEIGHTS = [90.0, 180.0, 128.0, 136.0, 100.0]`

コア寸法：(0,0)-(1436.4, 893.2)。行0=90.0-154.8, ch1=154.8-334.8,
行1=334.8-399.6, ch2=399.6-527.6, 行2=527.6-592.4, ch3=592.4-728.4,
行3=728.4-793.2, 上マージン793.2-893.2。

### 38.3 Nチャネルルータ（`route_channels_nrow_fm.py`、新規）

row-only/adjacent-pairネットは§37と同じ手法（行の半分ずつを隣接
2チャネルへ、track内衝突は空きトラックへの退避=case 1のみ対処）を
Nチャネルに一般化してそのまま踏襲。

新規に対応が必要だったのはspanning net（9net、touchesする行が
3つ以上または非隣接2行）：ピンによっては目的チャネルに直接隣接
しておらず、M1トラックがある行の実セル領域をM2で物理的に
突き抜けて（design_notes.md §35：実セルはピン以外のM2を一切
持たないため、他ネットのピンが使っていないXであれば行を貫通できる
という設計方針）通過する必要がある。

**試行錯誤の経緯**（すべて失敗を隠さず記録）：

1. 案1（単一X・行のみチェック）：spanning netの各ピンについて
   通過する行だけの実ピンM2とクリアランス判定する1本のXで
   通しで配線。結果：接続性検証で**70件の短絡**（しかも1本の鎖状に
   連結）。原因：通過経路が横切る「開いたチャネル」内の他ネットの
   スタブ／トランクを一切チェックしていなかったため。

2. 案2（単一X・実GDS全体を都度クエリ）：`begin_shapes_rec_touching`
   で実際に描画済みのGDS内容（実ピン＋これまで描画したスタブ全て）
   に対してクリアランス判定するよう変更。正しさは改善したが、
   経路全体（複数チャネル＋行をまたぐ数百um）を単一のXで
   同時にクリアさせる制約が強すぎ、候補がほぼ見つからなくなった
   （チャネル使用率が40〜78%あるため）。

3. 案3（区間分割ウォーク、採用）：経路をチャネル/行の境界で
   `build_segments()`により細かい区間列に分解し、各区間ごとに
   個別にクリアランスを判定する「小刻みなジョグ」方式へ再設計。
   さらに、区間の中で候補Xを1つに固定せず`WALK_STEP_Y=5.4um`ごとの
   区間で近い方から複数の目的Xを試す（`try_reach`）ことで、
   「行/チャネル境界そのものが全ネット共通のボトルネックになり
   最も混雑した場所でジョグを強制される」問題を回避した。
   （実装の詳細な失敗ログはコード内docstringに残してある：
   ジョグ経路が旧Xを含んで掃引される設計だと、旧Xがちょうど
   最小間隔ギリギリの位置にあるだけで新Xへの移動が軒並み塞がれる、
   等の具体的なバグを都度発見・修正した。）

案3でも、チャネル使用率が高い区間（特にch1=33/45=73%、
ch2=25/32=78%）では有効な経路が見つからないピンが残った
（探索半径・試行目的地数を増やしても解消せず）。§37.4/37.6で
確認した「探索ベースの手法は規模が大きくなると破綻する」という
知見の通り、38本のパススルー対象ピンのうち10本は経路探索が
尽きた。**探索失敗時は例外(`PathBlocked`)で検出し、通常の直線
スタブ（チェックなし）にフォールバックする**方針とし、無限リトライ
やハングを避けつつルータを完走させ、実際の短絡数はDRC・接続性
検証で正直に測定する（§37.4と同じ「隠さず記録する」方針）。

### 38.4 DRC・接続性検証結果

DRC（`drc_check_nrow_fm.py`）：M1は0違反。M2は幅0違反・
**間隔違反8件**。V1は間隔0違反、M1包絡0違反、**M2包絡違反2件**、
V1-GC間隔0違反。違反は前述のフォールバック（チェックなし直線
スタブ）10本に起因すると考えられる（他の155ネットはチェック付き
経路のためDRCクリーンなはず）。

接続性検証（`verify_connectivity_nrow_fm.py`、§36.4と同じレイヤー
考慮Union-Find）：165ネット・517ピンを検証し、**短絡25件**
（2行FM分割トライアルの7件から本数は増えたが、行数・ネット数も
増えている：spanning net数は2行トライアルの実質0本→4行では9本、
うちパススルーが必要だったピン38本中10本がフォールバック）。

**まとめ**：4段構成でも配置（FM分割・分散FILL）とチャネル見積もりは
狙い通り機能し、コア幅1436.4um（目標1700um以内）を達成。ルータは
row-only/adjacent-pairネット（156net）を引き続きクリーンに処理し、
新規のspanning net処理（9net、38パススルーピン）も78%
（38中28ピン）は実GDSに対するライブな衝突チェック付きで解決できた。
残り10ピン（=25件の短絡・8+2件のDRC違反の主因）は、案1〜3の
試行を経てもなお解決できなかった既知の制限として記録する。
`pin_map_nrow_fm.json`の座標で再現可能。今後の選択肢は§37.5と
同様（詳細/迷路ルータの実装、チャネル高さにさらに余裕を持たせる、
spanning netをより少なくするFM分割の調整など）。

### 38.5 v3：via_1 PCell化・グリッド遵守・TAP電源メッシュ（ユーザー指示）

ユーザーからのレビューを受け、§38.3のspanning net配線方式
（M2を古いXから新しいXまで幅広く覆うボックスで斜めに繋ぐ）を
再設計する指示があった：

> spanning netもM2はX軸は固定、X方向への移動は、チャネル領域で
> 一旦M1にVIAで接続し直して(Pcell via_1を使用）移動して、再度M2へ
> VIAで接続してY方向に移動する方針
> つまり、spanning netもX＝5.4 Y=4.0のチャネル領域のグリッドを
> 遵守してください。
> またTAPセルの電源M2はチャネル領域でもスタセル間でM2で接続ください。

対応前に、事前調査で以下を確認した：

- **via_1 PCell**：`klayout/tech/python/cells/via.py`で定義された
  TR-1umライブラリのPCell。V1カット＋M1パッド＋M2パッドを一括生成
  （デフォルトサイズ3.4×3.4um、既存の手描きパッドと同一寸法）。
  standalone Pythonから`import pya`（pipの`klayout`パッケージが
  `pya`互換シムを同梱）→`from cells import tr_1um`→
  `tr_1um("TR-1um")`でライブラリ登録→
  `layout.add_pcell_variant(lib, decl.id(), {"x":3.4,"y":3.4,"x0":"c","y0":"c"})`
  で生成したセルインデックスを`CellInstArray`で配置、という手順で
  klayout本体（GUI/CLIバイナリ）なしにインスタンス化できることを
  確認済み。M1/M2パッドは常に原点中心に生成される（x0/y0はV1カット
  配列の内部配置のみに影響）。
- **TAP2電源ストラップ**：LEF定義よりGND M2 X=1.0-4.4um（中心2.7）、
  VDD M2 X=6.4-9.8um（中心8.1）、共にセル全高から上下1.1um内側
  （Y=1.1-63.7、セル高64.8umに対して）まで。行間の接続は既存の
  慣例なし（今回新規に確立）。

**via_1化・M1ジョグへの再設計**（`route_channels_nrow_fm.py` v3）：
既存の全ビア（ピン接続・トランク経由のプライマリ接続・新規ジョグ）
を`place_via()`によるvia_1インスタンスに統一。spanning netの
X移動は、行を1本跨ぐたびに「その行の実セルM2をX=5.4刻みで避けた
クリアなX」を`find_row_clear_x()`でローカル探索し、直前のXと異なれば
via_1(M2→M1)→M1水平ジョグ→via_1(M1→M2)を挿入する方式に変更。
ジョグのtrack_yは、既存のcase-1（トラック隣接ビアパッド近接）用の
「空きトラックへの退避」機構（`claim_track`）をそのまま再利用して
**新規に確保**する——これにより、ジョグのM1配線はそのチャネルの
どの既存配線とも共有されない専用トラックを持つことが構造的に
保証され、v2で必要だった「チャネル全体に対するライブな衝突探索」
（`try_reach`の複雑な複数候補ウォーク）が一切不要になった。X探索も
Y探索も既存のグリッド（X=5.4um、Y=4.0umトラックピッチ）のみを使う
ため、v2で苦労した「最小間隔ぎりぎりでの誤判定」（epsilonマージンの
場当たり的な調整）も発生しなくなった。

ジョグはチャネルのトラックを追加消費するため、チャネル高さを
再見積もりする必要があった：初回実行でchannel2=43/32、channel3=
44/34とオーバーフロー（33本の行跨ぎピン全てでジョグが必要だった
ため）、`CH_HEIGHTS`を`[90, 180, 220, 224, 100]`に拡大して再実行し
全チャネルが予算内に収まることを確認（channel2=50/55、channel3=
44/56）。コア高さは893.2um→1073.2umに拡大。

**TAP電源メッシュ**：`draw_tap_power_mesh`相当の処理を追加し、
全TAP列（3列、行間で共通のX：0.0/712.8/1425.6um）×全チャネル
（5個）×VDD/GND 2ネットぶんのM2ストラップを、隣接行のTAP自身の
ストラップと確実に重なるよう1.1umの余裕を持たせて描画。行0の
GNDストラップ（Y=91.1開始）からチャネル1を経て行1のGNDストラップ
まで単一のmergedポリゴンとして連続していることを実測で確認した。
このTAP列のX範囲は`x_forbidden()`でsignal netのクリアX探索から
除外される。

**結果**：
- DRC（`drc_check_nrow_fm.py`）：**M1/M2/V1すべて0違反**（v2の
  M2間隔違反8件・V1包絡違反2件から改善。v2の違反はフォールバック
  10ピン＝チェックなし直線スタブが原因だったが、v3はチャネル内の
  ライブ衝突探索が構造的に不要になったため全spanning netピンが
  フォールバックなしで解決できた）。
- 接続性検証：**短絡31件**（v2の25件から増加）。個別に1件
  （`bit_cnt[2]`↔`bit_cnt[0]`）の実体を調査した結果、**spanning net
  やvia_1化とは無関係**で、§37.4/37.6で記録した「case 2」
  （row-only/adjacent-pairネットの直線スタブが、ライブ衝突チェック
  なしで描かれるため、別の行の同じローカルX位置——今回はTAP直後の
  最初のセルスロットが偶然どの行でも同じX=13.5umになる——を使う
  別ネットのスタブとチャネル内でY方向に重なって短絡する）だと確認
  した。DRCが0違反であることは、この短絡が間隔違反ではなく
  ポリゴンの直接重なり（マージ）によるものであることとも整合する。
  row-only/adjacent-pairネット（159net）の配線方式は今回変更して
  いないため、この限界は本セッションの変更が原因ではなく、既存の
  未解決課題がネット数増加に伴い件数として増えたものである。

**まとめ**：ユーザー指示の3点（via_1化、X=5.4/Y=4.0グリッド遵守、
TAP電源メッシュのチャネル間接続）はすべて実装・実測で確認できた。
副次的な効果として、v2で最も複雑だったライブ衝突探索ロジック
（`try_reach`/`walk_to_target`など）が不要になり、コードは大幅に
単純化された。DRCも完全にクリーンになった。一方で接続性の短絡は
今回対象外だったrow-only/adjacent-pairネットの既知の限界（case 2）
に起因するため未解消のまま残っている。次の一手としては、
row-only/adjacent-pairネットにも同種のグリッド準拠ジョグ機構を
適用する（トラック予算はさらに増えるが、実装は流用できる）のが
最も筋が良いと考えられる。

### 38.6 行幅固定（1620um）・左端FILL分散・行スタガー配置によるcase 2緩和

§38.5で確認した「case 2」短絡（row-only/adjacent-pairネットの直線
スタブが、ライブ衝突チェックなしで描かれるため、別の行の同じ
ローカルX位置——特にTAP直後の最初の実セルスロットが偶然どの行でも
同じXになりやすい——を使う別ネットのスタブとチャネル内で重なって
短絡する）について、ユーザーから配置レイアウトのスクリーンショット
とともに次の指示があった：

> 左のスタセルの領域外でspanning netが接続していますので、以下の
> 方針で配置と配線してください。
> １．X軸を5.4x300=1620とします。
> ２．FILLセルを左端にも分散配置してください。
> なお、row-only/adjacent-pairネット側の既知の限界の対策として
> 配置での対策ができるか？FILLとの入れ替え等。

**(1) 行幅固定（TARGET_ROW_WIDTH_UM = 5.4×300 = 1620.0um）**

`gen_placement_nrow_fm.py`の`TAP_INTERVAL_TRACKS`（旧: 固定130
トラック、内容依存の1436.4um幅を生んでいた）を、目標幅から逆算する
方式に変更：

```
TARGET_ROW_WIDTH_UM = 5.4 * 300          # 1620.0um
N_GAPS = 2                                 # TAP列3本・ギャップ2箇所
TAP_WIDTH_UM = 10.8
TAP_INTERVAL_TRACKS = round(
    (TARGET_ROW_WIDTH_UM - (N_GAPS+1)*TAP_WIDTH_UM) / N_GAPS / TRACK_UM)
# = 147   (検算: 3*10.8 + 2*147*5.4 = 1620.0um ちょうど)
```

これにより全4行が厳密に1620.0umで揃う（従来の1436.4umより
FILL用の正規トラック領域が増え、行またぎネットのクリアX探索の
「逃げ場」が広がる副次効果もある）。router側にも
`ROW_WIDTH_UM = 5.4*300`定数と、配置JSONの各行幅がこれと一致する
ことを確認するアサーションを追加した。

**(2) 左端FILL分散**

`pack_row_distributed()`のFILL挿入位置リストが従来ギャップ先頭
（TAP直後、position 0）を含んでいなかったため、FILLは常にギャップ
の途中以降にしか置かれず、TAP直後の最初の実セルは常にTAP幅
（10.8um）だけ進んだ固定オフセットに来ていた。挿入位置に0を追加：

```
insertion_positions = sorted(set(
    [0] + list(range(insert_period, len(seg), insert_period))
    + ([len(seg)] if seg else [])))
```

**(3) 行スタガー（row-only/adjacent-pairネットのcase 2緩和策）**

配置のみでcase 2の「異なる行が同じローカルX位置を使う」構造的
原因を崩すため、各行のギャップ0（TAP_0直後）に行番号に応じた
トラック数のFILLを強制的に追加挿入する`STAGGER_TRACKS = [0,2,4,6]`
（行0,1,2,3向け）を導入。これは"placement-only, no live-routing
changes"の緩和策であり、短絡ゼロを保証するものではない
（design_notesにもその旨明記）。

**実行結果**：4行とも幅1620.0umで一致。各行の最初の実セルの
絶対X位置は、旧版ではほぼ同一（TAP直後の固定オフセット）だったが、
新版では行0=27.0um、行1=32.4um、行2=43.2um、行3=54.0umと全て
異なる値になった（FILLセル数は行0=33・行1=31・行2=30・行3=25、
旧版の22/19/16/12から増加）。

配置GDS・チャネル見積もりを再実行（`CH_HEIGHTS`は§38.5のまま
`[90,180,220,224,100]`を維持——新しい行幅による必要トラック数見積
もり[14,29,28,26,19]tracksは現行予算に対して十分な余裕があるため
変更不要と判断）。コアbbox: `(0,0)-(1620.0,1073.2)`。

**router側の不具合修正（out-of-bounds bug、タスク#27）**：
`find_row_clear_x()`にX探索範囲の境界チェックがなく、周辺が
混雑している場合に探索がX<0やX>行幅まで彷徨い、スタセル領域外に
spanning netを接続してしまうバグ（`_126_buf1`でX=-8.1, -18.9umの
接続を確認）があった。`in_bounds(x)`（`PAD_HALF + M2_MIN_GAP`の
余裕を持たせて`[0, ROW_WIDTH_UM]`内に制限）を候補フィルタに追加して
修正。

**再実行結果**：
- DRC：**M1/M2/V1すべて0違反**（維持）。
- 境界外接続：**0件**（修正確認、`_126_buf1`含め全spanning net
  ピンが行幅内に収まっている）。
- 接続性検証：**短絡23件**（§38.5時点の31件から低減、約26%減）。
  スタガー配置は「異なる行の同じローカルX衝突」という*系統的な*
  原因の一部を崩す緩和策であり、残る23件は依然としてcase 2の
  範疇（row-only/adjacent-pairネットの配線方式自体は本ラウンドでも
  変更していない）と考えられる。件数減少はスタガーの効果を支持
  するが、ゼロにはなっておらず、根本解決には§38.5末尾で述べた
  「row-only/adjacent-pairネットへの同種グリッド準拠ジョグ機構の
  適用」が引き続き必要。

### 38.7 偶数行/奇数行のFILL左右アンカー分離によるcase 2追加緩和

§38.6の結果を受け、ユーザーから追加の緩和策の提案があった：

> スタセルの奇数行と偶数行で、FILLセルを入れるX座標が重ならないよう
> に配置することで、case2を減らせるか検討ください。

**分析**：実セルピンのX重複を調べたところ、チャネルを共有する隣接行
同士（row0×row1等）と、チャネルを共有しない非隣接行同士（row0×row2等）
とで重複率にほとんど差がなかった（隣接99箇所、非隣接104箇所、300
スロット中120〜160使用＝密度約40〜53%の統計的な帰結）。つまり現行の
FILL配置は「隣接行を意図的に避ける」構造を一切持っていなかった。
行番号が連続する限り隣接行は必ず奇数・偶数のペアになるため、
「偶数行と奇数行でFILL位置を分ける」提案は理にかなっている。

ユーザーからは続けて「奇数行を右詰めにする際、セルをXミラー反転
してはどうか」という追加提案があったが、検討の結果採用しなかった：
単純な右詰め（ブロックの位置シフトのみ、セル内部順序・向きは不変）
だけで、隣接行ペアの使用スロット区間そのものを分離できれば、内部の
セル順序に関係なく重複ゼロが数学的に保証される。ミラー反転はこの
保証に何も加えない一方、design_notes §22.6で単一セルのXミラーが
新規DRC違反・短絡増加を引き起こした前例があり、また§35.6で「行
ミラーによる座標変換ロジックは撤廃し二度と持ち込まない」という
設計方針を確定させた経緯がある。実装コストとリスクに見合う効果が
証明できないため、ミラーなしの単純右詰めで実装することにした。

**実装**：`gen_placement_nrow_fm.py`の`pack_row_distributed()`に
`anchor`引数（"left"/"right"）を追加。TAP列のX位置は内容によらず
固定（ギャップ幅`TAP_INTERVAL_TRACKS`が常に一定のため）なので、
各ギャップ内で偶数行は実セルをTAP直後から詰め（余りFILLは末尾）、
奇数行は逆に実セルを次のTAPの直前まで詰め（余りFILLは先頭）。
`main()`で行番号の偶奇に応じて`anchor="left"`/`"right"`を渡す。
旧`STAGGER_TRACKS`（先頭のみの小さな行別オフセット、§38.6）と
`insert_period`による複数点分散FILL挿入は本方式に置き換えられ廃止。

**実装上の落とし穴と修正**：最初の実装はギャップを行順（0→1）で
消費してから左右にアンカーするだけだったため、既存の「ギャップ0を
先に貪欲に埋め切ってからギャップ1に進む」処理順の影響で、右詰め
行も結局ギャップ0の右端に押し込まれるだけになり、行全体としての
分離にほとんど寄与しなかった（実測：row0×row1の重複99→84と
ほぼ変化なし）。原因を特定し、右詰め行はギャップを**逆順（最後の
ギャップから）**消費してから最終的に行順で配置し直すよう修正。
これにより奇数行の実セルは行の真の右端（最後のTAP直前）から
埋まるようになり、偶数行（真の左端から）と合わせて行全体
（2ギャップ計1587.6um）を分離プールとして使えるようになった。

**結果**（同一セッション内でFM分割を再実行したため、§38.6時点との
行内容の完全な同一性はない点に注意）：

| 行ペア | 修正前重複 | 修正後重複 |
|---|---|---|
| row0×row1（隣接） | 99 | 34 |
| row1×row2（隣接） | 112 | 52 |
| row2×row3（隣接） | 70 | 48 |
| row0×row2（非隣接、参考） | 104 | 106（ほぼ不変＝想定通り） |

隣接行のみ大幅に重複が減り、非隣接行はほぼ不変だったことから、
対策が意図通り「チャネルを共有する行ペア」だけを狙って効いている
ことを確認した。ただしrow1×row2やrow2×row3は行の実セル総幅が
2ギャップ合計の空き（1587.6um）を超えており（例: row1+row2=
1117.8+1150.2=2268um）、重複ゼロは原理的に不可能で、超過分は
強制的に中央で重なる。

配置・チャネル見積もり・配線・DRC・接続性検証を再実行した結果：
DRC**0違反**（維持）、境界外接続**0件**（維持）、接続性検証の
短絡は**22件**（§38.6の23件からさらに1件減、短絡ブロブ数は
9個→6個、巻き込まれたネット数は32→28に減少）。期待したほど劇的
な改善ではなかった——理由は上記の通り一部行ペアで重複ゼロ化が
原理的に達成できないこと、および§38.5で確認した「幅広いM1
トランクが無関係な別ネットのスタブを巻き込む」パターン（今回の
対策はrow-only/adjacent-pairネット自身のピンX分離のみを狙って
おり、このトランク巻き込みパターンには効果が及ばない）が依然
残っているためと考えられる。

**まとめ**：行の偶奇によるFILL左右アンカー分離は、隣接行の実ピンX
重複を50〜65%削減する効果を確認したが、短絡件数への直接的な効果は
1件減（23→22）にとどまった。根本解決には引き続き§38.5末尾で述べた
「row-only/adjacent-pairネットへのライブ衝突チェック付きジョグ機構
の導入」が必要と考えられる。

### 38.8 残る22件の詳細分析と3方向の対策実装

§38.7の22件について、ユーザーから「もう少し詳しく説明してほしい、
対策案を提案してほしい」との依頼を受け、実データを使って原因を
再分析した。

**分析1：分離予算超過（要因1）**。行幅1620umのうちTAP3本ぶん32.4um
を除いた1587.6umが2ギャップの予算。今回のFM分割後の各行実セル総幅
（row0=1069.2um, row1=1117.8um, row2=1150.2um, row3=1225.8um）を
隣接行同士で足すと、row0+row1=2187.0um、row1+row2=2268.0um、
row2+row3=2376.0umとなり、**3組すべてが予算を大きく超過**
（599〜788um）していた。§38.7の左右アンカーは超過分をゼロにはでき
ず、必ず中央で強制的に重なる。

**分析2：高ファンアウトの"ハブ"ネット（要因2）**。短絡ブロブの中身を
実際にたどると、`scl_buf0`（15ピン、X=402〜1584umに散在）、
`scl_buf1`（14ピン）、`_126_buf0`／`_126_buf1`（各13ピン）という
クロック/リセット系バッファツリーが繰り返し登場した。これらは1本の
M1トランクが行幅の70〜80%に及ぶ広さになり、`collides()`/
`claim_track()`が「登録済みの個別ピンX」しかチェックせず「トランクが
連続して占有するX区間」を認識しないため、無関係な別ネットのビアが
（そのネット自身のピンには全く近くなくても）トランクの下に来てしまう
ことが分かった。さらにこの種のネットは複数チャネルにピンを持つため、
チャネルごとに独立した"抽選"が発生し、Union-Findの推移性で無関係な
ネットが次々巻き込まれる（1つのブロブに12ネットが混入した例を確認）。

ユーザーから3方向の対策を指示され、実装した：

**(1) 高FOネット優先配線+専用ガードトラック（要因2④⑤）**。
`route_channels_nrow_fm.py`に`HIGH_FO_THRESHOLD=8`ピン以上のネット
を通常のレーン割り当てから除外し、チャネル先頭に専用領域を確保する
仕組みを追加。各ネットは自分専用のトラックを持ち、前後に
`HIGH_FO_GUARD_TRACKS=1`本の空きトラックを挟む。トラック間隔4.0um
に対し空きトラック込みで8.0um以上のY方向分離が生まれ、トランクの
半幅0.9um＋ビアパッド半幅1.7um=2.6um以内には他ネットが絶対に来ない
——**X方向の広さに関係なくY方向分離だけで幾何学的に接触を防ぐ**
設計。これらのネットは配置・描画とも他ネットより先に処理する
（優先配線）。

**(2) 4ネットの行ごと独立配線（ユーザー提案、要因2の代替実装）**。
`scl_buf0`/`scl_buf1`/`_126_buf0`/`_126_buf1`の4ネットについては、
ユーザーから「各スタセル行毎に独立にM1チャネル配線を持ち、M2で
スタセルに接続、他行とは共有しない」という、より現実のASIC設計
（クロック/リセット分配の定番手法）に近い方式の提案があり、そちらを
採用した。`PER_ROW_LOCAL_NETS`として指定し、ピンを持つ各行ごとに
「その行の直下のチャネル」に専用のローカルM1トランク（専用ガード
トラック付き）を持たせ、行をまたいで共有しない。複数行にまたがる
ネットを1本の電気的ネットとして繋ぐため、`find_row_clear_x`+
`draw_jog`（spanning netの行またぎ機構）を再利用した「スパイン」
（縦方向M2幹線）で各行のローカルトランクを順につなぐ。

実装中に2件の不具合を発見・修正した。1つ目はスパインの経路探索
（`channel_clear`）が誤ってM1レイヤーも見ていたため、無関係な別
トラックのトランクに触れると誤判定し「探索範囲内にクリアなXが
見つからない」というSystemExitが発生した問題（M1とM2は別レイヤー
でビアなしの交差はショートしないため、M1チェックは不要かつ有害
だった。`row_clear`が元々M2のみをチェックしていたのと同じ理由）。
2つ目は、スパインが複数行のローカルトランクを繋ぐ際、行をまたぐ
最後のジョグの着地X座標が「たまたま探索で見つかったクリアなX」の
ままで、接続先の行のトランクが実際に使っているX範囲
（`row_trunk_xmin`）と一致するとは限らず、ビアがトランクに触れずに
孤立し**ネット自体が分断される**（`verify_connectivity_nrow_fm.py`の
NET SPLIT）というバグ。最後に明示的に`row_trunk_xmin[r_hi]`へジョグ
してから接続するよう修正して解消した。

**(3) 重複ゾーン限定ライブチェック+ジョグ（要因1②、実装したが無効化）**。
`channel_clear()`でピンのスタブが通過するチャネル区間（行の境界Yから
自分のtrack_yまで）に既存のM2があるかを`begin_shapes_rec_touching`
で確認し、あれば`find_channel_clear_x`でクリアなXを探して
`draw_jog`でジョグする機構を実装した。実装過程で2つのバグを
修正した：(a) プローブのY範囲を誤ってピン自身の座標からtrack_yまで
全体にしていたため、行内部の無関係な標準セル配線（M1）まで拾って
誤検出していた——行の境界からtrack_yまでの、実際にチャネルに属する
区間だけに限定して修正。(b) 上記(2)と同じ理由でM1チェックが不要
だったため除去。

修正後、正しくスコープした状態で実行したところ、**176件のジョグが
必要**になり、これがchannel1/2/3に集中してトラック予算を大幅に
超過（channel1: 90/50、channel2: 126/55、channel3: 104/56）した。
これを解消するにはコア高さを約1093um→約1870um（+71%）に拡大する
必要があると判明。ユーザーに面積コストと短絡削減効果のトレードオフ
を提示して判断を仰いだ結果、**この機構は実装したまま無効化する
（`overlap_zone = {}`のオーバーライド）**方針で確定した。理由：
176件という数字自体が「この2つの隣接行ペアはそもそも同時に収まる
だけの配線資源がない」ことを裏付けており、対策すればするほど
コストが跳ね上がる領域だと分かったため、費用対効果の観点で今回は
見送りとした。

**結果**（(1)(2)を有効化、(3)は無効化した状態）：
- DRC：**0違反**（(1)(2)の追加トラック分だけchannel1を180→200umに
  拡大して確保）。
- 接続性検証：**短絡22件**（§38.7と同数）。ただし短絡ブロブ数は
  9個→7個に減少し、`scl_buf0`等4ネット自身の分断（NET SPLIT）は
  解消——これらのネット自体の配線は構造的に正しくなった。残る22件
  は要因1（分離予算超過）に起因する"素のcase 2"が支配的で、今回
  対策を見送った要因1②が対応すべき領域と考えられる。

**まとめ**：3つの対策のうち2つ（高FOネットの専用ガードトラック化、
4ネットの行ごと独立配線）を実装・実測で効果を確認した
（DRC 0違反、対象ネットの構造的な正しさを回復）。残る1つ
（重複ゾーンのライブチェック）は正しく実装できたが、面積コストが
釣り合わないと判断し無効化した状態で一旦区切ることにした。将来的に
コア面積の余裕ができた場合、または短絡ゼロが必須要件になった場合は
`overlap_zone = {}`のオーバーライドを外し、CH_HEIGHTSを再度拡大する
ことで再度有効化できる。

### 38.9 行ごと独立トランクのチャネル内配置をスタセル側に修正

§38.8実装時点では、`scl_buf0`/`scl_buf1`/`_126_buf0`/`_126_buf1`の
行ごとローカルトランク（Pass 0）は各チャネルの**トラックidx=0側**
（`track_y = ch_y0[channel] + TRACK0_OFFSET + idx*TRACK_PITCH`が
最小になる側、つまりチャネルの下端）に配置されていた。ユーザーから
「４本の専用トラックは、スタセルの下のチャネル領域の一番スタセルに
近い側のM1トラックを使うべきでは」との指摘を受けて検証したところ、
配置が逆になっていることが判明した：チャネルcは「行cの直下・行c-1の
直上」に位置するため、idxが大きいほどtrack_yは上がり行cに近づく。
行cのピンを持つローカルトランクが本来使うべきなのはidxが大きい側
（チャネル上端）だが、旧実装ではidx=0（下端＝行c-1に近い側）を
使っていた。

**修正**：`per_row_track_of`の割り当て順を変更し、Pass 1の通常ブロック
（row_below_upper / row_above_lower / adjacent_pair / spanning）を
高FOブロックの直後（idxの低い側）に詰め、行ごとローカルトランクの
ブロックをその後（idxの高い側＝チャネル上端＝行cに最も近い側）に
配置するよう並べ替えた。ブロック境界に新たに専用のガードトラックを
1本追加した（旧配置では末尾ガードが自然に下端境界を守っていたが、
新配置ではローカルトランクブロックが最後尾になるため、代わりに
先頭側の境界を明示的にガードする必要があったため）。

**結果**：DRC 0違反を維持したまま、接続性検証の短絡が
**22件→8件**に減少した。§38.8時点の22件は、`scl_buf0`等4ネットを
ハブとして無関係な多数のネットが1つの巨大なunion-findブロブに
連結される連鎖が支配的だったが、トランクをスタセル直近（M2スタブが
最短になる側）に寄せたことでこの連鎖の大部分が解消された。残る8件
はうち4件が高FO/per-row-localネットに直接触れる単発の短絡
（`txreg[1]`/`scl_n`、`_115_`/`_126_buf0`、`_080_`/`_126_buf1`、
`_195_`/`scl_buf0`）、残り4件は本修正と無関係な既存の単発短絡
（`_055_`/`bit_cnt[1]`、`_127_buf0`/`_059_`、`_058_`/`_172_`、
`_109_`/`shreg[5]`、§38.8以前から変化なし）。なお接続性検証には
本修正と無関係な既知の別問題（スパニングネットの一部ピンが
`PIN NOT FOUND ON M1`——22件、行なしベースラインでも同数）が
残っているが、これは今回のスコープ外。

### 38.10 短絡の正確な接触点ハイライト（ERRレイヤー拡張）

(253,0)の「ショートしたメタル形状」レイヤーは、Union-Findで
検出した連結成分（bad root）に属するM1/M2ポリゴンを丸ごとコピー
するもので、2ネットのどちらの配線かを問わず全体（特に高FOネットは
チップの広い範囲に及ぶ）をハイライトしてしまい、実際に2ネットが
接触している具体的な箇所を視覚的に特定しづらかった。ユーザーから
「同じチャネル内でX座標が一致し直接接触している箇所をハイライト
できるか」との要望を受け、新規レイヤー**(253,3)**を追加した。

**手法**：GDSに書き込み済みのマージ済みリージョンからは、一度
2ネットが接触して1つのポリゴンに融合すると「どちらのネットの
面積か」という情報は幾何学的に失われ復元不可能。そこで、GDSでは
なく`pin_map_nrow_fm.json`（各ピンのvia着地点＝(X,track_y)）と
配置JSON（各ピン自身の座標）から、`route_channels_nrow_fm.py`が
実際に描いたのと同じルールでネットごとに独立にM2スタブ（ピンから
track_yまでの縦方向矩形）とM1トランク（同一track_yを共有する
ピン群のmin~max X間の横方向矩形）を再構成し、2ネットそれぞれの
リージョンをklayoutの`Region`同士でAND演算して正確な重なり
ポリゴンを求める方式にした。ジョグ（行またぎ・スパイン）区間は
未再構成のため、それが接触箇所に絡む短絡は対象外。

**結果**：8件中6件を正確な重なりポリゴン（複数箇所ある場合は
すべて）としてピンポイント特定できた。残り2件
（`_126_buf0`/`_115_`、`_126_buf1`/`_080_`、いずれも行ごと
ローカルトランクのスパインジョグが絡む短絡）は今回の再構成手法では
特定できず、従来通り(253,0)の全体ハイライトのみで確認可能。
テキストレポート（`err_report_nrow_fm_details.txt`）にも各短絡ペア
の重なり座標（X範囲・Y範囲）を追記した。

### 38.11 残り2件（スパインジョグ絡み）の衝突位置も特定

§38.10のpin_map/配置ベースの再構成は、ピンのスタブ＋トランクしか
再現できず、行ごとローカルトランクの**スパインジョグ**（行をまたぐ
際の迂回セグメント：`draw_jog`が生成するM2縦区間・M1ジョグトランク・
row本体を貫通する縦区間）が絡む2件（`_126_buf0`/`_115_`、
`_126_buf1`/`_080_`）は再現できなかった。原因を裏付けるため、
GDSから幾何を復元するのではなく、`route_channels_nrow_fm.py`側で
実際に描画したM1/M2ボックスを**描画時にネット名付きでそのまま
記録**する方式に変更した（`m1_box`/`m2_box`をラップし、各パスの
`for net in ...:`ループ先頭で`cur_net`をセット、
`net_shapes_nrow_fm.json`へダンプ）。これによりジョグ・スパイン
区間を含む全セグメントが100%正確にネットへ紐付く。

`gen_err_report_nrow_fm.py`はこのログがあれば優先的に使用し
（無ければ従来のpin_mapベース再構成にフォールバック）、
`net_region()`で2ネットの全区間をAND演算する。

**結果**：8件中**8件全て**を正確な重なりポリゴンとして特定できた。
残り2件の実体は、スパインが行をまたぐ際に`find_row_clear_x`で
選んだクリアなX（その行内のピンとの衝突は避けている）が、
**別の行/チャネルにある無関係ネットのM2スタブとは衝突しうる**
というもの——`find_row_clear_x`は「その行」のクリアランスしか
見ておらず、スパインの縦区間が貫通する隣接チャネルの占有状況は
チェックしていないため。DRC 0違反・短絡8件（変化なし、今回は
可視化の精度向上のみ）を確認しコミットした。

### 38.12 スパイン修正を試行→コスト過大のため見送り（ロールバック）

§38.11で特定した2件（`_126_buf0`/`_115_`、`_126_buf1`/`_080_`）に
対し、スパインの行またぎ処理にチャネル分のライブチェックを追加する
修正を試みた。実装は以下のように段階的に複雑化した。

1. パス0b（スパイン）をパス1/2の**後**に実行するよう再構成
   （ライブチェックが他ネットの最終形状を見られるようにするため）。
2. `find_row_clear_x`に加えてチャネル区間もチェックする
   `find_row_and_channel_clear_x`を追加。
3. ジョグ先の着地点（`claim_track`が返すトラック）が自ネット自身の
   ビアパッド・スタブと「衝突」する誤検知を発見・除外
   （`foreign_channel_clear`：`net_shapes`ログで自ネット分を除外）。
4. チェック範囲が上限なく広がり無関係な形状に必ず当たってしまう
   バグを発見・修正（トラック候補をチャネル自身の物理範囲に制限）。
5. DRCマージン込みの判定が長い区間では厳しすぎ、実際には短絡して
   いない“隙間2.0um”程度の近接まで拒否してしまう問題を発見・修正
   （パッド実寸のみで判定するよう変更）。
6. `place_via()`が生成するvia_1 PCell自体のパッド形状が`net_shapes`
   に記録されておらず、自ネット除外が不完全だった問題を発見・修正。

ここまで直してようやく`_126_buf0`側は解消したが、**チャネルの
トラック予算がほぼ満杯**で、ライブチェックが「空いているY」を
探しても物理的な空きがなく、CH_HEIGHTSを90→280um、220→320um、
224→320um（コア高さ換算で約+25%）まで拡大してようやくエラーなく
通るところまで到達した。

**それでもなお短絡は8→9件に悪化**：ターゲットの2件は未解決のまま
残り、`scl_buf0`のスパインが別ルートに逃げた結果、新たに
`_070_`/`scl_buf0`、`sda_in_buf1`/`_195_`という短絡が発生した。
チャネルが密集しているため、1箇所の衝突を避けると別の場所で衝突する
「もぐらたたき」状態であることが判明した。これは§38.8で見送った
要因1②（重複ゾーンライブチェック、176ジョグ・+71%面積）と
本質的に同じ構造の問題である。

ユーザーに状況を報告し、対応方針を確認した結果、**この変更は
ロールバックし**（`git checkout`でコミット済み状態に復元）、
代わりに残る6件（スパイン非関連の単純なcase 2）への対策を先行する
方針となった。

なお副産物として、これまで「原因不明の別問題」として保留していた
22件の`PIN NOT FOUND ON M1`は、`verify_connectivity_nrow_fm.py`の
デフォルトスキャン範囲（Y≤900, 幅≤1500）がコア全体をカバーして
いなかっただけの見かけ上の問題（コアは既にY=1093/幅1620だった）で
あり、実害はないことを確認した。以後はスキャン範囲を明示的に指定
して実行する。

### 38.13 残り6件への対策：既存の重複ゾーン機構を対象ネット限定で流用

§38.8で実装済みの「重複ゾーン限定ライブチェック」（要因1②、
`channel_clear`+`find_channel_clear_x`+`draw_jog`）は、**ゾーン全体に
適用すると176ジョグ・+71%面積で見送った**が、同じ仕組みを
**残り6件で名指しされた低FO側ネット6本だけ**に絞って適用すれば、
コストを抑えつつ効果を得られるはずと考え実装した。

対象：`txreg[1]`、`_195_`、`_055_`、`_059_`、`_172_`、`_109_`
（いずれも§38.10で特定した6件の短絡の、ファンアウトが小さい側）。

**実装**：`FORCE_JOG_NETS`定数でこの6ネットを指定し、通常のパス1
（row-only/adjacent-pair）のプールから除外して、パス1・パス2が
すべて完了した**最後**に専用パスとして描画する。ゾーン判定
（`overlap_zone`メンバーシップ）を条件にせず、**無条件**でチャネル
区間のライブチェックを行い、衝突があればジョグする。既存の
`channel_clear`/`find_channel_clear_x`/`draw_jog`をそのまま再利用。

**結果**：17ピンがジョグされ、channel1のトラック予算超過が発生した
ため CH_HEIGHTS を [90,200,220,224,100] → [90,260,240,224,100]
（channel1: +60um、channel2: +20um）に拡大して収めた。DRC 0違反を
維持したまま、**短絡8件→6件**（-25%）に減少：`_195_`/`scl_buf0`、
`_127_buf0`/`_059_`、`_058_`/`_172_`の3件は完全解消。残り3件は
`_055_`/`bit_cnt[1]`と`_109_`/`shreg[5]`（ジョグはしたが対象ネットの
トランクが広く複数箇所で再衝突、部分改善に留まる）、および
`txreg[1]`/`scl_n`系（ジョグにより`_080_`を介した2ホップの連鎖に
組み替わり、`_126_buf1`/`scl_n`という新規短絡も1件発生）。

スパイン絡みの2件（`_126_buf0`/`_115_`、`_126_buf1`/`_080_`、§38.12で
見送り）は今回のスコープ外のため変化なし。合計では6件（スパイン
絡み2件＋今回の残留3件＋新規1件）。DRC・面積コストとも許容範囲内で
着地したと判断しコミットする。

### 38.14 `scl_n`を行ごと独立配線（Pass 0）に追加

ユーザー指示：「`scl_buf0`/`scl_buf1`/`_126_buf0`/`_126_buf1`の4ネットに
加えて`scl_n`も優先配線に加える」。`scl_n`（10ピン）はこのセッション中
一貫して短絡の当事者になり続けていた（`scl_n`/`txreg[1]`→
`scl_n`/`_126_buf1`→`scl_n`/`_080_`と相手を変えながら）ため、他の4
ネットと同じ「行ごと独立トランク＋スパイン」方式に切り替える。

**実装**：`PER_ROW_LOCAL_NETS`に`"scl_n"`を追加するのみ。分類ループが
`PER_ROW_LOCAL_NETS`のメンバーシップを高FO判定より先にチェックする
構造になっているため、追加の分岐なしで`scl_n`は自動的にパス0（高FO
から2ネットに減少、行ごと独立配線ネットは5ネットに増加）に流れる。

**結果**：DRC 0違反を維持したまま、`scl_n`自身が絡む短絡は
`scl_n`/`_126_buf1`と`scl_n`/`_080_`（`txreg[1]`との連鎖の一部）の
2件が解消した。ただし`scl_n`の新しいスパインが別のX位置を通ることで
`bit_cnt[2]`/`scl_n`という新規の短絡が1件発生し、さらに無関係な
`bit_cnt[1]`/`_115_`という短絡も1件新たに現れた（他ネットのトラック
構造割当てがわずかにシフトした副作用）。差し引き**短絡6件→7件**と
件数としては1件増加したが、これは§38.12/38.13で繰り返し確認している
「密集したチャネルでは1箇所を避けると別箇所で衝突する」現象の再現
であり、`scl_n`自体の短絡の性質（無関係な複数ネットを巻き込む幅広
トランクのハブ）は解消されている。ユーザーの明示的な指示に基づき
実装・コミットする。

### 38.15 ジョグ挿入箇所そのものを可視化する専用ERRレイヤー(253,4)

ユーザーからの指摘：「253/3のM2(6箇所)を見る限りFORCE_JOG_NETSで
衝突回避できると判断しているが、クリアーできていないのは何か不具合
がある可能性がある」。§38.13で実装したFORCE_JOG_NETSは6ネットを
対象にしたはずが、§38.14終了時点で短絡7件中3件（`_055_`/`bit_cnt[1]`、
`_109_`/`shreg[5]`、および連鎖した`txreg[1]`系）がFORCE_JOG_NETS
対象ネット自身のまま残っており、ユーザーの指摘通り「ジョグを入れた
はずなのに衝突が消えていない」状態だった。

原因を特定するには、ジョグが実際にどこへ挿入されたかを短絡箇所
(253,3)と重ねて視覚的に照合する必要がある。そこで`route_channels_
nrow_fm.py`のFORCE_JOG_NETSパスに`force_jog_events`ログ（ネット名・
ジョグ元/先X・ジョグ先Y・チャネル番号を記録）を追加し、
`force_jog_events_nrow_fm.json`へダンプ。`gen_err_report_nrow_fm.py`
側に新規レイヤー**(253,4) ERR_FORCE_JOG**を追加し、各ジョグのM1区間
と両端のvia位置を`JOG-FROM:net:inst.pin`/`JOG-TO:net:inst.pin`ラベル
付きで描画するようにした。

### 38.16 (253,4)で発覚した実バグ：`draw_jog`の「離脱区間」未チェック

(253,4)と(253,3)を重ねて見た結果、`net_shapes_nrow_fm.json`の生ログ
（ネット`_109_`）から実際のバグを特定できた：

```
M2 708.4 750.9 711.8 784.4   <- 行本体からの引き出し区間（約33.5um、正常）
M2 708.4 784.4 711.8 946.4   <- 問題箇所：ジョグ先トラックまでの162umの
                                 縦走区間が「元のX」のままノーチェックで
                                 描かれている
M1 710.1 945.5 823.5 947.3   <- ようやくclear_xへ到達する横走区間
```

`draw_jog`はM2の最初の区間（"離脱区間"）を**ジョグ元の座標のまま**
`cur_y`から`jog_y`まで縦に伸ばす。`jog_y`は`claim_track`が返す
「そのチャネルで次にまだ使っていないグローバルなインデックス」で
決まるが、FORCE_JOG_NETSパスはパイプラインの**最後**に走るため、他
の全パスが低インデックスのトラックを使い果たした後になる。結果、
`jog_y`が`cur_y`から遠く離れ、離脱区間がチャネルの高さのほぼ全体に
及ぶ縦走となり、しかもこの区間自体はライブチェックされないため、
**まさに回避しようとしていた衝突相手を再び貫通していた**。

**対策**：`claim_track_near(channel, cxs, target_idx)`を新設。
`target_idx`から外側へ（+1/-1交互に）探索し、未使用かつ衝突しない
最も近いインデックスを選ぶ（`claim_track`のような単調増加ではなく、
チャネルの物理トラック予算内に限定）。`draw_jog`に`near_y`引数を追加
し、指定時は`claim_track_near`で`channel_entry_y`に最も近いトラック
を選ぶようにした（離脱区間の長さを物理的に可能な限り短くする）。
FORCE_JOG_NETSパスの呼び出し箇所に`near_y=channel_entry_y`を設定。

**結果**：DRC 0違反を維持したまま、**短絡7件→4件**に減少（`_109_`/
`shreg[5]`、`bit_cnt[1]`/`txreg[1]`連鎖、`bit_cnt[1]`/`_115_`が解消）。
CH_HEIGHTS変更なし（既存の[90,260,240,224,100]のまま収まった）。
残る4件：`bit_cnt[2]`/`scl_n`（§38.14の副作用）、`_115_`/`_126_buf0`・
`_080_`/`_126_buf1`（§38.12で見送ったスパイン絡み、スコープ外）、
`_055_`/`bit_cnt[1]`（FORCE_JOG_NETS対象だが未解消のまま残留）。

**`_055_`/`bit_cnt[1]`の追加調査**：(253,3)の残留オーバーラップが
`_055_`の5ピンの元のX位置全てに散らばっていたため、離脱区間と同種の
もう一つの未チェック区間——ジョグ判定前に無条件で描く「行本体からの
引き出し区間」（`pin_edge_y`→`channel_entry_y`、常に元のcxのまま）
——が疑われた。試しにライブチェックの範囲を`channel_entry_y`起点から
`pin_edge_y`起点（行本体の引き出し区間も含む）に拡張したところ、
**DRC違反が0→2件、短絡が4件→9件に悪化**した。§38.12のスパイン修正
と同型の「密集チャネルでのモグラ叩き」（1箇所を避けると別箇所で新規
衝突）が再現したため、この変更は破棄し§38.16の状態（短絡4件、DRC
0違反）へ戻した。`_055_`/`bit_cnt[1]`は今回のスコープでは解決に
至らず、既存のトラック予算内では現在の手法での改善余地は乏しいと
判断し、現状で確定とする。

### 38.17 Pass 0スパインのトラック集約：ネットごとに1チャネル1トラックへ

ユーザーからの指摘：優先ネット（行ごと独立配線、`_126_buf0`/`_126_buf1`/
`scl_buf0`/`scl_buf1`/`scl_n`）を`net_shapes_nrow_fm.json`で確認すると、
「行本体直下の自トランク」と「行をまたぐスパイン」が同じチャネル内で
別々のM1トラックを使っていた（例：`_126_buf0`はchannel1で2トラック、
channel2で3トラック消費）。原因は、スパインのチャネル横断部分が既存の
`draw_jog`/`claim_track`（汎用共有トラックプール）をそのまま流用して
おり、そのネットがすでにそのチャネルに持っている専用トラック
（`per_row_track_y`）を再利用していなかったため。

**ユーザー指示の実装方針**：
1. 同一ネットがスパインで横断する全チャネルに、行に近い側から1本の
   専用トラックを予約（`per_row_channels_of`：ピンを持つ行だけでなく
   通過チャネルも含む）。
2. その専用トラックを最優先で配線し終えてから、他ネットを配線する
   （Pass 0が最初に走る既存の順序を維持）。
3. X座標が同じM2との衝突は、既存の`channel_clear`/`find_channel_clear_x`
   によるライブチェック＋ジョグ挿入（Y＝固定トラックのまま、X方向のみ
   ジョグ）で回避。ライブチェックが直接着地不可と判定した場合のみ、
   `claim_track_near`ベースの汎用フォールバックに退避（現行データでは
   未発火）。
4. 行本体を貫通してチャネルをまたぐ既存機構（`find_row_clear_x`＋M2
   縦走）はそのまま維持。

**実装上の落とし穴**：初回実装では、チャネル着地のライブチェック
（`channel_clear`）が、直前に自ネット自身が描いた行貫通区間の上端
（`cur_y`）と厳密に同じYで境界タッチしてしまい、常に「自分自身」を
衝突と誤検知していた（9/9件が誤って汎用トラックへフォールバック、
DRC 0→2件・短絡4→8件に悪化）。チェックの開始Yを`cur_y`から0.01um
内側にずらす（描画範囲は元のまま）ことで解消し、9/9件とも専用トラック
への直接着地に成功した。

**結果**：DRC 0違反、短絡4件（`bit_cnt[2]`/`scl_n`、`_115_`/`_126_buf0`、
`_080_`/`_126_buf1`、`_055_`/`bit_cnt[1]`）とも§38.16の状態から変化
なし——トラック浪費という別課題を解消しつつ、既存の短絡状況を悪化
させずに達成できた。チャネル別トラック使用数（channel1: 65/65、
channel2: 60/60、channel3: 56/56）も変化なし（元々ぎりぎり満杯だった
ため、今回の削減分は別の配線がすぐ埋めた形）。

ユーザー指示に基づき、Pass 0の各処理ステップ（`trunk`/`row_jog`/
`row_crossing`/`channel_land`/`channel_jog_fallback`/`trunk_land_jog`）
を`per_row_spine_events_nrow_fm.json`にログし、
`gen_spine_step_highlight_nrow_fm.py`で(256,0)〜(256,4)の専用ERR
レイヤーに可視化（ネット名ラベル付き）。既存の(253,x)短絡ハイライトも
本変更後の状態で再生成し、最終的な残存短絡を別レイヤーで確認できる
ようにした。

### 38.18 残り3件の切り分け：`bit_cnt[2]`追加は成功、`_115_`/`_080_`/`_055_`は断念

ユーザーから残り4件の性質を2種類に整理する指摘があった：(1)スタセルを
またいだM2でのショート（`_126_buf0`/`_115_`、`_126_buf1`/`_080_`、
`scl_n`/`bit_cnt[2]`——優先ネットのスパイン/トランクと、後から描画される
別ネットのピンが同じXで衝突）、(2)上下スタセルからのM2引き出しでの
ショート（`bit_cnt[1]`/`_055_`）。

**(1)への対応**：3件とも、Pass 0（優先ネット）が最初に描画され、その
時点で存在しない「後から描かれるネット」のピンとは衝突チェックできない
ことが原因と特定。該当ネット（`_115_`、`_080_`、`bit_cnt[2]`）を
FORCE_JOG_NETSに追加すれば、Pass 0後の専用パスでPass 0の実体を含めて
ライブチェックできるはずと考え、3件同時に追加して検証。結果：
`scl_n`/`bit_cnt[2]`は解消したが、`_115_`/`_080_`は衝突相手が別ネット
（`_034_`、`_076_`）に移動しただけで解消せず、さらに新たな短絡連鎖
（`_082_`/`_083_`/`_084_`/`_034_`）まで発生し、合計4→6件に悪化。
`bit_cnt[2]`のみ単独で追加し直したところ、DRC 0違反を維持したまま
**短絡4件→3件**に改善。`_115_`/`_080_`はFORCE_JOG_NETSに追加せず、
現状（`_126_buf0`/`_126_buf1`との短絡）のまま残す。

**(2)への対応（`_055_`/`bit_cnt[1]`、なぜジョグが効かないか）**：
`net_shapes_nrow_fm.json`と実GDSの接続性解析を突き合わせて根本原因を
特定。`_055_`の`_423_.B`ピン（row0, x=494.1）と`bit_cnt[1]`の`_464_.Q`
ピン（row1, x=494.1）が**偶然全く同じX座標**を持つ。`_423_.B`は
FORCE_JOG_NETSの対象でチャネル区間（`channel_entry_y`→`track_y`）は
ジョグ済み（clear_x=542.7）だが、ピン自身の行内引き出し区間
（`pin_edge_y`→`channel_entry_y`）は無条件・無チェックで元のX=494.1の
まま描画されており、ここで`bit_cnt[1]`の同一X上のvia列と直接接触して
いた。

対策として、行内引き出し区間を独立してライブチェック＋ジョグする
「2段階ジョグ」を実装（それぞれ局所的な範囲でのみクリアX探索を行い、
§38.16で悪化した「1回の大きな範囲探索」の再発を避ける設計）。実装は
成功したが、実行するとchannel1のトラック予算が枯渇（65/65が満杯で
新規トラックを確保できずクラッシュ）。CH_HEIGHTS[1]を340um（+80um）
まで拡大してようやく完走したものの、結果はDRC 0→3件・短絡3→15件と
大幅に悪化（`scl_n`→`_191_`→`_038_`→...→`_055_`まで連鎖する12ネットの
巻き込み）。§38.12と同型の「モグラ叩き」が、今回は非常に大規模な形で
再現したため、この変更は全て破棄（`git checkout`でCH_HEIGHTS拡大・
2段階ジョグとも元に戻す）し、`bit_cnt[2]`追加のみを再適用した状態
（短絡3件、DRC 0違反）を最終形として確定する。

**結論**：`_115_`/`_080_`/`_055_`/`bit_cnt[1]`が絡む残り3件の短絡は、
このチャネル密度・このジョグ機構では、局所修正が別箇所への衝突移動を
招く構造的な限界に達していると判断する。追加のクリアは、チャネル
全体を大幅に拡大するか、ルーティングアルゴリズムそのものをより
大きく見直す必要があり、今回のスコープでは見送る。

### 38.19 スパイン横断候補に既存ピンXを再利用（自己除外つき）

ユーザー提案：「既にスタセルに配線したM2をそのまま横断候補に使えるか」
——回答は「同一ネットの既存ピンX座標を再利用」。スパインが行を貫通する
際、`find_row_clear_x`による新規クリアX探索の代わりに、そのネットが
既にその行に持つピン位置（`row_trunk_xmin`）をそのまま再利用する案。

**単純な実装（ライブチェックなし）**：短絡3→7件に悪化。`_115_`/
`_126_buf0`は解消したが、既存ピンXの列が実は他ネットの本物のM2と
交差しており、新たな短絡（`phase[2]`/`scl_buf0`、`_137_`/`scl_buf1`、
`scl`/`_126_buf0`など）が発生した。

**自己除外つきの実装**：`row_clear`を、そのネット自身の`net_shapes`
ログ上のM2だけを除外して判定する`row_clear_self_excluding`に変更し、
既存ピンXが「他ネットとは無衝突」の場合のみ採用、そうでなければ従来の
`find_row_clear_x`にフォールバック。結果は元と全く同じ（短絡3件、
ジョグ数18件で不変）——実際には既存ピン列に他ネットの本物のM2が
存在するケースが大半で、常にフォールバックしていた。改善はなかったが
より正確な実装として維持する。

### 38.20 スパイン横断をPass 1完了後に延期（試行、悪化のため差し戻し）

ユーザー提案：「Pass1が終わった後でスパイン横断処理をする」。Pass 0が
最初に走ることが§38.18の根本原因（後から描かれるPass 1のネットとは
衝突チェックできない）だったため、スパイン横断（トランク自体は
そのまま最初に描画、横断だけ分離）をPass 2（spanning）の後・Pass 3
（FORCE_JOG_NETS）の前に移動。構造的なトラック/チャネル割当ては
全ネット共通で全パス開始前に確定済みのため、描画順序を変えるだけなら
他パスの結果に影響しないはずと判断した。

**結果**：DRC 0→6件（M1間隔違反）、短絡3→6件に悪化
（`scl_buf1`/`_126_buf1`、`_080_`/`scl_buf1`、`shreg[5]`/`_080_`など、
新規短絡が発生する一方`_115_`/`_080_`は解消せず）。Pass 1完了後という
「より情報のある」タイミングで探索しても、密集チャネルでは別の衝突
（今度はPass 1自身のM1トランクとの間隔違反まで）を新たに招く形で
悪化した。§38.12/38.16/38.18で繰り返し確認している「モグラ叩き」が
またしても再現したため、この変更は破棄（`git checkout`で差し戻し）し、
§38.18の状態（短絡3件、DRC 0違反）を最終形として維持する。

**総括**：`_115_`/`_080_`短絡の解消を目的とした4つの独立したアプローチ
（FORCE_JOG_NETS追加、行内引き出し区間の分離ジョグ、既存ピンX再利用、
スパイン横断の延期）を試したが、いずれも密集したchannel1における
「1箇所を直すと別箇所で衝突する」という構造的な壁に阻まれた。この
短絡2件と`_055_`/`bit_cnt[1]`の1件、計3件は、チャネル面積の大幅拡大か
ルーティングアルゴリズムの本質的な見直しなしには解消が難しいという
判断で確定する。

### 38.21 アルゴリズムの本質的な見直し（試行、いずれも悪化のため差し戻し）

ユーザー承認のもと、「その場しのぎの場当たり的なチェック追加」から
脱却する2段構えの根本的見直しを試みた。

**(A) 静的ピンX予約 + ピン一致プリパス**：`net_pins`構築直後、配線を
一切描く前の段階で、行ごとのピンX座標テーブル（`row_pin_xs`）を作り、
隣接行で同一X（またはごく近いX）を持つ異なるネットのペアを検出して
`FORCE_JOG_NETS`へ動的昇格させる案。しきい値`M1_PAD_SIZE+M2_MIN_GAP`
（約4.8um）で実装したところ180件以上のネットが「衝突」と判定され、
`claim_track_near`のトラック枯渇でクラッシュ。しきい値を0.5umまで
絞っても90件以上が該当し同様にクラッシュした。

**根本原因の気づき**：本設計は5.4umグリッド上に同形の標準セル列が
繰り返し並ぶため、隣接行でピンXが一致すること自体はごく普通に起こる
（異常ではない）。本当のリスク指標は「X座標の一致」ではなく、両ネットの
M2スタブが実際にチャネル内で**Y方向にも重なる**かどうかである。この
点ベースのプリパス案は前提が誤っていたため全体を破棄
（`git checkout`で該当ファイルを差し戻し）。

**(B) 静的スタブ範囲チェック（`static_stub_spans`/`static_stub_blocked`）**：
(A)の反省を踏まえ、`non_spanning_nets`+`high_fo_nets`の各ネットについて
「そのピン自身が最終的に辿る経路（ピン端Y〜`track_y`、X＝ピンXそのもの）」
を配線前に静的に算出し、`channel_clear`・`row_clear`双方に、候補位置が
他ネット自身のスタブとX・Y両方向で物理的に重なっていないかを追加
チェックする形で組み込んだ（順序に依存しない、配置データのみに基づく
判定）。

*結果*：クラッシュはせず完走。しかしDRC 0→2件（M1間隔違反）、
短絡3→4件に悪化——`_115_`/`_126_buf0`と`_080_`/`_126_buf1`は解消せず
残存したまま、新規に`_109_`/`shreg[5]`の短絡が発生した。

*原因の特定*：`per_row_spine_events_nrow_fm.json`を突き合わせたところ、
新しい静的チェックは狙いどおり`_126_buf0`のチャネル着地候補の衝突を
正しく検出し、`channel_jog_fallback`（フォールバック経路）への切替えを
引き起こしていた。しかし`draw_jog`の`near_y`アンカー付き
`claim_track_near`自身が、進入点（y≈479.6）付近に空きトラックを
見つけられず（channel1がほぼ満杯のため）、遠く離れた位置
（y=597.6、約118um先）にジョグ先を確保。その結果、進入点から
ジョグ先までを結ぶ「出発区間」（`draw_jog`の設計上、常に無条件・
無チェックで元のX座標のまま引かれる最初のM2区間）が、まさに
`_115_`のスタブを再び横切ってしまう——§38.16で発見・修正した
「出発区間未チェック」バグと全く同じ構造の問題が、今度は
`channel_jog_fallback`経路自身の内部に露見した形である。`_109_`/
`shreg[5]`も同一パターン（`_109_`のFORCE_JOG_NETSジョグの出発区間が、
チャネルの空きトラック位置が変わったことで新たに`shreg[5]`の
スタブと交差するようになった）と判断した。

**結論**：静的・順序非依存の衝突検出そのものは正しく機能するように
なったが、それは「衝突を検出した後の退避先（フォールバック）自体が
チャネル満杯時には安全な退避経路を持たない」という、より深い構造的
欠陥を露呈させただけだった。`draw_jog`の出発区間を再帰的に
チェック＆探索する形に作り直すか、`CH_HEIGHTS`を拡大してフォール
バックに余地を持たせるかしない限り、この種の問題は解消しないと
判断する。今回の変更は全て破棄（`git checkout`で差し戻し）し、
§38.20末尾の状態（短絡3件、DRC 0違反）を最終形として確定する。

**総括（38.18〜38.21）**：残り3件の短絡解消に対し、場当たり的な
ジョグ対象追加から、チェックのタイミング変更、ピン再利用、そして
アルゴリズムの本質的見直しまで、合計7つの独立したアプローチを
試みたが、いずれも密集したchannel1/channel2（トラック使用率
ほぼ100%）における構造的な壁——「1箇所の衝突を解決する退避経路が、
別の場所で新たな衝突を生む」——に阻まれた。これ以上の解消には、
チャネル面積の拡大（レイアウト密度を下げる）か、ジョグの退避経路
自体を含めた全面的なアルゴリズム再設計が必要と判断し、現状
（短絡3件、DRC違反0件）を本セクションの最終形として確定する。

### 38.22 draw_jogの改善（ユーザー指示、成功——短絡3→1件）

§38.21で判明した根本原因（`draw_jog`のフォールバック自体の出発区間が
未チェックで、チャネル満杯時には安全な退避先を持たない）を受け、
ユーザーから「draw_jogを改善して試行してください」と指示を受けた。
2段構えで実装。

**(1) 静的スタブ範囲チェックの再導入**：§38.21で構築した
`static_stub_spans`/`static_stub_blocked`（順序に依存しない、配置
データのみに基づくスタブ重なり判定）を再度導入し、`channel_clear`・
`row_clear`双方に組み込んだ。

**(2) `draw_jog`自身の出発区間チェック**：`claim_track_near`に
`extra_ok(idx, jog_y)`という追加検証コールバックを渡せるように拡張。
`draw_jog`が`near_y`アンカー付きで呼ばれる場合、単に「空いている
最寄りトラック」を採用するのではなく、各候補トラックについて
「出発区間（`cur_x`列、`cur_y`から`jog_y`まで）が実際に
クリアかどうか」（`channel_clear`＝ライブ形状＋静的スタブの両方）も
検証し、クリアな候補が見つかるまで外側へ探索を継続するように変更。
（自己衝突回避のため、`cur_y`側の端だけ§38.17と同じEPS=0.01の
ナッジを適用）

*最初の結果*：Pass 0のスパイン横断（`_126_buf0`/`_126_buf1`）で即座に
クラッシュ（`claim_track_near: ... satisfying all checks`）。原因は
根本的：`_115_`のスタブが対象チャネルのほぼ全高（約210um）を
`cur_x`列で専有しているため、**同じ`cur_x`のまま**Y方向にどれだけ
探索してもこの障害物を回避できるトラックは存在しない——出発区間の
「安全な退避先を探す」というアプローチそのものが、この種の障害物には
原理的に無力であると判明した。

**(3) 根本修正：行貫通探索とチャネル着地判定の統合**：出発区間検証を
後から強化するのではなく、Pass 0のスパイン横断ループを書き換え、
「行の本体を貫通できるX」（`find_row_clear_x`／`row_clear`）を探す
段階そのものに、「その同じXが次チャネルの固定トラックへ問題なく
着地できるか」（`channel_clear`、対象は次チャネル進入Y〜着地
`track_y`）という条件を**あらかじめ組み込んで**探索するよう変更
（`find_row_clear_x`に`extra_ok`引数を追加）。これにより、後から
ジョグで逃げる必要のあるXを最初から選ばなくなり、フォールバック
自体がほぼ発生しなくなった。フォールバック機構と(2)の出発区間
チェックはセーフティネットとして維持（万一発生した場合はクラッシュ
せず、警告を出して従来どおりの未チェック挙動にフォールバックする形に
変更——チャネル容量の限界そのものは、パイプライン全体を止める理由には
しない）。

**結果**：DRC 0違反（変わらず）、短絡**3→1件**に改善。
`_115_`/`_126_buf0`、`_080_`/`_126_buf1`の2件が解消し、Pass 0の
スパイン横断は50イベント全てが`channel_jog_fallback`を一度も使わずに
完走した（`trunk`/`row_jog`/`row_crossing`/`channel_land`/
`trunk_land_jog`のみ）。FORCE_JOG_NETSパス（`_055_`含む）は18件の
警告（出発区間の完全クリアなトラックが見つからず旧来の挙動に
フォールバック）が出たが、これらは全て実害なく完走した。

**残る1件（`_055_`/`bit_cnt[1]`）は§38.16/38.18で既に特定した、
構造的に異なる種類の問題である**：`_423_.B`（`_055_`側）と`_464_.Q`
（`bit_cnt[1]`側）のピンが行をまたいでX座標完全一致しており、
FORCE_JOG_NETSパスがチャネルへの経路（`channel_entry_y`〜`track_y`）
はライブチェック＋ジョグで保護しているが、ピン自身の実座標から
行境界までの「行内アプローチ区間」（`pin_edge_y`→`channel_entry_y`）は
**実ピンのX座標そのもの**で無条件に描画される。このXは物理的な
ピン位置であり移動できず、しかも行本体にはM1トラックが存在しない
（トラックはチャネルにのみ定義される）ため、Pass 0で解決した
「チャネル着地Xを事前に選び直す」という手法がこのケースには
原理的に適用できない。§38.18の結論（チャネル面積拡大か、行内
ジョグを新たに支える機構の追加が必要）は変わらず有効と判断する。

per_row_spine_events_nrow_fm.json / force_jog_events_nrow_fm.json /
net_shapes_nrow_fm.json / err_report / spine_steps GDS を再生成し、
`gen_err_report_nrow_fm.py`の`CH_HEIGHTS`が
`route_channels_nrow_fm.py`と食い違っていた既知の不整合
（`[90,180,220,224,100]` → 正しくは`[90,260,240,224,100]`）も
この機会に修正した。

### 38.23 draw_jogフォールバックのトラック再利用（ユーザー指摘、部分改善）

ユーザーがKLayoutのスクリーンショットで「チャネル内に未使用に見える
M1トラックがある」「特定の配線が無駄に迂回している」と指摘。検証の
結果、両方とも正しいことが確認された：

**未使用トラックの検証**：net_shapes再構成とGDS直接クエリの2通りの
独立した方法で、各チャネルのトラック使用状況を突き合わせたところ
完全一致（channel0: 使用13/空き9、channel1: 54/11、channel2: 38/22、
channel3: 32/24、channel4: 10/15）。ただし空きトラックは「クレーム
（予約）されていない」トラックであり、`_423_.B`の場合、空きは全て
`bit_cnt[1]`のスタブより上側（y=308.8以降）にしかなく、下側（安全な
側）は全て**別の無関係なネット**にクレーム済みだった。

**迂回配線の正体**：ユーザーが指摘した長いM1配線は、`_055_`の
`_415_.B`ピンのジョグ配線と判明。同じ「出発区間の安全なトラックが
見つからない」失敗パターンにより、本来近くに着地すべきところ151um
先まで飛ばされていた（たまたま衝突を起こさなかっただけ）。

**実装**：`claim_track_near`に`allow_claimed`パラメータを追加。
既存の「未クレームのトラックのみ」の探索が失敗した場合、2段目として
「他ネットが既にクレーム済みのトラックでも、そのネットが実際に描画した
M1形状と物理的に重ならなければ再利用可」というティアを追加。新設の
`m1_run_clear(y, x0, x1)`（ライブM1形状の直接クエリ）でこれを判定。

**結果**：`_050_`〜`_371_.A`（x=1050.3）付近で1件、実際にトラック
再利用が成功（`INFO: draw_jog reused an already-claimed track`）。
DRC・短絡数に変化なし（0違反・1短絡のまま、既存の`_055_`/`bit_cnt[1]`
は不変）——安全に容量を追加活用できることを確認。

**`_423_.B`自体は再利用でも解消しなかった理由（詳細調査）**：
デバッグ計装で全候補トラックの失敗理由を追跡した結果、`bit_cnt[1]`の
スタブが原因ではなく、**行内で直接隣接する2つの別ネットのピン**
（x=487.0とx=497.8、`_423_.B`からわずか5.4um＝1グリッド分の距離）
の出発区間（y=121.4〜176.8および121.4〜196.8）が、行/チャネル境界
（y=154.8）の直後からM2クリアランスを塞いでいることが判明。ジョグの
出発区間は`cur_y=154.8`から連続した1本の垂直配線である必要があり、
どの`jog_y`を選んでも、その経路は必ずこの隣接ピンの塞がった区間
（154.8直後）を通過しなければならない——`bit_cnt[1]`のスタブが
無かったとしても、この2つの隣接ピンだけで既に脱出不可能な状況
だった。

これは「トラックの空き容量」の問題ではなく、「出発点そのものが
両隣のピンに挟まれて身動きが取れない」という、さらに一段階根本的な
制約であることが分かった。トラック再利用機構は他の箇所（`_371_.A`）
では実際に容量を活用できたため保持する価値はあるが、`_055_`/
`bit_cnt[1]`の解消には別のアプローチ（行内での早期X移動を可能にする
新しい機構、あるいはセル配置自体の変更）が必要と判断する。

### 38.24 早期ジョグ提案＋境界値の丸め誤差修正（ユーザー提案、解決）

ユーザーがKLayout上で「トラック再利用が可能なら、黄色（M1）で早い
段階で横に逃げて、紫（M2）で目的のトラックまで登れば回避できるのでは」
という具体的な配線案をスクリーンショットで提示。§38.23の結論
（「両隣のピンに挟まれて出発直後から身動きが取れない」）と一見矛盾する
提案だったため、ライブ計装で再検証した。

**検証１（M2ライブチェック）**：`channel_clear`の判定に使っている
`begin_shapes_rec_touching`（境界が触れているだけでも衝突とみなす）
と、`begin_shapes_rec_overlapping`（実際に面積を持って重なっている
場合のみ衝突）を両方計測。y=154.8〜168.8等の短い出発区間では
touch_cnt>0だがoverlap_cnt=0——つまり隣接ピンとの間隔は法定最小値
（2.0um）ちょうどで、実際には違反していない（`_423_.B`自身の
行内区間y=121.4〜154.8も同じ間隔で既にDRC適合済み）。`_touching`は
この「ちょうど最小値」を誤って「塞がっている」と判定していた。

**修正１**：`channel_clear`のM2ライブチェックを`_overlapping`に変更。

**検証２（静的スタブチェック）**：修正１だけでは`_423_.B`は依然解消
せず。再度計装したところ、今度は`static_stub_blocked`が原因と判明：
しきい値`PAD_HALF*2+M2_MIN_GAP`（=5.4um、ちょうど5.4umグリッド1個分の
距離）に対し、実際の距離が浮動小数点演算の丸め誤差で`5.399999999999977`
となり、`< 5.4`の判定にわずかに引っかかっていた（`txreg[0]`の
ピンがx=488.7、ちょうど1グリッド先）。

**修正２**：`static_stub_blocked`のしきい値判定に`EPS=1e-6`の
許容誤差を追加し、ちょうど法定最小間隔にあたる境界ケースを正しく
「クリア」側に倒すよう修正。

**結果**：2つの修正を合わせた結果、`_423_.B`は`near_y=154.8`のすぐ
近く（jog_y=160.8、出発区間はわずか約6um）でジョグに成功。
`bit_cnt[1]`のスタブ（260.8以降）に到達する前に安全に別トラックへ
退避できるようになった。

- DRC：0違反（変化なし）
- 短絡：**1→0件**（`_055_`/`bit_cnt[1]`も解消）
- FORCE_JOG_NETSのジョグ数：21→5件（多くのネットがそもそもジョグ
  無しで直接クリアと判定されるようになったため、大幅減少）
- warning（退避区間が見つからずフォールバック）：17→0件

`verify_connectivity_nrow_fm.py`は0件の短絡を報告（`_107_`の
「PIN NOT FOUND ON M1」2件のみ残るが、これは本セクションの短絡調査
とは無関係の、以前から存在する別問題）。§38.18で「構造的な限界」と
結論づけていた3件の短絡は、全て解消された。

**総括（38.16〜38.24）**：一連の調査の結論は、当初考えていたような
「チャネル容量不足」でも「行内ピンの物理的な配置制約」でもなく、
ジョグの安全性判定ロジック自体に潜んでいた2種類の実装バグ
（`draw_jog`の出発区間が無検証だった点、および境界値ちょうどの
ケースを誤って塞がっていると判定していた点）だった。ユーザーが
KLayout上で実際の配線を目視確認し、具体的な代替ルートを提案した
ことが、この最終的な発見の決め手となった。

# 39. STDCELL再構築（DFF追加・DFFR: RST→RSTB改名）とLEF/.lib再生成

## 39.1 新GDS（DFF追加、DFFS廃止）からのLEF再生成

STDCELLライブラリのGDS（`LEF/TR-1um_STDCELL.gds`）が再構築され、
リセット無しのDFFセル（`DFF`、64.8um角、D/CK/Q/QBのみ、RSTピン無し）が
新規追加された。旧`DFFS`は今回のGDSには含まれない（意図的な削除と
判断、`gen_lef.py`の`PIN_META`にも元々未登録のため実害なし）。

`DFF`セルのピンマーカー形状を点内包判定で確認：M1PIN 2個→vdd/gnd、
M2PIN 4個→CK/D/QB/Q。GDS上のテキストラベルには`CKP`/`CKB`/`QM`/`QS`も
あるが、いずれもピンマーカー多角形の外にある内部ノードラベルであり、
実際のポートではないことを確認（`DFFR`と同じ設計思想）。

`script/gen_lef.py`の`PIN_META`に`"DFF"`エントリを追加し、
`LEF/TR-1um_STDCELL.lef`を再生成。全21マクロ（物理20セル＋
BUF_X2/X4/X16エイリアス）が正常出力、ピン未マッチ・row height不一致
エラー無し。

（なお、以前「アップロードGDSとディスク上GDSのMD5が不一致」と
疑っていた件は誤検出と判明：比較対象がuploads/に残っていた別セッション
由来の古いファイル（INV_X1単体1セルのみ）であり、実際のライブラリ一式は
Dropbox同期で`LEF/TR-1um_STDCELL.gds`に直接反映されていた。）

## 39.2 DFFRのリセットピン名変更：RB → RSTB、および.lib側の追随漏れ

初回のLEF生成時点で、新GDSの`DFFR`のリセットピンマーカーのラベルが
`RST`ではなく`RB`になっていることを発見。ユーザーへ報告したところ、
xschemスキーマティック（`DFFR.sch`/`DFFR.sym`）とGDSの双方でピン名を
`RSTB`に統一する修正が行われた（`DFFR.sch`のトップレベル`ipin.sym`が
`lab=RSTB`、内部配線ラベルは引き続き`RB`のまま——電気的に同一ネット）。

これを受けて：

- `TR1um_5_stdcell.lib`のDFFRセルブロック内、`pin(RST)`→`pin(RSTB)`、
  `clear: "!RST"`→`"!RSTB"`、`related_pin: "RST"`→`"RSTB"`（計4箇所）
  を改名。
- `script/gen_liberty.py`の`FF_CELLS`テーブルも`"RST"`→`"RSTB"`に追随。

**併せて発覚した潜在バグ**：`gen_liberty.py`の`ff_cell()`は
`clear: "RSTB"`のように常に無反転で出力する実装だった。つまり
`TR1um_5_stdcell.lib`のヘッダーに書かれた
「`Regenerate with: python3 script/gen_liberty.py > TR1um_5_stdcell.lib`」
を額面通り実行すると、38.x台のセッションで手で修正したはずの
active-low反転（`!RST`）が消え、RST極性バグが再発する状態になって
いた（.libファイルは手パッチ済みだがスクリプト側は追随していな
かった）。

`ff_cell()`に`active_low`引数を追加し、`FF_CELLS`テーブルで
`DFFR`を`active_low=True`と明示（DFFSは未確認のため`False`のまま
維持）。修正後のスクリプトで再生成した`.lib`と、手動改名した
`.lib`を`diff`したところ完全一致——スクリプトを再び正しい
ソース・オブ・トゥルースとして確定させた。

## 39.3 再合成（Yosys）で確認

修正済み`.lib`で`i2c_slave_async.v`を再合成
（`src/i2c_slave_async_net_v3.v`として出力）。

```
dfflibmap: cell DFFR (noninv, pins=5, area=8.00) is a direct match
           for cell type $_DFF_PN0_.
    \DFFR _538_ (.CK(C), .D(D), .Q(Q), .QB(~Q), .RSTB(R));
```

`$_DFF_PN0_`（posedge clock / active-low非同期リセット）に`DFFR`が
「直接一致」——余分な反転ゲート無しで正しく極性がマッチしている
ことを確認。セル数：DFFR×33、NOR2×40、NAND2×29、MUX2×19、
AND2_X1×16、INV_X1×10、他（NAND3/OR2/NOR3/NOR4/OR3/NAND4/OR4）。
全33個のDFFRインスタンスが`.RSTB(...)`で接続されており、
GDS/LEFのピン名と一致。

**未対応（保留中）**：`i2c_slave_async_net_v3.v`はまだ正式版
`i2c_slave_async_net.v`を置き換えていない——`plan_placement.py`他、
配置配線スクリプト群が正式版ネットリストを直接パースしており、
差し替えるとPass Aで完成させた配置配線が無効になるため。差し替えの
是非はユーザー未回答（38章末の質問を継続保留）。

## 39.4 v3の優先ネット確定

`i2c_slave_async_net_v3.v`のFO解析(39.3節)結果を受け、次回P&R実行時の
優先ネット(旧FORCE_JOG_NETS相当)を以下の4本に確定：

- `scl`（FO=25、SCLドメインクロック）
- `scl_n`（FO=9、SCL_nドメインクロック）
- `RSTB1`（旧`_085_`、FO=24、`~rst_n`由来のSCLドメインリセット前段）
- `RSTB2`（旧`_179_`、FO=24、start_pulse由来のSCLドメインリセット前段）

SCL_nドメイン側のリセット前段（AND2_X1(rst_n,busy)の入力2本）はFO<10と
比較的小さいため、今回は優先ネットに含めない。

正式ネットリスト差し替え・P&R再実行は未着手（ユーザー判断待ち、
39.3節末尾を参照）。実行時は`script/route_channels_nrow_fm.py`の
FORCE_JOG_NETSにこの4本を設定する。

# 40. scl/scl_n の行単位バッファ挿入とv4の配置配線（0短絡達成）

## 40.1 行単位バッファ挿入（insert_row_buffers.py）

ユーザー指示：「SCL SCL_N の優先ネットは、各スタセル行に BUF1 を挿入
してから配線することは出来ますか？」「スタセルは４行にしてください。」

既存の`insert_buffers.py`（18章）は配置より前に走り、行の概念を持たない
ラウンドロビン分割だった（旧設計の`scl_buf0/scl_buf1`等が結果的に複数行
にまたがり、ルーター側で`PER_ROW_LOCAL_NETS`機構による回避が必要だった
原因）。新規`script/insert_row_buffers.py`は：

1. `fm_multiway_partition`でリファレンス行割当を計算（N_ROWS=4）。
2. `scl`/`scl_n`各ネットのシンク（DFFR.CK）を行ごとにグループ化。
3. 行ごとに1個のBUF_X1を挿入、その行のシンクだけを新規ローカルネット
   （`scl_row0`〜`scl_row3`、`scl_n_row1`/`scl_n_row2`）へリダイレクト。

結果：`scl`は4行 FO=[6,4,5,12]、`scl_n`は2行 FO=[2,7]に分割。
`i2c_slave_async_net_v3.v`→`i2c_slave_async_net_v4.v`として出力
（v3は変更せず保持）。

**発覚した罠**：バッファ追加後に`fm_multiway_partition`を素で再実行す
ると、たった6個の小さいインスタンス追加だけで再帰二分割の上位カット
が変わり、大半のインスタンス（バッファとその意図したシンクの組含む）
が別の行に再配置されてしまうことを確認（再現性のない結果）。対策：
リファレンスパスの行割当をJSON（`LEF/row_assignment_v4.json`）として
保存し、2回目の配置では**再計算せずそのまま使う**よう
`gen_placement_nrow_fm.py`に`part_json`引数を追加。バッファは「作った
時点で行が分かっている」ため計算不要——この方式で全6バッファが意図
した行に厳密一致することを確認済み。

## 40.2 v4の配置配線パイプライン

`gen_placement_nrow_fm.py`/`gen_placement_gds_nrow_fm.py`/
`route_channels_nrow_fm.py`/`gen_err_report_nrow_fm.py`いずれも、
（元の`i2c_slave_async_net.v`用の）デフォルト引数を一切変えずに、
`net_file`/`placement_json`/`in_gds`/`out_gds`等をキーワード引数で
上書きできるよう拡張（CLI直接呼び出しでも`-`で個別にデフォルト維持
可）。旧ネットリスト用の0短絡/0DRC結果の再現性を損なわないことを
最後に再確認済み（回帰チェックOK）。

配置：全175個の元インスタンスはリファレンス行割当のまま、6個の新規
バッファは意図した行に配置。ネット分類：row-only=198, adjacent-pair=21,
spanning=8（旧ネットリストのspanning=2桁台と比べても悪化なし）。

## 40.3 配線の初回失敗と診断（サブエージェントに委譲）

初回ルーティング（FORCE_JOG_NETS/PER_ROW_LOCAL_NETS空）は
`RSTB1`（39.4節で確定した優先ネットの1つ、FO=24、4行にまたがる）の
行またぎ探索が失敗しクラッシュ。`RSTB1`/`RSTB2`を`PER_ROW_LOCAL_NETS`
に追加（旧設計の`scl_buf0`等と同じ、複数行にまたがる高FOネット用の
機構）して解決——ルーティング自体は完走。

DRC：M1間隔違反1件。接続性検証：18件の"SHORT SUSPECTED"（Union-Find
の連鎖報告のため実際の物理短絡ブロブ数はもっと少ない）。

この診断・修正の反復（Phase Aの38章と同種の作業）はサブエージェントに
委譲。結果：

- 18件の報告は**6個の物理的な短絡ブロブ**に集約されることが判明。
- 原因：一部の「ハブ」ネット（`_116_`, `_154_`, `scl`, `sda_in`）は
  pin_mapのY座標だけでは見えない**複数行にまたがる真のspanningネット**
  であり、Pass 2（spanning）はPass 0（per-row-local trunk）で既に
  描画済みの配線に対するライブ衝突チェックを一切行っていなかった
  （盲点）。これらを`PER_ROW_LOCAL_NETS`（ライブチェック付き）に移動
  して解決。
- 残りのrow-only/adjacent-pairの短絡は`FORCE_JOG_NETS`（既存機構）で
  個別に解決（計17ネット）。
- 1件のDRC違反（M1間隔）は上記どちらの機構でも直せない新規パターン
  だったため、`via_x_clear()`/`register_via_x()`をFORCE_JOG_NETSパスの
  着地点チェックに追加（隣接トラックのvia-vs-viaパッド近接を初めて
  ライブチェック）——ただしこれとは無関係の、`DEL1`セル自身の内部
  M1形状に起因する既存の1件（配置のみのGDSでも再現、標準セル
  ライブラリ側の問題）は対象外として残存。

最終確定設定：
```python
force_jog_nets = {'_048_','_049_','bit_cnt[0]','bit_cnt[2]','_003_','_097_',
                   'scl_row1','_106_','_108_','_203_','shreg[1]','shreg[4]',
                   'bit_cnt[3]','_071_','_107_','txreg[3]','_117_'}  # 17net
per_row_local_nets = {'RSTB1','RSTB2','_154_','sda_in','scl','_116_'}  # 6net
```

## 40.4 最終結果

- DRC：M1間隔違反1件（`DEL1`セル内部の既存欠陥、配置のみのGDSでも
  再現することを確認済み——今回の配線作業とは無関係、対象外）
- 接続性：**0短絡**（`ALL NETS FULLY CONNECTED, NO SHORTS DETECTED`）
- channel0のトラック使用数は予算をわずかに超過表示（23/22）だが、
  実際のM1最大Y=87.7umは行境界90.0um内に収まっており実害無し
  （トラック予算計算がやや保守的なだけ）。
- FORCE_JOG_NETS：17ネット、57ピンにジョグ挿入。
- per-row-local spine jog：32件。

出力ファイル：
- `Layout/i2c_slave_async_nrow_fm_v4_routed.gds`（配線済み）
- `Layout/i2c_slave_async_nrow_fm_v4_with_err.gds`（エラー/ジョグ
  ハイライト付き、旧設計と同じ253番台レイヤー規約）
- `Layout/err_report_nrow_fm_v4_details.txt`

旧ネットリスト（`i2c_slave_async_net.v`）用の0短絡/0DRC結果は
無傷で再現性維持を確認済み（回帰なし）。

# 41. チャネル領域のY方向圧縮ポストプロセス

ユーザー指示：「チャネル領域をY方向に圧縮する後プロセスを導入出来ますか？」

## 41.1 検討：ジオメトリ実測からの直接圧縮は不可

最初に試した方式（既に配線済みのGDSから、各チャネル帯域内に実際に
描画された図形のY方向最大到達点を直接測定し、そこから必要高さを
逆算する）は失敗した。理由は2つ：

1. `draw_tap_power_mesh()`のTAP2 VDD/GNDストラップは、構造上**常に
   チャネルの全高**を使って描画される（電源メッシュとして上下の行の
   TAPポートを繋ぐ必要があるため）。ストラップのX範囲を除外しても、
2. per-row-localネットのトランク→ピン間M2スタブが、**チャネルを
   越えて隣接する行の内部まで**（ピンの実際のY位置まで）正当に
   伸びている場合があり、これもチャネル帯域でクリップすると「全高
   使用中」と誤認識される。

いずれも「チャネルの高さそのものに対する制約」ではないため、生の
図形測定では圧縮余地を正しく判定できないと判明。

## 41.2 採用した方式：使用トラック数ベースの見積り＋再生成

ルーター自身が持つ`next_free_idx`（各チャネルで実際に払い出した
主トラック数）を真の使用量指標として採用。新しい高さは：

```
new_height[c] = min(orig_height[c],
                     (used_tracks[c]-1)*TRACK_PITCH + 2*TRACK0_OFFSET
                     + MARGIN_TRACKS*TRACK_PITCH)   # MARGIN_TRACKS=1
```

すでにトラック予算をほぼ使い切っているチャネル（この式が
`orig_height`を超える場合）は`min()`で元の高さのまま据え置き——
無理に縮めない。

配線済みGDSの図形を直接編集するのではなく、`gen_placement_gds_nrow_fm.py`
→`route_channels_nrow_fm.py`を**新しいCH_HEIGHTSで最初から再生成**する
方式を採用（両スクリプトとも`ch_heights`引数で上書き可能に拡張済み）。
理由：チャネル高さは行・トラック・viaの絶対座標全てに波及するため、
既存GDSへの後付け座標シフトは、TAPストラップやスパイン配線が複数
帯域をまたぐ場合に壊れるリスクが高く、既に検証済みの生成コードを
再利用する方が確実。

新規`script/compress_channels_nrow_fm.py`が一連の流れ（実測パス→
新高さ計算→再配置GDS→再配線→出力）を自動化。`main()`は
`placement_json`/`in_gds`/`orig_heights`/`force_jog_nets`/
`per_row_local_nets`/`out_prefix`を引数に取り、任意のパイプライン
（元ネットリスト・v4いずれも）に適用可能。

## 41.3 結果

route_channels_nrow_fm.pyに新規`channel_usage_path`引数を追加し、
チャネルごとの使用トラック数を実行毎にJSON出力するようにした
（`gen_err_report_nrow_fm.py`同様、圧縮判断の材料として恒久的に
利用可能）。

| チャネル | 元の高さ | 元ネットリスト圧縮後 | v4圧縮後 |
|---|---|---|---|
| ch0 (bottom margin) | 90.0 | 64.0 | 90.0（既に予算超過気味のため据え置き）|
| ch1 | 260.0 | 260.0（既にトラック使い切り、圧縮余地なし）| 260.0 |
| ch2 | 240.0 | 240.0（同上）| 240.0 |
| ch3 | 224.0 | 224.0（同上）| 224.0 |
| ch4 (top margin) | 100.0 | 44.0 | 44.0 |

コア全高：元ネットリスト 1173.2um→**1091.2um（-7.0%）**、
v4 1173.2um→**1117.2um（-4.8%）**。

両方とも圧縮後にDRC 0違反・接続性0短絡を再確認済み
（`i2c_slave_async_nrow_fm_compressed_routed.gds` /
`i2c_slave_async_nrow_fm_v4_compressed_routed.gds`、エラー
ハイライト版も同様に生成）。チャネル1〜3は既にトラック予算を
ほぼ使い切っており（高FOネット・per-row-localスパインの通過等）、
今回の安全マージン（+1トラック）では圧縮余地がなかった——より
踏み込んだ圧縮を狙うなら、これらのチャネルの配線密度自体を下げる
（ネットの再配置・優先ネット構成の見直し等）別の取り組みが必要。

# 42. NOR2/AND2_X1並列ゲート問題の調査と修正（v5/v6ネットリスト）

## 42.1 発端：NOR2が並列接続されている

ユーザーから「NAND2が並列で接続されている」（後に「NOR2の間違い」に
訂正）との指摘。`netlist_parser.parse_netlist()`で`i2c_slave_async_net_v4.v`
のインスタンスを型・入力ピン接続でグルーピングしたところ、確かに
**NOR2が24個、全く同じ入力`A=RSTB1, B=RSTB2`を持ち、それぞれ別々の
DFFRのRSTBピンへ1対1接続**されている状態を確認。同様に**AND2_X1も
9個、全く同じ入力`A=busy, B=rst_n`**を持つパターンを発見（ユーザーへ
の追加報告で判明）。

## 42.2 原因：Yosys合成スクリプトに`opt_merge`が欠落

7.5節のYosys合成スクリプト（`proc;opt / techmap;opt / dfflegalize /
dfflibmap / abc -liberty ...`）には、`abc`後の重複セル統合パス
（`opt_merge`）が含まれていなかった。`abc`は各DFFRのRSTB論理錐を
独立に最適化するため、元RTLで同一の非同期リセット条件が複数ビットに
個別に書かれている場合、機能的に同一なゲートが複製されたまま残る。

`opt_merge`のデフォルト挙動は「Yosys組み込み(`$`接頭辞)セルのみ」を
対象とし、liberty技術マップ済みの標準セル（`\NOR2`等）は対象外
――`-share_all`オプションが必要（`help opt_merge`で確認）。加えて、
`abc`直後に`read_liberty -lib`でセルライブラリをモジュールとして
再度読み込んでおく必要があった（`abc`が生成した外部セルインスタンスは、
ポート方向情報がない状態では`opt_merge`の比較対象にならない）。

## 42.3 修正・再合成（v5ネットリスト）

```
read_verilog i2c_slave_async.v
hierarchy -top i2c_slave_async -keep_portwidths
proc; opt
techmap; opt
dfflegalize -cell $_DFF_PP0_ 0
dfflibmap -liberty TR1um_5_stdcell.lib
abc -liberty TR1um_5_stdcell.lib
read_liberty -lib TR1um_5_stdcell.lib
opt_merge -share_all
opt_clean
write_verilog i2c_slave_async_net_v5.v
```

結果：175→144インスタンス（-31個）。NOR2 42→19、AND2_X1 16→8。
重複入力グループは0件に。両グループとも、統合後の単一ゲートが
統合前と全く同じDFFR集合（24個・9個）にRSTBを供給していることを
インスタンス名ベースで確認（電気的な誤配線が起きていないことの
構造的裏付け）。

**機能等価性の検証**：iverilogがサンドボックスに存在しなかった
（root権限なしでapt installできず）ため、Yosys内蔵の形式等価性
検証（`equiv_make`/`equiv_induct`/`equiv_simple`）を試行。DFFR以外の
組合せ論理は`stdcell_behavioral_stubs.v`（delay文を全て除去した検証
専用コピーを使用——`equiv_induct`はゼロ遅延のブール等価性しか
扱えないため）を使い、`flatten`でサブモジュールをプリミティブへ
展開後に実行。しかし本設計はDFF帰還ループが多く（phase/bit_cnt/
shreg/txreg等）、単純な`equiv_induct`+`equiv_simple`の反復では
191個中184個の`$equiv`セルが証明不能のまま残った（本格的な
`eqy`ツールチェーンが必要な規模——今回は見送り）。代わりに、
「`opt_merge`は入力ピン接続が完全一致するセルのみを統合し、
生き残ったセルの出力へ配線をつなぎ替えるだけ」という定義上の
安全性と、上記のDFFR集合完全一致という構造的検証を根拠に、
v5をv3の機能等価版として採用。

## 42.4 RSTB1/RSTB2の再定義

統合後、24個のNOR2は1個の`_145_`（A=`_040_`(=~rst_n)、B=`start_pulse`）
に、9個のAND2_X1は1個の`_146_`（A=`busy`、B=`rst_n`）に集約された。
出力ネット`_007_`（24個のDFFR.RSTBへファンアウト）・`_008_`（9個へ
ファンアウト）を、優先ネット命名規則に合わせて`RSTB1`・`RSTB2`へ
改名（旧v3/v4の同名ネットとは役割が異なる点に注意：旧版では
「24個の複製NOR2への入力」だったが、新版では「統合後の単一ゲートの
出力＝24個のDFFRへの直接ファンアウト」）。

## 42.5 【重大】DFFRのRSTBピンが過去の全レイアウトで未配線だったバグの発見

v6（v5に対しscl/scl_n行アウェアバッファを再挿入したネットリスト）の
配置JSON生成中に、`RSTB1`（fanout=25のはず）が配置JSONに1件
（ドライバのみ）しか現れないことに気付いた。調査の結果：

- `gen_lef.py`のPIN_META上、DFFRのリセットピン名は**`RB`**
  （GDSの物理マーカーのテキストラベルをそのまま採用——39.2節の
  コメントに「.libへの改名が必要」と明記されていたが、当時
  `gen_lef.py`側は追随していなかった）。
- 一方、`.lib`／合成ネットリストは39.2節の改名により**`RSTB`**を
  使用。
- `gen_placement_nrow_fm.py`の`attach_pins()`は
  `pinmap.get(lef_pin_name)`でネットリスト側の接続を検索するため、
  `RB`≠`RSTB`で常にNoneが返り、**全33個のDFFRのリセットピン接続が
  配置JSON生成時点で無条件に脱落**していた。
- `verify_connectivity_nrow_fm.py`は`pin_map`に存在するピンしか
  検査しないため、この欠落は一度もエラーとして検出されなかった
  （`pin_map_nrow_fm_v4.json`を直接確認し、RSTB系ネット
  （`_015_`等）が1件も含まれていないことで実証）。

**影響範囲**：この不具合は39.2節（DFFRのRB→RSTB改名）以降に生成
された**全てのGDS**（元ネットリスト・v3・v4・両圧縮版を含む）に
及ぶ。これらは全て「DRC 0違反・接続性0短絡」で「検証済み」と
報告してきたが、それはDFFRの非同期リセット網が**物理的に一切
配線されていなかった**ことをチェッカーが見ていなかっただけであり、
実際にはリセットが機能しない（フローティング）レイアウトだった。

**修正**：`gen_placement_nrow_fm.py`の`attach_pins()`に
`PIN_NAME_ALIASES = {"DFFR": {"RB": "RSTB"}}`によるピン名変換を
追加（GDS/LEFそのものは変更せず、P&Rスクリプト側のみで吸収）。
修正後、`RSTB1`は25件、`RSTB2`は10件、期待通り配置JSONに出現する
ことを確認。

**残課題**：元ネットリスト・v3・v4・両圧縮版のGDSは、この修正を
反映して再配置配線するまでは「リセット未配線」のままなので、
正式な最終版として扱うべきではない。次回、v6系列の確定後に
横展開が必要。

## 42.6 v6：再配置配線

v5に対し`insert_row_buffers.py`（パス引数化）でscl/scl_n行アウェア
バッファを再挿入 → v6（150インスタンス）。`fm_multiway_partition`で
参照行割当を新規計算（4行、cut=15/14/7）。

優先ネット構成（v6で新規に特定）：
- per-row-local: `RSTB1, RSTB2, sda_in, scl, addr_ok, _071_, _086_`
  （3行以上または非隣接2行にまたがる高FOネット）
- force-jog: `_009_, scl_n_row1, _134_[3], scl_row2, bit_cnt[3],
  _053_, bit_cnt[0], _073_, _133_[0], shreg[0], shreg[6], shreg[1],
  rx_data_r[6], shreg[2], rx_data_r[4], _037_`
  （接続性検証で短絡疑いが出た隣接行ペアネット、反復追加）

チャネル0の予算が23トラック使用/22トラック予算でオーバーフロー
（RSTB1が正しく配線されるようになった影響で、従来の90.0umでは
不足）→ 98.0umに拡張して解消。

**新規発見のルータ限界**：per-row-localネットの「行トランク→
ピン」接続ステップ（step 1）が、他の全ステップ（step 2の行/
チャネル跨ぎ）と異なり**ライブ衝突チェックを一切行っていない**
ことが判明（従来は同時使用するper-row-localネットが最大5個
程度だったため顕在化しなかったが、v6では7個に増え、
`sda_in`と`scl`の行トランクスタブが同一X座標で衝突）。
`channel_clear`（M2）+ 新設の`m1_clear`（横方向ジョグ用M1
チェック）を使った狭域サーチ＋ジョグ機構をstep 1に追加
（`route_channels_nrow_fm.py`、"v7"コメント参照）。

## 42.7 最終結果（v6）

- DRC：**0違反**（M1/M2幅・間隔、V1間隔・囲い、V1-GC間隔 全て0）
- 接続性：162ネット中**160ネット完全検証**（**2件の短絡疑いが残存**）
  1. `sda_in` vs `scl`（per-row-localトランクの行2スタブ同士が
     同一X座標で衝突。42.6の狭域サーチ＋ジョグ修正を適用したが、
     このペアは解消できず——根本原因はstep 2のスパイン横断が
     使うライブチェックの適用範囲がstep 1で新たに発生する
     ケースを完全にはカバーしていないため、さらなる調査が必要）
  2. `shreg[1]` vs `_071_`（FORCE_JOG_NETS機構(`claim_track`)と
     per-row-local機構(`per_row_track_y`)が、チャネル内の
     トラック占有情報を共有していないため、独立に同一X座標を
     選んでしまうアーキテクチャ上のギャップ。両機構の統合が
     根本修正だが、今回は時間の都合で見送り）

いずれも非クリティカルな内部信号（sda_in入力パスの一部、shregの
1ビット）に限られ、他160ネットおよびDRC全項目には影響しない。
次回セッションでの優先対応事項として記録。

出力：`Layout/i2c_slave_async_nrow_fm_v6_routed.gds`（コア高さ
1181.2um）、`_with_err.gds`（残存2短絡のハイライト付き、
layer 253/3に正確な重複ポリゴンをラベル`SHORT:netA<->netB`付きで
出力）。

## 42.11 shreg[1]/_071_の対策案A（m1_run_clearゲート＋安全フォールバック）：DRC回帰は解消、短絡は未解消

42.10節末の`bridge_final`は、ブリッジ探索が`channel_clear`（M2専用）
しか見ておらず、M1間隔違反を新規発生させていた。対策として：

1. `find_channel_clear_x`の`extra_ok`に`m1_run_clear`（既存の
   `draw_jog`のallow_claimed階層で使われているM1間隔チェック）を
   追加。
2. `find_channel_clear_x`は候補が1つも見つからないと`SystemExit`で
   即死するため、`bridge_final`側で`try/except SystemExit`により
   キャッチし、見つからなければ警告を出して従来の無検証描画へ
   フォールバック（1ネットの配置困難が他ネット全体の配線を
   道連れにしてクラッシュさせない設計）。

この2点を実装し、FORCE_JOG_NETSパス・per-row-localスパインの
チャネル着地フォールバックにも`bridge_final`を再度有効化した。

**結果**：DRC回帰は解消（0違反を維持）。しかし`shreg[1]`/`_071_`の
短絡は解消しなかった——実行ログを見ると、チャネル3内の該当箇所
（X=886〜890付近）でブリッジ探索が悉く失敗し、無検証フォールバック
に落ちていた。

**判明した構造的な限界**：`find_channel_clear_x`は探索半径を広げる
ほど「開始点`cur_x`から候補`x`までのM1ラン全体」を`m1_run_clear`で
検証する仕組みのため、**遠い候補ほどチェックが厳しくなる**（検証
すべきM1ランが長くなるため、途中で何かに当たる確率が上がる）。
つまり近傍で詰まっている場合、探索範囲を広げても救済にならない
——今回のチャネル3のように混雑した領域では、この探索方式自体が
原理的に有効な着地点を見つけにくい。

チャネル3の高さを240um（channel2と同じ）まで拡張して試したが、
使用トラック数が56→60へ増えただけで（新たに増えた予算も別の
ネットに使われてしまう）、この特定箇所の混雑は変わらず短絡は
解消しなかった。高さを元の224umへ戻した。

**現状**：DRC 0違反、162ネット中161ネット完全検証（`shreg[1]`/
`_071_`のみ残存）——42.10節から変化なしだが、`bridge_final`が
FORCE_JOG_NETS・per-row-localスパイン双方で「安全に」（DRC回帰
無しで）有効化された状態は維持されており、今回のケース以外の
将来的な同種衝突には効果がある可能性がある。

`shreg[1]`/`_071_`個別の解消には、次のいずれかが必要：
- 対応策B（対症療法）：`shreg[1]`または`_071_`の該当トラック/
  着地Xを手動でずらす。
- ブリッジ探索方式そのものの再設計（固定始点からの単一直線
  ブリッジではなく、複数回の短いジョグを許すステアケース方式等）。

## 42.10 残存2短絡への対策（sda_in/scl解消、shreg[1]/_071_は継続調査）

前節42.7で残っていた2件の短絡（同一X座標でのM2重複）に対策した。

**① sda_in vs scl（解消）**：per-row-localトランクのstep 1（ピン→
専用トラック接続）を再設計。従来は「ピンの真下/真上」固定X一本で
track_yまで縦にM2を引いており、途中の衝突を一切チェックしていな
かった。行内部分（ピン→行境界、常に安全という既存の暗黙の前提を
踏襲）と、チャネル内部分（行境界→track_y）を明確に2区間に分離し、
チャネル内部分のみ`channel_clear`でライブチェック、ブロックされて
いれば`find_channel_clear_x`+`draw_jog`（既にstep 2のチャネル横断
で実績のある機構）でM2ジョグ——M1ではなくM2でジョグするため、
混雑した行内でのM1探索失敗（前回の対策で発生）を回避できた。

併せて、per-row-localのトランク/スパイン着地Xを`register_via_x`で
共有台帳に登録し、FORCE_JOG_NETSの`via_x_clear`から見えるようにした。

**② shreg[1] vs _071_（未解消、原因特定済み）**：`draw_jog`が
`claim_track_near`で選ぶ着地トラック（jog_y）が、呼び出し元が事前に
ライブチェックした範囲（例：FORCE_JOG_NETSは`[channel_entry_y,
track_y]`を検証してから`draw_jog`を呼ぶ）の**外側**に着地しうる
ことが判明（`shreg[1]`の実測：jog_y=894.4が本来のtrack_y=870.4から
channel_entry_y=792.4の反対側に24um超えて着地）。その後に描画される
「jog_yからtrack_yまでの最終区間」がライブチェック無しで無条件に
引かれるため、たまたま_071_の既存per-row-localトランクと重なった。

この「最終区間の再検証＋追加ブリッジ」ロジック（`bridge_final`
ヘルパー）を実装しFORCE_JOG_NETSパス・per-row-localスパインの
チャネル着地フォールバックにも適用したところ、`shreg[1]`/`_071_`の
短絡自体は解消したが、代わりにM1・M2の最小間隔DRC違反が新規に
2件ずつ発生した（`bridge_final`が追加で引くM1ブリッジ自体は
`channel_clear`（M2専用）でしか検証しておらず、同一トラック上の
既存M1との間隔を保証していなかったため——`m1_run_clear`による
追加チェックを試みたが、今度は別のper-row-localネットの探索が
「範囲内に有効なXが見つからない」で失敗するようになった）。

限られた時間の中でこれ以上の連鎖的な副作用を避けるため、
`bridge_final`はper-row-local step 1のチャネル区間（①で確立済み・
副作用なしと確認済み）のみに残し、FORCE_JOG_NETSパスとper-row-local
スパインのフォールバックは元の実装に戻した。結果：

- DRC：**0違反**（回帰なし）
- 接続性：162ネット中**161ネット完全検証**、`shreg[1]`/`_071_`の
  1件のみ残存

`shreg[1]`/`_071_`の根本修正には、`bridge_final`の追加ブリッジ
そのものをM1間隔についても正しくライブチェックしつつ、探索失敗時に
安全にフォールバックする、より慎重な再設計が必要（次回セッションの
課題として記録）。

出力：`Layout/i2c_slave_async_nrow_fm_v6_routed.gds`（コア高さ
1181.2um、DRC 0違反）、`_with_err.gds`（残存1短絡のハイライト付き）。

## 42.9 GDS側のピン修正確認・LEF再生成

ユーザーがGDS（`LEF/TR-1um_STDCELL.gds`）のDFFRリセットピンマーカーの
テキストラベルを`RB`→`RSTB`に直接修正。GDSのTXM2レイヤーを確認し、
`RSTB 56700 29700`（他のD/CK/QB/Qと同じ形式）になっていることを確認。
ピンの矩形座標自体（RECT 55.000 27.900 58.400 31.300）は変更前と
完全一致——純粋なラベル変更で、物理形状の変更ではないことを確認。

`gen_lef.py`のPIN_META["DFFR"]を`"RB"`→`"RSTB"`に追随修正し、
`TR-1um_STDCELL.lef`を再生成（23マクロ、DFFRピンは
`D/CK/RSTB/Q/QB/VDD/GND`とネイティブに一致、42.5節で追加した
`PIN_NAME_ALIASES`ワークアラウンドは不要になったが、後方互換の
安全策として残置——`aliases.get(pname, pname)`は`RSTB`キーに対して
no-opなので実害なし）。

再生成したLEFではDEL1・TAP2・TAP3のOBS（内部M1障害物）ポリゴンにも
差分が出た（ピン形状ではなく、DFFR以外への影響はゼロ）——これは
本セッション中のルータ/配置スクリプトがOBS情報を一切参照しない
ため、v6の配置配線結果には影響しないことを確認済み。念のため
v6のプレースメント→配置GDS→配線を最新LEFで再実行し、結果が
完全に同一（DRC 0違反、160/162ネット検証、同じ2件の短絡疑い）で
あることを確認した。

## 42.8 変更したスクリプト

- `script/gen_lef.py`：PIN_META["DFFR"]を`"RB"`→`"RSTB"`（42.9節、
  GDS側のラベル修正に追随）。
- `script/gen_placement_nrow_fm.py`：`attach_pins()`に
  `PIN_NAME_ALIASES`（DFFR: RB→RSTB）を追加。
- `script/insert_row_buffers.py`：`main()`を`in_path/out_path/
  row_assignment_json/target_nets`引数化（v3専用のハードコードを
  解消、任意のネットリストに再利用可能に）。
- `script/route_channels_nrow_fm.py`：per-row-localネットのstep 1
  （行トランク→ピン接続）にライブ衝突チェック＋狭域ジョグを追加
  （`m1_clear`ヘルパー新設）。

# 43. 配置配線パイプラインのステップ別整理とレイアウトチェックポイント保存

ユーザー要望（「一旦整理したいので、もう一度処理ごとの結果を
レイアウトとして保存」）に応え、配置配線パイプライン全体を
処理単位に分解し、各ステップ終了時点のGDSを個別ファイルとして
保存できるようにした。対象はv6ネットリスト（150インスタンス、
162信号ネット）。

## 43.1 パイプライン全体のステップ一覧

| # | ステップ | スクリプト | 入力 | 出力 |
|---|---------|-----------|------|------|
| 0 | 論理合成（Yosys, `opt_merge -share_all`） | （インライン、`yowasp_yosys`） | `src/i2c_slave_async.v` | `src/i2c_slave_async_net_v5.v` |
| 0.5 | 行アウェアバッファ挿入（scl/scl_n） | `insert_row_buffers.py` | v5ネットリスト | `src/i2c_slave_async_net_v6.v` |
| 1 | 配置（行割当＋セル詰め） | `gen_placement_nrow_fm.py` | v6ネットリスト | `LEF/placement_nrow_fm_v6.json`, `LEF/row_assignment_v6.json` |
| 2 | 配置GDS生成（セル配置のみ、配線なし） | `gen_placement_gds_nrow_fm.py` | 配置JSON | `Layout/i2c_slave_async_nrow_fm_v6_placement.gds` |
| 3 | TAP電源メッシュ配線 | `route_channels_nrow_fm.py`（内部） | 配置GDS | （routed.gds内、チェックポイントstep1） |
| 4 | pass 0：per-row-localネット（トランク＋スパイン） | 同上 | — | チェックポイントstep2 |
| 5 | pass 1：高FO＋行内／隣接行ペアネット | 同上 | — | チェックポイントstep3 |
| 6 | pass 2：行またぎ（spanning）ネット | 同上 | — | チェックポイントstep4 |
| 7 | pass 3：FORCE_JOG_NETS（診断済みネットの最終ライブチェック付き配線） | 同上 | — | チェックポイントstep5＝`Layout/i2c_slave_async_nrow_fm_v6_routed.gds` |
| 8 | DRCチェック | `drc_check_nrow_fm.py` | routed.gds | 標準出力（違反数） |
| 9 | 接続性検証（短絡・未接続検出） | `verify_connectivity_nrow_fm.py` | routed.gds + pin_map | 標準出力（短絡/未接続一覧） |
| 10 | エラーハイライト生成 | `gen_err_report_nrow_fm.py` | routed.gds + pin_map + net_shapes | `Layout/i2c_slave_async_nrow_fm_v6_with_err.gds`, `err_report_nrow_fm_details.txt`（チェックポイントstep6） |

ステップ3〜7は`route_channels_nrow_fm.py`の`main()`内で連続実行される
（同一プロセス・同一`layout`オブジェクトへの累積描画）。各パスの
処理内容：

- **TAP電源メッシュ**：各行のTAP2セルのVDD/GND M2ストラップを、
  上下のチャネルを貫通する形で1本の垂直電源メッシュに接続。
- **pass 0（per-row-local）**：3行以上または非隣接2行にまたがる
  高FOネット（v6では`RSTB1, RSTB2, sda_in, scl, addr_ok, _071_,
  _086_`の7net）について、行ごとに専用ローカルトランクを引き、
  行間はスパイン（M2縦配線＋必要に応じジョグ）で接続。
- **pass 1（高FO＋行内／隣接行ペア）**：単一行または隣接2行のみに
  ピンを持つネットを、専用トラックに割り当てたM1トランク＋M2
  スタブで配線。強制重複ゾーンのライブ衝突チェック（現在は
  ユーザー判断待ちで無効化中）もここに属する。
- **pass 2（spanning）**：3行以上にまたがるが per-row-local
  指定されていないネット（v6では該当0件）。行を通過する際は
  `find_row_clear_x`でクリアなXを探索し、必要ならジョグ。
- **pass 3（FORCE_JOG_NETS）**：既知の短絡候補として個別診断済みの
  ネット（v6では16net）を最後に再配線。他の全ネットの最終形状に
  対してライブ衝突チェック（`channel_clear`／`m1_run_clear`）を行い、
  衝突があればジョグ。

## 43.2 保存したチェックポイントGDS

`script/run_route_v6_checkpoints.py`（新規、`route_channels_nrow_fm.
main()`の`checkpoint_dir`引数を使用）を実行し、`Layout/steps_v6/`
配下に7ファイルを保存：

- `v6_step_0_placement.gds` — 配置直後（配線なし）
- `v6_step_1_tap_power_mesh.gds` — TAP電源メッシュ配線後
- `v6_step_2_pass0_per_row_local.gds` — per-row-localネット配線後
- `v6_step_3_pass1_high_fo_row_only_adjacent.gds` — 高FO＋行内／隣接
  行ペアネット配線後
- `v6_step_4_pass2_spanning.gds` — spanningネット配線後（v6は0件の
  ため step3と幾何学的に同一）
- `v6_step_5_pass3_force_jog_nets.gds` — FORCE_JOG_NETS配線後
  （＝最終配線結果、`i2c_slave_async_nrow_fm_v6_routed.gds`と同一）
- `v6_step_6_with_err_highlight.gds` — エラーハイライト付き最終版

`checkpoint_dir`機構自体は`route_channels_nrow_fm.py`の`main()`に
恒久的に追加済み（デフォルト`None`＝無効、既存の呼び出しに
影響なし）ので、今後の再配線でも同じ方法でステップ別GDSを
再取得できる。

再実行結果はDRC 0違反、162ネット中161ネット完全検証、
`shreg[1]`/`_071_`の短絡疑い1件のみ残存——既知の結果と完全一致
（42.11節の状態から変化なし）。

## 43.3 ステップ別チャネル空きトラックのハイライト（レイヤー分離）

ユーザー要望（「それぞれステップの配線終了時に、次のステップで
配線可能なチャネルリソースをレイアウト上にハイライト、ステップ
毎に違うレイヤーで保存」）に応え、`checkpoint()`ヘルパーを拡張。

各チェックポイント時点で、まだ`channel_used_x`に登録されていない
（＝次のパスがまだ自由に使える）チャネルトラックを全チャネル・
全トラックインデックスにわたって走査し、そのtrack_y位置に薄い
ハイライト矩形（幅=行全幅、高さ=TRACK_PITCHの0.35倍）を、
非fabレイヤー**(258, N)**（Nはそのチェックポイントのステップ番号
1〜5）に描画する。同じレイヤー番号258・異なるdatatypeという
構成にしたのは、複数ステップのGDSを1つのKLayoutセッションに
まとめて開いた場合でも、レイヤーパネルで各ステップの空き状況を
個別にON/OFFできるようにするため。

**実装上の注意**：ハイライトは`layout.dup()`で作った使い捨ての
複製にのみ描画し、本物の`layout`（最終`out_gds`のベース）には
一切触れない。これにより258番台のレビュー用レイヤーが実際の
納品用GDS（`i2c_slave_async_nrow_fm_v6_routed.gds`）に混入する
ことはない——再実行後にDRC・接続性を再検証し、結果が完全に
不変（0違反、shreg[1]/_071_のみ残存）であることと、最終GDSに
レイヤー258が一切存在しないことの両方を確認済み。

**v6での空きトラック推移**（全チャネル合計）：

| ステップ | 空きトラック数 | 備考 |
|---------|--------------|------|
| step1（TAPメッシュ後＝全ネットの一次トラック割当完了時点） | 109 | |
| step2（pass 0：per-row-local配線後） | 108 | スパインジョグの大半はX方向のみ（同一トラック内）で新規トラック消費なし |
| step3（pass 1：高FO＋行内／隣接行ペア後） | 108 | v6は強制重複ゾーンチェックが無効化中のため変化なし |
| step4（pass 2：spanning後） | 108 | v6はspanningネット0件のため変化なし |
| step5（pass 3：FORCE_JOG_NETS後＝最終） | 63 | 51ピンのジョグのうち大半が新規トラックを消費（一部は既存トラックの空き区間を再利用） |

チャネル3（`shreg[1]`/`_071_`の衝突箇所）を含め、pass 3で空き
トラックが最も大きく減っており、残存短絡の物理的な混雑度を
`Layout/steps_v6/v6_step_5_*.gds`のレイヤー(258,5)で視覚的に
裏付けられる。

## 43.4 row_above_lower を配線元ピン最近接に振り直す実験（不採用）

ユーザー質問「配線元ピンに最も近いトラックを選ぶ方針に変更できる
か」を受け、限定範囲（`row_above_lower`のみ）で試験実装した。

**変更内容**：`row_above_lower`（各行cが自分の下側チャネルcへ
下向きに配線する行内ネット群）のパッキング順を、従来の
「`row_below_upper`の直後（チャネル下寄り）」から「`adjacent_pair`
／`spanning`の後（per_row_localの直前、チャネル上寄り）」に移動。
ブロックサイズは不変のため`channel_primary()`や`channel_total`の
計算には影響しない、純粋な再配置。

**結果**：
- M2配線長：合計 約5036um（6.3%）短縮
- DRC：0違反を維持
- 接続性：短絡疑い**1件→2件に悪化**（`shreg[1]`の相手が`_071_`→
  `rx_data_r[6]`に変化、新規に`_133_[0]`/`scl_n_row1`が発生——
  38.18節で見た「モグラ叩き」と同じ現象で、衝突が別ネットへ
  移動しただけ）

ユーザー判断により**不採用**。正式な`v6_routed.gds`・`steps_v6/`
一式は変更前の状態（DRC 0違反、短絡1件のみ）に復元済み。実験結果
のみ`Layout/i2c_slave_async_nrow_fm_v6_exp_pin_nearest_with_err.gds`
として別名保存（git管理外）。

## 43.5 チャネル空きハイライトを「実描画ジオメトリ基準」に再定義

43.3節のレイヤー258ハイライトは`channel_used_x`という**予約簿**
（そのトラックが一次トラック割当の時点で誰かに割り当て済みか）
だけを見ており、実際に描画された金属の有無は見ていなかった。
ユーザーが`v6_step_2`（pass 0直後）と`v6_step_3`（pass 1後）を
見比べて指摘した通り、この方式では「予約済みだがまだ描画されて
いない」領域（行内／隣接ペア／高FOネットは全てpass 1で描画される
ため、pass 0時点ではまだ空白）が「空き」と区別できず、誤解を招く
表示になっていた。

さらに、どのネットのM1トランクも`m1_box(xmin, ..., xmax, ...)`で
**自分のピンのxmin〜xmaxの範囲だけ**を引いており、チャネル全幅を
使うことはない。つまり「予約済み」なトラックでも、そのネット自身
の配線範囲の外側は実際には常に空いている（38.23節で確認した
「チャネルあたり9〜24本の完全アイドルトラック」はこの実例）。

**再実装**：`checkpoint()`のハイライト計算を`channel_used_x`の
参照から、**実際に描画されたM1形状**（`layout.dup()`後の複製に
対する`begin_shapes_rec_touching`によるライブジオメトリクエリ）
ベースに変更。各トラックのYバンド（track_y±M1_TRUNK_WIDTH/2）
において、チャネル全幅の矩形から実際のM1のマージ済みリージョンを
差し引いた**幾何学的な残り**を非fabレイヤー(258, N)に描画する。

- 初期のステップ（例：checkpoint 1）では、まだ描画されていない
  後続パスのネット用に予約された領域も「空き」として表示される
  （その時点でまだ誰にも実際には使われていないのは事実だが、
  他ネットに再利用可能というわけではない点に注意）。
- **最終ステップ（checkpoint 5、全パス完了後）の表示は、正真正銘
  「今後の追加配線に使える実残余容量」を表す**——ユーザーが
  意図した「チャネルを配線した後に空きとして再定義する」という
  要望に対応。

再実行結果：v6全体で空き面積は step1=0.6707mm²→step5=0.5288mm²
まで減少（最終ステップの残余容量が最も信頼できる数値）。本変更
はレビュー用オーバーレイの計算方法のみの変更であり、`layout.dup()`
にのみ描画する点は43.3節から不変——DRC・接続性を再検証し、結果が
完全に不変（0違反、shreg[1]/_071_のみ残存）であることと、最終GDS
にレイヤー258が一切存在しないことを確認済み。

## 43.6 【解決】draw_jogのトラック再利用を優先順位反転 → shreg[1]/_071_ 解消、DRC 0・短絡 0

ユーザー質問「ステップ5でのM1ジョグ位置をY軸方向になるべく衝突
しない限り再利用することはできますか」＋目標「最終的に使って
いないM1チャネルを最大化する」を受け、`draw_jog`（`near_y`指定
時）のトラック確保の優先順位を反転した。

**変更前（v10、design_notes 38.23）**：
1. まず`claim_track_near(..., allow_claimed=False)`で**新規未使用
   トラック**を試す
2. 失敗時のみ`allow_claimed=True`で既存トラックの空き区間を再利用

**変更後（v12）**：
1. まず`claim_track_near(..., allow_claimed=True, extra_ok=
   departure_leg_and_m1_ok)`で**既存トラックの空き区間の再利用**を
   優先的に試す（`m1_run_clear`によるM1実形状チェック込み）
2. 失敗時のみ新規トラックを確保
3. それも失敗時は従来通り無条件フォールバック

チェックする条件（`channel_clear`によるM2チェック＋`m1_run_clear`
によるM1チェック）自体は変更していない。「どちらを先に試すか」
だけを入れ替えた。

**結果**：
- DRC：**0違反**（変更なし）
- 接続性：**162ネット全て完全検証、短絡疑い0件**——セッション
  開始以来ずっと残存していた`shreg[1]`/`_071_`の短絡が解消
- 2回独立実行して同一結果を確認（決定的）
- `net_shapes`を比較すると、FORCE_JOG_NETS 15net全ての配線経路が
  変化（同じ本数のジョグでも、より近い/再利用可能なトラックを
  選ぶことで結果的に衝突を回避できた）

**なぜ効いたか**：`claim_track_near`は`target_idx`から外側に
1つずつ広げて探索し、条件を満たす最初の候補を採用する。従来は
「未使用トラックのみ」を条件に含めて探索するため、実際には
すぐ近くに使える空き区間（他ネットのトランクが届いていない
アイドル区間）があっても無視して、より遠い未使用トラックまで
探索範囲を広げてしまうことがあった。探索範囲が広がるほど
`m1_run_clear`等のチェックが必要とするM1ランが長くなり
（38.23/38.24節で判明した「遠いほど厳しくなる」問題）、
`shreg[1]`のケースではその結果ちょうど`_071_`の既存トランクと
交差してしまっていた。再利用を先に試すことで探索が`target_idx`
근처で早期に収束し、結果的に衝突を起こさない、より短い経路が
選ばれるようになった。

再利用優先により空きチャネル面積の変化は本ケースではほぼ横ばい
（0.5288mm²、43.5節と同一）——ただし総使用量が変わらなくても
「どのトラックを使うか」の選び方が変わることで短絡が解消した点が
重要。ユーザーの「最終的に使っていないM1チャネルを最大化する」
という目標には、今回はトラック総数ではなく既存アイドル区間の
有効活用という形で貢献した。

**正式反映**：v6の全成果物（`Layout/i2c_slave_async_nrow_fm_v6_
routed.gds`、`_with_err.gds`、`Layout/steps_v6/`一式、
`err_report_nrow_fm_details.txt`）をこの結果で更新。
`err_report_nrow_fm_details.txt`は「0 SHORT(S) SUSPECTED」に
変化。v6は初めて**DRC 0違反・短絡0件・162ネット全検証**の
完全クリーン状態に到達した。

## 43.7 v6チャネル高さのY軸圧縮（後工程）

ユーザー要望「後工程で使っていないチャネル領域をY軸方向に圧縮
できますか」を受け、既存の`compress_channels_nrow_fm.py`
（section 41、v3/v4パイプライン向けに既に実装済みの後工程
スクリプト）をv6にそのまま適用した。

**方式**（既存の仕組みをそのまま流用）：
1. 元の`CH_HEIGHTS=[98, 260, 240, 224, 100]`で一度配線し、各
   チャネルの実トラック使用数（`next_free_idx`）を測定
2. `new_height[c] = min(orig_height[c], (used[c]-1)*TRACK_PITCH +
   2*TRACK0_OFFSET + MARGIN_TRACKS(1)*TRACK_PITCH)` で新しい高さを
   計算（安全マージン1トラック分を残し、元の高さを超えない）
3. 新しい`CH_HEIGHTS`で配置GDS・配線を再実行し、DRC・接続性を
   再検証

**結果**：

| チャネル | 元の高さ | 使用トラック/予算 | 圧縮後の高さ |
|---|---|---|---|
| ch0 | 98.0um | 23/24 | 96.0um |
| ch1 | 260.0um | 65/65 | 260.0um（予算ぴったりのため圧縮なし） |
| ch2 | 240.0um | 60/60 | 240.0um（同上） |
| ch3 | 224.0um | 56/56 | 224.0um（同上） |
| ch4 | 100.0um | 9/25 | **40.0um** |

コア高さ：1181.2um → **1119.2um**（62.0um、5.2%削減）。
ch1〜3は43.6節の再利用優先化で使用トラック数が予算ちょうどまで
達しているため今回は圧縮の余地がなく、ch4（上端マージン、行3の
上側で行が1つしかなく使用9/25トラックのみ）が削減の大半を占めた。

DRC・接続性を新しいコア高さ（走査範囲0〜1119.2um）で再検証し、
**0違反・162ネット全検証・短絡0件**を確認——圧縮後も43.6節の
クリーン状態を完全に維持している。

出力：`Layout/i2c_slave_async_nrow_fm_v6_compressed_routed.gds`、
`_with_err.gds`、`err_report_nrow_fm_v6_compressed_details.txt`
（v3/v4圧縮版と同じ命名規則）。

## 43.8 必要最小限のチャネル高さまで圧縮（margin_tracks=0）

ユーザー要望「後処理として、必要最小限のチャネル高さにして配置
配線をし直してください」を受け、43.7節の安全マージン1トラック分
（`MARGIN_TRACKS=1`）を**0**にして`compress_channels_nrow_fm.py`を
再実行。さらに、圧縮後の高さで再度使用量を測定→圧縮を繰り返す
反復（fixed-point）でこれ以上縮まらないことを確認した。

**結果**（`margin_tracks=0`、収束は1回の圧縮で到達）：

| チャネル | 43.7版（margin=1） | 今回（margin=0、必要最小限） |
|---|---|---|
| ch0 | 96.0um（23/24トラック） | **92.0um**（23/23トラック、余白ゼロ） |
| ch1〜3 | 260/240/224um（変化なし） | 260/240/224um（変化なし——予算=使用のため元々余白ゼロ） |
| ch4 | 40.0um（9/25トラック） | **36.0um**（9/9トラック、余白ゼロ） |

コア高さ：1181.2um（元）→1119.2um（43.7節、margin=1）→
**1111.2um**（今回、margin=0）。元のCH_HEIGHTSからは**70.0um
（5.9%）削減**。

DRC・接続性を新しいコア高さ（走査範囲0〜1111.2um）で再検証し、
**0違反・162ネット全検証・短絡0件**を確認——安全マージンを
ゼロにしても43.6節のクリーン状態を完全に維持している。

**注意点**：`MARGIN_TRACKS=0`はどのチャネルも実測トラック数
ちょうどの高さしかなく、今後さらにネットを追加したり配線を
変更したりする場合は再度この圧縮をやり直す必要がある（今回は
「最終確定レイアウト」としての最小化が目的のため許容）。

出力：`Layout/i2c_slave_async_nrow_fm_v6_minheight_routed.gds`、
`_with_err.gds`、`err_report_nrow_fm_v6_minheight_details.txt`。

# 44. 配線済みGDSを一切再配線せずにY軸方向へ幾何学的に圧縮する後処理

ユーザー要望「配線が完了した後に、後処理として未使用のチャネルを
Y軸方向に圧縮する処理を実装」「既存配線はそのままに」を受け、
43.7/43.8節（CH_HEIGHTSを変えて配置配線を**やり直す**方式）とは
根本的に異なる、**既に完成したGDSのジオメトリだけを動かす**新しい
後処理`script/squeeze_channels_nrow_fm.py`を実装した。

## 44.1 なぜ「配線済みGDSを直接いじる」のは危険なのか、どう安全にしたか

各トラックは`track_y = ch_y0[c] + TRACK0_OFFSET + idx*TRACK_PITCH`
という一様グリッド上にある。「どのネットにも使われていない
idx」を単純に削除して上の全ジオメトリを詰めるだけなら、Y座標の
区分線形写像（空きスロットの区間だけ幅ゼロに潰す）を全形状に
かければ理屈の上では安全に見える。しかし実際には**新たに隣接
関係になる2本のトラック同士は、元のレイアウトでは一度も
`collides()`（via同士のX近接チェック、MIN_VIA_X_SEP）で検証
されていない**という盲点がある——元の隣接ペアしか検証されて
いないため、圧縮で新たに隣り合うことになったペアが実は近すぎる
X位置にviaを持っていた場合、圧縮によって新規の短絡を生みかねない。

**安全策**：
1. `route_channels_nrow_fm.py`に`compaction_info_path`引数を追加
   （section 44 instrumentation）。各チャネルの`channel_used_x`
   （実際に使われているidxとその上のvia X座標一覧）と
   `guard_idx`（HIGH_FO_GUARD_TRACKS/PER_ROW_GUARD_TRACKS用に
   意図的に空けてあるidx——ワイドトランクの安全マージンなので
   これは絶対に除去してはいけない）をダンプ。
2. `squeeze_channels_nrow_fm.py`の`compute_kept_indices()`が、
   guardまたはvia実データのあるidxを「保持」対象とし、それ以外を
   除去対象とする。**保持対象同士が、元は隣接していなかったのに
   圧縮後に新たに隣接することになる場合**、両者の実via X座標
   （`used_x`）を比較し`MIN_VIA_X_SEP`未満ならその1箇所だけ
   スペーサー（除去せず残すidx）を1本残して安全マージンを回復する。
   guardスロット自身にはviaがないため、guardが絡む新隣接は
   常に安全（guardは常に自分が守るべきネットの真横に残るため、
   反対側で何が詰められても保護効果は変わらない）。
3. `build_y_map()`が上記の「保持/除去」判定から、Y座標に対する
   区分線形写像（行の中と保持トラックの帯は傾き1＝恒等、除去
   トラックの帯は傾き0＝幅ゼロに圧縮）を構築。
4. 全形状（M1/M2/V1の矩形・ポリゴン・テキスト）と全セル
   インスタンス（標準セル・via_1 PCellインスタンス）のY座標に
   この写像を適用——インスタンスは単一点変換（行の中に完全に
   収まり、圧縮境界をまたがないため安全）、矩形/ポリゴンは
   各頂点独立に写像を適用（軸並行矩形は両端が独立に写像されても
   正しく変換される）。

## 44.2 結果

v6の`minheight`版（43.8節、CH_HEIGHTS=[92,260,240,224,36]、コア
高さ1111.2um）を入力に実行：

| チャネル | 予算トラック | 保持 | 除去 | 削減 |
|---|---|---|---|---|
| ch0 | 23 | 23 | 0 | 0.0um |
| ch1 | 65 | 49 | 16 | 64.0um |
| ch2 | 60 | 42 | 18 | 72.0um |
| ch3 | 56 | 35 | 21 | 84.0um |
| ch4 | 9 | 9 | 0 | 0.0um |

**コア高さ：1111.2um → 891.2um（220.0um、19.8%削減）**。
元の未圧縮v6（1181.2um）からは**290.0um（24.6%削減）**。

DRC・接続性を新コア高さ（走査範囲0〜891.2um）で再検証し、
**0違反・162ネット全検証・短絡0件**を確認——`gen_err_report_
nrow_fm.py`による独立な短絡検出（union-find、ch_heightsに
依存しない）でも0件で一致。M1/M2/V1の形状数（1117/619/1213）も
圧縮前後で完全一致——ジオメトリの消失・重複がないことも確認済み。

pin_map・net_shapes・force_jog_eventsも同じY写像で座標変換し、
`_squeezed`接尾辞で一式保存（エラーレポート生成や今後の検証に
そのまま使える）。

出力：`Layout/i2c_slave_async_nrow_fm_v6_squeezed_routed.gds`、
`_with_err.gds`、`_pin_map.json`、`_net_shapes.json`、
`_force_jog_events.json`、
`err_report_nrow_fm_v6_squeezed_details.txt`、
`script/squeeze_channels_nrow_fm.py`（新規）。

## 45. 行数を変えた面積比較（2行/3チャネル、3行/4チャネル）

ユーザー要望「同じ処理フローで、スタセル３チャネルとの面積比較を
してください」「Row=3も比較してください」に対応。同一ネットリスト
（`i2c_slave_async_net_v6.v`）に対し、行数だけを2/3/4と変えて
配置→配線→最小チャネル高さ圧縮（41節）→幾何学的Yスクイーズ（44節）
の同じパイプラインを通し、コア面積を比較した。

### 45.1 fm_multiway_partition の一般化（2の冪乗制限の撤廃）

`fm_partition.py`の`fm_multiway_partition`は元々「n_rowsは2の冪乗
のみ」という制約があった（再帰二分割のみ実装）。3行を作るために、
`fm_bipartition`に`target_ratio`引数を追加（デフォルト0.5=均等）し、
`fm_multiway_partition`を「n_top=n_rows//2, n_bottom=n_rows-n_top、
target_ratio=n_top/n_rows」で再帰する形に一般化した。2の冪乗では
常にratio=0.5になるため既存の4行分割と完全に同一の結果（cut=16等）
を再現することを確認済み——後方互換な拡張。

### 45.2 TAP_INTERVAL_TRACKS/N_GAPS 制約

`gen_placement_nrow_fm.py`は`TARGET_ROW_WIDTH_UM=1620um`・
`N_GAPS=2`が固定値だったが、行数が減ると1行あたりのセル内容量が
増え（2行:2354um・3行:1571um、いずれも4行時の588um前後より大幅増）、
2ギャップでは収まらず「row content needs 3 gaps」でアサート
失敗した。TAP_INTERVAL_TRACKS（=147トラック、v6と同じTAPメッシュ
間隔）は変えずにN_GAPS=3に増やし、TARGET_ROW_WIDTH_UMを
`(N_GAPS+1)*TAP_WIDTH_UM + N_GAPS*TAP_INTERVAL_TRACKS*TRACK_UM
= 2424.6um`に再計算——同じTAP粒度のまま行幅だけ広がる形にした
（2行・3行とも同じ2424.6umに収束）。

### 45.3 ルーターの潜在バグ発見・修正：高FO×FORCE_JOG_NETS併用でネット消失

2行配置で配線したところ、`scl_row2`が`pin_map`から完全に欠落
（DRC 0違反・短絡0件と報告されるのに、実際は163ネット中2ネットが
検証すらされていなかった——39.2節のDFFR RB/RSTBバグと同種の
「サイレント消失」）。原因は`route_channels_nrow_fm.py`の
`force_jog_pads`が`non_spanning_nets`からしか作られておらず、
「高FO（8ピン以上）かつFORCE_JOG_NETS所属」というネットが
pass1（FORCE_JOG_NETSはスキップ）にもpass3（`force_jog_pads`に
存在しない）にも描画されずに消えていた。4行構成では該当ネットが
複数行にまたがりspanning分類されていたため一度も発現しなかった
潜在バグ。`force_jog_pads`に`high_fo_nets`由来分も追加して修正
（`route_channels_nrow_fm.py`、恒久修正・v6にも遡って影響なし
確認済み）。

### 45.4 残課題：2行・3行構成での短絡

修正後、2行構成で163/163ネットが`pin_map`に存在するようになった
が、FORCE_JOG_NETS同士が密集チャネル内で干渉し合い、
`bridge_final`の「安全な着地X未検出→未検証の直接配線」フォール
バック経由で実際に短絡が発生（DRC上は0違反のまま——via_1パッド
同士がトラックピッチぎりぎりで接触し、Union-Findベースの
接続検証でのみ検出可能な種類の短絡と実測で確認）。2行構成で
6短絡ペア（163ネット中約157ネットが完全クリーン）、3行構成で
5短絡ペア（同158ネット）が残存。チャネル高さのマージンを広げても
解消せず（bridge_finalのX探索は同一passの後続ネットを考慮しない
ため、マージン増だけでは根本解決しない）。v6（4行）が0短絡達成に
複数セッションを要した経緯を踏まえ、今回はこの2構成の完全収束
までは追わず、面積比較を優先した。

### 45.5 面積比較結果

| 構成 | 行幅×コア高さ | 面積 | v6比 | 検証状況 |
|---|---|---|---|---|
| 4行/5ch（v6, 基準） | 1620.0 × 891.2 um | 1.4437 mm² | 100.0% | 162/162クリーン、短絡0 |
| 3行/4ch（新規） | 2424.6 × 766.4 um | 1.8582 mm² | 128.7% | 158/163クリーン、短絡5ペア |
| 2行/3ch（新規） | 2424.6 × 657.6 um | 1.5944 mm² | 110.4% | 157/163クリーン、短絡6ペア |

行数を減らすほど（チャネル数が減るほど）面積は増加する——主因は
チャネル高さの節約分よりも、TAPギャップ増加による行幅拡大
（1620um→2424.6um、+49.7%）が支配的なため。DRC違反はいずれも
0だが、2行・3行構成は45.4の短絡が未解消のため製造可能な最終
成果物ではなく、あくまで面積傾向の参考値。

出力：`LEF/placement_nrow_fm_v6_3ch.json`、`_4ch.json`、
`Layout/i2c_slave_async_nrow_fm_v6_3ch_squeezed_routed.gds`、
`_4ch_squeezed_routed.gds`（各minheight/squeezed中間GDSも保存）、
`script/fm_partition.py`（target_ratio一般化）、
`script/route_channels_nrow_fm.py`（force_jog_pads高FOバグ修正）。

### 45.6 3行構成でN_GAPS=1（行幅1620um、v6と同一）を試行

ユーザー質問「3Rowで横幅1602は可能ですか？」に対し、45.2のTAP
ギャップ数を検討。3行構成の最大行内容量（行0=1571.4um=291
トラック）は、N_GAPS=2（147トラック/ギャップ×2＝294トラック）
だとビンパッキングの都合で3ギャップ必要になり1620um幅に収まらない
が、N_GAPS=1（1ギャップ=296トラックの連続領域、TAP列2本のみ）
にすると全行が1ギャップに収まり、**1620.0umちょうど**が実現できる
ことを確認（1602.0umは整数トラック制約により厳密には作れない
——最も近いのは1598.4um/1603.8um）。

ユーザー承認を得てN_GAPS=1・幅1620.0umで配置→配線→minheight
圧縮→スクイーズを実行。DRCは0違反（他構成と同様）。ただし接続性
検証で**短絡15ペア**（163ネット中148ネットのみクリーン）を検出
——N_GAPS=2の2424.6um幅版（短絡5ペア）より大幅に悪化。原因は
TAP列を3本→2本に減らした分、行内のFILLセル（=force_jog着地用の
「逃げ場」）が大きく減り、45.4で確認した「bridge_finalの安全な
着地X未検出→未検証フォールバック」がより頻発するため。TAP間隔も
793.8um→1620um（ほぼ倍）に広がり、電源IRドロップ／ラッチアップ
マージンの観点でも要検討（このPDKでの許容タップ間隔の規定は
本セッションでは未確認）。

面積は行幅がv6と同じ1620umのため、チャネル削減の効果がそのまま
現れ、**v6（1.4437 mm²）より小さい1.2610 mm²（87.3%）**を達成
——4構成中最小。ただし短絡未解消の参考値である点は45.4と同じ。

| 構成 | 行幅×コア高さ | 面積 | v6比 | 検証状況 |
|---|---|---|---|---|
| 4行/5ch（v6, 基準） | 1620.0 × 891.2 um | 1.4437 mm² | 100.0% | 162/162クリーン、短絡0 |
| 3行/4ch, N_GAPS=2（幅2424.6） | 2424.6 × 766.4 um | 1.8582 mm² | 128.7% | 158/163クリーン、短絡5ペア |
| 3行/4ch, N_GAPS=1（幅1620、v6と同一） | 1620.0 × 778.4 um | 1.2610 mm² | 87.3% | 148/163クリーン、短絡15ペア |
| 2行/3ch, N_GAPS=1（幅2424.6） | 2424.6 × 657.6 um | 1.5944 mm² | 110.4% | 157/163クリーン、短絡6ペア |

出力：`LEF/placement_nrow_fm_v6_4ch_ngap1.json`、
`Layout/i2c_slave_async_nrow_fm_v6_4ch_ngap1_squeezed_routed.gds`
（他minheight/squeezed中間GDSも保存）。

## 46. トップブロックのフレーム接続準備：STDCELL GDS修正後の再合成確認とトップピンハイライト

ユーザーが`LEF/TR-1um_STDCELL.gds`を手修正した後の「再度合成くださ
い。STEP7で止めて、トップピンの位置をハイライトしてください」に
対応（次工程「トップブロックのピンをフレームに接続」の準備）。

### 46.1 GDS修正内容の確認

`gen_lef.py`でLEFを再生成し旧版と比較したところ、差分は**MUX2セル
内部のOBS（配線障害物）ポリゴン3個のみ**（配線の見た目調整と推定）。
全マクロのSIZE・PIN RECTは1行も変わらず——セル外形・ピン位置は無
変更。

### 46.2 再合成の実施と結果

`yowasp-yosys`で7.5節/42.3節と同じ合成スクリプト（`proc;opt /
techmap;opt / dfflegalize / dfflibmap / abc / opt_merge -share_all /
opt_clean`）を再実行（注：WASI版yosysは`/tmp`配下のパスを読み書き
できないため、出力先を`src/`直下の相対パスに変更する必要があった
——ハマりどころとして記録）。結果を`i2c_slave_async_net_v5.v`と比較
したところ、**RSTB1/RSTB2の手動リネーム（42.4節）を除いて完全一致**
（インスタンス数・接続とも同一）。46.1のGDS修正はSIZE/PINに影響
しないため、当然の結果として確認。

**結論：v6ネットリスト・配置・配線は変更不要**——既存の検証済み
成果物（0 DRC・162/162ネットクリーン）をそのまま使用する。念のため
配置を独立に再実行したところ、セル数・行分割・行幅は完全再現した
一方、1セル分の行内配置順序に説明のつかない差異が見られた（Python
の非決定的な集合順序が疑われるが未特定）。今回のGDS修正とは無関係
と判断し、既存の検証済み配置・配線ファイルを差し替えないことで
リスクを回避した。

### 46.3 トップレベルピンのハイライト

`script/highlight_top_pins_nrow_fm.py`（新規）：トップモジュール
`i2c_slave_async`の全24信号ポート＋VDD/GNDの物理位置を、v6の
STEP7（`Layout/steps_v6/v6_step_7_minheight_compressed.gds`、43.8節
のminheight圧縮版＝44節の幾何学的スクイーズを適用する直前の状態）
の上にマーカーとテキストラベルで可視化。

ポート名と実際のネット名が一致しないケースに注意——Yosysが出力
末尾で`assign rw = rw_bit; assign addr_match = addr_ok; assign
rx_data = rx_data_r; assign sda_oe = sda_oe_r;`という別名接続を
生成しているため、`sda_oe`/`addr_match`/`rw`/`rx_data[7:0]`は
それぞれ`sda_oe_r`/`addr_ok`/`rw_bit`/`rx_data_r[7:0]`という
内部ネット名でしか配置データに現れない。スクリプト内`PORT_NET_ALIAS`
/`BUS_PORT_NET_ALIAS`で対応表を持たせて解決。

また、`rst_n`/`scl`/`sda_in`/`rx_valid`/`busy`は単一ピンの
「スタブ」ではなく、複数セルへの多ファンアウト／多ファンインで
**既に内部配線済み**のネットだった（3〜7ピン）。この場合「唯一の
外部接続点」は存在しないため、そのネットの全ピン位置をハイライト
——M1/M2は連続導体なので、どのピンの位置からでもフレームへのタップ
が可能（ユーザーが行の位置に応じて選べるように全点表示）。
VDD/GNDはPOWER/GROUND用途のためnet_pinsから常に除外される
（35.9節）ので別扱い：コア上端・下端（Y=0とY=core_height）における
3本のTAP列のVDD/GNDストラップX位置を電源タップ点としてマーク。

出力レイヤー（`Layout/steps_v6/v6_step_7_top_pins_highlighted.gds`、
元のstep7ファイルは非改変・複製先に描画）：
- (260,0)/(260,1)：信号ピン55箇所（24ポート）のマーカー／ラベル
- (260,2)/(260,3)：VDD/GND電源タップ12箇所（3 TAP列×2ネット×
  上下端）のマーカー／ラベル

| ポート | 実ネット名 | ピン数 |
|---|---|---|
| rst_n | rst_n | 3 |
| scl | scl | 4 |
| sda_in | sda_in | 7 |
| sda_oe | sda_oe_r | 1 |
| rx_valid | rx_valid | 4 |
| addr_match | addr_ok | 5 |
| rw | rw_bit | 3 |
| busy | busy | 3 |
| tx_data[7:0] | tx_data[7:0] | 各1（[7]のみ2） |
| rx_data[7:0] | rx_data_r[7:0] | 各2 |

# 47. v7（優先M2コリドー配置）ルーティング＋リップアップ&リルート、再現性の記録

## 47.1 背景

v7ネットリスト（BUFTH挿入版、47.6参照の`run_route_v7_checkpoints.py`
docstring）を「優先M2コリドー」placement variant
（`LEF/placement_nrow_fm_v7_priomch.json`）で配置配線した結果
（通称v7v2）には配線後に9件の短絡疑いが残った。これをrouter自体の
各パスにパッチを当てて解消しようとした試みは複数回リグレッションを
招いたため放棄し、代わりに**配線済みGDSに対する汎用ポストプロセス**
として`script/ripup_reroute_shorts.py`を新規実装した。

## 47.2 ripup_reroute_shorts.py（汎用リップアップ&リルート）

- 入力：ルーター出力の`net_shapes`ログ（JSON）から同一レイヤー×異
  ネット間のボックス重なりを総当たりで検出（`find_conflicts`）。
- 検出した重なりを1件ずつ、幾何学的に小さい側のネットを「動かす側」
  として選び、ライブ衝突チェック付きでジョグ／トラック移設して解消
  （`try_fix_vertical`/`try_fix_horizontal`）。
- **複雑ネット保護ルール**：ピン数>8、または30um以上の実トランク
  セグメントが複数トラックレベルに存在するネットは「動かす側」に
  絶対選ばない（フォールバックでも不可）。両側が複雑ネットの場合は
  修正を諦めて未解決のまま報告する。これは`RSTB1`（23ピン・4チャネル
  跨ぎのリセットスパイン）をフォールバックで動かした結果、無関係な
  ネット（`txreg[4]`↔`scl_row3`）に新規短絡を作ってしまった実際の
  リグレッションを受けて追加したガード。
- via_1 PCellが`net_shapes`に現れない自前のM1/M2パッドを描画する点
  （自己衝突誤検出の原因）を、パッドの中心線ベースのボックス除外で
  対処済み。

v7v2に適用した結果：DRC違反0（M1/M2幅・スペース、V1関連すべて）、
9件の短絡疑いのうち複雑ネット保護ルールにより解決可能だった分を修正、
**7件が複雑ネット同士の短絡として意図的に未解決のまま残存**（下記
47.3）。この結果を`Layout/i2c_slave_async_nrow_fm_v7rr_routed.gds`
（＋`script/pin_map_nrow_fm_v7rr.json`/`net_shapes_nrow_fm_v7rr.json`）
として確定し、現在の正式な最終成果物としている。

## 47.3 残存7短絡の根本原因

GDSフォレンジック（step3→step4チェックポイント間のM2形状数比較）に
より、**pass 2（複数行にまたがるスパニングネット配線）にはライブ
衝突チェックが存在しない**ことが根本原因と特定。7件はすべてこの
パスで発生していた：

| # | net A | net B |
|---|---|---|
| 1 | RSTB1 | sda_in_row3 |
| 2 | scl_n_row2 | scl_n_row1 |
| 3 | sda_in_row3 | _074_ |
| 4 | RSTB2 | txreg[4] |
| 5 | scl_n_row1 | txreg[3] |
| 6 | txreg[4] | txreg[2] |
| 7 | scl_row0 | _134_[3] |

7件すべて、片側または両側が複雑ネット（幹線・高FO）のため
`ripup_reroute_shorts.py`の保護ルールにより安全側で未修正。

## 47.4 検討して不採用／保留にした案

- **パス順の入れ替え（pass2をpass1より先に）**：理論上は「スパニング
  vs 既に描画済みの他分類ネット」型の短絡（最大3件）しか解消できず、
  「スパニングvsスパニング」型（4件）はどちらの順でも無理。実装して
  テストしたが、無関係な別クラッシュ（RSTB2のrow1交差失敗）に阻まれ
  検証未完了のまま**元に戻した**（現在のコードにこの変更は含まれない）。
- **配置レベルの根本修正**（`RSTB1`/`RSTB2`を`PER_ROW_LOCAL_NETS`へ
  追加＋他5ネットを強制高FO扱い）：繰り返しrow1のチャネル容量クラッシュ
  を誘発し不安定。ユーザー判断により、安全な現状（v7rr）で一旦終了と
  決定。

## 47.5 ROW_X_TRIES修正（調査・実装済み、副作用なしと確認）

`route_channels_nrow_fm.py`の`find_row_clear_x`（行またぎクリアX探索）
の探索半径`ROW_X_TRIES`を200→250に拡大（±1080um→±1350um）。

原因：row1の交差可能な空きスロットは、処理が進むにつれて先行ネットの
交差によって徐々に消費される。さらにrow1右側約770umは未配線の生placement
の時点でほぼ空きスロットがない（配置密度の偏り）。この2つが重なると、
`RSTB2`のような後発ネットが交差点を探す時点で本当に必要な空きXが
探索半径のすぐ外側に出てしまう。ライブ計装した複製ロジックで実測：
最寄りの真に空いている列は211ステップ先だったのに対し、旧制限は200
ステップ——**11ステップ（≒60um）の不足**が直接の原因。

この修正はロジック変更を一切伴わない探索範囲の拡大のみで、単体では
副作用なしと確認済み。**現在のコードに適用済み**（47.6のスクリプトで
使用した状態でも回帰なしを確認）。

**追加検証（ユーザー依頼）**：47.6で確立した正確なv7v2再現レシピ
（正しい`CH_HEIGHTS`/`FORCE_JOG_NETS`/`PER_ROW_LOCAL_NETS`）に対して
`ROW_X_TRIES`を200に戻して再実行したところ、**クラッシュせず、
net_shapesも165ネット全てv7v2とバイト単位で完全一致**した。つまり
このベースライン自体はROW_X_TRIES=200でも250でも結果は同一で、
row1交差の探索が200ステップを超える場面は実際には発生していない。

結論：以前観測した「RSTB2のrow1交差失敗」クラッシュは、当時使用して
いた**推測（誤り）のCH_HEIGHTS/FORCE_JOG_NETS**——すなわち今回解消
した再現性ギャップそのもの——が原因で発生した現象であり、v7v2/v7rr
の正しいレシピに内在するバグではなかった。`ROW_X_TRIES=250`への
変更自体は無害でコストもゼロなので安全マージンとして維持するが、
**現行ベースラインの再現には必須ではない**——将来、行1のチャネル
密度がより厳しい別バリアント（CH_HEIGHTS変更やplacement変更）を
試す際の保険として残す位置づけ。

## 47.6 再現性ギャップの発見と解消

前回セッションでコンテキスト圧縮が起きた際、v7v2を生成した正確な
パラメータ（特に`FORCE_JOG_NETS`の実際の集合と`CH_HEIGHTS`）が失われ、
「ゼロから再生成」を試みるたびにクラッシュか大幅なDRC/短絡の悪化を
招いていた。今回、以下の手順で正確なレシピを再構築し、実行して
**検証済み**：

1. `script/run_route_v7_checkpoints.py`（Aug 20付で現存していた、
   本セッション最初期のドライバースクリプト）から`FORCE_JOG_NETS`
   （16ネット）と`PER_ROW_LOCAL_NETS`（7ネット）を復元。
2. `script/force_jog_events_nrow_fm_v7v2.json`（実際のジョグイベント
   ログ）と突き合わせ：ジョグが発生した15ネットは全て`FORCE_JOG_NETS`
   の要素（残り1つ`scl_n_row1`はジョグ0件で無矛盾）——**完全一致を確認**。
3. `CH_HEIGHTS`は上記スクリプトの値（`[98,320,300,280,100]`、コメント
   に「最初のv7試行時点の値」と明記）ではなく、
   `script/channel_usage_nrow_fm_v7v2.json`の`height_um`フィールド
   （ルーター自身が入力値をそのまま書き戻したもの）から実測値
   `[98.0, 332.0, 300.0, 280.0, 100.0]`を採用。
4. placement元は`LEF/placement_nrow_fm_v7_priomch.json`（優先M2
   コリドー版、`Step0-7_Review/README.txt`のstep0説明と一致）。

これらを`script/run_route_v7_from_scratch.py`（新規、恒久保存）に
まとめ、`gen_placement_gds_nrow_fm.py`→`route_channels_nrow_fm.py`
→`ripup_reroute_shorts.py`→DRC/接続性チェックの全パイプラインを
ゼロから実行。

**結果・確認事項：**
- 配置生成＋ルーター本体（`route_channels_nrow_fm.py`）は**完全に
  決定論的**であることを実証——2回独立実行した`net_shapes`ログが
  165ネット全件バイト単位で一致、かつ`channel_usage_nrow_fm_v7v2.json`
  （オリジナル）とも完全一致。つまり上記の復元パラメータは**正確**。
- 一方`ripup_reroute_shorts.py`は、同一の入力（v7v2のnet_shapes/
  pin_map）に対して実行するたびに「何件のボックス衝突を実際に修正
  できるか」が変動する場合があることを確認（今回の再実行では
  「1本＋1本」修正・8件残存 vs 元のv7rr生成時の「8本＋1本」修正・
  0件残存）。ただし**短絡が疑われる7つのネットクラスタ自体は両方の
  実行で完全に同一**（個々の重なりボックスの修正成否が異なるだけで、
  最終的にどのネット同士が繋がってしまうかという結果は変わらない）。
  DRCは両方の実行で違反0。原因はripup_reroute_shorts.py内部の
  幾何探索（klayout Region操作）のどこかに実行間で揺らぎがある
  可能性が高いが、未特定（優先度低：安全性・短絡件数に影響しないため）。
- 結論：**現行の`v7rr`成果物はそのまま正式版として維持**（今回の
  再現実行はracqui検証目的であり、v7rrより悪い結果だったため置き換え
  ない）。今後v7系を配線し直す必要が生じた場合は、必ず
  `script/run_route_v7_from_scratch.py`を実行すること——このスクリプト
  1本でplacement生成からripup_reroute適用・DRC/接続性検証まで再現
  できる。

## 47.7 現在の成果物・ファイル一覧（v7系、本節時点）

- `Layout/i2c_slave_async_nrow_fm_v7v2_routed.gds` /
  `script/{pin_map,net_shapes,channel_usage,force_jog_events}_nrow_fm_v7v2.json`
  ：ルーター単体の出力（決定論的に再現可能、47.6参照）。
- `Layout/i2c_slave_async_nrow_fm_v7rr_routed.gds` /
  `script/{pin_map,net_shapes}_nrow_fm_v7rr.json` ：
  ripup_reroute適用後の**正式最終レイアウト**（DRC違反0、短絡7件
  ※すべて複雑ネット同士で意図的に未解決）。
- `Layout/i2c_slave_async_nrow_fm_v7rr_with_err.gds` /
  `Layout/v7rr_err_report_details.txt` ：エラーレイヤー
  （(253,0)〜(253,4)）付きレビュー用コピーと座標詳細。
- `Layout/Step0-7_Review/`：placementからripup_reroute後・エラー
  ハイライトまでの8段階チェックポイントをまとめたレビュー用フォルダ
  （README.txt付き）。
- `script/run_route_v7_checkpoints.py`：初回v7試行（priomch適用前・
  CH_HEIGHTS未拡大）のドライバー。歴史的記録として保持。
- `script/run_route_v7_from_scratch.py`：**本節で確立した、v7v2/v7rr
  を再現するための正式な決定論的ドライバー**（新規）。

## 47.8 クリーンアップ記録

本セッション中に生成された実験的な中間ファイル（`v7`（無印）,
`v7final`, `v7final2`, `v7pri`〜`v7pri6`, `v7pri_minheight_*`,
`v7rowfix`、および対応する`Layout/steps_v7*`チェックポイントディレクトリ
と`script/*_nrow_fm_v7*.json`ログ）は、上記47.6の検証作業で生成した
一時ファイル（`v7fresh`/`v7freshrr`系）とあわせて全て削除済み。
`v7v2`／`v7rr`／`Step0-7_Review`／両ドライバースクリプトのみを保持。

# 48. STEP4短絡の根本修正：pass2ライブチェック追加・TRACK_PITCH化・
チャネル高さのグリッドスナップ

47節の続き。47.3で特定した「pass2（スパニングネット配線）にライブ
衝突チェックが存在しない」という根本原因に対し、実際にコードを修正
して短絡件数とDRCを検証した記録。

## 48.1 pass2の最終区間へのライブチェック追加

`route_channels_nrow_fm.py`のpass2ループで、行またぎ後（または直接）
トラックに到達するまでの最終区間が無チェックで描画されていた。
pass1のforced-overlap-zone処理と同じ`channel_clear`/
`find_channel_clear_x`/`draw_jog`パターンをこの最終区間にも追加。
`top`は全パス共有のライブなレイアウトなので、この1つのチェックで
「スパニングvs既存パス」型と「スパニングvsスパニング」型（同一
pass2ループ内で先に処理された別のスパニングネット）の両方をカバー
できる。

## 48.2 副作用：既存の潜在バグの顕在化とその修正

上記48.1の変更だけを入れてフルパイプラインを実行したところ、DRC違反
が0→123件に激増。原因調査の結果、**pass2の行またぎジョグ機構
（既存・今回変更していない箇所）が、`draw_jog`を`near_y`を渡さずに
呼び出しており、無条件かつチャネルの物理トラック上限を一切チェック
しない`claim_track`フォールバック経路に落ちていた**ことが判明
（該当箇所1615行目）。この経路は48.1変更以前から存在する潜在バグで、
元のv7v2でもchannel1が83本の予算に対し84本使用と1本オーバーしていた
が、たまたま実害が出ていなかっただけだった。48.1の変更が同一pass2
ループ内の後続ネットの行またぎ検索結果を変え、この潜在バグを大きく
顕在化させたのが123件激増の直接原因。

修正：1615行目の`draw_jog`呼び出しに`near_y=cur_y`を追加し、他箇所
（pass1・pass3・48.1の新規チェック）と同じ、チャネル予算内に収まる
安全な`claim_track_near`経路に統一。→ DRC違反123件→1件に激減。

## 48.3 残る1件のDRC違反：TRACK_PITCHとVIAパッドサイズの構造的な余裕不足

残存したM1スペース違反1件を調査した結果、`TRACK_PITCH`（トラック
ピッチ、当時4.0um）と`M1_PAD_SIZE`（via_1のパッドサイズ、3.4um）の
組み合わせが構造的にM1最小スペース1.4umの余裕を欠いていたことが
判明。M1トランク幅(1.8um)同士なら隣接トラックで隙間2.2umと十分だが、
**VIAパッド(3.4um)同士が隣接トラックの同じXに来ると隙間はわずか
4.0-3.4=0.6um**しかない。`draw_jog`の出発VIA（着地VIAとは違い、
衝突チェックされないまま元のピンのXに打たれる）が、たまたま自分自身
の本来のトラックと同じXで隣接トラックに着地するケースがこれに該当。

ユーザー判断により、個別のライブチェック追加ではなく**構造的な解決**
として`TRACK_PITCH`を4.0→5.4umに変更（`route_channels_nrow_fm.py`・
`ripup_reroute_shorts.py`両方、値を一致させる必要あり）。隣接トラック
の同X間隔が5.4-3.4=2.0umとなり、M1(1.4um)・M2(2.0um)双方の最小
スペースを常に満たす。

CH_HEIGHTSはピッチ変更に伴い右詰め見直しが必要（5.4/4.0=1.35倍の
高さが必要）。「生成的に大きすぎる高さを与えると使用トラック数が
予算に比例して膨張し続ける」という別の現象（後述48.5、以前の
セッションでも観測済み）があるため、既知の安定した4.0pitch時の
使用トラック数（[22,83,75,70,7]）から5.4pitch換算＋マージン2本分
で算出した値（[130,460,416,390,150]付近）を初期値として使用。
→ ルーター単体でDRC違反0件・全チャネルOK（オーバーフローなし）を
確認。

## 48.4 ripup_reroute_shorts.py側のvia近接チェック漏れ

48.3のTRACK_PITCH変更後、ルーター単体はDRC 0件だが、
`ripup_reroute_shorts.py`適用後にM1スペース18件・M2スペース2件の
新規DRC違反が発生。原因は`clear_excluding`の全5呼出し箇所が
`margin=0.0`（文字通りの重なりのみ検出、DRCの最小スペースルールを
反映していない）だったこと。`M1_MIN_GAP=1.4`定数を新設し、
`via_pad_clear`・`try_fix_vertical`・track relocationロジックの
全呼出し箇所に、レイヤーに応じた適切なmargin（M1は1.4、M2は2.0）を
渡すよう修正。→ DRC違反0件を回復（ただし判定が厳格になった分、
自動修正の成功率も下がり、短絡は3件ではなく5件が残存——複雑ネット
保護ルールにより無理な修正は行わないため、これは安全側の結果）。

## 48.5 チャネル境界での「行境界通過点 vs 通常トラック」近接問題

48.4のDRC違反のうち1件（`scl_row0`↔`_134_[3]`）を深掘りした結果、
via近接チェック漏れとは別の、より根本的な構造要因を発見。

該当VIA座標: (888.3, 650.4)〈scl_row0側〉と(888.3, 654.8)
〈`_134_[3]`側〉。Y方向間隔は4.4umで、期待される5.4um（1トラック
ピッチ）ではない。

- Y=650.4：チャネル1の**最後の正規トラック**（idx=84、
  `TRACK0_OFFSET + 84*TRACK_PITCH`）。
- Y=654.8：**row1の下端＝チャネル1の上端という行の境界座標そのもの**
  （`row_y0[1] = ch_y0[1] + CH_HEIGHTS[1]`）。トラックグリッドの対象
  外だが、`_134_[3]`（複数行にまたがるスパニングネット）がpass2の
  行またぎ処理で必然的に通過する物理的な地点。

チャネル高さ460umは`(460-2*TRACK0_OFFSET)/TRACK_PITCH`が割り切れず
（84.44…）、最後のトラックから境界までの余白が本来の1ピッチ分
（5.4um）ではなく端数の4.4umしかなかったことが直接原因。

**恒久対策**：`route_channels_nrow_fm.py`に`snap_channel_height()`
ヘルパーを新設。任意の目標高さを`TRACK0_OFFSET + K*TRACK_PITCH`
（K整数）ちょうどの値に切り上げる。この形にすると、
`budget = int((H-2*TRACK0_OFFSET)/TRACK_PITCH)+1 = K`となり、最後の
トラックから境界までの余白が必ずちょうど1ピッチ（5.4um）になる
ことを解析的に確認・実装：
```python
def snap_channel_height(min_height, track_pitch=TRACK_PITCH, track0_offset=TRACK0_OFFSET):
    k = math.ceil((min_height - track0_offset) / track_pitch)
    k = max(k, 1)
    return track0_offset + k * track_pitch
```
ユーザー指示により**全チャネル**に適用（1チャネルだけの対症療法に
しない）。[130,460,416,390,150] → [131.6, 461.0, 417.8, 390.8, 153.2]
に微増（各チャネル数umのみ）。

## 48.6 最終結果

`script/run_route_v7_from_scratch.py`（48.1〜48.5の全修正を反映、
CH_HEIGHTSは`snap_channel_height()`で自動算出）をゼロから実行し、
以下を確認：

- ルーター単体（pre-ripup）：**DRC違反0件**、全チャネルOK
  （オーバーフローなし）。
- `ripup_reroute_shorts.py`適用後（post-ripup、正式`v7rr`）：
  **DRC違反0件を維持**、短絡**5件**（元の7件から2件改善）：
  `scl_n_row1`↔`scl_n_row2`、`scl_row1`↔`scl_n_row1`、
  `txreg[4]`↔`RSTB2`、`txreg[2]`↔`txreg[4]`、`_134_[3]`↔`scl_row0`。
  いずれも片側または両側が複雑ネット（幹線・高FO）のため、
  `ripup_reroute_shorts.py`の安全ルールにより意図的に未修正のまま。

`Layout/i2c_slave_async_nrow_fm_v7v2_routed.gds`（ルーター出力）・
`Layout/i2c_slave_async_nrow_fm_v7rr_routed.gds`（正式最終成果物）・
対応する`script/*_nrow_fm_v7v2.json`/`*_nrow_fm_v7rr.json`を、この
結果で上書き確定。旧`Layout/i2c_slave_async_nrow_fm_v7rr_with_err.gds`
と`Layout/v7rr_err_report_details.txt`は旧短絡7件時点のまま**未更新
（stale）**——次にエラーレイヤーハイライトが必要になった際は
`script/gen_err_report_nrow_fm.py`を新しい`v7rr`一式で再実行する
こと。

## 48.7 変更したファイル一覧

- `script/route_channels_nrow_fm.py`：`TRACK_PITCH`4.0→5.4、
  `snap_channel_height()`新設、pass2最終区間へのライブチェック追加
  （`span_entry_jog_count`）、pass2行またぎジョグへの`near_y`追加。
- `script/ripup_reroute_shorts.py`：`TRACK_PITCH`4.0→5.4（route側と
  一致必須）、`M1_MIN_GAP=1.4`新設、`clear_excluding`の全5呼出し
  箇所に適切なmargin指定。
- `script/run_route_v7_from_scratch.py`：CH_HEIGHTSを
  `snap_channel_height()`経由の自動算出に変更、docstringを48節の
  内容に合わせて更新。

## 48.8 未着手・今後の課題

- 残存5件の短絡はいずれも複雑ネット同士のため、安全側で未解決の
  まま。47.4で検討済みの配置レベル見直し（RSTB1/RSTB2の
  PER_ROW_LOCAL_NETS化、高FO強制指定）は繰り返しrow1容量クラッシュ
  を招いており未解決——再挑戦する場合は本節の教訓（生成的に大きい
  CH_HEIGHTSを与えると使用トラック数が予算に比例して膨張し続ける
  現象、48.3参照）を踏まえ、根拠のある値から始めること。
- エラーレイヤーハイライト（`v7rr_with_err.gds`）と詳細レポート
  （`v7rr_err_report_details.txt`）は48.6時点でstale。

## 49. ループ配線・不要ジョグの調査と根本修正（`channel_clear`の自己衝突誤検知）

### 49.1 発端

ユーザーがKLayoutで`v7rr_routed.gds`を目視確認し、黄色でループ状の
配線、オレンジで不要に見えるジョグを複数箇所ハイライトしたスクリー
ンショットを提示。「M2をそのまま伸ばしても衝突は起こらないのに、
なぜジョグが入るのか」との具体的な指摘（座標: Y≈349/391のM1、
X≈360/389のループ）を受け調査。

### 49.2 座標追跡による特定

`net_shapes_nrow_fm_v7rr.json`から該当座標に一致する形状を検索し、
ネット`_061_`（4ピン、全ピンtrack_y=349.6の row-only ネット）を特定。
14個の形状のうち、以下がループの正体だった：
```
M2 357.4,196.4-360.8,392.8   (X≈359で196.4から392.8まで大きく迂回)
M1 359.1,391.9-391.5,393.7   (Y≈392でX=359→391.5へジョグ)
M2 389.8,349.6-393.2,392.8   (X≈390で392.8から349.6へ戻る)
```
`_166_.Y`(X=359.1)・`_167_.A`(X=391.5)・`_176_.B`という3ピンが同じ
row0にあり、いずれも channel_entry_y=196.4 から track_y=349.6まで
登る経路を必要とする。

### 49.3 真因: pass1の`channel_clear`に自ネット除外が無かった

`route_channels_nrow_fm.py`のpass1（row-only/adjacent-pair/high-FO）
は`channel_clear()`のみでライブチェックしており（`via_x_clear`は
pass2/pass3にしか無い）、`channel_clear`は`top.begin_shapes_rec_
overlapping`で実ジオメトリを直接クエリするだけで、**自ネット自身が
直前に描いた金属**を全く除外していなかった。

トレース結果：pin1(`_166_.Y`)が最初に処理されジョグしてX=359.1に
着地→ pin2(`_167_.A`)の自然X もたまたま359.1で、channel_clear が
pin1自身のM2ラン（357.4-360.8, Y=241.6-349.6）を「障害物」と誤認
→ 391.5まで回避ジョグ、さらにpin3(`_176_.B`, 自然X=391.5)も391.5
のpin2自身の金属を障害物と誤認…という連鎖でループ状の迂回が生成
されていた。同一ネットの金属同士がどれだけ近接・重複してもDRC上
問題にならないにもかかわらず、である。

### 49.4 修正1: `channel_clear`に自ネット除外を追加（v18）

`net_shapes[cur_net[0]]`からM2形状を再構成し、クエリ結果から
`db.Region`減算で除外。

### 49.5 副作用: via_1パッドは`net_shapes`に記録されていなかった

修正1だけでは`_061_`のループが解消しなかった。追加トレースで、
`channel_clear`のhitsに**via_1パッドの足跡（M2レイヤー上）**が2件
残っていることが判明——`place_via()`は`m2_box()`を呼ばないため、
`net_shapes`にvia位置が一切記録されておらず、自ネットのvia自体を
除外できていなかった。`net_vias[net]`（`place_via`呼び出し時に
(cx,cy)を記録）を新設し、`channel_clear`でvia_1パッド相当の箱
（M1_PAD_SIZE四方）も減算対象に追加（v18b）。これで`_061_`の
ループは完全に解消（14形状→8形状、Y=392.8への迂回が消滅）。
`_074_`も自己重複が367.2um²→110.2um²に改善。

### 49.6 `via_x_clear`（pass2/pass3）にも同様の修正を試したが不採用

同じ自己衝突誤検知ロジックの弱点が`via_x_clear`（pass2スパニング
ネット最終区間・pass3 FORCE_JOG_NETS）にもあると推測し、
`channel_used_x`にネット名をタグ付けして`collides(exclude_net=net)`
を追加する修正も実装・検証した。しかしこちらは実際に短絡数を
5→8〜9件に悪化させることが切り分け実験で判明（`channel_clear`修正
だけを有効にした場合は短絡5件のまま変化なし、`via_x_clear`修正を
足すと必ず増加）。原因はFORCE_JOG_NETS（`bit_cnt[3]`等）が以前は
誤検知のおかげで衝突ゾーンを迂回していたが、正しく直進するように
なった結果、複合ネット（`_061_`等、`ripup_reroute_shorts.py`が
安全のため移動対象から除外するネット）の既存トランクと実際に近接・
重複するようになったため。チャネル高さに余裕を持たせても解消せず
（使用トラック数が予算に比例して膨張する既知の現象を再確認）、
根本的にはトラック割当順序への依存が深いと判断し、`via_x_clear`
側は元の（自己除外なしの）挙動に**差し戻した**。

### 49.7 最終結果

- `channel_clear`の自ネット除外（`net_vias`込み）のみ採用。
- DRC: 0/0/0 violations（router出力・ripup後とも変化なし）。
- 短絡: 5件のまま変化なし（元のbaselineと完全に同じ組み合わせ）。
- `_061_`の目視ループパターンは解消。`_074_`の自己重複も改善。
- 全体の自己重複合計: 8034.1um² → 7777.1um²（6ネット中2ネットで
  改善、他4ネット(RSTB1/addr_ok/_071_/_086_ の PER_ROW_LOCAL_NETS
  スパイン機構由来)は本修正のスコープ外で変化なし）。

### 49.8 検証スクリプト

`script/detect_loops_jogs.py`を新設。`net_shapes_*.json`を読み、
(1) 各ネットの形状をグラフ化して閉路（真の冗長配線）を検出、
(2) 短いM1橋渡し（ジョグ候補）をカウント、
(3) klayout Regionで同一ネット内の自己重複面積を算出、の3種の
客観指標でループ・不要ジョグを定量評価できる。
`python3 detect_loops_jogs.py <net_shapes.json>`で実行。

### 49.9 変更したファイル

- `script/route_channels_nrow_fm.py`: `channel_clear`に自ネット
  除外（`net_vias`新設含む）。`via_x_clear`は検証の上、元の挙動に
  差し戻し（コメントで経緯を記録）。
- `script/run_route_v7_from_scratch.py`: `step4_verify`が
  `verify_connectivity_nrow_fm.py`にscan_wを渡していなかった
  バグを修正（デフォルト1500umが実際の行幅1620umより狭く、
  X>1500の全ピンが「PIN NOT FOUND」等の偽陽性になっていた——
  今回の調査で発見、本質的な回帰ではないが独立した既存バグとして
  修正）。
- `script/detect_loops_jogs.py`: 新規（検証スクリプト）。
- `Layout/i2c_slave_async_nrow_fm_v7v2_routed.gds` /
  `i2c_slave_async_nrow_fm_v7rr_routed.gds`、および対応する
  `script/*_v7v2.json` / `*_v7rr.json`: 本節の最終結果で更新。

## 50. `via_x_clear`再有効化とRSTB1系3ウェイ短絡の根本修正

### 50.1 経緯

49節で`via_x_clear`（pass2/pass3の自己衝突除外修正）は「5→8~9件に
短絡が増える」ため不採用として元に戻していた。しかしユーザーが
Y=1112のM1（`shreg[2]`、X=953/967のM2）で別の不要ジョグを発見。
調査の結果、49.6で見送った`via_x_clear`のバグ（自ネットの登録済み
via Xを除外していない）と全く同じ根本原因クラスだと判明。
ユーザー判断により`via_x_clear`を再有効化し、それにより増える短絡を
別途解消する方針を採用（このセッションの一連の作業）。

### 50.2 `_074_<->scl_row2`調査で見つかった2つの独立バグ

`via_x_clear`再有効化後、`_074_<->scl_row2`が実質同一track_yに
割り当てられる短絡を調査し、2つの独立したバグを特定・修正:

- **Fix1**: `find_row_clear_x()`が`priority_corridor_x`を
  `start_x`からの距離を無視して無条件優先していたため、1166um先の
  corridorが、実際はもっと近くにあった空きXより先に採用されていた。
  corridorとlocal gridを距離順にマージした1本の候補リストに統一。
- **Fix2**: `claim_track_near()`の`allow_claimed=True`（他ネットの
  空きストレッチ再利用）が、FORCE_JOG_NETS所属ネット用に構造上
  予約済みだが**まだ描画されていない**トラックを「空き」と誤認して
  横取りしていた（FORCE_JOG_NETSの実描画はpass3まで意図的に遅延
  されるため）。`force_jog_track_idx`集合を新設し、
  `claim_track_near`の探索から除外。

Fix1+Fix2で`_074_<->scl_row2`・`_009_<->scl_row1`が解消。ただし
Fix1単独では新規の短絡`RSTB1<->scl_n_row2`が発生することが判明
（isolation testで確認）。ユーザーは「Fix1+Fix2を採用し、RSTB1衝突
も追加修正する」を選択。

### 50.3 Fix3: pass2スパニングネットの「未チェックleg」

`RSTB1<->scl_n_row2`をGDS実地調査した結果、pass2の`ordered_rows`
クロス処理で、前回位置(`cur_y`)から次の行の近傍境界(`entry_y`)まで
の区間（チャネル全域を跨ぐ場合がある）が`channel_clear`で一切
チェックされずに描画されていたことが判明。行本体の通過
（`find_row_clear_x`/`row_clear`）や最終leg（48.1で修正済み）とは
異なり、この区間だけノーチェックだった。`channel_clear`+
`find_channel_clear_x`によるチェックを追加（8→5件に改善、
`_134_[3]<->scl_row0`もついでに解消）。ただし`RSTB1<->scl_n_row2`
（`scl_row1`も巻き込む3ウェイ）は解消せず、残存。

### 50.4 Fix4: pass2最終run区間の`bridge_final`呼び出し漏れ

pass2のspanning-net最終run（`cur_y`→`track_y`）は、衝突検出時に
`draw_jog`でジョグ先を探すが、**`draw_jog`が返した後の区間を
再チェックしていなかった**（pass3のFORCE_JOG_NETS処理は
`draw_jog`の後に`bridge_final`を呼んで再検証しているのに、pass2の
このブロックだけ欠けていた）。特に`draw_jog`自身の最終フォール
バック（`departure_leg_ok`すら満たすトラックが無い場合、無条件で
最寄りの空きトラックを掴む）が発火すると、着地区間は完全に無検証
のまま描画されていた。`bridge_final`呼び出しを追加（安全な改善だが
単独ではRSTB1短絡は解消せず）。

### 50.5 真因確定: leg検証と行クリアランス探索の順序バグ

`route_channels_nrow_fm.py`にnet固有のデバッグ出力を仕込み、
`scl_row1`の実際の描画順序をライブトレースして特定:

pass2の`ordered_rows`ループ内で、(a) チャネル跨ぎleg
（`cur_y`→`entry_y0`）のチェックが**先**に行われ、(b) その後
`find_row_clear_x()`が行本体のクリアランスのために`cur_x`を
**別のXへジョグする場合がある**、という順序だった。scl_row1の
あるピンでは、(a)の時点ではX=602.1でleg・クリアとも問題なかった
が、(b)で行2のセル本体クリアランスのためX=575.1へジョグ。その
575.1で描画されたlegの区間 (X=573.4-576.8, Y=735.0-1140.0) は
**一度も検証されないまま**RSTB1の既存トランク（同じXレンジ、
Y=722.2-886.2）に重なって描画されていた。

2種の追加修正を試したがどちらも他ネットのトラック枯渇を誘発し
総短絡数が5→7に悪化（採用せず、経緯はコード内コメントに記録）:
  - 順序を入れ替えて行クリアランスジョグを先に行い、その後の
    最終cur_xでlegを再チェック・再ジョグ（`draw_jog`のトラック
    確保機構を再利用） → 他ネットのフォールバックを誘発。
  - 同様の再チェックを`bridge_final`と同じ「新規トラックを
    確保しない水平M1橋渡し」方式に変更 → この特定leg
    （Y=735-1140, channel2）には橋渡し可能な空きXが元々存在せず、
    効果なし。

**採用した修正**: `find_row_clear_x()`自体の探索に、legの
`channel_clear`要求を`extra_ok`として畳み込み、行クリアランスと
leg クリアランスを**同時に満たすX**を最初から探すように変更
（`leg_ok`ラムダ）。これにより追加のジョグ/橋渡しが一切不要になり、
他ネットのトラック予算を消費しない。`find_row_clear_x`が
そのようなXを見つけられない場合のみ`SystemExit`を捕捉し、
legを外した従来探索にフォールバック（警告付き）。

### 50.6 最終結果

5件→**2件**に短絡削減。ユーザーが指摘した`RSTB1<->scl_n_row2`
（`scl_row1`巻き込みの3ウェイ）は完全に解消。DRC 0/0/0を維持。

残る2件（未解消、詳細は50.8参照）:
  - `scl_n_row1<->scl_n_row2`: 同一Xコラム（692.2）での重なり
    （Y=1113.0-1129.2）。
  - `_133_[0]<->txreg[2]`: 元の5件短絡ベースラインから存在していた
    既存の未解決短絡。

### 50.7 変更したファイル

- `script/route_channels_nrow_fm.py`: `via_x_clear`再有効化
  （`channel_used_x`をnetタグ付きタプルへ変更、`exclude_net`
  パラメータ追加）。Fix1（`find_row_clear_x`距離順マージ）、
  Fix2（`force_jog_track_idx`によるトラック横取り防止）、Fix3
  （pass2 leg区間の`channel_clear`チェック追加）、Fix4（pass2
  最終runの`bridge_final`呼び出し追加）、Fix5
  （`find_row_clear_x`の`extra_ok`にleg クリアランスを畳み込み）、
  Fix6/v26（pass3 FORCE_JOG_NETSの「出発leg」がチェック範囲外で
  無条件描画されていた同種の漏れを修正——本セッションの`scl_n_row1`
  自体には効かなかったが、実際にpass3経路を通る他のFORCE_JOG_NETS
  ネットに対する独立した改善として維持）。
- `Layout/i2c_slave_async_nrow_fm_v7v2_*` /
  `i2c_slave_async_nrow_fm_v7rr_*`、および対応する
  `script/*_v7v2.json` / `*_v7rr.json`: 本節の最終結果（短絡2件、
  DRC 0/0/0）で更新。
- `v7rr_with_err.gds` / `v7rr_err_report_details.txt`も本節の
  最終結果（短絡2件）で再生成済み。

### 50.8 残る2件の追加調査: どちらもチャネル容量の枯渇が真因

ユーザー指摘（「どちらも複数のチャネルをまたぐスパインネット」）を
受けて追加調査。

- `scl_n_row1<->scl_n_row2`: `scl_n_row1`は実際には`FORCE_JOG_NETS`
  に含まれるが、行をまたぐピン（`u_buf_scl_n_row1.Y`, row3→channel2）
  を持つため`span_channel_assign`に分類され、**pass3ではなくpass2の
  `spanning_nets`ループで処理されている**ことを確認（コード上
  `force_jog_pads`は`non_spanning_nets`からのみ構築されるため）。
  実際のライブトレースで、pass2の最終run区間の`channel_clear`は
  正しく衝突を検出していた（`channel_clear=False`）。しかし
  `draw_jog`が`y=1140.0`付近のchannel2で「クリアな出発legを持つ
  トラックが見つからない」ため、**無条件で最寄りの空きトラックを
  掴む最終フォールバック**が発動し、その出発leg自体が
  `scl_n_row2`の既存トランクにそのまま重なって描画されていた。
  これはコードのチェック漏れではなく、`channel_clear`
  ・`draw_jog`とも正しく機能した上での**純粋な容量枯渇**
  （channel2は77/77トラックで満杯）。
- `_133_[0]<->txreg[2]`: 同様に`bridge_final`の警告
  （"found no clear+M1-safe landing X near x=434.7 ... channel 1"）
  が示す通り、channel1（85/85トラックで満杯）での容量枯渇による
  同種のフォールバック。
- 検証: channel1・channel2に+10um（トラック数+2相当）ヘッドルーム
  を追加して再ルーティングを試したが、49節で既に確認された
  「使用量が予算に合わせて拡大する」病理が本セッションの一連の
  修正後も再現し（channel1: 85→87/87、channel2: 77→79/79、
  警告メッセージも同一X/Yのまま）、単純な容量追加では解消しない
  ことを再確認。
- 結論: 残る2件は、コードのチェック漏れではなく、**同じチャネルの
  同じ狭いY範囲を複数の独立したマルチチャネル・スパインネットが
  同時に必要とする構造的な輻輳**が真因。解消には（a）チャネル1/2の
  トラック予算配分そのものを見直す、（b）`scl_n_row2`のように
  1ネットが並列に多数のXコラムを消費するスパイン構造自体を
  再設計する、（c）pass2のネット処理順序を輻輳箇所ごとに動的に
  調整する、といった、より大きな構造変更が必要——本セッションの
  対象外として保留。

### 50.9 「優先行チャネル（priority_corridor_x）はなぜ使われないのか」（ユーザー質問）

ユーザーから、複数チャネルをまたぐ優先M2コラム
（`priority_corridor_x` = [13.5, 18.9, 796.5, 801.9, 818.1, 823.5,
1601.1, 1606.5] — 全行で共通してFILL2セルのみのため構造的に常に
クリアな8トラック）が、なぜこの2件の輻輳を回避するのに使われて
いないのか質問があり調査。

**発見**: `priority_corridor_x`は`find_row_clear_x`（行本体の
セルクリアランス探索、Fix1参照）にしか組み込まれておらず、
`find_channel_clear_x`（チャネル内の衝突回避探索——pass1の強制
重複ゾーン処理、pass2のleg橋渡し・最終run、pass3の
FORCE_JOG_NETS、`bridge_final`が全てこの関数を経由）には
**一切候補に入っていなかった**。つまり、行のセル本体を跨ぐ処理
だけがコラムを共有する機会を持ち、他ネットの既存配線を避ける
衝突回避処理は毎回ピン自身の自然なX付近だけを独立に探索していた。

**Fix v27**: `find_channel_clear_x`にも`priority_corridor_x`を
Fix1と同じ距離順マージ方式で候補に追加。

**結果**: 短絡数・DRC・M1/M2/V1コンポーネント数とも変化なし
（`net_shapes`のボックス数も992個で完全一致）。理由は、今回の
2件の真因（50.8）が**着地X（`clear_x`）ではなく出発leg**にある
ため: `draw_jog`は「ピン自身の元のX（変更不可）」から新しい
track_yへ**垂直に**離脱するlegをまず必要とし、`claim_track_near`が
その離脱leg用のトラックを見つけられないと、離脱先のX（コラムを
含む）に関わらず即座に無条件フォールバックへ落ちる。コラムは
「水平ジャンプ先」の共有には効くが、「垂直離脱そのものが可能か」
には無関係——scl_n_row1のピンはX=693.9に固定されており、
その列をscl_n_row2がchannel2のほぼ全域で占有しているため、
コラムがどこにあっても離脱できない。

Fix自体は他の（今回の2件以外の）衝突回避経路に対する一般的な
改善として安全に維持（副作用ゼロを確認済み）。真の解決には、
`claim_track_near`側（垂直方向のトラック選択）にも同様の
「共有しやすい優先トラック」概念を導入するか、50.8で挙げた
より大きな構造変更が必要。

### 50.10 ユーザー指示「b＋dを試しましょう」— 実装結果

50.8でまとめた構造的な見直し案のうち、(b)スパインネットの並列
Xコラム構造の再設計と(d)離脱leg側への優先トラック概念導入を
実装・検証した。

**(d) draw_jogの最終フォールバックをbridge_finalスタイルに変更
（v28、採用）**: `draw_jog`が「クリアな出発legを持つトラックが
見つからない」場合の最終フォールバックは、以前は無条件で最寄りの
空きトラックを掴んでいた（出発leg・M1走行とも完全に未検証）。
これを、呼び出し元が既に検証済みの`clear_x`へ、**新規トラックを
一切確保せず**、現在の`cur_y`のまま水平M1ブリッジで直接到達を
試みる方式に変更（`m1_run_clear`のみ確認）。安全性: 短絡数・DRCとも
変化なし（副作用ゼロを確認）。効果: 残る2件には効かなかった
——`bridge_final`の検証で、ブリッジ候補地点（`cur_y`=行境界、
例えばY=1140.0）自体が他の多数のネットの行クロスM1トランクで
既に混雑しており、`m1_run_clear`が失敗した。これは「着地先Xの
選択肢」の問題ではなく「橋渡し地点そのものが空いていない」問題
であることが分かった。それでも、他の状況（橋渡し地点が空いている
ケース）に対する安全な改善として維持。

**(b) spanning netの並列コラムをtrunk+spine方式に統合（v29、
不採用・ロールバック済み）**: `scl_n_row2`（7ピン、設計全高を貫く
14本の独立Xコラム）のような、同じ行から同じターゲットへ複数ピンが
向かうケースをグループ化し、各ピンは自分の行からの最初の交差だけ
個別に行い、その後は共有の1本のコラムへ収束させ、行境界Y位置に
1本のM1「収束トランク」でまとめて接続する設計を実装・テスト。

結果は悪化: 短絡2件→8件（scl_n_row1/scl_n_row2/scl_row1/_074_/
txreg[6]/txreg[4]/txreg[2]/shreg[4]/scl_row3が1つの巨大な
連結成分にマージ）、DRC M1スペース違反 0件→34件。

**原因**: 新設した「収束トランク」を置く行境界Y（例えばY=1140.0）
は、そのネット固有の位置ではなく、**設計全体で多数の
spanning netが行クロスの際に共通して使う境界値**である。
そこに幅広いM1トランクを新たに敷いたことで、既に同じY付近に
存在していた他のネットのM1ジオメトリと直接衝突し、以前は
無かった短絡とDRC違反を大量に生み出した。PER_ROW_LOCAL_NETSの
trunk+spine方式（50.9参照、pass0）がこの問題を起こさないのは、
各ネットが行ごとに**専用に確保された**track_y
（`per_row_track_y`辞書）を使うためで、spanning netにはこの
専用トラック予約の仕組みが無い。同じ着想を安全に適用するには、
収束トランクの位置ごとに専用のtrack_yを別途確保する仕組みが
別途必要——今回のセッションの範囲を超えるため、v29の変更は
全てロールバックし、元の1ピン1コラム方式に復帰した
（コード内コメントに経緯を記録）。

**結論**: (d)は安全な副作用ゼロの改善として採用、(b)は
今回の実装では悪化したため不採用・ロールバック。最終状態は
50.6/50.8と同じ短絡2件・DRC 0/0/0（`scl_n_row1<->scl_n_row2`、
`_133_[0]<->txreg[2]`）のまま。真の(b)実現には、spanning net
にも per-row-local相当の専用track_y予約機構を追加する、
より大規模な設計変更が必要。

## 51. 残る2件の短絡を「行優先チャネル」アプローチで解消（v30〜v32b）

### 51.1 ユーザー指示

「scl_n_row1<->scl_n_row2も、`_133_[0]<->txreg[2]`も、行優先ネット
（`priority_corridor_x`）を使えば解決出来るように見えます。調査
ください。」との指示を受け、50.9で「着地Xの共有だけでは効かない」
と結論づけた`priority_corridor_x`アプローチを、離脱leg側にも及ぶ
形で再検討・実装した。

### 51.2 v30: pass2の最終行クロス探索に「target_yまでの全区間」
ルックアヘッドを畳み込み（`scl_n_row1<->scl_n_row2`を解消）

50.8の調査で、`scl_n_row1`は実際にはpass2の`spanning_nets`ループで
処理されており、`ordered_rows`の**最後の**行クロスで選ぶ`clear_x`
（`find_row_clear_x`が返す、行本体クリアランスのためのX）が、
その後の「チャネルへの最終着地〜`track_y`」区間では検証されない
ままだったことが真因と判明していた（Fix5/v26は"次の1leg"だけを
畳み込んでおり、"その先track_yまでの全区間"は見ていなかった）。

**修正**: `ordered_rows`ループで`row_k`が最後の行のとき、
`find_row_clear_x`に渡す`extra_ok`（`leg_ok`）に、次の1leg分の
チェックに加えて、**そのXが最終的な`track_y`まで届く残り区間
全体でもクリアかどうか**（`channel_clear`）と`via_x_clear`を
追加で要求するよう変更。それ以外（最後の行でない場合）は従来通り
1leg分のみのチェック。これにより、探索自体が「後々track_yまで
問題なく到達できるX」を最初から選ぶようになり、後段で
着地不能に陥ってから場当たり的にフォールバックする必要が
なくなった。

**結果**: `scl_n_row1<->scl_n_row2`短絡が解消（短絡2件→1件）。
DRC 0/0/0を維持。pass2最終run区間のジョグ件数が3→1に減少し、
Y=1140.0/X=693.9で出ていた警告も消滅。

### 51.3 `_133_[0]<->txreg[2]`はpass2ではなくpass3（FORCE_JOG_NETS）
であることを確認

`_133_[0]`は`FORCE_JOG_NETS`に列挙されているが、`scl_n_row1`とは
異なり全ピンが単一行内（row-only）のため`non_spanning_nets`に
分類され、実際に`force_jog_pads`に含まれてpass3で処理される
ことを確認。pass2にはこの短絡と対応するコードパスが存在しない
ため、v30と同じ形の修正はそのまま移植できない
（pass3にはpass2の`ordered_rows`のような行クロス探索ステップが
無く、ピンの`cx`は標準セル自身の配置からそのまま決まる）。

### 51.4 v31（不十分）: `draw_jog`の`near_y`アンカーを`track_y`に変更

GDS/`pin_map`実地調査で、`_133_[0]`のピン`_177_.A`
（`cx`=402.3, `pin_edge_y`≈163、row0内）が`clear_x`=434.7へ
ジョグされ、`draw_jog`の最終フォールバック（無条件で最寄りの
空きトラックを掴む）でジョグ先が`jog_y`=403.6に着地している
ことを確認。当時の`near_y`引数は`pin_edge_y`（=163、
channel1の実レンジ196.4-657.4の**外**）であり、
`claim_track_near`はchannel内のインデックスしか扱えないため
無意味な基準点になっていた、という仮説の下、`near_y`を
`track_y`（=360.4、常にchannel内かつ検証済み範囲内）へ変更。

**結果**: 警告メッセージのY基準は163.0→360.4に変わったが
（パラメータ変更自体は効いている）、`draw_jog`のトラック探索・
v28の直接ブリッジ・`bridge_final`側のフォールバックのいずれも
同じ理由で失敗を続け、短絡は解消しなかった。

### 51.5 真因の特定：検証済み区間の「外」への着地

`pin_map`を直接調査した結果、決定的な事実が判明した:

```
_133_[0]:  _177_.A -> (434.7, 360.4)   track_y=360.4
txreg[2]:  _248_.Q -> (434.7, 387.4)   track_y=387.4
```

`_133_[0]`の`clear_x`探索（`find_channel_clear_x(cx, [pin_edge_y,
track_y]=[163, 360.4], ...)`）は、X=434.7が**この[163,360.4]の
範囲でだけ**M2クリアであることしか検証していない。ところが
`txreg[2]`の`Q`ピンは同じX=434.7、Y=387.4——**この検証範囲の外
（360.4より上）**に存在する。`draw_jog`の最終フォールバックが
選んだ`jog_y`=403.6も同様にこの検証範囲の外（360.4より上）で
あり、その後`bridge_final`がジョグ着地点(434.7, 403.6)から
本来のtrack_y=360.4まで引く最終区間が、まさにY=387.4
（`txreg[2]`のQピン）を通過してしまう。`bridge_final`自身は
この衝突を検出して迂回Xを探すが、探索範囲内に代替Xが見つからず
「検証なしでそのまま描画」という最後のフォールバックに落ち、
これが短絡として顕在化していた。

すなわち根本原因は、`draw_jog`の**いずれのフォールバック層も**、
呼び出し元（`find_channel_clear_x`）が実際に検証した区間
`[pin_edge_y, track_y]`の**外**へジョグ先が着地しうる、という
構造的な穴だった。v31の`near_y`変更は探索の「優先順位」を
変えただけで、この穴自体は塞いでいなかった。

### 51.6 v32/v32b: `draw_jog`に`y_bound`を追加し、全フォールバック層を
検証済み区間内に拘束（採用、`_133_[0]<->txreg[2]`を解消）

**v32**: `draw_jog`に新しい任意引数`y_bound=(lo, hi)`を追加。
`departure_leg_ok`（tier1/tier2共通）に`in_y_bound(jog_y)`
チェックを追加し、tier1・tier2の探索候補を`y_bound`内に限定。
v28の直接ブリッジ分岐にも`in_y_bound(cur_y)`を追加（`cur_y`は
通常`y_bound`の端点そのものなので実質的に無条件で許可される）。
最終フォールバック（無条件`claim_track_near`）も、`y_bound`が
与えられている場合は`in_y_bound`のみを`extra_ok`として渡す
制約付き版に置き換え、それでも見つからない場合にのみ真に
無制約な最終手段へ落ちる3段構成にした。pass3側の呼び出しで
`y_bound=(min(pin_edge_y, track_y), max(pin_edge_y, track_y))`
（＝`find_channel_clear_x`が実際に検証した区間そのもの）を渡す。

**v32b（v32だけでは不十分と判明、追加修正）**: v32を試した結果、
「`y_bound`内に空きトラックが1つも無い」という新しい失敗
（`channel1`が85/85トラックで満杯——50.8で確認済みの容量枯渇と
同種）に直面した。`allow_claimed=False`のままでは未使用
（未クレーム）インデックスがそもそも存在しないため、`y_bound`を
満たすかどうか以前に候補が尽きていた。43.6のアイデア
（`allow_claimed=True`で他ネットが確保済みだが物理的に空いている
区間を再利用）と`y_bound`を組み合わせ、`m1_run_clear`と
`in_y_bound`の両方を満たす場合のみ他ネットのトラックを共有する
段（v32b）を追加。

**結果**: `_133_[0]<->txreg[2]`短絡が解消。`ALL NETS FULLY
CONNECTED, NO SHORTS DETECTED`（短絡0件）。DRC
0/0/0を維持（M1=1140, M2=609, V1=1202）。

### 51.7 最終結果

短絡2件（`scl_n_row1<->scl_n_row2`、`_133_[0]<->txreg[2]`）が
いずれも解消し、**短絡0件・DRC 0/0/0**を達成。
`gen_err_report_nrow_fm.py`による独立検証（Union-Find再構築）
でも短絡0件・共有連結成分0件を確認済み。

### 51.8 変更したファイル

- `script/route_channels_nrow_fm.py`:
  - v30: pass2の`ordered_rows`ループ、最後の行クロスの
    `leg_ok`に「`track_y`までの全区間クリアランス＋
    `via_x_clear`」を追加。
  - v31: pass3 FORCE_JOG_NETSの`draw_jog`呼び出しの`near_y`を
    `pin_edge_y`から`track_y`に変更（単独では不十分だったが
    v32との組み合わせでは有用な探索優先順位として維持）。
  - v32/v32b: `draw_jog`に`y_bound`引数を追加。全フォールバック
    層（departure_leg_ok経由のtier1/2、v28直接ブリッジ、
    最終フォールバック）が、呼び出し元の検証済みY範囲の外へ
    絶対に着地しないよう拘束。`y_bound`使用時の最終フォール
    バックは「制約なしで空きトラック確保」の前に、まず
    「`allow_claimed=True`＋`m1_run_clear`＋`y_bound`」
    「`y_bound`のみ」の2段を追加。pass3側で
    `y_bound=(pin_edge_y, track_y)`を渡すよう変更。
- `Layout/i2c_slave_async_nrow_fm_v7v2_*` /
  `i2c_slave_async_nrow_fm_v7rr_*`、および対応する
  `script/*_v7v2.json` / `*_v7rr.json`: 本節の最終結果
  （短絡0件、DRC 0/0/0）で更新。
- `v7rr_with_err.gds` / `v7rr_err_report_details.txt`も
  本節の最終結果（短絡0件）で再生成済み。

### 51.9 今後の課題

- `gen_err_report_nrow_fm.py`の`NET_FILE`定数が
  `src/i2c_slave_async_net.v`を指しているが、この
  ファイルには`scl_n_row1`/`scl_n_row2`が存在せず、実際の
  該当ソースは`src/i2c_slave_async_net_v7.v`らしいことが本
  セッション中に判明した（未修正）。`top_level_ports()`による
  ポート判定が誤ったネットリストを見ている可能性があり、
  err reportの「PORT: / OPEN:」ラベル分類に影響しうるため、
  次回要確認。
- `y_bound`は現状pass3のFORCE_JOG_NETS呼び出しにのみ渡している。
  pass2側の同種フォールバック（v28）にも同じ穴が理論上あり
  うるため、同様の事象が将来発生した場合は同じ`y_bound`拘束を
  移植できる。

## 52. v33: (253,4) ERR_FORCE_JOGハイライトの意味と、不要だった
最後の1件のジョグ（`bit_cnt[0]`）の解消

### 52.1 ユーザー質問

短絡0件達成後、`v7rr_with_err.gds`の(253,4)レイヤー
（`FORCE_JOG_NETS`が実際に挿入したジョグをハイライトする層——
`gen_err_report_nrow_fm.py`冒頭のdocstring参照）に表示されていた
ジョグについて、「253/4でハイライトしているジョグの意味は？
これもジョグは必要ないパスですね」との質問・指摘があった。

この時点で(253,4)には2件のジョグが残っていた
（`force_jog_events_nrow_fm_v7v2.json`）: `bit_cnt[0]`
（X=1185.3→1196.1）と`_133_[0]`（51節で解消した短絡の当事者、
X=402.3→434.7——こちらは51節の修正で実際に短絡を防いでいる
正当なジョグ）。ユーザーの指摘は残る`bit_cnt[0]`側を指している
と判断し調査した。

### 52.2 調査: `bit_cnt[0]`のジョグは実際に必要だったか

`route_channels_nrow_fm.py`のpass3該当箇所に一時的なデバッグ出力
を仕込み、`bit_cnt[0]`のピン（`_262_.Q`, cx=1185.3, channel1,
track_y=355.0）に対する`channel_clear`/`via_x_clear`の判定結果を
ライブトレースした。

結果: `via_x_clear=True`（衝突なし）だが`channel_clear=False`。
ところが`channel_clear`内部のライブM2衝突検出（実際に描画された
形状に対する`begin_shapes_rec_overlapping`プローブ）は
**hits count=0**——つまりFalseを返しているのはこの直後の
`static_stub_blocked`（他ネットの"未描画時点での"静的スタブ位置と
の衝突チェック）だと判明。

さらに`static_stub_blocked`にもデバッグ出力を追加したところ、
`_024_`ネットによってブロックされていることが判明:
`sx=1185.2999999999997`（`bit_cnt[0]`の`cx`とほぼ完全一致、
dx≈4.5e-13）。しかし`net_shapes`で`_024_`の実際に描画された
M2形状を確認すると、中心x=1190.7（1189.0-1192.4）——`_024_`
自身も別の衝突を避けるためX=1185.3から1190.7へジョグ済み
だった。

### 52.3 根本原因: `static_stub_blocked`のpass順序前提がpass3には
当てはまらない

`static_stub_blocked`は「他ネットがまだ描画されていない時点」の
順序依存の穴を埋めるための、**ジョグ前の生のピン座標**に基づく
保守的な事前チェックである（元々は`_115_/_080_`短絡——pass0が
pass1より先に描画されるために発生した——のために導入されたもの、
38.18/38.21参照）。

しかしpass3（FORCE_JOG_NETS）は設計上**最後に実行される**パス
であり、`_024_`を含む他の全ネットはpass3が始まる時点で既に
実際に（ジョグ後の最終位置で）描画済みである。つまりpass3に
関しては、ライブジオメトリチェック（`channel_clear`のM2衝突
検出）だけで既に完全——`static_stub_blocked`が本来埋めるべき
「まだ描画されていない」というギャップがpass3には存在しない。
それにも関わらず`static_stub_blocked`は`_024_`の**ジョグ前の
古い座標**（1185.3、既に1190.7へ移動済みで実体が存在しない）
を使って判定するため、実体のない場所との衝突を検出したことに
なり、`bit_cnt[0]`に不要なジョグを強制していた。

### 52.4 修正（v33）

`pass3_active`という単一要素リストのフラグ（`cur_net`と同じ
パターン）を新設し、pass3のFORCE_JOG_NETSループの先頭で`True`に
設定。`static_stub_blocked`は、このフラグが立っている間は
即座に`False`（ブロックしない）を返し、ライブの`channel_clear`
M2衝突検出のみを判定根拠とするよう変更。pass0〜pass2では
このフラグはFalseのままなので、`static_stub_blocked`の元々の
挙動（順序依存の穴を埋める）はそれらのパスでは維持される。

### 52.5 結果

`bit_cnt[0]`の不要なジョグが消滅（`FORCE_JOG_NETS: 15 net(s),
0 pin(s) jogged`）。短絡は引き続き0件、DRCも0/0/0を維持。
メタル部品数はわずかに減少（M1: 1140→1138、V1: 1202→1198、
M2: 609→607——不要だったジョグ用M1走行・via_1 2個分が消えた分）。
`gen_err_report_nrow_fm.py`で再生成した(253,4)レイヤーは
これでFORCE_JOG_NETSのジョグ0件（空）となり、`_133_[0]`
（51節で解消した短絡当事者、こちらは正当なジョグ）も含めて
FORCE_JOG_NETS全15ネットが一切ジョグなしで直接配線されている
ことを確認した。

### 52.6 変更したファイル

- `script/route_channels_nrow_fm.py`: `pass3_active`フラグを
  新設し、`static_stub_blocked`をpass3実行中はスキップするよう
  変更。デバッグ出力は調査後に削除済み（残留なしを確認）。
- `Layout/i2c_slave_async_nrow_fm_v7v2_*` /
  `i2c_slave_async_nrow_fm_v7rr_*`、および対応する
  `script/*_v7v2.json` / `*_v7rr.json`: 本節の最終結果
  （短絡0件、DRC 0/0/0、FORCE_JOG_NETSジョグ0件）で更新。
- `v7rr_with_err.gds` / `v7rr_err_report_details.txt`も
  本節の最終結果で再生成済み。

## 53. v34/v35: `gen_err_report_nrow_fm.py`のポート別名解決と、(253,2)
座標がチャネル内に表示される不具合の修正

### 53.1 `sda_oe_r`が`OPEN:`に誤分類される問題（v34）

52節までで(253,2)の意味（stub＝M2ピンが1個しかないネット。
`PORT:`＝トップレベルポート＝相手はチップ外、`OPEN:`＝それ以外の
疑わしいケース）を説明した際、ユーザーから「`sda_oe_r`はなぜ
`OPEN:`なのか」の指摘を受け調査。ネットリストを確認したところ
`assign sda_oe = sda_oe_r;`により実際はトップレベル出力ポート
`sda_oe`へ直結しており、他の9件の`PORT:`ネットと構造的に同一
（もう一方の接続先はチップ外パッド）と判明。`top_level_ports()`が
ポート名の文字列（`sda_oe`）としか照合しておらず、レイアウト上の
内部ワイヤ名（`sda_oe_r`）とは一致しないためだった。

**修正（v34）**: `top_level_ports()`に、単純な
`assign <port> = <wire>;`形式のエイリアス解決を追加（スカラー・
ベクター両対応、連結・ビットスライスは対象外）。`sda_oe_r`は
自動的に`ports`集合へ追加され、`PORT:`表示になった。

### 53.2 (253,2)マーカーがチャネル内に表示される問題（v35、より
重大なバグ）

修正後の(253,2)をKLayoutで確認したユーザーから「253/2の表示が
チャネル領域にある。何か間違いがあるのでは」との指摘。tx_data[0]
のマーカーがY=444.4に表示されており、これはchannel1の実範囲
[196.4,657.4]の内部——スタンダードセルが並ぶ行(row)ではなく
配線チャネルの中——であることを確認し、真因を調査した。

**根本原因**: `gen_err_report_nrow_fm.py`の`rebuild_net_pins()`
関数内に、`CH_HEIGHTS = [90.0, 260.0, 240.0, 224.0, 100.0]`という
**関数ローカル変数**でハードコードされた古いチャネル高さの値が
残っていた。これは本セッション序盤の`TRACK_PITCH`
4.0→5.4um変更（47/48節）に伴うCH_HEIGHTS再スナップより**前**の
値。一方`main()`は`ch_heights`引数を受け取り`global CH_HEIGHTS`
経由でモジュールグローバル変数へ代入していたが、
`rebuild_net_pins()`内の同名ローカル変数がこれを完全に
シャドーイングしており、`main()`にどんな`ch_heights`を渡しても
一切反映されない状態だった。結果として、stubネット（`pin_map`に
一度も登場しないため、このルートでしか座標を取得できない）の
行オフセット計算が常に**古いCH_HEIGHTS**（row1が[414.8,479.6]相当）
で行われ、実際のルーティング結果（row1=[657.4,722.2]）とズレた
座標がマーカーとして描画されていた。

なお(253,1)（短絡関与ネットのピンハイライト）は`pin_map`
（ルーター自身が出力した正しい最終座標）を使っており、この
バグの影響を受けていないことも確認した。影響範囲は(253,2)の
stubピンマーカーのみ。

**修正（v35）**: `rebuild_net_pins()`に`ch_heights`引数を追加し
（ローカル変数によるシャドーイングを解消）、`main()`は
モジュールグローバル経由ではなく`ch_heights`をそのまま
`rebuild_net_pins(placement_json, ch_heights=ch_heights)`へ
渡すよう変更。

### 53.3 結果

再生成後、`tx_data[0]`は(1190.70, **687.00**)（row1=[657.4,722.2]
の内側）、`_115_`等は(…, **1169.60**)（row2=[1140.0,1204.8]の
内側）と、全stubピンマーカーが正しく実際の行の内部に収まる
ことを確認した。短絡・DRC判定ロジック自体（Union-Find短絡検出、
DRCチェック）はこのバグの影響を受けておらず、今回の修正は
表示専用（可視化ツール`gen_err_report_nrow_fm.py`）の座標計算
バグの修正であることを付記する。

### 53.4 変更したファイル

- `script/gen_err_report_nrow_fm.py`: `top_level_ports()`に
  `assign`エイリアス解決を追加（v34）。`rebuild_net_pins()`の
  `ch_heights`をローカルの隠れたハードコード値からパラメータ化
  （v35）、`main()`はモジュールグローバル経由をやめて直接
  引き渡すよう変更。
- `v7rr_with_err.gds` / `v7rr_err_report_details.txt`を
  本節の修正後の結果（`sda_oe_r`が`PORT:`、全stubマーカーが
  正しい行内座標）で再生成済み。

## 54. v36: `PER_ROW_GUARD_TRACKS`（Step2のM1トラック一つ飛ばし）を
TRACK_PITCH=5.4um化に合わせて撤廃

### 54.1 ユーザー質問

Step2（pass0、per-row-local系7ネットのトランク＋スパイン配線）の
GDSを見たユーザーから、「M1トラックを一つ飛ばしにする理由は？
Yグリッドも5.4umに変更したので、一つ飛ばしにしなくても良いはず」
との指摘・調査依頼があった。

### 54.2 調査

`per_row_track_of`の割当ループ（`route_channels_nrow_fm.py`
577行目付近）を確認したところ、各per-row-localネットの専用
トラックの直後に`PER_ROW_GUARD_TRACKS=1`本のガード（空き）
トラックを挟んでいることを確認。この定数は元々
（design_notes 38.8、`HIGH_FO_GUARD_TRACKS`と共通の設計）
`TRACK_PITCH=4.0um`時代に導入されたもので、根拠は次の通り:

隣接トラックを無ガードで使うと、Y方向の分離幅はトラックピッチ
そのもの（当時4.0um）。ワイドトランクの半幅（0.9um＝
`M1_TRUNK_WIDTH/2`）＋隣接トラックのvia_1パッド半幅（1.7um＝
`M1_PAD_SIZE/2`）＝2.6umが両者の不可避占有分となり、
4.0-2.6=1.4umしか余白が残らない——ちょうどM1最小間隔DRC規則
（1.4um）ぎりぎりで、信頼して良い余裕がなかった。そこでガード
トラックを1本挟んでY分離を実質2倍（8.0um）にし、
8.0-2.6=5.4umの余裕を確保していた。

しかし本セッション序盤（47/48節、via_1パッド同士のX方向近接性
問題を理由に）`TRACK_PITCH`は4.0→5.4umへ既に変更済み。この
変更後の数値で同じ計算をやり直すと、無ガードでも
5.4-2.6=2.8umの余裕が残り、M1最小間隔1.4umの**2倍**の
マージンがある——実は、この設計の他の箇所（通常ネット同士の
隣接トラック、via_1パッド同士）が無ガードで許容している余裕
（5.4-1.7-1.7=2.0um、47/48節でDRCクリーンと確認済みの基準値）
より**さらに大きい**。つまりガードトラックは、TRACK_PITCH変更
後は最初から不要な過剰マージンになっていた。

### 54.3 検証

`PER_ROW_GUARD_TRACKS`を1→0に変更し、フルスクラッチで
再配置・再配線・ripup/reroute・DRC・接続性検証を実行:

- 短絡0件、DRC 0/0/0を維持（変化なし）
- per-row-localの予約トラックインデックスが一つ飛ばし
  （例: channel0で`[12,14,16,18,20]`）から連続
  （`[11,12,13,14,15]`）に変化
- channel0の使用トラック数が22→16に減少（空いた分は
  実際に再利用可能な余剰容量として回収された）

### 54.4 結論・変更したファイル

`PER_ROW_GUARD_TRACKS`を`1`→`0`に変更（`HIGH_FO_GUARD_TRACKS`は
同種の仕組みだが今回の質問の対象外のため未変更のまま——ユーザー
判断があれば同様に見直し可能）。

- `script/route_channels_nrow_fm.py`: `PER_ROW_GUARD_TRACKS = 0`
  に変更、根拠をコメントに記録。
- `Layout/steps_v7_v2/v7v2_step_*.gds`（5ステップ）を
  本節の結果で再生成。
- `Layout/i2c_slave_async_nrow_fm_v7v2_*` /
  `i2c_slave_async_nrow_fm_v7rr_*`、対応する`script/*_v7v2.json` /
  `*_v7rr.json`、`v7rr_with_err.gds` /
  `v7rr_err_report_details.txt`を本節の結果で再生成・再配布済み
  （短絡0件、DRC 0/0/0を維持）。

## 55. v37: STEP1に優先M2コリドー（"スパイン専用トラック"）の
ハイライトを追加、実際にはほぼ使われていない理由を調査

### 55.1 ユーザー依頼

STEP1（TAPパワーメッシュのみ、信号配線前）のGDSに、STEP2以降の
(258,N)（残余トラック容量ハイライト）と同じように、
`priority_corridor_x`（8箇所、FILLPRI_*セルにより全行で構造的に
常時クリアなM2コラム——"M2のスパイン専用トラック"）を別レイヤーで
ハイライトしてほしいとの依頼。また、なぜ実際の配線がこの専用
トラックをほとんど使わないのか調査してほしいとの依頼。

### 55.2 実装（v37）

`PRIORITY_CORRIDOR_LAYER = (259, 0)`を新設。`checkpoint()`内で
`priority_corridor_x`の各X位置に、コア全高（Y=0〜`core_h`）に
及ぶ縦帯（半幅=`X_GRID/2`、同じFILLPRI_*由来の隣接2トラックが
1本の帯にマージされる）を、(258,N)と同じ`layout.dup()`（実際の
出力には一切影響しない、レビュー専用コピー）に描画するよう追加。
(258,N)と異なりステップ番号に依存しない固定レイヤーなので、
STEP1を含む全ステップのGDSに同じ8本の帯が表示され、
KLayoutでSTEP間の比較がしやすい。

### 55.3 「なぜ使われないか」の調査

`net_shapes_nrow_fm_v7v2.json`（165ネット、984個のM1/M2ボックス）
を全数走査し、中心Xが8箇所のコリドーいずれかと一致するM2ボックスを
検索したところ、該当は**6個のボックス、2ネット分**
（`sda_in_buf`と`_074_`）のみだった。`per_row_spine_events_nrow_fm.json`
（pass0の70イベント）で見ると、実際に「探索の結果コリドーへ着地
した」のは`sda_in_buf`の行クロス1件のみ（`RSTB1`はコリドーと
たまたま自然X（トランクの中心）が一致していただけで、意図的な
着地ではない）。`force_jog_events_nrow_fm_v7v2.json`（pass3
FORCE_JOG_NETS）は現状ジョグ0件のため該当ゼロ。

**根本原因**: `find_row_clear_x`/`find_channel_clear_x`はどちらも
`if ok(start_x): return start_x`——**そのネット自身の自然な（現在の）
X**がすでにクリアなら、コリドーを含む候補リストを一切見ずに
即座にそのXを採用する構造になっている。コリドーが候補として
検討されるのは、自然なXが何かと衝突していて**ジョグが必要な場合
のみ**。本セッションの一連の修正（v26〜v35）で、行/チャネルクロス
の大部分が衝突なく自然なXのまま通せるようになった結果——現状
FORCE_JOG_NETS全15ネットがジョグ0件、pass2スパニングネットの
ジョグも1件のみ——コリドーが「フォールバック候補として検討される
機会」自体がほとんど発生しなくなっている。つまりコリドーの仕組み
自体は正しく機能しているが、**それを必要とする衝突がこの設計では
稀にしか起きない**ため、結果的にほとんど使われていない。

### 55.4 変更したファイル

- `script/route_channels_nrow_fm.py`: `PRIORITY_CORRIDOR_LAYER =
  (259, 0)`を新設、`checkpoint()`に優先M2コリドーの縦帯ハイライト
  描画を追加（レビュー専用、実際の`out_gds`には影響なし）。
- `Layout/steps_v7_v2/v7v2_step_*.gds`（5ステップ）を本節の結果
  （STEP1から(259,0)レイヤーが見える状態）で再生成。

## 56. STEP6: トップピンをBBOX端まで延伸（v38〜v40）

### 56.1 ユーザー依頼

STEP6として、全トップレベルポートを設計全体のBBOXまで延伸する。
ルールは次の通り：
- ROW0とROW3の両方（または同じ行内の重複）にピンを持つポートは
  ROW0を優先して1点に絞り、M2で延伸。
- ROW1/ROW2のみにピンを持つポート（ROW0/ROW3に一切現れないもの）は
  チャネル（channel2、row1とrow2の間）を経由してM1でチャネル左右端
  まで延伸。
- BBOX端でM2PIN/M1PINマーカーを配置。

旧v6時代のスクリプト`script/route_top_pins_nrow_fm.py`（および
ポート↔ネット別名解決を行う`highlight_top_pins_nrow_fm.py`）を、
現行v7レシピ（`TRACK_PITCH=5.4`、`i2c_slave_async_nrow_fm_v7rr_routed.gds`
入力）向けに書き直して対応。

### 56.2 v38: ドッグレッグ（水平迂回）自体の未チェックによる14ネット短絡

行0/3のM2縦探索は、`x_start`でクリアな列が見つからない場合に
±1umステップで±200umまでXをずらして再探索する
（`find_clear_x_vertical`）。ずらした先のXが見つかった場合、
`x_start`からそのXまでを結ぶ横長の「ドッグレッグ」矩形を描画するが、
**そのドッグレッグ矩形自体は一切衝突チェックされていなかった**。

GDS差分（変更前後のマージ済みリージョン集合比較）で検証したところ、
`addr_ok`のドッグレッグ（自然X=450.9→採用されたクリアX=477.9）が
row0内部の他の複数ネットのピンスタブ間の狭い隙間
（M2_MIN_GAP=2.0umぎりぎり）を横断的にまたぎ、14ネットが1つの
短絡ブロブに繋がっていたことが判明。

**修正**: `find_clear_x_vertical`に`dogleg_y`パラメータを追加。
候補Xが`x_start`と異なる場合、ドッグレッグ帯（ピン自身のY±half）の
横方向スパンも`m2_clear`でチェックしてからでないと候補を採用しない
ようにした。

### 56.3 v39: 探索区間の分割（leg1固定＋leg2ライブチェック）

v38修正後、行0/3のポートの多くで±200um以内にクリアな列が
一切見つからなくなった。原因は、探索の開始区間がrow0/row3という
標準セルが密集した内部（他ネットのスタブがM2_MIN_GAP=2.0um間隔で
びっしり並ぶ領域）から始まっているため、ドッグレッグの横断チェックが
追加された結果、探索窓全体で「横断的にクリアな帯」自体がほぼ
存在しなくなったこと。

**修正**: `route_channels_nrow_fm.py`のPER_ROW_LOCAL_NETSがすでに
採用している構造（固定X・無チェックの短い脱出区間→チャネル内の
ライブチェック区間）を踏襲し、2区間に分割：
1. leg1：ピン→行境界（row0_bound/row3_bound）、固定X、無チェック
   （他の全ネットのrow内脱出区間と同じ前提に依拠）。
2. leg2：行境界→BBOX端、`channel0`/`channel4`（上下マージン）内に
   完全に収まる区間のみ、ドッグレッグ込みでライブチェック。

探索対象をrow内部の密集領域からマージン領域に限定したことで、
探索が現実的に成功するようになった。

### 56.4 v40: 自己衝突誤検知（root cause）と修正

v39適用後もテスト実行で24ポート全てが"CHECK"（未検証扱い）となる
現象が発生。直接GDSジオメトリクエリで調査したところ、
**既に配線済みのネット自身の既存M2スタブ**（例：`rw_bit`の
(33.4,12.8)-(36.8,159.6)）が、そのネット自身の探索でクリアXを
求める際に「自分自身」を衝突として検出していたことが根本原因と
判明（本スクリプトの衝突チェックはネット非依存で、GDS上の
全M2/M1形状を無条件にヒット扱いするため）。

試みた解決策：
- **(a) 却下**：既存の配線済み位置（`pin_map`のトラックY）から
  探索を開始する案。`rst_n`/`busy`/`rx_data_r[0]`など、既存の
  トランクがBBOX方向と逆側（例：channel4方向ではなくchannel3方向）
  に伸びているネットがあり、方向不一致で安全に使えないことが判明。
  実装後に問題判明、`resolve_start`/`with_resolved_start`/
  `port_to_net`関連のコードを全て削除して撤回。
- **(b) 採用**：`net_shapes_nrow_fm_v7rr.json`（ルータが配線した
  各ネットの矩形ログ）を読み込み、ネットごとに「自分自身の既存
  形状」を衝突判定から除外する仕組みを追加
  （`route_channels_nrow_fm.py`の`channel_clear`が採用済みの
  自己除外パターンと同型）。`own_region(net, layer)`ヘルパーと、
  `m1_clear`/`m2_clear`/`_collision_count`への`exclude_net`
  パラメータを追加し、ポートからネット名を引く
  `port_to_net_name(port)`を新設して各探索呼び出しに配線。

### 56.5 v40b: ネット自身の"未ログ形状"（標準セルの物理ピン）の見落とし

v40適用後も直接ジオメトリクエリで検証すると、`rw_bit`の探索が
まだ自分自身に阻まれていた。原因は、`net_shapes_log`が記録して
いるのは**ルータが自分で描いた配線スタブのみ**で、配線の起点となる
標準セル自体の物理ピン形状（例：(33.4,11.1)-(36.8,12.8)、ルータの
スタブ(33.4,12.8)-(36.8,159.6)の直下にちょうど隙間なく接している）
はログに含まれていないこと。単純な`Region`減算では、この
ログ外・だが実際には同一ネットの一部である形状が衝突として
残ってしまっていた。

**修正**: `_exclude_own(hits, own)`ヘルパーを新設。`own`
（ログ済みの自己形状）を、探索窓内の`hits`のうち`own`に接触/重複
する形状に向けて微小量（0.01um、M1/M2_MIN_GAPより十分小さい）
だけ膨張させながら数ラウンド繰り返しマージ（フラッドフィル）し、
「探索窓内で物理的につながっている自己ブロブ全体」を求めてから
`hits`から減算するようにした。

### 56.6 結果

修正後、24ポート中14ポートがクリア経路"OK"、残り10ポートは
探索窓内に完全クリアな経路が見つからず"CHECK"（フォールバックとして
自然X/最小衝突トラックのまま描画）フラグとなったが、
**DRC 0/0/0、`verify_connectivity_nrow_fm.py`で0短絡を確認**——
"CHECK"はレビュー推奨の意味であり、実際の違反ではないことを確認済み。

出力：`Layout/steps_v7_v2/v7v2_step_6_top_pins_routed.gds`
（EXTEND_UM=0.0のためM1PIN/M2PINはBBOX端ちょうどに配置）。

### 56.7 変更したファイル

- `script/route_top_pins_nrow_fm.py`: v7レシピ向けにパス/定数を
  更新（TRACK_PITCH=5.4、EXTEND_UM=0.0、IN_GDS/OUT_GDS/CH_HEIGHTS
  をv7rrに変更）。ROW0/ROW3優先・重複排除ロジック（`gather_pins`）、
  `dogleg_y`パラメータ付き`find_clear_x_vertical`（v38）、
  leg1/leg2分割の行0/3ループ（v39）、`own_region`/`exclude_net`/
  `port_to_net_name`によるネット自己衝突除外（v40）、
  `_exclude_own`によるフラッドフィル式自己ブロブ除外（v40b）を追加。
- `Layout/steps_v7_v2/v7v2_step_6_top_pins_routed.gds`：新規生成
  （DRC 0/0/0、短絡0件確認済み）。

## 57. STEP7 v1（誤り、58節で訂正・置き換え）: 完全未使用M1トラック分の
チャネル圧縮によるBBOX縮小

**本節の結果は誤りであることが判明した（58節参照）。ユーザーが
KLayoutでこの節の出力GDSを目視確認し、channel1/2/3の内部に大きな
未使用M1領域が明らかに残っていることを指摘、調査の結果、本節の
「channel1〜3は完全未使用トラック0本」という判定自体が誤った指標
（`next_free_idx`）に基づく誤診断だったと判明した。以下は誤りの
記録として残し、58節が正式なSTEP7の結果である。**

### 57.1 ユーザー依頼

STEP7として、完全に未利用のM1トラック分のスペースをY方向に圧縮し、
設計全体のBBOX面積を縮小する。

### 57.2 方針

`compress_channels_nrow_fm.py`（41節、v6時代に確立済みの手法）と
同じパターンを、現行v7レシピ（`run_route_v7_from_scratch.py`の
`PLACEMENT_JSON`/`FORCE_JOG_NETS`/`PER_ROW_LOCAL_NETS`——本セッションの
v17〜v40の全修正はレシピのパラメータではなく`route_channels_nrow_fm.py`
本体に直接組み込まれているため、同じレシピを今再実行するだけで
現行の検証済み0短絡挙動がそのまま再現される。事前に
`run_route_v7_from_scratch.py`を単体で再実行し、DRC 0/0/0・短絡0件を
再現することを確認済み）に適用する新規スクリプト
`script/run_route_v7_step7_compress.py`を作成。

1. 元のCH_HEIGHTSでの実測トラック使用数（`channel_usage`ログ）を取得。
2. `new_height = min(orig, (used-1)*TRACK_PITCH + 2*TRACK0_OFFSET +
   MARGIN_TRACKS*TRACK_PITCH)`（MARGIN_TRACKS=1、41節と同じ安全マージン）
   で圧縮後の目標高さを計算し、`snap_channel_height`
   （TRACK0_OFFSET+K*TRACK_PITCHグリッドへの切り上げ、v20）で
   スナップ。
3. 圧縮後のCH_HEIGHTSで配置→配線→`ripup_reroute_shorts.py`を再実行、
   DRC・接続性を検証。
4. `route_top_pins_nrow_fm.py`（STEP6ロジック）を新しい（縮小した）
   BBOX/CH_HEIGHTSに対して再実行——STEP6は`core_h`とchannel0/
   channel4のマージン形状に依存するため、圧縮の影響を直接受ける。
   DRC・接続性を再度検証。

### 57.3 実測結果：圧縮できるのはchannel0とchannel4のみ

| channel | used/budget tracks | 元の高さ | 圧縮後 |
|---|---|---|---|
| 0（下マージン） | 16/24 | 131.6um | 93.8um |
| 1 | 85/85 | 461.0um | 461.0um（変更なし） |
| 2 | 77/77 | 417.8um | 417.8um（変更なし） |
| 3 | 72/72 | 390.8um | 390.8um（変更なし） |
| 4（上マージン） | 7/28 | 153.2um | 45.2um |

channel1〜3は実測使用トラック数が予算とちょうど一致しており
（完全未使用トラック0本）、圧縮の余地が無い。圧縮の余地があるのは
上下マージン専用のchannel0/channel4のみ（TAP電源メッシュと、
row0/row3直近の一部ネットしか使っていない）。

### 57.4 結果

- コア全体のBBOX高さ：**1813.6um → 1667.8um（145.8um削減、
  全チャネル予算の9.4%）**。
- 圧縮後の配置配線＋ripup/reroute：DRC 0/0/0、
  `verify_connectivity_nrow_fm.py`で短絡0件を確認。
- 新BBOXへのトップピン再配線（STEP6ロジック再適用）：DRC 0/0/0、
  短絡0件を確認。OK/CHECK内訳（14 OK / 10 CHECK）はSTEP6と
  同じ傾向——"CHECK"はレビュー推奨のフラグであり、DRC・接続性
  検証は両方パス済みのため実害なし。

出力：`Layout/steps_v7_v2/v7v2_step_7_compressed_top_pins_routed.gds`
（圧縮後のBBOX・チャネル高さで配置配線＋トップピン配線まで完了した
最終形）。中間成果物は`Layout/i2c_slave_async_nrow_fm_v7comp_*`
（配置/配線/ripup-reroute後）および対応する
`script/{pin_map,net_shapes}_nrow_fm_v7comp*.json`。

STEP1〜6（`Layout/steps_v7_v2/v7v2_step_1..6_*.gds`）は本節の対象
外で、引き続き圧縮前（元のBBOX、core_h=1813.6um）のまま。フルに
統一したステップ0〜7のレビューセットが必要な場合は、
`run_route_v7_step7_compress.py`の圧縮後CH_HEIGHTSを
`route_channels_nrow_fm.py`の`checkpoint_dir`/`checkpoint_prefix`
機構に渡して1〜5を再生成すればよい（未実施）。

### 57.5 変更したファイル

- `script/run_route_v7_step7_compress.py`：新規。圧縮後CH_HEIGHTSの
  算出、配置→配線→ripup/reroute→STEP6トップピン配線→DRC/接続性
  検証までの一連のパイプラインを自動化。
- `Layout/i2c_slave_async_nrow_fm_v7comp_placement.gds` /
  `_routed.gds` / `_rr_routed.gds`、対応する
  `script/*_v7comp*.json`：新規生成。
- `Layout/steps_v7_v2/v7v2_step_7_compressed_top_pins_routed.gds`：
  新規生成（DRC 0/0/0、短絡0件確認済み）——**ただし58節の通り、
  圧縮の判定指標自体が誤っており、実際にはchannel1〜3の大きな
  未使用領域が未圧縮のまま残っている。正式な結果は58節の
  `v7v2_step_7_squeezed_top_pins_routed.gds`。**

## 58. STEP7訂正：`next_free_idx`の過大計上バグを特定、
`squeeze_channels_nrow_fm.py`による幾何学的圧縮に切り替え

### 58.1 ユーザー指摘

57節の結果（`v7v2_step_7_compressed_top_pins_routed.gds`）を
KLayoutで目視確認したユーザーから、「それはおかしいですね。
チャネル1,2,3には大きな未使用配線領域があります。何処かで完全
未使用トラックの情報が消えていないか調査ください」との指摘。

### 58.2 調査：`next_free_idx`（"used_tracks"）は真の使用量ではない

`compress_channels_nrow_fm.py`（41節）由来の`compute_new_heights()`
は、`channel_usage_path`が報告する`used_tracks`
（`route_channels_nrow_fm.py`内部の`next_free_idx[channel]`）を
「そのチャネルの実使用トラック数」として扱っていた。しかし
`next_free_idx`は**単調増加のハイウォーターマーク・カウンタ**に
過ぎない：

```python
def claim_track(channel, cxs, net):
    idx = next_free_idx[channel]
    next_free_idx[channel] += 1
    while collides(channel, idx, cxs):        # 衝突していれば
        idx = next_free_idx[channel]           # 次のインデックスへ
        next_free_idx[channel] += 1            # カウンタは進むが
    channel_used_x[(channel, idx)].extend(...) # 実際に使われるのは
    return idx                                  # 最終的に成功したidxだけ
```

`collides()`で**却下されたインデックス**にも`next_free_idx`は
恒久的に加算されるが、そのインデックスには一切ジオメトリが
描画されない（`channel_used_x`に登録されるのは最終的に成功した
idxのみ）。スパニングネットのジョグが密集するチャネル
（多数のネットが近いX位置で衝突を起こし、却下→次点へ、を
繰り返す）ほど、この「却下されただけで永久に空のまま」の
インデックスが大量に発生し、`next_free_idx`が実使用数を大きく
超えて膨張する。

実際、`route_channels_nrow_fm.py`自身の`checkpoint()`関数
（(258,N)レビューレイヤー用、43.5節）は、**まさにこの同じ問題を
v3で既に修正済み**だった（"v2 marked a track free purely from
channel_used_x BOOKKEEPING... over-counts used"）。57節の
`compress_channels_nrow_fm.py`はこの教訓を継承しておらず、
古い（既知に誤りと分かっている）指標をそのまま再利用していた。

`i2c_slave_async_nrow_fm_v7rr_routed.gds`に対して実ジオメトリを
直接クエリした結果：

| channel | budget | `next_free_idx`（誤指標） | 実ジオメトリ使用数 | 空洞ギャップ |
|---|---|---|---|---|
| 0 | 24 | 16 | 16 | なし |
| 1 | 85 | 85 | 44 | idx 42-81（40トラック連続空） |
| 2 | 77 | 77 | 37 | idx 29-68（40トラック連続空） |
| 3 | 72 | 72 | 30 | idx 29-70（42トラック連続空） |
| 4 | 28 | 7 | 7 | なし |

channel1〜3は、各チャネルの**予算の最上部付近にたった1〜2本だけ
たまたま高インデックスのジョグトラックが着地している**ため
`next_free_idx`が予算いっぱいまで到達していただけで、実際には
広大な連続した空きインデックス帯が中間に残っていた。57節の
`compress_channels_nrow_fm.py`方式（チャネル高さを単純に「最上部の
使用トラックまで」切り詰めるだけ）では、この**チャネル内部の
空洞**を回収できない——最上部の1本の外れ値ジョグに引きずられて
圧縮余地が実質ゼロと誤診断されてしまう。

### 58.3 修正：`squeeze_channels_nrow_fm.py`（44節、v6時代に確立済み）
への切り替え

44節で既に、「配線済みGDSから真に未使用のトラックだけを幾何学的に
除去し、それより上を詰める」post-processツール
`squeeze_channels_nrow_fm.py`が実装済みだったことが判明。
`compute_kept_indices()`が保持すべきインデックス（ガードトラック
`guard_idx`、または実際にvia/トランクが登録されている
`used_x`のキー）を判定し、`build_y_map()`が「保持インデックスは
恒等写像、除去インデックスは幅ゼロに潰す」区分線形なY写像を構築、
GDS上の全形状・全インスタンスのYをこの写像で変換する。除去後に
新たに隣接することになったインデックスペアについては、
`MIN_VIA_X_SEP`を満たすか再検証し、満たさなければスペーサーを
1本残すという安全策も内蔵済み。

必要な`compaction_info`（`guard_idx`/`used_x`）は
`route_channels_nrow_fm.py`の`compaction_info_path`から得られるが、
**この出力は`ripup_reroute_shorts.py`が実行される前のスナップ
ショット**である点に注意が必要——ripup/rerouteが数ネットを移動
させる可能性があるため、`used_x`（どのインデックスが実際に
使われているか）は post-ripup の**実ジオメトリから再構築**する
方式を採用（`guard_idx`はper-row-localトランクの位置なので
ripupの影響を受けず、そのまま再利用可能）。

新規スクリプト`script/run_route_v7_step7_squeeze.py`のパイプライン：
1. 元のCH_HEIGHTSで配置→配線（`compaction_info_path`付き）
2. `ripup_reroute_shorts.py`
3. DRC・接続性の健全性確認（0短絡・DRC 0/0/0を再現）
4. post-ripup GDSの実ジオメトリから`used_x`を再構築
   （チャネルごと・トラックインデックスごとに、そのY帯域全幅を
   走査し実M1が存在するか、存在するならその中心X群を記録）
5. `squeeze_channels_nrow_fm.py`で幾何学的圧縮
6. 圧縮後GDSのDRC・接続性検証
7. 圧縮で使われた`y_map`から新しいCH_HEIGHTS相当値を逆算
8. STEP6ロジック（`route_top_pins_nrow_fm.py`）を新BBOXに再適用
9. 最終GDSのDRC・接続性検証

### 58.4 結果

| channel | budget | keeping | removing | 削減量 |
|---|---|---|---|---|
| 0 | 24 | 16 | 8 | 43.2um |
| 1 | 85 | 45 | 40 | 216.0um |
| 2 | 77 | 37 | 40 | 216.0um |
| 3 | 72 | 30 | 42 | 226.8um |
| 4 | 28 | 7 | 21 | 113.4um |

**コア全体のBBOX高さ：1813.6um → 988.2um（825.4um削減、45.5%）**
——57節（v1、誤り）の145.8um（9.4%）を大幅に更新。

圧縮後のGDS（`i2c_slave_async_nrow_fm_v7sq_squeezed.gds`）・
STEP6トップピン再配線後の最終GDS
（`Layout/steps_v7_v2/v7v2_step_7_squeezed_top_pins_routed.gds`）
いずれもDRC 0/0/0、`verify_connectivity_nrow_fm.py`で短絡0件を
確認済み。圧縮後のGDSに対して同じ実ジオメトリ走査を再度行い、
channel1に1本のスペーサー（`compute_kept_indices`の安全策による
意図的な1トラック分の空き）を除き、大きな空洞が残っていないことも
直接確認済み。

### 58.5 変更したファイル

- `script/run_route_v7_step7_squeeze.py`：新規。上記9ステップの
  パイプラインを自動化。post-ripup実ジオメトリからの`used_x`
  再構築（`rebuild_used_x_from_geometry`）を含む。
- `Layout/i2c_slave_async_nrow_fm_v7sq_placement.gds` /
  `_routed.gds`、`i2c_slave_async_nrow_fm_v7sqrr_routed.gds`、
  `i2c_slave_async_nrow_fm_v7sq_squeezed.gds`、対応する
  `script/*_v7sq*.json` / `compaction_info_nrow_fm_v7sq_{pre,post}.json`：
  新規生成。
- `Layout/steps_v7_v2/v7v2_step_7_squeezed_top_pins_routed.gds`：
  新規生成（DRC 0/0/0、短絡0件確認済み）——**正式なSTEP7成果物**。
- `script/run_route_v7_step7_compress.py`／その出力
  （`v7comp*`、`v7v2_step_7_compressed_top_pins_routed.gds`）は
  誤った指標に基づく57節の成果物として残置（削除はユーザー確認後）。

### 58.6 追加修正（v42）：row3側M2PINがBBOX端から-0.7umずれる不具合

ユーザーが`v7v2_step_7_squeezed_top_pins_routed.gds`をKLayoutで確認し、
左右（M1PIN）はBBOX端に一致しているが、上下（M2PIN）のうち**上側
（row3側）だけ**BBOX端から-0.7umずれていると指摘。直接ジオメトリ
クエリで確認すると、M2PIN上端が985.2-988.2um（真のBBOX上端は
988.2umなので、下端側が988.2-3.0=985.2ではなく987.5-3.0=984.5に
なっていた＝pinそのものが0.7um低い位置にあった）。

**原因**：`compute_new_ch_heights()`が、圧縮後の各チャネル高さを
`squeeze_channels_nrow_fm.py`の`y_map(old_hi) - y_map(old_lo)`で
算出していたが、`y_map`の区分線形写像を構築する`build_y_map()`は、
「あるチャネルの最初の保持トラックのスライス下端
（`track_y - TRACK_PITCH/2`）が、そのチャネル自身の下端
（`band_lo`）より下に食い込む」場合（`TRACK0_OFFSET(2.0) <
TRACK_PITCH/2(2.7)`のとき常に発生）、breakpointsリストに
old_Yが逆行する（非単調な）点を追加してしまう。通常はこの逆行
区間の直前に「1つ前の行の`row_hi`をそのまま引き継ぐ」正しい
breakpointが既に存在するため実害が無いが、**チャネル0の下端
（old_Y=0.0、設計全体の絶対原点）だけは、それより前に何の
breakpointも無い**ため、線形補間ループが誤って壊れた区間に
マッチし、`y_map(0.0)`が正しい0.0ではなく0.7を返していた
（実際の幾何変換では、Y=0.0ちょうどに図形が存在しないため
この不具合は顕在化しなかった——直接ジオメトリクエリで、圧縮後の
row0の実際の開始Yが86.4um（`y_map(131.6)`の値、こちらは正しい）
であることを確認、`new_ch_heights[0]`は86.4-0.0=86.4であるべきが
86.4-0.7=85.7と計算されていた）。

この0.7umの過小評価が、`route_top_pins_nrow_fm.py`内部で
`gather_pins`がCH_HEIGHTSを絶対Y=0起点から単純に積み上げて
`core_h`を再構築する際にそのまま伝播し、`core_h`が真値988.2umより
0.7um小さい987.5umと計算された。row3側の出口ロジックは
`y_end = core_h + EXTEND_UM`（計算値に依存）を使うため、この誤差の
影響を直接受けた。一方row0側は`y_end = -EXTEND_UM`（絶対Y=0の
リテラル定数、`core_h`計算に非依存）だったため、たまたま無傷
だった——ユーザーが「左右とrow0側は正しいのにrow3側だけずれている」
と観察した非対称性の理由はこれで説明できる。

**修正**：`compute_new_ch_heights()`で、チャネル0の下端境界だけは
`y_map(0.0)`を呼ばず、設計の絶対下端であることが自明な`0.0`を
直接使うよう変更（他チャネルの境界は、直前の行の恒等区間により
常に正しく評価されるため変更不要——本節前半で述べた通り、
`y_map`自体のバグはchannel0の下端1点にのみ実害がある）。
修正後、`new_ch_heights[0]`は86.4umとなり、合計core_hは988.2um
（実測BBOXと完全一致）。`route_top_pins_nrow_fm.py`を再実行し、
row3側M2PINの外側端が正しく988.2um（真のBBOX上端）に一致することを
直接ジオメトリクエリで確認。DRC 0/0/0、短絡0件を再確認。

### 58.7 変更したファイル（追加）

- `script/run_route_v7_step7_squeeze.py`：`compute_new_ch_heights()`
  でチャネル0の下端境界評価を`y_map(0.0)`から絶対値`0.0`に変更。
- `Layout/steps_v7_v2/v7v2_step_7_squeezed_top_pins_routed.gds`：
  修正後のCH_HEIGHTSで再生成（DRC 0/0/0、短絡0件、M2PIN両端が
  BBOX端に正しく一致することを確認済み）。
- `Layout/steps_v7_v2/v7v2_step_7_compressed_top_pins_routed.gds`
  （57節、誤指標による誤った成果物）：ユーザー確認の上、削除済み。

### 58.8 今後の課題

- `next_free_idx`の過大計上バグ自体（`claim_track`が却下した
  インデックスを恒久的に消費する設計）はルータ本体の設計であり、
  今回は修正していない——`channel_usage_path`が報告する
  "used_tracks"は今後も参考値程度に留め、真の圧縮判断には
  必ず実ジオメトリ（本節の手法、または(258,N)レビューレイヤー）を
  使うこと。
- STEP1〜6（`Layout/steps_v7_v2/v7v2_step_1..6_*.gds`）は本節の
  対象外で、引き続き圧縮前（元のBBOX、core_h=1813.6um）のまま。

## 59. LVS準備：v7ネットリストからxschemスキーマティックを生成

### 59.1 ユーザー依頼

STEP7完了（DRC 0/0/0確認済み）を受け、LVS検証作業に着手。「NETから
SPICEを作成出来ますか？DFFRやBUFTHはLEF/下にあるsch/symを使って
ください」との依頼の後、方針変更：「NETから schematic を作成
ください。一度やったことがあるのでスクリプトが残っているはず」との
指示。

### 59.2 SPICE直接生成の調査と断念

まずNET(`src/i2c_slave_async_net_v7.v`)から直接SPICEサブサーキットを
生成する方針を検討。`LEF/DFFR.sch`/`LEF/BUFTH.sch`はトランジスタ
レベル（`MP.sym`/`MN.sym`プリミティブ）で描かれており、正しい
SPICEを得るには本来xschem自身のネットリスタが必要。しかしこの
サンドボックスにxschemは無く（`apt-get install xschem`はネットワーク
制限で403、rootも無いためdpkg展開も不可）、`MP.sym`/`MN.sym`の
正確なピン形状も本プロジェクトのマウント済みフォルダには存在しな
かった。

`LEF/DFF.sch`と、既存の正解データ`LEF/DFF.spice`（過去にユーザーの
実機xschemで生成済み）を突き合わせるフォレンジック（24個の
トランジスタの接続関係から座標オフセットを逆算）により、
`MP.sym`/`MN.sym`のピン配置規則（ゲート=(0,0)、ドレイン風=(40,+30)、
ソース風=(40,-30)、バルク=(40,0)、回転則 rot0:(x,y) rot1:(-y,x)
rot2:(-x,-y) rot3:(y,-x)、flip=1は回転前にx反転）を完全に特定・
複数インスタンスで検証まで完了したが、この時点でユーザーから
「NETからスキーマティックを作ってほしい、以前使ったスクリプトが
あるはず」と方針変更の指示があり、このSPICE直接生成の経路は
未使用のまま保留。

### 59.3 既存スクリプトの発見と適用

`script/gen_schematic.py`（ラベルスタブ方式、グリッド配置）と
`script/gen_schematic_routed.py`（レベライズ配置＋直交配線、
より可読性の高い版）の2つが既存。いずれも旧ネットリスト
（`src/i2c_slave_async_net.v`）を対象としていたため、`gen_schematic_
routed.py`をベースに、v7ネットリスト（`src/i2c_slave_async_net_v7.v`、
154インスタンス）向けの新スクリプト`script/gen_schematic_v7.py`を
作成。ユーザー指示通り、`DFFR`と`BUFTH`のみ`LEF/DFFR.sym`・
`LEF/BUFTH.sym`（`LEF_OVERRIDE_TYPES`）を参照し、他の全セルタイプ
（AND2_X1, BUF_X1, DEL1, INV_X1, MUX2, NAND2/3/4, NOR2/3/4,
OR2/3/4）は`STDLIB/TR-1um_5_stdcell`を参照するよう分岐を追加。

### 59.4 v43: 複数出力ピンを持つセルの配線元ピン取り違えバグ

生成直後、独立検証スクリプト`script/verify_schematic_v7.py`
（xschemを使わず、`.sch`のワイヤ・ラベル・コンポーネント配置を
直接再パースし、幾何学的な結合成分（union-find、ワイヤ端点一致＋
T字接続＋`lab_pin.sym`/`lab_wire.sym`のグローバル結線）を構築して
Verilogネットリストの接続関係と突き合わせる、`verify_spice.py`の
スキーマティック版）を新規作成して検証したところ、580件中517件が
不一致という結果になった。

**原因調査の第一段階**：不一致は全て見た目上「配線がどこにも
存在しない」ように見えたが、実際には`gen_schematic_routed.py`
由来のシグナル配線ルーチンは`add_wire()`に`lab=`を一切渡して
おらず（電源/GND専用の`lab_pin.sym`スタブ以外は無ラベル、xschemの
接続はワイヤの`lab=`属性ではなく純粋な幾何学的接触で決まるという、
本プロジェクトで既に確立済みの前提通り）、検証スクリプト側が
「`lab=`が付いたワイヤを探す」という誤ったチェック方法だった
ことが判明。検証スクリプトを「ネットの全(inst,pin)点が同一の
幾何学的連結成分に属するか」というunion-findベースの正しい
チェックに書き直し、再検証したところ32件に減少。

**真の原因（v43）**：残る32件は全て`DFFR`インスタンスの`Q`ピンの
みで発生。`DFFR.sym`のピン順は`VDD,QB,D,Q,RSTB,GND,CK`（`QB`が`Q`
より先）。Yosysは`_262_ DFFR (.Q(bit_cnt[0]), .QB(_133_[0]), ...)`の
ように`Q`と`QB`の**両方**を接続することが多く、配線ルーチンの
「そのインスタンスの`out`方向ピンで何かに接続されている最初の
ピン」という取得ロジック（`outpin[0]`）が、辞書順で先に来る`QB`を
誤って"駆動元ピン"として選択し、`Q`のネット（例：`bit_cnt[0]`）の
配線を**物理的に無関係な`QB`ピンの座標**から引いてしまっていた。

**修正**：シンク側で既に使っていた「そのネットに実際に接続されて
いる特定のピンを探す」ロジック（`canon_label(expr) == net`で一致
するピンを探す）を、駆動元ピンの探索にも適用。`outpin[0]`の
決め打ちを廃止し、対象ネットに実際に接続された`out`方向ピンを
明示的に特定するよう変更。

**結果**：再生成・再検証の結果、604件（インスタンスピン580件＋
トップレベルポート24件）全てが一致、**PERFECT MATCH**を確認。

### 59.5 出力

- `schematic/i2c_slave_async_net_v7_routed.sch`：v7ネットリスト
  154インスタンスの完全な接続を反映したxschemスキーマティック
  （レベライズ配置、直交配線）。DFFR/BUFTHは`LEF/`のsch/symを参照。
  幾何学的接続検証済み（604/604一致、トップレベルポート含む）。

### 59.6 変更・新規作成したファイル

- `script/gen_schematic_v7.py`：新規。`gen_schematic_routed.py`を
  v7ネットリスト向けに移植、DFFR/BUFTHのLEF参照分岐を追加、
  v43の駆動元ピン取り違えバグを修正。
- `script/verify_schematic_v7.py`：新規。xschem非依存の幾何学的
  接続検証ツール（union-find、ワイヤ端点＋T字接続＋lab_pin/lab_wire
  グローバル結線）。
- `schematic/i2c_slave_async_net_v7_routed.sch`：新規生成
  （接続検証PASS）。

### 59.7 今後の課題

- SPICE直接生成（59.2）で確立した`MP.sym`/`MN.sym`のピン配置・
  回転規則は、将来xschemが使える環境が用意できれば、または
  ユーザーが実機で本スキーマティックをネットリストした結果と
  突き合わせれば、そのまま活用できる。
- 本スキーマティックはLVSの「スキーマティック側」ネットリストの
  元データとして使う想定——実際のLVS実行（レイアウト抽出ネットリスト
  との比較）には、ユーザー側でxschemによるSPICE書き出し、または
  KLayoutのLVSエンジンでの`.sch`読み込みが必要（次のステップ）。

## 60. ユーザーによるLVS実施後、LEF再生成（MUX2）

ユーザーが`LEF/TR-1um_STDCELL.gds`の全セルを`LEF/`配下のsch/symと
実機LVSで照合。この過程で`MUX2`のレイアウトが修正された
（GDSファイルサイズ127482→134168バイト）。

`LEF/TR-1um_STDCELL.lef`（前回生成: 8/20）が更新後のGDS（8/24更新）
より古く、GDSとの整合性が問われたため確認。`script/gen_lef.py`で
GDSから改めてLEFを再生成し、既存LEFと比較した結果、**差分は
MUX2セル1つのOBS（M1配線ブロッケージ）形状のみ**——ピン位置・
マクロサイズ・他16セルは完全一致（バイト単位で同一）であることを
確認。ユーザーがBBOX/ピン位置無変化を確認・承認したため、
`gen_lef.py`を再実行し`LEF/TR-1um_STDCELL.lef`を最新のGDSに
同期させた。

### 60.1 変更したファイル

- `LEF/TR-1um_STDCELL.lef`：MUX2のOBS形状を現行GDSに合わせて更新
  （他は変化なし）。

## 61. スキーマティックにFILL2/FILL3フィラーセルを追加

### 61.1 ユーザー指摘

`schematic/i2c_slave_async_net_v7_routed.sch`には、STEP1（配置時点、
`gen_placement_gds_nrow_fm.py`）でLayoutに追加したFILL2/FILL3
フィラーセルが含まれていない。STEP1で配置した個数をスキーマティック
にも配置してほしいとの依頼。

### 61.2 対応

フィラーセルはゲートネットリスト（論理）には一切現れないため
（VDD/GNDのみでロジックを持たない）、`gen_schematic_v7.py`は
これまでネットリスト由来のインスタンスしか配置していなかった。
LVSでのデバイス数一致のため、`LEF/placement_nrow_fm_v7_priomch.json`
（v7配置、STEP1のGDSの元データ）から実際の配置数を直接カウントし
（**FILL2: 23個、FILL3: 91個、計114個**）、同数のインスタンスを
スキーマティックに追加。`FILL2.sym`/`FILL3.sym`は`STDLIB`には無く
`LEF/`にのみ存在するため、DFFR/BUFTHと同じLEFオーバーライド機構で
参照。物理配置とは別に、既存の回路本体と重ならない専用グリッド領域
（トップレベル出力ピン群のさらに右側）に配置し、他の全セルと同じ
`lab_pin.sym`グローバル結線でVDD/GNDに接続。

**確認**：スキーマティック内のFILL2/FILL3インスタンス数（23/91）が
配置JSONと一致すること、全114個のVDD/GNDピンが他の全セルと同じ
VDD/GND結線グループに属すること（VDD/GND用`lab_pin`タイ数が
128個ずつに増加、既存回路本体分＋フィラー分と整合）を確認。
`verify_schematic_v7.py`によるネットリスト側の接続検証も引き続き
604/604一致（フィラーは論理ネットリストに存在しないため対象外、
既存の検証結果に影響なし）。

### 61.3 変更したファイル

- `script/gen_schematic_v7.py`：`LEF/placement_nrow_fm_v7_priomch.json`
  からFILL2/FILL3インスタンス数を読み取り、専用グリッドに配置して
  VDD/GND結線するセクションを追加。
- `schematic/i2c_slave_async_net_v7_routed.sch`：FILL2×23・FILL3×91を
  追加して再生成。

## 62. 実xschem LVSで発覚した誤ショートの修正（v44）

### 62.1 ユーザー指摘

ユーザーが実際にxschemで`i2c_slave_async_net_v7_routed.sch`を開き、
ネットリスト抽出を実行したところ、大量の誤ショートが検出された：

- `GND - VDD`（多数、FILL2/FILL3ブロック内）
- `rst_n - tx_data[0..7]`、`rst_n - rx_data[0..7]`、`rst_n - rx_valid`、
  `rst_n - addr_match`、`rst_n - rw`、`rst_n - busy`

スクリーンショットでは、FILL2/FILL3を縦に積んだ際、上のセルのGNDピンと
下のセルのVDDピンのラベル（`GNPVDD`のように文字が重なって見える）が
完全に重なっていることが視覚的に確認できた。ユーザーからは「無理に
ワイヤーを引かずにネットラベルで繋いでください」との方針指示。

### 62.2 根本原因（2つ）

1. **FILL2/FILL3のGND-VDD直結**：`FILL2.sym`/`FILL3.sym`のピンは
   ローカルy=-60（VDD）とy=+60（GND）にあり、ピン間隔は120。§61で
   設定した行ピッチ`FILL_DY=120`がこれと完全に一致していたため、
   行Nの GND（絶対y = row_y+60）と行N+1の VDD（絶対y =
   (row_y+120)-60 = row_y+60）が**同一座標**になり、直結（ショート）
   していた。

2. **信号配線（driver→sink）でのワイヤー重なり誤結線**：
   `gen_schematic_v7.py`は元々、driver-sink間をL字/コの字の3セグメント
   ワイヤーで実際に「配線」しており、約600本の配線を`lane`変数
   （7種類のオフセットを全ネットで使い回し）で振り分けていた。
   全体で見ると無関係な2ネットの配線セグメントが同一座標で共線・
   重複するケースがあり、xschemはこれを（意図しない）電気的接続と
   みなしてマージしていた。これが`rst_n`と多数の出力ポートとの
   誤ショートの原因と推定される（本セッションの独自検証スクリプト
   `verify_schematic_v7.py`は「同一ネットの分断」しか検出しておらず、
   「異なるネットの誤結合」は検出できていなかった――今回xschemの
   実ネットリスト抽出で初めて顕在化）。

### 62.3 対応

ユーザー指示どおり、配線ワイヤーを完全に廃止し、**全ての信号ピン・
VDD/GNDピンを、ピンの絶対座標に直接`devices/lab_pin.sym`を置いて
ネットラベルで結線**する方式に変更（`gen_schematic_v7.py` v44）：

- セクション7（インスタンスのinout=VDD/GNDピン）：従来はピンから
  15ユニットオフセットした位置へ短いワイヤーを引いてから`lab_pin`を
  置いていたが、オフセットワイヤーを廃止し、ピン座標に直接
  `lab_pin.sym`を配置。
- セクション8（信号ネットのdriver→sink配線）：3セグメントの
  ワイヤールーティングを全廃。各ネットについて、driver側の出力ピン
  座標とsink側の入力ピン座標それぞれに、そのネットの正規化ラベル名
  `lab=`を持つ`lab_pin.sym`を直接配置するのみに変更（トップレベル
  ポートは既存の`ipin.sym`/`opin.sym`自体が`lab=`を持つのでそのまま
  利用、追加のタイは不要）。
- トップレベルポートの`ipin.sym`/`opin.sym`の`lab=`属性を、生の
  ポートキーではなく`canon_label(key)`（他の結線箇所と同じ正規化
  ネット名）に変更し、ラベル文字列の不一致による未結線を防止。
- FILL2/FILL3のグリッド間隔を`FILL_DX=120→150`、
  `FILL_DY=120→200`に拡大（シンボル全高125に対して十分な余裕を
  確保、GND-VDD直結を根本的に解消）。

### 62.4 検証

- `verify_schematic_v7.py`：604/604一致、mismatches 0（変更前と同じ
  結果を維持）。
- 追加で、書き出した`.sch`内の全ネットラベル系シンボル
  （`ipin`/`opin`/`iopin`/`lab_pin`/`lab_wire`）の配置座標を集計し、
  **同一座標に異なる`lab=`が存在しないこと**（＝幾何学的な誤ショートが
  起こり得ないこと）を独立にチェック：811個のタイ点全てで単一ラベルの
  みであることを確認（0件のショート）。
- 生成結果のワイヤー本数は0（`N ...`行なし）――配線はネットラベルの
  みで表現されるようになった。

### 62.5 変更したファイル

- `script/gen_schematic_v7.py`：ワイヤーベースの配線を廃止し、
  全信号・全VDD/GND接続をピン座標直接配置の`lab_pin.sym`ネット
  ラベル方式に統一。FILL2/FILL3のグリッド間隔を拡大。
- `schematic/i2c_slave_async_net_v7_routed.sch`：再生成（ワイヤー0本、
  タイ点811点、誤ショートなしを確認）。

## 63. ユーザーによる実SPICE確認で発覚：合成セルのVDD/GND浮き

### 63.1 経緯

ユーザーが§62修正後のスキーマティックをxschemで実際にSPICE展開し
（`~/.xschem/simulations/i2c_slave_async_net_v7_routed.spice`）、この
セッションでその内容を確認したところ、§62で報告されていたショートは
解消していた一方、Yosys合成由来のセルインスタンス（`_135_`など、
`INV_X1`のVDD/GNDピンに`net1`/`net2`のような自動採番ノード名が
付いている＝**VDD/GNDが浮いている**）と、手動挿入したバッファ/
ディレイセル（`u_del_sda`など、こちらは`VDD`/`GND`という正しい
ノード名が付いている）とで扱いが食い違っていることに気付いた。

### 63.2 根本原因

`src/i2c_slave_async_net_v7.v`を確認すると、Yosysが合成した154個の
標準セルインスタンス（`_135_`等）はVDD/GNDピンを**ポート接続リストに
一切含めていない**（電源はグローバル暗黙接続という前提でネットリスト
化されている）。一方、手動で挿入した`u_del_sda`・`u_inv_scl`・
`u_buf_scl_row0`等（DEL1/INV_X1/BUF_X1/BUFTH、計12個程度）は
`.VDD(VDD)`・`.GND(GND)`を明示的に持つ。

`gen_schematic_v7.py`のinoutピン結線ループは`conns.get(pin)`が`None`
（＝ネットリストに明示接続が無い）の場合に`continue`でスキップして
おり、これがVDD/GNDタイの`lab_pin.sym`を一切配置しない結果を招いて
いた。§62で全ての結線をワイヤーからネットラベル方式へ切り替えた際も
このスキップ挙動自体は温存されており、154個中ほぼ全ての合成セルで
VDD/GNDが浮いたままだった。独自検証スクリプト`verify_schematic_v7.py`
もexpr=Noneの場合は同様にチェック対象から除外していたため、この不備は
これまで検出されずに残っていた。

### 63.3 対応

- `gen_schematic_v7.py`：inoutピンの結線ループで、`conns.get(pin)`が
  `None`の場合はネット名として**ピン名そのもの**（`VDD`/`GND`）を
  デフォルト採用するよう変更（本ライブラリの全セルがVDD/GNDという
  ピン名を共通のグローバルレール名として使っている慣習に基づく）。
  FILL2/FILL3セクションは元々`net = pin`で同じ扱いをしていたため
  今回の不整合の影響を受けていなかった。
- `verify_schematic_v7.py`：`net_points`構築ロジックを同じデフォルト
  （inoutピンでexpr=NoneならピンをネットGND/VDD扱い）に合わせて修正。

### 63.4 検証

- `verify_schematic_v7.py`：チェック対象接続数が604→884に増加
  （浮いていた約280個のVDD/GNDピンが新たにチェック対象へ）、
  mismatches 0、PERFECT MATCH。
- 書き出した`.sch`内の全ネットラベル系タイ点（1091点）で座標重複に
  よる誤ラベル（異なる`lab=`の衝突）が無いことを再確認（0件）。
- VDD用`lab_pin`タイ数・GND用`lab_pin`タイ数がともに268個
  （ロジック154＋フィラー114、全インスタンス分）に一致することを
  確認（修正前は手動挿入セル分＋フィラー分のみだった）。

### 63.5 変更したファイル

- `script/gen_schematic_v7.py`：inoutピンのデフォルトネット名を
  ピン名（VDD/GND）にフォールバックするよう修正。
- `script/verify_schematic_v7.py`：同じデフォルトルールを検証ロジックに
  反映。
- `schematic/i2c_slave_async_net_v7_routed.sch`：再生成（VDD/GNDタイ
  268個ずつ、誤ショート0件を確認）。

### 63.6 ユーザー再展開後の確認結果

ユーザーが改めてxschemから
`~/.xschem/simulations/i2c_slave_async_net_v7_routed.spice`を再展開
（19:38生成）。このセッションで実ファイルを検証：

- 全268インスタンス（ロジック154＋フィラー114）のVDD/GNDピン計536箇所
  全てが文字通り`VDD`/`GND`ノードに接続（`net1`等の自動採番は0件）。
- `i2c_slave_async_net_v7.v`の正規ネットリストと実SPICEの
  (インスタンス,ピン)対応を1件ずつ突き合わせる独立チェック
  （860接続・200ネット）を実施：同一ネットの分断（未接続）0件、
  異なるネットの誤結合（ショート）0件。

以上により、LVS用ネットリストとしての接続性は確立したと判断。

## 64. 最終レイアウトの再確認：LEF変更を反映した再生成

### 64.1 ユーザー指摘

「比較するlayoutの最終形を再度確認してください。LEFが変わっています。」
――§60でユーザーが手動LVSに基づきMUX2のGDS（`LEF/TR-1um_STDCELL.gds`）
を修正し、それに合わせてLEFを再生成した（17:04〜17:09）。一方、最終
レイアウト成果物`Layout/steps_v7_v2/v7v2_step_7_squeezed_top_pins_routed.gds`
はその日の10:05生成――つまりMUX2修正より前――のままだった。

### 64.2 確認

`gen_placement_gds_nrow_fm.py`は`LEF/TR-1um_STDCELL.gds`を
`klayout.db.Layout.read()`でその場読み込みし、セル定義をトップGDSに
コピーして配置する方式（ライブラリ参照ではなく実体コピー）。そのため
生成済みの最終レイアウトGDSは、生成時点のMUX2ジオメトリを凍結して
埋め込んだままになる。

実際に両GDS内のMUX2セルをレイヤ別ポリゴン面積で比較したところ、
M1関連レイヤ（(3,1)/(3,2)/(8,1)/(11,0)/(13,0)）で面積が不一致
（PIN/BBOXに対応するレイヤ(19,0)/(20,0)/(48,1)/(49,1)/(140,0)/(235,0)
は一致）――ユーザー報告どおり、最終レイアウトは旧MUX2（LVS修正前）を
埋め込んだままだったことを確認。

### 64.3 対応

`run_route_v7_from_scratch.py`＋`run_route_v7_step7_squeeze.py`の
パイプラインを、更新済み`LEF/TR-1um_STDCELL.gds`に対して最初から
再実行（配置→配線→リップアップ&リルート→STEP7圧縮→STEP6トップピン
再配線→各段階でDRC/接続性再検証）。

結果：配置・配線判断（MUX2のBBOX/PIN位置は不変のため）は完全に同一
――チャネル使用トラック数・CH_HEIGHTS・コア高さ圧縮結果
（1813.6um→988.2um、-45.5%）まで全て前回と一致。DRC 0/0/0/0/0/0、
全段階（圧縮前v7sqrr／圧縮後squeezed／トップピン再配線後の最終成果物）
で全ネット完全接続・ショート0件を確認（`verify_connectivity_nrow_fm.py`
はscan_y_hi/scan_wをコア実寸に合わせて呼ぶ必要がある点に注意――
デフォルト値(900um/1500um)だと本設計のコアを部分的にしかスキャンせず
大量の偽陽性"PIN NOT FOUND"が出る。パイプライン内部は正しい値で呼んで
いるため無関係）。

再生成後の最終レイアウトGDS内のMUX2を、現行`LEF/TR-1um_STDCELL.gds`の
MUX2と全レイヤでバイト一致（面積比較で完全一致）することを確認。

### 64.4 変更したファイル

- `Layout/steps_v7_v2/v7v2_step_7_squeezed_top_pins_routed.gds`：
  再生成（19:43）。修正後MUX2を反映、DRC 0/ショート0を再確認。
- `Layout/i2c_slave_async_nrow_fm_v7sq_placement.gds`,
  `..._v7sq_routed.gds`, `..._v7sqrr_routed.gds`,
  `..._v7sq_squeezed.gds`：中間生成物も同時に再生成。

## 65. 未参照セルの除去 ＆ スキーマティック/レイアウトのTop Cell名統一

### 65.1 ユーザー指摘・要求

「Top Cellの名前は同じですか？」との質問に対し、3者（レイアウト
`i2c_slave_async_nrow_fm`／スキーマティック・SPICE
`i2c_slave_async_net_v7_routed`／元Verilogモジュール
`i2c_slave_async`）が全て異なることを回答。加えて、レイアウトGDSの
トップレベル（親を持たない）セルとして、設計本体以外に未参照の
`DFF`・`TAP3`が残っていることも報告。

ユーザーからの返答：「すべてクリーンにしてください。また schematic と
Layout の Top Cell 名は同じにしてください。」

### 65.2 未参照セルの除去

`gen_placement_gds_nrow_fm.py`が`LEF/TR-1um_STDCELL.gds`全体を
`klayout.db.Layout.read()`でトップGDSに読み込む方式のため、ライブラリ
内の未使用セル（`DFF`・`TAP3`――現行フローでは使われていない旧世代
セル）もまるごとコピーされ、どこからも参照されないままGDSに残って
いた。

`Layout.prune_cell(cell_index, -1)`（そのセル自身＋他から参照されて
いない子セルのみを削除）で、以下5個のレイアウトGDS全てから
`DFF`・`TAP3`を除去：

- `Layout/steps_v7_v2/v7v2_step_7_squeezed_top_pins_routed.gds`
  （最終成果物）
- `Layout/i2c_slave_async_nrow_fm_v7sq_placement.gds`
- `Layout/i2c_slave_async_nrow_fm_v7sq_routed.gds`
- `Layout/i2c_slave_async_nrow_fm_v7sqrr_routed.gds`
- `Layout/i2c_slave_async_nrow_fm_v7sq_squeezed.gds`

除去後、トップレベル（親なし）セルは全ファイルで設計本体
`i2c_slave_async_nrow_fm`のみになったことを確認。除去後も最終成果物で
DRC 0/0/0/0/0/0、全ネット接続・ショート0件を再確認（ジオメトリ自体は
無変更のため当然だが、念のため再検証）。

### 65.3 Top Cell名の統一

レイアウト側の`i2c_slave_async_nrow_fm`という名前は、配置配線・DRC・
接続性検証など約80本のスクリプト全体に`TOP_CELL_NAME`定数として
埋め込まれており、変更は大規模かつリスクが高い。一方スキーマティック側
の名前はxschemの慣習で「ネットリスト対象の`.sch`ファイル名」がそのまま
サブサーキット名になる仕組みのため、**ファイル名を変更するだけ**で
名前を揃えられる。

対応：
- `gen_schematic_v7.py`の`OUT_FILE`を
  `i2c_slave_async_net_v7_routed.sch`から`i2c_slave_async_nrow_fm.sch`
  へ変更。
- `verify_schematic_v7.py`の`SCH`パスも同様に変更。
- 再生成・再検証：`verify_schematic_v7.py`は884接続・200ネット・
  mismatches 0でPERFECT MATCH（内容は無変更、ファイル名のみ変更のため
  想定通り）。
- `schematic/i2c_slave_async_nrow_fm.sch`として書き出し。旧ファイル名
  `schematic/i2c_slave_async_net_v7_routed.sch`は新ファイルに完全に
  置き換わったため、削除をユーザーに確認依頼中（`README.md`の参照は
  更新済み）。

### 65.4 結果

- レイアウトTop Cell名：`i2c_slave_async_nrow_fm`
- スキーマティック/SPICE Top Cell名（今後の netlist 時）：
  `i2c_slave_async_nrow_fm`（**両者一致**）
- レイアウトGDS内の未参照セル（DFF/TAP3）：全5ファイルから除去済み

### 65.5 変更したファイル

- `script/gen_schematic_v7.py`：`OUT_FILE`を新Top Cell名に変更。
- `script/verify_schematic_v7.py`：`SCH`パスを新Top Cell名に変更。
- `schematic/i2c_slave_async_nrow_fm.sch`：新規生成（旧
  `i2c_slave_async_net_v7_routed.sch`の後継）。
- `Layout/`配下5個のGDS：`DFF`・`TAP3`セルを除去して再書き込み。
- `README.md`：スキーマティックファイル名の参照を更新。

## 66. KLayout LVSでのSPICE読み込みエラー（インスタンス名の予約文字衝突）

### 66.1 ユーザー報告

KLayoutのNetlist Database Browser（LVS）で
`~/.xschem/simulations/i2c_slave_async_nrow_fm.spice`を読み込んだ際、
下記のスクリプトエラー：

```
RuntimeError: Not a known element type: 'U' in
  /Users/okamura/.xschem/simulations/i2c_slave_async_nrow_fm.spice,
  line 148 in Netlist::read
```

### 66.2 原因

SPICEの構文では、インスタンス名の**先頭1文字**でデバイス種別が決まる
（R/L/C/D/Q/M/V/I/E/F/G/H/B/S/W/U/K/T/O等は抵抗・容量・電流制御電流源
等の予約済みプリミティブデバイスコード、サブサーキット呼び出しは`X`が
慣習）。xschem自身の慣習も各`.sym`の`template="name=x1"`が示す通り、
インスタンス名は`x`始まりを前提にしている。

ところが`gen_schematic_v7.py`は、Verilogネットリスト側のインスタンス名
（Yosys合成セルは`_135_`等で無害だが、手動挿入したバッファ/ディレイ
セルは`u_del_sda`等）や配置JSON側のフィラー名（`FILL_0`等）を**そのまま**
スキーマティックのインスタンス名として書き出していた。行148の
`u_del_sda`は先頭が`u`（`U`＝分布定数RC線の予約コード）のため、
KLayoutのSPICEリーダーがサブサーキット呼び出しではなく`U`型プリミティブ
デバイスとして解釈しようとして失敗していた。同様に`FILL_0`等（先頭`F`＝
電流制御電流源）も、`U`の問題を直しただけでは同じ理由で次に失敗する
はずだった。

さらに調査の過程で、フィラーセルの配置JSON側の名前（`FILL_0`,
`FILLPRI_0`等）が**行ごとにリセットされる非グローバル一意な名前**である
ことも判明（複数行で同じ名前が再利用されている）。1つのスキーマ
ティックにフラット化すると重複インスタンス名になり、これもSPICE上
不正（同一サブサーキット内でのインスタンス名重複）。

### 66.3 対応

`gen_schematic_v7.py`：
- 全インスタンス（ロジックセル・手動挿入バッファ・フィラー）の
  `name=`属性を、元の名前が何であれ常に`x`を先頭に付加して出力する
  よう変更（`x_135_`, `xu_del_sda`等）。内部のPython側辞書キー
  （`xy[name]`等）は変更していない――出力ファイルに書く文字列だけを
  安全化。
- フィラーセルは、配置JSON側の名前だけでなくループindex `i`
  （0〜113、グローバル一意）を末尾に付加し
  （`xFILL_0_37`のように）、重複インスタンス名を解消。

### 66.4 検証

- `verify_schematic_v7.py`：884接続・200ネット・mismatches 0で
  変更前と同じPERFECT MATCH（名前を変えただけで接続関係は無変更のため
  想定通り）。
- 書き出した`.sch`内の全268インスタンス名が重複なく一意であることを
  独立に確認（重複0件）。

### 66.5 変更したファイル

- `script/gen_schematic_v7.py`：インスタンス名を`x`プレフィックス＋
  （フィラーのみ）グローバル一意indexで出力するよう修正。
- `schematic/i2c_slave_async_nrow_fm.sch`：再生成。

### 66.6 次のステップ

ユーザー側で改めてxschemからSPICEを再展開し、KLayout LVSでの
読み込みエラーが解消していることの確認が必要。

## 67. KLayout LVSでのFILL3デバイス不一致（Lパラメータの転記ミス）

### 67.1 ユーザー報告

KLayoutのNetlist Database Browser（Cross Reference）で、`FILL3`のみ
不一致（赤丸アイコン）。Devicesを展開すると、レイアウト側は
NMOS[L=8.6, W=1(→13.1系)]・PMOS[L=8.6, W=2(→21.2系)]、リファレンス側は
NMOS[L=3.2, W=13.1]・PMOS[L=3.2, W=21.2]で、L値が食い違っていた。
`FILL2`は同じ木構造で不一致なし。

### 67.2 調査

`LEF/TR-1um_STDCELL.gds`のFILL2/FILL3セル自体は、git HEAD（コミット
済み版）と作業ツリー版でレイヤ別ポリゴン面積が完全一致――§64のMUX2の
ような「ライブラリGDSが後から変わってスキーマティックが古くなった」
パターンではないことをまず確認。

次に、ユーザーが手動LVSの過程で生成した`LEF/FILL3.extracted`
（KLayoutによる実ジオメトリからのデバイス抽出結果、10:40生成）を
直接確認したところ：

```
XM$1 vdd gnd vdd vdd PMOS L=8.6u W=21.2u ...
XM$2 gnd vdd gnd gnd NMOS L=8.6u W=13.1u ...
```

W値（21.2/13.1）は`LEF/FILL3.sch`の記述と一致するが、**L値が
8.6u**（`FILL3.sch`は3.2u）。一方`LEF/FILL2.extracted`はL=3.2uで、
`FILL2.sch`（L=3.2u）と一致。`FILL2.sch`と`FILL3.sch`のMP.sym/MN.sym
行を比較すると、幅・トランジスタ配置・ネット構成が完全に同一で、
Lの値だけが「FILL2の値のまま」――`FILL3.sch`は`FILL2.sch`からコピーして
作成された際に、FILL3の実際のL寸法（8.6u、FILL3はFILL2よりtapの
チャネル長が長い異なる物理セル）へ更新されなかった、単純な転記漏れと
判断。

（同じチェックで`DEL1`と`MUX2`も一見不一致に見えたが、`DEL1`はSPICEの
`m=`（マルチプライヤ）パラメータを考慮していない検査スクリプト側の
誤検出（`m=2`のPMOS/NMOSはextracted側で実効W倍のデバイス1個として
現れるため）、`MUX2.extracted`は§64のMUX2 GDS修正（17:04）より前
（16:58生成）の古いスナップショットで、現在のKLayout LVSでは実際に
一致している（スクリーンショットでもMUX2の行にエラーアイコンなし）。
どちらも実際の問題ではない。）

### 67.3 対応

`LEF/FILL3.sch`のMP.sym（M7）・MN.sym（M2）の`l=3.2u`を`l=8.6u`に修正
（`LEF/FILL3.extracted`の実測値に合わせる）。

### 67.4 次のステップ

`LEF/FILL3.sch`はFILL3.sym経由でxschemの階層ネットリスト時に自動展開
されるため、ユーザー側で改めてSPICEを再展開すればFILL3のL値が
反映される。その上でKLayout LVSを再実行し、FILL3の不一致が解消して
いることの確認が必要。

### 67.5 変更したファイル

- `LEF/FILL3.sch`：PMOS/NMOSの`l=3.2u`を`l=8.6u`に修正。

## 68. トップレベルピンラベルがピンBOXの外にはみ出るバグ（M1PIN/M2PIN）

### 68.1 ユーザー報告

`v7v2_step_7_squeezed_top_pins_routed.gds`に対する全体LVSで、大量の
「Net $NNN is not matching any net from reference netlist」に加え、
`Port mismatch '$509' vs 'RX_DATA[1]'`・`'$527' vs 'RX_DATA[2]'`という
ポート名不一致（レイアウト側が無名の自動採番ネットになっている）を
確認。

### 68.2 原因

`route_top_pins_nrow_fm.py`の`add_m1_pin()`/`add_m2_pin()`（STEP6、
M1PIN/M2PINマーカー生成）が、ラベルテキスト（TXM1/TXM2レイヤ）を
**ピンBOXの外側**（M1は常にBOX上端+1.0um上、M2は`edge=="right"`の
場合BOX右端+1.0um右）に配置していた――ビューア上で見やすくする
ための意図的なオフセットだったと見られるが、GDSテキストによる
ピン認識（KLayoutのLVS抽出）は**テキストがピン形状の内側/接触**で
ある必要がある。

`rx_data[]`群はBBOX左端にM1で5.4umピッチで縦に並んでおり、実際に
座標を検証したところ、`rx_data[1]`のテキスト(1.5, 630.9)は自分のBOX
(1.5付近, [626.9, 629.9])からも隣の`rx_data[2]`のBOXからも外れた
**隙間**に位置していた（`rx_data[2]`も同様）。このためKLayoutの
抽出スクリプトがラベルとピン形状を関連付けられず、無名の自動採番
ネット（$509等）として扱われ、ポート名の突き合わせに失敗していた。

なお、報告にあった大量の「Net $NNN...」「Net rst_n is not
matching...」等、ポート名一致・不一致に関わらない大規模な不一致に
ついては、xschem/KLayout側のLVS参照ネットリストの解決経路
（`run.lvs`は`"simulation/" + source.cell_name + ".spice"`という
相対パスで参照SPICEを読む）に依存するため、このセッションからは
原因を特定できていない。ユーザー側でNetlist Database Browserの
「Schematic」タブが期待通り（devices ~268個、nets ~200個）表示されて
いるか、参照SPICEが確かに最新の`~/.xschem/simulations/
i2c_slave_async_nrow_fm.spice`を指しているかの確認が必要。

### 68.3 対応

`add_m1_pin()`/`add_m2_pin()`のテキスト配置座標を、BOX外側オフセット
ではなく**BOX中心**（`(cx, cy)`）に変更。どの辺・どの方向のピンでも
必ずBOX内側に収まるようにした。

`run_route_v7_step7_squeeze.py`のフルパイプラインを再実行（決定的な
処理のため、チャネル使用量・CH_HEIGHTS・コア高さ988.2umは前回と完全
一致）。

### 68.4 検証

- 全24トップレベル信号ピンのラベル座標が、対応するM1PIN/M2PIN BOX内に
  収まっていることを直接検証（全て`inside: True`)。
- 参考までに、DFFRセル内部の`QS`/`QM`/`CKB`/`CKP`ラベル（ライブラリ
  セル自身の内部ノード表示用、STDCELLライブラリ由来でこのバグとは
  無関係）はBOX外に残っているが、トップレベルの24ポートには一切
  影響しない（DFFRの外部ピンはVDD/QB/D/Q/RSTB/GND/CKのみで、
  QS/QM/CKB/CKPはトップレベルLVSの対象にならない内部ノード）。
- DRC 0/0/0/0/0/0、全ネット接続・ショート0件を最終成果物で再確認。
- 未参照セル（DFF/TAP3）を5個のGDS全てから再度除去。

### 68.5 変更したファイル

- `script/route_top_pins_nrow_fm.py`：M1PIN/M2PINのラベルテキスト座標
  をBOX中心に変更。
- `Layout/steps_v7_v2/v7v2_step_7_squeezed_top_pins_routed.gds`：
  再生成。
- `Layout/i2c_slave_async_nrow_fm_v7sq_placement.gds`,
  `..._v7sq_routed.gds`, `..._v7sqrr_routed.gds`,
  `..._v7sq_squeezed.gds`：中間生成物も同時に再生成。

### 68.6 次のステップ

ユーザー側でKLayout LVSを再実行し、`rx_data[1]`/`rx_data[2]`の
ポート名不一致が解消しているかの確認が必要。また、大量の内部ネット
不一致については、参照SPICEの解決パスが正しいかの確認が別途必要
（68.2参照）。

## 69. `tx_data[0]`↔`_074_`短絡がなぜP&R段階の検証で検出されなかったか

### 69.1 ユーザー報告

KLayout LVSで`tx_data[0]`が参照2接続に対しレイアウト側12接続と報告
（§68以前の一連のLVS結果）。`verify_connectivity_nrow_fm.py`は毎回の
パイプライン実行で「ALL NETS FULLY CONNECTED, NO SHORTS DETECTED」を
報告しており、社内検証を完全にすり抜けていた。その原因を調査。

### 69.2 調査結果：2つの独立した見落とし穴が重なっている

**(A) `pin_map`登録段階での欠落（`route_channels_nrow_fm.py` L.422-485）**

`net_pins`はスタンダードセルの実ピン（M2矩形）のみから構築され、
トップレベルポートのピンは一切含まれない（トップピンの配線は後段の
`route_top_pins_nrow_fm.py`が別途担当）。続く分類ループ：

```python
for net, pads in net_pins.items():
    if len(pads) < 2:
        stub_nets += 1
        continue          # <- ここで完全にスキップ、pin_mapに一切登録されない
```

`tx_data[0]`はVerilog上、内部セル側のピンが`MUX2.A`一箇所のみ
（残るもう一端はトップポート自身）。そのため`net_pins["tx_data[0]"]`の
長さは1で、`len(pads) < 2`に該当し`stub_nets`としてカウントされる
だけで`pin_map`には一切書き込まれない。実際に確認：

```
pin_map（squeezed版, 165ネット中）に "tx_data[0]" キーは存在しない
（"tx_data[7]" 等ファンアウト2以上のビットは正常に存在）
```

この「2ピン未満のネットは`pin_map`に登録されない」仕様自体は
FILL/デバッグ用ではなく元々の設計だが、副作用として**そのネットの
唯一の内部ピンの実配線先が正しいかどうかすら、一度も検証対象に
入らない**。

**(B) 検証の`pin_map`固定タイミング（`run_route_v7_step7_squeeze.py`
L.154-319）**

最終検証（`verify(TOP_PINS_OUT_GDS, SQUEEZED_PIN_MAP, ...)`,
`[8-9/9]`）は、STEP7（`route_top_pins_nrow_fm.py`によるトップピン
配線）の**後**に実行されるにもかかわらず、渡している`pin_map`は
STEP7より前の`SQUEEZED_PIN_MAP`（STEP2の`route_channels_nrow_fm.py`
が書き出したものの派生）のまま。`route_top_pins_nrow_fm.py`は
`pin_map`を読み取り専用で参照するのみで、自身が新たに引いたトップ
ピン用M1/M2ジョグ配線の座標を`pin_map`へ書き戻すことは一切ない。

結果として、STEP7で新規に描画される配線（トップピンからチャネルの
空きトラックへ向かうM2立ち上がり＋M1トランク）は、**その配線が
たとえ別ネットのトラックと物理的に重なっていても、検証スクリプトが
参照する座標リストにそもそも存在しないため検出しようがない**。

**根本原因（共通）**：`verify_connectivity_nrow_fm.py`のアルゴリズムは
`for net, pins in pin_map.items()`という、**与えられたリストにある
ネット・ピンだけ**を辿るチェックであり、GDS上の全配線から独立に
ネットを発見する仕組みではない。`pin_map`に無いもの・`pin_map`が
作られた後に描かれたものは、どれだけ深刻な短絡があっても原理的に
「検出されない」（「検出して問題なし」ではなく「そもそも見ていない」）。

### 69.3 既知の同型バグとの関係

これは本セッション以前に確認済みの、同じ「`pin_map`からのサイレント
消失」バグ系列の3件目にあたる：

- §42.5：DFFRの`RSTB`ピンがピン名不一致（`RB`→`RSTB`改名の追随漏れ）
  により`pin_map`に1件も現れず、リセット網全体が無配線のまま「DRC 0・
  短絡0」と報告されていた。
- （§に不掲載、~L.5737）：2行配置時に`scl_row2`が`force_jog_pads`の
  構築漏れにより`pin_map`から完全消失し、163ネット中2ネットが検証
  対象から抜けていた。
- 本件（`tx_data[0]`）：スタブネット（内部ピン1個のみ）が
  `len(pads)<2`で最初から`pin_map`に入らない、かつトップピン配線
  自体が`pin_map`更新後に描かれる、という2つの経路がどちらも同じ
  結果（検証対象外）に帰着する。

### 69.4 副次確認：他の「LVS不一致」項目は誤報だった

同じ調査の一環で、KLayout LVS Logが報告していた他の不一致
（`busy`⇔`RST_N`、`rst_n`⇔`BUSY`、`addr_match`、`rx_valid`、`rw`）を
実ジオメトリ（M1/M2/V1のvia経由union-find、`verify_connectivity_nrow_fm.py`
と同じアルゴリズム）で直接検証：

- `rw`⇔`rw_bit`、`addr_match`⇔`addr_ok`、`rx_data[N]`⇔`rx_data_r[N]`
  は`highlight_top_pins_nrow_fm.py`の`PORT_NET_ALIAS`/
  `BUS_PORT_NET_ALIAS`による意図的な命名差（ポート名と内部ネット名が
  違うだけで物理的には同一ネット）。KLayoutが「名前が違う」と報告する
  のは想定通りの挙動で、バグではない。
- `busy`と`rst_n`は互いに物理ルートを共有しておらず、実際にはショート
  していない（LVS Log上の見た目の関連付けは誤報）。
- 全24トップレベルポートのうち、`tx_data[0]`↔`_074_`のみが唯一の
  真正の物理ショート。

### 69.5 対応（未実施）

`tx_data[0]`↔`_074_`の物理ショート自体の修正方法（マルチセグメント
迂回配線の実装／チャネル2の予備トラック確保／個別パッチ／保留して
他項目を先に片付ける、等）は未決定。ユーザーへの選択肢提示
（`AskUserQuestion`）は一度差し戻されており、次の指示待ち。

本節で判明した検証パイプライン自体の構造的弱点（(A)スタブネット
非登録、(B)トップピン配線が`pin_map`確定後に描かれる）については、
恒久対策（例：`route_top_pins_nrow_fm.py`がトップピン座標を`pin_map`
へ書き戻す、または最終検証をGDSの生ジオメトリから独立にネットを
発見する方式へ変更する）も未着手。

## 70. `tx_data[0]`↔`_074_`短絡の修正（v49/v50）と、修正過程で発覚した第3の原因

### 70.1 ユーザー指示

「バグを修正して、配置配線をやり直してください」との指示を受け、
§69で特定した`route_top_pins_nrow_fm.py`のrow1出口配線ロジックを
修正。

### 70.2 v49：ジョグ（迂回）配線の実装

row0/row3出口が既に持つ`find_clear_x_vertical`（候補X探索＋dogleg
区間ごと検証）の仕組みを、row1/row2出口にも拡張。直接X（ピン自身の
natural X）で全37トラックが失敗した場合、track候補ごとに±60um以内で
迂回可能なXを探し、見つかればそのXで垂直立ち上げ＋via＋M1トランクを
描く。見つからなければ従来通りの「衝突最小」フォールバックに委ねる
（安全網として維持）。

`route_channels_nrow_fm.py`のpass 0-3や、既存のrow0/row3出口が確立
していた「dogleg区間も含めて経路全体を検証してから描く」という
設計原則をrow1/row2にもそのまま適用した形。row1/row2のロジックを
`route_row1_row2()`という共通関数に統合（左右対称なので重複コードを
解消）。

### 70.3 v49だけでは不十分だった：第3の原因（自己衝突誤検出）の発覚

v49適用後にフルパイプラインを再実行したところ、`tx_data[0]`は
依然として「衝突最小」フォールバックに落ち、ジオメトリで再検証すると
`_074_`との短絡がそのまま残っていた。デバッグprintで調査した結果、
`find_clear_x_vertical`が**全37トラックでジョグ先Xすら見つけられない**
（`jog_x=None`が37件全て）という、想定と全く違う結果に。

原因（§69.2で判明した(A)(B)とは別の、第3の見落とし穴）：`m2_clear()`
の`exclude_net`引数は`own_region()`経由で`net_shapes_log`（配線
スクリプトが自分で引いたワイヤーの記録）からしか自己除外領域を
作らない。しかし`tx_data[0]`のような「スタブネット」は
`net_shapes_log`にも`pin_map`と同様に一切登録されない（§69.2(A)と
同根）。そのため`own`が常に空になり、**ピン自身の物理パッド
（配置由来のMUX2.Aピン形状そのもの）を一切自己除外できない**。

さらに、ここに使われる`SELF_MARGIN=3.0um`は「ピンから3.0um離れた
所から探索を始めれば自分自身にはぶつからない」という前提だったが、
`m2_clear()`自体が`M2_MIN_GAP=2.0um`分だけ探索ボックスをY方向にも
外側へ広げるため、実効マージンは`3.0-2.0=1.0um`まで縮む。これは
ピン自身の半幅`PAD_HALF=1.7um`より小さく、**探索ボックスが常に
ピン自身のパッドに0.7um分だけ再侵入し、どのトラックを試しても
「自分自身とぶつかっている」と誤判定**していた。これが、実際には
Y=514.0um以降にしか存在しない`_074_`の障害物とは無関係に、37
トラック全てが塞がって見えた真因（外部要因ではなく内部バグ）。

直接ジオメトリで検証：ピン自身のパッド（cx±1.7,cy±1.7、余裕0.2um
込み）を明示的に除外領域へ加えると、Y=461.0〜509.6um（idx 0〜9）の
10トラックは即座に`ok1=True`に変わり、Y=515.0um以降（idx≥10）のみ
`_074_`の実障害物によって`ok1=False`のまま——ジョグ無しでも直接
X=1190.7のまま10トラックが有効になることを確認。

### 70.4 v50：ピン自身の物理パッドを明示的に自己除外領域へ追加

`m2_clear()`に`extra_own`引数を追加し、`_exclude_own()`が
`net_shapes_log`由来の`own_region`と合成して使えるようにした。
`find_clear_x_vertical()`にも同引数を追加し内部呼び出しへ伝播。
`route_row1_row2()`側で、各ポートごとに自分の物理パッド近傍を表す
`self_own`リージョン（`(cx±half2±0.2, cy±half2±0.2)`）を1回だけ生成し、
row1/row2のok1判定・ジョグ探索の両方に`extra_own=self_own`として
渡すよう修正。この自己除外は`net_shapes_log`の有無に関係なく常に
効くため、スタブネットに限らずrow1/row2出口の全ポートに一律で恩恵が
ある。

### 70.5 検証

- フルパイプライン（`run_route_v7_step7_squeeze.py`）を再実行。
  DRC（M1/M2幅・間隔／V1間隔／V1-M1・V1-M2内包／V1-GC間隔）全段階で
  0違反。
- `route_top_pins_nrow_fm.py`のレポート：`tx_data[0]`
  （及び`sda_oe`/`tx_data[1]`/`tx_data[7]`も同時に）が`[CHECK]`から
  `[OK]`（直接Xで確実にクリアな経路が見つかった）へ改善。row1/row2の
  24ポート中フォールバック依存はゼロ、`[CHECK]`が残るのは
  row3出口の`scl`/`sda_in`/`tx_data[2..6]`のみ（row0/row3経路は
  今回の自己衝突バグの対象外——探索区間がピン自身の行から離れた
  マージンチャネル内に限定される設計のため）。
- `verify_connectivity_nrow_fm.py`（`pin_map`ベース、165ネット・519
  ピン）：全段階で「ALL NETS FULLY CONNECTED, NO SHORTS DETECTED」。
- §69で確立した、`pin_map`に依存しない直接ジオメトリ検証（M1/M2/V1
  のvia経由union-find）を最終GDSに対して再実行：
  - `pin_map`登録済み165ネットの相互ショート走査：0件。
  - 全24トップレベルポート個別の物理ルートを、登録済み165ネットの
    ルートと突き合わせ：`tx_data[0]`は**もはや`_074_`と共有ルートを
    持たない**（空集合）。他の23ポートも全て期待通り（意図的な
    エイリアス`rw`⇔`rw_bit`／`addr_match`⇔`addr_ok`／
    `rx_data[N]`⇔`rx_data_r[N]`のみ、それ以外は空）。
  - 24ポート同士の総当たり（276ペア）でも共有ルート0件——`[CHECK]`
    表示が残る`scl`/`sda_in`/`tx_data[2..6]`についても、他のどの
    登録ネット・他のどのポートとも物理的に短絡していないことを確認
    （`[CHECK]`は「衝突最小フォールバックを使った」という注意喚起
    であって、実際の短絡を意味しない）。

### 70.6 変更したファイル

- `script/route_top_pins_nrow_fm.py`：
  - row1/row2出口ロジックを`route_row1_row2()`に統合し、直接X失敗時に
    ±60um以内のジョグ（迂回）を試す段階を追加（v49）。
  - `m2_clear()`/`find_clear_x_vertical()`に`extra_own`引数を追加し、
    `net_shapes_log`の有無に関係なくピン自身の物理パッドを常に自己
    除外できるようにした（v50、真因の修正）。
- `Layout/steps_v7_v2/v7v2_step_7_squeezed_top_pins_routed.gds`および
  中間生成物（`i2c_slave_async_nrow_fm_v7sq_placement.gds`,
  `..._v7sq_routed.gds`, `..._v7sqrr_routed.gds`,
  `..._v7sq_squeezed.gds`）：フルパイプライン再実行により再生成。
  コア高さ988.2umは前回から不変（配線内容は決定的に同一、STEP7の
  トップピン配線のみ変更）。

### 70.7 次のステップ

ユーザー指示により、再度KLayout LVSでチェックする前に、Schematicから
出力したSPICEネットリストとVerilogゲートレベルネットリスト
（`i2c_slave_async_net_v7.v`）から独立に生成したSPICEネットリストの
等価性を確認する予定（§71以降）。

## 71. Schematic SPICE と Verilog（net.v）由来ネットリストの等価性確認

### 71.1 ユーザー指示

「もう一度LVSでチェックする前に、spice net の等価性(NET.V vs
Schematic)を確認します」との指示。KLayout LVSに戻る前に、xschemが
Schematicから書き出したSPICE（`~/.xschem/simulations/
i2c_slave_async_nrow_fm.spice`）と、Verilogゲートレベルネットリスト
（`src/i2c_slave_async_net_v7.v`、"NET.V"）そのものを、`gen_schematic_v7.py`
を経由しない**別系統のパーサーで直接照合**することで、スキーマ生成
スクリプト自身のバグでは検出できない食い違いがないかを確認する。

### 71.2 手法

1. Schematic SPICEから、各ライブラリセルの`.subckt CELLTYPE p1 p2 ...`
   宣言を読み取り、ピン順序を取得（このSPICEファイル自身が全ての
   使用セルのサブサーキット定義を埋め込んでいるため、外部ライブラリ
   ファイルを別途参照する必要がない）。
2. Schematic SPICEのトップサブサーキット本体から、268個の
   インスタンス（154ロジック＋114フィラー）をピン順序に基づき
   `{インスタンス名: (セル型, {ピン名: ネット名})}`の形にパース。
3. `i2c_slave_async_net_v7.v`を、`gen_schematic_v7.py`とは別に新規に
   書いた正規表現ベースのパーサーで直接読み、154個のゲートレベル
   インスタンスを同じ形にパース（Yosys合成インスタンスは`.VDD()`/
   `.GND()`を持たないため、§69/70で確立した「省略時はピン名自身を
   ネット名とみなす」規則をここでも適用）。
4. インスタンス名（Verilog側`NAME`⇔Schematic側`xNAME`、v47の
   'x'接頭辞規則）で対応付け、ピン単位で接続ネット名を突き合わせ。

### 71.3 結果（1回目）

154個中154個のインスタンスが1:1対応（欠落・余剰0件、セル型不一致
0件）。フィラー114個を除く全インスタンスがVerilogに存在し、逆も
成立。ピン単位の不一致は38件（154インスタンス中）：

- 24件：`rw`⇔`rw_bit`、`addr_match`⇔`addr_ok`、`rx_data[0..7]`⇔
  `rx_data_r[0..7]`——`highlight_top_pins_nrow_fm.py`の
  `PORT_NET_ALIAS`/`BUS_PORT_NET_ALIAS`と同一の、意図的なポート名
  ⇔内部ネット名エイリアス（`gen_schematic_v7.py`の`canon_label()`が
  トップポート名を優先して正規化するため）。バグではない。
- 1件：`sda_oe`⇔`sda_oe_r`——上記と同型（`PORT_NET_ALIAS`に
  `sda_oe: sda_oe_r`として明記済み）。バグではない。
- **23件：`net1`〜`net23`という自動採番プレースホルダー**——全て
  DFFRインスタンスの`QB`ピン（Yosysが`unused_bits`と注記した、
  `Q`のみ使われ`QB`はどこからも読まれないビット）。Verilogでは
  `.QB(_128_)`のように明示的にネット名が書かれているにもかかわらず、
  Schematic側ではこのピンに一切ネットラベルが貼られておらず、
  xschem自身のネットリスト抽出時に無名の自動採番ノードとして
  扱われていた。

### 71.4 原因（`net1`〜`net23`）

`gen_schematic_v7.py`の第8節（信号ネットのラベル付けループ）：

```python
for net in sorted(all_nets):
    drv = net_driver.get(net)
    sinks = net_sinks.get(net, [])
    if drv is None or not sinks:
        continue
```

`not sinks`（そのネットにシンクが1つも無い）の場合、**ドライバ側の
ピンにすら**ラベルを貼らずスキップしていた。ドライバはあるがシンクが
無いネット（＝Yosysの`unused_bits`が示す、`QB`は繋がっているが
どこにも読まれていない出力ピン）がこれに該当し、そのピンが
完全に無ラベルのまま残っていた。電気的には実害なし（他に何も
繋がっていないネットなので、どちらにせよ短絡や未接続の实害は
無い）が、Verilogが実際に持っているネット名（`_128_`等）が
Schematic側では失われ、xschemの自動採番に置き換わってしまう、
という意味で正真正銘の食い違いだった。

### 71.5 修正

`if drv is None or not sinks: continue`を`if drv is None: continue`
に変更（v51）。シンクが無くてもドライバがあればそのピン自身への
タイは行うようにした（`for sk in sinks:`のシンク側ループは元々
`sinks`が空なら何もしないので変更不要）。

### 71.6 検証

`gen_schematic_v7.py`を再実行し、23件全てのDFFR.QBピンについて、
期待したVerilogネット名（`_128_`〜`_114_`等）のラベルが正しい
座標に配置されていることを直接ジオメトリで確認（23件全てOK）。
`verify_schematic_v7.py`（884接続・200ネット・PERFECT MATCH）も
引き続き成立を確認（この検証スクリプト自体は元々`net_driver`/
`net_sinks`の同じ「シンク無しは検査対象外」規則を使っており、
今回追加された23件のタイを検査対象に含めていないため、退行しても
拾えない可能性がある点は注意——ただし直接ジオメトリ確認で個別に
実証済み）。

修正後のSchematicを`schematic/i2c_slave_async_nrow_fm.sch`に反映。

### 71.7 未完了・ユーザー側の作業が必要な部分

このセッションのサンドボックスにはxschem実行環境が無いため、
修正後の`.sch`から実際に`.spice`を再エクスポートする作業は
**ユーザー側のxschemで再度行う必要がある**。再エクスポート後、
同じVerilog照合をもう一度実行すれば、不一致がPORT_NET_ALIAS系の
25件（`rw`/`addr_match`/`sda_oe`/`rx_data[0..7]`）のみに収束する
はずである。KLayout LVSの再実行は、このSPICE再エクスポート確認の
後に行う予定。

### 71.8 変更したファイル

- `script/gen_schematic_v7.py`：第8節のネットタイ・ループから
  `or not sinks`条件を削除（v51、ドライバのみでシンクが無い
  ネットにも正しいネット名でタイを行うようにした）。
- `schematic/i2c_slave_async_nrow_fm.sch`：再生成・反映。

### 71.9 再エクスポート後の最終確認（ユーザーがxschemで`.spice`を再出力）

ユーザーが修正後の`.sch`から`~/.xschem/simulations/
i2c_slave_async_nrow_fm.spice`を再エクスポート（10:03生成）。同じ
Verilog照合を再実行：

- インスタンス対応：154/154（欠落・余剰0件）。
- `net1`〜`net23`プレースホルダー不一致：**0件**（§71.4/71.5の修正で
  解消を確認）。
- 残る不一致：**25件のみ**、全て既知の意図的エイリアス（`rw`⇔
  `rw_bit`、`addr_match`⇔`addr_ok`、`sda_oe`⇔`sda_oe_r`、
  `rx_data[0..7]`⇔`rx_data_r[0..7]`）。それ以外の不一致は0件。
- FILL3のL値（8.6u）を含むデバイスパラメータも再エクスポート後の
  ファイルで引き続き正しいことを確認。

結論：Schematic SPICEとVerilog（net.v）ゲートレベルネットリストは、
既知の意図的なポート名エイリアスを除いて完全に等価であることを
確認した。KLayout LVSを再実行してよい状態。

## 72. ユーザーがアップロードした`error.lvsdb`の解析

### 72.1 ユーザー指示

「LVS の lvsdb が読めますか？」に続き「調査お願いします」との指示。
`~/.xschem/simulations/i2c_slave_async_nrow_fm.spice`再エクスポート後、
ユーザーが実際にKLayout LVSを実行して得た`error.lvsdb`（KLayoutの
LVS比較データベース）を解析。

### 72.2 読み込み方法

`klayout.db`の`LayoutToNetlist`クラスでは`H(...)`（デバイス抽象化
テーブル）ブロックのパースに失敗する（"Expected ')' here" エラー）。
正しくは`LayoutVsSchematic`クラス（LVS結果のクロスリファレンスを
含む、`LayoutToNetlist`の拡張）で読み込む必要があり、これで正常に
読めた。`.xref()`から`NetlistCrossReference`オブジェクトを取得し、
`each_circuit_pair`/`each_net_pair`/`each_subcircuit_pair`/
`each_device_pair`で詳細を取得できる。

`internal_top_cell().bbox()`のY方向最大値が988.2umと、今回の最終
成果物（`Layout/steps_v7_v2/v7v2_step_7_squeezed_top_pins_routed.gds`、
squeeze後のコア高さ）と完全一致しており、このLVS実行が最新の
修正済みレイアウトに対するものであることを確認。

### 72.3 結果サマリー

- 18種類のライブラリセル回路（AND2_X1〜FILL3）は全て`Match`
  （トランジスタW/L含め完全一致）。
- トップ回路（`i2c_slave_async_nrow_fm`）自体は`NoMatch`：
  - ネットペア：217件中68件`Mismatch`
  - サブサーキット（インスタンス）ペア：283件中55件`Mismatch`
  - ピンペア：26件全て`Match`（トップレベル24ポート＋VDD/GND）

### 72.4 内訳と原因の切り分け

**(a) 11件（`_110_`〜`_131_`付近）**：§71で修正したDFFRの未使用`QB`
出力ネット。Verilog上は名前を持つが、実回路としては1ピンのみ
（ドライバのみ、シンク無し）でレイアウトには何も配線されない
（`05_Compare.lvs`の`netlist.purge_nets`がレイアウト側の浮遊ネットを
削除するため）。**想定通り・実害なし**。

**(b) 残り57件（ネット）／55件（サブサーキット）**：
`_180_`〜`_186_`（MUX2、shreg→rx_data_rのビット多重化チェーン）、
`_254_`〜`_274_`（DFFR、同チェーンの出力レジスタ）、`u_lat_q`/
`u_lat_qn`（NOR2、busy/qnのクロスカップルドSRラッチ）、およびこれら
周辺の組み合わせ論理（AND2_X1/NOR2/NOR3/NOR4/OR3/NAND2/INV_X1、
FSM制御ロジック）に集中。レイアウト側インスタンス名が空文字列
（`''`）または対応無し（`None`）——KLayoutの位相（トポロジー）
ベースのネットリスト比較アルゴリズムが、これらのインスタンスを
スキーマティック側と確信を持って対応付けられなかったことを示す。

**検証**：これら全ての疑わしいインスタンスに関わる登録済みネット
（80件、`pin_map_nrow_fm_v7sq_squeezed.json`ベース）について、
§69で確立した層・via対応済みunion-findアルゴリズムを最終GDS
（`v7v2_step_7_squeezed_top_pins_routed.gds`）に対して直接適用：
**80件全て完全接続、クロスネットショート0件、ピン欠落0件**。
`_012_`〜`_018_`（MUX2→DFFR Dピン間の各ビット配線）を個別に
座標追跡した結果も含め、物理的には全て正しく配線されていることを
直接確認。

### 72.5 結論

`error.lvsdb`が報告する57件のネット・55件のサブサーキット不一致は、
**実際のレイアウト欠陥ではなく、KLayoutネットリスト比較器の
「位相的あいまい性（ambiguous match）」によるもの**と結論。

想定される理由：
- shreg→rx_data_rチェーンは、ほぼ同一構造のMUX2+DFFRペアが7回
  繰り返される、高度に反復的なトポロジー。
- `u_lat_q`/`u_lat_qn`はクロスカップルドNOR2ラッチ（SRラッチ）で、
  一般にネットリスト比較アルゴリズムが自己対称的な構造の解決に
  弱いことが知られている（グラフ同型問題の一種）。
- レイアウト抽出（`01_Extract.lvs`/`02_Extract.lvs`）はテキスト
  ラベルを一切使わない純粋ジオメトリベースの抽出であり、トップ
  レベル24ポート以外の内部ネットには一切名前のヒントが無い
  （純粋にトポロジーだけで対応付けを試みるしかない）ことも、
  あいまい性を助長している可能性が高い。

### 72.6 今後の選択肢（未実施、ユーザー判断待ち）

- このまま許容する：本セッションの直接ジオメトリ検証（union-find、
  DRC 0違反、pin_map全件PASS）は既にKLayout公式LVSより厳密な
  レベルで完了しており、電気的には問題無いと判断できる。
- `05_Compare.lvs`の`#netlist.simplify`コメントアウトを解除して
  試す（対称構造の統合に効く場合がある）。
- 内部ネットにも（トップレベルポートのTXM1/TXM2と同様の）名前付き
  テキストラベルを追加し、KLayoutの抽出器がトポロジーだけでなく
  名前ヒントも使えるようにする（根本対策だが、165ネット全てに
  ラベルを追加する大規模な変更が必要）。

### 72.7 変更したファイル

なし（調査のみ、`uploads/error.lvsdb`はユーザーがアップロードした
参照データ）。

### 72.8 選択肢(2)を試した結果：`netlist.simplify`は効果なし

ユーザーが`05_Compare.lvs`の`#netlist.simplify`コメントアウトを
解除して再実行、結果を`error2.lvsdb`としてアップロード。

`error.lvsdb`と`error2.lvsdb`をバイト比較したところ、レイヤー宣言
（`L(...)`)の内部的な列挙順序が入れ替わっているだけ（意味的な差分
なし）で、それ以外は完全に同一。`LayoutVsSchematic`で再読込して
ネット・サブサーキットの不一致件数を再集計しても、**両ファイルで
完全に同じ結果**（ネットペア149 Match/68 Mismatch、サブサーキット
ペア228 Match/55 Mismatch）。

結論：`netlist.simplify`は今回のあいまい性（反復的なMUX2+DFFR
シフトレジスタチェーン、クロスカップルドSRラッチ）には効果が
無かった。§72.6の残る選択肢は、(1)このまま許容する、または
(3)内部ネットへの名前ラベル追加、の2つ。

## 73. `.lib`の真理値表とLEF/下スキーマティック（トランジスタレベル）の照合検証

### 73.1 ユーザー指示

「.lib の真理値と LEF/下の schematic の動作は検証出来ていますか？」
→ 未検証だったため、実施することに。ユーザー指示：「LEF の下に
利用している全てのセルの schematic があります。確認してください」。

### 73.2 手法

Schematic SPICE（`i2c_slave_async_nrow_fm.spice`）に既に埋め込まれて
いる各ライブラリセルの`.subckt`定義（実トランジスタネットリスト、
PMOS/NMOSの drain/gate/source/bulk）を直接利用。Pythonでスイッチ
レベル論理シミュレータを実装：

- PMOSはgate=0で導通、NMOSはgate=1で導通する「スイッチ」として
  モデル化。
- 各入力の全組み合わせについて、VDD/GNDを固定電位のルートとして
  Union-Findでノード同士の導通関係を解決（内部ノードがゲートに
  現れる場合は多段で反復解決）。
- 出力ノードがVDDのみに接続＝1、GNDのみに接続＝0、両方＝
  コンテンション（X）、どちらにも繋がらない＝フローティング（Z）
  として判定。
- `TR1um_5_stdcell.lib`の`function`文字列（`'`=NOT、`*`=AND、`+`=OR
  の後置/中置記法）を評価する小さなパーサーを実装し、真理値表を
  直接比較。

組み合わせ論理セル（15種：INV_X1, BUF_X1, BUFTH, AND2_X1, OR2/3/4,
NAND2/3/4, NOR2/3/4, MUX2, DEL1）全てで、入力の全組み合わせ（2〜16
通り）を網羅的にチェック。

### 73.3 結果（組み合わせ論理セル）

**15種類全てで、実トランジスタネットワークが導く真理値と`.lib`の
`function`宣言が完全一致**（コンテンション・フローティング発生も
0件）。

```
INV_X1     fn="A'"              -> OK
BUF_X1     fn="A"               -> OK
BUFTH      fn="A"               -> OK
AND2_X1    fn="(A*B)"           -> OK
OR2        fn="(A+B)"           -> OK
OR3        fn="(A+B+C)"         -> OK
OR4        fn="(A+B+C+D)"       -> OK
NAND2      fn="(A*B)'"          -> OK
NAND3      fn="(A*B*C)'"        -> OK
NAND4      fn="(A*B*C*D)'"      -> OK
NOR2       fn="(A+B)'"          -> OK
NOR3       fn="(A+B+C)'"        -> OK
NOR4       fn="(A+B+C+D)'"      -> OK
MUX2       fn="(S*B)+(S'*A)"    -> OK
DEL1       fn="A"               -> OK
```

BUFTH（Y=A宣言）は内部にヒステリシス用のフィードバックMOS
（`GND`/`VDD`ノード自体を drain/source に持つ特殊な構成）を含むが、
理想的な0/1入力でのDC動作点はA=0→Y=0、A=1→Y=1と正しく解決される
ことを確認（アナログ的なしきい値特性はLiberty `function`の対象外）。

### 73.4 結果（DFFR、順序回路）

`.lib`宣言：`ff(IQ,IQN) { clocked_on:"CK"; next_state:"D";
clear:"!RSTB"; }`、`Q.function="IQ"`、`QB.function="IQN"`
（＝CK立ち上がりエッジでD取り込み、RSTB=0で非同期・負論理リセット、
Q=0/QB=1）。

フィードバックループ（マスターラッチ`net2`/`net4`、スレーブラッチ
`QS`/`net5`）を含むため、組み合わせ論理と同じ総当たり評価は
そのまま適用できない。26個のMOSFETの接続を手動でトレースし、
以下を構造的に確認：

- `CKB`=NOT(CK)、`CKP`=NOT(CKB)=CK（バッファ済みCK）の2段インバータ
  を確認。
- **マスターラッチ**：CK=0の間、`net2`はD経由のパストランジスタ
  （MM1/MM2/MM7/MM8）により`net2=D'`に追従（透過）。CK=1の間は
  `net2`⇔`net4`（MM4/MM15、インバータMM10/11/MM3/13で構成される
  クロスカップルド対）がラッチしてCK 0→1エッジ直前の値を保持——
  `QM`（`net2`の反転）＝CK 0→1エッジ直前のDの値。
- **スレーブラッチ**：CK=1の間、`QS`は`QM`に透過的に追従
  （MM5/MM6）。CK=0の間は`QS`⇔`net5`（MM14/MM16、クロスカップルド
  インバータMM9/12）がラッチして保持。
  → 全体として、CK立ち上がりエッジでDの値が`QS`に転送される、
  標準的な正エッジトリガD-FF構造であることを確認（`.lib`の
  `clocked_on:"CK"`と一致）。
- **非同期リセット**：`QB`のプルアップ経路はMM22（`QS=0`で導通）と
  MM26（`RSTB=0`で無条件導通）の2系統、プルダウン経路はMM17
  （`RSTB=1`で導通）→MM25（`QS=1`で導通）の直列2段のみ。`RSTB=0`
  の間はMM17が遮断されるため、プルダウン経路とMM26のプルアップが
  同時に導通することはなく（コンテンション無し）、`QB`は無条件に
  1へ強制される。`RSTB=1`（通常時）は`QB=NOT(QS)`。`Q`はMM18/MM19
  （`QB`の単純なインバータ）で`Q=NOT(QB)`——`RSTB=0`時`Q=0`、通常時
  `Q=QS`。→ `.lib`の`clear:"!RSTB"`（`RSTB=0`でIQ=0/IQN=1）と完全に
  一致。

**結論：DFFRのトランジスタレベル実装は、`.lib`が宣言する
「CK立ち上がりエッジでD取り込み、RSTB非同期負論理リセット」という
動作と構造的に完全に一致**。コンテンション・不定状態も発見されず。

### 73.5 対象外

`FILL2`/`FILL3`は論理を持たないVDD/GNDタイセルのため、`.lib`にも
セル定義自体が存在しない（想定通り、対象外）。`DFF`/`DFFS`/
`BUFTH_X1_AC`は`.lib`・`LEF/`双方にスキーマティックはあるが、本設計
（v7ネットリスト）では未使用のため今回の検証対象外（使用中の16
セル型を優先）。

### 73.6 総括

`.lib`の真理値表（組み合わせ15種）およびDFFRの順序動作は、
`LEF/`下の実際のトランジスタレベルスキーマティックと完全に整合
している。この項目は特性値（タイミング）ではなく機能（論理）の
検証であり、論理合成（Yosys）が`.lib`を根拠に行った最適化判断が、
実回路の動作と矛盾しないことを裏付けるもの。

## 74. レイアウト抽出ネットリストのスイッチレベル論理シミュレーション（進行中）

### 74.1 目的
`layout/steps_v7_v2/i2c_slave_async_nrow_fm.extracted`（KLayout LVS抽出済みネットリスト、
268インスタンス・約1800トランジスタ）を、実際のトランジスタ接続に基づいたスイッチレベル
シミュレータで動作検証し、波形（VCD）として観測できるようにする。

### 74.2 シミュレータのアーキテクチャ（`switch_sim.py`）
各PMOS/NMOSを理想スイッチとしてモデル化（PMOS: gate=0で導通、NMOS: gate=1で導通）。
毎ステップ、VDD（`$49`）とGND（`VSS`）を根とするunion-findで全ノードを解決する
（Jacobi型固定点反復、最大25回）。VDD/GNDどちらにも属さない孤立成分は直前値を保持
（DFFRのクロスカップル・フィードバックが特別扱いなしにメモリとして機能する仕組み）。

**v2修正**: 反復内でゲート値を`self.state`（呼び出し前の古い値）ではなく`prev_state`
（同一`settle()`呼び出し内で逐次更新される値）から読むように修正。これが無いと1回の
`settle()`内で多段論理が伝播しきらない。

### 74.3 発見した2つの構造的な問題（トランジスタレベルの限界）

単純な理想スイッチモデルでは正しく解決できない、意図的に非ideal-switchなセルが2種類
存在することが判明した。**どちらも実際の物理配線の欠陥ではなく、ゼロ遅延・二値スイッチ
モデルの原理的限界**である。

**(a) BUFTH（シュミットトリガ・バッファ、`sda_in`/`scl`の入力バッファ）**:
抽出ネットリストを見ると、PMOSのドレインが直接`gnd`に、NMOSのドレインが直接`vdd`に
接続される正帰還段を持つ（`XM$1 gnd $5 $7 vdd PMOS` / `XM$9 vdd $5 $3 gnd NMOS`）—
典型的なシュミットトリガのヒステリシス構成。これはトランジスタの相対的な駆動力（W/L比）
の競合で動作するアナログ的な回路であり、二値スイッチモデルでは内部ノードが
VDD/GNDを直接ブリッジする状態に解決されてしまう（実際に`X$166`＝`sda_in`用BUFTHの
内部ノードで、VDD-GND直結の疑似ショートを検出・追跡した）。

**(b) DFFR内部のトランスミッションゲート競合**: マスター・スレーブ型のDFFRは、
`CKB`/`CKP`（`CK`の1段・2段遅延バッファ）でトランスミッションゲートを
break-before-makeで開閉するよう設計されている。ゼロ遅延解決ではこの意図的な段差が
消え、CKエッジの瞬間にマスター・スレーブ両ラッチのパスが同時に開く
（make-before-break）ことで、内部ノード（例: `X$111.$9`）を介してVDD-GNDが
瞬間的にブリッジする擬似コンテンションが発生することを確認した
（`X$111`のQB経由でVDD-GND unionが最初に成立する具体的なトランジスタ番号まで特定）。

**対策**: 両セルとも、`.lib`との照合済み（§73）の関数レベルの振る舞いに置き換えた：
- BUFTHはトランジスタを展開せず、`Y=A`のライブな組み合わせ論理として毎反復解決。
- DEL1（意図的遅延線）も同様に`Y=A`だが、1ステップ分のレジスタ遅延を追加
  （`sda_d`が`sda_in`から確実に1ステップ遅れるようにし、START/STOP検出の
  エッジ競合が機能するようにした）。
- DFFRはトランジスタを展開せず、`.lib`の`ff`宣言
  （`clocked_on:"CK"; next_state:"D"; clear:"!RSTB"`）通りの正エッジトリガ型
  レジスタとして振る舞いモデル化。全DFFRのD/RSTB/CKは「旧Q値」を使った組合せ
  ネットワークの固定点解決後にまとめて評価し、まとめて更新することで、
  RTL的なレジスタ転送セマンティクス（同一エッジで動くシフトレジスタ段が
  1段だけ正しく進む）を再現している。

### 74.4 現在の結果
- リセット状態（12/12）、リセット解除、START条件によるbusyラッチのセットが
  正しく動作し、コンテンションはゼロ。
- 誤アドレス（0x51）へのWRITEトランザクションはNACK動作を含めて全チェック合格。
- STOP後のbusyクリアは3トランザクションとも合格。
- VCD波形を`outputs/i2c_layout_sim.vcd`に出力済み（GTKWave等で観測可能）。

### 74.5 未解決の問題
正しいアドレス(0x50)+WRITEトランザクションで、アドレス一致(`addr_match`)が
立たない。原因をネットレベルで追跡した結果、生のシフトレジスタ段の1つ
（`X$105`、QB出力ネット`$644`）が期待値と異なる値を保持しており、
アドレス比較ツリー（`NOR4`×2段のXNOR的比較木）で誤ミスマッチとして検出される
ことを特定した。7～8ビットのアドレス+RWシフト自体はカウンタ（`bit_cnt`相当、
複数DFFRのトグルパターンで確認）は正常に動作しているため、シフトレジスタ
チェーン内の特定1ビット、またはテストベンチのビット順序に起因する可能性が
高い。抽出ネットリストは内部ネットに名前ラベルが一切無い（純粋な幾何学的抽出、
§72で既確認）ため、この先はschematicの実ネット名とのトポロジー照合、または
テストベンチのビット送出順序の再検証が必要。次回はここから再開する。

関連ファイル: `outputs/switch_sim.py`（シミュレータ本体）,
`outputs/i2c_tb.py`（I2Cバス・テストベンチ）,
`outputs/i2c_layout_sim.vcd`（波形出力）。

## 75. トップレベル統合（FRAME/GIO結線）とLVSクリーン化

コアセル（`i2c_slave_async_nrow_fm`）単体のP&R/DRC/LVS基準確立（§1〜74）に続き、
実チップフレーム（`FRAME/TR-1um_frame_25x25.gds`のI/Oセル`OSS_FRAME_GIO`、42本の
信号ピン＋VDD/VSS）とコアを実際に結線し、トップレベル（`tr_1um_i2c_slave_async`）で
LVSクリーンを達成するまでの記録。

### 75.1 方針: 実ボンディングパッドではなくGIOのM1/M2 PINへ接続

当初、VDD/VSSの一部をGIO内の実100×100umワイヤボンドパッド（フレームの外周パッド
フラッシュ）に直接接続する案を検討したが、ユーザー指示により方針転換:
「実際のボンディングパッドには接続しないでください。OSS_FRAME_GIOに配置した
M1/M2 PINへ接続してください」。以降、すべての結線はGIOの42個の名前付き信号ピン
（`HIZ1〜15`, `OUT1〜15`, `P1〜15`— 座標は`LEF/OSS_FRAME_GIO.lef`のPIN矩形、
ORIGIN(1250,1250)オフセット補正で実座標に変換）とVDD/VSSの2ピンのみを対象とする。

### 75.2 FRAME再生成の複数ラウンドへの追従

ユーザーがFRAME GDSを繰り返し編集（VDDドーナツ形状の廃止→14分散パッド化、
VDD/VSSラベルの入れ替わり訂正など）するたびに、`LEF/OSS_FRAME_GIO.lef`を
テキストラベル一致方式（`gen_gio_lef.py`、"VDD"/"VSS"という文字列そのものを
探すため、ラベルが入れ替わっても自動追従）で再生成し、トップGDS
（`src/tr_1um_i2c_slave_async.gds`）内のGIOインスタンスを`ly.read(FRAME_GDS)`で
差し替え、`script/route_gio_core.py`でGIO⇔コア間20信号＋VDD/VSSを再配線する
パイプラインを確立した。

### 75.3 GIO⇔コア配線ルータの設計

`script/route_gio_core.py`: コア外周の各ネット出口点からGIOの各ピンまでを、
コアを中心とする「リング（周回）座標」上の最短弧でつなぐ独自ルータ。
複数ネットが同じ半径帯を使えるよう区間スケジューリング（グリーディ）で
レーン（半径オフセット）を割り付け、干渉するネットのみ別半径にずらす。
VDD/VSSは配線経路が長い・幅を太くする必要がある等の理由で共有レーンプールから
外し、専用半径（`connect_core_to_gio_private`）で個別ルーティングしている。
DRC基準は`M1.W1≥1.8/M1.S1≥1.4/M2.W1≥3.0/M2.S1≥2.0/V1.S1≥1.5/V1.M1・M2.V1
enclosure≥1.0`（コア単体で確立した基準を流用）。

### 75.4 LVSデバッグ1: VDDポートがネットリストに現れない

ユーザーがコアセル単体LVS（`W(i2c_slave_async_nrow_fm)`、レイアウト側を独立
抽出して参照netlistと比較する方式）を繰り返し実行し、`.lvsdb`をアップロード。
`$49 vs VDD`という"Port mismatch"が繰り返し発生。原因調査の結果:

- VDD電源行レール（M1、コア全幅で連続する4本）にGDSテキストラベル"VDD"を
  追加しただけでは、LVS抽出でPORTとして認識されなかった。
- 既存の全"本物"ピン（`busy`/`rw`/`rx_valid`/`tx_data[*]`等）はすべて、
  テキストラベルに加えて**3×3umの境界マーカー矩形**（M1系はレイヤ48/1
  = M1PIN、M2系は49/1 = M2PIN）を伴っていた。
- さらに、全"本物"ピンはセル境界から1.5〜7.8um以内に位置していた
  （セル中央のような内部位置のラベルはPORTとして拾われない）。

対策: VDD行レールの1箇所（境界近傍、既存ピンと同じ規約のx=1618.5）に
テキスト+3×3um M1PINマーカーを追加。これでLVS抽出netlistの先頭ポートに
`VDD`が正しく現れるようになった（`.SUBCKT i2c_slave_async_nrow_fm VDD ...`）。

### 75.5 LVSデバッグ2: VSSはグローバルネットだった

同様にVSS（GND）行レールにも境界隣接のラベル+マーカーを追加したが、今度は
トップレベルLVSで「`No equivalent pin VSS from netlist found in reference
netlist. This is an indication that additional physical connections are
made to the subcircuit cell.`」という新しいエラーが発生。`.lvsdb`を精査した
結果、このLVSデックはレイヤl1/l2（基板/ウェルタップ系）を

```
G(l1 VSS)
G(l2 VSS)
```

という**グローバルネット**として"VSS"を宣言していることが判明。グローバル
ネットは、それに触れる形状のあるすべての階層境界に自動的に暗黙のピンとして
現れる（定義上、明示ラベル不要）。そのためVSSの明示ラベル追加はVDDと違って
不要かつ有害（階層LVSで「参照側にない余計な接続」として検出される）と判断し、
VSSの明示ラベル+マーカーは削除（VDDのみ維持）。

### 75.6 LVSデバッグ3: コアsymbolのGND/VSS命名を全体でVSSに統一

コアの`i2c_slave_async_nrow_fm.sym`は当初、電源ピンを`VDD`/`GND`と命名して
おり、GIO/FRAME/配線スクリプトの`VSS`命名と不一致だった（"Port mismatch
'VSS' vs. 'GND'"）。ユーザーがsymbolのGNDピンをVSSへ改名し、
`i2c_slave_async_nrow_fm.spice`を再ネットリストしたところ、コアセル単体LVSは
（VDD/VSS/`busy`↔`rst_n`入れ替わり疑惑も含め）**完全にクリーン**になった。
トップレベルの参照配線名（`tr_1um_i2c_slave_async.sch`側のワイヤ名）も
`GND`→`VSS`に統一したことで、トップレベルLVSのエラーもVDD/VSS関連は解消。

### 75.7 GIO/OUT2の誤配線（実配線バグ）の発見と修正

上記解消後もトップレベルLVSに1件残った未マッチネット`NET27`を、
`tr_1um_i2c_slave_async.spice`のGIOインスタンス（`x1 ...`）呼び出しを
位置引数ベースで全44ピン照合した結果、GIOの`OUT2`ピンは本来スキーマティック上
未使用ネット（`net27`、孤立・1ピンのみ）に繋がるべきところ、
`script/route_gio_core.py`の`NETS`辞書内`"VSS_OUT2"`エントリにより**誤って
VSSへ接続**されていたことが判明（過去セッションで「未配線ピンをVSSへ接続」した
際にOUT2を誤って対象に含めたことが原因）。同様の他エントリ（`HIZ9`/`HIZ10`/
`OUT13`→VSS、`HIZ2`/`HIZ7`/`HIZ15`→VDD、および全20信号）は全ピンを
スキーマティックの位置引数と照合し直し、いずれも正しいことを確認。
`VSS_OUT2`エントリのみ削除し、GIOの`OUT2`ピンは完全に未接続（フローティング、
スキーマティックの意図通り）とした。

### 75.8 最終結果

再配線後、トップレベルLVSがクリーンに通過（ユーザー確認: 「LVS 通りました。」）。
DRCは全カテゴリ0違反を維持。最終成果物:
`src/tr_1um_i2c_slave_async.gds`（トップGDS本体）、
`src/tr_1um_i2c_slave_async_routed.gds`（GIO⇔コア配線済み最終出力）、
`script/route_gio_core.py`（再現可能な配線スクリプト、上記全修正を反映）。

**教訓**: (1) LVS抽出のPORT認識は「テキストラベルの有無」だけでなく「境界隣接の
専用マーカーレイヤ（M1PIN/M2PIN）の有無」に依存するPDKがある。(2) グローバル
ネット宣言のあるネット名（本プロジェクトでは`VSS`）は、階層LVSでは明示ラベルを
足さない方が安全な場合がある。(3) 「未配線ピンへの一括対応」のような機械的な
修正は、対象ピンごとにスキーマティックの実際の結線意図（使用/未使用）を
個別確認しないと、本ケースのOUT2のように新たな誤配線を生みうる。

## 76. IRSIMチップレベル動作検証用`.sim`/`.cmd`生成（2026-08-27）

トップレベルLVSクリーン化（§75）に続き、ユーザーがローカル環境にIRSIMを
インストール・動作確認済みであることを受け、チップ全体（コア＋GIOパッド
リング）を対象としたIRSIM用`.sim`ファイルおよび刺激（`.cmd`）スクリプトを
準備した。

### 76.1 方針

`src/tr_1um_i2c_slave_async.extracted`（KLayout LVS抽出、トップレベルLVS
クリーン済み・トランジスタレベル）を情報源として採用。既存の
`script/sim/switch_sim.py`（コア単体を対象にした自前Python製switch-level
シミュレータ、BUFTH/DEL1/DFFRの3セル種をビヘイビア置換して理想スイッチ
モデルの限界を回避）とは異なり、IRSIM本体は実デバイスサイズに基づく
RC/イベント駆動モデルを持つため、**ビヘイビア置換を一切行わずコア+GIO+
全16パッドセルまで完全にトランジスタレベルへ再帰的フラット化**する方針とした
（`script/gen_irsim_sim.py`）。

### 76.2 フラット化の実装

`.extracted`は`X$N ... <セル型名>`形式のインスタンス呼び出しが多段階層
（トップ→コア/GIO→GIOの場合はさらに`OSS_ESD_5V_DIO`等の1段深い階層）を
成すSPICEネットリスト。再帰関数で各インスタンスの実引数を子セルの仮引数へ
位置対応させ、子セル内部専用ノード（仮引数に無い名前）はインスタンスパス
（例: `X$1.X$13.$9`）でユニーク化。結果: トランジスタ2082個、ノード866個、
インスタンス286、階層深さ最大2。全参照が解決し未解決セル型エラー無し
（ネットリストの構造的健全性を確認）。

ESD保護クランプダイオード（各`OSS_ESD_5V_DIO`に2個、`D$17`/`D$18`、
`VSS`↔`OUT`/`HIZ`間、モデル名`DN`）は通常動作時は逆バイアス・非導通であり
論理機能に寄与しないこと、およびIRSIM `.sim`形式にダイオード素子が存在
しないことから、意図的に省略した（機能検証上は無害な省略）。

### 76.3 信号→フラットノードの導出と相互検証

チップの真のトップサブサーキット（`tr_1um_i2c_slave_async`）はポートを
`VSS`しか持たない（他の全信号はGIO/コア間の内部匿名ネット）ため、信号名から
ノード名への対応は総当たりでなく、コア側インスタンス`X$3`
（`... i2c_slave_async_nrow_fm`）の実引数リストを、`i2c_slave_async_nrow_fm`の
`.SUBCKT`仮引数順（`VDD, tx_data[7], sda_oe, tx_data[1], tx_data[0], rx_data[4],
rx_data[6], rx_data[5], rx_data[7], rx_data[3], rx_data[1], rx_data[2],
rx_data[0], rst_n, scl, sda_in, tx_data[6], tx_data[5], tx_data[4], tx_data[3],
tx_data[2], VSS`）と位置照合して機械的に導出した:

```
VDD=$1  VSS=VSS  rst_n=$106  scl=$102  sda_in=$58  sda_oe=$45
tx_data[0..7] = $26 $19 $21 $22 $57 $103 $72 $68
rx_data[0..7] = $32 $15 $34 $13 $42 $78 $83 $64
busy/rw/addr_match/rx_valid = X$3.busy / X$3.rw / X$3.addr_match / X$3.rx_valid
（チップ外部ピンには出ていない内部専用信号 -- ユーザー既述の通り
「busy rw rx_valid addr_match は使ってません」。IRSIM上は実機のプローブ同様、
任意の内部ノードを直接watch可能なので支障無い）
```

この結果はGIO側インスタンス`X$1`（`... OSS_FRAME_GIO`）の実引数リストとも
独立に相互検証できた: `X$1`の第1引数（GIO仮引数`VDD`の位置）が`$1`と一致
（コア側`VDD=$1`と同一値）、`HIZ13`位置の実引数が`$45`（=`sda_oe`）と一致、
`P15`(rst_n)/`P2`(scl)/`P13`(sda_in)各位置の実引数もそれぞれ`$106`/`$102`/`$58`
と一致。また過去の`route_gio_core.py`実装（§75）で確定済みの各種固定結線
（`VDD_HIZ2`/`VDD_HIZ7`/`VDD_HIZ15`→`$1`、`VSS_HIZ9`/`VSS_HIZ10`/`VSS_OUT13`
→`VSS`、DISチェーン8ピン→単一ネット`$2`）も全て`X$1`の実引数と厳密に一致し、
既存の物理配線実装との整合性を再確認した。

### 76.4 SDAパッドのオープンドレイン構成とプルアップ追加

`sda_in`（$58）はGIOのP13パッド自体と同一ノード（コアの`sda_in`入力ピンが
直接パッドに結線されている）。同じ`OSS_ESD_5V_DIO`インスタンスの`OUT`端子は
`VSS`に固定配線されている（`OUT13`仮引数の実引数が`VSS`）ため、このパッドの
ドライバは実質**オープンドレイン専用**（`sda_oe`(=`HIZ13`)が1のときのみ
SDAをLOWへ駆動、0で解放）として機能する。オンダイのプルアップ抵抗は
このネットリストに存在しないため、`.sim`ファイルへ外付け相当のプルアップ
（`r $58 $1 20000` = 20kΩ、実I2Cバスの代表値）を追加し、既存MyHDL
テストベンチ（`bus_model`の`not (sda_master_oe or sda_slave_oe)`論理）と
等価なバス挙動を実現した。

### 76.5 GIOの共有パッド構成（P/OUT分離、SCLパッドの未使用ドライバ）の発見

tx_data/rx_dataの8ビットは、GIOの物理パッド8個（P1/P3/P4/P5/P6/P11/P12/P14、
いずれも共有DISチェーンでHIZが制御される）を`PAD`端子（tx_dataの直接タップ、
DIS経由の駆動状態に関わらず常時読み出し可）と`OUT`端子（rx_dataの駆動元、
DISが有効化されない限り外部パッドまで到達しない内部信号）とで使い分けている
ことが判明。IRSIM上のノード観測はこの共有パッド機構を経由せず、コア直結の
`OUT`側タップ（`$32`等）を直接watchするため、DISチェーンの状態に関わらず
rx_dataの真値を正しく観測できる（実機プローブと同様、パッケージピンでの
外部観測ではなく内部ノード直読みのため、本セクションの共有パッド機構は
IRSIM検証上は迂回できる）。

なお`scl`パッド（P2）に対応する`OSS_ESD_5V_DIO`インスタンス（`X$10`）の
`OUT`端子（`OUT2`）はGIOのトップレベル仮引数リストに現れない
純粋な内部専用ノードで、どこにも別途配線されていない（§75.7で発見した
「NET27/OUT2は未使用ネット」の裏付けと一致）。SCL/RSTNは入力専用ピンとして
運用されており、ドライバ側は意図的に未使用のまま残されている。

### 76.6 ソフトウェアpre-flightチェックの試行と限界

本セッションのサンドボックス環境には実IRSIMが無いため、代替として
`script/sim/switch_sim.py`と同じ理想スイッチ・union-findエンジンを使い、
フラット化済み2082トランジスタ全体に対しリセット後の状態遷移を試算した
（`preflight.py`、成果物ディレクトリのみ・プロジェクトへは未配置）。結果、
DFFRBのトランスミッションゲート型マスタ/スレーブ構造や`OSS_ESD_5V_DIO`
内部のキーパー的フィードバック構造に起因する多数の見かけ上のVDD/GND
コンテンションが発生した。これは`switch_sim.py`のdocstringが既に警告して
いた「ゼロ遅延の理想スイッチモデルはCKエッジ時のmake-before-break競合を
実際より悲観的に短絡と誤判定する」現象そのもの（同ドキュメント内、
X$111の`$9`ノードでの実例と同種）であり、**ネットリストの配線誤りではなく
理想スイッチモデル固有の既知の限界**と判断した。実IRSIM（RC/相対デバイス
強度モデル）はこの種のアーティファクトを生じないため、本チェックはこれ以上
追い込まず、構造的健全性（全参照解決・トランジスタ数/ノード数の一貫性・
§76.3の相互検証）の確認をもって代替とした。フラット化ロジック自体の正しさは
`gen_irsim_sim.py`が例外無く完走したこと、および§76.3の複数系統からの
独立クロスチェックが一致したことで担保している。

### 76.7 生成物と実行方法

- `script/gen_irsim_sim.py` -- `.sim`生成（再帰フラット化、プルアップ抵抗追加）
- `script/gen_irsim_cmd.py` -- `.cmd`生成（`test_i2c_slave_async.py`/
  `test_i2c_slave_async_negative.py`と同一のバス上トランザクションを機械的に
  h/l/x/sコマンド列へ翻訳）
- `irsim/tr_1um_i2c_slave_async.sim`（2082トランジスタ、866ノード）
- `irsim/irsim_test_main.cmd`（write 0xA5 → read 0x3C、SLAVE_ADDR=0x50）
- `irsim/irsim_test_negative.cmd`（誤アドレスNACK確認）
- `irsim/README.md`（信号対応表・実行コマンド）

`.prm`（電気パラメータファイル）はTR-1um実測の特性化データが無いため未作成。
ユーザーが動作確認済みのローカルIRSIM環境が既に使っている`scmos.prm`
（プレースホルダ、論理機能検証には十分）の流用を`irsim/README.md`で案内した。
実行: `irsim <scmos.prm> irsim/tr_1um_i2c_slave_async.sim -@ irsim/irsim_test_main.cmd`

### 76.8 実機IRSIMでの初回実行結果と修正（2026-08-27）

ユーザーのローカルIRSIM（9.7.121）で実行したところ2つの問題が判明、修正した。

1. **`.prm`未指定**: `irsim <sim> -@ <cmd>`のように`.prm`を省略すると、
   `.sim`ファイルが誤って`.prm`として解釈されエラーになる。`.prm`は
   必ず最初の引数に必要（`irsim <prm> <sim> ...`）。ユーザー環境では
   `/usr/local/lib/irsim/prm/scmos100.prm`（1umプロセス名に最も近い
   同梱ファイル）を暫定使用することとした。
2. **ノード名の`$`が実機でエラー**: `$58`のような（KLayoutが無名ネットに
   自動付与する）ノード名を実際にIRSIM 9.7へ読ませたところ、
   `subcircuit "$58" is not defined!`等のエラーが発生。IRSIM 9.7の
   コマンドインタプリタ（Tcl由来）は先頭の`$`を変数展開記号として扱う
   ため、リテラルなノード名として使えないことが実行結果から判明した。
   `gen_irsim_sim.py`に`sanitize()`（全ノード名の`$`→`N`置換、例:
   `$1`→`N1`、`X$3.busy`→`XN3.busy`）を追加し、`.sim`/`.cmd`双方を
   再生成、`irsim/README.md`の対応表も合わせて更新した。
3. **`-@ cmdfile`のコマンドライン指定も実機で機能せず**: `.cmd`ファイルが
   コマンドスクリプトとしてではなく通常のネットリストとして読み込まれ、
   多数の構文エラー（`Unrecognized input line`等）が発生。原因は未特定
   （ビルド差異の可能性）だが、インタラクティブに起動してプロンプト上で
   `@ cmdfile`を使う方法（IRSIMの伝統的な使い方、複数の独立文書で確認済み）
   を代替手順として`irsim/README.md`に記載した。
4. **電源/GNDノード名の不一致によるセグメンテーションフォールト**: `.prm`を
   正しく指定して`.sim`単体を読み込ませたところ、`Using default name "Vdd"
   for power net` / `"Gnd" for ground net`という警告の直後（インタラクティブ
   プロンプトへ到達する前）に実機でセグメンテーションフォールトが発生した。
   IRSIMはデフォルトで電源/GNDネットを`Vdd`/`Gnd`という文字通りの名前で
   自動検出しようとするが、当時のチップの供給ノードは`N1`（元`$1`）/`VSS`
   という名前のままだったため認識されず、電源/GND未確定のまま後続の内部
   解析処理がクラッシュしたと推定される。`gen_irsim_sim.py`に`rename_rails()`
   を追加し、チップのVDD/GND供給ノードのみを`Vdd`/`Gnd`へ追加リネーム
   （`-p`/`-g`コマンドラインオプションに頼るより、ファイル自体を素直な既定名に
   合わせる方が確実と判断）。`.sim`/`.cmd`双方を再生成、`irsim/README.md`の
   対応表も合わせて更新した。

### 76.9 `Vdd`/`Gnd`リネーム後も同一箇所でクラッシュ、`lldb`による原因特定と`r`素子の排除

`Vdd`/`Gnd`リネーム後も同一セグメンテーションフォールトが再発。ユーザーが
`lldb`でバックトレースを取得した結果、クラッシュ箇所は`.sim`読み込み完了
直後の`ConnectNetwork`→`connect_txtors`（トランジスタをノードへ接続する
IRSIM内部処理）であることが判明。切り分けのため二段階のテストを実施:

1. **環境そのものの健全性確認**: IRSIM公式チュートリアルのAOIゲート例
   （トランジスタ6個、本プロジェクトと無関係、`irsim/sanity_check.sim`として
   保存）を実機でロード——正常にプロンプトまで到達。IRSIM本体・`.prm`・
   環境自体には問題が無いことを確認。
2. **`.sim`ファイルの末尾1行（`r N58 Vdd 20000`、SDAプルアップ用の抵抗素子）
   を除去した版**を実機でロード——**2082トランジスタ・867ノードが正常に
   ロード・接続完了**（`shorted=228`は`FILL2`/`FILL3`デカップリング容量的
   構造がソース=ドレイン=同一レールとして意図的に短絡扱いされているだけで
   異常ではない）。

これにより、クラッシュの原因は**`r`（抵抗）素子の行**（Webから得た情報を基に
書式を推測しており、実機での正式な検証はできていなかった）に確定した。
正式な`r`素子の書式を裏付け無く使い続けるリスクを避け、代わりに**常時ON
（ゲートをGndへ固定）の弱いPMOSプルアップ**（`p Gnd Vdd N58 20 1`、長チャネル
・細幅で高抵抗相当）に置き換えた。通常のトランジスタ行は既に実機で動作
確認済みのため、これで書式起因の再発リスクを排除できる。実チップの
オンダイI2Cプルアップも抵抗ではなくこの種の弱い常時ONトランジスタで実装
されることが多く、設計的にも妥当な代替。`gen_irsim_sim.py`/`.sim`/
`irsim/README.md`を更新。

### 76.10 リセット後もbusy/sda_oe/rw/addr_matchがXのまま（ゲート型リセットの発見）

`r`素子の除去後、実機でトランザクション全体を実行したところロードは成功、
`N102`(scl)は正しくトグルし`N106`(rst_n)も意図通り一度LOW→HIGHになったが、
`N58`(sda_in)は断続的にX、`N45`(sda_oe)・`XN3.busy`・`XN3.rw`・
`XN3.addr_match`は**シミュレーション終了まで一貫してX**のままだった。

`src/tr_1um_i2c_slave_async.extracted`のコア本体を解析し、33個の`DFFRB`
インスタンス全てのRSTB結線を機械的に洗い出したところ、**どのDFFRBもコアの
`rst_n`ピンへ直接は繋がっておらず**、2種類の派生（ゲート済み）リセットネット
のいずれかを使っていることが判明した:

- `$28`（24個のDFFRBが使用、`rw`/`addr_match`/`shreg[*]`/`rx_data[*]`等）
  = `NOR2($702, $813)`の出力（`$702`/`$813`の由来は本セッションでは未追跡）
- `$227`（9個のDFFRBが使用、**`sda_oe`のDFFRBを含む**）
  = `AND2_X1(busy, rst_n)`の出力 -- すなわち**`busy` AND `rst_n`**

一方`busy`自体はDFFRBではなく、`logic_cells_mapping.md`/design_notes既述の
通り**NOR2 2個の交差結合によるSRラッチ**（`start_pulse`でSET、
`stop_pulse | ~rst_n`でCLEAR）であり、DFFRBのような単純な非同期クリア機構
を持たない。電源投入直後（IRSIMのシミュレーション開始時点）はこのラッチの
内部ノードが未定義（X）であり、`rst_n=0`を印加している間は
`~rst_n=1`によりCLEARが強制的に1へ確定する（1との論理和は入力に依らず1）
ため`busy`は理論上0へ収束するはずだが、**`rst_n`を1へ戻した瞬間、`busy`の
ラッチ自体がまだ収束し切っていなければ`$227 = AND(busy, 1)`が再びXへ戻り、
`$227`をRSTBに使う9個のDFFRB（`sda_oe`含む）のクリアが不定になる**という
構造的な依存関係を持つ。

これに対応するため、`gen_irsim_cmd.py`の`preamble()`を修正:
リセットのアサート時間を100ns→**3000ns**（`irsim_test_main.cmd`/
`irsim_test_negative.cmd`）へ延長し、加えて**リセットのみを長時間保持して
`sda_oe`/`busy`/`rw`/`addr_match`が定義値へ収束するかだけを素早く確認する
専用スクリプト`irsim/irsim_reset_check.cmd`**（100/500/1000/3000/8000ns時点
でチェックポイントdump）を新設した。まずこちらを実行し、Xが解消する時間帯を
特定してから本トランザクションテストに進むことを推奨。

**教訓**: 非同期リセットを謳うDFFRBセルでも、RTL/合成の結果としてRSTBピンに
「素のリセット信号」ではなく他の状態信号との組み合わせ（ゲート型リセット）
が配線されることがあり、その場合は依存元の状態（本件では`busy`ラッチ）が
先に収束していないと、当該レジスタの非同期クリアが実質的に機能しない
（IRSIMの3値(0/1/X)シミュレーションでは特に顕在化しやすい）。ネットリスト
上のRSTB結線を機械的に洗い出して確認する価値がある。

### 76.11 実機での`irsim_reset_check.cmd`結果と真因（SDAパッドの循環依存）

延長版`irsim_reset_check.cmd`を実機で実行したところ、リセットアサート中
（t=100〜8000ns）は`sda_oe`/`busy`/`rw`/`addr_match`全てが速やかに**確定した0**
へ収束することを確認（0ns～8000nsの間、遅い設定時間の問題ではないと判明）。
しかし**リセット解除直後（t=8100ns）に全て再びXへ戻る**という結果だった。

`busy`の駆動元（`i2c_slave_async_nrow_fm`本体を機械的に解析）を辿ったところ、
`busy = NOR2($695, $707)`で、その片方の入力`$695 = NAND2(rst_n, $696)`。
`rst_n=0`の間は`NAND(0, X)=1`が入力値に依らず確定するため`busy`は問題無く
0へ収束する。しかし**`rst_n=1`（解除後）では`$695 = NOT($696)`となり、
`$696`自体の値に依存するようになる**。`$696`をさらに辿ると
（`$696 = NAND3($817, $666, $697)`、`$817`は`sda_in`の遅延コピーの反転、
`$666`はバッファ済み`scl`、`$697`はバッファ済み`sda_in`——START/STOP
エッジ検出ロジックの一部、design_notes §3のものと一致）、**`$696`は純粋に
`sda_in`と`scl`のみに依存する組合せ回路**で、レジスタ経由の循環は無い。

問題は`sda_in`（`N58`）自体の値だった: `sda_oe`(`N45`)はSDAパッドの
オープンドレインドライバのイネーブル信号（`OSS_ESD_5V_DIO`のHIZ入力）であり、
**`N45`がX（不定）の間、IRSIMはそのドライバがLOWを駆動している可能性を
否定できないため、外付けの弱いプルアップだけでは`N58`を確定した1に
解決できない**。すなわち: `sda_oe`(N45)がX → SDAパッドドライバのイネーブル
状態が不明 → `sda_in`(N58)もX → `$696`経由で`busy`もX → `busy`が
RSTB2(`$227`=`busy AND rst_n`)を経由して`sda_oe`自身のDFFRBのクリアを不定に
する、という**真の循環依存**が存在することが判明した（レジスタを介さない
純粋な組合せ的循環ではなく、"弱いプルアップ vs 不定状態のドライバ"という
IRSIMの3値ロジックならではの循環）。

**対策**: `gen_irsim_cmd.py`の`preamble()`（および`irsim_reset_check.cmd`）で、
SDAをリセット・立ち上げ期間中は`x`（解放、弱いプルアップ任せ）ではなく
**`h`（強制的にHIGH）**にするよう変更。IRSIMの`h`/`l`は「回路が何をしようとも
関係なく強制する」仕様（絶対的な外部ドライブ）のため、これでSDAパッド側の
不確定なドライバ状態に関わらず`sda_in`を確定した1にでき、循環を断ち切れる。
実際のSTART条件が始まる`start()`の冒頭で`x SDA`へ切り替え、以降は弱い
プルアップに委ねる（この時点では`busy`/`sda_oe`は既に確定済みのはず）。
`.sim`/`.cmd`双方を再生成、`irsim/README.md`も更新。

**教訓**: オープンドレイン双方向パッドを持つ設計をIRSIM等の3値シミュレータで
検証する場合、「弱いプルアップを付けておけば安全に1に収束する」とは限らない
——**そのパッドの駆動イネーブル信号自体がX（不定）の間は、シミュレータは
プルアップとドライバのどちらが勝つか原理的に判定できない**ため、パッドの
値もXのまま伝播しうる。ブリングアップ／リセット期間中は該当パッドを
テストベンチ側から明示的に`h`/`l`で確定させ、プロトコルが実際に必要とする
タイミングでのみ解放（`x`）するのが安全。

**教訓**: (1) LVS抽出ネットリストは階層情報を保持しているため、IRSIM `.sim`
（完全フラット）への変換は再帰的な仮引数↔実引数バインディングの一般化で
機械的に行える(switch_sim.py方式のような特定階層深さへのハードコードは
不要）。(2) チップのトップサブサーキットにポートが無い（＝全信号が匿名
内部ネット）設計では、単一の情報源（例えば片方のサブサーキット呼び出しの
みを起点とする位置照合）に頼らず、複数の独立した経路（本件ではコア側と
GIO側の双方の実引数リスト、および既存の物理配線実装）から同じ結論が導ける
ことを確認してから確定させるべき。(3) 理想スイッチ・ゼロ遅延の
ソフトウェアシミュレータは、トランスミッションゲート型FFや帰還を持つ
アナログ的I/Oセルを含む設計の完全フラットネットリストには原理的に不向き
（switch_sim.py方式のビヘイビア置換か、実RCモデルを持つ本物のシミュレータ
（IRSIM等）が必要）。

### 76.12 `BUFTH`自体の帰還ループ問題と、内部ノードforce/releaseによる回避策

§76.11の対策（SDAを`h`で強制）を適用しても、`sda_oe`/`busy`/`rw`/
`addr_match`は依然としてリセット解除直後にXへ戻ることが実機で判明した。
`irsim_debug_busy_chain.cmd`（`busy`への全ファンイン経路上の中間ノードを
悉皆watchする診断スクリプト）で切り分けたところ、**`BUFTH`（`scl`・
`sda_in`双方の入力に挿入されているSchmitt-triggerバッファ）自体の出力
（`$3.N37`=BUFTH(sda_in)出力、`$3.N712`=BUFTH(scl)出力）が、入力を
13000ns以上確定値で保持してもXのまま収束しない**ことが判明した。

手計算によるternary論理トレース（BUFTHの8トランジスタ構成、
`XM$1,$3,$4,$5,$6,$7,$8,$9`）では、`A=1`固定時に`XM$7`/`XM$8`
（両方ゲート=`A`のみに依存、既知値）が`Gnd→N6→N2`という決定的な直列
プルダウン経路を形成し、これにより内部ノード`N2`は確定的に0、続いて
`XM$5`（ゲート=`N2`=0でON）経由で出力`Y`は確定的に`Vdd`＝1に解決される
はず、という結論だった。しかし実機の結果はこれと矛盾していた。

**単体切り分け**: BUFTHの8トランジスタだけを`src/tr_1um_i2c_slave_async.extracted`
から抽出し、`vdd`/`gnd`のみ`Vdd`/`Gnd`にリネームした単体`.sim`
（`bufth_isolated.sim`）と、`A`を`h`固定して2000ns超保持する`.cmd`
（`bufth_isolated.cmd`）を作成し実機で実行。**フルチップから完全に
切り離した単体でも同じ現象を再現**（`A=1`を2000ns超保持しても
`Y`/`N2`/`N6`全てX）。これによりフルチップ内の他ノードとの相互作用ではなく
**BUFTH自身のトポロジーに起因する問題**であることが確定した。

**真因（仮説、実機テストで裏付け）**: `N2`は`XM$9`（NMOS、ゲート=`N2`自身、
`vdd`から`N6`への経路）を介して`N6`へ帰還しており、`N6`はさらに`XM$7`
経由で`N2`自身へ戻る——**`N2`と`N6`は`A`が既知でも互いに依存し合う真の
帰還ループ**であり、これはBUFTHがSchmitt trigger（ヒステリシス）として
機能するための本質的な正帰還構造そのものである。手計算のグラフ到達可能性
トレース（`Gnd→XM$8→N6→XM$7→N2`、両ゲートとも`A`のみに依存）は経路の
存在自体は正しいが、IRSIMのternaryソルバーは`N2`↔`N6`の相互依存を
**「X同士が自己無矛盾な不動点」として扱い、冷起動（電源投入直後の全ノードX
状態）から一度もこのループの対称性を破れない**ものと推測される。すなわち
`XM$9`（ゲート=`N2`、現在X）が「導通するかもしれない」X強度の経路を`N6`へ
供給し続け、これが`XM$8`（ゲート=`A`、既知でON、確定的に0を供給）と
「競合」した結果として保守的にXのまま据え置かれ、`A`の値が変化してもこの
据え置き状態からは自発的に脱出しない、という動作と考えられる。

**確認テスト（`bufth_isolated_v2.cmd`）**: 上記仮説の検証として、単体BUFTH
に対し内部ノード`N2`を一度だけ`l`で強制し、直後に`x`で解放するテストを
追加。結果は**強制解放後も`N2=0`/`Y=1`/`N6=0`が500ns以上正しく保持**され、
Xへ戻らなかった。これは「`A`が既知である限りBUFTHの周辺回路は完全に
決定的であり、帰還ループの対称性さえ一度崩せば、あとは自力で正しい値を
保持し続ける」という仮説を裏付ける結果。

**対策**: `gen_irsim_cmd.py`の`preamble()`（`irsim_test_main.cmd`/
`irsim_test_negative.cmd`共通）および`gen_reset_check()`
（`irsim_reset_check.cmd`）の両方に、SDA/SCLを`h`固定した直後・リセット
待機に入る前のタイミングで、両方のBUFTHインスタンスの内部`N2`ノードへの
一回限りのforce/release（`l`→`s`→`x`→`s`）を追加。実際のノード名は
フラット化後の`.sim`から特定:

  - BUFTH(sda_in)：インスタンス`$3.X166`、内部ノード`$3.XN166.N2`
    （置換後`XN3.XN166.N2`）、出力`XN3.N37`、入力`A=N58`
  - BUFTH(scl)：インスタンス`$3.X311`、内部ノード`$3.XN311.N2`
    （置換後`XN3.XN311.N2`）、出力`XN3.N712`、入力`A=N102`

（`.sim`中で`A`に該当するノード名（`N58`/`N102`）をgrepし、8トランジスタの
ブロックから`XM$1`相当の行のgate/source/drainパターンで内部ノード名を
特定した。）`.cmd`を再生成し、`irsim_reset_check.cmd`をユーザーが再実行して
`busy`/`sda_oe`/`rw`/`addr_match`が今度こそリセット解除後も確定値を保持
するか確認待ち。

**教訓**: (1) 手計算によるternary論理の到達可能性トレース（「既知のゲート値
のみに依存する経路が存在するなら値は確定するはず」）は、**帰還ループを
含む回路には当てはまらない**——スイッチレベルシミュレータの不動点計算は
グラフ到達可能性だけでなく、ループを構成するノード同士の相互評価順序にも
依存しうる。(2) ヒステリシス／正帰還構造（Schmitt trigger、SRラッチ等）を
含むセルは、`switch_sim.py`のdocstringが警告していた通りIRSIMのような
ideal-switchベースのシミュレータでも冷起動からの自発的な収束を保証
できない場合がある——電源投入シーケンスの一部として、こうしたセルの内部
状態ノードを一度だけ明示的にforce/releaseして対称性を破ることが実務上
必要になりうる。(3) 疑わしい箇所はフルチップから完全に切り離した最小
単体テスト（本件の`bufth_isolated.sim`）で再現性を確認してから対策すると、
「他ノードとの相互作用か、セル自体の問題か」を確実に切り分けられる。

### 76.13 `DFFRB`自体にも同種の帰還ループ問題（33インスタンス共通）、`QS`ノードのforce/releaseで解決

BUFTH修正版`irsim_reset_check.cmd`を実機で実行したところ、`N58`/`sda_oe`(`N45`)/
`busy`はリセット解除後も正しく確定値（1/0/0）を保持し続けることを確認——BUFTH
修正は成功。しかし**`rw`/`addr_match`はリセット解除直後にXへ反転**した。

`irsim_debug_rw_addrmatch.cmd`でさらに追跡したところ、`$28`（`rw`/`addr_match`の
RSTB、`NOR2($813,$702)`）自体は解除後に**本当に確定値1へ解決**していた
（`$702=INV(rst_n)`によるドミナンスの見かけ上の0ではなく、$813の実際の値に
基づく正しい1）。つまり問題は`$28`ではなく、**`DFFRB`セル自体**にあった。

`rw`を駆動するDFFRBインスタンス（`X$2`）の8トランジスタ超のトランジスタレベル
構成を手で解析: `Q(=rw)`は`QB`の単純なインバータ、`QB`は（RSTB=1の間は）`QS`の
単純なインバータ——ここまでは決定論的。しかし**`QS`（マスタースレーブ構造の
スレーブ側ストレージノード）自体は、クロック位相`CKB`/`CKP`でゲートされた
2本のトランスミッションゲートを介して自分自身にフィードバックする**構造
（一方はマスタ側`QM`から、もう一方は`QB`を介した保持ループから）。このチップは
非同期設計で、まだ一度もプロトコル動作（＝実際のクロックエッジ）が発生して
いないため`CK`関連ノードもX——BUFTHの`N2`↔`N6`と全く同種の、冷起動状態からは
自発的に解決できないternary不動点になっていた。**RSTBは`QB`/`Q`だけを直接強制し、
`QM`/`QS`などの内部ストレージノードには一切触れない**ため、`RSTB`解除後は
この帰還ループの解決待ちで`Q`がXのまま固まる。

`irsim_debug_dffrb.cmd`で`rw`のDFFRB一つに絞り、内部`QS`ノード
（`XN3.XN2.QS`）を一度だけ`l`→`x`するテストを実機で実行したところ、
**解放後も`QS=0`/`QB(N4)=1`/`rw=0`が2000ns以上正しく保持**され、
BUFTHと全く同じ対策パターンが有効であることを確認した。

**スコープ**: `DFFRB`はこのコアで**33箇所**使われている（design_notes §76.10
参照）。全て同一サブサーキットのインスタンスなので内部ノード名パターンは共通
（`XN3.<インスタンス名>.QS`）。33個を洗い出し、RSTBグループ別に分類:

  - Aグループ（24個、RSTB=`$28`、`rst_n`解除と同時に解除）: `rw`(`X$2`)、
    `addr_match`(`X$16`)含む。`X$2, X$16, X$32, X$46, X$104〜X$123`（一部欠番）。
  - Bグループ（9個、RSTB=`$227`=`busy AND rst_n`、`busy`が初めて1になる
    ＝最初のSTART条件成立時にのみ解除）: `sda_oe`自身の register(`X$318`)含む。
    `X$314〜X$322`。

**対策**: `gen_irsim_cmd.py`に`CmdGen.force_release(nodes, value)`ヘルパーを
追加（BUFTHの2ノードと合わせて共通化）。Aグループ24個は`preamble()`内で
`rst_n`解除の直後に一括force/release。Bグループ9個は`start()`に`first=True`
引数を追加し、その中で「最初のSTART後、busyが初めて1になった直後」に
一括force/release（`gen_main()`/`gen_negative()`の最初の`start()`呼び出しに
`first=True`を指定）。`irsim_test_main.cmd`/`irsim_test_negative.cmd`/
`irsim_reset_check.cmd`を再生成。`irsim_reset_check.cmd`はAグループの
force/releaseまでしか含まない（START前なのでBグループはまだ意味を持たない）。

**教訓**: BUFTHで見つかった「非同期クリア（RSTB/CLR）がセルの出力段だけを
強制し、内部のマスタースレーブ・ストレージノードやヒステリシスノードには
触れない」という設計パターンは、**このチップで使われている複数種類の
regenerative/保持回路に共通する構造的特徴**であり、IRSIMのような理想
スイッチ・ternaryシミュレータでは、影響を受ける全インスタンスに対して
同じ「一度だけの内部ノードforce/release」対策が必要になる。1個のセルで
確認できた対策パターンは、同一セルの他インスタンスにも機械的に横展開できる
（本件は`gen_irsim_cmd.py`側でPythonループとして実装、手作業での複製ではない）。
現状はrw/addr_match（Aグループ代表1個）のみ実機確認済み——残り23個の
Aグループと9個のBグループは、次回`irsim_reset_check.cmd`/`irsim_test_main.cmd`
の実行結果待ち。

**追記（実機確認済み）**: 上記対策を適用した`irsim_reset_check.cmd`を実機で
再実行し、`N58`/`sda_oe`(`N45`)/`busy`/`rw`/`addr_match`全てがリセット解除後
100ns以上（t=8200ns時点）も確定値（1/0/0/0/0）を保持し続けることを確認。
Aグループ24個の一括force/releaseで実際に解決した。Bグループ9個（`start()`の
`first=True`経路）は次の`irsim_test_main.cmd`実行で確認予定。

### 76.14 BUFTHの問題は「冷起動時だけ」ではなく「入力遷移の度に」再発すると判明、恒久的なweak biasトランジスタで対策

`irsim_test_main.cmd`（プリアンブル直後に実際のSTART条件を発行する）を実機で
実行したところ、`sda_oe`(`N45`)と`busy`が解除後もXのままだった。波形を見ると
`N106`解除直後は問題無いが、**実際のSTART エッジ（SDAをSCL=1のままH→L）が
発生した瞬間からXに転じている**。`irsim_debug_start.cmd`でstart()の各ステップ
を個別にdumpしたところ、`N58`をl（実際のSTARTエッジ）にした直後、
`XN3.N697`（=`BUF_X1(BUFTH(sda_in)出力)`）が**再びX**になっていることを確認
（`N712`=`BUFTH(scl)`出力は`scl`が変化していないため1のまま健全）。

これは重大な追加発見: **BUFTH の帰還ループ問題は、冷起動時の一度きりの
不動点ではなく、`A`（`scl`/`sda_in`）が遷移する度に毎回再発する**。手計算では
`A=0`の場合も（`A=1`と対称に）`XM$3`/`XM$4`という別の決定的経路で`N2=1`に
確定するはずだが、実機はXのまま——`bufth_isolated_v2.cmd`で検証したのと
同じ「force/releaseで一度だけ対称性を破れば解ける」性質と考えられるが、
プロトコル動作中は`scl`/`sda_in`が絶えず遷移する上、ACK/読み出しビットなど
**スレーブ側が駆動する遷移はテストベンチ側が値を事前に知らない**ため、
毎回force/releaseを機械的に差し込む方式（`.cmd`スクリプトへの逐次介入）は
現実的でない。

**対策（恒久的、SDAプルアップと同じ手法）**: BUFTH内部の`N2`ノードに、
SDAパッドの弱いプルアップと全く同じ考え方で、**常時ONの弱いバイアス
トランジスタ**を`.sim`ネットリスト自体に追加した（`gen_irsim_sim.py`）。
ゲートをVdd固定にした弱いNMOS（`n Vdd Gnd <N2> 20 1`、BUFTH本来の
トランジスタ（L=1u, W=3.4〜10.2u）よりはるかに高抵抗）を`N2`へのプルダウン
として常設。`A=1`の場合（真値`N2=0`）は元々の正しい方向を弱く後押しするだけ
で無害。`A=0`の場合（真値`N2=1`）はBUFTH自身の直列プルアップ経路
（`XM$3`+`XM$4`、ゲート=`A`で確定的にON）の方がはるかに低インピーダンスなので、
弱いバイアスは負ける——SDAパッド自体で`sda_oe=1`が外付けの弱いプルアップに
確実に勝つのとまさに同じ比率解決のロジック。BUFTHインスタンス2個
（`sda_in`側=`XN3.XN166.N2`、`scl`側=`XN3.XN311.N2`）双方に追加し、
`.sim`を再生成（トランジスタ数2082→2085：SDAプルアップ1個＋BUFTHバイアス
2個）。`gen_irsim_cmd.py`側の起動時force/release（76.12）は無効化はせず
残してある（冗長だが無害、初回の立ち上がりを確実にする保険として）。

**教訓**: 冷起動時に一度だけ発生する不動点問題（force/releaseで解決可能）と、
値が変化する度に繰り返し発生する不動点問題は区別が必要——後者は
「スクリプト側の一時的な介入」では対応しきれない（特に相手側が駆動する
不定な遷移には介入しようがない）ため、**ネットリスト自体に恒久的な弱い
バイアス素子を追加する**という、SDAパッドで既に確立した手法を内部ノードにも
再利用するのが正しい対応。同じ問題パターンに気づいたら、都度スクリプトで
パッチするのではなく「同種の問題に前回使った解決策を一般化できないか」を
先に検討すべき。

### 76.15 BUFTHをBUF_X1へ置換（ユーザー明示指示）——76.14のweak biasも効果無しと実機確認

76.14のweak biasトランジスタ追加版`.sim`を実機で再実行したところ、
`irsim_debug_start.cmd`の結果は**biasなしの時と完全に一致**（同じタイムスタンプ・
同じノードが同じ値でX）——weak biasは全く効果が無かったことが実機で確認された。
これは、IRSIMのternaryソルバーが「弱いドライバ対強いドライバ」という比率解決を
行っているのではなく、**ゲートがXである限りそのトランジスタの導通状態自体が
不定として扱われ、強度に関係無く接続先ノードをXへ伝播させる**という、より
保守的なアルゴリズムであることを示唆する。76.14の仮説（弱いバイアスは強い
確定経路に負けるはず）は誤りだったと実機で反証された。

ユーザーからの明示指示「BUFTHをBUF_X1と同じにしてください」を受け、
`gen_irsim_sim.py`に`substitute_bufth_with_buf_x1()`を追加: パース直後に
`blocks["BUFTH"] = blocks["BUF_X1"]`とすることで、以降のflatten処理で
`BUFTH`型の全インスタンス（`sda_in`側`X$166`、`scl`側`X$311`）が
`BUF_X1`（ピンリスト`A vdd Y gnd`が偶然完全一致、帰還の無い単純な2段
インバータバッファ、4トランジスタ）として展開されるようにした。インスタンス
パス名（`XN3.XN166.*`/`XN3.XN311.*`）はそのまま——ノード名の対応表
（`irsim/README.md`）に変更は無い。実トランジスタ数は2082→2074
（BUFTH×2個分16石→BUF_X1×2個分8石、差分-8）。

これは本プロジェクトが当初掲げていた「ビヘイビア置換無し、全トランジスタを
機械的に展開する」という方針（§76.1）からの明示的な逸脱だが、BUFTHの
Schmitt-trigger特性（ノイズ耐性・ヒステリシス）はアナログ的な特性であり、
このチップレベルIRSIM検証の目的（write/readトランザクションのデジタル
プロトコル論理の正しさの検証）にとっては本質的でない。ユーザーの判断で
明示的に許可された、スコープの限定された例外。`gen_irsim_cmd.py`側の
BUFTH固有のforce/release処理（76.12関連）は全て削除——もう不要。

### 76.16 SDAプルアップをsda_oeゲート付きに変更、`h SDA`による強制も撤去（ユーザー指示）

さらにユーザー指示: 「SDA_OE:Lの時に、SDA:HにするPMOSをSDAに接続してWeakPは
削除してください」「SDAに接続するPMOSはWeakにして、そとからドライブする時に
Xに成らないようにしてください」。

`gen_irsim_sim.py`のSDAプルアップを変更: ゲートを`Gnd`固定（常時ON、
ドライバの状態に関わらず独立に効く）から**`sda_oe`(`N45`)自体**へ変更
（`p N45 Vdd N58 20 1`）。サイズ（L=20u W=1u）は従来通り弱いまま。これにより
`sda_oe=1`（ドライバがアクティブにLOWを駆動中）の間はプルアップPMOSは
確実にOFF（ゲート既知の1）、`sda_oe=0`の間は確実にON——ドライバとプルアップが
同時に「両方ともX」的な不定な競合状態になることが無くなり、より実回路に
近いkeeper型プルアップになった（IRSIMにとっても「弱いプルアップ対不定な
ドライバ」という比率判定を一切求められない設計に変わった）。

さらに、76.13（DFFRBのQS修正）と76.15（BUFTH→BUF_X1置換）が両方入った
現在、`sda_oe`自体は実は**SDAの値と無関係にAND優勢則だけで常に確定した0を
維持できる**ことに気づいた: `RSTB2($227)=AND2(busy, rst_n)`は、`rst_n=0`の間は
`busy`の値に関わらず0（AND優勢）、`rst_n`解除後も`busy=0`（まだSTART条件が
起きていない）間はやはり0のまま——つまり`sda_oe`のDFFRBは`SDA`が何であれ、
このブリングアップ期間中ずっと非同期クリアで強制的に0に保たれ続ける
（実際、修正済みの`irsim_reset_check.cmd`の実行結果でも`N45=0`がリセット
解除後も一貫して確定値だったことと整合する）。つまり76.11で見つけた
「循環依存」は、当時BUFTH/DFFRBの根本原因が未修正だったために`busy`自体が
誤ってXになっていたことの**副作用**であり、循環依存そのものが本質的に
避けられないものではなかった、と判明した。

これに基づき、`preamble()`/`gen_reset_check()`から`h SDA`（強制High）を撤去し、
単純に`x SDA`（解放、sda_oeゲート付きプルアップ任せ）に変更した。
`sda_oe`ゲートは上記の理由で常にブリングアップ中は確定0のため、プルアップは
Xを挟むことなくSDAを確定した1へ解決できるはず。次回`irsim_reset_check.cmd`の
実行結果で確認予定。

**追記（実機確認済み、ただし想定外の結果）**: 76.16適用後の`.sim`を実機で実行した
ところ、X（不定）は完全に無くなった（BUFTH置換・DFFRB修正・SDAプルアップ
ゲート化が揃って効いた）。しかし`N58`は`0`に収束——期待していた`1`ではなかった。

### 76.17 SDAパッド内部トレースで判明: プルアップのゲート極性が逆だった（HIZ/OUTとsda_oeの関係を誤解）

`N45`(sda_oe/HIZ13)を直接forceして`N58`の応答を実測したところ、`N45=0`でも
`N45=1`でも`N58=0`のままだった。パッド内部（`OSS_ESD_5V_DIO`、GIOインスタンス
`X$1`→DIOインスタンス`X$16`、ノードパス`XN1.XN16.*`）を全ノードdumpして確認:

- `N45=0`（`HIZ13=L`）: `N8=1, N9=1` → `N9`（PADをGndへ引くNMOSのゲート、
  `XM$36`）がON、PADを**能動的にLOW駆動**。
- `N45=1`（`HIZ13=H`）: `N8=1, N9=0` → 両方OFF、PADは**解放（Hi-Z）**。

ユーザーからの説明で、このパッドセル自体の仕様が判明: 「HIZ=Hで出力Hi-Z、
HIZ=LでGIO出力はOUTピンの極性に従う。SDAのOUTはL固定配線なので、HIZ=Lで
SDA=L、HIZ=Hはプルアップが無ければLのまま」——これは上記の実測と完全に
一致し、**パッドセル自体は正常**と確認された。

問題は76.16で追加したプルアップのゲート条件だった: `N45`に直結し
「`N45=0`でON」としていたが、`N45=0`はまさにパッド自身が能動的にLOW駆動
している瞬間であり、そこでプルアップもONにすると衝突する（無意味な）組み
合わせだった。RTL側の意図（`src/i2c_slave_async.v`70行目のポートコメント
「`1 = drive SDA low, 0 = release (Hi-Z)`」、`i2c_slave_async_model.py`、
テストベンチも同じ極性で一貫、専用エージェントで確認済み）は
「`sda_oe=1`でLOW駆動、`sda_oe=0`で解放」だが、パッドの`HIZ`ピンの実際の
意味（ユーザー確認）はこの逆（`HIZ=L`で駆動、`HIZ=H`で解放）——コアの
`sda_oe`出力とGIOの`HIZ13`入力の間にインバータは無く直結（トップレベルの
ネットリストで確認済み）なので、**ネット名`sda_oe`の値そのものはRTLの
命名意図とは逆センスでパッドのHIZピンに使われている**、というのが実態
（これがバグかどうかは別途要検討——ただし少なくともパッドセル自体は
正常動作しており、テストベンチの根本ロジック（SDA/SCLの直接force、`busy`等の
直接watch）はこの極性に依存していないため、実際のトランザクション試験で
プロトコルが正しく動くかどうかで実質的な影響を判断できる）。

**対策**: プルアップのゲートを`N45`ではなく**`XN1.XN16.N9`**（パッド自身の
「今まさに能動的にLOW駆動しているか」を表す信号、`XM$36`のゲート）に変更。
`N9=0`（解放中）でON、`N9=1`（駆動中）でOFF——パッドの実際の駆動状態と
構造的に必ず排他になるため、`sda_oe`が何を意味するかに関わらず衝突しない。
`.sim`を再生成（トランジスタ数は変わらず2075、ゲートのみ変更）。次回
`irsim_reset_check.cmd`で`N58`が確定した1に収束するか確認予定。

**追記（実機確認済み）**: プルアップのゲートを`XN1.XN16.N9`に変更した版を実機で
再実行したところ、`N58=0`のまま変化無し。`N45`はこのブリングアップ期間中
終始0（`sda_oe`のリセットデフォルト値）であり、76.17で確認した通り`N45=0`は
パッドを能動的にLOW駆動する状態なので、これはプルアップの問題ではなく
**パッド自体が終始能動的にSDAをLOW駆動し続けている**ことを意味する。
すなわちリセット直後・アイドル時に実チップがバスを能動的にLOWへ引き続ける
——正しいI2Cスレーブの振る舞い（アイドル/リセット時は解放し、外部プルアップで
Highにアイドルさせる）とは逆。

ユーザー判断: 「sda_oeとGIOの間にinvを入れて動作確認しましょう」。

### 76.18 sda_oe⇔HIZ13間へシミュレーション上のインバータを挿入（実配線ではなく検証目的）

`gen_irsim_sim.py`に`insert_sda_hiz_inverter()`を追加。単一ノード`N45`を
分割: コアのDFFRB（`X$318`）が`N45`を駆動する部分（`XN3.NI1204`→`N45`、
`Q`出力）はそのまま——「本当の`sda_oe`レジスタ値」として残す。一方
`OSS_ESD_5V_DIO`（SDAパッド、インスタンスパス`XN1.XN16`）内でHIZピンとして
`N45`をゲートに使っている4トランジスタ（`XM$1`/`XM$5`/`XM$10`/`XM$16`、
`.sim`内の正確な`(type,gate,source,drain,L,W,model)`シグネチャで一意に特定・
置換、実行時に個数を`assert`で検証）だけを新ノード`N45_HIZ_INV`へ繋ぎ替え、
`N45`→`N45_HIZ_INV`间に合成的な2トランジスタCMOSインバータを追加した。
これは実チップの配線を主張するものではなく、**「もしここにインバータが
入っていたら正しく動くか」を確認するための、シミュレーション限定の実験**。
`.sim`のトランジスタ数は2075→2077（+2）。次回`irsim_reset_check.cmd`の
実行結果で、これによって`N58`がリセット/アイドル時に正しく1（解放）へ
収束するか確認予定。仮に正しく動作すれば「コアの`sda_oe`出力とパッドの
`HIZ13`入力の間にインバータが1個欠落している」という具体的な実配線上の
指摘が実証されたことになる。

**追記（実機確認済み、仮説が実証された）**: インバータ挿入版を実機で実行した
ところ、`N58=1`（確定、解放・プルアップでHigh）、`N45=0`（`sda_oe`=解放、
RTL意図通り）、`busy`/`rw`/`addr_match`も全て確定0——**Xが完全に無く、かつ
SDAが正しくアイドルHighに収束**した。8000ns超のリセット保持中も、解除後
100ns時点でも安定して保持。これでBUFTH置換（76.15）・SDAプルアップの
ゲート化（76.16/76.17）・DFFRBのQS修正（76.13）・そして今回のsda_oe⇔HIZ13間
インバータ挿入（76.18）が揃い、チップレベルIRSIMのブリングアップ段階が
完全にクリーンな状態で収束することを実機で確認した。

これにより「コアの`sda_oe`出力とSDAパッドの`HIZ13`入力の間にインバータが
1個欠落している」という指摘が実証された形になる。実シリコンのままでは
リセット直後・アイドル時にSDAを能動的にLOW駆動し続けてしまい、I2Cスレーブ
として機能しない可能性が高い——これは合成/配置配線工程の見直しが必要な、
実設計上の指摘としてユーザーに申し送る。このIRSIM検証自体は今回の
インバータをあくまで「シミュレーション上の実験」として扱っており、
`.sim`ファイルにこの補正を含んだまま以降のプロトコル機能検証
（write/read トランザクション）を継続する（実配線の修正はこのIRSIM作業の
範囲外）。

### 76.19 DIS(P7)信号を見落としていた——ユーザー指摘で追加

チップ全体を通して、トップレベルの`$2`（`N2`）ノードに一切値を設定していなかった
ことが判明。GIOの結線を確認すると`$2`は実ピン`P7`であると同時に、未使用の
8個のパッド（`HIZ3/4/11/12/6/1/5/14`）のHIZ入力としても共有配線されている
重要な信号だった。ユーザーから: 「トップP7につながっているDIS信号がLの場合は、
READレジスタはWriteレジスタの内容が読まれます」——`DIS=L`だと読み出しデータ
レジスタが実際のRXシフトレジスタではなく直前のWriteデータをそのまま
ループバックするテスト/診断モードになる。通常動作には`DIS=H`が必須。

**対策**: `gen_irsim_cmd.py`に`DIS="N2"`を追加し、`preamble()`/
`gen_reset_check()`双方で`h(DIS)`を明示的に発行するよう修正、`.cmd`群を
再生成。

### 76.20 STOP後にbusyがクリアされない問題——DEL1のセトリング時間不足と判明

`irsim_test_main.cmd`の実機実行で、WRITEトランザクションのアドレス/データ
（0xA5）は完全に正しく送受信できた（`addr_match=1`/`rw=0`確認、ACK確認2回、
`rx_data`が0xA5と一致）ものの、STOP後（40ns後）に`busy`が0へクリアされず
`1`のまま、続くREADトランザクションで`rw`が1に更新されず、読み出しデータ
8ビット全て`1`（`tx_data=0x3C`と不一致）という結果になった。

`irsim_debug_stop.cmd`でSTOP後busyを100〜8000ns（合計約8000ns）追跡したが
一貫して`1`のまま——単純なセトリング時間不足ではないと判明。次に、STOPの
エッジ検出が`DEL1`（`N704=DEL1(N697)`、意図的な遅延素子、76.10/76.13で
既出）を経由することに着目し、`gen_irsim_cmd.py`の`stop()`がSDAを
1ステップ（20ns）しか低く保持せずに解放していた点を疑った。SDAを
2000ns低く保持してから解放する診断（`irsim_debug_stop_v2.cmd`）を実機で
実行したところ、**STOPエッジ直後（40ns後）に`busy=0`へ正しくクリアされ、
以降8000ns超安定して保持**——仮説が実証された。20nsでは`DEL1`の遅延伝播が
完了する前に立上りエッジが来てしまい、STOP検出回路がエッジを「取りこぼす」
ため、`busy`が永久にセットされたままになっていた。

**対策**: `CmdGen.stop()`にSDA低保持時間のパラメータ`sda_low_hold_ns`
（デフォルト2000ns）を追加し、`self.s()`（1ステップ）を
`self.s(sda_low_hold_ns)`に変更。`.cmd`群を再生成。

**教訓**: START条件は同じ短い（20ns）タイミングで一貫して正しく動作した
（`busy`が毎回正しくセットされた）ため、STARTとSTOPで検出回路の実装が
非対称（STOPだけが`DEL1`のような遅延素子を経由する）である可能性が高い。
プロトコルの対称な2つのエッジ（START/STOP）でも、内部実装のタイミング
要求は対称とは限らない——実チップの最小バスフリー時間要求として、この
`DEL1`遅延に相当する実時間（このシミュレーションでは2000ns、`.prm`が
プレースホルダのため絶対値はTR-1umの実特性を反映しない）をSTOP後に
確保する必要がある、という設計上の知見として記録。

**追記（実機確認済み、想定外の再発）**: 76.19/76.20を反映した`irsim_test_main.cmd`
を実機で再実行したところ、WRITEのアドレス/データ（0xA5）は引き続き完全に
正しく動作したが、**STOP後に`busy`が再びクリアされなかった**（同じ2000ns保持を
使っているにも関わらず）。`irsim_debug_stop_v2.cmd`（START直後、アドレス/データ
無しで即座に2000ns保持のみ）では成功したのに、実際のアドレス/データ送受信を
経由した完全なトランザクションでは同じ2000ns保持で失敗する、という違いが判明。

### 76.21 STOP前の直近のビット活動が`DEL1`の伝播に影響——settle猶予+保持時間延長

推定原因: `DEL1`は遅延線であり、その出力（`N704`）は`N697`の**過去の値**を
反映する。アドレス/データ送受信中は`N697`（`sda_in`のバッファ済み値）が
高速に何度もトグルしているため、STOPシーケンスに入った直後は`N704`がまだ
直前のビット群の遷移履歴を「引きずっている」状態である可能性が高い。
`irsim_debug_stop_v2.cmd`はSTART直後（`N697`が長時間安定して1だった状態）
から測定していたため2000nsで十分だったが、実際のトランザクションでは
STOP開始時点の`N697`の履歴が全く異なり、同じ2000nsでは不十分だった。

**対策**: `CmdGen.stop()`に、STOPシーケンス開始前の短いsettle猶予
（`settle_before_ns`、デフォルト500ns——直前のビットの残留伝播が収まるのを待つ）
を追加し、さらにSDA低保持時間（`sda_low_hold_ns`）のデフォルトを2000ns→
8000nsへ延長（実測`DEL1`遅延の絶対値が不明なため、以前の診断ノートで
言及した「8000ns超必要な可能性」を踏まえ安全マージンを取った）。`.cmd`群を
再生成。次回`irsim_test_main.cmd`の実行結果で、実際のトランザクションを経由
した状態からでもSTOP後に`busy`が正しくクリアされるか確認予定。

**教訓**: 遅延線（`DEL1`）を含む回路の妥当な待ち時間は、**その素子への入力が
どれだけ「静かだったか」の直近の履歴に依存する**——単発の孤立したテストで
確認した待ち時間が、実際の（直前に高速なビット活動があった）シナリオでは
不足することがある。診断は必ず実際の使用シナリオに近い文脈で行い、孤立
テストでの成功だけで十分とみなさないこと。

**追記（実機確認済み）**: settle猶予+保持時間延長版を実機で実行したところ、
WRITEトランザクションは完全に成功（アドレス/データ0xA5一致確認済み）、
かつ**STOP後に`busy`が正しく0へクリア**（t=13200）、続くREAD開始で
`busy=1`も正しく再セット（t=13300）——76.20/76.21のSTOP問題は解決した。

しかし新たな問題: READトランザクションのアドレス（0xA1）送信後、ACKチェック
（t=13980）で`N58=1`（NACK）、`rw`チェック（t=14000）も`0`のまま
（本来1のはず）——2回目のトランザクションのアドレス認識が完全に失敗した。

### 76.22 RTLソース確認: `phase`/`bit_cnt`/`shreg`レジスタバンクはSTARTのDEL1グリッチでのみリセットされる

RTLソース（`src/i2c_slave_async.v`）を専用エージェントで確認。`phase`/
`bit_cnt`/`shreg`/`addr_ok`/`rw_bit`は単一のレジスタバンクで、非同期
リセットは`rst_scl_domain = (~rst_n) | start_pulse`（約103行目）。
`start_pulse`は`scl & sda_d & ~sda_in`という**狭いコンビナショナル・
グリッチ**で、STOPの`stop_pulse`と同じ`DEL1`ベースのエッジ検出パス由来。

`busy`（単純なNOR2交差結合SRラッチ）はこの生エッジに即座に反応して自己保持
するが、`phase`/`bit_cnt`/`shreg`（本物のDFFRフロップ）は`DEL1`を通して
パルスが実際に伝播・整定してから初めてリセットされる。1回目のトランザクション
ではこの問題が隠れていた——`reset_hold_ns`（3000ns超）による`~rst_n`側の
リセットで、START前に既にレジスタバンクが初期化済みだったため。しかし
2回目以降のトランザクションでは`start_pulse`だけが頼りであり、修正前の
`start()`はSTARTエッジからわずか約60ns（`2*T`＋次ビットの`s()`）しか
待たずにアドレスビットのクロッキングを始めていた——STOPで見つけたのと
全く同じ`DEL1`整定不足パターン。結果: `phase`が前トランザクション終了時点
（`PH_DATA_WR`相当）のまま更新されず、`case(phase)`が`PH_ADDR`の分岐を
通らないため`addr_ok`/`rw_bit`が一切更新されない（NACK・rw固定という
観測結果と完全に整合）。

**対策**: `CmdGen.start()`に`settle_after_ns`パラメータ（デフォルト8000ns、
`stop()`と同じマージン）を追加し、STARTエッジ後・busy確認後・（該当すれば
$227グループforce/release後）にこの待ち時間を追加してからアドレスバイトの
クロッキングを開始するよう変更。1回目のトランザクションには本質的には
不要（既に`~rst_n`側で初期化済み）だが、無害なので一律で適用。`.cmd`群を
再生成。次回`irsim_test_main.cmd`で2回目のトランザクション（READ）の
アドレス認識が正しく動くか確認予定。

**教訓**: 単一の非同期リセット信号（`rst_scl_domain`）が複数の異なる性質の
記憶素子（エッジ駆動で自己保持するSRラッチ vs. 実際のクロックドフロップ）を
同時に駆動する設計では、片方（SRラッチ）が正しく動いているからといって
もう片方（フロップ）も正しくリセットされているとは限らない——特に
リセットパルス自体が遅延素子由来の狭いグリッチである場合、フロップ側は
パルス幅に対して敏感になりうる。`busy`の見た目の正しさだけでSTART/STOPの
リセット効果全体を判断しないこと。

**追記（実機確認済み、想定外）**: `start()`に`settle_after_ns=8000`を
追加した版を実機で再実行したが、READトランザクションのアドレス認識は
**依然として失敗**（NACK、`rw`固定0）——76.22の対策だけでは不十分だった。

### 76.23 `phase`/`bit_cnt`が丸ごと無反応と判明——`$813`（START検出パルス）が`DEL1`の非対称伝播により立ち上がらない

`gen_irsim_debug_phase.py`で、`$28`グループの未特定7個のDFFRB出力
（`phase[2:0]`+`bit_cnt[3:0]`と推定、`XN3.N43/N11/N62/N243/N358/N137/N349`）
を2回目のSTART直後から8000ns settle期間中、さらにアドレスビットのクロッキング
中まで連続監視。結果: **8000ns待っても7ビットは1つも変化しない**——単なる
「遅いがいずれ変わる」ではなく、完全に無反応。アドレスビットのクロッキングが
始まって初めて値が動き出すが、その開始値は前トランザクション終了時点の
（誤った）残留値のままだった。

`gen_irsim_debug_rstb.py`で、実際に`$28`(RSTB)を駆動するゲートレベルの信号
（`$813=NOR2($697,$703)`、`$703=NAND2($666,$704)`、`$704=DEL1($697)`、
`$28=NOR2($813,$702)`）を2回目のSTARTエッジ周辺、20ns刻み（IRSIMの1ステップ
サイズ）×25ステップ（500ns分）で監視。結果: **`$813`は監視期間中ずっと0の
まま**——一度もパルスしない。同時に`$704`（`DEL1($697)`の出力）を見ると、
`$697`が立ち下がった直後、ほぼ1ステップ以内（<20ns）で`$704`も追従して
立ち下がっていた。

これは76.20/76.21で確認した`DEL1`の挙動と矛盾するように見える。76.20/76.21
では「STOP後の立ち下がり(SDA low)に対してDEL1の出力が2000～8000ns遅れて
追従する」という**遅い**挙動を前提に対策していたが、`$697`の立ち下がりに
対する`$704`の追従はほぼ瞬時——`DEL1`の伝播が**非対称**（立ち下がりは速い、
立ち上がりは遅い）であることが直接確認された。

`$813=NOR2($697,$703)`が1になる（＝RSTBがアサートされる）ためには、
`$697=0`かつ`$704`（→`$703`経由）がまだ`$697`の旧い値（1）を保持している、
という**一時的な不一致の窓**が必要。しかし`DEL1`の立ち下がりがほぼ瞬時である
以上、`$697`が0になった瞬間にはもう`$704`も0に落ちてしまっており、この窓が
実質的に開かない。一方`busy`のSETパスは`$695=NAND2(rst_n,$696)`、
`$696=NAND3($817,$666,$697)`、`$817=INV($704)`という**別系統**の論理式で、
こちらは正しく毎回STARTで発火することを確認済み（`busy`自体は正しく1に
セットされる）——同じ`DEL1`出力を使っていても、後段の組み合わせ論理の形が
違うため、片方だけが構造的に機能しなくなっている。

これはSTOPで見つけた「セトリング時間不足」とは性質が異なる問題で、
時間をいくら延ばしても解決しない——`$813`は`DEL1`が正しく追従して「いる」
ことそのものによって発火できていない（`.prm`の立ち上がり/立ち下がり時定数の
非対称性、またはこの特定の`DEL1`インスタンスの実際の回路構造そのものに
起因する可能性がある）。

この結果をユーザーに提示し、実チップのバグか、プレースホルダ`.prm`由来の
アーティファクトかの切り分けが必要と判断。ユーザーが`DEL1`インスタンス
（`XN312`）自体のトランジスタレベル回路を直接確認し、「問題なさそうです」
と報告。次のステップとして、`DEL1`を`BUF_X1`2段カスケードに置き換えた
シミュレーション専用の代替回路で同じテストを行い、非対称性が本物の回路
特性なのか、それとも今回のプレースホルダ`.prm`による時定数の産物なのかを
切り分けることになった（76.24）。

### 76.24 `DEL1`を`BUF_X1`2段カスケードに置換して非対称性を切り分け（診断目的、実配線ではない）

`gen_irsim_sim.py`に`substitute_del1_with_buf_x1_cascade(blocks)`を追加し、
`blocks["DEL1"]`をシミュレーション専用の合成サブサーキットで上書き：

```
.SUBCKT DEL1 A vdd Y gnd   (置換後の中身)
X$1 A vdd $MID gnd BUF_X1
X$2 $MID vdd Y gnd BUF_X1
.ENDS DEL1
```

`BUF_X1`のピン列（`A vdd Y gnd`）が`DEL1`と一致しているため、そのまま
2段カスケードとして流用できる。`BUF_X1`は全トランジスタ`L=1u`の対称的な
標準セルであり、`DEL1`本来の`XM$1`（`L=2u`の長チャネルPMOS、非対称遅延の
原因と推定される素子）を含まない。`BUFTH→BUF_X1`置換（76.15）とは異なり、
今回は単一セルへのエイリアスではなく2段構成にしている——目的は遅延そのものを
除去することではなく、対称的な伝播で`$813`が正しく発火するかを検証すること。

再生成後の`.sim`を`grep`で構造確認：唯一の`DEL1`インスタンス（`XN3.XN312`、
`$697→$704`のもの、76.23で追跡していたのと同一インスタンス）が、期待通り
2つの`BUF_X1`インスタンス（`XN312.XN1`, `XN312.XN2`）のカスケードとして
展開されており、中間ノードは`XN3.XN312.NMID`として一意化されていた：

```
p  XN3.N697            Vdd  XN3.XN312.XN1.N2   (BUF_X1 #1, 1段目インバータ)
p  XN3.XN312.XN1.N2    XN3.XN312.NMID  Vdd     (BUF_X1 #1, 2段目インバータ→NMID)
p  XN3.XN312.NMID      Vdd  XN3.XN312.XN2.N2   (BUF_X1 #2, 1段目インバータ)
p  XN3.XN312.XN2.N2    XN3.N704        Vdd     (BUF_X1 #2, 2段目インバータ→N704)
```

トランジスタ総数は置換前後で完全に一致（2074個のまま）——元の`DEL1`が
ちょうど8個のトランジスタ（`XM$1`～`XM$10`のうち実質8個、L=2u品含む）で
構成されていたのに対し、`BUF_X1`×2＝4個×2＝8個で偶然一致したため。
DEL1インスタンスはこの1箇所のみ（チップ全体で`DEL1`は1インスタンスしか
無い）ことも確認済み。

**次のステップ**（ユーザーへ依頼予定）: `irsim_debug_rstb.cmd`を実機で
再実行し、`$813`が2回目のSTARTエッジで正しくパルスするかを直接確認。
成功すれば、`irsim_test_main.cmd`をフルで再実行し、READトランザクションの
アドレス認識・データ0x3C読み出しが正しく動作するかを確認する。

**追記（実機確認済み、結果：非対称性はDEL1固有ではなかった）**: `BUF_X1`
2段カスケード版で`irsim_debug_rstb.cmd`を実機再実行。結果、`$813`は
依然として2回目のSTARTエッジ周辺で**一度もパルスしなかった**
（監視期間中ずっと`$813=0`）。さらに重要な観測: `$697`が立ち下がる
瞬間（t=21240ns）、`$704`（＝カスケード出力）も**同一タイムステップで
同時に**0へ落ちていた——`DEL1`本来の非対称遅延（立ち下がり瞬時／立ち上がり
遅い）を排除した対称的な`BUF_X1`×2構成でも、結果は元の`DEL1`と全く同じ
（立ち下がりが即座に追従）だった。

これは76.23で立てた仮説（「`DEL1`固有の非対称遅延特性が原因」）を**否定する**
結果である。実際には、置換前後どちらの遅延素子を使っても、この特定の遷移
（`$697`の立ち下がり）に対して`$704`が全く遅延なく追従してしまう——つまり
問題は`DEL1`の回路特性ではなく、**IRSIMのターナリ・スイッチレベルモデルが
そもそもこの種の組み合わせ論理カスコードに対して有意な伝播遅延を再現して
いない**ことに起因すると考えられる。`$813=NOR2($697,$703)`が発火するために
必要な「$697だけが先に変化し、$704がまだ追従していない」という一時的な窓は、
実シリコンではアナログRC遅延によって（`.prm`の値がプレースホルダとはいえ）
存在しうるが、IRSIMの離散ステップ・ターナリソルバでは同一ステップ内で
組み合わせ論理コーンが即座に整定してしまうため、この種のグリッチベースの
エッジ検出回路はIRSIMレベルのシミュレーションでは原理的に観測できない
可能性が高い。

**教訓**: グリッチ／狭いコンビナショナルパルスに依存する回路（本来アナログ
RC遅延の存在を前提とした設計）は、スイッチレベル・ターナリシミュレータ
（IRSIM）では再現できないことがある——遅延素子を差し替えても結果が変わら
なかったことがその証拠。これはシミュレータのモデル粒度の限界であり、
実チップの設計バグを意味しない。`DEL1`は置換前（実回路）に戻すのが妥当
（テスト目的の代替は役目を終えた）。今後は`$813`/`$28`経路を実際に発火
させることをIRSIM上で目指すのではなく、`busy`が正しくSTARTを検出している
という事実（`$695`/`$696`経路で確認済み）を使い、シミュレーション専用の
合成的な手当て（例: `busy`が新規にSETされた直後に`$28`(RSTB)を短時間
force/releaseし、`phase`/`bit_cnt`レジスタバンクだけを強制的にリセットする）
を`.cmd`側に追加する方向で、後続のREADトランザクション検証を継続する方針を
ユーザーに提案する。

**実装・実機確認**: `gen_irsim_sim.py`の`substitute_del1_with_buf_x1_cascade()`
呼び出しをコメントアウトして無効化し、`.sim`を再生成——`grep`でXN3.XN312が
単一インスタンス構成（L=2u品を含む本来の8トランジスタ）に戻ったことを確認
（トランジスタ総数2074、インスタンス数288→286、階層深さ3→2）。`gen_irsim_cmd.py`
の`start()`から`settle_after_ns`待機を削除し、`busy`確認直後に新設した定数
`RSTB28="XN3.N28"`へ`force_release([RSTB28], value=0)`を適用するよう変更
（`$28`グループの24個のDFFRB全て——`rw`/`addr_match`自身の記憶素子も含む——を
一括で強制的にリセットする、シミュレーション専用の手当て）。全`.cmd`を再生成し、
ユーザーへ提示・再テスト依頼済み（次の実行結果待ち）。

その後ユーザーから3点の指示・訂正:

1. 「`DEL1`は実回路に戻してください」——上記の通り実施済み。
2. 「READレジスタは、Writeレジスタが接続されたGIOの出力につながっています。
   DIS:Lだと、WriteしてからREADすれば、Writeしたデータが読み出せるはずです。」
3. 「DIS:Lだと、GIOの初期値を読み出すはずです。」

### 76.25 DIS/GIOパッドの正確な構造をネットリストから再確認——76.19の理解を訂正・精緻化

ユーザーの指摘を受け、`src/tr_1um_i2c_slave_async.extracted`内の実際に使われている
`OSS_FRAME_GIO`サブサーキット定義（619行目、`src/OSS_FRAME_GIO.extracted`という
別ファイルにある同名だが**別物**の汎用定義ではなく、こちらがチップ実体で使われて
いる本物）のフォーマル・ピン列と、トップレベルのインスタンス実引数を位置的に
突き合わせて再検証した。

判明した構造（8個のパッド、`P1,P3,P4,P5,P6,P11,P12,P14`）:

| pad | P (bond pad node) | OUT (内部→パッド駆動) | HIZ |
|---|---|---|---|
| P1  | `$103` = `tx_data`bit5 | `$78` = `rx_data`bit5 | `$2`(DIS) |
| P3  | `$72`  = `tx_data`bit6 | `$83` = `rx_data`bit6 | `$2`(DIS) |
| P4  | `$57`  = `tx_data`bit4 | `$42` = `rx_data`bit4 | `$2`(DIS) |
| P5  | `$21`  = `tx_data`bit2 | `$34` = `rx_data`bit2 | `$2`(DIS) |
| P6  | `$22`  = `tx_data`bit3 | `$13` = `rx_data`bit3 | `$2`(DIS) |
| P11 | `$19`  = `tx_data`bit1 | `$15` = `rx_data`bit1 | `$2`(DIS) |
| P12 | `$26`  = `tx_data`bit0 | `$32` = `rx_data`bit0 | `$2`(DIS) |
| P14 | `$68`  = `tx_data`bit7 | `$64` = `rx_data`bit7 | `$2`(DIS) |

すなわち、これまで「`tx_data`レジスタ」と呼んでいたTXノード群（`gen_irsim_cmd.py`の
`TX`定数）は**内部レジスタではなく、これら8個の外部チップピンの物理パッドノード
そのもの**——I2Cコアが読み出す「送信データ」は、外部ピンの電圧を組み合わせ的に
直接サンプルしているだけで、フリップフロップ経由ではない。一方`RX`ノード群
（実際のDFFRレジスタ、I2C WRITEで受信した値を保持）は、これら同じ8パッドの
`OUT`（内部駆動）側に配線されている——つまり**同一パッドが入力(P/tx_data)と
出力(OUT/rx_data)の両方の意味を持つ、真の双方向GIO**であり、`HIZ`（＝`DIS`共通）
がどちらのモードで使うかを切り替える:

- `DIS=H`（現在の`preamble()`のデフォルト、Hi-Z）: パッド自身の内部ドライバは
  無効化——外部から自由に電圧を強制でき、I2Cコアはその値を`tx_data`として読む。
  `irsim_test_main.cmd`の「`DATA_RD=0x3C`をTXピンにpreload」ステップはまさに
  これ（外部ピンを直接force）。
- `DIS=L`: パッド自身の内部ドライバが有効化——`rx_data`（直近のI2C WRITE受信値）
  を同じパッドへ能動的に駆動する。この状態で外部からTXピンをforceすると
  **チップ自身の出力と衝突（contention）する**ため、DIS=Lのときは絶対にTXピンを
  外部forceしてはならない。

この構造により、ユーザーが説明した2つの動作がそのまま導出される: (1) DIS=Lで
WRITE→READを行うと、WRITEで`rx_data`に入った値が同じパッド経由で即座に`tx_data`
としても見える（ハードウェア的なループバック、レジスタ間の内部マルチプレクサでは
なく、単なるパッド共有による物理的エコー）ため、READで書き込んだ値がそのまま
返る。(2) 事前にWRITEを一度も行っていない状態でDIS=Lにすると、`rx_data`の
リセット直後の初期値（DFFRBのRSTB既定値、実質0）がそのまま読み出される。

これは76.19で立てた「DISはコア内部の読み書きレジスタ間ループバック・マルチプレクサ
の選択信号」という理解を訂正するものではなく、**メカニズムのレベルを一段具体化した
もの**——結果として観測される動作（DIS=Lで書いた値が読める）は同じだが、実体は
コア内部のマルチプレクサではなく、パッドレベルでの`tx_data`(=外部ピン直読み)と
`rx_data`(=同一ピンへの能動駆動)の共有である。

**現行`irsim_test_main.cmd`への影響**: このテストはWRITE=0xA5・READ=0x3Cという
**意図的に異なる値**を使う独立検証（`script/test_i2c_slave_async.py`と同じ設計）
であり、これには`DIS=H`＋TXピンの外部force（現状の実装）が正しい。`DIS=L`
ループバックモードは、今回のテストの代わりではなく、将来的な別の検証手段
（WRITE直後に同じ値がREADできることを確認する、より単純なテスト）として有用。
現状は`DIS=H`のまま、`RSTB28`のforce/release修正を反映した`irsim_test_main.cmd`
の実機再実行を優先する。

### 76.26 `RSTB28`直接force/releaseは失敗——QM/QS未更新のまま`RSTB`が解放され旧値に戻る

実機で`irsim_test_main.cmd`（`RSTB28`直接force/release版）を再実行。WRITEは
完全に成功（アドレスACK・`addr_match=1`/`rw=0`・データ0xA5一致・STOP後
`busy`クリア、すべて確認済み）。2回目のSTARTでも`busy=1`は正しく再セット
されたが、**その直後の`RW`/`ADDR_MATCH`チェック（`force_release([RSTB28])`
完了直後）で`addr_match`が既に`1`（前トランザクションの残留値）のまま**——
リセットされていなかった。結果、READのアドレス（0xA1）は今回も完全にNACK、
`rw`も`0`のまま。

原因: 76.13で確立した通り、`RSTB`（`$28`）はDFFRBの`Q`/`QB`を**アサートされて
いる間だけ**直接force-driveする——内部の`QM`/`QS`記憶ノードには一切触れない。
`force_release([RSTB28])`は`N28`を1ステップサイズ（20ns）だけ`l`で強制し、
即座に`x`で解放する。この短い間`Q`は確かに0へ強制されるが、`RSTB`が解放された
瞬間、`Q`は`QM`/`QS`（トランスミッションゲート経由）が保持している値へ**すぐに
戻ってしまう**——そして`QM`/`QS`はクロックエッジでD入力から新たにロードされない
限り更新されない。これは76.13のコールドスタート時デッドロックと**全く同じ
失敗モード**が、電源投入直後ではなく実行中盤で再発しただけだった。

**対策**: `RSTB28`を直接触るのをやめ、`preamble()`のコールドスタート対策と
全く同じ手法——`$28`グループ24インスタンス**それぞれ自身の`QS`ノード**を
直接force/release——を`start()`内でも適用するよう変更（`RSTB`経由を完全に
バイパスするので、`RSTB`が実際にパルスするかどうかに一切依存しない）。
`rw`/`addr_match`自身のレジスタもこの24インスタンスに含まれるため、同時に
正しくクリアされることを期待。`.cmd`群を再生成し、ユーザーへ提示・再テスト
依頼済み。

**教訓**: DFFRBの`RSTB`ピンを介した force/release は、`RSTB`ネット自体を
一時的に外部強制しても、内部`QM`/`QS`が更新されない限り**恒久的な効果を
持たない**——リセット対策は必ず各フロップの`QS`ノードに対して行うこと。
`RSTB`ネットへの直接force/releaseは、リセットが正しく機能しているかを
「見る」診断（`irsim_debug_rstb.cmd`など）には有用だが、リセットの代替
手当てとしては機能しない。

### 76.27 実機確認: WRITE(0xA5)→READ(0x3C) フルトランザクションが完全動作

`start()`の`$28`グループQS force/release版で`irsim_test_main.cmd`を実機
再実行。結果、**WRITE・READ双方が完全に成功**:

- WRITE: アドレスACK（t=3980、`N58=0`）、`addr_match=1`/`rw=0`（t=4000、
  一致）、データACK（t=4680）、`rx_data`が`RX`ノード（`N32,N15,N34,N13,
  N42,N78,N83,N64`＝bit0..bit7）から`10100101`＝`0xA5`と正確に再構成
  （`DATA_WR`と完全一致）、STOP後`busy`が正しく0へクリア（t=13240）。
- READ: 2回目のSTARTで`busy=1`再セット（t=13340）、直後の`$28`グループ
  QS force/releaseで**`addr_match=0`/`rw=0`に正しくリセット**（t=13380、
  76.26の失敗時と違い今回は残留値ではなくクリーンな0）、アドレス
  （0xA1）送信後にACK（t=14060、`N58=0`）、`rw=1`に正しく更新
  （t=14080）——76.22以来ずっと失敗していた2回目トランザクションの
  アドレス認識が初めて完全に成功。続くデータ8ビットのSDAサンプル
  （t=14140,14220,14300,14380,14460,14540,14620,14700の`N58`値:
  `0,0,1,1,1,1,0,0`、MSB→LSB）を再構成すると`00111100`＝`0x3C`——
  `DATA_RD`と完全一致。マスターNACK・STOP後`busy`が正しく0へクリア
  （t=23340）。

これで`script/test_i2c_slave_async.py`が検証しているのと同じシナリオ
（WRITE 0xA5、続けてREAD 0x3C）が、チップレベルのゲートレベル・
トランジスタレベルネットリスト上でIRSIMにより**完全にエンドツーエンドで
確認された**。76.10（BUFTH冷起動デッドロック）から始まったこのセッション
の一連の調査・修正（BUFTH→BUF_X1置換、DFFRB QS force/release手法の確立と
汎用化、SDAパッドHIZ極性・プルアップゲート修正、sda_oe⇔HIZ13間インバータ
挿入、DIS信号の制約追加、STOP/STARTの`DEL1`セトリング対策、そして今回の
`$28`グループQS force/releaseの`start()`への適用）が、すべて組み合わさって
初めて成立した結果である。

**次のステップ**: `irsim_test_negative.cmd`（誤アドレスNACKテスト、今回の
セッションでは未実行）の実機確認、および`irsim/README.md`のセッション
全体を反映した更新。

### 76.28 実機確認: `irsim_test_negative.cmd`（誤アドレスNACK）も完全成功

`irsim_test_negative.cmd`（誤アドレス0x11宛のWRITE、`ADDR+W=0x22`）を実機で
実行。結果:

- アドレス送信後（t=3980）: `N58=1`（SDAがHIGHのまま＝スレーブが応答して
  いない、正しいNACK）。
- 同時刻: `XN3.addr_match=0`（一致していないアドレスに対して正しく
  非アサート）。
- STOP後（t=12540）: `XN3.busy=0`（正しくクリア）。

Xは一切現れず、期待通りの挙動を一発で確認。76.27のWRITE/READ本線テストに
続き、ネガティブテストも実機で完全にパスした。

これにより、このセッションで対象とした3本の`.cmd`スクリプト
（`irsim_reset_check.cmd`/`irsim_test_main.cmd`/`irsim_test_negative.cmd`）
すべてが実機IRSIM実行で確認済みとなり、`script/test_i2c_slave_async.py`と
`script/test_i2c_slave_async_negative.py`（MyHDL機能検証）が検証している
シナリオが、チップレベルのゲートレベル・トランジスタレベルネットリスト上でも
再現されていることが確認できた。

### 76.29 プレースホルダ`.prm`から実TR-1umモデルベースの`TR-1um.prm`生成スクリプトを整備

これまで全ての実行で`scmos100.prm`（IRSIMインストール先の一般的な2umプレー
スホルダ、TR-1umの実特性を一切反映しない）を流用してきた。ユーザーからロー
カル環境にngspiceがあるとの申し出があり、実TR-1umプロセスの実測フィット済み
SPICEモデル（`~/Dropbox/91_OpenPDK/TR-1um/libs.tech/spice/models/ip62_models`
経由で`.include`される`models_IP62_{mos,cap,res,diode}_v*.lib`、ユーザー指定
のパスを`request_cowork_directory`で新規に接続）を使い、本物の`TR-1um.prm`
を生成するスクリプトの作成を依頼された。

**方式の出典**: IRSIM自身の公式キャリブレーション手順（irsimソースツリー
`lib/calibrate_spice3/{README,ckt.spi,findr.c}`、
https://github.com/RTimothyEdwards/irsim ）をこのセッション中にGitHub上で
直接取得・精読し、そのままの測定回路・遅延抽出式をngspice用に移植した——
独自に考案した手法ではなく、IRSIM開発者自身が配布している標準手順。あわせて
`irsim/base/{sim.c,config.c}`のソースも読み、この プロジェクトの`.sim`
（MITフォーマット、`N`/`M`/面積・周囲長注釈なし）に対して`.prm`のどのパラ
メータが実際に効くか（`capga`のみが常に有効、`capda`/`cappda`等の拡散容量は
`diffext`を設定して`TDIFFCAP`機構を有効化しない限り一切加算されないこと、
配線層容量`capm2a`等はこのフローでは常に無効であること）を正確に確認した。

**実測データの確認**（`models_IP62_mos_v2.lib`を直接読み取り）: 実測フィット
済みBSIM3v3（level=49）のPMOS_mst/NMOS_mst（`tox=1.95e-8m`共通、`cj`は
PMOS=1.02e-3, NMOS=7.44e-4）、拡張ドレイン版MPE/MNE（I/Oパッド用、
`tr_1um_i2c_slave_async.extracted`の"NMOSE"30個に対応）を確認。既存の
プロジェクト内テストベンチ（`simulations/BUFTH_X1_AC.spice`,
`simulations/IO_DC.spice`等、複数ファイルで一致）から実際の電源電圧
`vdd=5.0V`を確認済み。

**`script/gen_prm_characterize.py`**: IRSIM公式`ckt.spi`と同じ4種の測定
サブ回路（out1: 自己駆動インバータ、out2: out1駆動の2段目インバータ＝
現実的な鈍ったエッジでの遅延測定用、out3: NFET単体のプルアップ試験＝
n-channel dynamic-high、out4: PFET単体のプルダウン試験＝p-channel
dynamic-low）を、実TR-1umの`PMOS`/`NMOS`ラッパーサブサーキット・実際の
`.include`パス・`vdd=5.0`で構成したngspiceデッキを生成。基準サイズは
`PMOS w=10.2u`/`NMOS w=3.4u`——`INV_X1`の単位インバータと`DEL1`自身の
L=2u長チャネル素子の幅の両方に一致する値を選定（設計中で実際に使われて
いる2点、L=1u・L=2uの特性化で`.prm`の抵抗テーブル補間に十分なカバレッジ
が得られる）。`.control`ブロックで`set filetype=ascii`＋`write ...raw`
を使い、ngspiceのASCII rawファイル（spice3互換）を出力する構成。

**`script/build_tr1um_prm.py`**: (1) `findr.c`をPythonへ移植し、ngspiceの
ASCII rawファイルを解析、`out1`/`out2`の遅延比較によりエッジレート依存性を
除去した"static"抵抗値を含む`resistance n-channel/p-channel dynamic-high/
dynamic-low/static ...`テーブルをIRSIM公式の式通りに算出。(2) `capga`は
`models_IP62_mos_v2.lib`の`tox`から直接`Cox/Tox`（IRSIM公式READMEの式）で
計算（シミュレーション不要）。(3) `capda`/`cappda`は同モデルの`cj`をそのまま
流用（F/m²とpF/um²は数値的に完全に一致するため変換不要）。(4) `diffext`は
モデル自身の`sdwidth=2.8u`（AS/AD計算に実際に使われている拡散延長幅の仮定
そのもの）をそのまま流用——恣意的な値ではない。(5) `capdp`/`cappdp`
（側壁/周囲長方向の接合容量、`cjsw`相当）は、入手できたモデルダンプに
`cjsw`が含まれていなかったため0のまま（面積のみの近似、スクリプト出力に
明示的に警告表示）。(6) `lambda 1.0`（この`.sim`は既にlambda=1u換算の実
マイクロン値を使用——実行時の`lambda:1.00u`表示で確認済み）。

このセッション中に両スクリプトの非ngspice依存部分（モデルファイルの
`tox`/`cj`抽出、ASCII rawパーサ）を実際にテスト実行し、正しく動作すること
を確認済み（`capga=0.0017708 pF/um²`など、手計算と一致）。ngspice自体は
この作業環境では実行できないため、`gen_prm_characterize.py`が生成した
`irsim/prm_char_L1.spi`/`prm_char_L2.spi`をユーザーがローカルで
`ngspice -b`実行し、`build_tr1um_prm.py`で最終的な`irsim/TR-1um.prm`を
組み立てる、という2段階のワークフロー。

**実機確認（エラー、修正済み）**: ユーザーが実機で`ngspice -b prm_char_L1.spi`
を実行したところ、`Fatal error: instance vgnd is a shorted VSRC`で
シミュレーションが1つも走らず失敗。原因: ngspiceは`GND`という名前のノードを
（`0`と同様に）予約済みのグラウンド・エイリアスとして特別扱いするため、
`VGND GND 0 DC 0`が実質「同一ノード同士を結ぶ電源」＝ショートと判定された。
このプロジェクト自身の既存テストベンチ（`simulations/IO_DC.spice`等）が
一貫して`GND`ではなく`VSS`をローカルグラウンドのノード名に使っている理由も
これだったと判明。**対策**: `gen_prm_characterize.py`の全ノード名を
`GND`→`VSS`へ変更（`VVSS VSS 0 DC 0`）し、`.spi`を再生成。ユーザーへ再提示
済み、実行結果待ち。

**実機確認（2件目のエラー、修正済み）**: 修正版で`ngspice -b prm_char_L1.spi`
は正常終了、`irsim/prm_char_L1.raw`が生成されたが、続く
`python3 build_tr1um_prm.py`が`IndexError: list index out of range`で
クラッシュ。ユーザーに`head -n 40 irsim/prm_char_L1.raw`の実際の出力を
貼ってもらい確認したところ、`build_tr1um_prm.py`の`read_ascii_raw()`が
前提としていた「Values:セクションは1行1トークン（インデックス行だけ
`インデックス 値`の2トークン、残りは値1個ずつ改行）」という想定が誤りだった
と判明——実際のngspice ASCII rawは行の折り返し位置を厳密に保証しない
（`No. Variables: 15`——`time`本体8個に加え`i(vin1)`等の電流変数7個も自動的に
含まれることも確認）。**対策**: 行単位のパースをやめ、`Values:`セクション
以降を丸ごと空白区切りでトークン化し、「各点＝[インデックス, 値0, 値1, ...,
値(nvars-1)]」というフラットなトークン列として順に消費する方式に書き換え
（`read_ascii_raw()`）。期待トークン数（`npoints*(nvars+1)`）との一致検証も
追加。

**実機確認（成功）**: ユーザーが`prm_char_L2.spi`も実行済みの状態
（`prm_char_L1.raw`/`prm_char_L2.raw`とも約2.8MBで存在確認）で
`build_tr1um_prm.py`を再実行したところ、正常終了。結果:

```
capga=0.001771 pF/um^2, capda=0.000744 pF/um^2, cappda=0.001020 pF/um^2
L=1u: n-channel static=5711 ohm, p-channel static=3650 ohm
L=2u: n-channel static=9658 ohm, p-channel static=6825 ohm
```

L=2uがL=1uの約1.7～1.9倍（純粋な線形スケーリングなら2倍——L=1uでの
短チャネル効果（速度飽和等）により、L=1u側が相対的に「効きが良い」ぶん
小さい、という物理的に妥当な傾向）。PMOS/NMOSの抵抗比も、実測モデルの
`u0`（移動度、NMOS=0.0668 > PMOS=0.0197、比約3.4倍）と基準幅比（PMOS
10.2u : NMOS 3.4u、比3倍）を踏まえると素朴な単純計算とは多少ずれるが、
BSIM3の二次効果（`rdsw`等の寄生抵抗、`vsat`速度飽和、高Vgs=5Vでの移動度
劣化など）を考えれば十分説明可能な範囲内——このためにハンド計算ではなく
実ngspice特性化を行っている。`irsim/TR-1um.prm`を実際に生成・保存済み。

**次のステップ**: `irsim TR-1um.prm tr_1um_i2c_slave_async.sim`で
`irsim_reset_check.cmd`/`irsim_test_main.cmd`/`irsim_test_negative.cmd`を
再実行し、プレースホルダ`scmos100.prm`との結果の違い（特に`DEL1`の
非対称遅延・STOP/START直後のセトリング時間まわり）を確認する。ただし
76.23/76.24で確認済みの通り、`$813`（START検出パルス）がIRSIMのターナリ・
ソルバの原理的な限界により発火しない問題は`.prm`の値を変えても解決しない
はずなので、既存の`QS`force/release方式のワークアラウンド自体は実TR-1um.prm
に切り替えても引き続き必要と予想される。`irsim/README.md`も更新予定
（未反映）。

### 76.30 実TR-1um.prmへの切り替えで新たな回帰: `rw`/`addr_match`がQS force/release後もX

ユーザーが`irsim TR-1um.prm tr_1um_i2c_slave_async.sim`で
`irsim_reset_check.cmd`を実行、結果:

- t=100～8000ns（リセットアサート中）: `N58`は最初X→t=3000ns以降`1`に収束、
  `N45`(sda_oe)/`busy`/`rw`/`addr_match`は**t=100nsの時点で既に**確定した
  `0`——`scmos100.prm`のときより早く、かつQS force/release無しで自然収束
  している点が興味深い（実RC定数が速いことを示唆）。
- `rst_n`解放後、`$28`グループ24インスタンスのQS force/release実行
  （`preamble()`と同じ手順、force→1ステップ→release→5ステップ分＝計120ns
  のマージン）——**この直後の確認（t=8160ns）で`rw`/`addr_match`がX**。
  `scmos100.prm`では全く同じ手順・同じマージンで問題なく`0`に収束していた
  ため、これは実`.prm`への切り替えによる新規の回帰。

`busy`/`sda_oe`はQS force/releaseなしで自然収束したのに対し、`rw`/
`addr_match`はforce/releaseを経てもXのまま、という非対称な結果から、単純な
「セトリング時間不足」（RC定数から見て考えにくい——静的抵抗は数kΩ、
ゲート容量は`capga`から見て数fF単位で、素直なRC積は20nsのIRSIMステップ
サイズよりずっと小さいはず）というより、DFFRBの`QS`→`QB`→`Q`という内部
伝播チェーンのどこかで、実`.prm`の抵抗比（プレースホルダとは絶対値も相対比
も異なる）によって以前は暗黙に成立していた強さの前提が崩れている可能性を
疑っている——たとえばQSを保持するトランスミッションゲート・ラッチ構造の
keeperと外部forceの綱引きなど。

**次のステップ**: `rw`自身のDFFRB（インスタンス`XN2`、76.13で特定済み）の
`QS`/`QB`(`N4`)/`Q`(`rw`)を、force/release前後で1ステップサイズ刻みの細粒度
で追跡する`irsim_debug_dffrb2.cmd`（`script/gen_irsim_debug_dffrb2.py`で
生成）を新規作成。76.13の`irsim_debug_dffrb.cmd`と同じ手法を、新しい
`TR-1um.prm`下で・より細かい時間分解能で再実行し、`QS`自体がforce直後に
保持できているか、`QB`が追従しているか、`Q`(`rw`)が追従しているか、を
切り分ける。ユーザーへ提示・実行依頼済み。

**実機確認結果**: `irsim_debug_dffrb2.cmd`実行結果、force中（t=3000～
8040ns）は`XN3.XN2.QS=0 XN3.N4=1 XN3.rw=0`と完全に確定した値を示すが、
release直後（+20ns、t=8060ns）で`XN3.XN2.QS=X XN3.N4=X XN3.rw=X
XN3.addr_match=X`に反転し、以降+400ns（t=8440ns、最終チェックポイント）
まで一貫してXのまま。76.13で文書化した「QSはCK由来の2つの相補的な
トランスミッションゲートにのみ接続され、CKが一度もトグルしないため
Xゲートされ続ける」という構造上の事実そのものは`scmos100.prm`と
`TR-1um.prm`で変わらないはずなのに、前者ではforce後の値をセッション中
すべてのテストで保持し続け、後者ではreleaseした瞬間（1ステップサイズ
＝20ns以内）にXへ反転する、という非対称な挙動が新たな疑問として残った
——IRSIMのターナリソルバの「フローティングノードがどれだけの時間で
Xへ減衰するか」という内部ヒューリスティックが`.prm`の抵抗値（絶対値・
相対比とも`scmos100.prm`と`TR-1um.prm`で異なる）に依存している可能性を
疑い、実トランジスタでの本当の挙動をSPICEレベルで直接確認することにした
（76.31）。

### 76.31 実SPICEレベルでのDFFRB QS保持特性の直接検証——IRSIM側のアーティファクトと結論

76.30の疑問（`QS`はIRSIM上、実`.prm`でforce/release直後にXへ反転するが、
`scmos100.prm`では保持され続ける）に決着をつけるため、ユーザーの提案
「ngspiceでDFFRBのリセット挙動を確認するスクリプトを書けますか」により、
IRSIMのターナリ・スイッチレベルモデルを介さず、DFFRBの実26トランジスタ
構成（`simulations/DFFRB.spice`／`tr_1um_i2c_slave_async.extracted`
1015-1069行と一致、簡略化なし）を実TR-1umモデルで直接トランジェント
解析するテストベンチ`script/gen_dffrb_reset_tb.py`を新規作成。

**構成**: `D`/`CK`を固定（`CK`はSCLアイドル時と同じくHigh固定——一度も
トグルしない状態を再現）、`RSTB`を100ns間Low→High解放のPWLパルスとして、
`.ic V(x1.qs)=0`で`.cmd`スクリプトの「QS force=0」ステップを模擬した
初期条件を与え、以降は一切触れずに40us（このセッション最長の実行
`irsim_test_main.cmd`の終端＝約39260nsを十分カバー）トランジェント解析。

**実機確認（1件目のエラー、修正済み）**: `simulations/DFFRB.spice`から
そのままコピーした`MM<n>`プレフィックスのインスタンス行が、SPICEの
「M」プレフィックス＝MOSFETプリミティブ呼び出しと解釈され、
`.model PMOS`（実際は`.subckt PMOS`のラッパーのみ存在）が見つからず
`could not find a valid modelname`で失敗。**対策**: 全26行を`XM<n>`
（サブサーキット呼び出し構文）にリネームし再生成、実機再実行で成功
（4053データ行、致命的エラーなし）。

**実機確認（2件目、警告のみ・回避）**: `.control`内の
`print v(rstb) v(x1.qs) v(x1.qb) v(x1.q) > ...`が
`vector x1.qb is not available`という警告を出した——原因はトップレベル
の呼び出し行`X1 VDD QB D Q RSTB VSS CK DFFRB`で使われている実引数の
リテラル文字列（"QB"・"D"・"Q"・"RSTB"・"CK"）がそのままトップレベル
ノード名として昇格するため（`x1.`スコープではなく`v(qb)`/`v(q)`で直接
アドレス可能）、内部専用ノードの`QS`だけが本当に`v(x1.qs)`という階層
アドレスを要する、という非対称性を見落としていたため。`.raw`ファイル
自体（`write ... .raw`）は全ベクトルを正しく含んでいたため、`.spi`を
再編集・再実行させる代わりに、`build_tr1um_prm.py`の`read_ascii_raw()`
を再利用し変数名を自動検出する新規スクリプト
`script/analyze_dffrb_reset_tb.py`を作成、既存の`.raw`をそのまま
解析させて対処。

**結果（実機確認済み）**:

```
       t (s)     RSTB       QS       QB        Q
  1.0000e-11    0.000    3.395    3.370    3.720   (起動直後の過渡)
  5.0664e-08    5.000    0.000    5.000   -0.000
  1.0000e-07    5.000    0.000    5.000    0.000
  1.0100e-07    5.000    0.000    5.000    0.000
   ...（t=150ns～4.0e-05sまで、13ポイントすべて同一値）...
  4.0000e-05    5.000    0.000    5.000    0.000
```

t=100ns以降、40us（5桁の時間スケール）にわたって`QS=0.000V`・
`QB=5.000V`・`Q=0.000V`が完全にフラットで、ドリフトが一切測定されない。
（自動クロッシング検出は「t=2.0ns」を報告したが、これはテストベンチ自身の
`VRSTB`のPWLブレークポイント記述ミス——`PWL(0n 0 1n 0 2n {VDD} 100n
{VDD} 101n {VDD})`は意図に反しt=2ns時点で既にHighへ達しており、実際の
RSTB解放はt=100nsではなくt≈2ns付近で起きていた。単なる起動直後の過渡が
検出されただけで、t=100ns以降のプラトー区間の「ドリフトなし」という結論
自体には影響しない。）

**結論**: 実トランジスタレベルでは、`QS`はCK固定によりトランスミッション
ゲートが両方オフになった後も、サブスレッショルド・接合リーク電流だけで
少なくとも40us間、force値を実用上完全に保持する——実チップの動作として
何の問題もない。したがって76.30で見つかった「実`TR-1um.prm`下のIRSIMで
QSがrelease直後（20ns以内）にXへ反転する」現象は、実回路の物理的な欠陥
ではなく、**IRSIMのターナリソルバ自身の動的ノード減衰ヒューリスティック
が`.prm`の抵抗値に依存して過敏に反応しているシミュレータ側のモデリング
アーティファクト**であると結論づける（`scmos100.prm`ではこの同じ
ヒューリスティックがたまたま発火しなかっただけで、むしろ`scmos100.prm`側
の挙動の方が実シリコンに近い、という側面もある）。

**今後の方針（未決定、ユーザーへの確認待ち）**: (a) 機能検証の主軸は
引き続き`scmos100.prm`（このアーティファクトを踏まない）で行い、
`TR-1um.prm`は将来のタイミング関連の参考値としてのみ保持する、
(b) IRSIM自身の`settle`/`relax`コマンド（リリースノートで言及、未調査）
等でこのアーティファクトを回避し、`TR-1um.prm`下でも
`irsim_test_main.cmd`/`irsim_test_negative.cmd`をクリーンに通す方法を
探る、のいずれかを検討する必要がある。

ユーザーは(b)を選択——76.32で実施。

### 76.32 `settle`コマンドの調査と適用——IRSIM公式ドキュメントが正確に一致

IRSIM公式サイト（http://opencircuitdesign.com/irsim/notes.html ,
`reference.html`）を調査。Revision 28で導入された`settle`コマンドが、
まさに今回の問題を狙って作られたものであることが判明:

> Revision 28 introduces a new command "settle" to implement a delay time
> for resolving the value of nodes driven by more than one input at
> different logic levels. This avoids the common problem where a
> back-to-back inverter holding a logic value is driven by an incoming
> (different) logic value. [...] IRSIM's switch-level model is too simple
> to understand the behavior of the back-to-back inverters, so it sees a
> glitch and propagates an "unknown" ("X") value into the circuit. A value
> for "settle" that is longer than a typical gate delay but shorter than a
> clock phase suffices to prevent unknown values from taking over a
> circuit simulation.

これはDFFRBのQM/QS構造（相補的トランスミッションゲートで結ばれた
back-to-back inverter対）そのものの説明であり、76.31のSPICE実測（QSは
実際には全くドリフトせず保持される）とも整合する——`settle`未設定時、
IRSIM はforce解除直後の一瞬の「競合に見える」状態を即座にXと断定するが、
`settle t`を設定すると、その判定をt ns遅らせ、実際には競合が解消して
一方の値に収束する場合はXへ倒さずに済む、という仕組み。

（もう一つの候補`relax`コマンドは、回路全体のXノードを次の1psで強制的に
片方の値へ倒す「初期条件の力技」——公式ドキュメント自身が"should be used
with caution, and as a last resort"と明記しており、`settle`の方が今回の
問題の物理的実体（実際に収束するのに、IRSIMの判定が早すぎるだけ）に
合致するため、`settle`を採用。）

**適用**: `script/gen_irsim_cmd.py`に`SETTLE_NS = 50`定数を追加
（公式ドキュメントの"on the order of 50"という目安値をそのまま採用——
この非同期設計にはクロック位相という上限の目安がないため、チューニング
はせず公式デフォルト目安をそのまま使用）。`preamble()`・
`gen_reset_check()`の両方で、`stepsize {T}`の直後に`settle {SETTLE_NS}`
を発行するよう変更。3本の`.cmd`（`irsim_test_main.cmd`=641行、
`irsim_test_negative.cmd`=320行、`irsim_reset_check.cmd`=115行）を
再生成し、いずれも2行目付近に`settle 50`が出力されていることを確認済み。

**実機確認（成功）**: ユーザーが`irsim TR-1um.prm
tr_1um_i2c_slave_async.sim`で`irsim_reset_check.cmd`（`settle 50`込み）
を実行。結果、`$28`グループのQS force/release完了直後（t=8160ns、
76.30で同じ箇所がXに反転していたのと同一ポイント）で
`XN3.rw=0 XN3.addr_match=0`——Xへ反転せず確定した`0`のまま。`settle`
コマンドが実`TR-1um.prm`下でのX反転アーティファクトを解消することを
実機で確認済み。（`N58`=SDAは既存の設計上、この時点ではまだXのままで
正常——76.10/76.11参照、sda_oeのグループはbusy AND rst_nゲートのため
まだ別グループ。）

**次のステップ**: `irsim_test_main.cmd`/`irsim_test_negative.cmd`も
`settle 50`込みでTR-1um.prm下で再実行し、WRITE/READ/NACKのフル
トランザクションがscmos100.prmのときと同じ結果になることを確認する。

### 76.33 TR-1um.prm下での新たな回帰: WRITEアドレスバイト後、ADDR_MATCH/RXが更新されない

ユーザーが`settle 50`込みの`irsim_test_main.cmd`を実TR-1um.prm下で実行
（analyzerウィンドウのスクリーンショットで「何かおかしいです」と指摘）。
console/analyzerとも既存の`gen_main()`のスクリプト位置に照らして精査した
結果、以下が判明:

- WRITEアドレスバイト（0xA0=SLAVE_ADDR 0x50、W）送信・ACK直後の
  `d(ADDR_MATCH, RW)`チェック（`read_ack()`終了直後、追加マージン0ns）
  で`ADDR_MATCH=0`（期待値1）——scmos100.prmでは同じスクリプト位置で
  正しく1になっていた（76.27で確認済みのフルパス）。
- 続くDATAバイト（0xA5）送信・ACK後の`d(*RX)`でも全ビット0（期待値
  `0xA5`=10100101）——ADDR_MATCHが立たなかったため、書き込みデータの
  キャプチャ自体が行われなかったことと整合。

**76.32の`settle`修正とは別の、新規の問題**（`irsim_reset_check.cmd`は
`settle 50`で正しくパスすることを既に確認済み——76.30/76.32参照）。
33個全DFFRBインスタンスは`$28`/`$227`いずれかのグループに属し、
どちらも既にforce/release済みのため、cold-Xが原因とは考えにくい。
むしろ実`TR-1um.prm`の実測抵抗値（数kΩ、76.29）がプレースホルダより
遅く、`send_bit()`の1ビットあたりのマージン（`2*T=40ns`、scmos100.prm
でのみ経験的にチューニング済み）や、アドレスバイトACK直後の
チェック自体のマージン（現状0ns——`read_ack()`終了直後に間髪入れず
チェックしている）が、実プロセスの遅い伝搬遅延に対して不足している
可能性が高いと仮定。

**切り分け方法**: `script/gen_irsim_debug_addrmatch.py`を新規作成。
WRITEアドレスバイト送信・ACK直後の同じチェック位置で、+0/+100/+200/
+500/+1000/+2000/+4000nsの複数チェックポイントで`ADDR_MATCH`/`RW`/
`BUSY`を繰り返しダンプする`irsim_debug_addrmatch.cmd`（319行）を生成。
時間経過とともに1へ収束すれば単純なマージン不足（`.cmd`側の待ち時間を
全体的に増やせば解決）、+4000nsまで一貫して0のままなら構造的な問題
（このセッションで見つけた$813の恒久的な未発火と同種の可能性）と
切り分けられる。ユーザーへ提示・実行依頼済み。

**実機確認結果**: `irsim_debug_addrmatch.cmd`実行結果、`ADDR_MATCH`は
+4000ns（絶対時刻t=8000ns）まで一貫して`0`——一度も`1`に近づく気配すら
なし。ユーザー提供のanalyzerスクリーンショット（0～8040ns表示）でも
`XN3.rw`/`XN3.addr_match`は`busy`アサート直後（force/releaseによる想定内の
一瞬のX）を除き、完全にフラットな`0`（グリッチも無し）——**単純な
マージン不足ではなく、構造的な問題と判定**。33個のDFFRBは全て`$28`/
`$227`いずれかのグループでforce/release済みのためcold-Xも考えにくく、
アドレス比較ロジック自体（またはシフトレジスタでの入力ビットキャプチャ）
が実`TR-1um.prm`下で正しく機能していない可能性が高い。

### 76.34 シフトレジスタ`shreg[6:0]`の実ビット単位トレース

`src/tr_1um_i2c_slave_async.extracted`（96-134行）の再調査により、
アドレス受信用7ビットシフトレジスタが、フラット化後の`.sim`でも
**RTLの実名のまま生存**していることを確認: `XN3.shreg[0]`～
`XN3.shreg[6]`（`$`記号を含まないネット名は`gen_irsim_sim.py`の
sanitize()によるリネームの対象外——ブラケット`[]`はIRSIMのコマンド
構文上問題にならないはず）。

`script/gen_irsim_debug_shreg.py`を新規作成し、`irsim_debug_shreg.cmd`
（319行）を生成——WRITEアドレスバイト（0xA0）の8ビットを1ビットずつ
送信しながら、**各ビット送信直後**に`shreg[0..6]`全ビット＋`rw`/
`addr_match`をダンプする。目的: シフトレジスタが実際に0x50相当の
パターンを正しく捕捉しているか（＝比較ロジック側の問題）、それとも
シフト自体が正しく起きていない・破損している（＝キャプチャ側の問題）
かを切り分ける。ユーザーへ提示・実行依頼済み。

**実機確認結果**: `irsim_debug_shreg.cmd`実行結果、`shreg[0]`～`shreg[6]`
は8ビット全て送信・ACK後まで**一貫して`0`のまま**——1ビットも変化なし。
単なる比較ロジックの不一致ではなく、シフトレジスタ自体が全くクロック
されていない、より根本的な症状と判明。

### 76.35 シフトレジスタの派生クロック（$635/$666/$34）を追跡

`src/tr_1um_i2c_slave_async.extracted`のDFFRBインスタンス化行
（46-152行）を精査した結果、`$28`グループの各レジスタは単一の共有
派生クロックではなく、**複数の異なる派生クロックネット**に分かれて
いることが判明: `shreg[6]`/`shreg[4]`/`shreg[5]`/`rx_data[*]`の一部、
および**`addr_match`自身のDFFRB**（`X$16`）は`CK=$635`（sanitize後
`XN3.N635`）を共有。`shreg[0]`/`shreg[1]`/`shreg[2]`/`shreg[3]`は
`CK=$666`（`XN3.N666`）。`rw`自身のDFFRB（`X$2`）は`CK=$34`
（`XN3.N34`）と、また別のネット。`.sim`側でも`XN3.N635`/`XN3.N666`/
`XN3.N34`がそれぞれ対応するDFFRBインスタンスの`CKB`ピンを直接駆動して
いることを確認済み——単なる名前の偶然一致ではなく、実際の個別派生
クロック入力。

もしこれらの派生クロックが実SCLエッジに対して全くトグルしていなければ、
「`shreg`が初期値のまま」「`addr_match`自身のDFFRBが一度も発火しない」
の両方を同時に説明できる（`addr_match`のDFFRBも同じ`N635`でクロック
されているため）。

`script/gen_irsim_debug_shregclk.py`を新規作成し、`irsim_debug_shregclk.cmd`
（323行）を生成——アドレスバイトの8ビット送信中、各ビットのSCL立ち上がり
エッジ直後に`SCL`/`N635`/`N666`/`N34`を、ホールド終了時に`shreg[0..6]`を
それぞれダンプし、これらの派生クロックが実際にトグルしているかを確認する。
ユーザーへ提示・実行依頼済み。

**実機確認結果（要注意: コンソール出力の見かけ上の罠あり）**:
コンソールの`d`ダンプ単体では`h N102`直後に`N102=0`と表示され、一見
「SCLがトグルしていない」ように見えたが、ユーザー提供のanalyzer
スクリーンショット（連続波形、0～3972ns表示）では`N635`/`N666`/`N34`は
`N102`と完全に同期して正しくトグルしていることが確認できた——**これは
IRSIMの表示上のアーティファクトで、`h`/`l`で強制した直後（経過時間0）に
同じコマンド列内で`d`を呼ぶと、まだイベントキューが処理される前の値が
表示される**（`s`を1回でも挟めば正しい値が見える）ためと判明。派生
クロック自体は正常にトグルしていることが確定——クロック供給は原因では
ない。

### 76.36 D入力`$697`の素性——複数箇所で共有される信号、構造は健全

`src/tr_1um_i2c_slave_async.extracted`をさらに追跡し、`shreg[0]`のD入力
`\$697`（`X$109`）の素性を特定: `X$167`（BUF_X1）が`\$37`から駆動、
かつ`\$697`は同時に(1)`shreg[0]`のD入力、(2)DEL1（`\$704`、76.22-76.24
既知の限界を持つチェーン）、(3)NOR2（`\$703`と、恒久的に発火しない
`\$813`を生成）、(4)NAND3（`\$666`と`\$817`から、**busy自身の正常動作中の
エッジ検出網`\$696`**を生成）——複数箇所で共有される信号と判明。

busy自身のエッジ検出（`\$696`経由）はTR-1um.prm下でも一貫して正しく
動作している（全実行で確認済み）ため、`\$697`自体の値は恐らく健全——
残る有力な仮説は、**DFFRBが実際にトグルするD入力を実際にトグルする
クロックエッジでキャプチャする動作**（76.31のSPICE検証は「CK固定・
一度force/releaseした値を静的に保持できるか」のみを検証しており、
実エッジでの動的キャプチャは未検証）が、実`TR-1um.prm`のタイミング下で
うまくいっていない、という可能性。

`script/gen_irsim_debug_n697.py`を新規作成し、`irsim_debug_n697.cmd`
（348行）を生成——`h`/`l`/`x`の直後に必ず1ステップサイズのsettleを
挟んでから`d`でチェックする（本節で判明した表示アーティファクトを
回避）構成で、`SDA`/`N697`/`N666`/`shreg[0]`をアドレスバイト全8ビットに
わたって追跡する。ユーザーへ提示・実行依頼済み。

**実機確認結果（大きな進展）**: ユーザー提供のanalyzerスクリーンショット
（連続波形、0～4086ns表示）で判明——`N58`(SDA)と`XN3.N697`は**完全に
同一の波形**（crosshatch→black(1)→white(0)、START条件で1→0に落ちた後、
アドレスバイト全体を通じて**平坦に0のまま**、ビット7・5で明示的に
`x SDA`（リリース、プルアップで1に戻るはず）してもSDAが全く回復しない）。
コンソールの`d`ダンプ（1ステップサイズのマージン込み）でも同じ結論——
`N697`はSDAの単純なバッファ済みコピーであり（`X$167`=BUF_X1）、SDA
自体がSTART後ずっと0に張り付いているため、`N697`も`shreg`も追従して
0のまま、という一本の説明で**両方の既存の謎が同時に解ける**。

これは単なる「弱いプルアップの充電が遅い」ではなく、**能動的に0へ
引っ張られている**（フラットで揺らぎが全くない）ように見える波形。
最有力容疑者: `sda_oe`（スレーブ自身のオープンドレイン駆動イネーブル、
DFFRB`X$318`の出力、RSTB=`$227`、CK=`$513`）が、ビットクロッキング中に
`$513`が再トグルすることで再クロックされ、古い/誤ったD値
（`\$333`）をラッチして誤って1へ張り付き、SDAを能動的にLowへ引っ張って
いる可能性。

### 76.37 `sda_oe`がアドレスバイト送信中にSDAを能動的にLowへ引っ張っているか確認

`script/gen_irsim_debug_sdaoe.py`を新規作成し、`irsim_debug_sdaoe.cmd`
（315行）を生成——`SCL`/`SDA`/`SDA_OE`(N45)/`XN3.N513`（sda_oeのクロック
入力）を`ana`で連続波形表示しつつ、アドレスバイト全8ビットにわたり
1ステップサイズのマージン込みで`d`チェックする。`sda_oe`が確かに
バイト送信中に1へ変化しているか、`N513`が意図せず再トグルしているかを
直接確認する。ユーザーへ提示・実行依頼済み。

**実機確認結果・根本原因の特定**: `irsim_debug_sdaoe.cmd`実行結果、
`N45`(sda_oe)はアドレスバイト送信中も一貫して`0`のまま——スレーブ自身が
SDAを能動的にLowへ引っ張っている可能性は明確に否定された（`sda_oe`は
容疑者から除外）。（`N513`はコンソールの0ns-margin表示アーティファクトの
影響を再び受けたが、analyzerの連続波形では実際にはSCLと同期してトグル
しており、これも原因ではないと判断——ただし決定打はsda_oeが終始OFFで
あることの方。）

sda_oeが犯人でないなら、SDAが「解放（`x SDA`）してもプルアップで1に
戻らず、平坦に0のまま」なのは、能動的な競合ではなく**外付けの弱い
プルアップ抵抗自体が、実TR-1um.prm下では効いていない**という可能性を
検討。`script/gen_irsim_sim.py`のSDAプルアップ実装
（`PULLUP_NODE`=SDA、ゲート=`XN1.XN16.N9`＝パッド自身のプルダウンNFET
ゲート）を確認したところ、**L=20u/W=1u**という非常に細長い形状の
PMOSトランジスタで実装されていた——この値は`scmos100.prm`下で「なんと
なく弱そう」という感覚だけで選ばれており、目標抵抗値へのキャリブレー
ションは一度も行われていなかったと判明。

**定量的な検証**: `build_tr1um_prm.py`が算出した実TR-1um.prmの
p-channel static抵抗値（L=1u, W=10.2u基準で3650Ω、76.29）から、
シート抵抗`Rsq = 3650 × (10.2/1) = 37230 Ω/□`を逆算。この`Rsq`を
旧ジオメトリ（L=20/W=1）に適用すると、`R = 37230 × 20 = 744,600Ω`
（約745kΩ）——本来の設計意図（約20kΩ）の**約37倍も弱い**。これは、
実測データに基づく`.prm`切り替えにより偶然露呈した、プレースホルダ
`scmos100.prm`固有の（キャリブレーションされていない）シート抵抗値に
たまたま「それらしく」フィットしていただけの値だったと考えられる。
745kΩという極端に高い抵抗は、このセッションのテストで使われている
どのビット単位の待ち時間（数十ns）内でもSDAのノード容量を目に見える形で
充電できず、「解放しても平坦に0のまま」という観測結果と定量的に
完全に一致する。

**修正**: `gen_irsim_sim.py`のプルアップジオメトリを、実TR-1um.prmの
同じ`Rsq`を使い目標20kΩ向けに再計算——`W = Rsq×L/R_target =
37230×1/20000 ≈ 1.86`→`L=1u, W=1.9u`（結果R≈19.6kΩ、元の設計意図に
近い）。L=1uを選んだ理由: `build_tr1um_prm.py`が実際に特性化した
L=1u/2uの範囲内に収め、旧L=20uのような較正範囲外への大幅な外挿を
避けるため。`.sim`を再生成（`python3 gen_irsim_sim.py`、2074トランジスタ・
863ノード、以前と同数——このプルアップ1個の形状のみ変更）、
`XN1.XN16.N9 Vdd N58 1 1.9`という行を確認済み。

**注意（ユーザーへの申し送り事項）**: この再キャリブレーションは
**実TR-1um.prm専用**——`scmos100.prm`のシート抵抗値は未知（おそらく
`Rsq`が大幅に異なる）ため、同じ新ジオメトリ（L=1/W=1.9）を
`scmos100.prm`下で使うと、意図せず「強すぎる」プルアップになる
可能性がある。今後`scmos100.prm`での再検証が必要になった場合は
要相談。

**次のステップ**: ユーザーが実機で`irsim TR-1um.prm
tr_1um_i2c_slave_async.sim`（再生成後の`.sim`）を再ロードし、
`irsim_debug_shreg.cmd`または`irsim_test_main.cmd`を再実行して、
SDAが今度は正しく1へ回復し、アドレスバイトが正しくキャプチャされ、
`addr_match`が意図通り`1`になるかを確認する。

**実機確認結果**: プルアップ再キャリブレーション後の`.sim`で
`irsim_debug_shreg.cmd`を再実行——`shreg[0..6]`は依然として全て`0`の
まま。ただし今回は`XN3.busy`がSTART直後に一旦正しく`1`になった後、
アドレスバイトの最初の1ビット目付近で**再び`0`に落ちる**という新しい
挙動が判明（analyzerスクリーンショットで確認）。旧プルアップ
（745kΩ）では弱すぎてこの問題自体が露呈しなかった、という可能性。

### 76.39 プルアップの再キャリブレーションが露呈させたスタブ的な問題:
SCL上昇前のSDAセットルマージン不足によるスプリアスSTOP

`busy`がアドレスバイト送信の極めて早い段階（1ビット目付近）で予期せず
クリアされる、という新パターンから、**スプリアスSTOP条件**（SDAが
SCL=Highの間に上昇＝プロトコル上のSTOP）を疑った。

`send_bit()`/`read_ack()`/`recv_bit()`/`send_ack_from_master()`は
いずれも共通して「SDAをセット（解放`x SDA`または強制`l SDA`）→
**わずか1ステップサイズ（20ns）**→SCLを上げる（`h SCL`）」という
パターンで実装されていた。`scmos100.prm`下ではこれで問題なかったが、
76.37/76.38で正しく約20kΩへ再校正したプルアップは、たとえ正しい抵抗値
であっても、SDAノードの実容量を充電するのに実際のRC時間を要する——
1ステップサイズ（20ns）ではSDAの立ち上がりが完了する前にSCLが上がって
しまい、「SCL=High中にSDAが上昇」というSTOP条件に見えてしまう。これが
`busy`の早期クリアと、それによる`shreg`の未キャプチャ（コアがSTOPと
誤認識してビット処理を止めてしまう）の両方を説明する。

**修正**: `script/gen_irsim_cmd.py`に`SDA_RELEASE_SETTLE_NS = 500`
定数を追加（`stop()`の既存マージン`settle_before_ns`/
`sda_low_hold_ns`と同程度のオーダー——このセッションで既に実測されて
いる、この設計の実際のタイミングスケール感と整合）。`send_bit()`/
`read_ack()`/`recv_bit()`/`send_ack_from_master()`いずれも、SDAを
セットしてからSCLを上げるまでの1ステップサイズの`s()`を
`s(SDA_RELEASE_SETTLE_NS)`に置き換え。`irsim_test_main.cmd`
（641行）/`irsim_test_negative.cmd`（320行）/`irsim_reset_check.cmd`
（115行、影響なし）を再生成し、`irsim_test_main.cmd`中に`s 500`が
38箇所出力されていることを確認済み。`gen_irsim_debug_shreg.py`も
独自の簡易ビットループを削除し、修正済みの本物の`send_bit()`を呼ぶ
よう書き換え、`irsim_debug_shreg.cmd`（320行）を再生成。

**次のステップ**: ユーザーが実機で`irsim_debug_shreg.cmd`を再実行し、
今度は`busy`がアドレスバイト送信中ずっと`1`を維持し、`shreg[0..6]`が
実際に`0xA0`相当のビットパターンを正しくキャプチャするかを確認する。

**実機確認結果（成功）**: `SDA_RELEASE_SETTLE_NS=500`込みで
`irsim_debug_shreg.cmd`を再実行。結果、`shreg[0..6]`が8ビット全てに
わたって実際にビットごとに変化する様子が確認でき（`shreg[0]=1`→
`shreg[1]=1`→...と、送信ビットに応じて1ビットずつ正しくシフトして
いく挙動）、最終的に**`XN3.addr_match=1`、`XN3.rw=0`、
`XN3.busy=1`**（アドレスバイト送信完了後もbusyは維持されたまま——
スプリアスSTOPは解消）と、全て期待通りの値に到達。プルアップの
再校正（76.37/76.38）と本節のSDAセットルマージン修正の**両方**が
揃って初めて、WRITEアドレスバイトの認識が実TR-1um.prm下で正しく
機能することが確認できた。

**次のステップ**: `irsim_test_main.cmd`/`irsim_test_negative.cmd`
（いずれも`SDA_RELEASE_SETTLE_NS`込みで再生成済み）をTR-1um.prm下で
フル実行し、WRITE 0xA5→READ 0x3Cの完全なトランザクション、および
誤アドレスNACKテストが、scmos100.prmのときと同じ結果になることを
確認する。

**実機確認結果（部分的成功、新たな同種バグを発見）**: `irsim_test_main.cmd`
をTR-1um.prm下でフル実行。**WRITEトランザクションは完全成功**——
アドレスバイト後`XN3.addr_match=1 XN3.rw=0`（t=8320ns）、DATAバイト
（0xA5）ACK後の`rx_data`ダンプが`N32=1 N15=0 N34=1 N13=0 N42=0 N78=1
N83=0 N64=1`と、期待される0xA5のビットパターン
（bit0..bit7=1,0,1,0,0,1,0,1）と完全一致（t=13340ns）。

しかし`stop("after write")`の`d(BUSY)`チェック（t=21880ns）で
`XN3.busy=1`（期待値0）——STOPが検出されていない。続くREADトランザクション
開始直後の`d(RW,ADDR_MATCH)`チェック（t=22020ns）でも`addr_match=1`
（本来$28グループforce/releaseで0にクリアされるはず）という残留値、
さらにREADアドレスバイトのACK後`d(RW)`チェック（t=27040ns）で
`XN3.rw=0`（期待値1、READ方向）と、後続のREADトランザクション全体が
崩れた。

**根本原因（76.39と同種の見落とし）**: `stop()`内部の
`x(SDA)`（STOPエッジ用のSDA解放）から`d(BUSY)`チェックまでの間隔が、
76.39で他の関数を直したときに**見落とされたまま**`2*T=40ns`の
ままだった——同じくSDAの実RC充電時間（約500ns級）に対して不足しており、
STOPエッジがまだ検出される前にチェックしてしまっていた（`busy`が
本当は間もなく0になるはずが、まだ1のまま読まれた）。ユーザー提供の
analyzerスクリーンショットでも、`busy`行にちょうどこの位置で細い
グリッチ（一瞬0に落ちてすぐ次のSTARTで1に戻る）が見えており、この
仮説と整合する。

### 76.40 `stop()`自身のSTOP後マージンも同じ`SDA_RELEASE_SETTLE_NS`へ統一

`script/gen_irsim_cmd.py`の`stop()`関数、`x(SDA)`後`d(BUSY)`チェック
前の`self.s(2 * T)`を`self.s(SDA_RELEASE_SETTLE_NS)`（500ns）に
修正。`irsim_test_main.cmd`（655行）/`irsim_test_negative.cmd`
（327行）/`irsim_reset_check.cmd`（115行、影響なし）を再生成。
`gen_main()`側のSTOP完了から次のSTARTまでの間隔（`2*T`）はそのまま
維持——一度`busy=0`が確認できれば、次のSTARTの`l(SDA)`はSDAの状態に
関わらず即座に強制Lowできるため、追加マージンは不要と判断。

**次のステップ**: ユーザーが実機で`irsim_test_main.cmd`を再実行し、
今度はSTOP後に`busy=0`が正しく読め、READトランザクション
（addr_match/rw含む）も正しく完走するかを確認する。

**実機確認結果（WRITE完全成功、STOP修正も有効、新たな別バグ発見）**:
`irsim_test_main.cmd`再実行。**WRITEトランザクションは引き続き
完全成功**（addr_match=1/rw=0確認、rx_data=0xA5完全一致）。STOP修正も
有効——`d(BUSY)`が今度は正しく`XN3.busy=0`（t=22340ns、76.40の修正どおり
+500ns後）を示した。

しかしSECOND START（READトランザクション開始）の`$28`グループforce/
release直後、`d(RW, ADDR_MATCH)`チェック（t=22480ns）で
`XN3.rw=0`（正しい）だが**`XN3.addr_match=1`**（期待値0、WRITEの
マッチ結果が残留）。analyzerスクリーンショットでは、`addr_match`行に
このタイミングでちょうど細いグリッチ（一瞬0に落ちてすぐ1に戻る）が
見え、「forceは一瞬効くが、releaseした途端に何かが1へ再駆動している」
という挙動を示唆——`rw`は同じforce/release機構で正しく0を維持できて
いるため、`addr_match`固有の非対称な問題。

### 76.41 `addr_match`固有のforce/release後の再駆動を追跡

`addr_match`自身のDFFRB（インスタンス`XN16`、`DFFRB_28_INSTANCES`に
含まれることを確認済み）のD入力`XN3.N642`（比較結果、2段のインバータ
`XN3.XN17.N6`→`XN3.N642`経由）とCK`XN3.N635`（76.35でSCLと同期して
正しくトグルすることを確認済み、`shreg[4..6]`/`rx_data`の一部とも
共有）に着目。仮説: `addr_match`のD入力`N642`が、`shreg[6:0]`自身の
force/release（同じ`$28`グループ内、同じ呼び出しで並行実行）が
比較ロジックへ伝搬しきる前の、まだ「マッチ」を示す古い値のままの
瞬間に、`N635`の実クロックエッジ（READトランザクションの新しい
アドレスバイト送信で既にSCLがトグルし始めている可能性）で再ラッチ
されてしまい、我々のforceを上書きしている可能性。

`script/gen_irsim_debug_addrmatch2.py`を新規作成——`preamble()`/
`start()`/`send_byte()`/`read_ack()`/`stop()`という**実際の本番用
プリミティブをそのまま使って**WRITEトランザクション全体を忠実に
再現した上で、2回目の`start()`だけを手動で分解し、$28グループの
force/release前後を1ステップサイズ（20ns）刻みで`XN16.QS`/`N642`/
`N635`/`rw`自身の`QS`（`XN2.QS`）/`addr_match`/`rw`を追跡する
`irsim_debug_addrmatch2.cmd`（481行）を生成。forceが全く効いていない
のか、それとも効いた直後に再駆動されているのかを切り分ける。
ユーザーへ提示・実行依頼済み。

**実機確認結果（決定的）**: 1ステップサイズ刻みのトレースにより完全に
解明。t=22420（force直後）: `XN3.XN16.QS=0 XN3.N642=0 ... XN3.addr_match=0`
——forceは正しく効いている。t=22440（release後+1ステップ）:
`XN3.XN16.QS=1 XN3.N642=1 XN3.N635=1 ... XN3.addr_match=1`——即座に1へ
反転。この間`XN3.N635`（`addr_match`自身のDFFRBのCK入力）は
**一貫して`1`のまま、一度もトグルしていない**。

**根本原因の特定**: DFFRBの`QS`は2つのトランスミッションゲートに
接続されている——`QM<->QS`（CK=1でtransparent）と`QS<->net5`
（CK=0でtransparent、`net5=NOT(QB)`のキーパー経路）。**CKが定義された
実際の値を持つ限り、この2つのうち必ずどちらか一方が常にtransparent**
——コールドスタート時（CKが一度もトグルせずXのまま）にのみ、両方の
ゲートが同時にXゲートされ、`QS`が真に浮遊（force可能）になる、という
特殊ケースだった。`N635`が実際に`1`で安定している今回のようなケースでは、
`QM<->QS`は常にtransparentであり、`QS`は`D`（`N642`、まだクリアされて
いない`shreg`由来の古い「マッチ」結果）をリアルタイムで反映し続ける
——外部からの一時的なforceは、releaseした瞬間に本物のドライバに
即座に上書きされる。これは**IRSIMのターナリソルバのアーティファクト
ではなく、物理的に正しいトランスペアレントラッチの挙動**——実シリコン
でも同じことが起きるはず。

**76.31のSPICE結論への訂正**: `dffrb_reset_tb.spi`（76.31）は
`CK`を`VDD`（=1、transparent）に固定していたが、**`D`も同時に`0`固定**
していたため、force値とD値がたまたま一致しており、「本当に孤立して
保持している」のか「単にDと一致するトランスペアレント通過を見ていた
だけ」なのかを区別できていなかった。今回のバグは、まさにこの
「Dがforce値と一致しない」ケースであり、76.31のテスト設計では原理的に
検出できなかった。76.31自体の「QSは実質的に安定して値を保持できる」
という結論そのものは（D=forceの場合に限り）引き続き有効だが、
「force/releaseは常に安定する」という一般化は誤りだったと訂正する。

### 76.42 `force_release_gated()`: CKも同時にforceして真の孤立を再現

修正方針: `QS`をforceする**同じ期間中に、そのグループの派生CKネットも
一緒にLowへforce**する。CK=0にすると`QM<->QS`がopaque（Dから孤立）に
なり、`QS<->net5`がtransparentになるが、`net5=NOT(QB)`かつ`QB`は
`QS`の常時稼働インバータであるため、この経路は**forceした値と自己
矛盾なく強化し合うループ**になる（`QS=0→QB=1→net5=0→QSへフィード
バック=0`）。さらに、グループ全体（`shreg`のインスタンスも含む）を
**同時に**孤立・リセットすることで、CKネットが実際の値へ解放された
時点では、下流の比較ロジック（`addr_match`のD入力`N642`）が既に
新鮮な（クリア済みの）レジスタ状態を反映しているようにする。

`script/gen_irsim_cmd.py`: `force_release()`に孤立ノードの前提条件
（コールドスタートのみ有効）を明記するdocstring修正、新規メソッド
`force_release_gated(nodes, gate_nets, value=0)`を追加（CKネットも
force/release対象に含める）。`DFFRB_28_CLOCK_NETS = ["XN3.N34",
"XN3.N635", "XN3.N666", "XN3.N343"]`定数を追加（4つの派生CKネットの
sanitized名、`$28`グループの24インスタンスがこの4つに分かれることを
`extracted`netlistから再確認）。`start()`内の`$28`グループforce/release
呼び出しを`first`引数で分岐: **初回**（`first=True`）はSCLがまだ
一度もトグルしていない＝CKネットはまだコールドスタートのXのままなので
**従来の`force_release()`のまま**（これまでのWRITEトランザクション
成功runは全てこの経路——変更なし、リグレッションリスクなし）。
**2回目以降**（`first=False`、READ側の2回目のSTARTなど）は新しい
`force_release_gated()`を使用。`irsim_test_main.cmd`（683行）/
`irsim_test_negative.cmd`（334行）を再生成し、`l XN3.N34`/`l
XN3.N635`/`l XN3.N666`/`l XN3.N343`が2回目の`$28`グループforce直前に
出力されていることを確認済み。

**次のステップ**: ユーザーが実機で`irsim_test_main.cmd`を再実行し、
READトランザクション開始時、今度は`addr_match`が正しく`0`にクリア
され、READアドレスバイト送信後に正しく`1`（マッチ）・`rw=1`
（READ方向）となり、READデータ（0x3C）が正しく読み出せるかを確認する。

**実機確認結果（部分的成功）**: `irsim_test_main.cmd`再実行。
`force_release_gated()`の効果を確認——READ START直後の`d(RW,
ADDR_MATCH)`チェック（t=22480ns）で**`XN3.rw=0 XN3.addr_match=0`と
両方とも正しく0にクリア**（76.30-76.41で悩まされていた「addr_matchが
古い1のまま残留する」問題は解消）。analyzerスクリーンショットでも
`addr_match`がREAD側で正しく0→1（READアドレス一致）と再現よく遷移
している様子を確認。

ただし、READアドレスバイトのACK後の`d(RW)`チェック（t=27500ns）で
`XN3.rw=0`（期待値1、R/W=1のはず）——`rw`固有の別の問題が残っている。
（analyzer画像の`rw`行は終始ベタ塗りに見えるが、これは74.xで確認済みの
「値が変化しない信号はベタ塗りで描画される」という表示上の癖の可能性が
高く、コンソールの明示的な`rw=0`の方を信頼する。）

### 76.43 `rw`自身のD/CK（N207/N34）をREADアドレスバイト中に追跡

`rw`自身のDFFRB（`XN2`、D入力`XN3.N207`、CK`XN3.N34`——`extracted`
netlistで確認済み）が、READアドレスバイトの8ビット送信中に正しく
R/Wビット（=1）を捕捉できているかを検証するため、
`script/gen_irsim_debug_rw2.py`を新規作成。`preamble()`/`start()`/
`send_byte()`/`read_ack()`/`stop()`という実際の本番プリミティブで
WRITEトランザクション全体とREAD側のSTARTまでを忠実に再現した上で、
READアドレスバイトの8ビットだけを手動で分解し、各ビット送信後に
`rw`自身の`QS`/`N207`/`N34`/`rw`出力を追跡する`irsim_debug_rw2.cmd`
（585行）を生成。`N34`が`force_release_gated()`によるforce/release後も
正しくSCLの実トグルを追従し続けているか（副作用で狂っていないか）、
`N207`（D入力）がR/Wビット送信時に正しく`1`を示すか、を切り分ける。
ユーザーへ提示・実行依頼済み。

**実機確認結果**: `N34`はチェック地点（各ビットの`h(SCL)`+ホールド後、
すなわちSCLが再びHighに戻った瞬間）で一貫して`1`——これは「SCLと
正しく同期している」ことの証拠であり（両方High時にチェックしている
だけ）、「stuck」の証拠ではないと判断（早期の見かけ上のアーティ
ファクトと同じ罠を避けるため再解釈）。一方`N207`（D入力）は8ビット
全てを通じて`0`のまま——R/Wビット（value=1）送信時も含め変化なし。

### 76.44 RTLソース精読による方針転換: `rw_bit`/`addr_ok`は`bit_cnt==7`の
特定エッジでのみ更新される登録値

`src/i2c_slave_async.v`（102-164行）を直接精読し、根本的な設計理解を
修正: `rw_bit`と`addr_ok`は共に**登録値**（レジスタ）であり、
**SCLのposedgeかつ`phase==PH_ADDR && bit_cnt==7`という特定条件が
成立した瞬間にのみ**更新される（`rw_bit <= shreg_next[0]`、
`addr_ok <= (shreg_next[7:1]==SLAVE_ADDR)`）——単純にSDAやshregを
combinationalに追跡しているわけではない。一方`shreg`自体は
`phase`/`bit_cnt`に関係なく**毎SCLエッジ無条件に**シフトする
（`shreg <= {shreg[6:0], sda_in}`）。

`rw_bit`と`addr_ok`は**全く同じクロックエッジ**で更新される設計なので、
以前のフルトランザクションのanalyzer画像で`addr_match`がREADアドレス
バイト中に確かに1へ立ち上がっているように見えた点（76.42時点）を踏まえ
ると、`rw`も同じ瞬間に更新されているはずだが、我々の`read_ack()`直後の
チェック（マージン0ns相当）では**間に合っていない**だけ、という
「単純なマージン不足」仮説（76.32/76.37-76.40と同種）が有力になった。

`script/gen_irsim_debug_rwmargin.py`を新規作成——READアドレスバイトの
ACK直後から、+100/+200/+500/+1000/+2000/+4000nsの複数チェックポイントで
`rw`/`addr_match`/`busy`を繰り返しダンプする`irsim_debug_rwmargin.cmd`
（583行）を生成。時間経過で正しい値（`rw=1`, `addr_match=1`）に収束
すれば単純なマージン不足、+4000nsまで一貫して`rw=0`のままなら
`phase`/`bit_cnt`自身のmid-runリセットに何らかの構造的問題が残って
いる、と切り分けられる。ユーザーへ提示・実行依頼済み。

**実機確認結果**: +4000nsまで`XN3.rw=0 XN3.addr_match=0`のまま——
単純なマージン不足ではなく、`phase`/`bit_cnt`自身のmid-runリセットに
構造的な問題が残っていると判明。

### 76.45 未特定の$28グループ7インスタンスを`phase`/`bit_cnt`と同定、
`bit_cnt`カウントアップの実挙動を追跡

`DFFRB_28_INSTANCES`の24個のうち、`rw`(XN2)・`addr_match`(XN16)・
`shreg[0..6]`+`rx_data[*]`（14個）を除いた残り7個
（XN32/XN46/XN104/XN111とXN115/XN116/XN117）が、RTLの
`phase[2:0]`（3ビット）+`bit_cnt[3:0]`（4ビット）＝7個と正確に一致
することを`extracted`netlistから確認。うち`XN32`/`XN46`/`XN104`/
`XN111`は**`rw`と同じCK=`N34`を共有**（4個、`bit_cnt`候補）、
`XN115`/`XN116`/`XN117`はCK=`N343`を共有（3個、`phase`候補）。

`script/gen_irsim_debug_bitcnt.py`を新規作成——推定`bit_cnt`
（Q出力: `N43`/`N11`/`N62`/`N243`）と推定`phase`
（Q出力: `N358`/`N137`/`N349`）を、READアドレスバイトの8ビット
それぞれの後に追跡する`irsim_debug_bitcnt.cmd`（583行）を生成。
`bit_cnt`が実際に0→7とカウントアップしているか、それとも`rw`と同じ
`N34`クロックを共有していることに関連して同様に停止しているかを
確認する。ユーザーへ提示・実行依頼済み。

**実機確認結果（大きな進展）**: 候補7信号を8ビット分ダンプした結果、
`N358`/`N349`/`N243`の組み合わせが、bit7→bit0の各サンプルで
`1,2,3,4,5,6,7,0`という**きれいな3ビットバイナリカウントアップ
パターン**を示し、ちょうど`bit1`送信直後（＝RTL上`bit_cnt==7`に
到達する瞬間）で最大値`7`に達していることを確認。さらに`N43`/`N11`が
その**同じ瞬間**に揃って`0→1`へ遷移——RTLの`phase<=PH_ADDR_ACK`
遷移と時刻的に一致。**`bit_cnt`/`phase`自体は正しく機能している**と
判断——問題はカウンタ機構ではなく、`rw`/`addr_match`自身の
キャプチャ経路に絞られた。

### 76.46 `rw`のD入力マルチプレクサ（`N23`選択線）を直接追跡

`irsim_debug_rw2.cmd`（76.43）で`rw`のD入力`N207`が終始`0`のまま
だった理由を、`N207`を駆動するゲート構造の直接精読で特定:
`N207`は2:1トランスミッションゲート・マルチプレクサ`XN1`の出力の
インバータで、**`N23`が選択線**——`N23=0`なら`N207=N40`（新しい値を
キャプチャ）、`N23=1`なら`N207=rw`自身（フィードバック＝ホールド）。
さらに、**`addr_match`自身の類似マルチプレクサ（`XN17`）も同じ`N23`を
共有**していることを確認——両方とも同じ選択信号で「今キャプチャすべき
瞬間か、それとも保持すべきか」を判定している。もし`N23`が常に`1`
（ホールド）のまま`0`（キャプチャ）へ一度も落ちなければ、`rw`と
`addr_match`の両方が同時に、恒久的にホールド状態へ張り付く——実際に
観測されている症状と正確に一致する。`N23`自体は`N11`（`bit_cnt`候補の
一つ）を含む信号から組み合わせ論理で生成されており、実質的に
「`bit_cnt==7 && phase==PH_ADDR`」のデコードゲートである可能性が高い。

`script/gen_irsim_debug_n23.py`を新規作成——`N23`（選択線）・`N40`
（新値ソース）・`N207`（D入力）・`shreg[0]`（R/Wビットを保持しているはず）
・`rw`・`addr_match`を、READアドレスバイトの8ビットそれぞれの後に
追跡する`irsim_debug_n23.cmd`（581行）を生成。`N23`が`bit_cnt==7`の
瞬間に実際に`0`（キャプチャ）へ落ちるか、落ちた場合`N40`が正しく`1`
（R/Wビット）を示しているかを確認する。ユーザーへ提示・実行依頼済み。

**実機確認結果（レース条件を特定）**: `N23`は`bit1`送信直後
（t=25840ns）に**1回だけ**`0`（キャプチャ許可）になり、その直後
`bit0`（実際のR/Wビット、値=1）が来る頃には既に`1`（ホールド）へ
戻ってしまっていた。RTL上、`bit_cnt==7`という判定自体は`bit1`の
エッジで初めて`bit_cnt`が`7`になった**次のエッジ**（＝`bit0`の
エッジ）で行われるべきだが、`N23`の許可ウィンドウは1エッジ早く開いて
1エッジ早く閉じている——本来キャプチャすべき`bit0`のタイミングには
既に閉じてしまっている、という**1サイクルのレース**であることが
判明。ユーザーの選択により、`N23`自体の駆動ゲートをさらに特定する
方針で継続。

### 76.47 `N23`の駆動論理を特定: `NAND(N123, N235)`——`bit_cnt`由来と
`phase`由来、どちらが原因かを追跡

`N23`を直接駆動するトランジスタを`.sim`から特定: 並列2PMOS
（プルアップ、`N123`・`N235`それぞれゲート）＋直列2NMOS
（プルダウン、`N235`→`N123`の順）という構成——**`N23 =
NAND(N123, N235)`**（`N123`・`N235`が両方`1`のときのみ`N23=0`）。
さらに`N123`自体は`N11`（確認済みの`bit_cnt`候補ビット）でゲートされる
別のNAND/NORゲートの出力、`N235`は設計内の非常に多くの他のゲートで
共有されている（ファンアウトが高い）ことを確認——`N137`（`phase`候補）
がその入力の一つになっており、`phase==PH_ADDR`相当のデコードである
可能性が高い。

`script/gen_irsim_debug_n123n235.py`を新規作成——`N23`への直接2入力
（`N123`・`N235`）と`N23`自体、および`N11`/`N137`を、READアドレス
バイトの8ビットそれぞれの後に追跡する`irsim_debug_n123n235.cmd`
（581行）を生成。`N123`（`bit_cnt`由来）が1エッジ早く`1`になって
いるのか、`N235`（`phase`由来）が1エッジ遅く`0`から離れているのか、
それとも両方が絡んでいるのかを切り分ける。ユーザーへ提示・実行
依頼済み。

**実機確認結果（真の原因: 同一エッジ内での競合）**: `N123`は
`bit7`～`bit1`まで一貫して`1`、`bit0`直後で初めて`0`へ——`N235`は
`bit7`～`bit2`は`0`、`bit1`直後だけ`1`、`bit0`直後は再び`0`。両方とも
**それぞれのサンプル時点における`bit_cnt`の実際の値に対して正しい**
——`N235`（`bit_cnt==7`のデコードと判明、`phase`ではなかった）は
`bit_cnt`が本当に`7`である唯一の窓（`bit1`のエッジ後～`bit0`のエッジ前）
でのみ`1`になり、`bit0`のチェック時点では`bit_cnt`が**既に`0`へ
リセットされた後**なので`0`——RTL通り。

**真の原因**: `bit_cnt<=0`（カウンタの自己リセット）と`rw_bit<=
shreg_next[0]`（`rw`のキャプチャ）は**全く同じクロックエッジ**で
トリガーされる。しかし`rw`のキャプチャが成立するには、そのエッジで
`bit_cnt`の**古い値（7）**が組み合わせ論理チェーン（`N235`→`N23`→
`N207`）を通り抜けて`rw`自身のレジスタに間に合う必要がある——一方
`bit_cnt`自身のレジスタは**同じエッジで新しい値（0）へ**更新される。
両者が同じ組み合わせ論理パスを奪い合う、**実デジタル設計でいう
ホールドタイム違反に相当する、同一エッジ内での競合**であると判断。
`scmos100.prm`の（較正されていない）タイミング比では偶然この競合が
好ましい方向に決着していた可能性が高く、実測に基づく`TR-1um.prm`の
異なる（かつ実際に近い）遅延プロファイルの下で逆転した、という解釈が
最も整合的。

この種の同一エッジ内レース条件は、IRSIMのスイッチレベルシミュレーション
だけでは確定的な結論（実チップで本当に問題になるか）を出しきれない
領域——本格的な検証には静的タイミング解析（STA）または、この特定の
競合を再現するSPICEレベルのテストベンチが必要になる。

**方針決定**: ユーザーの判断により、このREAD側の同一エッジレースは
IRSIM検証の限界として本セクションに記録した上で一旦保留し、
`TR-1um.prm`下でのこのセッションの成果を**WRITEパス（完全に動作
確認済み）を中心にまとめる**方針とする。SPICEレベルでの定量的検証は
将来の課題として残す。

### 76.48 実TR-1um.prm下でのこのセッションの到達点まとめ

**完全に動作確認済み**:
- `irsim_reset_check.cmd`: コールドスタートからのリセット解除、
  `settle 50`込みで全信号が正しく確定値へ収束（76.30/76.32/76.38）。
- `irsim_test_main.cmd`のWRITEトランザクション部分:
  START検出→アドレスバイト(0xA0)送信→ACK→`addr_match=1`/`rw=0`確認
  →DATAバイト(0xA5)送信→ACK→`rx_data`が`0xA5`と完全一致→STOP→
  `busy=0`確認、まで実機で完全にパス（76.39/76.40/76.42で3件の
  マージン/構造バグを修正した結果）。

**未解決（既知の課題として保留）**:
- READトランザクション: STARTの検出・`$28`グループの mid-run リセット
  （`force_release_gated()`、76.42）自体は正しく動作するが、READ
  アドレスバイト送信後の`rw`/`addr_match`キャプチャが、`bit_cnt`の
  自己リセットとの同一エッジ内レース（76.47）により機能しない。
  IRSIMのスイッチレベルモデルでは実際の物理的な有意性を確定できない
  ため、SPICEレベルでの追加検証は将来の課題とする。
- `irsim_test_negative.cmd`（誤アドレスNACKテスト）は、本セッションで
  適用した一連の修正（`settle`/プルアップ再校正/`SDA_RELEASE_
  SETTLE_NS`/STOP修正/`force_release_gated()`）を反映した状態では
  まだ実TR-1um.prm下で再検証していない——WRITE単体のトランザクション
  （START〜STOP、2回目のSTARTを伴わない）なので、READ側の同一エッジ
  レースの影響を受けない可能性が高く、次回の検証候補。

**このセッション全体を通じて見つかった`.prm`切り替えに起因するバグ・
修正一覧**（時系列）: (1) `settle`コマンド追加によるDFFRB QS
X反転の解消（76.32）。(2) SDAプルアップジオメトリの誤り
（L=20/W=1、実測`Rsq`で約745kΩ相当、意図は約20kΩ）を修正
（L=1/W=1.9、76.38）。(3) SDA解放後のセットルマージン不足
（`SDA_RELEASE_SETTLE_NS=500`追加、76.39）。(4) `stop()`自身の
同種マージン不足の見落とし修正（76.40）。(5) DFFRBの`QS`は実際の
定義されたCKレベル下では2つのトランスミッションゲートのどちらかが
常にtransparentであり、mid-runのforce/releaseがそのままでは機能
しないことを発見・`force_release_gated()`で対処（76.41/76.42）。
(6) READ側`rw`/`addr_match`キャプチャの同一エッジレース発見（未解決、
76.43-76.47）。

いずれも「プレースホルダ`scmos100.prm`でのみ経験的にチューニングされた
マージン・想定が、実測特性化された`TR-1um.prm`下では成り立たない」
という共通パターンであり、実TR-1um.prmへの切り替えという当初の目的
（76.29）が、副次的にこれだけ多くの潜在的なタイミング前提の甘さを
洗い出す結果となった。

## 77. V8世代の計画: RTL根本修正（ウォーキングワン方式）+ sda_oe極性修正（2026-08-28）

### 77.1 背景・決定事項

§76で見つかった問題のうち、以下の2件は**IRSIM側のワークアラウンドでは
なく実設計（RTL）の修正が必要**と判断されているもの：

1. **READ側`rw`/`addr_match`キャプチャの同一エッジレース**（76.43〜76.47）:
   `bit_cnt`の自己リセットと`rw_bit`/`addr_ok`の取り込みが同一SCLエッジ・
   同一組み合わせデコードパス（`N235`="bit_cnt==7"判定→`N23`→mux）を
   共有しており、実測`TR-1um.prm`下ではこのパスのタイミング余裕が
   保証できない。ホールドタイム違反に類する構造。
2. **`sda_oe`⇔パッド`HIZ13`間の極性不一致**（76.17〜76.18）:
   コアの`sda_oe`出力（RTL意図: `1=LOW駆動, 0=解放`）とSDAパッドの
   `HIZ13`入力（パッド実仕様: `HIZ=L→駆動, HIZ=H→解放`）が直結されて
   おり、両者のセンスが逆。実シリコンのままではリセット直後・アイドル時に
   SDAを能動的にLOW駆動し続け、I2Cスレーブとして機能しない。IRSIM検証では
   `.sim`内だけの実験的インバータ（76.18）で回避していたが、これは
   検証専用の回避策であり実配線の修正ではない。

ユーザー判断（2026-08-28）: 両方をRTLレベルで修正し、
**Verilog検証 → 論理合成（NET） → 配置配線 → DRC/LVS検証 → IRSIM検証**
の一連のフローを再実行する新しい設計世代として進める。この世代を
**V8**と呼ぶ（v1〜v7は配置配線側のレシピ世代呼称として既に使われている
ため、V8はRTLの実体変更を伴う初めての「フル再実行世代」という位置づけ）。

`rw`/`addr_match`キャプチャの修正方式は、事前に検討した2案
（1エッジ先読みレジスタ追加 vs. ウォーキングワン化）のうち、
**案2: ウォーキングワン（one-hot）シフトレジスタ方式**を採用する
（ユーザー選択）。

### 77.2 修正1: bit_cntの同一エッジレース解消（ウォーキングワン方式）

**現状の構造**（`src/i2c_slave_async.v` 106行目、123〜154行目）:

`bit_cnt`は4bitのバイナリアップカウンタで、各フェーズ（`PH_ADDR`/
`PH_DATA_WR`/`PH_DATA_RD`）で共通して「`bit_cnt==7`なら最終ビット」を
組み合わせ論理で判定し、**同じSCLエッジ**で（a）`bit_cnt`自身を`0`へ
リセット、（b）`rw_bit`/`addr_ok`/`rx_data_r`への取り込みを行う。
合成後のゲートレベルでは、この「`==7`判定」の組み合わせパス
（`N235`→`N23`→mux→`N207`等、design_notes 76.46/76.47でトレース済み）が、
同一エッジで自分自身の入力（`bit_cnt`のQ出力）が変化し始める前に
確定していなければならず、`TR-1um.prm`の実測遅延ではこの余裕が
確保できていない可能性が高い。

**修正方針**: `bit_cnt`（4bitカウンタ）+ 比較器（`==7`デコード）という
構造そのものを廃止し、`shreg`と対称的な**8bitウォーキングワン
（one-hotシフト）レジスタ**に置き換える。「今何ビット目か」を
2進数のカウント値ではなく、シフトレジスタ中の`1`の位置そのものとして
直接表現することで、比較器（組み合わせデコード）を完全に除去する。

```verilog
reg [7:0] bit_walk;   // reset時: 8'b1000_0000 (MSBが立った状態でスタート)
wire      is_last_bit = bit_walk[0];   // LSBまで歩いてきたら最終ビット

// 各SCLエッジ:
//   rst_scl_domain時: bit_walk <= 8'b1000_0000;
//   通常時          : bit_walk <= is_last_bit ? 8'b1000_0000 : {1'b0, bit_walk[7:1]};
```

`is_last_bit`は`bit_walk`という1本のレジスタのビットそのもの
（Q出力）であり、「今まさに取り込むべきエッジか」を判定するのに
一切の組み合わせ比較器を経由しない。前エッジで確定した`bit_walk`の
値が、次エッジの取り込み判定にそのまま使われる——結果として
`rw_bit`/`addr_ok`/`rx_data_r`の取り込み許可信号は、同エッジで
変化する信号（`bit_walk`自身の次state）とは完全に分離され、
1エッジ分（クロック→Q遅延のみ）の安定余裕を持つ。

既存の`bit_cnt`は`PH_DATA_RD`で「カウントアップし続ける」使い方
（148〜150行目、`bit_cnt <= bit_cnt + 1`を`is_last_bit`到達後も継続）
もしており、これも`bit_walk`のシフト（`is_last_bit`到達で先頭へ
巻き戻し）に単純に置き換えられる（歩き続けるレジスタなので巻き戻し後は
自動的に次バイトの1ビット目から再開する）。

`bit_cnt`という信号名・4bit幅の比較演算（`==7`、Yosysの`abc`で
コンパレータとして合成される可能性が高い）が消え、代わりに8bitの
シフトレジスタ+マルチプレクサ（`shreg`の構造とほぼ同型）になるため、
**合成後のインスタンス数・DFFRB個数は増加する見込み**（3bit相当の
カウンタ+比較器 → 8bit相当のシフトレジスタ、フェーズ毎の使い回しは
従来通り共通化可能）。v7の配置配線レシピ・placement.jsonはそのままでは
使えず、v8として新規に配置配線をやり直す。

### 77.3 修正2: sda_oe極性の反転

**根本原因**（76.17/76.18で確定済み）: コアの`sda_oe`出力ピンと、
SDAパッド（`OSS_ESD_5V_DIO`）の`HIZ13`入力ピンは、トップレベル
ネットリストで**直結**（インバータなし）。ところがRTLの意図
（`1=LOW駆動, 0=解放`、69〜70行目のコメント）と、パッドセルの実仕様
（`HIZ=L→駆動, HIZ=H→解放`、ユーザー確認）はセンスが逆——コアが
「解放のつもり（`sda_oe=0`）」で出す値を、パッドは「駆動しろ」
（`HIZ=L`）と受け取ってしまう。IRSIM検証では`.sim`内だけの実験的な
インバータ挿入（`insert_sda_hiz_inverter()`、76.18）でこれを再現・
回避していたが、あくまで検証用のシミュレーション限定パッチであり
実配線ではない。

**修正方針**: 物理配線側（GIO⇔コア結線、`route_gio_core.py`）に
インバータセルを新規追加するのではなく、**RTL側でセンスを反転**する
——`sda_oe`出力そのものを「パッドの`HIZ13`が直接期待するセンス」
（`0=LOW駆動, 1=解放`）に合わせて出力する。ネット自体
（`sda_oe`⇔`HIZ13`間の配線）は直結のまま変更不要で、コア内部の
最終出力段だけがセンス反転する。具体的には`sda_oe_r`更新ロジック
（171〜193行目）はそのまま「1=駆動」で書いた上で、出力ポートへの
`assign`だけ反転する:

```verilog
assign sda_oe = ~sda_oe_r;   // sda_oe_r: 1=駆動(内部表現), 出力ポート: 0=駆動(パッドHIZ13の実仕様に一致)
```

および59行目の古い（実態と矛盾した）ポートコメントと70行目のコメントを
統一・修正する。

**波及確認が必要な箇所**（V8のVerilog検証フェーズで漏れなく確認）:

- `src/i2c_slave_async_tb.v` / `stdcell_behavioral_stubs.v` /
  `i2c_slave_async_net_tb.v` — テストベンチ側のSDAバスモデルが
  `sda_oe`の値をどちらのセンスで解釈しているか確認・必要なら反転。
- MyHDLモデル（`script/i2c_slave_async_model_v3.py`ほか、
  `test_i2c_slave_async.py`/`test_i2c_slave_async_negative.py`）
  — 同様にRTLと同じセンスへ追従させる。
- `script/gen_irsim_sim.py` — SDAプルアップのゲート条件は76.17の
  修正で既に`sda_oe`(`N45`)自体ではなく**パッド内部信号
  `XN1.XN16.N9`**（パッドが実際に駆動中かどうか）を見る構造に
  変更済みなので、`sda_oe`自体のセンスには非依存——影響なしの見込み
  （要再確認）。`insert_sda_hiz_inverter()`（76.18のシミュレーション限定
  ワークアラウンド）はRTL側で正規に修正されるため**不要になり、削除**する。
- `script/gen_irsim_cmd.py` — `sda_oe`ノード（`N45`）を直接dumpして
  期待値を検証している箇所があれば、期待値のセンスを反転。

### 77.4 V8移行フロー（実行順）

ユーザー指定の順序: **Verilog検証 → NET合成 → 配置配置 → DRC/LVS検証
→ IRSIM検証**。

1. **Verilog修正・検証**
   - `src/i2c_slave_async.v`: `bit_cnt`→`bit_walk`（ウォーキングワン化、
     77.2）、`sda_oe`極性反転（77.3）、ヘッダコメントをv3へ更新。
   - `iverilog`によるRTLシミュレーション再実行（`i2c_slave_async_tb.v`）。
   - MyHDLモデルを同じロジックへ追従させ、`test_i2c_slave_async.py`/
     `test_i2c_slave_async_negative.py`を再実行（write/read/アドレス
     不一致の3シナリオ、README「実行方法」節参照）。
   - 特にREADシナリオで、同一エッジレースが解消され`rw`/`addr_match`が
     正しく取り込まれることをRTLシミュレーションレベルで確認
     （これがV8着手の成功基準そのもの）。
2. **NET合成**
   - Yosys再合成（README記載のコマンド）→ `i2c_slave_async_net.v`。
   - v7で行ったBUFTH挿入等の後処理（design_notes §18, §39〜42相当）を
     v8ネットリストに対しても必要に応じて再実施 → `i2c_slave_async_net_v8.v`。
   - ゲートレベル`iverilog`シミュレーション（`stdcell_behavioral_stubs.v`
     使用）で合成後ネットリストの機能等価性を再確認。
3. **配置配線**
   - インスタンス数・構成がv7から変わるため（77.2参照）、
     `run_route_v7_from_scratch.py`系のスクリプトをv8ネットリスト向けの
     configとして再実行（配置→配線→リップアップ&リルート→
     チャネル圧縮→トップピン引き出し）。DRC 0違反・短絡0件を目標値として
     維持。
4. **DRC/LVS検証**
   - `gen_schematic_v7.py`/`verify_schematic_v7.py`相当をv8ネットリスト向けに
     再実行（幾何学的接続検証）。
   - コアセル単体LVS、トップレベル統合（`route_gio_core.py`）、
     トップレベルLVSを再実行。**sda_oe極性反転はコア内部ロジックのみの
     変更であり、`HIZ13`への物理配線自体は変更不要**という想定（77.3）を
     ここで実地に確認する——もしGIO側結線やLEFピン定義に極性依存の
     前提が別途あれば、この段階で検出されるはず。
5. **IRSIM検証**
   - `gen_irsim_sim.py`/`gen_irsim_cmd.py`をv8ネットリスト・v8配置配線
     成果物向けに再生成。`insert_sda_hiz_inverter()`ワークアラウンド（76.18）
     を削除。
   - §76で確立した全テストスイート（`irsim_reset_check.cmd`/
     `irsim_test_main.cmd`/`irsim_test_negative.cmd`、`settle 50`・
     `SDA_RELEASE_SETTLE_NS=500`・`force_release_gated()`等、§76で
     得られた知見はそのまま流用）を実`TR-1um.prm`下で再実行。
   - 今回のV8の主目的である「READトランザクションの`rw`/`addr_match`が
     正しく取り込まれること」「アイドル/リセット時にSDAが能動的にLOW
     駆動されないこと」を実機IRSIMで確認するのが最終ゴール。

### 77.5 リスク・留意事項

- ウォーキングワン化でゲート数・DFFRB個数が増える見込みのため、
  v7で達成した「DRC違反0・短絡0件・コア高さ988.2um」を維持できるかは
  配置配線をやり直してみないと分からない。面積が増える可能性は許容
  （機能正しさを優先）。
- sda_oe極性反転はRTL側1箇所の反転で済む想定だが、77.3に挙げた
  波及箇所（テストベンチ、MyHDLモデル、IRSIM生成スクリプト）の
  対応漏れがあると「合成後は正しいのにシミュレーション上だけ矛盾する」
  ような紛らわしいバグを新たに作り込むリスクがある——Verilog検証段階で
  チェックリストとして扱う。
- READ側同一エッジレース（77.2の修正対象）は、IRSIMのスイッチレベル
  モデルでは「解消されたように見える」ことの確認はできても、実測
  `TR-1um.prm`のタイミングマージンが実際にどれだけあるかの定量評価
  （STA相当）はV8のIRSIM検証だけでは得られない。ウォーキングワン化に
  より構造的にはレースの可能性自体を排除できる（同エッジ内で
  取り込み許可信号自身が変化する経路が無くなる）ため、これは
  タイミング余裕の問題ではなく設計そのものの解決と位置づけられる。

### 77.6 配置配線パラメータ変更: TAP電源メッシュ強化（3列→5列、ユーザー指示）

ユーザー指示（2026-08-28）: 「配置配線の前で止めてください。TAPの配置を
３列から５列に増やして電源メッシュを強化します。」——V8フローの
Verilog修正・NET合成（77.4のステップ1〜2）は着手可能だが、**配置配線
（ステップ3）に入る前に、この電源メッシュ強化をレシピへ反映してから
進める**方針。実際の配置配線・DRC/LVS・IRSIM実行はまだ行わない
（本節はパラメータ変更点の記録のみ）。

**現状（3列、design_notes §38.5・§45.2）**: TAP列数は
`script/gen_placement_nrow_fm.py`の`N_GAPS`（現在`2`、109〜110行目）
から`TAP列数 = N_GAPS + 1`で決まる。行幅`TARGET_ROW_WIDTH_UM`
（1620.0um固定）・`TAP_WIDTH_UM`（10.8um）・`N_GAPS`から
`TAP_INTERVAL_TRACKS`（TAP列間のギャップ幅、トラック単位）を逆算する
構造（117行目）。§45.2に、行内セル数の都合で`N_GAPS`を2→3
（TAP3列→4列）へ増やした前例があり、その際は`TARGET_ROW_WIDTH_UM`も
連動して見直している。電源メッシュ自体を描く`route_channels_nrow_fm.py`
はTAP列のX座標を配置JSONから動的に読み取る構造で、列数のハード
コーディングは無いため、**配線側の修正は不要**——配置生成側の
パラメータ変更のみで済む見込み。

**変更内容（V8で配置配線に着手する際に適用）**:
1. `script/gen_placement_nrow_fm.py` 110行目: `N_GAPS = 2` →
   `N_GAPS = 4`（TAP 3列→5列）。
2. 118行目の`assert`に残っているハードコードされた係数`3`
   （`3 * TAP_WIDTH_UM + ...`）を`(N_GAPS + 1) * TAP_WIDTH_UM`相当に
   修正——このまま`N_GAPS`だけ変更すると、この行のassertが必ず
   失敗する。
3. `TAP_INTERVAL_TRACKS`は`N_GAPS`変更に伴い自動的に再計算される
   （ギャップ数が2→4に増えるため、1ギャップあたりの幅は狭くなる）。
   V8はウォーキングワン化（77.2）でゲート数が増える見込みのため、
   370行目の`assert n_gaps <= N_GAPS`（各行のセル内容が利用可能な
   ギャップ数に収まっているか）を実際の配置生成時に確認し、収まらない
   場合は§45.2の前例に倣い`TARGET_ROW_WIDTH_UM`（行幅）を合わせて
   見直す。
4. 変更後、配置配線（77.4ステップ3）を実行し、DRC 0違反・短絡0件を
   維持できるか確認。

この変更は「V8で必要になったから」ではなく、独立した電源メッシュ
強化の要求（ユーザー判断）——ウォーキングワン化によるゲート数増加とは
無関係に、V8の配置配線をやり直すこの機会に合わせて適用する。

### 77.7 `src/i2c_slave_async.v` v3実装（77.2/77.3の適用、2026-08-28）

77.2（ウォーキングワン化）・77.3（sda_oe極性反転）を実際に`src/
i2c_slave_async.v`へ適用した。ヘッダコメントを`v3`に更新し、変更内容の
サマリを追記。

**適用内容**:
- `reg [3:0] bit_cnt` → `reg [7:0] bit_walk`（reset/各バイト開始:
  `8'b1000_0000`、`is_last_bit = bit_walk[0]`）。PH_ADDR/PH_DATA_WR/
  PH_DATA_WR_ACK/PH_DATA_RD/PH_DATA_RD_ACKの全分岐で`bit_cnt==7`判定を
  `is_last_bit`に、`bit_cnt<=0`を`bit_walk<=8'b1000_0000`に、
  `bit_cnt<=bit_cnt+1`を`bit_walk<={1'b0,bit_walk[7:1]}`に置換
  （77.2で設計した通り）。
- negedge領域（`sda_oe`/`txreg`生成）: `bit_cnt==0`判定を`bit_walk[7]`に、
  `txreg[7-bit_cnt]`という算術インデックスを`|(txreg & bit_walk)`
  （ワンホットAND-ORによるビット選択）に置換——`bit_walk`はposedge領域の
  レジスタをnegedge領域から直接読む、v2から変わらないクロスドメイン
  参照構造（`phase`も同様）。
- `assign sda_oe = sda_oe_r;` → `assign sda_oe = ~sda_oe_r;`
  （77.3の通り、`sda_oe_r`自体の内部センスは`1=駆動`のまま維持し、
  出力段だけ反転）。ポートコメント（旧69/70行目、矛盾していた59行目も
  含め）を`v3: 0=駆動, 1=解放`に統一。

**簡易検証（iverilog不在のため代替）**: 作業環境（サンドボックス）に
`iverilog`/`verilator`が無く、`apt-get`もroot権限が無いためインストール
不可——実機での`iverilog`実行は引き続きユーザー側で必要（77.4ステップ1）。
代わりに、`bit_cnt`版と`bit_walk`版のposedge領域FSMをPythonで
ビット精度で書き起こし（non-blocking代入と同じ「edge開始時の値で
右辺を計算」というセマンティクスを踏襲）、両者を同一のランダムSDA
ビット列でロックステップ実行して`phase`/`addr_ok`/`rw_bit`/
`rx_data_r`が全エッジで一致するかを検証するスクリプト
（`check_bitwalk_equiv.py`、一時ファイル）を作成・実行した。結果:
50000エッジのランダム比較で不一致0件、および実際のwrite(0x50→0xA5)/
read(0x50)/wrong-address(0x11)の3シナリオでも新旧一致
（read シナリオで`rw=1`が正しく確定することも含め）を確認。
txregのインデックス方式（旧算術インデックス vs 新ワンホットマスク）も
2000ランダムバイト×8ビット位置で別途一致確認。

**注意**: このPythonチェックはRTLの論理的等価性（non-blocking代入の
セマンティクス上、新旧どちらの記述も同一の値を計算する）を確認した
だけであり、77.2で解決を狙っている「同一エッジ内の実配線遅延に
起因するレース」はRTLシミュレーションのレベルでは原理的に再現され
得ない（77.5でも言及の通り、これはゲートレベルの物理的タイミング
現象であり、確認には合成後の実配線・IRSIM/STAが必要）。したがって
このチェックは「書き間違いが無いか」の確認であって、「レースが
解消されたことの証明」ではない——後者はV8のNET合成〜IRSIM検証
（77.4ステップ2〜5）まで到達しないと確認できない。

**新たな発見（MyHDLモデルの陳腐化）**: `script/i2c_slave_async_model.py`
の中身を確認したところ、現在のRTL（v2/v3で共通のDEL1遅延線＋NOR2
クロスカップルSRラッチによる無クロックSTART/STOP検出アーキテクチャ、
`phase`/`rst_scl_domain`ベースの単一always構成）とは異なる、より古い
アーキテクチャ（`state`=IDLE/ADDR/...の7状態、`sda_in`のposedge/negedge
それぞれに個別のalways blockを持つSTART/STOP検出）のままだったことが
判明した。RTLヘッダコメント（旧v2時点）が参照していた
`i2c_slave_async_model_v3.py`/`test_v3_positive.py`/`test_v3_negative.py`
という名前のファイルも実際には存在せず（実際のファイル名は
`i2c_slave_async_model.py`/`test_i2c_slave_async.py`/
`test_i2c_slave_async_negative.py`）——ヘッダコメントの記述と実体が
既にv2の時点で乖離していたと考えられる。RTLヘッダコメントはこの
乖離を明記する形に修正済み（次のVerilog検証フェーズ、77.4ステップ1で
MyHDLモデルの全面見直しが必要と明記）。

`src/i2c_slave_async_tb.v`（iverilog用testbench）は現行v2/v3
アーキテクチャに追従しているため軽微な修正（`sda`アサインの極性反転:
`s_oe ? 1'b0 : 1'bz` → `s_oe ? 1'bz : 1'b0`）のみで足りると判断——
実機`iverilog`実行はユーザー環境で次回実施予定。

### 77.8 `script/i2c_slave_async_model.py`全面書き直し + MyHDL回帰実行（サンドボックスで実施、2026-08-28）

77.7で判明したMyHDLモデルの陳腐化（v2以前の古いアーキテクチャのまま
放置）を受け、`script/i2c_slave_async_model.py`を**RTL（v3）の直訳**として
全面書き直しした。独立した振る舞い記述ではなく、意図的にRTL構造へ
忠実に対応させている（DEL1遅延線を`@instance`のbehavioral delay line
で再現、`start_pulse`/`stop_pulse`/`busy_clr`/`rst_scl_domain`/
`rst_sdaoe_domain`を`@always_comb`で構成、busyはSET/CLEARのedge-triggered
ラッチとしてモデル化、`phase`/`bit_walk`ベースのFSMは77.2のRTL変更を
そのまま転写、`sda_oe`出力も77.3同様に内部センス`sda_oe_r`と出力段の
反転を分離）——目的はモデルとRTLの独立検証ではなく、RTLのバグを確実に
拾えるようにすること。

`script/test_i2c_slave_async.py`/`test_i2c_slave_async_negative.py`の
バス機能モデル（`bus_model`）も、`sda_slave_oe`が新しい極性
（`0=駆動`）になったことに合わせて
`sda_line.next = not (sda_master_oe or (not sda_slave_oe))`へ修正。

**実行結果（このサンドボックス環境、MyHDL 0.11.51が利用可能だったため
実施——iverilogは引き続き不在）**:

```
$ python3 -m script.test_i2c_slave_async
[t=160] OK: busy asserted after START
[t=840] OK: slave ACKed matching address (write)
[t=840] OK: addr_match asserted
[t=840] OK: rw indicates WRITE
[t=1540] OK: slave ACKed data byte
[t=1580] OK: rx_data == 0xA5 (got 0xA5)
[t=1640] OK: busy cleared after STOP
[t=2420] OK: slave ACKed matching address (read)
[t=2420] OK: rw indicates READ
[t=3080] OK: read byte == 0x3C (got 0x3C)
[t=3220] OK: busy cleared after final STOP
All checks PASSED

$ python3 -m script.test_i2c_slave_async_negative
[t=840] OK: unmatched address -> NACK (no slave ack)
[t=840] OK: addr_match not asserted for foreign address
[t=920] OK: busy cleared after STOP following NACK
All checks PASSED
```

特に`rw indicates READ`（READアドレスバイト受信直後の`rw`確認）が
正しくPASSしている点が重要——これは§76.43〜76.47でチップレベルIRSIM
（実`TR-1um.prm`）下で失敗が確認された、まさに同じチェック。ただし
これはRTLレベルのシミュレーション（non-blocking代入セマンティクスの
みに従う、ゲート遅延ゼロの世界）であり、77.7で述べた通り実配線
タイミングに起因するレースはこのレベルでは原理的に見えない——
このPASSは「新しいRTL記述に論理的な誤りが無いこと」の確認であって、
「実チップでレースが解消されたことの証明」ではない点は変わらない。
最終的な確証はV8のNET合成〜配置配線〜IRSIM検証（77.4ステップ2〜5）まで
到達して初めて得られる。

**残作業**: `iverilog`によるゲートレベルRTL（`i2c_slave_async_tb.v`）の
実機実行は、このサンドボックスに`iverilog`/`verilator`が無く
（root権限も無くapt-getでのインストール不可）、引き続きユーザー環境で
実施が必要。

**追記（実機`iverilog`確認済み）**: ユーザー環境で
`iverilog -o sim i2c_slave_async.v stdcell_behavioral_stubs.v
i2c_slave_async_tb.v && vvp sim`を実行——当初`stdcell_behavioral_stubs.v`
（DEL1/NOR2/INV_X1を含む、既存の振る舞いスタブ一式）のコンパイルが
コマンドに含まれておらず`Unknown module type: DEL1/NOR2/INV_X1`で
エラーになったため、コマンド例とtestbenchヘッダコメント・READMEの
「実行方法」節を修正（`stdcell_behavioral_stubs.v`を明示的に含めるよう
指示）。修正後、実際の構造的DEL1/NOR2/INV_X1ゲート（`#1`/`#4`遅延付き）を
含むゲートレベルRTLシミュレーションで、write/read/wrong-addressの
全3シナリオ・14チェック全てPASS（`rw indicates READ`含む）。これで
Verilog検証（MyHDL回帰＋実iverilog実行の両方）が完了——V8の
「Verilog検証」フェーズ（77.4ステップ1）はクリア。

### 77.9 Yosys再合成をサンドボックスで試行 → WASM版ABCがクラッシュ、ユーザー環境での実行が必要と判明

`pip install yowasp-yosys`相当（`yowasp-yosys`コマンド）はこのサンドボックス
に既にインストール済みだったため、README記載の合成コマンドを
`src/i2c_slave_async.v`（v3）に対して試行した。`dfflibmap`まではv2同様
正常動作（`DFFRB`/`DFFS`へのマッピング成功、今回は`bit_walk`化で
フリップフロップ数がv2の33個→37個に増加——4bit二値カウンタ+比較器を
8bitワンホットに置き換えた分、想定通り増加）。

しかし直後の`abc -liberty TR1um_5_stdcell.lib`（技術マッピング本体）が、
`Replacing 10 occurrences of constant undef bits with constant zero bits`
というログの直後で**エラーメッセージ無しに終了コード1で異常終了**——
`write_verilog`まで到達せずネットリストが生成されない。切り分けのため
以下を実施:

- `abc -liberty ...` → `abc -g simple`（Liberty非依存の汎用ゲートマッピング）
  に変えても同じ箇所で同じ異常終了。Liberty解析側の問題ではないと判明。
- 最小限のダミーモジュール（1bit DFFのみ、組み合わせ論理なし）は
  正常に合成完了——ただしABCが実質何もしない（"Don't call ABC as there is
  nothing to map"）ケースなので、ABC本体のテストにはなっていない。
- 8bitカウンタ+XOR程度の小さな組み合わせ論理を持つダミーモジュール
  （`tiny2.v`）でも、`i2c_slave_async.v`と全く同じ箇所
  （`Extracting gate netlist...`の直後）でログが途切れ、ABCが完走しない
  ことを確認——**この`i2c_slave_async.v`固有の問題ではなく、この
  サンドボックスにインストールされている`yowasp-yosys`（WASM版ABC、
  Yosys 0.67）が、非自明な組み合わせ論理を伴う設計全般でABCの技術
  マッピングを完走できない、環境側の制約**であると判断した。

**結論**: Yosys再合成（77.4ステップ2）はこのサンドボックスでは実行不可。
`iverilog`/IRSIM同様、**ユーザー環境（ネイティブ版yosys、または
ローカルのyowasp-yosys）での実行が必要**。コマンド自体はREADME記載の
ものと同一（`src/i2c_slave_async.v`はv3に更新済み）:

```sh
pip install yowasp-yosys
yowasp-yosys -p "
  read_verilog src/i2c_slave_async.v
  hierarchy -top i2c_slave_async -keep_portwidths
  proc; opt
  techmap; opt
  dfflegalize -cell \$_DFF_PP0_ 0
  dfflibmap -liberty TR1um_5_stdcell.lib
  abc -liberty TR1um_5_stdcell.lib
  write_verilog i2c_slave_async_net.v"
```

期待される差分（v2/v7との比較用）: DFFR/DFFRB系フリップフロップが
33個→37個程度に増加（ウォーキングワン化、77.2/77.7参照）。ゲート数
全体も、8bitシフトレジスタ+ワンホットAND-OR選択ロジックがbit_cnt比較器
より若干大きくなる可能性があるため、v7の154インスタンスから増加する
見込み（77.5で既に想定済みのリスク）。

**追記（ユーザー環境でも同じ箇所でクラッシュ、原因を特定）**: ユーザーが
実機で上記コマンドを実行した結果（`ABC.log`）を確認したところ、
ログヘッダに`Yosys 0.68 ... Clang /workspace/YoWASP/yosys/wasi-sdk-33.0-
x86_64-linux...`とあり、**ユーザー環境も`yowasp-yosys`（WASM/WASI版）
だった**ことが判明。`dfflibmap`まで正常動作（DFFRB 37個へのマッピング
成功、サンドボックスでの結果と完全一致）した後、サンドボックスと
**全く同一の箇所**（`Replacing 10 occurrences of constant undef bits
with constant zero bits`の直後）でログが途切れて終了——サンドボックス
のダミー回路（77.9既述）でも再現したことと合わせ、**`i2c_slave_async.v`
固有の問題ではなく、`yowasp-yosys`のWASM/WASI版ABCが非自明な回路の
技術マッピングを完走できないという、このツール配布形態自体の制約**
である可能性が非常に高いと判断した（WASM実行環境のスタック/メモリ
制限がABCの再帰的処理に対して厳しすぎる可能性を推測）。

**対策（ユーザーへ提案）**: ネイティブ版yosys（例: macOSなら
`brew install yosys`）への切り替えを提案。コマンド自体は`yowasp-yosys`
→`yosys`に変えるだけで同一。ネイティブ版でも同じ箇所で再現する場合は
RTL側の問題を疑う必要があるため、結果待ち。

**追記（ネイティブyosysで合成成功、`ABC1.log`確認）**: ユーザーが
Homebrew版ネイティブyosys（`Yosys 0.68+post ... AppleClang`）で再実行した
ところ、`abc`ステップを含め最後まで完走（`src/i2c_slave_async_net.v`
生成済み）。77.9で仮説として立てた「`yowasp-yosys`のWASM/WASI版ABC自体の
実行時制約」という見立てが実証された形になる——**以後、このプロジェクトの
Yosys合成はネイティブ版（Homebrew等）を使うこと**をREADMEにも明記する。

合成結果（`src/i2c_slave_async_net.v`、31338 bytes）を確認:
- セル数: 174個（内訳: `DFFRB`×37、`AND2_X1`×13、`INV_X1`×6、`MUX2`×19、
  `NAND2`×39、`NAND3`×1、`NAND4`×2、`NOR2`×39、`NOR3`×8、`NOR4`×2、
  `OR3`×3、`OR4`×2、`DFFS`×**0**）。
- ユーザー指示（「DFFSは使わないでください。DFFRBもしくはDFFだけ使って
  ください」）は**既に満たされていた**——`bit_walk`の非ゼロreset値
  （`8'b1000_0000`）がSET系セル（DFFS）を要求するのではと事前に懸念して
  いたが、実際には`dfflegalize -cell $_DFF_PP0_ 0`（README記載の既存
  合成コマンドに元から含まれるオプション）が、reset値が1のビットを
  自動的に極性反転（DFFR/DFFRBの0リセット + 周辺組み合わせ論理でのNOT
  吸収）で処理しており、DFFSは一切インスタンス化されなかった。
  `dfflibmap`ログ中の「`cell DFFS ... is a direct match for cell type
  $_DFF_PP1_`」は単にライブラリ中に存在するセル型の一覧であり、実際に
  このモジュールで使われたことを意味しない（371行目「mapped 37
  $_DFF_PN0_ cells to DFFRB cells」が実際の全37個の割り当て結果）。
- フリップフロップ数はv7の33個から37個に増加（+4）——`bit_cnt`
  （4bit二値カウンタ+比較器相当）を`bit_walk`（8bitワンホット
  シフトレジスタ）に置換した分（77.2/77.7で想定済みの増加、design_notes
  §77.5でも事前にリスクとして記載済み）。
- 総セル数はv7の154個から174個に増加（+20）。

これでV8の「NET合成」（77.4ステップ2）の中核（Yosys合成本体）は完了。

### 77.10 v7後処理パイプライン（行バッファ+BUFTH挿入）をV8ネットリストへ適用

77.9の続きとして、v7同様の後処理（scl/scl_nの行毎バッファ挿入 →
scl/sda_inへのBUFTH挿入、design_notes §42.2〜42.8/§59参照）をV8
ネットリスト（174セル）に適用した。

**`insert_row_buffers.py`が見つからない問題**: `script/`ディレクトリを
確認したところ、`insert_row_buffers.py`が存在しなかった——`git log`で
調べたところ、コミット`1d586ea`（「Top-level FRAME/GIO integration, LVS
クリーンアップ、プロジェクト整理」、design_notes記載の以前のセッションで
の作業）の「~52本の旧世代・重複スクリプトを削除」に含まれて削除されて
いたことが判明。v7ネットリストは既に生成・チェックイン済みだったため
不要と判断されたと思われるが、V8で同じ後処理を再実行する必要が生じた
ため、`git show <削除コミットの親>:script/insert_row_buffers.py`で
復元した。復元にあたり、`sys.path`のハードコードを他の新しいスクリプト
と同様`pathlib.Path(__file__)`ベースの相対解決に変更した以外は元の
ロジックのまま——この関数は元々`main(in_path=..., out_path=...,
row_assignment_json=..., target_nets=...)`という引数付きで書かれており
（モジュールレベルの`V3_PATH`/`V4_PATH`等はデフォルト値としてのみ
使われる）、V8向けにファイル自体を書き換える必要はなく、そのまま
異なる引数で呼び出すだけで再利用できた。

`insert_bufth_scl_sda.py`（削除されず現存）は`main()`が無引数で
モジュールレベルの`V6_PATH`/`V7_PATH`/`ROW_ASSIGNMENT_JSON`/
`TMP_STAGE1_PATH`を直接参照するハードコード構造だったため、
`insert_row_buffers.py`と同じパターン（`main(in_path=..., out_path=...,
row_assignment_json=..., tmp_stage1_path=...)`引数化、モジュールレベル
定数はv6→v7のデフォルト値として温存）にリファクタリングした。

**実行結果**（このサンドボックスで実施、両スクリプトとも純粋な
Pythonテキスト処理でyosys/ABC不要のため問題なく動作）:

1. `insert_row_buffers.py`を`i2c_slave_async_net.v`（174セル、raw）に
   対し`target_nets=["scl","scl_n"]`で実行 →
   `i2c_slave_async_net_v8_rowbuf.v`。scl: 4行全てにタッチ
   （`scl_row0`〜`scl_row3`、FO=6/6/7/12）、scl_n: 2行にタッチ
   （`scl_n_row1`/`scl_n_row2`、FO=3/6）。
2. `insert_bufth_scl_sda.py`を上記出力に対し実行 →
   `i2c_slave_async_net_v8.v`。stage1の`assert n_scl_a == 4`
   （scl_bufの`.A()`シンクがちょうど4個=行毎バッファのみであることの
   確認）は問題なくPASS。sda_in: 7個のセルピンシンクを検出（`u_del_sda.A`
   含む、v7の「7個+死んだassign1個」という構造と一致）、3行にタッチ
   （`sda_in_row1`/`row2`/`row3`、FO=2/1/4）。

**最終ネットリスト検証**（`src/i2c_slave_async_net_v8.v`、1827行）:
- セル数186個の内訳: `DFFRB`×37、`DFFS`×**0**（引き続き満たされている）、
  `BUFTH`×2（`u_bufth_scl`/`u_bufth_sda_in`）、`BUF_X1`×9（行バッファ、
  scl4+scl_n2+sda_in3）、`AND2_X1`×13、`INV_X1`×7、`MUX2`×19、
  `NAND2`×39、`NAND3`×1、`NAND4`×2、`NOR2`×41、`NOR3`×8、`NOR4`×2、
  `OR3`×3、`OR4`×2、`DEL1`×1。
- トップレベルポート（`scl`/`sda_in`含む12個）は変更なしで保持されて
  いることを確認（`rename_word()`がヘッダ/宣言行をスキップする設計
  通り）。
- `u_inv_scl`（scl_n生成用）の入力が`scl_row2`（行バッファ経由）に
  正しく付け替わっていることを確認——v7と同じ構造
  （77節冒頭のスクリプト説明「scl_nは既存の行バッファ出力から派生
  するため別対応不要」）。

これでV8の「NET合成」（77.4ステップ2）が完全に完了——`src/
i2c_slave_async_net_v8.v`がv7の`i2c_slave_async_net_v7.v`に対応する、
配置配線の入力として使えるネットリストとなった。次は配置配線
（77.4ステップ3）、まずTAP電源メッシュ強化（77.6、N_GAPS 2→4）を
`gen_placement_nrow_fm.py`へ反映してから着手する。

### 77.11 V8配置配線 STEP1（TAP電源メッシュ配置）実施、STEP2（配線）で初回ルーティング障害

ユーザー指示: 「配置配線結果をSTEP毎にlayout/step8に保存しながら
進めてください」。`layout/step8/`ディレクトリを新設し、v7の
`layout/steps_v7_v2/`と同じ命名規則（`v8_step_N_<説明>.gds`）で
チェックポイントを保存する方針とした。

**N_GAPS=4化に伴うTARGET_ROW_WIDTH_UM再導出**: `gen_placement_nrow_fm.py`
のTAP_INTERVAL_TRACKSは元々`TARGET_ROW_WIDTH_UM`（1620.0um固定）から
逆算していたが、N_GAPS=2→4では割り切れない
（`(1620-5*10.8)/4/5.4=72.5`、整数にならない）ことが判明。方針を反転し、
**TAP_INTERVAL_TRACKS=73を固定入力とし、そこからTARGET_ROW_WIDTH_UM
（=1630.8um、302トラック）を逆算**する形に変更（72の場合1609.2um/298
トラックとなり僅かに縮む方を選ばず、v8はv7よりインスタンス数が
多い（186 vs 154）分の余裕を見て73を選択）。

**STEP1（配置＋TAP電源メッシュ）**: `gen_placement_nrow_fm.py`
（part_json=`row_assignment_v8_final.json`再利用）→
`gen_placement_gds_nrow_fm.py`で`layout/step8/v8_step_1_tap_power_mesh.gds`
を生成。行あたりインスタンス数74/76/58/70（TAP5個+実セル+FILL、
コアBBOX 1630.8um×1813.6um、v7のチャネル高さ目標をそのまま流用
（[130,460,416,390,150]→スナップ後[131.6,461.0,417.8,390.8,153.2]）。

**STEP2（配線）で問題発生**: `route_channels_nrow_fm.py`にも
`ROW_WIDTH_UM=1620.0`のハードコードが別途あり（`gen_placement_nrow_fm.py`
と同期させる必要があるコメント付き）、`R.ROW_WIDTH_UM = 1630.8`を
呼び出し前にモジュール属性として上書きして対応。

`force_jog_nets`/`per_row_local_nets`を空集合にした初回（v7の
ネットリスト固有ハードコードリスト——`bit_cnt[3]`等、v8では
`bit_walk`化により存在しない信号名や、Yosysの自動採番ネット名
`_009_`等でv8では別の番号になっているもの——をそのまま使うのは
無意味なため、まず素の状態で試行）で、**`route_channels_nrow_fm.py`が
`SystemExit`で異常終了**: `no clear X found in overlap zone near
x=224.1 (y 722.2-1091.4)`。停止直前のログでは以下も観測:

- priority M2 corridor: **16トラック**（v7は8トラック相当——N_GAPS
  4→8箇所×2トラックで想定通り倍増、電源メッシュ強化の意図通り）。
- `forced overlap zone(s): {1: (121.5,1557.9), 2: (121.5,1574.1),
  3: (234.9,1574.1)}` — チャネル1〜3でほぼ行全幅にまたがる強制
  オーバーラップゾーンが発生。
- `draw_jog could not find a track`警告×2、`find_row_clear_x found
  no row-clear X`警告×1（いずれも「代替手段でフォールバックしたが
  短絡の可能性あり」）。

**仮説（未検証）**: TAP列を3→5に増やしたことで、電源メッシュ用の
優先M2コリドー予約幅が倍増（8→16トラック）した分、各ギャップ内で
信号配線に使える実質的な横方向の余裕が減り、v7では問題にならなかった
密集箇所で経路探索が行き詰まった可能性が高い——電源メッシュ強化と
信号配線余裕はトレードオフの関係にあり、その境界がv8で顕在化した
と考えられる。

**ユーザー判断**: 「行幅/チャネル高さを拡張して再試行」を選択。

### 77.12 行幅・チャネル高さ拡張 → STEP2配線成功、STEP3リップアップでv7同等品質に到達

`TAP_INTERVAL_TRACKS`を73→**100**（行幅2214.0um、410トラック、77.11の
1630.8umからさらに+35.8%）に拡張。チャネル高さ目標もv7比+約30%
（[170,600,540,510,195]、スナップ後[174.8,601.4,542.0,515.0,196.4]）に
拡張してSTEP1を再生成（`layout/step8/v8_step_1_tap_power_mesh.gds`、
コアBBOX 2214.0um×2288.8um）。

**STEP2（配線、初回=FORCE_JOG/PER_ROW_LOCALなし）**: 今度は`SystemExit`
無しで完走（`layout/step8/v8_step_2_routed_raw.gds`）。ただし複数の
`draw_jog could not find a track`警告（フォールバックで継続、
短絡の可能性を示唆）。DRC（`drc_check_nrow_fm.py`）: **0違反**。
接続性検証（`verify_connectivity_nrow_fm.py`、scan_w=2214.0を明示、
run_route_v7_from_scratch.pyのコメントにある既知の落とし穴通り）:
**6件のSHORT SUSPECTED**（`addr_ok`/`_094_`/`scl_row0`/`scl_row1`/
`scl_n_row1`/`sda_in_buf`が絡む）。

6件全てが`scl_row*`/`scl_n_row1`/`sda_in_buf`/`addr_ok`という、
**v7の`PER_ROW_LOCAL_NETS`/`FORCE_JOG_NETS`にも実際に含まれていた
（同名の）ネット**が絡んでいたことから、v7で経験的に得られた知見が
v8でも部分的に有効という直接的な手がかりを得た（`scl_n_row1`は
v7の`FORCE_JOG_NETS`に、`sda_in_buf`/`addr_ok`はv7の
`PER_ROW_LOCAL_NETS`に、それぞれ元から含まれていた）。

`ripup_reroute_shorts.py`を試したが、6件全てが「multi-segment/
high-fanout（動かすとカスケードリスクが高い）」として保護されている
ネット同士の衝突で、**0件しか自動修正できず**（想定通り——v7でも
この保護ルールがある）。

**対策**: `route_channels_nrow_fm.py`の`force_jog_nets`引数に
`{scl_row0,scl_row1,scl_row2,scl_row3,scl_n_row1,scl_n_row2,
sda_in_buf,addr_ok,_177_,_115_,_094_}`（6件のSHORTに関わったネット+
v7のFORCE_JOG_NETSからの類推）を指定して再配線
（`layout/step8/v8_step_2b_routed_fjog.gds`）。DRC: **0違反**維持。
接続性検証: SHORT SUSPECTEDが**6件→3件**に減少（残り: `scl_row0`↔
`_177_`、`scl_n_row1`↔`scl_row1`、`sda_in_buf`↔`scl_row0`——いずれも
`scl_row*`/`scl_n_row1`/`sda_in_buf`が絡む、上記と同種の組み合わせ）。

この結果に対し再度`ripup_reroute_shorts.py`を実行
（`layout/step8/v8_step_3_ripup_reroute.gds`）——3件とも
multi-segment/high-fanout保護ネット同士の衝突のため、やはり
**自動修正0件**（想定通り）。

**現在の到達点の評価**: DRC 0違反・SHORT SUSPECTED 3件（全て
`scl`/`scl_n`/`sda_in_buf`という、意図的に「動かせない複雑ネット」に
保護されているネット同士の組み合わせ）という結果は、**v7自身が
到達した最終品質（DRC 0違反・短絡5件、同じく高ファンアウト保護ネット
絡み、design_notes §48）と同種であり、件数としてはむしろv7を上回る**
（3件 < 5件）。ウォーキングワン化・sda_oe極性反転・TAP5列化という
V8の3つの変更を経てなお、初回の反復（FORCE_JOG_NETS 1回の追加のみ）
でv7同等以上の配線品質に到達できたことになる。

**保留・要ユーザー判断**: この3件をさらに追い込む（v7の`sections
47/48`のように、GDSレベルでの手動セグメント修正まで踏み込む）か、
このv7同等の状態を「V8の暫定配線結果」として受け入れ、STEP6
（トップピン引き出し）・STEP7（チャネル圧縮）・DRC/LVS検証へ進むか、
ユーザーに確認する。

**ユーザー判断**: 「一旦止めます」——今回のセッションはここまでとし、
STEP6/7・DRC/LVS検証・IRSIM再検証は次回以降に持ち越す。

### 77.13 このセッションのV8到達点まとめ（配置配線を一旦保留）

`layout/step8/`に保存済みのチェックポイント（v7の`steps_v7_v2/`と
同じ命名規則）:

- `v8_step_1_tap_power_mesh.gds` — 配置＋TAP電源メッシュ（5列、
  行幅2214.0um、コアBBOX 2214.0×2288.8um）
- `v8_step_2_routed_raw.gds` — 初回配線（FORCE_JOG/PER_ROW_LOCALなし）、
  DRC 0違反・短絡6件
- `v8_step_2b_routed_fjog.gds` — FORCE_JOG_NETS適用後、DRC 0違反・
  短絡3件
- `v8_step_3_ripup_reroute.gds` — ripup_reroute適用後（自動修正0件、
  想定通り）、**現時点の最終到達点**: DRC 0違反・短絡3件
  （v7の最終品質「DRC 0・短絡5件」と同種、件数はv7より少ない）

**未着手（次回以降の候補、design_notes §77.4ステップ3の残り）**:
- STEP6相当（トップレベルポートのBBOX端引き出し、
  `route_top_pins_nrow_fm.py`）
- STEP7相当（チャネル空き領域の圧縮、`squeeze_channels_nrow_fm.py`）
- 残り短絡3件の追い込み（手動セグメント修正、要検討）
- DRC/LVS検証（77.4ステップ4）
- IRSIM検証（77.4ステップ5、実TR-1um.prm下でのフルスイート再実行）

V8全体の到達点（このセッション終了時点）: RTL修正（ウォーキングワン化+
sda_oe極性反転、§77.2/77.3/77.7）→ Verilog検証（iverilog+MyHDL、
§77.8/77.9末尾）→ NET合成（Yosys、DFFRB×37/DFFS×0、§77.9/77.10）→
配置配線 STEP1〜3（TAP5列化、v7同等以上の品質、§77.11/77.12）まで完了。

### 77.14 DFFSセルの物理レイアウト追加を受け、DFFS許可合成へやり直し

ユーザーが`LEF/`配下に`DFFS.sch`/`DFFS.sym`を作成し、
`TR-1um_STDCELL.gds`にDFFSの物理レイアウトを追加（bbox 64.8×64.8um、
行高グリッドに整合、ピン D/CK/SET/Q/QB=M2PIN×5、vdd/gnd=M1PIN×2、
klayoutで確認済み）。これに伴い「DFFSは使わない」制約（§77.9脚注相当）
を撤回し、DFFSを許可した合成へやり直す。

- `TR1um_5_stdcell.lib`: 既存の`cell(DFFS){area:8; ff(IQ,IQN){
  clocked_on:"CK"; ...}}`定義がそのまま使えることを確認、Liberty側の
  変更は不要。
- `script/gen_lef.py`: `PIN_META`辞書に`"DFFS"`エントリを追加
  （D/CK/SET=INPUT、Q/QB=OUTPUT、+電源ピン）。実行して
  `LEF/TR-1um_STDCELL.lef`を再生成、`MACRO DFFS`が追加されたことを
  grepで確認。
- Yosys合成コマンドの`dfflegalize`制約を`-cell $_DFF_PP0_ 0`のみから
  `-cell $_DFF_PP0_ 0 -cell $_DFF_PP1_ 0`（SET付きDFFSタイプも許可）
  に変更してユーザーに提供、ユーザーが自環境（native yosys）で実行し
  成功（"dump出来ました"）。
- 結果: `src/i2c_slave_async_net_v8.v`（生合成出力、174セル）が
  36×DFFRB + 1×DFFS に変化（DFFS禁止版は37×DFFRB + 0×DFFS）。
  組合せゲート構成もABCの合成経路依存でDFFS禁止版と大きく異なる
  （AND2_X1: 41 vs 13、NOR2: 9 vs 39 等）。
- 後処理（`insert_row_buffers.py`→`insert_bufth_scl_sda.py`）を
  DFFS許可ネットリストにも適用し、
  `src/i2c_slave_async_net_v8dffs.v`（184セル: DFFRB36+DFFS1+BUFTH2+
  BUF_X1 8+組合せ133）を生成。

### 77.15 DFFS許可ネットリストの配置配線 — DFFSなし版よりも行幅を要し、短絡もむしろ増加

DFFS許可版の配置（`row_assignment_v8dffs*.json`、
`placement_nrow_fm_v8dffs.json`）→ 配線を実施。

- 初回`TAP_INTERVAL_TRACKS=100`（DFFSなし版で採用した値）で試したところ、
  `SystemExit: no clear X found for row 1 near x=2151.9 (net='rst_n')`
  でルーティングが行き詰まり。DFFS許可版の方がゲート構成・配置密度が
  異なり、DFFSなし版で確保した配線余裕（2214.0um幅）では不足すると判断。
- `TAP_INTERVAL_TRACKS`を100→**115**（行幅2538.0um）へさらに拡張し、
  チャネル高さ目標も比例して拡大（100トラック版比+15%程度）。これで
  ルーティング完走（STEP1: `v8dffs_step_1_tap_power_mesh.gds`、STEP2:
  `v8dffs_step_2_routed_raw.gds`、DRC 0違反・短絡6件）。
- FORCE_JOG_NETS（`196.4,693.2,623.0,590.6,228.8`——短絡に絡んだネット
  座標から特定）を指定して再配線（`v8dffs_step_2b_routed_fjog.gds`）。
  DRC 0違反維持、短絡6→**5件**に減少（DFFSなし版は同種の対策で
  6→3件まで減少しており、効果はDFFSなし版より弱い）。
- `ripup_reroute_shorts.py`を実行（`v8dffs_step_3_ripup_reroute.gds`）
  ——5件全てmulti-segment/high-fanout保護ネット同士の衝突のため
  自動修正0件（想定通り、v7/V8DFFSなし版と同じパターン）。
- `verify_connectivity_nrow_fm.py`で最終GDSを独立検証、**DRC 0違反・
  SHORT SUSPECTED 5件**を確認（内訳: `_095_`↔`rx_data_r[4]`、
  `rst_n`↔`_095_`、`_091_`↔`rst_n`、`scl_n_row3`↔`txreg[3]`、
  `scl_row3`↔`_091_`——いずれも`scl_row3`/`scl_n_row3`/`rst_n`という
  高ファンアウト保護ネットが絡む、既知パターンと同種）。

**DFFSなし版との比較（現時点）**:

| 項目 | DFFSなし版（§77.11/77.12、採用済み） | DFFS許可版（本節） |
|---|---|---|
| DFF構成 | DFFRB×37, DFFS×0 | DFFRB×36, DFFS×1 |
| セル数（後処理後） | 186 | 184 |
| 行幅 | 2214.0um（TAP_INTERVAL_TRACKS=100） | 2538.0um（同115、+14.6%） |
| DRC違反 | 0 | 0 |
| SHORT SUSPECTED | 3件 | 5件 |

DFFS許可版は、ユーザー追加のDFFS物理セルを使うことでFF1個分の面積・
配線資源を節約できる期待があったが、実際には**行幅を14.6%広げても
短絡が3件→5件へ悪化**しており、現時点ではDFFSなし版（§77.12の
`v8_step_3_ripup_reroute.gds`）の方が配線品質・面積の両面で優れる。
原因はABC合成経路の違いによる組合せゲート構成の変化（DFFS許可版は
AND2_X1が3倍以上に増加）が配置密度・配線混雑パターンを変えたためと
推定されるが、未確証。

**要ユーザー判断**: (a) DFFS許可版に対して追加のFORCE_JOG調整・
手動セグメント修正でDFFSなし版に追いつけるか試すか、(b) DFFSなし版
（§77.12到達点）をV8の正式版として採用し、DFFSは今回見送るか、
(c) 他の方針、を次回セッションで確認する。

**ユーザー判断**: 「DFFSナシ版で短絡を解決ください」——DFFS許可版は
一旦保留し、DFFSなし版（`v8_step_3_ripup_reroute.gds`、短絡3件）の
残り短絡を解消する方針が確定。次節§77.16に対応。

### 77.16 DFFSなし版の残り短絡3件を手動修正 — DRC 0・短絡0で完全収束

`ripup_reroute_shorts.py`が「両側とも複雑ネット（高FO/per-row-local
スパイン）」として自動修正を拒否した残り3件（§77.12参照: `scl_row0`
↔`_177_`、`scl_n_row1`↔`scl_row1`、`sda_in_buf`↔`scl_row0`）について、
KLayout Python APIで実ジオメトリを直接調査し、根本原因を特定した上で
手動修正した（新規スクリプト`script/fix_v8_remaining_shorts.py`）。

**調査手法**: `net_shapes_*.json`（ルーターの描画ログ）だけでなく、
`klayout.db`で実際にGDSに描かれているM1/M2形状を直接クエリし、
via_1 PCellの自前パッドやセル自身のピン形状（どちらも`net_shapes.json`
には記録されない）まで含めて衝突点を特定。3件とも「2つの異なるネットが
全く同じX列（垂直M2ラン）または同じ track_y（水平M1トランク）を
使っていた」という、文字通りの座標重複であることを確認した。

**各短絡の根本原因と対策**:

1. **`scl_row0`↔`_177_`**（X=881.2列でのM2重複、Y743.8〜830.2が重複）
   ：`_177_`はチャネル境界ごとに3分割されて`net_shapes.json`に記録
   されているが実体は継ぎ目なしの1本の縦配線（Y77.6→830.2）。この
   3片をまとめて1本として扱い、`ripup_reroute_shorts.py`の
   `Fixer.try_fix_vertical`と同じライブ衝突チェック
   （`clear_excluding`/`via_pad_clear`/`find_own_trunk`）でX=899.1列
   （+3トラックピッチ）へ丸ごと移設。`scl_row0`側は一切変更せず。

2. **`scl_n_row1`↔`scl_row1`**（X=989.2列でのM2重複、Y1984.4〜2022.2
   が重複）：`scl_row1`も同様に3分割記録されている1本の縦配線
   （Y1107.6→2022.2）。同じ手法でX=985.5列（-1トラックピッチ）へ
   丸ごと移設。`scl_n_row1`側は変更せず。

3. **`sda_in_buf`↔`scl_row0`**（M1トランクが全く同じtrack_y
   451.3〜453.1で重複、X710.1〜1017.9が重複）：`Fixer.try_fix_horizontal`
   （トランク+全スタブ+全viaをチャネル内の別track_yへ丸ごと再配置）を
   試みたが、`scl_row0`・`sda_in_buf`いずれの側も失敗——原因を追跡した
   結果、両ネットとも「行内部（セル本体）を貫通するピン引き下ろし線」
   を1本ずつ持っており、その行内部分は`route_channels_nrow_fm.py`の
   pass 0が元々**一切ライブチェックしない**設計（design_notes記載の
   既知の前提）のため、**未移動のまま**でも`M2_MIN_GAP`マージン付き
   判定では最初から不合格（実際の物理的重なりは無い——マージン無し
   判定は合格）。`try_fix_horizontal`は移動先候補ごとに「変化しない
   行内部分」まで含めた矩形全体を毎回再チェックするため、target
   track_yに関わらず常に同じ理由で失敗していた。
   **対策**：`try_fix_horizontal_incremental`（本セッション新規実装、
   `fix_v8_remaining_shorts.py`内）——M1トランク自体は従来通り全体を
   チェックするが、各M2スタブについては「旧track_yと新track_yの間の
   差分帯」のみをライブチェックし、変化しない遠端（実ピン側）は
   再検証しない、という設計に変更。`scl_row0`側のトランクを
   track_y=452.2→490.0（+7トラックピッチ、チャネル1内）へ移設して解決。

**結果**：`layout/step8/v8_step_4_manual_short_fix.gds`を生成し、
`drc_check_nrow_fm.py`・`verify_connectivity_nrow_fm.py`で独立検証：

```
DRC: M1/M2 width・space違反 0、V1関連違反 0
接続性: 194ネット・616ピン全チェック — ALL NETS FULLY CONNECTED,
        NO SHORTS DETECTED
```

**DRC 0違反・短絡0件で完全収束**——v7の最終品質（短絡5件）はもちろん、
§77.12時点のV8暫定結果（短絡3件）も上回り、この設計で初めて配線が
完全収束した。V8（DFFSなし版）を正式なV8配線結果として確定する。

`v8_step_4_manual_short_fix.gds`が現時点のV8最終配線成果物、
対応する`script/pin_map_nrow_fm_v8final.json`・
`script/net_shapes_nrow_fm_v8final.json`が最新の座標台帳。

未着手（次工程、→§77.17〜77.19で着手）：STEP6相当（トップピン引き出し）・
STEP7相当（チャネル圧縮）・DRC/LVS検証（77.4ステップ4）・IRSIM検証
（77.4ステップ5）。

### 77.17 STEP6（トップピン引き出し）実施・検証

`route_top_pins_nrow_fm.py`をV8向けパラメータ
（`placement_nrow_fm_v8.json`、`v8_step_4_manual_short_fix.gds`、
CH_HEIGHTS=[174.8,601.4,542.0,515.0,196.4]）で実行し
`layout/step8/v8_step_6_top_pins_routed.gds`を生成。23ポート中10ポート
（tx_data[0/1/2/3/4/5/6]・sda_oe・scl・sda_in）が`[CHECK]`
（内部ライブチェックで安全なXを確定できずフォールバック）と報告された。

これらは全て`pin_map`に登録されない「スタブネット」（内部セル1ピンのみ、
`net_shapes_log`が2ピン未満のネットを記録しない設計のため元々未追跡）
であることを確認。`verify_connectivity_nrow_fm.py`はM1レイヤーの
ピンしか探索しないため、M2のみで存在するこれら10ポートを検証できない
（"PIN NOT FOUND ON M1"）という盲点も判明——`locate()`をM1優先・M2
フォールバックへ拡張した`verify_connectivity_nrow_fm_m1m2.py`を新規作成。
10ポート分の新規ピン座標を`pin_map`に手動追加した拡張版で再検証した結果、
**204ネット・626ピン全チェックでALL NETS FULLY CONNECTED, NO SHORTS
DETECTED**、DRCも0違反を確認。STEP6は完了・検証済み。

### 77.18 STEP7（チャネル圧縮）— 3段階の根本原因を特定して完全解消

ユーザー指示「圧縮はマストです」を受け、圧縮を機能させるまで原因究明を
継続。最終的に**3つの独立したバグ**が複合していたことが判明（新規
ドライバ`run_v8_step7_squeeze_step6_toppins.py`、および共有モジュール
`squeeze_channels_nrow_fm.py`の恒久修正として記録）：

**背景**：v7の`run_route_v7_step7_squeeze.py`は`compaction_info`
（guard_idx+used_x）をroute_channels_nrow_fm.py自身の
`compaction_info_path`ダンプから取得していたが、V8の実配線は複数回の
アドホックな再試行＋本セッションの手動短絡修正3件を経て組み上がって
おり、単一の対応するダンプが存在しない（かつ手動修正はダンプ取得後の
track位置を動かしている）。そこで`v8_step_4_manual_short_fix.gds`の
**実ジオメトリから直接**compaction_infoを導出する方式に変更
（`build_empirical_compaction_info()`：M1トラックの実使用有無を
スキャンし、幅30um以上のトランクの隣接indexをguard化）。

**バグ1（ユーザー発見の1.243umギャップ・M2 Smin違反224件の主因）**：
`squeeze_channels_nrow_fm.py`の`budget`計算式は`TRACK0_OFFSET(2.0) <
TRACK_PITCH/2(2.7)`という既知の構造（v7 design_notes 47/48で一度発見
済みの同種問題）により、**チャネル境界に最も近いindexのスライスが
チャネルの真の境界を最大2.7um超えて隣接ROWへはみ出す**。この超過分を
スキャンすると、隣接ROW内部のセルピン形状や行またぎスパイン配線を
「チャネル内容」として誤検出し、`build_y_map()`のROW境界での
恒等写像（傾き1）区間を汚染する。

ユーザーの指摘（「エラーはすべてM2配線とSTDCELLのM2PINとの間の
スリット」）を手がかりに、KLayoutで実座標を追跡し確定：あるDFFRBの
物理ピン（セルインスタンスごと剛体並進で移動）と、そのピンに接続する
はずのトップレベル配線（自分のボックス端点を個別に`y_map()`で変換）が、
**同一の旧Y座標（1479.1um）に対し異なる新Y（919.6 vs 920.843、
差1.243um）を返す**という直接証拠を得た——これがユーザー発見の
ギャップの正体そのもの。

**バグ2（OFFGRID 0.050違反340件、`DRC7_error.lyrdb`で発覚）**：
簡易チェッカー（`drc_check_nrow_fm.py`）はOFFGRIDを検査していないため
見落としていたが、ユーザーが実DRCデック（`drc.lydrc`）で確認した
`.lyrdb`から発覚。`y_map()`の浮動小数点累積（170以上のブレークポイントに
わたる`cur_new +=`の繰り返し）が実製造グリッド0.05umからわずかに
ドリフトする（例：920.843um、0.05の倍数ではない）。`um_map()`に
0.05umグリッドへの明示スナップを追加して解消。

**バグ3（バグ1修正の副作用：`scl_row3`が13分割、NET SPLIT）**：
バグ1の初回対策として、はみ出すindexを`used_x`収集から完全に除外
（skip）したところ、`scl_row3`（行またぎスパインを持つ高FOネット）が
チャネル境界ぎりぎりに実配線を持っていたケースで、その実配線ごと
「空き」と誤認され圧縮対象になり、ネットが13個の断片に分裂。
**真の修正**：indexを丸ごと除外するのではなく、**スキャン窓をチャネルの
真の境界でクリップ**（`clip_lo=max(slice_lo,band_lo)`、
`clip_hi=min(slice_hi,band_hi)`）する方式に変更——ROW内部のみの
コンテンツ（ピン等）は除外されつつ、境界ぎりぎりに存在する正当な
チャネル内配線は引き続き検出される。さらに`build_y_map()`本体
（`squeeze_channels_nrow_fm.py`、共有モジュール）の各trackスライス
計算にも同じクリップを追加——これが実質的な**根本修正**（バグ1の
1.243umギャップも、このクリップにより完全解消）。

**結果**：3バグ修正後、`run_v8_step7_squeeze_step6_toppins.py`を
再実行し、以下を確認：

```
sanity（圧縮前）  : DRC 0/0/0、短絡0
squeezed          : DRC 0/0/0、短絡0
squeezed+top-pins : DRC 0/0/0、短絡0
OFFGRID           : M1/M2とも0件（別途直接検証）
```

コア高さ: 2288.8um → **1333.0um（-41.8%）**。

STEP6（トップピン引き出し、10件のスタブネット含め拡張connectivity
チェックで検証）とSTEP7（チャネル圧縮）を統合した最終成果物は
`layout/step8/v8_step_8_squeezed_top_pins_routed.gds`。

変更ファイル一覧：
- `script/squeeze_channels_nrow_fm.py`（共有モジュール、恒久修正）：
  `um_map()`に0.05umグリッドスナップ追加、`build_y_map()`の
  track-slice計算をチャネル境界でクリップ。
- `script/run_v8_step7_squeeze_step6_toppins.py`（新規）：V8向け
  empirical compaction_info導出＋squeeze＋STEP6を統合したドライバ。
  `build_y_map_with_channel_heights()`（チャネル高さ再計算の
  y_map再クエリ起因の丸め誤差バグの回避、design_notes 77.18バグ1と
  同系統だが個別に発見・修正）を含む。
- `script/verify_connectivity_nrow_fm_m1m2.py`（新規）：M1優先・M2
  フォールバックの拡張connectivityチェッカー。

### 77.20 VDD/VSSトップピン追加（TAPセルのM2/M1 BBOX端）

ユーザー指示（原文）：「TOPピンをつける時にTAPのM2のBBOX端とM1の
BBOX端にVDD/VSSのピンを追加ください」。`v8_step_8_squeezed_top_pins_
routed.gds`（STEP6+7完了後の最終成果物）に対し、新規スクリプト
`script/add_power_pins_v8.py`で実装。

**構造調査（実GDSクエリで確認、LEFの記載のみに頼らず）**：
TAPカラムは5列（gap配置に対応）。各列に垂直M2ストラップが2本あり、
どちらもコア高さ全体（Y=0～core_h=1333.0）を1本の連続ポリゴンとして
貫通していることを直接確認：
- 列内 低X側ストラップ（col_x0+1.0～+4.4）= **GND**
- 列内 高X側ストラップ（col_x0+6.4～+9.8）= **VDD**
（TAP2単体のLEF記載だけでなく、5列全てで個別に連続性検証済み——
行ミラーリングでLEF上のGND/VDDが物理的に入れ替わる可能性を排除する
ため。）

M1側は連続レールではなく、行境界チャネルごとに独立したvia接続パッド
（8箇所/列、GND/VDD交互）——BBOX左右端（X=0, X=row_width=2214.0）に
実際に接するのは**最左列（col_x0=0.0）と最右列（col_x0+10.8=row_
width）の2列のみ**。中央3列のM1パッドはどちらの端にも触れない。

**実装**：
- M2ピン（上下BBOX端）：5列×2ネット×2端＝**20個**、Y=0/core_hの
  すぐ内側（PIN_SIZE_UM=3.0центр）に配置。中央3列も含め全5列。
- M1ピン（左右BBOX端）：最左列8個＋最右列8個＝**16個**、各via接続
  パッドのY位置に対応。ネット判定はvia位置から直接導出（Y位置からの
  推定ではなく、via下のストラップX位置で判定——行ミラーリングに
  対しても頑健）。
- レイヤ/ラベル規約は`route_top_pins_nrow_fm.py`の信号ピンと同一
  （M1PIN(48,1)/M2PIN(49,1)/TXM1(48,0)/TXM2(49,0)、ラベルはピン
  中心に配置）。

**検証**（`v8_step_8...`→`layout/step8/v8_step_9_power_pins_added.gds`）：
```
DRC              : M1/M2幅・スペース違反0、V1関連違反0（drc_check_nrow_fm.py）
信号net connectivity: 194net/616pin、全結線・短絡0（既存pin_map、m1m2版で確認）
電源net connectivity: 追加した36個の電源ピン全てが金属上に着地、
                      VDD側が1連結成分・GND側が1連結成分、
                      VDD/GND間の共有成分＝0（独自Union-Findで検証）
```
全て問題なし。この時点で残タスクは実DRC/LVS・IRSIM検証のみ。

変更ファイル：
- `script/add_power_pins_v8.py`（新規）。
- `layout/step8/v8_step_9_power_pins_added.gds`（新規、最終成果物）。

### 77.21 v7幅への行幅圧縮の実現可能性検証（N_GAPS=2, N_GAPS=3案の検討）

ユーザー指摘：「V8のBBOXがv7比+36.4%/面積+84.0%で大きすぎる。幅をv7と
同じにして再配置配線できるか検証してほしい」。

**過去ログの確認**：design_notes §77.11に、TAP_INTERVAL_TRACKS=73
（N_GAPS=4、行幅1630.8um——v7の1632.6umとほぼ同一）が実際に試され、
`route_channels_nrow_fm.py`が`SystemExit`（"no clear X found in
overlap zone"）で異常終了していたことを確認。その後TAP_INTERVAL_
TRACKS=100（行幅2214.0um）まで拡張して初めて配線が完走している。

**ユーザー判断**：電源メッシュをv7と同じN_GAPS=2（3TAP列）に戻して
再検証。

**検証方法**：`src/i2c_slave_async_net.v`（raw、175セル）から
`insert_row_buffers.py`→`insert_bufth_scl_sda.py`を再実行し、
186インスタンスの整合したnetlist/row_assignmentペアを`outputs/`に
再生成（既存の`src/i2c_slave_async_net_v8.v`が実際には174セルの
rawネットリストのままであり、design_notes記載の186セル版と一致して
いなかったため——原因未特定、要調査だが今回の検証には影響しない
スクラッチ領域での再生成で対応）。`gen_placement_nrow_fm.py`の
`N_GAPS`/`TAP_INTERVAL_TRACKS`/`TARGET_ROW_WIDTH_UM`と
`route_channels_nrow_fm.py`の`ROW_WIDTH_UM`をモジュール属性上書きで
変更し、STEP1（配置）→STEP2（配線、FORCE_JOG_NETS/PER_ROW_LOCAL_NETS
なしの素の初回試行）を複数の行幅で試行。

**結果**：

| N_GAPS | TAP_INTERVAL_TRACKS | 行幅 | STEP1(配置) | STEP2(配線) |
|---|---|---|---|---|
| 2 | 147（v7と同一） | 1620.0um | OK | **失敗**（SystemExit） |
| 2 | 180 | 1976.4um | OK | **失敗**（SystemExit） |
| 2 | 190 | 2084.4um | OK | **失敗**（SystemExit） |
| 2 | 200 | 2192.4um | OK | **成功**（DRC 0違反、素の初回試行でSHORT SUSPECTED 8件——ただし全て1本の連鎖ネット群、v7/V8の過去の初回結果と同等） |

**結論**：v7と同じ幅（1620.0um）はもちろん、その後2084.4umまで
拡張してもN_GAPS=2では配線が完走しない。配線可能な最小行幅は
2084.4um〜2192.4umの間で、現行V8（N_GAPS=4、2214.0um）とほぼ同じ
（差は最大でも約1.7%）。**電源メッシュのTAP列数（N_GAPS）を2に
戻しても、行幅はほとんど縮まらない**——v7比+36.4%という行幅の
拡大は、TAP列数の増加（電源メッシュ強化）が主因ではなく、V8の
ネットリスト自体がv7より大きい（インスタンス数186 vs 154、
+20.8%）ことに起因すると判断できる。

したがって、v7と同じ幅への圧縮は現実的な選択肢ではない。N_GAPS=2
への変更も、電源メッシュを弱める代償に対して得られる行幅縮小効果が
（約1.7%以下と）ごくわずかであり、割に合わない。現行のV8ベースライン
（N_GAPS=4、TAP_INTERVAL_TRACKS=100、行幅2214.0um、
`layout/step8/v8_step_9_power_pins_added.gds`）を維持するのが妥当。

検証に使ったスクリプト・中間生成物は全て`outputs/`スクラッチ領域
（`script/test_ngaps2_placement.py`、`script/test_ngaps2_route.py`
はリポジトリに残置——再現用）で完結しており、既存の採用済み成果物
には一切影響していない。

### 77.22 行数5（N_ROWS=5）への変更を検証 — 幅の大幅削減に有効

ユーザー指示：「行数を5に増やして検討できますか」。N_GAPS=4（現行の
電源メッシュ強度）は維持したまま、行数のみ4→5に変更して再検証。

**下準備**：`fm_partition.py`の`fm_multiway_partition()`は既に
`n_rows`を2の累乗に限定しない一般化版（コメントに明記）だったため、
`gen_placement_nrow_fm.py`/`insert_row_buffers.py`/`insert_bufth_scl_
sda.py`の`N_ROWS`定数（いずれもモジュール属性）を5に上書きするのみで
対応可能と判明。`route_channels_nrow_fm.py`/`gen_placement_gds_nrow_
fm.py`はplacement JSONの`len(rows)`からn_rowsを動的に取得しており、
そもそも変更不要（ソース確認済み）。`src/i2c_slave_async_net.v`
（raw、175セル）から行バッファ+BUFTH挿入を再実行し、N_ROWS=5用の
186インスタンスnetlist/row_assignmentペアを新規生成（行内訳
44/47/27/27/38）。

**検証**（N_GAPS=4のまま、TAP_INTERVAL_TRACKS=80→行幅1782.0um、
チャネル高さ暫定[130,380,380,380,380,150]→スナップ後[131.6,380.0×4,
153.2]、素の初回試行）：

```
STEP1（配置）  : OK
STEP2（配線）  : 完走（SystemExitなし）
DRC            : 0違反
接続性         : 18件SHORT SUSPECTED（複数ネットが連鎖した数本の
                 短絡グループ——N_ROWS=4の初回試行でも6件あり、
                 FORCE_JOG_NETS一巡で3件まで削減できた実績あり）
チャネル使用率 : ch1〜4いずれも70/70トラック（予算ぴったり——暫定
                 高さ380umは切り詰めすぎの可能性、余裕を持たせれば
                 短絡もさらに減る見込み）
```

**寸法比較**（いずれも圧縮=STEP7前の素の値）：

| 構成 | 行幅 | コア高さ | 面積 |
|---|---|---|---|
| N_ROWS=4（現行V8、N_GAPS=4、圧縮前） | 2214.0um | 2288.8um | 5,067,443um² |
| N_ROWS=5（本検証、N_GAPS=4、圧縮前） | **1782.0um（-19.5%）** | 2128.8um（+... 実際は行1本+チャネル1本分の増加を含むが暫定値のため未最適化） | **3,793,522um²（-25.1%）** |

行幅・面積とも圧縮前の時点で大幅に縮小しており、N_GAPS=2実験
（幅ほぼ変化なし）とは対照的に**行数を増やす方が明確に効く**ことを
確認。チャネル高さはSTEP7圧縮で通常40%前後圧縮できる実績があるため
（N_ROWS=4では-41.8%）、最終的な高さはさらに縮む見込み。

**現状の到達点と残課題**：これはあくまで素の初回試行（FORCE_JOG_
NETS等のチューニング前）のフィージビリティ確認であり、実際にこの
構成を採用するには、(1) チャネル高さの適正化（380um固定は暫定値、
使用率ぴったりのため余裕を持たせて再試行）、(2) 短絡18件の
FORCE_JOG_NETS等による削減、(3) STEP3リップアップ・STEP6/7・
DRC/LVS・IRSIM再検証、という残り作業が必要。検証に使ったスクリプト・
中間生成物は全て`outputs/`スクラッチ領域（`script/test_nrows5_
route.py`はリポジトリに残置——再現用）で完結しており、既存の採用済み
成果物には一切影響していない。

### 77.23 行幅を厳密に1620.0um（5.4×300）に固定して検証 — N_GAPS=2では行数を増やしても解決しない

ユーザー要件：「OSS_FRAME_GIOに入れるために幅は1620（5.4x300）に
押さえてください」——BBOX幅はv7に近づけるという目安ではなく、
**フレーム接続のためのハード制約**として厳密に1620.0umが要求される
ことが判明。

**1620.0umに整数トラックで割り切れるのはN_GAPS=2のみ**：
`TARGET_ROW_WIDTH_UM = (N_GAPS+1)*10.8 + N_GAPS*TRACKS*5.4`に1620.0を
代入すると、N_GAPS=2はTRACKS=147（v7と同一値）でちょうど割り切れるが、
N_GAPS=3は97.33、N_GAPS=4は72.5と非整数になる（gen_placement_nrow_fm.
py既存コメント通り）。そのためN_GAPS=2固定で行数（N_ROWS）を4→5→6→8
と増やしながらSTEP1+STEP2を試行。

`insert_bufth_scl_sda.py`に`n_scl_a == 4`というN_ROWS=4決め打ちの
assertionを発見・修正（`n_scl_a <= N_ROWS`に一般化、N_ROWS非依存で
再利用可能に）。

**結果**（いずれもN_GAPS=2、TAP_INTERVAL_TRACKS=147、行幅1620.0um
ちょうど、素の初回試行）：

| N_ROWS | STEP1(配置) | STEP2(配線) |
|---|---|---|
| 4 | OK | 失敗（SystemExit） |
| 5 | OK | 失敗（SystemExit） |
| 6 | OK | 失敗（SystemExit） |
| 8 | OK | 失敗（SystemExit） |

いずれの行数でも`forced overlap zone`が行幅のほぼ全域にまたがって
発生し、`draw_jog could not find a track`/`no clear X found`で
異常終了。チャネル使用率は常に予算に対して大幅な余裕あり（例:
N_ROWS=6で最大26/70トラック）——**チャネル高さや行数（Y方向の余裕）
は全くボトルネックになっていない**。

**結論**：N_GAPS=2（TAP列3本）では、行数をいくら増やしても1620.0um
では配線が完走しない。原因は各ギャップ内のジョグ用FILL回廊が
TAP列3本分（ギャップ2箇所）しかなく、X方向の迂回経路が構造的に
不足しているため——これはセル詰め込み（パッキング）やチャネル高さの
問題ではなく、**TAP列数（N_GAPS）そのものに起因する配線トポロジー上の
制約**であり、行数を増やす方向のアプローチでは解決しない。

一方、N_GAPS=4（TAP列5本）はN_ROWS=5・行幅1782.0umで素の初回試行が
完走済み（77.22）——N_GAPS=4の方が配線自体は成立しやすいが、1782.0um
は1620.0umより大きく、かつ1620.0umはN_GAPS=4では整数トラックで
割り切れない。

**今後の選択肢**（ユーザー判断待ち）：
1. `gen_placement_nrow_fm.py`のギャップ幅を「全ギャップ一律
   TAP_INTERVAL_TRACKS」から「ギャップごとに異なるトラック数を許容
   （合計だけを1620.0umに一致させる）」よう改修し、N_GAPS=4のまま
   （例: 72/72/73/73トラックの組み合わせ、合計290トラック×5.4+
   54.0=1620.0um）1620.0umちょうどを狙う——コード変更が必要
   （`pack_row_distributed`のギャップ幅前提、`route_channels_nrow_
   fm.py`側のTAP列X位置計算の対応も要見直し）。
2. OSS_FRAME_GIO側の制約に幅1620.0umちょうど以外の許容（例えば
   1620um以上であれば良い等）がないか確認。
3. その他、ユーザーの想定するアプローチ。

検証はすべて`outputs/`スクラッチ領域で完結（`script/test_
fixed1620_route.py`はリポジトリに残置——再現用）。`insert_bufth_
scl_sda.py`のassertion一般化のみ、リポジトリの恒久修正として反映
済み（既存の採用済み成果物には影響なし——V8ベースラインはN_ROWS=4
で生成されており、この修正は該当コードパスを通らない）。

### 77.24 v4回路（bit_cnt+last_bit_pending）実装 — 行幅1620.0umちょうどで配線成功

ユーザー承認（前節の回路提案に対し「はい、進めてください」）を受け実装。

**RTL変更**（`src/i2c_slave_async.v`、`script/i2c_slave_async_model.py`
のMyHDLツインモデルも同期更新）：8bitウォーキングワン`bit_walk`を廃止し、
3bit二値カウンタ`bit_cnt`＋新規1bitレジスタ`last_bit_pending`
（`bit_cnt==6`を1エッジ前倒しで先読み登録）に置換。SCL negedge側の
TXビット選択も`bit_walk[7]`/`txreg&bit_walk`のone-hot方式から元の
`bit_cnt==0`/`txreg[7-bit_cnt]`算術インデックスへ復元。

**機能検証**：MyHDLモデル（`script/test_i2c_slave_async.py`/
`test_i2c_slave_async_negative.py`、iverilogがサンドボックスに無いため
MyHDLの自前イベント駆動シミュレータで代替）で、WRITE/READ/誤アドレス
NACKの全シナリオがPASS（RTLレベルの機能等価性を確認——実チップでの
同一エッジレース解消そのものは別途IRSIM/SPICE検証が必要、77.19節に
継続記載）。

**合成結果**（yowasp-yosys、`i2c_slave_async`単体）：DFFRB
37個→**33個**（-4個）、セル総数175個→158個（-9.7%、ABCが新規に
選択したAND3_X1/AND4_X1/XOR2/XNOR2はTR1um_5_stdcell.libには定義済み
だが共有GDSライブラリ`TR-1um_STDCELL.gds`に未マージのため、
一時的にこれら4セルを除外したliberty版でABCを実行——77.25節に
恒久対応の注記）。行バッファ+BUFTH挿入後の最終インスタンス数も
186個→**168個（N_ROWS=4）/169個（N_ROWS=5,6）**（約-9.7%）。

**行幅1620.0umちょうどでの配置配線再検証**（N_GAPS=2、TAP_INTERVAL_
TRACKS=147、素の初回試行）：

| N_ROWS | STEP1(配置) | STEP2(配線) |
|---|---|---|
| 4 | OK | 失敗（旧186インスタンス版と同じ壁、ただし警告数は大幅減） |
| 5 | OK | 失敗 |
| 6 | OK | **成功**（DRC 0違反、SHORT SUSPECTED 14件——連鎖した数個の
    グループ、v7/旧V8の初回試行と同程度でFORCE_JOG_NETSでの
    削減が見込める） |

**結論**：ユーザー要件「幅を1620（5.4×300）に押さえる」を、行数を
6に増やすことで**達成**。v4回路（DFF-4個）だけでは1620um・N_ROWS=4/5
では足りなかったが、N_ROWS=6と組み合わせることで初めて配線が完走した
——77.23節で確認した「N_GAPS=2はTAP列数由来のジョグ回廊不足で行数を
増やしても解決しない」という結論は、旧186インスタンスの重いネット
リストでは正しかったが、v4のDFF削減で正味の配線需要が下がったことで
再びN_ROWS拡大が効くようになった、と解釈できる。

現時点はあくまで素の初回試行のフィージビリティ確認であり、実際に
採用するには以下が残る：(1) チャネル高さの適正化（暫定380um固定を
使用率に基づき調整）、(2) 短絡14件のFORCE_JOG_NETS等による削減、
(3) STEP3リップアップ〜STEP6/7〜DRC/LVS・IRSIM再検証一式、
(4) V8全体（`i2c_slave_async_nrow_fm`トップレベル）のネットリスト・
placement/routeパイプラインをこの新net_v4系に切り替える本実装。

検証は全て`outputs/`スクラッチ領域で完結（`script/test_v4_1620_
route.py`はリポジトリに残置——再現用）。恒久的にリポジトリへ反映した
のは：`src/i2c_slave_async.v`（v4本体）、`script/i2c_slave_async_
model.py`（MyHDLツイン）、`src/i2c_slave_async_tb.v`（ヘッダコメント
のみ）、`script/insert_bufth_scl_sda.py`（N_ROWS一般化assertion、
77.23で実施済み）。

### 77.25 V9パイプラインSTEP3（短絡修正）着手 — 高FOバックボーンネット群の連鎖短絡が残存

77.24の成功構成（N_GAPS=2・TAP_INTERVAL_TRACKS=147・行幅1620.0um・
N_ROWS=6）を正式にSTEP1〜3へ進めた。

**`ripup_reroute_shorts.py`（自動修正）**：素の初回配線（14件短絡）
に対して実行 → **0件自動修正**（14件全てが`busy`/`rst_n`/
`start_pulse`/`scl_row*`/`scl_n_row2`/`sda_in_buf`等、multi-segment/
high-fanoutとして保護されたネット同士の衝突——v7/旧V8と同じ保護
ルールに阻まれるパターン）。

**`FORCE_JOG_NETS`（14件の短絡関与ネット全て）を指定して再配線**：
14件→**12件**に減少（`bit_cnt[1]`↔`_006_`の1組のみ解消）。残り12件は
ほぼ1本の連鎖ブロック（`_073_`-`scl_row0`-`sda_in_buf`-`scl_n_row2`-
`_076_`-`_067_`-`start_pulse`-`busy`-`rst_n`-`_087_`、および別途
`scl_row1`↔`rst_n`、`scl_row5`↔`_103_`、`_021_`↔`_061_`）。

**チャネル高さ拡大の効果検証**：380um→480um（予算70→89トラック）に
拡大して素の初回配線を再実行 → **短絡は文字通り同一の14件**（ネット
名・ペアまで完全一致）。チャネル使用率は常に予算ぴったり（89/89）
——v7セッションで既に文書化されていた「トラック予算を増やしても
使用量がそのまま増えるだけで短絡パターンは変わらない」という同じ
病理が再現。**チャネル高さはこの短絡群の根本原因ではない**と結論。

**現状**：DRC 0違反は全構成で維持。短絡12件（`v4_1620_r6_step2b_
fjog.gds`、FORCE_JOG_NETS適用後）が最良の到達点——自動修正ツールの
能力が尽きた状態。関与ネットの多くが`busy`/`rst_n`/`start_pulse`
（リセット/START検出まわりの構造的バックボーンネット、ファンアウト
非常に高い）であり、旧V8（N_ROWS=4・行幅2214.0um）では問題にならな
かった組み合わせが、N_ROWS=6・行幅1620.0umという大幅に制約された
物理空間で新たに衝突するようになったと考えられる。

**次の一手（未着手）**：v8_step_4同様の手動GDSサージェリー
（`ripup_reroute_shorts.Fixer`のプリミティブを再利用し
`classify_complex`の保護を意図的にバイパスする一点物スクリプト）が
必要。ただし今回は12件が密に連鎖しており、v8の3件よりも規模・
相互依存度が大きく、より慎重な調査が必要——ユーザーへ状況報告し
方針確認中。

検証は全て`outputs/`スクラッチ領域で実施。

### 77.27 AND3_X1/AND4_X1/XOR2/XNOR2の物理セル追加、フルliberty
再合成、V9パイプライン再実行（N_ROWS=6・行幅1620.0um）

ユーザーがAND3_X1/AND4_X1/XOR2/XNOR2の物理レイアウトを
`TR-1um_STDCELL.gds`共有ライブラリへ直接マージし、対応する
schematic/symbol/extracted netlistを`LEF/`へ追加（77.26の懸案が
ユーザー側で解消）。

**新規セルの検証**：PR境界（235/0）寸法は全セルrow height=64.8um・
5.4umグリッドに整合（AND3_X1/XOR2/XNOR2=27.0um幅=5トラック、
AND4_X1=32.4um幅=6トラック）。`gen_lef.py`のPIN_METAへ4セル追加し
再生成 → エラーなし、4マクロとも正しいピン構成（A/B[/C/D]/Y +
VDD/GND）で出力。バックアップとのdiffで既存マクロへの影響ゼロ
（純粋追加のみ）を確認。DRC（M1/M2幅・間隔）は4セルとも違反0件。
（`LEF/XOR.extracted`はXOR2への改名前の残骸、未参照・実害なし）

**フルliberty再合成**：新セルマージ後、`TR1um_5_stdcell.lib`を
制限なしでABCに渡してv4 RTL（bit_cnt+last_bit_pending版）を再合成。
結果は**158インスタンス／総面積519.5**——直前の制限liberty版
（AND3/AND4/XOR2/XNOR2を除外した158インスタンス／519.5）と**完全に
同一**。新セルはAND4_X1×1・XOR2×1・XNOR2×1のみ採用され、AND3_X1は
不使用。ABCがこの回路では新セルにコスト上の優位性をほぼ見出さな
かったことを意味する——「STDCELL種類を増やすと配線が減るか」という
ユーザーの問いへの実測での回答：**このネットリストに関しては、
セル数・総面積は変化しなかった**（ただし後述の通り配線結果には
効果があった）。

**V9パイプライン再実行**（row buffer→BUFTH→配置→配線、N_ROWS=6・
N_GAPS=2・TAP_INTERVAL_TRACKS=147・行幅1620.0um固定）：
row buffer+BUFTH挿入後170インスタンス（77.24の制限liberty版と
ほぼ同数）。配置・配線ともDRC 0違反で完走。

**短絡検証**：`verify_connectivity_nrow_fm.py`で**7件**（77.24/77.25
の制限liberty版はFORCE_JOG適用前14件・適用後でも12件が最良）——
**FORCE_JOGすら使わない素の初回配線で12件を下回る7件**まで改善。
セル数・総面積は同一だったにもかかわらず、AND4_X1/XOR2/XNOR2による
局所的なゲート統合が正味の配線トポロジ（ネット隣接パターン）を
変化させ、結果として短絡衝突が減ったと考えられる。

`ripup_reroute_shorts.py`（自動修正）を実行 → やはり**0件自動修正**
（7件全ての衝突ペアが両側multi-segment/high-fanout保護——
`scl_row0`/`scl_row1`/`scl_row4`/`scl_n_row3`/`scl_row5`/`sda_d`/
`start_pulse`/`busy_clr`/`shreg[0]`/`_073_`等、77.25と同種の
backboneネット群）。残存7件：
`scl_row1`↔`scl_row0`、`start_pulse`↔`_073_`、`sda_d`↔`start_pulse`、
`scl_row5`↔`sda_d`、`busy_clr`↔`scl_row5`、`scl_n_row3`↔`scl_row4`、
`shreg[0]`↔`busy_clr`（連鎖的に繋がった1本のクラスタ）。

**現状のベスト到達点**：`v4_fulllib_1620_r6_step2_routed.gds`
（DRC 0違反・短絡7件、FORCE_JOG未適用）。77.25の12件版より明確に
改善——次点としてFORCE_JOG_NETSをこの7件に適用すればさらに減る
可能性があるが、未試行。

検証は全て`outputs/`スクラッチ領域で実施
（`i2c_slave_async_net_v4_fulllib*.v`、`placement_nrow_fm_v4_fulllib_
1620_r6.json`、`v4_fulllib_1620_r6_step{1,2,3}*.gds`等）。

**FORCE_JOG_NETS適用結果（77.27の7件短絡全てを指定して再配線）**：
0 jog event、DRC 0違反、短絡は**文字通り同一の7件**（ネット名・
ペアまで完全一致）。77.25で確認した「トラック予算を増やしても
短絡パターンは変わらない」と同種の頑健性——今回はFORCE_JOG機構自体
が空振り（強制対象ネットのいずれも、そもそもrow-crossingのjog
選択が発生する経路になっていなかった）。**自動ツール（ripup_reroute
+ FORCE_JOG）はこれで完全に手を尽くした**——7件は手動GDSサージェリー
以外では解消不可と判断。

**N_ROWS=4での再試行（77.27のfulllibネットリスト、169インスタンス）**：
N_GAPS=2・TRACKS=147・行幅1620.0um固定で配置配線を再試行 →
**配線失敗**（`SystemExit: no clear X found for row 1 near
x=1574.1 (net='busy')`）。DRC/短絡以前の完走不能——77.23で特定した
「N_GAPS=2（TAPカラム3本）構成でのX方向jog経路不足」という根本原因
は、v4/フルlibでインスタンス数が減っても解消しないことを確認。
1620.0um幅で配線が完走するのは引き続き**N_ROWS=6のみ**。

### 77.29 v7とのセル数比較調査、ABC重複ゲートの発見、自動重複除去
（`dedup_gates.py`）、N_ROWS=4・1620um固定での再配線成功

**調査の発端**：「v7とセル数がそれほど変わらない(155 vs 169、+9%)のに
N_ROWS=4・1620umでJOG経路が見つからない理由」というユーザーの問い。

**原因特定**：v7ネットリスト(`src/i2c_slave_async_net_v7.v`)を精査した
ところ、DFFRBのRSTBピンは`RSTB1`(ファンアウト24)・`RSTB2`(ファンアウト
9)という**2本の共有トランクネット**に集約されており、`busy`/`rst_n`/
`start_pulse`への直接ファンアウトはごく僅か(各3箇所)だった。一方、
77.27のfulllibネットリスト(N_ROWS=4版、169インスタンス)では、ABCの
`abc -liberty`ステップが**同一論理(busy&rst_n、NOR(_067_,start_pulse))
をDFFごとに個別複製**——33個のRSTBピン全てがそれぞれ専用のfanout=1
ゲートで独立に駆動されており、結果busy/rst_n/start_pulseの直接ファン
アウトが3→11/11/26まで膨張していた。セル総数はv7とほぼ同等でも、
**同じ論理を計算する独立ゲートが全行に散らばり、それぞれがbusy/
rst_n/start_pulseまで個別に長距離配線を引く**構造になっており、TAP
カラム付近の狭いjog経路にバックボーン配線が集中していたことが
N_ROWS=4での配線失敗（`no clear X found`）の直接原因と判明。

`opt_merge`をabc後に追加する対処は効果なし（0件マージ、原因不明）。

**`script/dedup_gates.py`（新規、恒久スクリプト）**：`netlist_parser.py`
の別名解決済みインスタンスリストを使い、単一出力の純粋組合せセル
（INV_X1/BUF_X1/BUFTH/AND2-4_X1/OR2-4/NAND2-4/NOR2-4/XOR2/XNOR2/MUX2/
DEL1/DEL2/DEL4）について「型＋出力ピン以外の全ピン接続」が完全一致
するインスタンス群を検出し、代表1個を残して残りをテキストサージェリ
ーで除去・出力ネットを代表ネットへ付け替える、手動CSE（共通部分式
除去）パス。トップレベルポートネットは常に代表として温存（削除しな
い）よう安全策を実装。

fulllibネットリスト(158インスタンス)に適用 → **2グループ・31個の
冗長インスタンスを検出・除去**（`busy`&`rst_n`のAND2_X1×7、
NOR2(_067_,start_pulse)×24）、158→**127インスタンス**。
busy/rst_n/start_pulseの直接ファンアウトは3/3/3に復元——v7と同水準。

**形式等価性検証（LEC）**：`src/stdcell_behavioral_stubs.v`の振舞い
モデルを用い、yosys `equiv_make`+`equiv_simple`で除去前後のネット
リストを比較。165個の$equivセル中134個は自動証明、残り31個
（=dedup_gates.pyが統合した数と完全一致）は`busy`ラッチ
（NOR2交差結合の組合せループ、両ネットリストに元々存在・今回の変更
とは無関係）でyosysのSATエンジンが行き詰まり自動証明不可——これは
既知のツール制約（非同期SRラッチはequiv_induct/equiv_simpleの標準
フローでは扱えない）であり、31件はいずれも「同一セル型・同一入力
ネット」という定義上自明な等価性のみで構成されるため、数学的には
無条件に正しい変換と判断。証明できなかった集合が除去対象と1対1で
一致したことも、他に意図しない差分がないことの傍証となった。

**N_ROWS=4・1620um固定で配置配線を再試行**（dedupネットリスト、
row buffer+BUFTH挿入後137インスタンス、77.27と同条件のN_GAPS=2・
TRACKS=147）：**配線成功**（77.27直前の同条件では`no clear X found`
で失敗していたのが、dedupだけで解消）。DRC 0違反。短絡疑い**5件**
（`_073_`↔`bit_cnt[0]`、`_008_`↔`txreg[1]`、`scl_row0`↔`_121_`、
`_009_`↔`scl_row0`、`_111_`↔`_008_`）。`ripup_reroute_shorts.py`で
2件自動修正（`bit_cnt[0]`↔`_073_`、`_121_`↔`scl_row0`）→**残り3件**
（`_008_`↔`txreg[1]`、`_009_`↔`scl_row0`、`_111_`↔`_008_`）、DRC 0違反
維持。

**総括**：N_ROWS=4・行幅1620.0um・137インスタンス・コア高さ
**1684.0um**（N_ROWS=6版の2573.6umより大幅に小さい）で、短絡わずか
3件・DRC 0違反というこれまでで最良の結果。セル数がv7と近いという
見た目に反して実際には配線混雑度が全く違っていた、という調査の
初期洞察が、修正後には最良の到達点（N_ROWS=6の7件を下回る3件、かつ
チップ面積も大幅減）に直結した。

検証は全て`outputs/`スクラッチ領域で実施
（`i2c_slave_async_net_v4_fulllib_dedup.v`、
`i2c_slave_async_net_v4_dedup_final_r4.v`、
`placement_nrow_fm_v4_dedup_1620_r4.json`、
`v4_dedup_1620_r4_step{1,2,3}*.gds`等）。恒久スクリプトは
`script/dedup_gates.py`。

**N_GAPS=4（5 TAPカラム、旧V8方式）での配線可否調査**：dedupネットリ
スト・N_ROWS=4・137インスタンスはそのまま流用（行構成はN_GAPSに
非依存）。TAP_INTERVAL_TRACKS=72（N_GAPS=4で1620.0um以下に収まる
最大値、行幅1609.2um——N_GAPS=4は`(1620-5*10.8)/(4*5.4)=72.5`が非
整数のため、現行コード（全ギャップ均等トラック数）では1620.0um
ちょうどには原理的に到達不可。GIO制約は「1620um以下に収める」こと
なので1609.2umで十分）で配置配線 → **配線成功**。DRC 0違反。短絡
疑い**4件**（`_057_`↔`bit_cnt[0]`、`scl_row0`↔`_057_`、`sda_in_buf`↔
`_052_`、`_009_`↔`_008_`）。`ripup_reroute_shorts.py`は**0件自動修正**
（全て保護ネット同士の衝突）——N_GAPS=2版（5件→3件）よりわずかに
劣るが、同水準。

**結論**：N_GAPS=4でもN_ROWS=4・1620um以下（1609.2um）で配線可能。
ただしN_GAPS=2版（短絡3件、TAPカラム3本）の方がN_GAPS=4版（短絡4件、
TAPカラム5本）よりわずかに良好——TAPカラムを増やしてもこのdedup後
ネットリストでは短絡削減に寄与しなかった（TAPカラム数と短絡数の
間に単純な相関はない）。現時点の最有力候補は引き続き**N_GAPS=2・
N_ROWS=4・行幅1620.0um・短絡3件**。

### 77.31 ウォーキングワン方式（v3、bit_walk）をフルliberty再合成+dedup
してN_GAPS=2・N_ROWS=4・1620.0umで検証

「レース修正（bit_cnt+last_bit_pending）が本当に幅問題の解決に必要
だったのか、それとも77.29のdedup修正だけで十分だったのか」を切り
分けるため、ウォーキングワン方式（`i2c_slave_async_v3_walkingone_
true.v`、8bit one-hot `bit_walk`版）をフルliberty（AND3/AND4/XOR2/
XNOR2込み）で再合成 → **175インスタンス**（旧来の175と一致）。
`dedup_gates.py`適用 → 2グループ・35個の重複除去 → **140インスタ
ンス**、busy/rst_n/start_pulseのファンアウトも3/3/3に復元（v4系と
同じ改善効果）。

row buffer+BUFTH挿入（N_ROWS=4）→149インスタンス。N_GAPS=2・
TRACKS=147・行幅**1620.0umちょうど**で配置配線 → **配線成功**
（77.23で確立していた「旧v3・186インスタンスでは1620umに絶対収まら
ない」という結論を、dedup適用後のウォーキングワン方式175→140インス
タンスでは覆した）。DRC 0違反。短絡疑い**6件**（`_090_`↔`_100_`、
`addr_ok`↔`_084_`、`_008_`↔`scl_row3`、`scl_row0`↔`phase[0]`、
`_009_`↔`_090_`、`_082_`↔`_008_`）。`ripup_reroute_shorts.py`は0件
自動修正（全て保護ネット同士）。

**結論**：dedup修正それ自体がN_GAPS=2・N_ROWS=4・1620.0um実現の
主要因であり、bit_cnt+last_bit_pendingへの回帰（77.24のレース修正）
は幅問題そのものの解決には必須ではなかったことが判明。ただし
短絡数はbit_cnt版（3件）の方がウォーキングワン版（6件）より少なく、
レース修正には別の価値（回路規模削減・短絡削減）が引き続きある。
なお、ウォーキングワン方式は76.43-76.47で特定された同一エッジレース
条件それ自体は未解決のまま（bit_cnt方式に切り替えた本来の動機）
——幅の観点では両立可能というだけで、レース問題を再度抱えることに
変わりはない。

検証は全て`outputs/`スクラッチ領域で実施
（`i2c_slave_async_net_walkingone_fulllib.v`、`_dedup.v`、
`_dedup_final_r4.v`、`walkingone_dedup_1620_r4_step{1,2,3}*.gds`等）。

### 77.32 N_GAPS=3（4 TAPカラム）での配線可否調査（bit_cnt+dedup版）

dedupネットリスト・N_ROWS=4・137インスタンスをそのまま流用。
TAP_INTERVAL_TRACKS=97（N_GAPS=3で1620.0um以下に収まる最大値、行幅
1614.6um——N_GAPS=3も`(1620-4*10.8)/(3*5.4)=97.33`が非整数のため
1620.0umちょうどには到達不可）で配置配線 → **配線成功**。DRC 0違反。
短絡疑い**5件**（`_073_`↔`bit_cnt[2]`、`_008_`↔`bit_cnt[0]`、
`scl_row0`↔`_073_`、`_009_`↔`scl_row0`、`_111_`↔`_009_`）。
`ripup_reroute_shorts.py`は0件自動修正（全て保護ネット同士）。

**3種のTAPカラム構成の比較（bit_cnt+dedup版・N_ROWS=4・すべて1620um
以下・DRC 0違反）**：

| N_GAPS | TAPカラム数 | 行幅(um) | 短絡件数 |
|---|---|---|---|
| 2 | 3 | 1620.0 | 3（自動修正後） |
| 3 | 4 | 1614.6 | 5（自動修正効果なし） |
| 4 | 5 | 1609.2 | 4（自動修正効果なし） |

N_GAPS=2（v7方式の3 TAPカラム）が引き続き最良。TAPカラムを増やす
（4本・5本）ほうがむしろ短絡がやや増える傾向——TAPカラム数と短絡数
の間に単純な相関はなく、この設計では3カラム構成が最も相性が良い
ことが3パターンの比較で裏付けられた。

現時点の最有力候補は変わらず**N_GAPS=2・N_ROWS=4・行幅1620.0um・
短絡3件**（77.29）。

検証は`outputs/v4_dedup_ngaps3_r4_step{1,2,3}*.gds`等に保存。

### 77.33 「N_GAPS=3/4がなぜ1620.0umちょうどにならないか」の原因特定
と`gen_placement_nrow_fm.py`の一般化（不均等ギャップ幅対応）

ユーザーの指摘：「TAP2幅10.8umは2トラック分。FILL2/FILL3を使えば
300トラックにきっちり収まるはずでは」——これを受けて`fill_combo()`
（`gen_placement_2row.py`）を確認したところ、**1トラック以外の任意の
整数トラック数を埋められる**ことを確認（FILL側に制約なし、ユーザー
の指摘通り）。

真の原因は`gen_placement_nrow_fm.py`の`TAP_INTERVAL_TRACKS`が**全
ギャップ共通の単一スカラー値**という設計上の単純化だった（TAP列の
X座標を全行で揃えて電源メッシュを一直線に通すための仕組み——
`pack_row_distributed`のコメントに明記）。「N_GAPS×TRACKS」の式が
整数解を持つことを要求していたが、本来必要なのは「全ギャップの
合計トラック数」が整数であることだけ。実際に計算すると：
N_GAPS=3は`(1620-4×10.8)/5.4=292`トラック（**整数**）、N_GAPS=4は
`(1620-5×10.8)/5.4=290`トラック（**整数**）——ギャップ間で不均等に
分割してよいなら、どちらもFILL2/3が余りを吸収してちょうど1620.0um
に到達できるはずと判明。

**`gen_placement_nrow_fm.py`を一般化**（恒久修正）：`TAP_INTERVAL_
TRACKS`をスカラーまたはギャップ数分のリストとして受け付けるよう
変更（`_tap_tracks(gap_i)`ヘルパー新設、`TARGET_ROW_WIDTH_UM`の
式・`_gaps_needed`のdry-run・`pack_row_distributed`のgap_budget計算
の3箇所を対応）。全行が同じギャップ幅の並びを使う限りTAP列は行間
で完全に揃ったまま（電源メッシュの整合性は維持）——「同じギャップは
全行で同じ」という制約だけが必要で、「全ギャップが互いに同じ」
という制約は不要だったことがコード上も確認された。

**N_GAPS=3・TRACKS=[97,97,98]（合計292、1620.0umちょうど）で検証**
（bit_cnt+dedup版・N_ROWS=4・137インスタンス）→ 配置生成で
`row_width=1620.0um`ちょうどを確認、配線も成功。DRC 0違反。短絡疑い
**3件**（`scl_row0`↔`_051_`、`_009_`↔`sda_in_buf`、`_111_`↔
`scl_row0`）——**N_GAPS=2の最良結果（3件）と同水準**。
`ripup_reroute_shorts.py`は0件自動修正。

**結論**：ユーザーの指摘通り、N_GAPS=3/4が1620.0umちょうどに到達
できなかったのはFILLセルの限界ではなく、コードの均等割り当て
という前提のせいだった。一般化後はN_GAPS=3でも1620.0umちょうど・
短絡3件を達成——TAPカラム数についてN_GAPS=2と同等の選択肢が
生まれた（N_GAPS=4も同様に不均等分割で1620.0umちょうどに到達
できる見込みだが未検証）。

検証は`outputs/v4_dedup_ngaps3_exact_r4_step{1,2,3}*.gds`等に保存。
恒久修正は`script/gen_placement_nrow_fm.py`（`_tap_tracks`/
`_total_gap_tracks`ヘルパー追加）。

### 77.34 N_GAPS=4も不均等分割で1620.0umちょうどに到達、N_GAPS=2/3/4
統一比較（bit_cnt+dedup版・N_ROWS=4・すべて1620.0umちょうど）

N_GAPS=4をTRACKS=[72,72,73,73]（合計290、1620.0umちょうど）で検証
→ 配線成功、DRC 0違反。短絡疑い5件 → `ripup_reroute_shorts.py`で
1件自動修正 → **残り4件**（`_008_`↔`bit_cnt[0]`、`scl_row0`↔`_008_`、
`_001_`↔`sda_in_buf`、`_078_`↔`_001_`）。

**最終比較表**（bit_cnt+last_bit_pending+dedup版、N_ROWS=4・137
インスタンス、すべて行幅1620.0umちょうど・DRC 0違反）：

| N_GAPS | TAPカラム数 | 短絡（配線直後） | 短絡（自動修正後） |
|---|---|---|---|
| 2 | 3 | 5 | 3 |
| 3 | 4 | 3 | 3 |
| 4 | 5 | 5 | 4 |

N_GAPS=3（不均等分割、TRACKS=[97,97,98]）が最も少ない配線直後短絡数
（3件、自動修正の必要なし）で並んで最良。N_GAPS=2も自動修正後は同じ
3件に到達。N_GAPS=4はやや劣る（4件）。TAPカラムを増やすほど電源
メッシュは強化されるが短絡数は改善しない——電源メッシュの強度を
より重視するならN_GAPS=3、実績のあるシンプルな構成を好むならN_GAPS
=2が候補。

検証は`outputs/v4_dedup_ngaps4_exact_r4_step{1,2,3}*.gds`等に保存。

### 77.35 N_GAPS=3版を"v9"として正式採用、`layout/step8/`へ記録

ユーザー決定：N_GAPS=3（不均等分割、短絡3件・自動修正不要）で確定、
恒久ファイルとして記録。V8の`_v8.v`/`_v8_rowbuf.v`/`layout/step8/
v8_step_N_*.gds`の命名規則に倣い、"v9"として以下へ保存：

- `src/i2c_slave_async_net_v9.v`（ABC合成+dedup後、row buffer挿入前
  ——`i2c_slave_async_net_v4_fulllib_dedup.v`のコピー、127インスタ
  ンス）
- `src/i2c_slave_async_net_v9_rowbuf.v`（row buffer+BUFTH挿入後、
  最終ネットリスト——`i2c_slave_async_net_v4_dedup_final_r4.v`の
  コピー、137インスタンス）
- `LEF/row_assignment_v9.json`、`LEF/placement_nrow_fm_v9.json`
  （行幅1620.0umちょうど、N_GAPS=3・TAP_INTERVAL_TRACKS=[97,97,98]）
- `script/pin_map_nrow_fm_v9.json`、`script/net_shapes_nrow_fm_v9.
  json`、`script/channel_usage_nrow_fm_v9.json`、`script/
  force_jog_events_nrow_fm_v9.json`（ripup-reroute後の最終状態、
  短絡3件版と対応）
- `layout/step8/v9_step_1_placement.gds`（配置、TAP列+FILL含む）
- `layout/step8/v9_step_2_routed_raw.gds`（初回配線、短絡3件）
- `layout/step8/v9_step_3_ripup_reroute.gds`（自動修正後——実質
  step_2と同一、ripup_reroute_shorts.pyは0件修正だったため）

保存後、`v9_step_3_ripup_reroute.gds`に対してDRC・接続性チェックを
再実行し、スクラッチ領域での結果（DRC 0違反、短絡3件：`scl_row0`↔
`_051_`、`_009_`↔`sda_in_buf`、`_111_`↔`scl_row0`）と完全一致する
ことを確認済み。

このv9が現行の最有力レイアウト候補（N_ROWS=4・N_GAPS=3・行幅
1620.0umちょうど・コア高さ1684.0um・DRC 0違反・短絡3件）。次は
残存短絡3件への対処（手動GDSサージェリー）、またはSTEP4以降
（top-pin配線・電源ピン追加・DRC/LVS正式検証）への継続。

### 77.36 残存短絡3件の解決方法を調査 — v8の手動修正手法を適用も
全6方向で失敗、根本原因を特定

v8の前例（§77.16、`script/fix_v8_remaining_shorts.py`：
`ripup_reroute_shorts.Fixer`のライブジオメトリチェック付き移動
プリミティブを再利用し「どちらのネットを動かすか」のガードだけ
バイパスする手法）を新規`script/fix_v9_remaining_shorts.py`として
v9の3件に適用。

**衝突の正体**（`find_conflicts()`で正確なbox特定）：
1. `_051_`↔`scl_row0`：M2、同一列X≈1221.4-1224.8（垂直ラン）
2. `scl_row0`↔`_111_`：M1、同一track_y≈354.1-355.9（水平トランク）
3. `sda_in_buf`↔`_009_`：M1、同一track_y≈359.5-361.3（水平トランク）

v8と異なり、いずれも単一boxのクリーンな重複（複数box連結の複雑化
なし）。移動側は総配線長が少ない方を優先（`_051_`1536um/6box vs
`scl_row0`7120um/41box→`_051_`、`_111_`2787um/9box vs`scl_row0`
（同上）→`_111_`、`sda_in_buf`2455um/16box vs`_009_`10250um/54box→
`sda_in_buf`）。

**全て失敗**：`try_fix_vertical`/`try_fix_horizontal`を6方向
（各衝突の両ネット）全て試したが1件も成功せず。

**原因診断**：
- 衝突1（`_051_`↔`scl_row0`のM2垂直ラン）：両ネットとも一方の端点
  が「行の内部（セル本体）に埋もれた実ピン」（`_051_`はY=604.3が
  row1内部、`scl_row0`はY=189.9がrow0内部）。`find_own_trunk`で
  もう一方の端点（トランク側）は見つかるが、列移動候補
  cx±k×5.4um（k=1〜299、行幅ほぼ全域）を全探索しても、行内部の
  ピンまで到達できるクリアなFILLコーナーが他に見つからない——
  この特定の行内部ピンに到達できるFILL列が実質1本しかない可能性。
- 衝突2・3（M1トランク同士）：track_y移動先候補も全滅。チャネル1
  のトランクトラック予算を検証したところ**使用量が予算ぴったり
  （70/70）**——試しにチャネル高さを380um→482.6umへ拡大して
  再配線したところ、予算は70→89へ増えたが**使用量も89/89へ完全に
  比例して増加**し、**短絡は文字通り同一の3件のまま**（ネット名
  ペアまで完全一致）。§77.25で確認した「チャネル高さを増やしても
  使用量がそのまま増えるだけで短絡パターンは変わらない」という
  既知の病理が今回も再現——チャネル予算の絶対量はレバーとして
  機能しない（ルーター自身のトラック割当ロジックが常に予算を
  使い切るよう動作するため、絶対的な余裕を作ることができない）。

**結論**：v8の3件（構造的にシンプルな列/トラック競合）とは異なり、
v9のこの3件は「行内部の実ピンへの到達経路がその特定X位置にしか
存在しない」という配置（FILL列の配置）に起因する、より根深い
制約。`ripup_reroute_shorts.Fixer`が持つ「配線後のGDSを局所的に
動かす」語彙では解決不可——解決には配置（`pack_row_distributed`の
FILL列配分）自体を変更するか、この3件を現構成の実用上の下限として
受け入れるかの二択。N_ROWS=6構成で以前確認した12件→7件のケースと
同様、自動化ツールを使い尽くした状態。

検証は`outputs/v9_tallch_step{1,2}*.gds`（チャネル高さ検証、短絡
パターン不変を確認）に記録。

### 77.37 ユーザー指摘で再調査 — 3件中2件は`Fixer`自体の偽陽性と
判明、修正して短絡3件→1件へ

ユーザーから「M1/M2トラックは余っているはずなので回避できるのでは」
との指摘。§77.36の「チャネル予算飽和」という結論を鵜呑みにせず、
`clear_excluding`の失敗理由を1候補ずつ直接ダンプして再調査した。

**発見**：`Fixer.clear_excluding`の「自ネット自身のvia/ピン足跡を
除外する」ロジック（`own`領域構築）が、全てのネット終端を「記録
されているY座標を中心とした対称なvia-pad矩形（3.4×3.4um）」と仮定
していた。しかし実際には：

1. **実セルピンの矩形は対称ではない**：`_223_.B`（ネット`_111_`の
   終端）の実際の矩形は絶対座標でY=[604.4, 607.8]——記録されている
   スタブ端点Y=604.4を「下端」として片側にしか伸びていない。対称
   仮定（Y=[602.7, 606.1]）は実矩形の上半分をカバーし損ね、
   このネット自身の未除外分が残り、**移動先候補が何であっても
   常に「自分自身の geometry と衝突」と誤判定**されていた——
   本当の配線資源不足ではなく、チェックロジックの偽陽性。
   `LEF/placement_nrow_fm_v9.json`の実ピン矩形（セルローカルY＋
   行オフセット）と、`clear_excluding`が除外し損ねた「remainder」
   をum单位で突き合わせ、完全一致を確認。
2. **via_1 PCellの実センターが記録boxの端点と一致しない**場合が
   ある（約1.7umのズレ、klayoutでV1レイヤーを直接クエリして確認）
   ——同じ種類の偽陽性を生む別要因。

**修正**（`script/fix_v9_remaining_shorts.py`を書き換え）：`Fixer`
本体は変更せず、`fixer.net_shapes[net]`へ実ピン矩形（placement JSON
から正確に算出、対称仮定ではなく実測値）を一時的に追加してから
`try_fix_horizontal`/`try_fix_vertical`を呼び、終わったら追加分を
除去（実際に描画された配線ではなく除外チェック専用の補助データの
ため、`net_shapes.json`には残さない）。

**結果**：
- `_111_`↔`scl_row0`：`_111_`側を修正、track_y=371.2へ再配置——**解消**
- `sda_in_buf`↔`_009_`：`sda_in_buf`側を修正——**解消**
- `_051_`↔`scl_row0`：両方向とも、修正後の正しいvia中心座標で
  再試行したが**解消せず**——原因を追跡した結果、こちらは偽陽性
  ではなく**本物の配線資源制約**と判明。`scl_row0`側の失敗候補を
  詳細調査すると、候補via位置(1228.5, 189.9)付近に幅広いM1障害物
  （X=[1220.4,1259.4] Y=[182.7,195.3]）が実在——これは行0自身の
  VDD電源レール（セルLEF内の`OBS`形状、行境界に沿った実配置）。
  この電源レール帯域には元々の配線が使っているX=1223.1以外に
  ほぼクリアな抜け道がなく、`_051_`・`scl_row0`の両方がこの
  同じ1本の隙間を必要としていたことが根本原因——これは
  `ripup_reroute_shorts.Fixer`の「配線後GDSを局所的に動かす」語彙
  では解決不可能（配置そのものの変更が必要）。

DRC・接続性を独立検証（`layout/step8/v9_step_4_manual_short_fix.gds`
に保存）：DRC 0違反、**短絡3件→1件**（`scl_row0`↔`_051_`のみ残存）。

**結論**：ユーザーの指摘通り、3件中2件は自動修正ツール自体の
モデリング上の欠陥（偽陽性）であり、真の配線資源不足ではなかった。
残る1件のみが電源レール帯域の実際の資源制約による、本物の解決
困難なケース。`script/fix_v9_remaining_shorts.py`を恒久スクリプト
として保存（実行すればいつでも同じ2件を再現・修正可能）。

### 77.38 `Fixer`本体の恒久修正 — `_endpoint_pad_region`のtier-a照合を
pin_map座標一致からピン矩形の幾何近接判定へ再設計

§77.37の一時パッチ（`net_shapes`への実矩形の一時追加）を、`Fixer`
本体（`script/ripup_reroute_shorts.py`）の恒久修正に格上げする作業。
`Fixer.__init__`に`placement=None`引数を追加し、渡された場合は
`self.pin_geom`（`(inst,pin) -> 絶対座標矩形リスト`、placement JSON
のセルローカル矩形＋行Yオフセットから構築）を保持するようにした。
`main()`も`placement`をそのまま`Fixer(...)`へ渡すよう修正済み。

実装時に、当初案（pin_mapの`(vx,vy)`とオフセット`VIA_MATCH_EPS_UM`
以内で一致した場合のみ実ピン矩形を使う）で**新たな偽陽性を発見**：
`pin_map`は「そのピンへの現在のvia着地点」を追跡する可変テーブルで
あり、`try_fix_*`が成功するたびに該当エントリの`(vx,vy)`が書き換え
られる（実LEFピン座標の固定テーブルではない）。このため：

1. ネット`_051_`のpin_mapは`_176_.Y`を(1223.1, 257.8)——チャネル1
   内部でどの行内部でもない座標——として記録していたが、
   `_176_.Y`の実矩形はY=[604.3,607.7]（約347um離れたrow1内）。
   座標一致でこれを拾ってしまうと、347um離れた無関係な矩形を
   「この終端の自分自身の足跡」として誤って除外領域に採用し、
   本来除外すべき局所的なvia足跡がすり抜ける——§77.37で見つけた
   ものと同型の偽陽性が、tier-a照合自体からも発生しうると判明。
2. 逆に、ネット`_111_`のpin_mapは`_223_.B`を(1363.5, 355.0)——
   トランクとM2脚の中継via点——として記録しており、実ピン矩形
   （Y=[604.4,607.8]、M2脚の反対側の終端）とは別の位置。この
   ケースでは座標一致チェックが「一致しない」と判定してtier-aが
   一切発火せず、tier-c（対称仮定）にフォールバックして§77.37と
   全く同じ偏心ピン矩形の偽陽性を再現してしまった。

**根本原因はどちらも同じ**：`pin_map`の`(vx,vy)`はライブな
via位置トラッカーであり、`pin_geom`への安定したキーとして使えない。

**再設計**：`(vx,vy)`との座標一致チェックを廃止し、`pin_map.get(net,
[])`は「このネットが所有する(inst,pin)の一覧を得るためだけ」に使う
よう変更。各(inst,pin)の実矩形が終端(ex,ey)から
`PIN_RECT_SANITY_UM`（15um）以内にあるかどうかという**純粋な幾何
近接判定**のみで、tier-aを採用するか決定するようにした（新ヘルパー
`_rects_near_point`）。15umという閾値は、実ピンなら自分のvia着地点
の数um以内に矩形があるはずで、数百um離れた矩形は原理的に別の終端の
ものである、という判断に基づく。

**検証**：`v9_step_3_ripup_reroute.gds`（元の3短絡状態）に対し、
§77.37のような手動`net_shapes`パッチを一切使わず、恒久修正のみで
`fixer.try_fix_horizontal("_111_", ...)` / `("sda_in_buf", ...)`を
直接呼び出したところ、**両方とも自動的に成功**（`True`）。恒久修正が
§77.37の一時パッチを完全に代替できることを確認。`_051_`↔`scl_row0`
は、恒久修正後の正しいtier-a/tier-bロジックで両方向・両ネットとも
再試行しても引き続き**解消せず**（598候補中0件成功）——§77.37の
「本物の資源制約」という結論は、偽陽性2件を潰した後でも変わらない。

`script/fix_v9_remaining_shorts.py`をv2として全面書き換え（手動
矩形パッチのコードを削除、`Fixer(..., placement=placement)`と
`try_fix_horizontal`の直接呼び出しのみに簡素化）、実行して
`layout/step8/v9_step_4_manual_short_fix.gds`を再生成。
`drc_check_nrow_fm.py`（0違反）・`verify_connectivity_nrow_fm.py`
（`scl_row0`↔`_051_`のみSHORT SUSPECTED、他146ネットは正常）で
独立に再検証、§77.37と完全に同じ結果（短絡3件→1件）を確認。

### 77.39 `_051_`↔`scl_row0`短絡の根本原因を精査 — 電源レールでは
なく行内部ピン密度が真因と判明

§77.37時点の「行0のVDD電源レール（OBS形状）が原因」という説明は、
ユーザーからの再質問（「電源レール帯域の制約の意味がわからない」）
を受けて再検証した結果、不正確だったため**撤回**する。

実際の衝突形状を`net_shapes_nrow_fm_v9final.json`から直接確認する
と、これは2本の独立したスパイン配線が偶然同じX≈1223.1でチャネル1
（row0とrow1の間、Y=196.4〜576.4）を縦断しようとしている単純な
「同一X帯の奪い合い」だった：`_051_`のM2脚はY=257.8（自身のM1
トランク端、可動）からY=604.3（row1内の固定ピン）まで、`scl_row0`
のM2脚はY=189.9（row0内の固定ピン）からY=365.8（自身のM1トランク
端、可動）まで伸びており、Y=257.8〜365.8の区間で重なる。

`try_fix_vertical`が試す598通りの候補X（クリアな列を探索）を全て
段階別に集計した結果：
- M2列自体（Y=257.8〜604.3の全高）が他ネットで塞がっている：168件
- 自ネットのM1トランクを新Xまで横に延伸するチェックで失敗：228件
- row1側の固定ピン端の短絡ジョグ（M1）で失敗：102+94件

行内部（row0、`scl_row0`の固定ピン端）を直接GDSクエリしたところ、
X=[900,1300]・row0内部Yバンド（188.5〜191.5）に**39個**の他セル
由来のM1形状（隣接セルの内部配線・ピン、5〜15um間隔で密に配置）
を確認。これは電源レール1本ではなく、**行内部にセルが隙間なく
詰まっていることそのもの**が、固定ピン端からの水平ジョグの経路を
塞いでいる、という描像の方が正確。

**結論**：`_051_`↔`scl_row0`は依然として本物の配置起因の制約で、
`Fixer`の限界（配線後GDSの局所移動のみ）では解決不可能——ただし
真因は「電源レール帯域」ではなく「row0・row1それぞれの内部で、
ほぼ同じXに配置された2つのセルのピンが、チャネル1の同じ高さ帯を
共有せざるを得ない」こと。次の一手は配置レベルの変更（§77.30
未着手リストのFILL列配分調整タスクへ継続）。

### 77.39-補足 訂正 — 「39個密集」は不正確、真因は単一トラックの
連続経路が長距離にわたり確保できないこと

§77.39の「row0内部にM1形状が5〜15um間隔で密集し逃げ場がない」との
説明を、ユーザーがKLayoutで実際のレイアウトを目視確認した上で
「周囲にM1のスペースはたくさんある」と指摘。再調査の結果、**この
指摘は正しく、§77.39の説明も不正確だった**ので訂正する。

該当Yバンド（X=400〜1620、Y=[188.5,191.5]）の実形状のギャップを
全て計測すると、9〜17.4um幅の空きが多数存在し、局所的には
via/パッド1個分（必要幅約5.2um）を通すのに十分な余白がある。
「密集して隙間がない」というのは誤り。

真因は`try_fix_vertical`が固定ピン端をジョグする際に生成する
M1ラン矩形の性質にある：`run = (min(cx,clear_x), y-half_w,
max(cx,clear_x), y+half_w)`——これは**移動元cx(=1223.1)から
移動先clear_xまでの区間全体**を1本の矩形でチェックする。局所的な
ギャップがいくつあっても、cxからclear_xまでの数百umを1本道で
貫通できなければ不合格になる。実際、候補X=990.0（233um離れた
候補）では経路上に20個以上の他セル由来M1形状（例：
X=[998.7,1003.3] Y=[168.5,192.7]、X=[1198.5,1203.1]
Y=[168.5,192.7]など、実際は縦に高い矩形）が引っかかる。さらに
候補X=1200.0（元の位置からわずか23um先）でさえ、その短い経路上に
2つの障害物が入り込み失敗する。

つまり「row0の同じYトラックは行内の多数のセルの内部配線に共有
されており、固定ピンからどちらの方向にジョグしても、その1本の
トラック上のどこかに必ず何かが刺さっている」という構図——電源
レールでも局所密集でもなく、**単一トラック・単一直線ジョグという
`try_fix_vertical`の探索方式の限界**が本質。多段ジョグ（トラックを
一旦上下にずらしてから横に進む）を許せば解決する可能性はあるが、
現行`Fixer`はその機能を持たない。

結論・次の一手は変わらず配置レベルの変更だが、狙いを再定義する：
「電源レール回避」でも「密集回避」でもなく、「row0側のこのセルの
Xをずらし、row1側の対応セルとチャネル1内で同じXを取り合わない
ようにする」こと。

### 77.41 衝突回避アルゴリズムの再調査 — ルーター自身の
draw_jogフォールバックが真因、FILL列ナッジで解決（短絡0件達成）

ユーザーから「row0/row1でM1のスペースはたくさん余っているように
見える。チャネル内で同じXを取り合わないようにする衝突判定
アルゴリズム自体を再調査してほしい」との指摘。§77.39の説明
（「単一トラック・単一直線ジョグの限界」）も不完全だったため、
`route_channels_nrow_fm.py`本体（配線後のFixerではなく、配線を
実際に生成するルーター）のコードを深く追跡した。

**発見**：`scl_row0`はrow0・row1・row3の3行にピンを持つ、実質的に
"spanning"（3行以上）ネットと分類される（`pin_map`とplacement JSON
の行割当を突き合わせて確認）。spanningネットはpass 2で配線され、
pass 1（row-only/adjacent-pairネット、`_051_`はこちら）より**後**
に描画される。`_051_`が先にX=1223.1を占有した後、`scl_row0`の
row0ピン用コードがpass 2のfinal-runチェック（`channel_clear`、
route_channels_nrow_fm.py:2143付近）でこれを正しく検知し、
`find_channel_clear_x`+`draw_jog`で回避を試みる——ここまでは
アルゴリズムは正常に機能している。

問題は回避（`draw_jog`）自体が失敗すること：ピンの実際のY
（y=189.9）付近でどのトラックも「クリアな出発脚」を持たず、
見つかった遠方のクリアX（clear_x=585.9）への直接ブリッジも
安全でない——そのため`draw_jog`は最終フォールバック
（route_channels_nrow_fm.py:1605付近、"may leave a short to
investigate"と自己申告するコード）に落ち、警告を出しながら
そのまま描画する。これは今回発見した2件の`Fixer`偽陽性
（§77.38）とは異なり、**ルーター自身が「短絡が残るかもしれない」
と自覚した上で採用している、正規のフォールバック経路**——過去の
v27/v28/v32/v32b（design_notes参照）で何度も洗練されてきた
`draw_jog`のロジックが、この特定の1箇所だけ力尽きるケース。
未変更のv9配置を`force_high_fo_nets={"scl_row0","scl_n_row2"},
per_row_local_nets=set()`で再配線し、全く同じ警告文言・同じ
(x,y)が再現することを確認、真因の特定を裏付けた。

**修正方針**：`draw_jog`自体を賢くする（過去のリスクの高い変更
履歴を考えると危険）のではなく、この衝突判定が発火する原因＝
X=1223.1の偶然の一致そのものを配置レベルで解消する。
`gen_placement_nrow_fm.py`に`NUDGE_BEFORE`機構（新規）を実装：
指定インスタンスの直前に指定トラック数分のFILLを挿入し、同じ
ギャップの末尾スラック（row0のgap2は末尾に約49トラック分の
余裕あり）から同量を差し引くことで、TAPカラムX・行幅1620.0umを
一切変えずに、そのインスタンス以降のセルだけを右にずらす。
対象は`_276_`（row0、scl_row0のCKピンを持つDFFRB）。

**副作用の発見と対処**：ナッジすると同じギャップ内の後続セル
（`u_buf_sda_in_row0`、sda_in_buf信号のrow0バッファ）も同量ずれる
ため、**新しい偶然のX一致**を別の場所で作ってしまうリスクがある
ことが判明（実測）。ナッジ量を掃引して都度フル配置配線＋自動
短絡修正を実行し比較：

| ナッジ量 | 結果 |
|---|---|
| 20トラック(108.0um) | 新規短絡2件（未解消） |
| 14トラック(75.6um) | 新規短絡4件 |
| 12トラック(64.8um) | 新規短絡4件 |
| 11トラック(59.4um) | 新規短絡3件 |
| 10トラック(54.0um) | 新規短絡1件（未解消） |
| 8トラック(43.2um) | 新規短絡3件 |
| **9トラック(48.6um)** | **draw_jog警告0件、DRC 0違反、短絡2件（いずれも通常のM1トランク移動ケースで自動修正可能）→ 自動修正後 短絡0件** |

9トラックで再配線した結果、`draw_jog`の警告は一切出ず（＝
scl_row0の当該ピンが誰とも衝突しなくなった）、DRC 0違反、
残った2件（`scl_row0`↔`_009_`・`scl_row0`↔`_111_`、いずれもM1
トランクの通常の衝突）は恒久修正済みの`Fixer`
（`try_fix_horizontal`）で両方とも自動修正——**最終的に短絡0件**。
`drc_check_nrow_fm.py`（0違反）・`verify_connectivity_nrow_fm.py`
（"ALL NETS FULLY CONNECTED, NO SHORTS DETECTED"）で独立に検証。

保存ファイル：`LEF/placement_nrow_fm_v9b.json`、
`layout/step8/v9b_step_1_placement.gds`/`_step_2_routed_raw.gds`/
`_step_3_ripup_reroute.gds`（＝最終、短絡0件）、
`script/pin_map_nrow_fm_v9bfinal.json`/
`script/net_shapes_nrow_fm_v9bfinal.json`。

**結論**：ユーザーの「衝突判定アルゴリズムを再調査してほしい」
という指摘が正しく、真因は電源レールでも局所密集でもなく、
ルーター自身の`draw_jog`が持つ既知のフォールバック限界だった。
配置レベルのX分離（FILL列ナッジ）で回避することで、v9で唯一
残っていた短絡を完全に解消——v9(N_GAPS=3)は短絡0件・DRC 0違反の
完成形に到達した。

### 77.42 v9b（FILL調整版・短絡0件）をv9として正式採用・恒久保存

ユーザー確認・承認（「レイアウト確認しました。先に進めてください」
→「v9bをv9として正式採用・保存」）を受け、§77.41のv9b（NUDGE_BEFORE
9トラック版、短絡0件・DRC 0違反）を、これまで"v9"の名で保存して
いた3短絡版の永続ファイル群に**上書き**して正式採用した。

上書きしたファイル：
- `LEF/placement_nrow_fm_v9.json`
- `layout/step8/v9_step_1_placement.gds` / `_step_2_routed_raw.gds` /
  `_step_3_ripup_reroute.gds`（＝現在の最終形、短絡0件）
- `script/pin_map_nrow_fm_v9.json` / `net_shapes_nrow_fm_v9.json`
  （配線直後・自動修正前の生データ）
- `script/pin_map_nrow_fm_v9final.json` /
  `net_shapes_nrow_fm_v9final.json`（Fixer自動修正後・最終）
- `script/channel_usage_nrow_fm_v9.json` /
  `force_jog_events_nrow_fm_v9.json`

`LEF/row_assignment_v9.json`・`src/i2c_slave_async_net_v9*.v`は
変更なし（今回の修正は配置内のFILL配分調整のみで、行割当・
ネットリストは§77.35時点から不変）。

上書き後のファイルに対して`drc_check_nrow_fm.py`
（0違反）・`verify_connectivity_nrow_fm.py`（"ALL NETS FULLY
CONNECTED, NO SHORTS DETECTED"）を再実行し、独立に再検証。

`layout/step8/v9_step_3_with_err.gds`・`v9_err_report_details.txt`・
`v9_step_4_manual_short_fix.gds`・`pin_map_nrow_fm_v9final.json`の
旧内容（3短絡→1短絡版）は、§77.37〜77.40の調査過程の記録として
そのまま残す（削除不可のポリシーのため、また調査の経緯を追える
価値があるため）——`v9_step_3_ripup_reroute.gds`と
`*_v9final.json`が現在の正式な最終形である点に注意。

**現状**：v9（N_GAPS=3、bit_cnt+last_bit_pending方式、dedup済み
ネットリスト、N_ROWS=4、行幅1620.0umちょうど）が、短絡0件・DRC 0
違反の完成形として確定。

### 77.43 v9 STEP6（トップピン配線）・STEP7（チャネル圧縮）実施

ユーザー指示「STEP3以降を進めて完成させてください」を受け、v8の
確立済みレシピ（`run_v8_step7_squeeze_step6_toppins.py`：squeeze
（STEP7）を先に実行し、圧縮後の新BBOXに対してtop-pin配線
（STEP6）を再適用）をそのままv9用にパラメータだけ差し替えて実行
（`run_v9_step7_squeeze_step6_toppins.py`、新規）。

**STEP7（`squeeze_channels_nrow_fm.py`、実測M1形状からcompaction_
infoを経験的に構築）**：
- channel0: budget=25 → keeping=11（-14トラック、75.6um）
- channel1: budget=71 → keeping=43（-28トラック、151.2um）
- channel2: budget=71 → keeping=33（-38トラック、205.2um）
- channel3: budget=71 → keeping=29（-42トラック、226.8um）
- channel4: budget=29 → keeping=11（-18トラック、97.2um）
- コア高さ：1684.0um → 936.1um（**-747.9um、-44.4%**）
- `layout/step8/v9_step_7_squeezed.gds`として保存。DRC 0違反・
  接続性検証"ALL NETS FULLY CONNECTED, NO SHORTS DETECTED"を確認。

**STEP6（`route_top_pins_nrow_fm.py`、圧縮後の新CH_HEIGHTS=
[58.7, 231.5, 174.8, 153.2, 58.7]に対して再適用）**：
全24トップレベルポート（`rst_n`/`scl`/`sda_in`/`sda_oe`/
`rx_valid`/`addr_match`/`rw`/`busy`/`tx_data[0:7]`/
`rx_data[0:7]`）をBBOX端まで配線。18件`[OK]`（既存配線との衝突
無し確認済み）、6件`[CHECK]`（`scl`/`sda_in`/`sda_oe`/
`tx_data[4:6]`、row3のM2上方向配線——過去バージョンでも同様の
内訳で目視確認のみ、実害なしのパターン）。
`layout/step8/v9_step_8_squeezed_top_pins_routed.gds`として保存。
DRC 0違反・接続性検証"ALL NETS FULLY CONNECTED, NO SHORTS
DETECTED"を確認（各ステップ後に独立に`drc_check_nrow_fm.py`・
`verify_connectivity_nrow_fm.py`を実行、パイプライン内部の検証結果
だけでなく再検証済み）。

保存ファイル：
- `layout/step8/v9_step_7_squeezed.gds` /
  `v9_step_8_squeezed_top_pins_routed.gds`
- `script/compaction_info_nrow_fm_v9_empirical.json`
- `script/pin_map_nrow_fm_v9sq.json` /
  `net_shapes_nrow_fm_v9sq.json`
- `script/run_v9_step7_squeeze_step6_toppins.py`（新規、v8版の
  そのままの移植・パラメータ差し替えのみ）

**DRC実デック・LVS・IRSIM実機検証について**：`tech/drc/run.drc`
（KLayout Ruby DRCマクロ、193ルール）およびIRSIM本体は、いずれも
このサンドボックス環境には存在しない（`klayout`/`irsim`バイナリ
未インストール、`klayout.db`のPython APIのみ利用可能）——§22.4・
§76と同じ、以前からの既知の制約。IRSIM用`.sim`/`.cmd`
（`script/gen_irsim_sim.py`等）は、コア単体ではなくGIOパッド
リング込みのチップ全体LVS抽出済みSPICE（`src/tr_1um_
i2c_slave_async.extracted`）に依存するため、今回のコア単体の
配置配線更新だけでは再生成できない（チップ統合・LVS抽出という
別の大きな工程が必要）。実DRCデック・LVS・IRSIMは引き続き
ユーザー側のローカル環境での実行が必要。

**結論**：v9のSTEP1〜STEP3（配置配線・短絡修正）・STEP6（トップ
ピン配線）・STEP7（チャネル圧縮）が完了。コア高さ936.1um、短絡0
件・DRC 0違反の状態でコア単体の物理設計が完成。

### 77.30 未着手（次回）

- N_GAPS=2・N_ROWS=4・bit_cnt+dedup版（短絡3件、77.29）を引き続き
  最有力候補として、残存短絡への対処を進める。
- ウォーキングワン版の6件短絡は、レース修正を採用する前提であれば
  対応不要（bit_cnt版を採用する場合）。

### 77.30 未着手（次回）

- 残存短絡（3件、N_ROWS=4・dedup版）への手動GDSサージェリー、または
  さらなるFORCE_JOG_NETS試行。
- `dedup_gates.py`をyosys合成フローの標準ステップとして組み込む
  （`abc`直後、`write_verilog`の前）——今後の全再合成で自動的に
  この種の重複を防ぐ。
- N_ROWS=6側にも同じdedupネットリストを適用し、コア高さ・短絡数が
  さらに改善するか確認。
- N_ROWS=4・dedup版（137インスタンス、コア高さ1684.0um）を新たな
  最有力候補として、V8全体パイプライン（STEP1〜STEP7・DRC/LVS・
  IRSIM検証）へ本格移行するか検討。
- 残存短絡（7件、N_ROWS=6/fulllib版、77.27時点でのベスト）への手動
  GDSサージェリー
  （`ripup_reroute_shorts.Fixer`のプリミティブを再利用し
  `classify_complex`の保護を意図的にバイパスする一点物スクリプト）。
- V8全体パイプラインを新net_v4_fulllib系（170インスタンス、N_ROWS=6/
  行幅1620.0um）へ本格移行し、STEP1〜STEP7・DRC/LVS・IRSIM検証を
  最初から通す。
- DRC/LVS検証（77.4ステップ4、実DRCデック・LVSでの正式検証）。
- IRSIM検証（77.4ステップ5、実TR-1um.prm下でのフルスイート再実行、
  bit_cnt+last_bit_pending方式が実際に同一エッジレースを解消して
  いるかの実機確認を含む）。

- `src/i2c_slave_async_net_v8.v`が本来186セルの最終ネットリストである
  はずが174セルのraw版になっていた件（77.21で発覚）——原因調査・
  修復（現行の採用済み成果物には影響なし、`LEF/placement_nrow_fm_v8.
  json`は既に確定済みのため）。
- DRC/LVS検証（77.4ステップ4、実DRCデック・LVSでの正式検証）。
- IRSIM検証（77.4ステップ5、実TR-1um.prm下でのフルスイート再実行）。

### 77.44 v9「TAPのVDD/VSSピンがついていません」調査・恒久修正

ユーザー指摘「TAP のVDD/VSS ピンがついていません」を調査。AskUserQuestion
で確認箇所を特定した結果、「BBOX端にVDD/VSSがPINでありません」——TAPセル
自体のLEF/GDS定義の話ではなく、**コアBBOX境界（Y=0/Y=core_h）における
チップレベルのVDD/GNDピン注釈**が無いことを指しているとわかった。

**切り分け過程（いずれも正常と確認、TAPセル自体には問題なし）**：
- `LEF/TR-1um_STDCELL.lef`（1614〜1699行目）：TAP2/TAP3とも`PIN GND`/
  `PIN VDD`がM1+M2の実RECTジオメトリ付きで完備。
- `LEF/TR-1um_STDCELL.gds`のテキストラベル：layer(48,0)に'gnd'/'vdd'
  各1、layer(49,0)に'gnd'/'vdd'各2（上下M2ストラップ端子分）——直接
  `klayout.db`で確認、欠落なし。
- xschemの`.sch`/`.sym`はTAP2/TAP3/FILL2/FILL3のみプロジェクト内どこにも
  存在しない（他の全セルは完備）——これはこの時点では副次的な発見で、
  今回のBBOXピンの件とは別問題（LVS用netlistの話、77.45以降で対応）。

**真の原因**：`route_channels_nrow_fm.py`の`draw_tap_power_mesh`で
TAP2のVDD/GND M2ストラップは全チャネルを貫通し、`y_lo=max(0, ch_y0[0]-
margin)`/`y_hi=min(core_h, ...)`によって**物理的にはBBOX端（Y=0と
Y=core_h）まで正しく到達している**（コード確認済み）。しかし
`route_top_pins_nrow_fm.py`は信号ポート（rst_n, scl, ...）についてのみ
`add_m2_pin()`で実ピンレイヤー（M2PIN=(49,1)box + TXM2=(49,0)text）に
"rst_n"等のラベルを置くが、VDD/GNDにはこの処理が一度も行われていな
かった。`highlight_top_pins_nrow_fm.py`のVDD/GNDマーカーは診断専用の
別レイヤー(260,2)/(260,3)であり、実ピンレイヤーではない。v9_step_8の
TXM2/TXM1を直接ダンプして確認：テキスト12個、全て信号ポート名、
'VDD'/'GND'は0個——ユーザー指摘のとおり、金属はあるがピン注釈が本当に
無かった。

**恒久修正**：新規`script/add_power_pins_nrow_fm.py`。
`placement_nrow_fm_v9.json`からTAPカラムX（ローカル0.0, 534.6, 1069.2,
1609.2、全行共通）を取得し、各カラム×GND/VDD（オフセットは
`route_channels_nrow_fm.py`と同じTAP_GND_X_LOCAL=(1.0,4.4)/
TAP_VDD_X_LOCAL=(6.4,9.8)）×上端/下端の計16箇所に、
`route_top_pins_nrow_fm.py`のadd_m2_pin()と全く同じ規約（PIN_SIZE_UM=
3.0、ラベルをボックス中心に配置——v48修正、KLayoutのLVSテキスト抽出が
ラベルをボックス内側に要求するため）でM2PIN+TXM2ピンを追加。

- 入力：`layout/step8/v9_step_8_squeezed_top_pins_routed.gds`
- 出力：`layout/step8/v9_step_9_power_pins_added.gds`（新たな最終成果物）
- 追加16ピン確認：GND/VDD各4カラム×上下2端=16、既存12信号ピンと合わせて
  TXM2上に計28テキスト——座標を直接ダンプして全て正しい位置
  （x=2.7/8.1/537.3/542.7/1071.9/1077.3/1611.9/1617.3、y=1.5/934.6）
  にあることを確認。
- DRC再検証：M1/M2幅・間隔違反0、V1関連違反0（変化なし）。
- 接続性再検証：`verify_connectivity_nrow_fm.py` 147ネット・472ピン、
  ALL NETS FULLY CONNECTED, NO SHORTS DETECTED（変化なし——ピン注釈の
  追加のみで実配線ジオメトリは一切変更していないため当然の結果だが、
  独立検証として実施）。

**結論**：v9の最終物理設計成果物は
`layout/step8/v9_step_9_power_pins_added.gds`に更新。信号ポート12本
＋VDD/GND16本、計28本の実ピン（M1PIN/M2PIN+TXM1/TXM2）がBBOX端に
揃った状態でDRC 0・短絡0を維持。

### 77.45 v9のLVS用SPICEネットリストを生成

ユーザー要望「２．LVS用の .spice ファイルを作成ください」に対応。

**既存の確立された流儀の調査**：本プロジェクトではLVS参照ネットリスト
は「Verilogゲートレベルネットリスト→（スクリプトでワイヤ・ラベル・
コンポーネント配置を生成した）xschem `.sch`→ユーザーが手元のxschemで
開いてSPICEをエクスポート（`~/.xschem/simulations/<名前>.spice`、この
プロジェクトでは接続フォルダ`simulations/`として見える）」という経路
を辿ってきた（71.9節等）。KLayoutのLVSマクロ`run.lvs`は
`"simulation/" + source.cell_name + ".spice"`を参照する規約
（トップセル名は常に`i2c_slave_async_nrow_fm`、レイアウト側スクリプト
群のTOP_CELL_NAMEと一致）。

既存の`simulations/i2c_slave_async_nrow_fm.spice`（xschem由来）を確認
したところ、対応する`schematic/i2c_slave_async_nrow_fm.sch`は8/25生成
・**v9のネットリスト`src/i2c_slave_async_net_v9_rowbuf.v`（8/28生成）
より古い**——インスタンス数もv7/v8世代のもの（140個、うちDFFRB系の
命名`x_267_`等）でv9（137インスタンス、bit_cnt+last_bit_pending方式）
と一致しない。つまり既存の.sch/.spiceはv9に対して**既に陳腐化**して
いた。

**方針**：本セッションのサンドボックスにはxschem/klayout CLIが無い
（76節・77.43節で既確認）ため、xschem経由の手作業を経ずに、v9の
Verilogネットリストから直接、決定的にSPICEを生成するスクリプトを
新規作成（`script/gen_lvs_spice_v9.py`）。flatなインスタンス呼び出し
形式の`.subckt`/`x<name> ... TYPE`/`.ends`はVerilogとライブラリセル
のピン順から機械的に一意に決まる内容であり、xschem固有の情報は含ま
れないため、この生成方法でも内容としては等価。

**発見した2つの重要な癖**（いずれも直接確認、推測なし）：
1. v9_rowbuf.v内の138インスタンス中、`.VDD()`/`.GND()`をVerilog本文
   に明記しているのはBUFTH/BUF_X1/DEL1と一部のINV_X1/NOR2のみ（計約
   13個）——残りはRTLテキスト上は電源ピン省略。これは本プロジェクト
   の既存規約通り（`route_channels_nrow_fm.py`がPOWER/GROUND用途ピン
   を信号配線から意図的に除外し、TAP2メッシュで別途給電する設計、
   モジュールdocstring項目5）。物理的には全インスタンスが実際に給電
   されるべきなので、本スクリプトはVerilog本文の有無に関わらず**全
   インスタンスのVDD/GND位置引数を無条件にグローバルVDD/GNDへ結線**
   （246箇所で強制結線、実行ログで確認）。
2. 出力ポートの別名解決：`assign rw = rw_bit;`・
   `assign addr_match = addr_ok;`・`assign rx_data = rx_data_r;`（直接
   grepで確認した唯一の3件の実質的assign）——既存precedentファイルの
   慣習（ポート名そのものを使う、`rw_bit`/`addr_ok`ではなく`rw`/
   `addr_match`）に合わせて正規化。`sda_oe`は別名不要（駆動元DFFRBの
   QB出力が直接ネット"sda_oe"に接続——`.Q(sda_oe_r)`/`.QB(sda_oe)`と
   直接確認、"_r"側は未使用の別ネット）。

**各セルタイプの位置ピン順**は`simulations/<TYPE>.spice`の`.subckt`宣言
行から直接読み取り（推測せず）：INV_X1/BUF_X1/BUFTH/DEL1=VDD A Y GND、
AND2_X1/NAND2/NOR2/OR2/XOR2/XNOR2=VDD Y A B GND、AND4_X1/NOR4/OR4=A B
(C) D VDD Y GND系、NAND3/OR3=(C) VDD Y A B GND、NOR3=B C VDD Y A GND、
MUX2=A B S VDD Y GND、DFFRB=VDD QB D Q RSTB GND CK。

**インスタンス命名規約のバグと修正**：最初`x_{name}`（"x_" + 名前）で
生成したところ、Verilogの実インスタンス名自体が"_148_"のように前後
アンダースコア付きのため"x__148_"と二重アンダースコアになってしまっ
た。元の（陳腐化前の）`simulations/i2c_slave_async_nrow_fm.spice`を
上書き前に確認していた直接ダンプ結果「x_135_ VDD bit_cnt[0] _034_
GND INV_X1」（名前"_135_"に対し単一アンダースコア）と照合し、正しい
規約は`x{name}`（"x"のみ+名前、余分なアンダースコアを挿入しない）と
判明——修正して再生成。

**出力・検証**：
- `schematic/i2c_slave_async_nrow_fm_v9.spice`（新規、プロジェクト内
  永続成果物）
- `simulations/i2c_slave_async_nrow_fm.spice`（上書き——KLayout LVS
  マクロが参照する既定パス・ファイル名と一致させるため。ただし
  この接続フォルダはユーザー手元のxschemが書き出す個人用ディレクト
  リでもあるため、ユーザーが（陳腐化した）`i2c_slave_async_nrow_fm.
  sch`を開いて再ネットリストすると本ファイルは上書きされる点に注意
  ——ユーザーへの報告で明示。
- 検証：137/137インスタンス一致（Verilog側の正規表現カウントと生成
  ネットリストのインスタンス行数が完全一致）、VDD/GND両ネットとも
  存在確認、rw_bit/addr_ok/rx_data_rの未解決残存ゼロ、インスタンス名
  重複ゼロ。

**未対応・ユーザーへの申し送り事項**：
- xschem `.sch`自体（TAP2/TAP3/FILL2/FILL3のシンボル欠落含む、77.44
  で発見）は今回未着手。今回生成したのはSPICEネットリストのみで、
  xschemで開ける`.sch`が必要な場合は別途対応が必要。
- 本サンドボックスに`klayout`/`xschem` CLIが無いため、生成した
  `.spice`を実際にKLayoutのLVSエンジンへ通す最終確認はユーザー側の
  ローカル環境でのみ可能（76節・77.43節と同じ制約）。

### 77.46 実LVS実行でエラー「Not a valid pin name in circuit 'AND2_X1'」を修正

ユーザーが実際にローカル環境でLVSを実行したところ、KLayoutスクリプト
エラー：
```
RuntimeError: Not a valid pin name in circuit 'AND2_X1' in
'equivalent_pins': A
  .../_lvs_netter.rb:896:in `block in _equivalent_pins'
  .../05_Compare.lvs:101:in `execute'
```

**根本原因**：`/Users/okamura/Dropbox/91_OpenPDK/TR-1um/libs.tech/
klayout/tech/lvs/05_Compare.lvs`（79〜92行目）は`schematic(Sch_file,
reader)`でSch_file（`"simulation/" + cell_name + ".spice"`、今回は
77.45で生成した`i2c_slave_async_nrow_fm.spice`）を読み込んだ直後、
`equivalent_pins("AND2_X1","A","B")`等をライブラリセルごとに呼び出す
（対称入力ピンの入れ替え許容設定）。77.45版のスクリプトは各セルを
**インスタンス化するだけ**（`x148 ... AND2_X1`）で`.subckt AND2_X1
...`本体を一度も定義していなかったため、KLayoutのSPICEリーダーは
未定義サブサーキットを実ピン名の無いブラックボックスとして自動生成
——`equivalent_pins`が要求する実ピン名"A"が存在せず即エラー。

**発覚した欠落の裏付け**：未変更のprecedentファイル
`simulations/i2c_slave_async_net_v7_routed.spice`を確認したところ、
使用する全ライブラリセル（INV_X1, NAND2, NOR2, AND2_X1, ...）の
トランジスタレベル`.subckt ... .ends`本体が**同一ファイル内に埋め込
まれている**（xschemの階層フラット化ネットリスト出力の既定動作）。
77.45版はこの埋め込みを欠いていた。

**恒久修正**：`gen_lvs_spice_v9.py`に`load_library_bodies()`を追加。
使用される18セルタイプそれぞれについて、`simulations/<TYPE>.spice`
（トランジスタレベル本体、既存ライブラリファイル）から本文をそのまま
読み込み、末尾にインラインで埋め込む（INV_X1のみ`simulations/`に単独
ファイルが無いため、precedentファイル群から確認した本体をそのまま
定数として保持）。読み込み時、各本体の`.subckt`宣言行のピン順が
`SPICE_PIN_ORDER`と一致するかを検証（不一致なら即座にエラー——生成
インスタンス行とライブラリ定義のピン順が将来ズレた場合に静かに壊れる
のを防止）。

**検証**：`AND2_X1`の埋め込み本体を直接確認——`.subckt AND2_X1 VDD Y
A B GND`（ピンA・B実在）、`equivalent_pins("AND2_X1","A","B")`が要求
する条件を満たす。18セルタイプ全ての`.subckt`宣言行を出力ファイルから
抽出し、`SPICE_PIN_ORDER`と目視突合、全一致確認。生成スクリプト自体
の再実行時サニティチェックも18セル全てパス。

**出力**：`schematic/i2c_slave_async_nrow_fm_v9.spice`・
`simulations/i2c_slave_async_nrow_fm.spice`とも再生成・上書き
（383行、トップ階層1個＋ライブラリセル18個の`.subckt`）。実LVSでの
最終確認は引き続きユーザー側ローカル環境が必要（本サンドボックスに
klayout CLIが無いため）。

### 77.47 実LVS実行結果（.lvsdb共有）を解析、VDD/GND 2件の不一致を特定・修正

ユーザーが77.46修正後に再度LVSを実行し、結果の`.lvsdb`ファイル
（`layout/step8/LVS_error.lvsdb`、604KB、KLayoutのLVSデータベース
テキスト形式）を共有。本サンドボックスに`klayout` CLIは無いが、
`klayout.db`（Pythonバインディング）には`db.LayoutVsSchematic`クラス
が含まれ、`.lvsdb`ファイルの直接読み込み・回路ペア/ネットペア/デバイス
ペアの走査が可能——これを使い、GUI無しでLVS結果を直接解析した。

**解析結果**：
- 18個のライブラリセル回路（AND2_X1, NAND2, ...）は全てMatch——77.46
  の修正（サブサーキット本体の埋め込み）が正しく機能したことを確認。
- トップ回路`i2c_slave_async_nrow_fm`は総合判定NoMatch。ただし内訳を
  詳しく見ると、トップレベルピン27組は全てMatch、183ネット中181ネット
  がMatch——不一致はVDD・GNDの2ネットのみ、しかもサブサーキットピン数
  （137、インスタンス数と完全一致）も両側で一致。差異は
  **layout側にのみ存在する8個のデバイス端子**（`terminal_count`：
  layout=8 vs schematic=0）。

**根本原因の特定**：不一致ネットの`each_terminal()`を直接ダンプした
ところ、layout側に4個のデバイス（PMOS×2、NMOS×2）が直接存在し、
いずれも4端子（S/D/B/G）が全て同一レール（PMOSはVDD、NMOSはGND）に
短絡されている——ロジックとして無機能な、典型的な「MOSデカップリング
キャパシタ」構造。どのセルの実体かを切り分けるため、標準セルライブラリ
GDS（`LEF/TR-1um_STDCELL.gds`）のTAP2/TAP3/FILL2/FILL3各セルの
poly（GC、レイヤー(8,1)）層の有無を直接ジオメトリクエリで確認：
TAP2/TAP3はGC層の形状が**ゼロ**（77.44で疑ったTAPは今回の件とは無関係
と判明）、一方FILL2/FILL3は共にGC層形状2個がactive層(3,1)/(3,2)と重なる
——デカップリングキャパシタ構造はFILL2/FILL3側にあると特定。

**正確なL/W値の取得**：`01_Extract.lvs`・`02_Device.drc`
（`/Users/okamura/Dropbox/91_OpenPDK/TR-1um/libs.tech/klayout/tech/`
配下、既存のPDK・LVSデッキ）を読み、MOS抽出に使う派生レイヤー式
（`MP = AP.interacting(GC) & WN - ESD`等）を正確に把握した上で、
`klayout.db`の`db.LayoutToNetlist` + `db.DeviceExtractorMOS4Transistor`
を使い、**同じレイヤー式を再現して実際にFILL2/FILL3セル単体に対して
本物のデバイス抽出を実行**（手計算や推測ではなく、KLayout自身の抽出
エンジンで導出）。結果：
- FILL2: PMOS(L=3.2um, W=21.2um) + NMOS(L=3.2um, W=13.1um)
- FILL3: PMOS(L=8.6um, W=21.2um) + NMOS(L=8.6um, W=13.1um)

いずれも端子構成（S/D/B同一レール、Gは反対レール）は`.lvsdb`から確認
した実際の接続パターンと一致。

**恒久修正**：`gen_lvs_spice_v9.py`に`FILL_DECAP`定数（上記L/W値）・
`count_fill_instances()`（`placement_nrow_fm_v9.json`から実際の配置
インスタンス数を取得——v9はFILL2が36個、FILL3が92個）・
`build_fill_decap_lines()`（配置インスタンス1個につきPMOS+NMOSペアを
1組、計256個のMOSFETデバイス行を生成）を追加。レイアウト側の
`combine_devices`によるマージ処理と同じ粒度で比較できるよう、事前に
手動でマージ・多重度パラメータ化はせず、**実配置と同数**のデバイスを
個別に生成する方針とした（マージ後の等価性のみに依存し、KLayoutの
マージ挙動の詳細仮定を避けるため）。トップレベル`.subckt`内・`.ends`
直前に挿入。

**検証**：生成ファイルを直接確認——`M_FILL2_1_p VDD GND VDD VDD PMOS
w=21.2u l=3.2u`等256行が正しくトップサブサーキット内に挿入され、
`M_FILL2_*_p`/`_n`が36個ずつ、`M_FILL3_*_p`/`_n`が92個ずつ、期待通り
生成されていることを確認。

**出力**：`schematic/i2c_slave_async_nrow_fm_v9.spice`・
`simulations/i2c_slave_async_nrow_fm.spice`とも再生成・上書き（405行）。
実LVSでの最終確認（VDD/GND不一致が解消しCongratulations判定になるか）
は引き続きユーザー側ローカル環境でのみ可能。

**確認（ユーザー報告）**：ユーザーが実際にローカル環境で本修正版
`.spice`を使いLVSを再実行し、「LVSはクリーンになりました」と報告。
FILL2/FILL3のデカップリングキャパシタ追加でVDD/GND不一致が解消し、
v9コア（`i2c_slave_async_nrow_fm`、137インスタンス、18ライブラリ
セル、TAP電源メッシュ＋FILL2/FILL3デカップリングキャパシタ含む）の
レイアウト対schematic LVSが完全一致（Congratulations判定）した。

**結論**：v9の物理設計・LVS検証が完了。77.44（BBOX端VDD/GND実ピン
追加）・77.45（schematic側.spice新規生成）・77.46（ライブラリセル本体
埋め込み）・77.47（FILL2/FILL3デカップリングキャパシタ追加）の一連の
修正により、コア単体のレイアウト・接続性・DRC・LVSが全て確認済みの
状態に到達。残るのは引き続きIRSIM検証（チップ統合後）・実DRCデックの
正式最終確認など、design_notes既存のTODO項目のみ。

## 78. トップブロック（チップ統合）着手

v9コア単体の物理設計・LVSが完了したため、GIOパッドリングとの統合
（トップブロック作成）に着手。ユーザー指示：「まず、schematic/
tr_1um_i2c_slave_async.sch を参考にしてGIOとの接続関係を整理して
JSON ファイルに記録お願いします。」

### 78.1 GIO⇔コア接続関係の抽出、`schematic/gio_connections.json`として記録

**手法**：`schematic/tr_1um_i2c_slave_async.sch`はxschemのラベル方式
（同名ラベルが電気的に同一ネット）で書かれており、目視での対応付けは
誤りやすいため、以下の厳密な手順で機械的に導出した。
1. `schematic/OSS_FRAME_GIO.sym`・`schematic/i2c_slave_async_nrow_fm.
   sym`から各ピンのシンボル内ローカル座標（ピンボックスの中心）を取得。
2. `.sch`内のインスタンス配置行（`C {...} X Y rot flip {name=...}`
   ——GIOフレームx1は(280,-530)、コアx2は(780,-400)、共に回転・反転
   無し）からローカル座標→絶対座標への変換を適用。
3. 算出した各ピンの絶対座標を、`.sch`内の配線ラベル（`N`行・
   `devices/lab_wire.sym`インスタンス）の座標と突き合わせ、どのピンが
   どのネット名を持つかを座標一致で確定（推測に依らない）。
4. GIOパッドセル内部の`HIZ`/`OUT`制御ピンの意味は
   `simulations/OSS_FRAME_GIO.spice`（xschem展開済みトランジスタレベル
   ネットリスト、`OSS_ESD_5V_DIO`サブサーキット）をトランジスタ段から
   追跡して確定：`HIZ=1`（VDD結線）→ドライバ無効化（Hi-Z/入力専用）、
   `HIZ=0`（VSS結線）→ドライバ有効化（`OUT`のデータをPADへ出力）。

**主な発見**：
- コアの14個の入出力ポート中、`rx_valid`・`addr_match`・`rw`・`busy`
  の4個が本トップスキーマティックのどのパッドにも接続されていない
  （未配線）。
- `SDA`（P13/HIZ13/OUT13）はI2Cのオープンドレイン規約通り正しく実装
  されている：`OUT13`はVSSに固定結線（常にLow方向にしか能動駆動しない）
  、`HIZ13`は`core.sda_oe`（ネット名`SDA_O`）で動的制御。ただし
  `HIZ=0`で有効という極性から逆算すると、`sda_oe`は「0でLow駆動確定・
  1でHi-Z解放」という**負論理（active-low）**である必要がある——RTL側
  の実際の極性と一致するか要確認（`open_questions`に記載）。
- `HIZ2`・`HIZ7`・`HIZ15`はVDD結線（Hi-Z/入力専用、`scl`・未使用パッド
  ・`rst_n`に対応——入力ピンとして妥当）。一方`HIZ9`・`HIZ10`はVSS結線
  （ドライバ有効）だが対応する`P9`/`P10`・`OUT9`/`OUT10`はいずれも
  未配線——ドライバが有効なのに駆動データ未定義という不整合な状態を
  発見、要確認事項としてフラグ。
- 8個のHIZピン（HIZ1,3,4,5,6,11,12,14）は`DIS`という名前のネットに
  結線されているが、このネット自体はスキーマティック内のどこにも
  VDD/VSSへ結線されていない（未解決のプレースホルダー的ネット）——
  入力用パッド（P1,3,4,5,6,12,14）はVDD結線が期待値、出力用パッド
  （P11、rx_data[1]用）はVSS結線が期待値と推測されるが、確定はユーザー
  確認が必要。

**出力**：`schematic/gio_connections.json`（新規）。構成：
`core_ports`（コアの入力/出力/電源ポート一覧）、`frame_pads`（GIOの
14データパッド＋電源パッド一覧）、`connections`（パッド単位の要約）、
`connections_per_terminal_detail`（P<n>/OUT<n>/HIZ<n>個別のネット・
対応コア信号）、`core_outputs_not_connected_to_any_pad`、`power`、
`open_questions`（上記4件の要確認事項）。JSON構文検証済み。

**申し送り**：チップ統合（GDSレベルでのGIOフレーム配置・配線）を進める
前に、`open_questions`の4件（特に`DIS`ネットの意図した結線先、P9/P10
の扱い、`sda_oe`極性、rx_valid/addr_match/rw/busyの扱い）についてユー
ザー確認が必要。

### 78.2 `src/tr_1um_i2c_slave_async.gds`にコア＋GIOを配置（配線前）

ユーザー指示：「まず、...V7とBBOXサイズを比較してください」（78節
冒頭で実施済み、v7=1632.6x988.2um、v9=1632.6x936.1um、高さ-52.1um）
に続き「それではトップブロックを作りましょう...src/tr_1um_i2c_slave_
async.gds にコアとGIOを配置します。OSS_FRAME_GIO は FRAME/TR-1um_
frame_25x25.gdsからインポートして（0,0）に配置してコアはV7に比べて
Yが小さくなった分50um 移動して(-180,-140)で配置してください。また
コア部の下にV7と同じようにPTECTエリアを設定ください。配線する前に
確認しますのでそこで止めてください。」

**X座標の確認**：既存v7の`src/tr_1um_i2c_slave_async.gds`を直接読み、
コアの配置オフセットが(-810.0,-190.0)であることを確認。コアの
ネイティブBBOX中心X=810.0（幅1632.6umの中点）がオフセット-810.0で
GIOフレーム内のX=0にちょうど中央揃えされる設計だった。ご指示の
「(-180,-140)」はXが-180だが、これだと大きく中心から外れる
（GIOフレームは2500x2500um角）。AskUserQuestionで確認したところ、
「X=-810のまま(V7と同じ中央揃え、推奨)」を選択——X=-180は転記ミスと
判明、X=-810（V7と同一）・Y=-140（ご指示通り、+50um上方移動）を採用。

**配置内容**（`script/assemble_top_v9.py`、新規）：
- OSS_FRAME_GIO：`FRAME/TR-1um_frame_25x25.gds`から新規インポート、
  (0,0)配置（ネイティブBBOX -1250,-1250〜1250,1250、v7組み込み版と
  同一サイズを確認）。
- コア（`i2c_slave_async_nrow_fm`、v9最終成果物
  `layout/step8/v9_step_9_power_pins_added.gds`）：オフセット
  (-810.0,-140.0)。チップ座標系でのBBOX：
  (-816.3,-140.0)〜(816.3,796.1)——X方向はV7と完全に同一区間（中央
  揃え維持）、Y方向はV7の(-190.0〜798.2)に対し上方へ移動（コア底辺
  -190.0→-140.0、コア上辺798.2→796.1、高さ減少52.1umに対し50um分
  平行移動のため上辺は約2.1um低い位置に）。
- PTECT area（layer 63/1）：V7と同じ関係規則（コア底辺から10.0um
  ギャップ、1600x600um、X中心0）をコアの新しい底辺(-140.0)に対して
  再計算：(-800.0,-750.0)〜(800.0,-150.0)。V7の(-800,-800)〜
  (800,-200)と比べY方向のみ+50um平行移動、サイズ・ギャップは同一。

**検証**：生成した`src/tr_1um_i2c_slave_async.gds`をklayout.dbで直接
読み返し、GIO/コアのインスタンス変換・PTECTボックス座標が意図通りで
あることを確認。コアBBOXがGIOフレーム範囲内（±1250um）に収まって
いることも確認。

**ユーザー指示通り、配線は行わずここで停止**。次のステップ（信号・
電源配線）は既存v7の`script/route_gio_core.py`が確立された手法だが、
78.1の`gio_connections.json`にある未解決事項（DIS ネットの結線先、
P9/P10の扱い、sda_oe極性、rx_valid/addr_match/rw/busyの扱い）の確認
がユーザーレビュー待ち。

### 78.3 PTECTエリアをチャネル幅~120umになるよう修正（ユーザーがGDS
を直接編集、スクリプトへ反映）

ユーザー：「確認しました。PTECTエリアをトップ配線のチャネル幅が
120um程度になるように修正しました。確認してスクリプトに反映して
ください。」——`src/tr_1um_i2c_slave_async.gds`を直接手編集。

**確認内容**：GDSを読み返し、PTECTボックスが(-800,-800)〜(800,-150)
（サイズ1600x650um）に変更されていることを確認——78.2版
(-800,-750)〜(800,-150)、600x600umからボックス**下辺**のみ-750.0→
-800.0へ50um拡張（上辺=コア底辺からの10umギャップは不変）。

**「チャネル幅120um」の意味を特定**：v7の既存配線スクリプト
`script/route_gio_core.py`のNETS辞書で、GIO側ピンの投影半径として
一貫して`921.7`（um、チップ中心からの距離）が使われていることを確認
（例："...,921.7,'TOP','M2'..."が多数）——これがGIOの実ピンが並ぶ
配線可能境界。PTECT下辺(-800.0)からこの境界(921.7)までの距離
＝921.7-800.0＝121.7um ≈ ユーザーの言う「120um程度」と一致。これが
コア下方のGIO⇔コア間トップ配線チャネルの実質幅。

**恒久反映**：`script/assemble_top_v9.py`の`PTECT_HEIGHT`を600.0→
650.0に変更（`PTECT_GAP_BELOW_CORE=10.0`・X範囲[-800,800]は不変）。
スクリプトを再実行し、`src/tr_1um_i2c_slave_async.gds`を再生成——
PTECTボックス(-800.0,-800.0)〜(800.0,-150.0)が手編集版と完全一致する
ことを確認。スクリプトが再び唯一の正としてGDSと同期した状態。

### 78.4 `gio_connections.json`の内容検証——route_gio_core.pyとの
突き合わせ

ユーザー：「JSON ファイルを検証出来ましたか？」——78.1で作成した
`schematic/gio_connections.json`は生成時にJSON構文の妥当性のみ確認
済みで、**内容そのもの**（スキーマティックからの手動座標照合が正しい
かどうか）はまだ独立検証していなかった。ユーザー指摘を受け、既存の
v7チップ配線スクリプト`script/route_gio_core.py`（実際に配線され、
コメントからLVSで確認・修正された履歴が読み取れる、物理的な正解に
近い情報源）と突き合わせて検証した。

**照合結果**：
- VDD結線（HIZ2, HIZ7, HIZ15）・VSS結線（HIZ9, HIZ10, OUT13）は
  route_gio_core.pyの`VDD_HIZ2`/`VDD_HIZ7`/`VDD_HIZ15`・
  `VSS_HIZ9`/`VSS_HIZ10`/`VSS_OUT13`という命名のネット定義と完全一致
  ——78.1の判定は正しかったと確認。
- **「DIS」ネットの正体が判明・open_questionsから解決済みへ移動**：
  route_gio_core.pyの`DIS_CHAIN_POINTS`/`DIS_LINKS`により、DISは
  P7パッド＋8個のHIZピン（HIZ1,3,4,5,6,11,12,14）を数珠つなぎに結線
  した**単一の実ネット**（配線効率のためのデイジーチェーン、方向性の
  ある「P7が駆動する」という意味ではない）であり、VDD/VSSへは意図的
  に結線しない——外部（テスタ・ボード側）からP7パッド経由で論理値を
  与える設計と判明。78.1時点の「未解決の浮遊ネットではないか」という
  疑念は誤りで、正常な設計。
- **OUT2**：route_gio_core.py自身のコメントに「以前OUT2が誤ってVSSに
  短絡されていたが、LVS参照ネットリストとの照合で検出・修正し、
  スキーマティック通りの未結線（no-connect）に直した」という履歴が
  記載されており、78.1の「OUT2未接続」判定と一致——独立に裏付け。
- **P9/P10**：HIZ9/HIZ10がVSS結線（ドライバ有効）なのにP9/P10・
  OUT9/OUT10が未配線という状態は、route_gio_core.pyのNETS辞書にも
  P9/OUT9・P10/OUT10のエントリが一切無く、**v7の実チップでも全く同じ
  状態が引き継がれている**ことを確認——v9固有の見落としではなく、
  v7から持ち越された仕様（意図的か単なる未使用スペアかは要ユーザー
  判断だが、「バグ」ではない）と判明。
- **rx_valid/addr_match/rw/busy未配線**：route_gio_core.pyのNETS辞書
  にもこの4信号のエントリが無く、独立に確認——v9固有ではなくv7から
  一貫した状態。
- **sda_oe極性**：route_gio_core.py（純粋な物理配線スクリプトで論理
  極性には関与しない）では解決されず、引き続き要確認事項として残存。

**JSONファイル更新**：`_meta.cross_verified_against`を追加、
`pad_cell_behavior.dis_net_resolved`を新設してDISネットの正体を記載、
`connections`内の該当ノートを更新（DIS＝正常な外部制御ネット、P9/P10
＝v7から持ち越し確認済み）、`open_questions`を`open_questions_resolved`
（DISネット、1件）と`open_questions`（P9/P10の扱い、sda_oe極性、
rx_valid/addr_match/rw/busyの扱い——3件、いずれもv7から一貫した状態
である旨を明記）に分割。JSON構文再検証済み。

### 78.5 V7→V9でコアの信号出口位置がどう変わったか調査

ユーザー：「V7とコアからの信号の出口位置が変わっていると、GIOとの
接続で困難が生じる可能性があります。調査出来ますか？」——的確な懸念。
調査した結果、**実際に問題になる差異が見つかった**。

**手法**：v9の実ピン位置を`layout/step8/v9_step_9_power_pins_added.
gds`から直接抽出（M1PIN(48,1)/M2PIN(49,1)+TXM1(48,0)/TXM2(49,0)の
テキストラベル座標をklayout.dbで読み取り）、現在の配置オフセット
(-810.0,-140.0)（`assemble_top_v9.py`と同一）でチップ座標系に変換。
v7側は`script/route_gio_core.py`のNETS辞書に既にチップ座標系で
ハードコードされている値をそのまま使用。両者を信号ごとに突き合わせ。

**発見1（重大）：4信号がBBOX上で出口の「辺」自体が変わっている**：
- `sda_oe`：v7ではRIGHT辺だったが、v9ではTOP辺に変化。
- `tx_data[2]`：v7 TOP → v9 RIGHT。
- `tx_data[3]`：v7 TOP → v9 RIGHT。
- `tx_data[7]`：v7 RIGHT → v9 TOP。

route_gio_core.pyのリング配線アルゴリズム（`perimeter_s`/
`ring_waypoints`/`project_to_R`）は各ネットがどの辺から出るかに基づいて
経路を計算するため、この4信号は座標値の微調整では済まず、GIO側の
接続先パッド再選択・エッジ/レイヤー指定の作り直しが必要——単純な
パッチでは、コア本体やPTECTエリアを突っ切る配線を生成しかねない。

**発見2：同じ辺に留まった信号も、辺ごとに異なる規則性でシフト**：
- TOP辺の一部（`rst_n`・`tx_data[4,5,6]`）：X方向に一律+226.8um、
  Y方向に一律-2.1um（高さ減少分の反映）。
- 一方、同じくTOP辺の`scl`・`sda_in`・`rx_data[0]`は上記と異なる量
  （それぞれ-270.0um・-270.0um・-232.2um）でシフト——TOP辺全体が
  単純平行移動しているわけではなく、内部配置（NUDGE_BEFORE適用や
  FILL2/FILL3個数、チャネル圧縮結果がv7と異なる）に応じて信号ごとに
  個別にズレている。単一の補正定数は使えない。
- RIGHT辺（`tx_data[0,1]`）：X完全一致（0.0um、行幅固定のため）、
  Yのみ一律+5.4um（トラックピッチ1本分）。
- LEFT辺（`rx_data[1]`〜`rx_data[7]`、7本）：X完全一致（0.0um）、
  Yは全て一律-16.2umでシフト——このグループは全体で揃って移動。

**発見3：VDD/GND電源分配アーキテクチャが根本的に別物**：
- v7：コア内部の行電源レールから直接、LEFT辺（GND、x=-810）と
  RIGHT辺（VDD、x=810）の固定X・特定行Y位置でM1分岐タップ→10um M2
  バスに集約→GIOへ1本ずつ出口。
- v9：TAP列4本×上下端＝各ネット8本ずつのピンがTOP/BOTTOM辺に分散
  （77.44節、TAP列のローカルX位置に対応）。
- route_gio_core.pyのVDD_ROWS/GND_ROWS/バス配線一式（v7の行レール
  タップ方式に特化）はv9にはそのまま使えず、構造的な作り直しが必要
  （v9の8ピン/ネットをどう1本または複数本の出口にまとめるか、設計が
  必要）。

**発見4（参考）**：`addr_match`・`busy`・`rw`・`rx_valid`はv7でも
GIOへ未配線だった（78.4で確認済み）が、v9では実際のBBOX出口ピン座標
が存在する（addr_match: RIGHT、busy: TOP、rw・rx_valid: BOTTOM）ため、
今回新たに配線したい場合の材料は揃っている。

**出力**：`schematic/v7_v9_pin_migration.json`（新規）——信号ごとの
v7/v9座標・辺・差分（dx/dy）の全24信号分の詳細テーブル、上記4分類の
サマリー、および推奨事項（route_gio_core.pyのNETS辞書は手パッチでは
なく再生成が必要、電源配線セクションは構造的作り直しが必要）を記録。
JSON構文検証済み。

**結論**：ユーザーの懸念は的中——v7の配線スクリプトをそのまま座標
だけ差し替えて再利用することはできず、特に4信号のエッジ変化と電源
アーキテクチャの違いへの対応が必要。次の配線ステップに進む前に、
この移行方針（NETS辞書の再生成方法、電源出口の集約方法）について
ユーザーとすり合わせが必要。

### 78.6 LVS "Port mismatch '$I1117' vs. 'SDA_OE'" の調査

**報告**：ユーザーがKLayout Netlist Database BrowserのLogタブの
スクリーンショットを提示——Netlist "LVS[2]"、layout
`v9_step_9_power_pins_added.gds`にて「Circuit
i2c_slave_async_nrow_fm ⇔ I2C_SLAVE_ASYNC_NROW_FM」の下に1件、
"Port mismatch '$I1117' vs. 'SDA_OE'"。マッチング（ネット位相）の
問題ではなく、ポート名解決の問題である旨、ユーザー確認。

**該当機能**：`05_Compare.lvs`の`flag_missing_ports`（`compare()`とは
別の厳格チェック、line 94-104）。KLayoutフォーラム/ドキュメント確認
（[lvs_compare.html](https://www.klayout.de/doc-qt5/manual/lvs_compare.html)、
[lvs_tweaks.html](https://klayout.org/downloads/master/doc-qt4/manual/lvs_tweaks.html)）：
schematic側の各ポートについて対応するlayout側ネットの名前（ラベル）
を確認し、不一致ならエラーを出す。layout側が無名の場合は自動生成の
`$I<番号>`名になる。

**調査手順**：
1. `v9_step_9_power_pins_added.gds`のsda_oeピン周辺（TXM2(49,0)
   テキスト・M2PIN(49,1)ボックス・実M2(20,0)メタル）を直接
   klayout.dbで確認——テキスト"sda_oe"はM2PINボックスの中心に正確に
   位置し、実M2メタル(832.60,875.70)-(836.00,936.10)とも重なって
   いる。重複ラベルも無い（GDS全域を"sda_oe"で検索し1件のみ）。
   幾何形状上の欠陥は見当たらず。
2. PDKのLVSデック本体（`01_Extract.lvs`〜`05_Compare.lvs`）を精査。
   `run.lvs`の%includeは全てコメントアウトされており、実際の
   connect()ルール（M1-V1-M2の接続、およびラベルのアタッチ
   `connect(M1,M1_LBL)`/`connect(M2,M2_LBL)`）はDRCデック
   `02_Device.drc`内（line 187-190）にあることを確認——LVSデックが
   これをDRCデックと共有している。
3. このconnect()ルール一式を`klayout.db.LayoutToNetlist`で忠実に
   再現し、v9コアGDSに対して独立に再抽出テストを実施。
   - フラット抽出（`db.RecursiveShapeIterator`で階層を潰す）：
     sda_oeネットは正しく"sda_oe"と命名され、M2形状bboxは
     (832.60,875.70)-(836.00,936.10)と一致。
   - 階層（deep）抽出（GDSは実際に887個のサブセルインスタンスを
     持つ階層構造——`db.LayoutToNetlist(RecursiveShapeIterator)`で
     再現）：25回路（24標準セル型+トップ）を正しく認識。sda_oeネット
     はここでも正しく"sda_oe"と命名され、トップ回路の全15ピン中
     無名（$I形式）は0件。
   - いずれの再現でも、sda_oeのポート名解決に問題は見られなかった。

**結論（暫定）**：現在の`v9_step_9_power_pins_added.gds`と
`schematic/i2c_slave_async_nrow_fm_v9.spice`を、実PDKの接続規則を
忠実に再現した独立抽出で検証した限り、sda_oeピンのラベル/ネット名
解決は正常。また、ユーザーが以前提示した`layout/step8/
LVS_error1.lvsdb`も全項目Match・ログ0件で、今回のエラーとは無関係
（別実行の可能性）。以上より、スクリーンショットの"LVS[2]"は
現在のファイル一式に対する最新の実行ではなく、以前のバージョン
（今回のセッションで行った一連の修正のいずれかより前）を参照した
古いKLayoutセッション/キャッシュである可能性が高いと判断。
ユーザーに、現在のGDS（`layout/step8/v9_step_9_power_pins_added.gds`）
と現在のSPICE（`schematic/i2c_slave_async_nrow_fm_v9.spice`または
同期済みの`simulations/i2c_slave_async_nrow_fm.spice`）を明示的に
再読み込みしたクリーンな状態でLVSを再実行し、.lvsdbファイルとして
保存の上、再現するか確認を依頼（再現する場合は最新の.lvsdbを直接
調査する）。

### 78.7 真因判明・修正：`PORT_NET_ALIAS`の`sda_oe`エイリアスが
v6時代のまま残存

**ユーザー報告**：`LVS_error1.lvsdb`のNETクロスリファレンスを直接
確認するよう指示。「ピンのマッチングはlayout:sda_oe / schematic:
SDA_OEだが、**NET**のマッチングだとlayout:sda_oe ⇔ schematic:
SDA_OE_Rになっている」との指摘。

**確認**：`.lvsdb`をKLayoutの`NetlistCrossReference` APIで直接開き、
`each_net_pair()`をsda_oeで検索したところ、ユーザーの報告どおり
以下の2ペアを検出（従来、`each_log_entry()`/`each_error()`しか見て
おらず、ネットペアのNAME自体は未検証だったのが78.6での見落とし）：
```
NET PAIR: layout='$I1117'  schematic='SDA_OE'    status=Match
NET PAIR: layout='sda_oe'  schematic='SDA_OE_R'  status=Match
```
つまり：レイアウト側でラベル"sda_oe"が付いている配線は、電気的には
実際にはSDA_OE_R（Qピン、非反転出力）と同一ネット。一方、本当の
sda_oe（QBピン、反転出力＝真のポート）にはBBOXエッジへのラベル付き
出口が存在せず、無名（$I1117）のまま。

**真因**：`script/highlight_top_pins_nrow_fm.py`の`PORT_NET_ALIAS`
辞書（修正前）：
```python
PORT_NET_ALIAS = {
    "sda_oe": "sda_oe_r",   # v6時代のRTL用（assign sda_oe = sda_oe_r;）
    "addr_match": "addr_ok",
    "rw": "rw_bit",
}
```
`route_top_pins_nrow_fm.py`はこのモジュールから`port_net_name`を
importして`gather_pins()`内で`net_to_port[port_net_name(port)] = port`
という辞書を構築している。`port_net_name("sda_oe")`は
`PORT_NET_ALIAS["sda_oe"]`＝`"sda_oe_r"`を返すため、
`net_to_port["sda_oe_r"] = "sda_oe"`となり、ネット名"sda_oe_r"を
持つピン（DFFRB _293_のQピン、"sda_oe"ではなく）が誤って"sda_oe"
ポートの出口として捕捉され、そのピンの物理X座標（832.6-836.0、Q
ピンの位置）にBBOXエッジ配線＋"sda_oe"ラベルが描かれていた。真の
sda_oe（QBピン、X=821.8-825.2、net名"sda_oe"そのもの）はnet_to_port
に"sda_oe"というキーが存在しないため一度も走査対象にならず、BBOX
エッジへの出口が生成されなかった。

このコメント自体（`highlight_top_pins_nrow_fm.py`冒頭）にも明記の
とおり、"sda_oe": "sda_oe_r"というエイリアスはv6時代のRTL
（`i2c_slave_async_net_v6.v`の`assign sda_oe = sda_oe_r;`）由来。
v9のRTL（`src/i2c_slave_async_net_v9_rowbuf.v`）ではこのassignは
既に無く、QBピンがネット"sda_oe"に直接接続される設計に変更されて
いた（本セッション既出のRTL grep確認と一致）ため、このハード
コードされたエイリアスがv9に対して古いまま残っていたのが真因。
`addr_match`/`rw`のエイリアス（`addr_ok`/`rw_bit`）はv9のRTLでも
現役（`assign addr_match = addr_ok;`/`assign rw = rw_bit;`が実在）
のため変更不要。

**修正**：`PORT_NET_ALIAS`から`"sda_oe": "sda_oe_r"`エントリを削除
（コメントで経緯を記録）。

**再生成**：
1. `run_v9_step7_squeeze_step6_toppins.py`の`step_top_pins()`を、
   既存の圧縮済みGDS（`v9_step_7_squeezed.gds`）・pin_map・
   net_shapesを再利用して単独実行（squeezeステップ自体はこのバグと
   無関係のため再実行不要）→`v9_step_8_squeezed_top_pins_routed.gds`
   再生成。sda_oeの出口X座標が823.5um（QBピンの実位置と一致）に
   変わったことを確認。
2. `add_power_pins_nrow_fm.py`再実行→`v9_step_9_power_pins_added.gds`
   再生成。
3. `drc_check_nrow_fm.py`：M1/M2幅・スペース違反0、V1関連違反0
   （DRC 0/0、変化なし）。
4. `verify_connectivity_nrow_fm.py`：147ネット・472ピン、
   ALL NETS FULLY CONNECTED, NO SHORTS DETECTED（変化なし）。
5. klayout.dbによるLVS接続規則（`02_Device.drc`のconnect()一式）の
   独立再現で再検証：sda_oeネットのM2 bboxが
   (821.80,875.70)-(825.20,936.10)に変化（修正前は
   (832.60,875.70)-(836.00,936.10)＝Qピンの位置だった）。GDS全域
   検索で"sda_oe_r"ラベルが一切存在しないことも確認（内部ネットは
   正しく非露出のまま）。トップ回路の全15ピン中、無名（$I形式）は
   引き続き0件。

**結論**：ユーザー指摘のNETミスマッチが真因特定の決定打だった
（78.6時点のポート名チェックのみの調査では発見できなかった）。
根本原因はラベリングやLVS抽出側ではなく、`route_top_pins_nrow_fm.py`
が参照する`PORT_NET_ALIAS`辞書のv6時代の古いエイリアスがv9の
RTLに対して電気的に誤ったピンを選んでいたという配線バグ。修正・
再生成・DRC/接続性の再検証まで完了。実際のKLayout LVS
（`equivalent_pins`/`flag_missing_ports`含むフル実行）での最終
確認をユーザーに依頼。

**最終確認（ユーザー実行）**：実KLayout LVS（deep mode, strict port
mode, flag_missing_ports有効）を`v9_step_9_power_pins_added.gds`に
対して再実行し、"Congratulations! Netlists match."でクリーン通過を
確認。sda_oe PORT_NET_ALIASバグの修正が正しく機能したことが実LVSで
実証された。v9のコア物理設計（DRC 0/0、接続性0ショート、LVSクリーン）
が確定。次はトップブロック配線（78.5/78.6の移行方針検討）に進む。

### 79. トップブロック配線 再開：残課題の解決とv7→v9ピン移行データの
再生成

**v9_step_9再生成に伴う再検証**：78.7のsda_oe修正でコアGDS
（`v9_step_9_power_pins_added.gds`）が更新されたため、
`src/tr_1um_i2c_slave_async.gds`（`assemble_top_v9.py`再実行、配置
座標は変化無し：コアbbox (-816.3,-140.0)-(816.3,796.1)、PTECT
(-800,-800)-(800,-150)）と`schematic/v7_v9_pin_migration.json`
（新しいsda_oe位置=TOP辺、チップ座標(13.5,794.6)で再抽出）を再生成。
edge変化4信号（sda_oe/tx_data[2,3,7]）は修正後も同じ結果——sda_oeの
バグはTOP辺内での位置選択の誤りであり、v7→v9でのエッジ自体の変化
（RIGHT→TOP）は元々の設計上の差分であることを再確認。

**gio_connections.jsonのopen_questions 3件を解決**：
1. **core.sda_oe極性**：`src/i2c_slave_async.v` line 101-104/278-282
   に明記されていたRTLコメントで解決（ユーザー確認不要、コードに
   根拠あり）——v3で意図的に極性反転済み（`assign sda_oe = ~sda_oe_r;`）、
   「0=LOW駆動、1=解放」がHIZ13ピンの仕様（HIZ=L→駆動、HIZ=H→解放）
   に直接一致するよう設計されている。インバータ不要、直結が正しい。
2. **P9/P10パッド**：ユーザー判断——v7のまま維持（HIZ9/HIZ10=VSS固定・
   ドライバenabled・データ未配線）。変更なし。
3. **未配線出力4本**（rx_valid/addr_match/rw/busy）：ユーザー判断——
   全て未配線のまま、v7と同じ挙動を維持。

`gio_connections.json`の`open_questions`は空に、上記3件を
`open_questions_resolved`へ移動。

**次のステップ**：`route_gio_core.py`のNETS辞書のv9向け再生成（実際の
配線）と、v9電源分配アーキテクチャ（TAP列4本×上下端=8ピン/ネット）
に対応した新しい電源配線スキームの設計。

### 79.1 v9チップレベル配線の実装（script/route_gio_core_v9.py）

ユーザーの「電源以外のJSONの接続情報をGIOとコアのピンの配置で最適化
出来ますか？」に応え、24信号ネットについても電源と同じ手法（実測
コアピン位置 + v7と同一のGIO側固定パッド位置を使い、CW/CCW方向と
レーン割当をv7と同一の貪欲法アルゴリズムで再計算）で最適化し、
`schematic/v9_signal_routing_plan.json`として保存。必要レーン数11
（v7の実績値11と完全一致）。「進めましょう」の指示を受け、
`script/route_gio_core_v9.py`として実装：

- 24信号ネット：v9_signal_routing_plan.jsonの座標をそのまま使用、
  共有プール方式（v7と同一のR_NOM/LANE_R0/LANE_PITCH/NEAR_R定数）。
  rx_data[1]のみv7と同じくforce_dir="CW"を継承（PTECT近傍回避、
  安全側の踏襲）。
- 電源（VDD/GND）：v9_power_routing_plan.jsonの8本（TAP列4本×2ネット）
  を、v7と同じGIO側8箇所の固定タイ先（フレームschematic必須）へ
  private-radius方式で接続。初版はレッグ毎に1.5umずつ異なる半径を
  割り当てたが、M1配線幅1.8um+間隔1.4um要求に対して1.5umピッチは
  不足しM1スペース違反5件を新規発生。信号ネットと同じ区間スケジュ
  ーリング（貪欲法）を電源8本にも適用したところ必要レーンは実は2本
  のみ（大半のレッグは弧が重ならない）と判明、R=828.0/834.0の2レーン
  に変更して解消。
- DISチェーン（P7+8HIZピン）：GIO⇔GIOのみでコア非依存のためv7の値を
  そのまま流用。

**検証**（layout/step8/v9_top_routed.gds、klayout.db直接検証）：
1. DRC（M1/M2幅・スペース、フラット化トップセル全体）：新規違反0件
   （未配線ベースライン比較で完全一致確認）。M2側に既存5件の違反が
   あるが、未配線状態から同一座標・同一件数で存在——GIOフレーム自体
   の隅（(-964,-1036)付近、コア/新規配線から遠く離れた場所）に由来
   する既存の問題で、v9の配線とは無関係。
2. クロスネットショート検証：レイヤー別（M1/M2を混同しない）で全
   ネット間の直接形状重なりを総当たりチェック——本物のショートは
   0件。DISチェーンの隣接リンク同士（同一DISネットの一部として
   意図的に共有端点を持つ）のみ検出、想定通り。
   （初回チェックはM1/M2を1つのRegionにまとめてしまい、sda_oeの
   M2垂直リードがrx_data[0]のM1水平区間を「クロス」しただけの
   非電気的接触を誤検出——ビア無しの異層交差はDRC上安全、レイヤー
   別に分離して再検証し誤検出と確認。）

lanes_needed=11（信号）+2（電源）、DISチェーン8リンク、で全て正常に
描画完了。次はユーザーによる目視レビュー・実KLayout DRC/LVSでの
最終確認待ち。draft出力は`layout/step8/v9_top_routed.gds`
（`src/tr_1um_i2c_slave_async.gds`への確定コピーは未実施、レビュー後）。

### 79.2 ユーザー目視レビューで発見された2件の不具合と修正
（KLayoutスクリーンショット2枚、「１．M2ショート ２．PTECTエリアの
縦断（このVDD/VSS配線はしないM2：TOPとM1：サイドだけ）もう一度配線
お願いします。」）

**問題1：PTECTエリア縦断**

`v9_power_routing_plan.json`の初版（v1）は各TAP列がTOP/BOTTOMの
近い方を自由選択する方式だった。TAP1・TAP2のX位置（±267〜273um）は
PTECTボックス（X:-800〜800, Y:-800〜-150）の内側にあるため、この
2列がBOTTOM側を選んだ場合、コアピンからリング半径まで直線的に
降りる「ローカル退出ホップ」がPTECTを縦断してしまう。TAP0・TAP3
（X≈±802〜807）はPTECT範囲のすぐ外なのでBOTTOM使用は無害。

修正：`safe_edges(tap_x)`——`tap_x < -800 or tap_x > 800`の場合のみ
BOTTOM許可、それ以外（TAP1/TAP2）はTOP固定——としたv3プランを作成。
VDD合計配線長は無制約時の最適解と同じ929.2um（変化なし）、VSS合計は
4049.6um（v1の2536.6umより悪化するがPTECT安全）。全レッグの形状を
PTECTボックスと直接オーバーラップ判定し、0件（クリーン）を確認。

**問題2：M2ショート（2件、直接原因を特定）**

修正後の再配線・再検証で、レイヤー別クロスネットショートチェックに
3件の新規ショートが出現：
```
(VDD_TAP0_HIZ7, VSS_TAP0_HIZ9, M1)
(VDD_TAP1_HIZ2, VSS_TAP1_main, M2)
(VDD_TAP2_main, VSS_TAP2_OUT13, M2)
```
原因を2段階で特定：

(a) **レーン共有バグ**：電源8レッグ（VDD4本+VSS4本）を1つの共有
レーンプールで区間スケジューリングしていたため、VDD_TAP0_HIZ7と
VSS_TAP0_HIZ9が偶然同一レーン（R=819）に割り当てられた。原因は
区間計算自体にもあった——`unroll(s)=(s-CUT)%PERI`のmin/max近似は、
弧がCUT点をまたいで巻き付く場合に誤った区間を表現してしまう
（実際の描画方向を無視した近似のため）。正しい円周上オーバーラップ
判定（実際のCW/CCW方向を反映した弧表現＋巻き付きを扱う判定関数）
に置き換えたうえで、VDD用・VSS用を完全に別々のレーンプール（別々の
半径帯、VDD=R819のみ・VSS=R825のみ）に分離——ネット種別が異なる
限り同一半径には絶対に乗らない構造的な保証とした。これで
VDD_TAP0_HIZ7/VSS_TAP0_HIZ9のM1ショートは解消。

(b) **`main`レッグの近傍幅拡張が隣接TAP列ピンと衝突**：VDD_TAP1_HIZ2/
VSS_TAP1_mainとVDD_TAP2_main/VSS_TAP2_OUT13の2件は、レーン分離後も
残った。直接形状検証で判明：`PWR_WIDE_FIRST={"main": 10.0}`（v7の
VDD-main近傍10um幅拡張をそのまま踏襲）が、`main`ネットのTAP直近
ホップ（コアピンからリングまでの最初の垂直区間）を10um幅に拡張して
いたが、v9のTAP列はVDD用ピンとGND用ピンが同一列内でわずか5.4um
しか離れていない（v7のロウレール・バス方式とは配置が異なる）ため、
10um幅の最初のホップが隣のTAP列ピン（反対極性、通常幅3.4um）の
ホップと物理的に重なってしまっていた（例：VDD_TAP2_mainの拡張ホップ
X=262.3〜272.3が、VSS_TAP2_OUT13の通常ホップX=260.2〜263.6と重複）。

修正：拡張幅をTAP直近側（`wide_first_w`）ではなくGIO直近側
（`wide_last_w`）に付け替え——`main`ネットの太いバス幅という意図は
維持しつつ、TAP列同士が密集する場所では標準幅（3.4um）のまま退出
させることで隣接ピンとの衝突を回避。

**再検証結果**（修正後の`layout/step8/v9_top_routed.gds`）：
- DRC：M1/M2幅・スペース違反0件、ビア関連違反0件（未配線ベース
  ライン比較でも新規違反なし）。
- PTECTオーバーラップ：0件。
- レイヤー別クロスネットショート：DISチェーンの隣接リンク（同一
  ネット内、想定通り）14件のみ、本物のショートは0件。

ユーザー報告の2件（M2ショート／PTECT縦断）とも解消を確認。
`layout/step8/v9_top_routed.gds`への`src/tr_1um_i2c_slave_async.gds`
確定コピーは、ユーザーの最終レビュー後に実施予定。

### 79.3 電源配線アーキテクチャの根本的な誤りが判明、バスバー方式へ全面刷新

ユーザーから「(-200,-920)のVSSのGIOのPIN(M2)とコアのTAP:VSSの接続」
「(200,920)のVDD[後にVSSと訂正]のGIOのPIN(M1)とコアのTAP:VSSの接続」
という具体的な座標指定があり、実GDS（`src/tr_1um_i2c_slave_async.gds`
のOSS_FRAME_GIOセル）を直接調査した結果、**79.1で使用していた電源
接続先（VDD: main/HIZ2/HIZ7/HIZ15、VSS: main/HIZ9/HIZ10/OUT13）は
すべて誤りだった**ことが判明した。HIZ*/OUT*はOSS_FRAME_GIOのデジタル
I/Oピン名であり、電源とは無関係（ユーザー確認：「HIZやOUTは入力
です」）。

OSS_FRAME_GIOセル内のテキストラベル（layer 49/48、TR-1um PDK内の
ラベル層）を再帰的に走査し、実際のGIO側電源ピンを特定：
- VDD：34箇所
- VSS：47箇所
- GND：28箇所（X/Y=±200・±600、辺の内側921.7の位置に規則的配置）
ユーザー確認により、VSSとGNDは同一ネット（両方接続可）。

ユーザー指示「PTECTを含めたBBOXの上にVDDのM1を引いてTAPのM2と接続、
PTECTを含めたBBOXの下にVSSのM1を引いてTAPのM2と接続、それぞれを
GIOのPIN（VDD/VSS）と接続、M2/M1の配線幅は10um、VIAを複数配置」
を受け、`script/route_gio_core_v9.py`の電源配線部分（79.1/79.2の
リング配線＋レーンプール方式）を全面撤去し、以下のバスバー方式に
書き換えた：

- **VDDバス**：Y=800.0（コア天面796.1のすぐ上、PTECTのY範囲から
  大きく離れた安全域）に10um幅のM1バスを東西に敷設。4本のTAP列
  （X=-801.9/-267.3/267.3/807.3、いずれもコアのTOPマージンピン
  Y=794.6）から垂直M2で立ち上げてバスに接続、さらにバスから実在の
  GIO VDDピン（M1ピラー、実測(230,925)ラベル・実形状
  (210,920)-(250,990)）へ延長。
- **VSSバス**：Y=-805.0（PTECT底辺-800のすぐ下）に同じく10um幅のM1
  バスを敷設。TAP0（X=-807.3）・TAP3（X=801.9）はPTECT安全域外
  なので直接垂直降下。TAP1（X=-267.3）・TAP2（X=267.3、当初は
  TAP3も該当と判明——後述）はPTECTのX範囲内にあるため、TAPの
  ボトムマージン行（Y=-138.5、PTECTのY上端-150よりまだ安全域）を
  西または東へ迂回してPTECTのX範囲外に出てから南下、バスに合流。
  実在のGIO VSSピン（M2、実測(-200,-925)ラベル・実形状エッジ
  Y=-920）へバスから延長。

実装・検証は3段階の不具合発見と修正を要した：

1. **PTECT縦断（TAP3見落とし）**：10um幅配線の半幅5umを考慮せず
   中心線のみでPTECT安全判定していたため、TAP3（X=801.9、PTECT
   境界800からの余裕はわずか1.9um）の直下降配線が幅の分だけPTECT
   に食い込んだ（PTECTオーバーラップ1件を直接検出）。判定式を
   `中心線 ± 幅/2`ベースに修正し、TAP3も西迂回対象に追加して解消。

2. **信号ネットのM1リングレーンとの交差ショート**：VDDバスから
   実GIOピン（Y=955）へ垂直に立ち上げる区間をM1で描いたところ、
   信号24本の共有プール配線が使うM1水平レーン（Y=847～907、11本）
   をちょうど縦断し、9本の信号ネット＋DISチェーン1リンクと直接
   ショート（レイヤー別クロスネットチェックで検出）。信号ネット
   自身の「垂直=M2、水平=M1」という確立済みの規約に倣い、立ち
   上げ区間をM2に変更（M1レーンを無ビアで安全に横切れる）、実
   GIOピラーに接する最後のごく短い区間だけM1に戻すことで解消。

3. **TAPカラム直近でのVDD/VSS間ショート（DRCでは検出不可）**：
   実GDSを直接調査した結果、各TAP列にはVDD/GND双方の"元からある"
   電源ストラップが5.4um間隔で既に存在していた（TOP側：Y=736.3～
   796.1、BOTTOM側：Y=-140.0～-80.2）。10um幅（半幅5.0um）の新規
   配線をTAPピン中心に重ねると、隣接する反対極性の既存ストラップ
   （半幅1.7um）と最大1.3um重なってしまう——これは論理的には
   電気ショートだが、DRCの幅・間隔チェックはネット識別をせずレイヤー
   全体をフラットに扱うため、単純に重なった1つの大きな図形として
   処理され、幅・間隔違反としては検出されない（今回の実行でも
   DRC結果は0/0のままだった）。NET_SHAPESに記録されない"既存"
   メタルとの衝突のため、これまでのクロスネットチェックでも
   捕捉できなかった。ネット単位で追跡していない実GDSの生ジオメトリ
   を直接調べて初めて発覚。
   修正：TAPピン直近の危険域（TOP側Y=794.6～797.5、BOTTOM側
   Y=-142.0～-138.5）だけ幅4um（半幅2.0um、隣接ストラップ端から
   1.7umクリア）に細め、危険域を抜けた地点で10um幅に戻す（同一
   レイヤー内の幅遷移のみでビア不要）。VSSの迂回配線もこの安全な
   Y（-142.0）で水平移動するため、経路上どのTAP列の直近を通過
   しても危険域Yと重ならず安全。

**最終検証結果**（`layout/step8/v9_top_routed.gds`、電源部分を
バスバー方式に刷新後）：
- DRC：M1/M2幅・スペース違反0件、ビア関連違反0件。
- PTECTオーバーラップ：0件。
- レイヤー別クロスネットショート（NET_SHAPES追跡ネット間）：
  DISチェーンの隣接リンク14件のみ（想定通り、同一ネット）、
  本物のショートは0件。
- **TAP列8箇所（4列×TOP/BOTTOM）全てで、新規配線と隣接する反対
  極性の既存ストラップとの間隔を直接再計算——全箇所で設計通り
  1.7umのクリアランスを確認、重なりなし。**

24信号ネット・DISチェーンの配線は79.1/79.2から変更なし（引き続き
DRC/PTECT/ショートすべてクリーン）。電源部分のみ完全刷新。
`src/tr_1um_i2c_slave_async.gds`への確定コピーは、ユーザーの最終
レビュー後に実施予定。

### 79.4 ユーザーのKLayout目視レビューで発覚した3件の配線不備を修正

ユーザーが実際にKLayoutで`v9_top_routed.gds`を開いて確認し、
スクリーンショット2枚とともに3件を指摘：
1. GIOのVSS(M2)とバスバー(M1)の間にVIAがない
2. GIOのVDD(M1)がバスバー由来のM2でVSSとショートしている
3. GIOのVDD(M2)とバスバー(M1)の間にVIAがない

NET_SHAPESを直接確認したところ、指摘1・3は完全に正しかった：
`VSS_GIO_pin`・`VDD_GIO_pin`とも、M2区間がバスバー（M1）のY座標
ちょうどで終わっているにもかかわらず、その接続点にVIAを1つも
置いていなかった。M1の箱とM2の箱が同じ座標で単に接している
だけでは電気的には繋がらない（VIAが無いと絶縁されたまま）——
基本的な見落とし。両方にVIAを追加して解消。

指摘2（VDDがVSSとショート）はより深刻で、電源バスバー方式の
設計そのものに起因していた。GIO側の実VDDピン（M1ピラー、実測
(210-250,920-990)）へVIA接続する際、そのすぐ隣に実VSSピラー
（実測(150-190,920-990)）があることは把握していたが、直接
GDSを再調査したところ、**両ピラーとX方向に重なる第3のM1形状**
（(50-350,920-934)、両ピラーの底辺とY方向にもオーバーラップする
幅広の帯）が元のGIOフレーム設計に存在することが判明した。この
帯はVDDピラー・VSSピラー双方と幾何学的に重なっており、この帯の
電気的な正体（本当に両ピラーを繋いでいるのか、単なるフレームの
飾り/フィルなのか）はGDSジオメトリだけでは判別できない。従来の
接続はY≈920（この曖昧な帯のY範囲920-934の真っ只中）にVIAを
配置していたため、この帯を経由してVSS側と繋がってしまっていた
可能性が高い——ユーザーが指摘した通り。

安全側に倒し、この曖昧な帯とM1ピラーを完全に迂回する接続に変更：
GIO側の実VDD M2バス（(200-400,920-950)、VDD専用と確認済み、
曖昧さなし）へY=935（帯の上端934から1um、ピラーへの接続は
一切行わない）でM2のまま直結。M1には触れないためVIAも不要
（M2-M2の同一層接続）。VSS側はもともとM1ピラーを経由していない
ため、この曖昧さの問題自体が存在しない（VIA追加のみで解消）。

**再検証結果**：DRC 0/0、PTECTオーバーラップ0件、レイヤー別
クロスネットショート0件（DISチェーンの想定内14件のみ）、さらに
VDDの新配線がVSSピラー・曖昧な帯のいずれともM1レベルで一切
接触していないことを直接確認（VDD側のM1使用をこの区間から
完全に排除したため、判定は自明にFalseで確定——推測に頼らない
設計とした）。

### 79.5 テキストラベル頼りのピン特定が根本的に誤りと判明、LEFを正として全面修正

ユーザーが再度KLayoutで目視確認し、3件指摘：
1. GIOのVSS(M2)とバスバー(M1)の間にVIAがない
2. GIOのVDD(M1)がバスバー由来のM2でVSSとショートしている
3. GIOのVDD(M2)とバスバー(M1)の間にVIAがない

1・3はNET_SHAPES確認で即座に確定：`VSS_GIO_pin`・`VDD_GIO_pin`とも
バスバー（M1）とのY座標境界でM2区間が終わっているのにVIAを一つも
置いていなかった。両方に追加して解消。

2は深刻で、79.4の「修正」自体の設計判断が誤りだったことが判明した。
ユーザーから「GIOのM1PINとつなぐ前にTAPからのM2をM1に落として
からM1(10um)でGIOで接続」と再指示があり、対応を検討する過程で
79.4のM2接続先——実VDD"M2バス"と判断していた(200,920)-(400,950)
——を改めて疑い、ユーザーのヒント「OSS_FRAME_GIOのVDD/VSS実ピン
位置はFRAME/TR-1um_frame_25x25.gdsからLEFとして作成してませんか」
を受けて`LEF/OSS_FRAME_GIO.lef`を直接確認した。

結果、**79.1〜79.4を通じて使ってきた「テキストラベル近傍のM1/M2
形状を電源ピンとみなす」手法自体が誤りだった**と確定：
- `PIN VDD`（`USE POWER`）：LAYER M1、RECT(1300,2170)-(1600,2184)
  （LEFのORIGIN 1250,1250を引いてチップ座標に変換：
  (50,920)-(350,934)）——フレーム全体でこの1箇所のみ、TOP辺限定。
- `PIN VSS`（`USE GROUND`）：LAYER M2、4辺を一周する多数のRECT
  （例：BOTTOM辺RECT(800,316)-(1300,330)→チップ座標
  (-450,-934)-(50,-920)）——連続リング。

79.4で「VDD専用のM2バスで曖昧さなし」と判断していた
(200,920)-(400,950)は、実際には**VSSのLEFピン形状そのもの**
（VSS RECT(1200,2170)-(1700,2184)→チップ(-50,920)-(450,934)と
ほぼ同一）だった。つまり79.4の「修正」はVDDをVSSの実ピンへ
直結する、まさにユーザーが指摘した通りのショートを作っていた。
79.4以前に疑っていた「VDD/VSSピラーが謎の帯で連結している」件は
別の話で、その帯自体はLEFの実VDDピンと一体の正当なVDD形状（M1同士
が繋がっているだけ）であり、ショートの原因ではなかったと判明——
テキストラベルだけを頼りに"ピラー"を特定していたための誤診断
だった。

**根本修正**：LEFの実ピン座標を直接使用。
- VSS：`GIO_VSS_PIN`を(-200,-920)から(-200,-925)に変更（境界上
  ちょうどではなく実ピン領域の内側5um、確実な重なりを持たせる）。
  レイヤーはM2のまま、VIA不足も解消。
- VDD：実ピンはM1・1箇所のみで、バスバー(Y=800)からは信号24本の
  M1リングレーン（Y=847-907）を必ず横切る。ユーザー指示通りM1で
  往復する設計としつつ、レーン帯の横断だけは検証済みの「異なる
  層は無ビア交差なら安全」という原則に基づき、幅を絞った
  （BUS_Wの10umではなくTAP_STUB_Wの4um）M2ジョグに限定——
  バスバー→VIA→細いM2でレーン帯を抜ける（Y=800→910）→VIA→
  太いM1(10um)で実ピン(50,920)-(350,934)内、(200,927)へ着地。
  ユーザーの要求（M1でGIO接続）と、既に確認済みの安全な横断技法
  を両立させた設計。

**再検証結果**：DRC 0/0、PTECTオーバーラップ0件、クロスネット
ショート0件（DISチェーン14件のみ）。さらにLEFの実ピン形状を
レイヤーごとに正しく区別した検証を追加：VDDのM1が実VDD M1ピン
に正しく重なる／VDDのM2が実VSS M2ピンに一切触れない／VSSのM2が
実VSS M2ピンに正しく重なる／VDDのVIAが実VSSピンのY範囲
（920-934）内に一つも存在しない——すべて確認。テキストラベルの
近さだけに頼った推測ではなく、LEFの正式なピン定義を根拠とした
初めての検証。

### 79.6 TAP直近スタブがコア既存メタルと僅かに幅違いで二重輪郭に

ユーザーがKLayoutで再確認し「左下のTAPからバスバーへのM2が2重に
なっています。ここだけです」と指摘。原因：TAP直近の危険域を細く
した新規スタブ（TAP_STUB_W=4.0）を、コアの既存TOPマージンストラップ
（Y=736.3-796.1、幅約3.4um、design_notes.md 79.2bで測定済み）が
既にカバーしているY=794.6-796.1の区間にも重ねて描いていたため、
既存メタル（3.4um幅）と新規メタル（4.0um幅）の輪郭がほぼ一致しつつ
僅かにズレて二重に見えていた。BOTTOM側（VSS、既存ストラップ
Y=-140.0～-80.2）も同様の重複が-140.0～-138.5の範囲にあった。

修正：新規スタブの開始Yを、ピン自体のY（794.6/-138.5）ではなく
既存ストラップの端（TOP側796.1、BOTTOM側-140.0）に変更——電気的
接続は端で触れるだけで成立するため、重複領域を完全に排除。
再検証（DRC・PTECT・クロスネットショート）は全てクリーン、
TAP0での重複ゾーンを直接確認しても新規スタブの範囲
（796.1-797.5）のみで既存ストラップとの二重描画は解消。

### 79.7 PTECT左右のVSS迂回配線、片側だけ2本の縦配線が部分重なり

ユーザー指摘：「PTECTの左を上下に走るM2(10um)２本が5.3um重なって
います。右側は1本だけでOKです。」

原因：VSS側のPTECT迂回ロジックで、迂回先Xを常に固定の
`BUS_X_LO`/`BUS_X_HI`（die端、-812.0/812.0）にしていた。左側は
TAP0（X=-807.3）がPTECT安全域外で直接降下、TAP1（X=-272.7）だけが
迂回してBUS_X_LO=-812.0へ向かうため、**中心が4.7umだけ離れた別々の
10um幅バーが2本**でき、5.3um（=10-4.7）だけ部分的に重なっていた。
右側はTAP2・TAP3の両方が迂回対象（TAP3はPTECT境界からわずか1.9um
しか離れておらず10um幅では直接降下不可、design_notes.md 79.2参照）
のため両方ともBUS_X_HI=812.0へ迂回し、たまたま完全に一致して1本に
見えていた——設計としては同じ問題を抱えていたが、左右でTAP配置が
非対称だったため症状が片側にしか現れなかった。

修正：迂回先Xを固定のdie端定数ではなく、「その側に直接降下する
TAPが既にあれば、そのTAPと同じXを使う」方式に変更（`direct_x_by_side`）。
これによりTAP1の迂回先はBUS_X_LO(-812.0)からTAP0の実座標(-807.3)へ
変更、TAP0の直接降下とTAP1の迂回降下が完全に一致する1本の配線に。
右側はTAP2・TAP3とも迂回対象で「直接降下するTAP」が存在しないため
従来通りBUS_X_HI=812.0を使用、変更なし。

再検証：DRC 0/0、PTECTオーバーラップ0件、クロスネットショート0件。
NET_SHAPESで左側の縦配線を直接確認し、TAP0由来・TAP1由来の2本が
座標(-812.3,-805.0)-(-802.3,-142.0)で完全一致（重複記録されている
だけで実体は1本）していることを確認、右側も同様に完全一致を再確認。

### 79.8 実KLayout DRCエラー（VIAスペース1.5um、M2スペース2um、M2幅3um以上）

ユーザーが実際にKLayoutでDRCを実行し、`Layout/step8/DRC_error.lyrdb`
としてエラーを報告：「VIAのスペースは1.5umです。M2のスペースは2um
M2の幅は3um以上です。」（V1 Smin<1.5、M2 Wmin<3.0、M2 Smin<2.0、
計50件：V1スペース22件、M2幅14件、M2スペース14件）。

**重大な副発見：`drc_check_nrow_fm.py`自体のバグ**
このスクリプトの`TOP_CELL`はハードコードで`"i2c_slave_async_nrow_fm"`
（コア単体のセル名）に固定されており、`sys.argv[1]`にどのGDSを渡し
ても無視されていた。今セッション中`layout/step8/v9_top_routed.gds`
（実際のトップセルは`tr_1um_i2c_slave_async`で、コアはその中に
サブインスタンスとしてネストされている）に対して実行していたが、
`top.begin_shapes_rec()`は常にネストされた無関係の内側セルを見て
いたため、チップレベルの電源/信号配線を一切検出できていなかった。
このセッションで報告した「0 violations」は全てチップレベルでは
無意味だった。修正：`TOP_CELL = sys.argv[2] if len(sys.argv) > 2
else "i2c_slave_async_nrow_fm"`として第2引数でトップセル名を指定
可能にし、`tr_1um_i2c_slave_async`を渡して再検証、実際の違反
（M2幅15件・スペース19件、V1スペース22件）を正しく再現することを
確認した。

**修正1: TAP_STUB_W (4.0 → 3.2 → 最終3.4)**
当初4.0umはM2の実際の最小スペース値ではなくM1の値（~1.4um）を誤って
流用した想定に基づいていた。実際はM2 Smin=2.0um・Wmin=3.0um。
まず[3.0,3.4]の範囲内で3.2を試したが、まだ16件のM2スペース違反と
1件のM2幅違反が残った。原因を直接ジオメトリ確認したところ、全て
新規スタブ（3.2幅）と既存のコア内蔵ネイティブストラップ（実測で
正確に3.4幅、TAPピンXに正確に中心）との継ぎ目（T字接合）にあった
——3.2幅の新規形状を3.4幅の既存形状に突き合わせると、0.1umの
「ノッチ」が接合部にでき、これが幅/スペースチェッカーに引っかかる。
修正：TAP_STUB_Wをネイティブストラップの実測幅3.4umに一致させ、
継ぎ目を完全にフラットに——ノッチそのものをなくした。隣接TAP列
（VDD/GND間5.4umピッチ）とのクリアランスも実測で確認：コア自身の
ネイティブストラップ同士は既にちょうど2.0um（Smin丁度）で隣接して
おり、これはユーザーの違反リストに含まれていない＝このDRCデッキは
ちょうど最小値でも合格と判断できる。3.4umでも同じちょうど2.0um
クリアランスを再現するだけなので安全。

**修正2: 幅ステップ形状の完全排除**
TAP列からバスバー（またはPTECT迂回経路）までの縦配線を、途中で
幅を変える（狭い→広い）2セグメント方式から、常に単一のTAP_STUB_W
幅で最後まで通す1セグメント方式に統一。T字合流部の局所的な幅の
アノマリーを構造的に排除。

**修正3: multi_via()のVIAスペース計算式の誤り**
旧式は`(w - pad - 1.0) / (n - 1)`でVIAランドパッド（3.4x3.4）基準
にピッチを計算していたが、実際にDRCがチェックする「V1」レイヤーは
via_1 PCellが描画する固定1.4x1.4のVIAカット自体（違反箇所の
edge-pair形状から実測で確認：1.4um高さのエッジ、1.4umギャップ）。
n=3・w=10・pad=3.4でピッチ2.8umとなり、パッド同士は届いていたが
カット同士のギャップは1.4umしかなく、実際の最小値1.5umを0.1um
下回っていた。修正：ピッチをカットサイズ(1.4)+最小スペース(1.5)+
余裕(0.2)=3.1umから算出。デフォルトのVIA本数もn=2に変更（3.4um幅
パッドが10umバス内に収まる範囲で2本、3本だと現実的な余裕が取れない
ため）。狭いTAP_STUB_W(3.4)幅の配線上に乗るVIAは全てn=1に変更
（パッドが配線からはみ出すのを避けるため）。

**修正4: 迂回配線とTAP直接降下配線の重複VIA**
`direct_x_by_side`方式（79.7）でTAP0の直接降下とTAP1の迂回降下が
同じX座標に一致するようになったが、それぞれが独立に`multi_via()`
を呼んでいたため、ほぼ同じ場所に2組の別々のVIA配列が置かれていた
——実際のDRCがまさにこれを検出（TAP0由来のVIAとTAP1迂回由来のVIA
の間に0.15umギャップ、Smin1.5um未満）。右側もTAP2・TAP3が両方とも
BUS_X_HI(812.0)に迂回するため同じ問題があった。修正：`via_done`
セットで既にVIAを置いた(x,y)座標を記録し、`via_once()`ヘルパーで
重複呼び出しをスキップ——配線自体は同一ネット・同一レイヤーなので
そのままマージされ電気的な接続は変わらない。

**修正5: 迂回配線の縦降下部の幅ステップ**
修正4の重複VIA修正後も、迂回配線の縦降下部
（`detour_x, VSS_SAFE_Y → detour_x, VSS_BUS_Y`）が従来BUS_W(10um)幅
で描かれており、`direct_x_by_side`でdetour_xがTAP0自身のXと一致する
場合、TAP0自身の3.4um幅配線と同じX・重なるY範囲で幅10umの配線が
上書きされ、同じ幅ステップ問題を再現していた（実DRC：Y=
NATIVE_BOTTOM_Y付近でM2スペース違反）。修正：この縦降下部も
TAP_STUB_W(3.4)幅に統一。

**修正6: 右側迂回先のVDDネイティブストラップとのクリアランス不足**
右側の迂回先デフォルト`BUS_X_HI=812.0`にVIA（3.4幅パッド）を置くと、
パッド左端が810.3umとなり、TAP3のVDDネイティブストラップ右端
（809.0um）からわずか1.3umしか離れておらず、実際のSmin=2.0um未満。
修正：右側迂回先専用の`VSS_DETOUR_FALLBACK_HI=813.0`を新設し、
VIAパッド左端が811.3umとなり2.3umのクリアランスを確保。

**再検証結果（`layout/step8/v9_top_routed.gds`、正しいトップセル
`tr_1um_i2c_slave_async`で検証）：**
- M1: 幅違反0、スペース違反0
- M2: 幅違反1、スペース違反5（いずれも配線前の素のコアGDS
  `src/tr_1um_i2c_slave_async.gds`に既に存在する、今回の電源配線
  とは無関係の座標（X≈-963〜-1028、Y≈-1005〜-1107、電源配線の
  作業範囲外）——今回のスコープ外のコア既存問題と判断）
- V1スペース違反：0
- V1のM1/M2エンクロージャ違反：0
- V1-GCスペース違反：0
- PTECTオーバーラップ（VDD・VSS）：0
- クロスネットショート（VDD⇔VSS、VDD/VSS⇔24信号ネット＋DISチェーン、
  レイヤー別チェック）：0
- LEFピン整合性：VDD-M1は実VDDピン矩形(50,920)-(350,934)内に着地、
  VDD-M2は実VSSピンリングに一切接触せず、VSS-M2は実VSSピンリングに
  到達、VSS配線は実VDDピン矩形に一切接触せず——すべて確認済み。

今回の電源配線が新たに導入したDRC違反は0件。ユーザーへの再確認待ち。

### 79.9 VDD側GIO接続M2が10umでない、VIAが1本に減っている

ユーザーがKLayoutで確認：「VDDバスバーからGIOへのM2が10umになって
ません。VIAも一つに減っています。VSS側の接続はOKです。」

原因：79.5でこの信号レーン帯（Y=847-907）を横切るVDD側M2区間を
「フットプリント最小化」のため意図的にTAP_STUB_W幅に狭めていた。
しかし電気的には別レイヤー（M1の信号レーン走行区間とM2）なので
VIAなしで交差する限りM2の幅自体は安全性に無関係——狭くする必然性は
なかった。79.8のVIAスペース修正で、この狭い区間に乗るVIAをn=1に
減らしたことで症状がより顕著になった。

確認：交差区間X=[190,210]×Y=[800,910]の範囲に24信号ネット＋DIS
チェーンのM2セグメントが一切存在しないことをNET_SHAPESで直接確認
——10um幅に戻しても新たなショートは発生しない。

修正：VDD側GIO接続のM2交差区間をTAP_STUB_WからBUS_W(10um)に復元、
両端のVIAもn=1から標準の複数VIA配置に復元、VSS側と同じ構造に統一。

再検証：DRC（M1 0/0、M2 1/5=範囲外の既存問題のみ、V1スペース0）、
PTECTオーバーラップ0件、クロスネットショート（レイヤー別）0件、
LEFピン整合性すべてOK。

### 79.10 PTECT右側、TAPからのM2バスバーへの延伸が左側のように見えない

ユーザーがKLayoutで確認：「コア下のPTECT株のバスバーまでの配線が
TAPからのM2を延伸してない（右側）です。左側はOKです。」

原因：PTECT直接クリアランス判定`clears(tx)`が、79.8で全区間を
TAP_STUB_W(3.4)幅に統一した後もBUS_W(10um)半幅のままだった。この
結果TAP3（X=801.9）は本来TAP_STUB_W幅なら800.2でPTECT境界(800.0)を
0.2umクリアできるにもかかわらず「迂回が必要」と誤判定され、TAP2・
TAP3とも無関係な固定フォールバック座標(813.0)へ迂回——左側のように
TAP0からTAP1へ素直に繋がる1本のバーではなく、TAPそのものからは
短いスタブだけであとは離れた場所に別のバーが立つ構造になっていた。

修正：`clears()`の判定幅をBUS_W/2からTAP_STUB_W/2に変更（実際に
描画される幅に合わせる）。これによりTAP3が直接降下対象となり、
TAP2の迂回先は`direct_x_by_side`によりTAP3の実座標(801.9)を再利用
——TAP0/TAP1と同じ「直接降下＋迂回が同一Xで完全一致」パターンに
統一された。

再検証：DRC（M1 0/0、M2 1/5=範囲外の既存問題のみ、V1スペース0）、
PTECTオーバーラップ0件、クロスネットショート0件。NET_SHAPESで右側
のM2がY=-140(NATIVE_BOTTOM_Y)からY=-806.7(VSS_BUS_Y)まで単一の
連続ポリゴンとして繋がっていることを直接確認、左側と同一パターン。

### 79.11 TAPからのM2とバスバーのM1のVIAを縦に2つに

ユーザー：「TAPからのM2とバスバーのM1のVIAは縦に２つにしてください。」

TAP_STUB_W(3.4)幅の配線上では2VIAを横（X方向）に並べる余地がない
（パッド3.4+スペース1.5+パッド3.4で約8.3um必要）。VIAをX方向でなく
Y方向（配線の長手方向）にスタックする`multi_via_v()`を新設、ピッチ
計算は`multi_via()`と同じ（V1カット1.4+最小スペース1.5+余裕0.2=
3.1um）。

TAP側のM2スタブをバスバー中心線から3.5um（VIAスタックが必要とする
半スパン：ピッチ/2+パッド/2+余裕）だけ延長し、上下2VIAの両方に確実
にM2が乗るようにした。延長分はM1バスバー自身の縦幅（10um）の範囲内
に収まるため新たな懸念なし。対象：VDD各TAP、VSS直接降下TAP、VSS
迂回配線の遠端（バスバーとの接合部）——TAP近傍のM2→M1遷移VIAは対象
外（そちらは79.5-79.8のまま1本）。

再検証：DRC（M1 0/0、M2 1/5=範囲外の既存問題のみ、V1スペース0）、
PTECTオーバーラップ0件、クロスネットショート0件。VIAレイヤーを
直接確認し、VDD各TAP・VSS直接降下・VSS迂回遠端いずれも上下2つの
VIAカットが実在することを確認。

### 79.12 PTECT迂回のM1ジョグがコア内蔵配線とショート／VIA本数確認

ユーザー：「コアの下のバスバーがVIAが一つのままです。さらに、配線
のM1とショートしています。コアの上下のバスバーをBBOXより外にして
ショートしないようにしてください。」

**VIA本数の件**：現在のGDSをVSS_BUS_Y全域で直接確認したところ、
GIO_VSS_pin接合部（横2本）、TAP0直接降下（縦2本）、TAP2/TAP3共有
迂回着地点（縦2本）といずれも2本ずつ確認でき、「1本のまま」の箇所
は見つからなかった——ズーム倍率でVIAが近接して1つのアイコンに
見えていた可能性が高い。

**M1ショートの件（実際のバグ）**：VSS側PTECT迂回の水平M1ジョグ
（`VSS_SAFE_Y=-142`、幅BUS_W=10、Y=-137..-147）が、コア内蔵の
標準セル行の実配線と重なっていた。PRISTINE（配線前）のコアGDSを
直接確認すると、Y=-136.3..-139.7に渡ってX=-787.4~522.8まで連続する
共有レール状のM1形状があり、さらに個別ピンラベル（"rx_valid"
X=-737.1、"rw" X=45.9、いずれもY=-138.5）も同じ帯域に存在すること
を確認。TAP1の迂回ジョグ（X=-272.7→-807.3）がちょうど"rx_valid"の
X座標を横切っており、実際にM1形状同士が重なっていた（NET_SHAPESに
基づくクロスネットチェックは、この"rx_valid"のような信号ピンが
トップレベルのトラッキング対象24ネットに含まれていなかったため
検出できていなかった——チェック手法自体の盲点）。

原因の根本：Y=-140付近（コアの下端）は内部配線が密集しており、
迂回ジョグをこの高さで通すこと自体が危険だった。

修正：PRISTINE GDSを直接調査し、Y=-140~-150（コア内蔵配線の下端
-139.7とPTECT上端-150の間）にM1/M2が全域にわたって一切存在しない
「安全な窓」を発見。`VSS_SAFE_Y`を-142から-145に変更（この窓の中央
付近）、ジョグ自体の幅もBUS_W(10)からTAP_STUB_W(3.4)に狭めて余裕を
確保（-145±1.7 = -143.3..-146.7、コア側に3.6um・PTECT側に3.3umの
マージン）。この高さはPTECTのY範囲（-800~-150）より上（外側）なので
ジョグ自体はPTECTのX制約も受けない。

再検証：DRC（M1 0/0、M2 1/5=範囲外の既存問題のみ、V1スペース0）、
PTECTオーバーラップ0件、トラッキング対象ネットのクロスネットショート
0件。さらに今回、PRISTINE（配線前）コアのM1全体に対してVDD/VSS配線
のM1がオーバーラップしていないかを追加で直接チェックし、"rx_valid"
付近の重なりが解消されたことをM1形状の実座標で確認（新ジョグは
Y=-146.7..-143.3、rx_valid側の既存配線はY=-136.3..-139.7で、3.6um
のクリアランスが空いている）。VDD側の唯一のオーバーラップはGIO_VDD_
pinの実ピン矩形(50,920)-(350,934)内への意図的な着地（正しい接続）の
みで、ショートではない。

## 80. チップレベルLVS用SPICEネットリストの生成

ユーザー：「LVS用の spice ファイルをコアの LVS クリーンの spice と
GIOのschematic、JSON情報を使って作成ください。」——v9コア単体LVS
（77.47でクリーン確認済み）に続き、GIOフレームとの統合後のチップ
レベルLVSに必要な参照ネットリストを作成。

**3つの入力**（ユーザー指定通り）：
1. コアのLVSクリーンspice：`schematic/i2c_slave_async_nrow_fm_v9.
   spice`（トップ+18ライブラリセル本体+FILL2/FILL3デカップ、自己完結）
2. GIOのschematic由来spice：`simulations/OSS_FRAME_GIO.spice`（xschem
   由来のトランジスタレベルネットリスト、OSS_FRAME_GIO+依存3サブ
   サーキット(OSS_ESD_5V_DIO/_VDD/_VSS)を含み自己完結、73行）
3. JSON情報：`schematic/gio_connections.json`（78.1/78.4で導出・
   クロス検証済みのGIO⇔コア端子対応表）

**方針**：JSONは既に確定した接続関係の記録であり、本タスクでは
再導出しない。両spiceファイルの`.subckt`宣言行から実ポート順を
直接読み取り（手入力しない）、JSONの`connections_per_terminal_detail`
から各ポートのネット名を機械的に導出、x1(GIO)/x2(コア)の2インスタンス
からなるトップ`.subckt tr_1um_i2c_slave_async`（ポート無し、旧
`src/tr_1um_i2c_slave_async.cir`と同じ構造）を新規生成する
`script/gen_lvs_spice_top_v9.py`を作成。

**未接続ピンの扱い**：コア側`tx_data[1]`・`rx_valid`・`addr_match`・
`rw`・`busy`（5件）、GIO側`P9`・`P10`・`P11`(P側)・`OUT2`・`OUT7`・
`OUT9`・`OUT10`・`OUT15`（8件）はJSON上いずれも未接続と明記されて
いる。実チップで互いに無関係な複数の未接続ピンを1つの共有ネットに
まとめるとLVS比較の意味が変わってしまうため、`NC_<ピン名>`という
形でそれぞれ独立したフローティングネット名を割り当てた（計13件）。

**検証**：
- x1(OSS_FRAME_GIO)インスタンス行：44ネット、OSS_FRAME_GIOの実宣言
  ポート数44と完全一致（独立再パースで確認）。
- x2(i2c_slave_async_nrow_fm)インスタンス行：26ネット、実宣言ポート数
  26と完全一致（同上）。
- GIO側本体とコア側本体の間でサブサーキット名の衝突なし（GIO:
  OSS_FRAME_GIO/OSS_ESD_5V_DIO/OSS_ESD_5V_VDD/OSS_ESD_5V_VSSの4種、
  コア: 18ライブラリセル+トップの19種、重複ゼロを確認）。
- 電源：core.VDD⇔net"VDD"⇔frame.VDDパッド、core.GND⇔net"VSS"⇔
  frame.VSSパッド（JSON `power`ブロック通り）。

**出力**（コア版の慣例と同じく2箇所へ）：
- `schematic/tr_1um_i2c_slave_async_v9_lvs.spice`（プロジェクト内
  永続成果物、747行）
- `simulations/tr_1um_i2c_slave_async.spice`（KLayout LVSマクロの
  `Sch_file = "simulation/" + source.cell_name + ".spice"`規約に
  合わせたファイル名——チップトップセル名`tr_1um_i2c_slave_async`と
  完全一致）

**申し送り**：本サンドボックスにklayout CLIが無いため、実LVS実行での
最終確認（チップトップセルを`tr_1um_i2c_slave_async`に指定して
`v9_top_routed.gds`に対して実行）は引き続きユーザー側ローカル環境が
必要。またこのネットリストは論理接続（どのピンがどのネットに属するか）
のみを表現しており、79.x節の実配線（TAP列4本×2極+バスバー方式）の
具体的な配線トポロジーはLVS上は無関係（コアのVDD/GNDポートは元々
単一ネットとして扱われるため、物理的に何本のTAPで給電しているかは
LVSの比較対象外）。

## 81. 最終レイアウトを src/ に確定

ユーザー：「Layout を src/に移してください。」——79.x節の一連のDRC
修正がユーザー確認済み（「DRCクリーンです。ありがとうございます。」）
となったため、`layout/step8/v9_top_routed.gds`（チップレベル配線済み、
DRC実質クリーン、PTECT/クロスネットショート/LEFピン整合性いずれも
確認済み）を`src/tr_1um_i2c_slave_async.gds`へ上書きコピーし、確定版
とした。コピー前後でmd5sum一致を確認（951fcba0...）。

これにより`src/tr_1um_i2c_slave_async.gds`はチップ統合前のプレース
メントのみの状態から、コア+GIOフレーム全結線済みの最終レイアウトに
更新された。80節で作成したチップレベルLVS用SPICE
（`schematic/tr_1um_i2c_slave_async_v9_lvs.spice`）と合わせて、実DRC・
LVSの最終確認材料が揃った状態。

## 82. チップトップレベルLVSエラーの原因究明とTOP PIN追加による修正

ユーザーが`src/LVS_error.lvsdb`（80節のSPICEを使って実際にKLayout LVS
を走らせた結果）を提供。`klayout.db.LayoutVsSchematic`のxref APIで解析
したところ、18個のライブラリセル・OSS_ESD_5V_DIO/VDD/VSS・コア回路は
すべてMatch、OSS_FRAME_GIO回路のみNoMatch（HIZ2, HIZ7, HIZ9, HIZ10,
HIZ15, OUT13の6ピンがlayout=Noneで不一致）となり、その結果トップ回路
（tr_1um_i2c_slave_async）はSkipped扱いだった。

### 82.1 初期仮説とユーザーによる反証

当初「複数ピンが同一ネットを共有する構造がstrict `flag_missing_ports`
モード下でトポロジー的な曖昧さを生む」という仮説を立てたが、ユーザー
から「V7ではLVSがクリーンになりました。何が違いますか？」と指摘。
v7（GIOを含むチップレベルLVS、厳格ポートモード）で全く同じtie-pin構造
（HIZ2/HIZ7/HIZ9/HIZ10/HIZ15/OUT13の固定結線）が既にLVSクリーンで通っ
ていたことが判明し、上記仮説は誤りだったことが確定した。design_notes
75節（v7のチップレベルLVSクリーン化の記録）・76.3節（v7でもトップサブ
サーキットはVSSしかポートを持たない、という記述）を再確認したが、決定
的な差分は特定できなかった。

### 82.2 ユーザーによる根本原因診断

ユーザー自身が診断：「Schematic と Layout に Top PIN がついていないこと
が原因です。」——実際に`src/tr_1um_i2c_slave_async.extracted`（今回の
LVS実行によるレイアウト側抽出ネットリスト）を確認すると
`.SUBCKT tr_1um_i2c_slave_async VSS`となっており、ポートはVSSの1本のみ。
80節で生成したSPICE側（スキーマティック相当）も`.subckt
tr_1um_i2c_slave_async`とポートなしで、両側ともチップの実ボンドパッド
（P1-P7, P9-P15, VDD, VSS＝16本、P8はこのフレームに存在しない）をトッ
プレベルポートとして一切露出していなかった。これがKLayoutのstrictモー
ドピン/ポート整合に確たる手がかりを与えられず、OSS_FRAME_GIO階層のピン
不一致（HIZ2/HIZ7/HIZ9/HIZ10/HIZ15/OUT13）へ波及した、というのがユー
ザーの結論。

### 82.3 修正：スキーマティック（SPICE）側にTOP PIN追加

ユーザー指示：「まず src/simulation/tr_1um_i2c_slave_async.spice に
TOPピンをつけてください。P1~P7,VSS,P9-P15,VDDです。JSONにも追記くださ
い。」

`script/gen_lvs_spice_top_v9.py`を修正：

1. `TOP_PIN_ORDER = ["P1","P2","P3","P4","P5","P6","P7","VSS","P9","P10",
   "P11","P12","P13","P14","P15","VDD"]`を定義。
2. 各パッドが現在保持している内部ネット名（例：P1は"IN5"、P7は"DIS"、
   P9/P10/P11は未接続の`NC_P9`/`NC_P10`/`NC_P11`）を、パッド自身の正式
   名称にリネームするマップを構築し、`gio_net`・`core_net`双方に適用。
   これは既に同一である電気ノードの名前を揃えるだけの操作であり、接続
   関係自体は変更していない。
   - P7 -> "DIS"だった点は要注意：`gio_connections.json`の
     `dis_net_resolved`に記録済みの通り、"DIS"はP7パッドと8本のHIZピン
     （HIZ1/HIZ3/HIZ4/HIZ5/HIZ6/HIZ11/HIZ12/HIZ14）が意図的にデイジー
     チェーンで共有する実在ネットであり、単なるプレースホルダーではな
     い。したがって"DIS"の全出現箇所（9箇所）をP7へリネームするのは
     正しい——同一物理ノードの再命名であり、新たな結線ではない。
3. `.subckt tr_1um_i2c_slave_async`の宣言行に
   `" ".join(TOP_PIN_ORDER)`を追加し、16本の実ポートを明示的に宣言。

再実行し、`schematic/tr_1um_i2c_slave_async_v9_lvs.spice`と
`simulations/tr_1um_i2c_slave_async.spice`を再生成。独立検証スクリプト
で以下を確認：

- トップsubcktのポートリストが`TOP_PIN_ORDER`と完全一致（16本）。
- x1（OSS_FRAME_GIO、44ネット）・x2（i2c_slave_async_nrow_fm、26ネッ
  ト）のインスタンス行がそれぞれの宣言ポート数と一致。
- リネームが両方のインスタンス行に正しく反映（例：P1のネットが
  "IN5"から"P1"に、DISの9箇所すべてが"P7"に置換）。
- 2つの出力ファイル（schematic/とsimulations/）が完全に同一（diff差分
  なし）。

### 82.4 `gio_connections.json`への追記

ユーザー指示「JSONにも追記ください」に従い、`gio_connections.json`に
新規セクション`chip_top_pins`を追加：TOP_PIN_ORDER、P8が存在しない旨、
16本すべてのネットリネーム内容（DISの多重共有を含む）、生成元スクリプ
ト、出力ファイルパス、そして**レイアウト側は未対応**である旨を明記。
JSON構文の妥当性を`json.load`で検証済み。

### 82.5 残課題：レイアウト側のTOP PIN

ユーザーの診断は「Schematic と Layout」の両方を対象としていたが、今回
明示的に指示されたのはSPICE（スキーマティック）側のみ。レイアウト
（GDS）側に同様のTOPピンを追加するには、75.4節で確立された規約（セル
境界付近にM1PIN/M2PINマーカーレイヤー、3x3um、単なるテキストラベルで
はなく）に従い、P1-P15/VDD/VSSパッド位置にピンマーカーを追加する作業が
別途必要。ユーザーからの明示的な指示待ち。

## 83. チップトップレベルLVSエラーの修正：レイアウト側TOP PIN追加

82節でスキーマティック（SPICE）側は修正済みだが、ユーザーの元々の診断
「Schematic と Layout に Top PIN がついていないことが原因です」の
レイアウト側は未対応のまま残っていた。

### 83.1 中間の指示行き違い

ユーザーから「schematic/gio_connections.json の情報 "PAD" を使って
ください。」と短い指示があった。何を指すか曖昧だったため
AskUserQuestionで確認したところ、ユーザーから「指示間違いです。」と
返答があり、この指示は撤回された。直後に正しい指示が改めて与えられた
（83.2）。

### 83.2 ユーザー指示

「OSS_FRAME_GIO の PO layer の中心座標を使って Layout に 3x3um M2PIN
とTXM2 で TEXT つけます。JSON に記録。TEXTラベルは、(-200,1040)から
反時計回りでP1~P7,VSS,P9-P15,VDDです。」

### 83.3 PO layer調査

`tech/klayout/tech/drc/00_Layers.drc`を確認：`PO = input(14, 0)...
# PAD`——PO（レイヤ14/0）はコメントで明示的に"PAD"用途と確認。
`FRAME/TR-1um_frame_25x25.gds`の`OSS_FRAME_GIO`セルをklayout.dbで
`RecursiveShapeIterator`によりPO層を再帰的に走査したところ、20個の
形状を検出：
- 4個：チップ四辺の大きなバスバー用マーカー（2500x45um /
  45x2500um、VDD/VSSレール用と推定、個別パッドではない）
- 16個：80x80umの正方形マーカー、各1個ずつパッドに対応

16個の中心座標を実際のM2(20,0)メタル形状（層階層を再帰的に辿って）と
突合したところ、全16点が対応するM2パッド金属（100x100um角）の内部に
正確に一致することを確認——PO層マーカーが各パッドの中心座標を表して
いることが独立検証された。

`OSS_FRAME_GIO`インスタンスは`src/tr_1um_i2c_slave_async.gds`の
トップセル内で位置(0,0)・角度0・ミラー無し・倍率1（恒等変換）で配置
されていることをklayout.dbで確認。したがってPO層のローカル座標が
そのままチップ絶対座標として使用できる（変換不要）。

### 83.4 反時計回り順序の導出

ユーザー指定の起点(-200,1040)を含む16点それぞれについて、チップ中心
(0,0)を基準としたatan2(y,x)角度（x右・y上の標準数学座標系、角度増加
方向＝反時計回り）を計算し、(-200,1040)（角度100.89°）から角度増加
順に16点を並べた：

(-200,1040)→(-600,1040)→(-1040,600)→(-1040,200)→(-1040,-200)→
(-1040,-600)→(-600,-1040)→(-200,-1040)→(200,-1040)→(600,-1040)→
(1040,-600)→(1040,-200)→(1040,200)→(1040,600)→(600,1040)→(200,1040)

これに指示通りP1,P2,P3,P4,P5,P6,P7,VSS,P9,P10,P11,P12,P13,P14,P15,VDD
の順でラベルを割り当てた（対応関係は`gio_connections.json`の
`chip_top_pin_layout_coords`に記録）。

### 83.5 マーカー追加スクリプトと実行

`script/add_top_pins_gio_v9.py`を新規作成。75.4/68.x/77.44節で既に
確立・LVS検証済みのコアセル用ピン規約（3.0x3.0umボックス、M2PIN=
(49,1)、TXM2=(49,0)、ラベルはボックス中心に配置——68節のバグ修正済み
規約に準拠）をそのまま踏襲し、`src/tr_1um_i2c_slave_async.gds`の
トップセル`tr_1um_i2c_slave_async`（GIOセル自体ではなく、チップの
トップセルに直接）に16個のM2PINボックス＋TXM2テキストラベルを追加。
コアセルの信号ピン追加時と同じ「境界ピンは対象セル自身に直接追加する」
方針を踏襲——実際、修正前の`.extracted`ファイルでもトップセル自身の
直接マーカーのみがポートとして認識されていた（VSSの1本のみ、GIOの
内部マーカーは継承されない）ことと整合する。

出力：`outputs/tr_1um_i2c_slave_async_top_pins.gds`（一旦outputsに生成
後、md5sum一致を確認して`src/tr_1um_i2c_slave_async.gds`に上書き）。

### 83.6 検証

- トップセル直下（再帰なし）のM2PINボックス16個、TXM2テキスト16個を
  確認。全ボックスが正確に3.0x3.0umであることを確認。
- 全16個のテキストラベルが対応するM2PINボックスの内部（中心）にある
  ことを確認（68節のバグの再発防止）。
- 全16点がOSS_FRAME_GIO階層内の実M2メタル（再帰走査）と一致すること
  を再確認。
- DRC再実行（`drc_check_nrow_fm.py`）：M1 width/space=0/0、
  M2 width/space=1/5、V1関連=0。この`M2 width=1/space=5`は本修正前の
  `layout/step8/v9_top_routed.gds`（79節でDRCクリーンと確認済みの
  ベースライン）に対して同一スクリプトを実行しても全く同じ値が出る
  ことを確認済み——本修正（M2PIN/TXM2＝レイヤ49への追加のみ、
  `drc_check_nrow_fm.py`のM2チェックはレイヤ(20,0)のみ対象）による
  新規DRC劣化ではない、既知の非関連項目であることを確認。
- md5sum一致確認（`outputs/`→`src/`コピー前後）。

### 83.7 変更したファイル

- `script/add_top_pins_gio_v9.py`：新規作成、レイアウト側TOPピン
  追加スクリプト。
- `src/tr_1um_i2c_slave_async.gds`：トップセルに16個のM2PIN(49,1)+
  TXM2(49,0)ピンマーカーを追加して上書き。
- `schematic/gio_connections.json`：`chip_top_pins.layout_side_status`
  を更新し、新規セクション`chip_top_pin_layout_coords`（各パッドの
  絶対座標・導出方法・反時計回り順序の根拠）を追加。

### 83.8 残課題

チップレベルLVSを再実行し、`src/LVS_error.lvsdb`で見られた
OSS_FRAME_GIO階層のピン不一致（HIZ2/HIZ7/HIZ9/HIZ10/HIZ15/OUT13）が
解消されるか、トップサブサーキット自体が正しく16ポートで抽出される
かを確認する必要がある——ユーザー側でのLVS再実行待ち。

## 84. HIZ2/HIZ7/HIZ9/HIZ10/HIZ15/OUT13のVDD/VSSタイ結線を復元

ユーザーがLVSを再実行（新しい`src/LVS_error.lvsdb`、KLayout Netlist
Database Browserのスクリーンショット提供）。結果：DRCはクリーンだが
LVSはエラー——**83節のTOP PIN修正では解消しなかった**。不一致ピンは
前回と全く同じ6本（HIZ2, HIZ7, HIZ9, HIZ10, HIZ15, OUT13、すべて
OSS_FRAME_GIO回路内、「No equivalent pin ... from reference netlist
found in netlist. This is an indication that a physical connection is
not made to the subcircuit.」）。

### 84.1 真の根本原因の特定

`src/tr_1um_i2c_slave_async.extracted`のOSS_FRAME_GIO実抽出ネットリ
ストを直接確認したところ、`.SUBCKT OSS_FRAME_GIO`のポートリストに
HIZ2/HIZ7/HIZ9/HIZ10/HIZ15/OUT13が**一切含まれていない**ことを確認
（例：`X$10 P2 VDD OUT2 HIZ2 VSS OSS_ESD_5V_DIO`——HIZ2はインスタン
ス内部のネット名としては存在するが、セル境界を一切越えていない）。
つまり実レイアウト上でこの6ピンは本当にVDD/VSSへ未結線（フローティ
ング）だった。

原因は`script/route_gio_core_v9.py`自身のコメント（327-330行目）に
記録されていた：電源配線を書き直した際（"v4 bus-bar rebuild"）、
「HIZ2/HIZ7/HIZ15/HIZ9/HIZ10/OUT13へのタイはデジタルI/Oピン名であり
電源とは無関係」という**誤った判断**に基づき、この6本のタイ結線が
丸ごと削除されていた。実際にはこれらはスキーマティック上で意図的に
VDD/VSSへ固定結線する設計（Hi-Z制御の恒久固定）であり、`script/
route_gio_core.py`（v7の実配線スクリプト、`VDD_HIZ2`/`VDD_HIZ7`/
`VDD_HIZ15`/`VSS_HIZ9`/`VSS_HIZ10`/`VSS_OUT13`エントリ）では正しく
結線されていた——これがv7とv9のLVS結果の違いの真因であり、TOP PIN
の有無とは無関係だった。

### 84.2 各ピンの実座標調査とタイ結線の設計

`klayout.db.LayoutToNetlist`でM1(13,0)/V1(19,0)/M2(20,0)の接続性を
再抽出し、以下を直接検証：

- HIZ2/HIZ7/HIZ9/HIZ10/HIZ15/OUT13の各ピン自身の実ジオメトリ（M1/M2
  スタブ）座標を`probe_net`+`shapes_of_net`で特定。
- HIZ9/HIZ10/OUT13の位置は、VSSのM2リング自体に**あらかじめ切られた
  約2.6〜3.0umのDRCクリアランス・ノッチ**の中にあることを確認（ノッチ
  形状はVSSポリゴンの頂点列を直接調べて確認）——このタイ結線用に意図
  的に空けられたスペースと判断。
- HIZ2/HIZ7/HIZ15の直近には、当初想定していたVDD用M2ではなく**VDD用
  M1**しか無いことを判明（そのエリアにVDD用M2は存在しない）。さらに、
  ピンからVDDへの最短直線経路上に、**別ネット（おそらくOUT2自身の
  内部配線）のM1形状が障害物として存在**していることを発見——単純な
  直線接続では誤ってOUT2にショートする危険があった。

### 84.3 障害物回避のための自動衝突チェック

手作業での目視配線はショートのリスクが高いと判断し、
`klayout.db.Region`のブール演算を使った検証を導入：

1. 対象ウィンドウ内の生M1/M2形状から、自分のネット＋接続先ネット
   （VDD/VSS）以外の全形状を「forbidden領域」として抽出。
2. forbidden領域をDRCクリアランス分（M1: 1.4um、M2: 2.0um）膨張
   （`sized()`）。
3. 候補配線ポリゴンが膨張後のforbidden領域と一切重ならないことを
   面積0で確認——重なれば別経路を試す。
4. HIZ2について、グリッドベースBFS（0.5umステップ）で
   forbidden膨張領域を障害物としたパスを自動探索し、「まず横方向に
   OUT2的障害物を迂回してから縦方向にVDDへ抜ける」経路を発見。
   HIZ7/HIZ15は対称性（Y反転/X反転）を確認の上、同じ経路パターンを
   適用し、それぞれ個別に衝突チェックを再実行して確認。

### 84.4 M1コーナーの最小幅DRC違反と修正

上記経路をL字（水平セグメント+垂直セグメント）で最初に実装した際、
2セグメントを**辺だけで接する**形にしたところ、`drc_check_nrow_fm.py`
でM1幅違反が6件（各コーナー1件）発生。原因はL字内側コーナーの
「くびれ」——2つの矩形をマージした際、コーナー部のローカル幅が
1.8um未満に落ち込む古典的なDRCアーティファクト。修正：2セグメントを
コーナー部で1.8um分**重ねる**（辺同士を接するのではなく正方形状に
オーバーラップさせる）ことで解消。修正後、単独幅チェックで違反0件を
確認してから本適用。

### 84.5 実装・検証

`script/add_hiz_vss_ties_v9.py`を新規作成。9個の新規矩形（HIZ2/HIZ7/
HIZ15の各L字タイ×2セグメント＝6個、M1；HIZ9/HIZ10/OUT13の各ブリッジ
パッチ×1＝3個、M2）を`src/tr_1um_i2c_slave_async.gds`のトップセルに
直接追加。

検証：
- `outputs/tr_1um_i2c_slave_async_hiz_ties.gds`へ一旦出力。
- DRC再実行：M1 width/space=0/0（修正前は6件のwidth違反があったが
  84.4の修正で解消）、M2 width/space=1/5（79節でDRCクリーン確認済み
  の`layout/step8/v9_top_routed.gds`ベースラインと完全一致、本修正
  による新規劣化ではないことを83節で確認済みの値と同一）、V1関連=0。
- `klayout.db.LayoutToNetlist`で再抽出し、HIZ2/HIZ7/HIZ15が
  VDDネット（cluster_id一致）に、HIZ9/HIZ10/OUT13がVSSネット
  （cluster_id一致）に、それぞれ電気的に統合されたことを直接確認。
- md5sum一致確認（`outputs/`→`src/`コピー前後）。

### 84.6 変更したファイル

- `script/add_hiz_vss_ties_v9.py`：新規作成、VDD/VSSタイ結線復元
  スクリプト。
- `src/tr_1um_i2c_slave_async.gds`：トップセルに9個の新規矩形
  （M1×6、M2×3）を追加して上書き。

### 84.7 残課題

チップレベルLVSを再実行し、今回追加した6本のタイ結線でOSS_FRAME_GIO
階層のピン不一致が実際に解消されるかを確認する必要がある——ユーザー
側でのLVS再実行待ち。もし解消していれば、トップサブサーキット自体
（P1-P15/VDD/VSSの16ポート、82/83節）も含めてLVSクリーンとなる見込み。

## 85. P11/tx_data[1]未接続バグ（gio_connections.jsonの記載ミス）の発見と修正

ユーザーが84節の修正後にLVSを再実行し、新しいKLayout Netlist Database
Browserのスクリーンショットを提供。結果：

- **HIZ2/HIZ7/HIZ9/HIZ10/HIZ15/OUT13はログから完全に消えた**——84節の
  タイ結線修正は有効だったことをログが直接裏付けている（OSS_FRAME_GIO
  回路自体への言及がスクリーンショットから消滅）。
- 一方でユーザーは本文で「HIZ2,7,9,10,15,OUT13が未接続です」と報告した
  が、これはスクリーンショット自体の内容（新しいエラーはP11のみ）と
  矛盾していた。おそらくKLayoutでレイアウトを目視確認した際の所感で、
  LVSログの結果とは別——84節のタイはM1層（HIZ2/7/15側）主体のため、
  M2のみ表示するビュー設定だと見えない可能性がある旨、ユーザーに
  フィードバックする必要がある。
- **新しいエラー**：トップ回路`tr_1um_i2c_slave_async`で
  「Net P11 is not matching any net from reference netlist」
  「Net P11 may be shorting nets NC_CORE_TX_DATA[1] and P11 from
  reference netlist (fuzziness 0 nodes)」。

### 85.1 調査：本当にP11とtx_data[1]は物理的に繋がっているか

`klayout.db.LayoutToNetlist`でM1/V1/M2の接続性を再抽出し、P11の
チップトップピン座標(1040,-600)・P11自身のGIOネイティブラベル座標
(922.0,-530.0)・core.tx_data[1]のラベル座標(808.5,341.2)を
`probe_net`で照合したところ、**3点とも同一net（cluster_id一致、
M1形状59個+M2形状26個の実体あるネット）**であることを確認。単なる
微小な偶発ショートではなく、実際に太い配線で結ばれた「本物の」信号
接続だった。

### 85.2 根本原因：gio_connections.jsonの記載誤り

`schematic/v9_signal_routing_plan.json`（`route_gio_core_v9.py`が
実際の物理配線に使う一次ソース、いわば「真実」）を確認したところ、
`"tx_data[1]"`エントリのgio側座標が`(921.7,-530,RIGHT,M2)`——これは
P11パッド自身のピン位置と完全一致（GDS上のP11ネイティブラベル位置
922.0,-530.0と誤差0.3um）。つまり**v9は最初からP11とtx_data[1]を
実際に結線する設計・実装だった**。

ところが`gio_connections.json`の`connections_per_terminal_detail.P11`
は`{"net": null, "core_signal": null, "note": "unconnected..."}`と
誤って記載されていた（P9/P10と一緓に「予備ピン」としてまとめて
未接続扱いしてしまった、実際にはP11だけ v9で使用されていた）。
`v9_signal_routing_plan.json`の20信号ネット全件をGIO側ラベルと突合
した結果、この誤りはP11の1件のみで、他の19本（rst_n, scl, sda_in,
sda_oe, tx_data[0,2-7], rx_data[0-7]）はすべてgio_connections.json
の記載と完全に一致することを確認済み。

この記載ミスが80節・82節で作成した`gen_lvs_spice_top_v9.py`（JSON
から機械的にネットリストを生成）に伝播し、参照（スキーマティック）
側でP11とtx_data[1]を別々の孤立`NC_`ネットとして宣言してしまい、
実レイアウト（両者が実際に同一ネット）との間にLVS不一致を生んだ
——82節・84節の修正とは無関係の、別系統のバグ。

### 85.3 修正

`schematic/gio_connections.json`を修正：

- `connections`配列のP11エントリに、P-side（入力パス）が実際は
  tx_data[1]を運ぶ旨のnoteを追記（OUT-side=rx_data[1]と合わせて
  P13/sda_in+sda_oeと同様の双方向的パッドであることを明記）。
- `connections_per_terminal_detail.P11`を`{"net": "IN1",
  "core_signal": "tx_data[1]", "note": "CORRECTED..."}`に修正
  （ネット名"IN1"は旧v7時代の`src/tr_1um_i2c_slave_async.cir`が
  同じ位置で使っていた命名を踏襲——過去のサマリーで「v7ではP11が
  net 'IN1' でtx_data[1]相当に繋がっていた」という記録と一致）。
- `chip_top_pins.net_renames_applied.P11`のコメントも
  「floating→named port」から「real net、IN1→P11」に更新。

`script/gen_lvs_spice_top_v9.py`を再実行し、`schematic/
tr_1um_i2c_slave_async_v9_lvs.spice`・`simulations/
tr_1um_i2c_slave_async.spice`を再生成。診断出力で
`tx_data[1]`が「GIO pad接続なし」リストから、`P11`が「GIO pins with
NO connection」リストから、それぞれ消えたことを確認。生成された
x1行のP11位置・x2行のtx_data[1]位置がともに`P11`という同一ネット名に
なっていることを直接確認（floating net総数は13→11に減少、ちょうど
NC_CORE_tx_data[1]とNC_P11の2つが消えた分と一致）。2つの出力ファイル
は完全に一致（diff差分なし）。

### 85.4 変更したファイル

- `schematic/gio_connections.json`：P11の記載修正（`connections`・
  `connections_per_terminal_detail`・`chip_top_pins.
  net_renames_applied`の3箇所）。
- `schematic/tr_1um_i2c_slave_async_v9_lvs.spice` /
  `simulations/tr_1um_i2c_slave_async.spice`：P11修正を反映して
  再生成。

### 85.5 残課題

チップレベルLVSを再実行し、(1) 84節のHIZ系タイが実際にユーザー環境
でも解消済みと確認できるか（レイアウトの目視確認方法についてもM1
レイヤー表示の確認を依頼）、(2) 今回のP11/tx_data[1]修正でP11の
不一致が解消するか——ユーザー側での再実行待ち。

### 85.6 結果：DRC/LVSクリーン

ユーザー確認：「DRC/LVSクリーンです。」——82節（スキーマティック側
TOP PIN）・83節（レイアウト側TOP PIN）・84節（HIZ2/HIZ7/HIZ9/HIZ10/
HIZ15/OUT13タイ結線復元）・85節（P11/tx_data[1]記載ミス修正）の
一連の修正で、チップレベルLVSが最終的にクリーンとなった。

## 86. TOP PINラベルのTEXTサイズを20umに変更

ユーザー指示：「Layout の TopPIN の TEXT サイズを 20um にしてくださ
い。」——83節で追加した16個のTOP PIN（P1-P7, VSS, P9-P15, VDD）の
TXM2(49,0)テキストラベルは、`db.DText`生成時にサイズ指定なし
（デフォルト`size=0`）だった。

`script/set_top_pin_text_size.py`を新規作成：トップセル直下の
TXM2レイヤーから該当16ラベルを名前一致で特定し、`klayout.db.Text`
の`size`属性を20um（dbu換算20000）に設定（位置・文字列・レイヤーは
不変）。

検証：
- 更新後、全16ラベルの`size`が正確に20000（=20um）であることを
  直接確認。
- DRC再実行：M1 width/space=0/0、M2 width/space=1/5（既知のベース
  ラインと一致、TXM2はDRC対象レイヤーではないため変化なしは想定通り）。
- md5sum一致確認（`outputs/`→`src/`コピー前後）。

### 86.1 変更したファイル

- `script/set_top_pin_text_size.py`：新規作成。
- `src/tr_1um_i2c_slave_async.gds`：TOP PIN 16ラベルのテキストサイズ
  を20umに変更して上書き。

## 87. IRSIM再検証：LVS用spiceからの新.sim生成（gen_irsim_sim_v9.py）

ユーザー指示：「IRSIMでの再検証に入ります。LVSで使った.spice からIRSIM
用のファイルを準備してください。」——82-86節でチップレベルDRC/LVS
クリーンを確認した`schematic/tr_1um_i2c_slave_async_v9_lvs.spice`
（＝`simulations/tr_1um_i2c_slave_async.spice`と同一）を、旧
`src/tr_1um_i2c_slave_async.extracted`（KLayout LVS抽出ベース）の
代わりにIRSIM`.sim`生成のソースとして使う。

### 87.1 新旧ソースの構造比較

事前調査で新ソース（`tr_1um_i2c_slave_async_v9_lvs.spice`）の構文が
旧`.extracted`と異なることを確認：

- トランジスタ行：`M<任意サフィックス> drain gate source bulk model
  params...`（例`MXM1`/`MM2`/`M_FILL2_1_p`）。旧`XM$<n>`ではない。
- サブサーキットインスタンス行：`x<名前> net1..netN CELLTYPE`（小文字
  x、旧`X$<n>`ではない）。
- ESDダイオード：`D<名前> ...`（例`DD1`、旧`D$<n>`ではない）。
- `l=`/`w=`パラメータは小文字（旧は大文字`L=`/`W=`）。
- `.subckt`/`.ends`は小文字、かつ`.ends`はサブサーキット名を繰り返さ
  ない（旧`.ENDS <name>`と異なる）。
- **無名`$N`ネットが皆無**（24個の`.subckt`ブロック全てで確認）。トップ
  レベルのポートは82節の`TOP_PIN_ORDER`修正により文字通り`P1`..`P15`/
  `VDD`/`VSS`。内部ネットもYosysの実名（`bit_cnt[0]`、`_065_`等）。

このためVDD/GND識別（旧76.3の「探偵作業」）が不要になり、トップ
サブサーキットの仮引数リスト自体が`VDD`/`VSS`を持つ。

### 87.2 script/gen_irsim_sim_v9.py（新規作成）

`script/gen_irsim_sim.py`を上記の構文差分に合わせて書き直した新スクリ
プト。主な設計判断：

- パーサ：`.subckt`/`.ends`をブロック単位で素直に走査する方式に変更
  （旧の正規表現ベースの一括マッチではなく、`.ends`が名前を繰り返さ
  ないため）。`+`継続行のマージは旧同様、生テキスト全体に対して行う
  （コメント側の継続行は`*+`で始まるため誤って結合されない）。
- `M`/`x`/`D`の行頭1文字で分岐（大小文字両対応）。`l=`/`w=`パラメータ
  は大小文字問わず抽出。`m=`乗数パラメータ（`DEL1`等に存在）は旧同様
  無視（旧`.sim`は既にこの単純化のまま実機フル書き込みトランザクション
  検証に成功しているため、今回は再検討しない）。
- `VDD_NODE`/`GND_NODE`：トップの仮引数がそのまま`"VDD"`/`"VSS"`（
  探偵作業不要）。`Vdd`/`Gnd`への強制リネームは旧同様必要（IRSIM自体の
  デフォルト挙動要件、76.8）。
- `substitute_bufth_with_buf_x1()`：旧同様に必要（`BUFTH`の帰還ループ
  はIRSIMのターナリソルバー自体の恒久的限界、76.12-76.15、ハードウェア
  依存ではないためソース netlist が変わっても再発する）。新ソースでも
  `BUFTH`/`BUF_X1`の仮引数リストが完全一致（`VDD A Y GND`）することを
  直接確認済み。
- `insert_sda_hiz_inverter()`は**意図的に移植しなかった**：これはv7時代
  の`sda_oe`↔`HIZ13`間RTL極性バグ（本来あるべきインバータが無い）に
  対する検証用の埋め合わせだった。README.md記載の通りV8以降でRTL側の
  極性が修正済みであり、このv9 LVS用netlistは既にその修正済みRTLを
  反映したスキーマティックに対してLVSクリーンなので、旧インバータを
  再挿入すると逆に極性を壊す。実機IRSIM実行で`sda_oe`の極性が再び
  おかしければ、それはRTL側の退行を意味する（このジェネレータの不備
  ではない）。
- SDAプルアップ：概念（弱いPMOS、パッド自身の実駆動ゲートで排他制御）
  とサイジング（L=1u/W=1.9u、TR-1um.prm実測ベースで約19.6kΩ、76.29-
  76.38）はそのまま踏襲。ただしゲートノード（`PAD_DRIVE_NODE`）は
  ハードコードパスではなく**構造検索**で特定：フラット化後の全デバイス
  から`model=NMOSE, L=2, W=150`かつドレイン/ソースがトップネット
  `"P13"`に触れる唯一のトランジスタを検索し、そのゲートを使用（新
  ソースでは`OSS_ESD_5V_DIO`の`MXM2 PAD NG VSS VSS NMOSE w=150u
  l=2u`が該当、旧`XN1.XN16.N9`と役割は同一）。実行結果：
  `x1.x12.NG`が一意に見つかった（想定通り、`OSS_FRAME_GIO`の`x12`が
  P13＝SDAパッドのインスタンス）。プルアップ先ノード（`PULLUP_NODE`）
  も旧`sanitize('$58')`のようなエイリアス不要で、単に`"P13"`（トップ
  レベルの素の実ネット名）。

### 87.3 実行結果・検証

```
parsed 24 .subckt blocks from ../schematic/tr_1um_i2c_slave_async_v9_lvs.spice
flattened: 155 instances walked, 2076 transistors, 28 ESD diodes skipped, max hierarchy depth 2
SDA pad drive-gate node (structurally found): x1.x12.NG
by original model: {'PMOS': 1038, 'NMOSE': 30, 'NMOS': 1008, 'PMOS-pullup-gated': 1}
distinct nodes: 844
wrote ../irsim/tr_1um_i2c_slave_async.sim
```

旧`.sim`（2074実トランジスタ＋合成3個＝2077個、ノード863個）とほぼ
同水準（新：2076実＋合成1個＝2077個、ノード844個）——合成デバイス数の
差2個は不要になった`insert_sda_hiz_inverter()`の2トランジスタ分と正確
に一致し、想定通り。追加の直接検証：

- 出力`.sim`中に無名`$`ノードが実データ行に皆無であることを確認（ヘッダ
  コメント文字列中の1件のみ、実ノード名ではない）。
- `Vdd`（1080箇所）・`Gnd`（1060箇所）が期待通り多数のデバイス端子に
  出現することを確認。
- 末尾のSDAプルアップ行が`p x1.x12.NG Vdd P13 1 1.9`であることを直接
  確認（設計意図通り）。

### 87.4 トップレベルネット名の再導出（gen_irsim_cmd.py用の下準備）

新ソースはトップレベルポートが実名のため、`gen_irsim_cmd.py`が必要と
する主要信号の一部は、旧`.extracted`のような多段の位置合わせ調査
なしで**直接**特定できることを、トップサブサーキットの実引数リスト
（x1=`OSS_FRAME_GIO`、x2=`i2c_slave_async_nrow_fm`）の位置照合のみで
確認した（`.sim`中に対応ノードが実際に複数デバイス端子で使われている
ことも`awk`で追加確認済み）：

| 信号 | 新ノード名（旧） |
|---|---|
| rst_n | `P15`（旧`N106`） |
| scl | `P2`（旧`N102`） |
| sda_in（SDAパッド） | `P13`（旧`N58`） |
| sda_oe（内部、監視専用） | `SDA_O`（旧`N45`） |
| DIS（P7） | `P7`（旧`N2`、たまたま同名） |
| tx_data[0..7]（bit0..bit7） | `P12 P11 P5 P6 P4 P1 P3 P14`（旧`N26 N19 N21 N22 N57 N103 N72 N68`） |
| rx_data[0..7]（bit0..bit7） | `NET_0..NET_7`（旧`N32 N15 N34 N13 N42 N78 N83 N64`） |
| busy（内部、外部ピンなし） | `NC_CORE_busy`（旧`XN3.busy`） |
| rw（内部） | `NC_CORE_rw`（旧`XN3.rw`） |
| addr_match（内部） | `NC_CORE_addr_match`（旧`XN3.addr_match`） |
| rx_valid（内部） | `NC_CORE_rx_valid`（旧`XN3.rx_valid`） |

busy/rw/addr_match/rx_validが新ソースでは`i2c_slave_async_nrow_fm`の
正式な仮引数（トップまで配線済み、`NC_`接頭辞＝チップピン未接続の意）
になっている点が旧`.extracted`（内部専用ノード、パス指定`XN3.busy`
必須）との構造上の違い——今回のverilogベース再生成（77.45/77.46）に
よるもので、むしろ新方式の方がシンプル。

### 87.5 残課題：gen_irsim_cmd.pyは未更新（次のIRSIM再実行前に必要）

`script/gen_irsim_cmd.py`は依然として**旧`.sim`のノード名**（`N45`/
`N58`/`N102`/`N106`/`XN3.busy`/`XN2`等の33個のDFFRBインスタンスパス/
`N34`/`N635`/`N666`/`N343`のクロックドメインnet/`DEL1`の`N697`/
`N704`等）をハードコードしたままで、新`.sim`にはこれらのノードは
**存在しない**（インスタンス名の付け方自体が旧`X$n`→新`xn`、内部
ネット名もYosys再合成でおそらく完全に別物）。87.4の表はトップレベル
（ポートまで配線されている信号）のみ再導出済みだが、`gen_irsim_cmd.py`
が依存する**内部専用**のワークアラウンド系ノード（33個のDFFRBインス
タンスの`QS`パス、`$28`/`$227`リセットグループの構成、4本のクロック
ドメインnet、`DEL1`/`BUFTH`内部ノード）は、新ソースのインスタンス
階層・合成後ネット名に対して**再調査が必要**（旧76.10-76.42相当の
深い実機トレース作業に匹敵する規模）。ユーザーへ報告の上、続行するか
どうかの判断を仰ぐ。

## 88. IRSIM再検証：gen_irsim_cmd.pyの新ノード名対応（gen_irsim_cmd_v9.py）

### 88.1 DFFRBインスタンスのリセット構造再調査

`schematic/tr_1um_i2c_slave_async_v9_lvs.spice`の`i2c_slave_async_
nrow_fm`本体から、`... DFFRB`で終わるインスタンス行を全数grep（33個、
`x_269_`〜`x_301_`）。`DFFRB`の仮引数順（`VDD QB D Q RSTB GND CK`）は
固定なので、各行の5番目（RSTB）・7番目（CK）フィールドを直接読むだけ
で（推測なし）以下を確認：

- RSTBネットは2種類のみ：`_008_`（24個）／`_009_`（9個、`sda_oe_r`
  含む）——個数が旧設計の`$28`グループ（24個）／`$227`グループ（9個）
  と完全一致。
- 駆動源も直接確認：`_008_` = `NOR2(NOT(rst_n), start_pulse)`（旧
  `$28`と同じ「rst_n解放で即座に解放」構造）、`_009_` =
  `AND2_X1(busy, rst_n)`（旧`$227`と同じ「busyが初めて1になるまで
  解放しない」構造）。
- `_008_`グループのCKフィールドは`scl_row0`/`scl_row1`/`scl_row2`/
  `scl_row3`のちょうど4種類——旧設計の「4本の派生クロックnet」構造と
  個数一致。

各DFFRBインスタンス自身の`QS`ノード（サブサーキット内部の実ノード名、
旧設計と同じく`QS`のまま）は、フラット化後`x2.<インスタンス名>.QS`
という予測可能な形になる（`x2`＝トップでのコアインスタンス名）。

### 88.2 script/gen_irsim_cmd_v9.py（新規作成）

旧`gen_irsim_cmd.py`のバス機能モデルロジック（`CmdGen`クラス全体、
`force_release()`/`force_release_gated()`の手法自体）はMyHDLテスト
ベンチの機械的翻訳であり、ノード名に依存しないIRSIM側の一般的な制約
（トランスミッションゲート構造、RC充電時間等）への対処なので**無変更
で流用**。変更したのは定数のみ：

- トップレベル信号（87.4節の表通り）：`RST_N=P15`、`SCL=P2`、
  `SDA=P13`、`SDA_OE=SDA_O`、`DIS=P7`、`TX=[P12,P11,P5,P6,P4,P1,P3,
  P14]`、`RX=[NET_0..NET_7]`、`BUSY=NC_CORE_busy`、`RW=NC_CORE_rw`、
  `ADDR_MATCH=NC_CORE_addr_match`。
- `DFFRB_28_INSTANCES`（24個）／`DFFRB_227_INSTANCES`（9個）：88.1で
  特定した`x2.x_NNN_`形式のインスタンスパスに更新。
- `DFFRB_28_CLOCK_NETS`：`["x2.scl_row0", "x2.scl_row1",
  "x2.scl_row2", "x2.scl_row3"]`に更新。
- コメント中の旧ノード名（`N697`/`N704`/`$813`/`$695`/`$696`等）への
  具体的言及は、対応する新ノードが未特定のまま古い数値だけ残ると誤解
  を招くため、一般化した記述に置き換え（技術的な結論・手法自体は
  変更なし）。

### 88.3 検証

生成した3つの`.cmd`ファイル（`irsim_test_main.cmd`656行、
`irsim_test_negative.cmd`319行、`irsim_reset_check.cmd`114行）から
`h`/`l`/`x`/`d`/`ana`コマンドが参照する全ノード名（63個、重複除去後）
を抽出し、`tr_1um_i2c_slave_async.sim`の実際のノード集合（844個）と
突き合わせ。**63個全てが実在確認（未定義ノード0件）**。

### 88.4 実行結果（ユーザー実機、irsim 9.7.121）

`irsim TR-1um.prm tr_1um_i2c_slave_async.sim`のロード結果:
```
Using default name "Vdd" for power net.
Using default name "Gnd" for ground net.
Read tr_1um_i2c_slave_async.sim lambda:1.00u format:MIT
845 nodes; transistors: n-channel=1038 p-channel=1039 shorted=256
parallel txtors: n-channel=2 p-channel=2
```
Pythonでの集計（n=1038, p=1039, 合計2077トランジスタ）と完全一致。
「Unexpected first line:」警告は旧`.sim`と同一のヘッダー1行目文言
（`| units: 100 tech: scmos`）によるもので、旧セッション（§76.27で
エンドツーエンド成功済み）でも同様に出ていた既知の無害な警告と判断。
845ノードは自分の集計（844）と1個差だが実害なし（IRSIM内部の予約
ノード分と推定）。`shorted=256`はFILL2/FILL3デコップリング容量セル
（drain=source=VDD構成）由来、`parallel txtors`はDFFRBの並列バイアス
トランジスタ由来で、いずれも異常ではない。

BUFTH→BUF_X1置換も`.sim`内で直接確認（`x2.xu_bufth_scl.*`/
`x2.xu_bufth_sda_in.*`が各4トランジスタのBUF_X1構造になっており、
BUFTH本来の8トランジスタ帰還構造ではないことをgrepで確認）。

### 88.5 残課題

`irsim_test_main.cmd`等の実機実行はまだ行っていない（ユーザー側で
`irsim TR-1um.prm tr_1um_i2c_slave_async.sim`起動後`@ irsim_reset_
check.cmd`から順に実行予定）。旧`.sim`で発見・対処済みの各種問題
（DFFRBコールドスタート、SDA極性、DEL1セトリング等）が、v9の再合成
RTL・新ネットリスト上でも同様に必要か（あるいは既に解消しているか）
は、実行結果を見るまで未確定。

## 89. IRSIM実機バッチ実行結果（v9ネットリスト初回）

`irsim/run_batch.sh`（stdinヒアドキュメント経由、`-@`フラグ回避）で
`irsim_reset_check.cmd`→`irsim_test_main.cmd`→`irsim_test_negative.cmd`
を連続実行。ログ（`irsim_batch_run.log`）の全`d`コマンド出力を`.cmd`
ファイル中の対応する`d`行と順番通り突き合わせて解析。

### 89.1 ロード・reset_check：正常

845ノード、n=1038/p=1039（想定通り）。**驚くべき発見**：`reset_check.cmd`
は`rst_n`を100ns保持しただけで`SDA`/`sda_oe`/`busy`/`rw`/`addr_match`が
全て確定値に収束した（旧`.sim`で必要だった33個DFFRBの`QS`強制force/
release無しで、しかもチェックポイント全て安定）。この後`h(RST_N)`で
リセット解放＋Group-A（24個）`QS`force/releaseを行っても引き続き
全て確定値のまま——つまりv9の再合成ネットリストでは、旧設計にあった
「コールドスタート時のQM/QS未定義デッドロック」問題自体が**そもそも
再現していない可能性**が高い。

### 89.2 test_main：WRITEトランザクション（1回目のSTART、`first=True`）— 完全成功

- `busy`：STARTで正しく1へ。
- アドレスバイト`0xA0`（=0x50<<1|0）送信後、ACK確認：`P13=0`（正しく
  ACK）、`addr_match=1`/`rw=0`（正しくwrite判定）。
- データバイト`0xA5`送信後、ACK確認：`P13=0`（正しくACK）。
- `rx_data[0..7]`（`NET_0..NET_7`）＝`1 0 1 0 0 1 0 1` → **`0xA5`と完全
  一致**（シフトレジスタへの取り込みが正しく機能）。
- STOP後：`busy=0`（正しく解除）。

WRITEトランザクションはエンドツーエンド（START→ADDR+ACK→DATA+ACK→
rx_data確定→STOP→busy解除）で**全チェックが期待通り**。

### 89.3 test_main：READトランザクション（2回目のSTART、`first=False`）— アドレス一致せず

2回目のSTART直後の`busy=1`は正しく確認できたが、`start(first=False)`
内部の`force_release_gated()`（Group-A `QS`＋4本の行クロックnet
`x2.scl_row0..3`を同時にforce/release）実行後、アドレスバイト`0xA1`
（=0x50<<1|1）送信後のACK確認で：

- `P13=1`（**ACKされず**、期待は`P13=0`）
- `NC_CORE_rw=0`（**期待は1**——読み出しなのでrwビットが立つはず）

以降の8ビット`recv_bit()`もSDA(`P13`)が全て`1`のまま変化なし（スレーブ
がtx_data送信を一切していない＝addr_matchしていないため無反応、という
一貫した結果）。

**分析**：89.1で判明した通り、v9ネットリストではQS force/releaseが
そもそも不要な可能性が高いのに対し、`force_release_gated()`は
（旧設計の対策のまま）Group-Aの`QS`だけでなく**4本の行クロックnet
自体も強制的にLOWへ落とす**——もし新ネットリストの`scl_row0..3`
生成ロジックが旧設計と完全に同一でない場合、この強制動作がむしろ
回路を壊している可能性がある（旧設計での「正しい」対策が、新設計
では不要かつ有害になっている可能性）。次の実験候補：2回目以降の
`start()`でも`force_release_gated()`ではなく単純な`force_release()`
（Group-A `QS`のみ、クロックnet強制なし）を試す、あるいは何も
force/releaseせず素の回路動作に任せてみる（89.1の知見が正しければ
これで十分な可能性）。

### 89.4 test_negative：addr_match自体は正しいが、NACKチェックに
タイミングマージン不足

`addr_match=0`（不一致アドレス`0x22`=0x11<<1|0を正しく非一致判定）
は確認できたが、直前の`P13`（SDA）チェックが`0`（NACKなら`1`である
べき）と出た。原因を精査した結果、この`d(SDA)`直前の`x(SDA)`後の
待ち時間が`g.s()`（1ステップサイズ=20ns）のみで、76.39/76.40で判明・
修正済みのSDA外付けプルアップのRC充電時間マージン（`SDA_RELEASE_
SETTLE_NS`=500ns）が**この1箇所だけ未適用のまま**（旧`gen_irsim_cmd.py`
から無変更でコピーした`gen_negative()`の独自インラインチェックが、
`read_ack()`等と違って個別に実装されていたため見落とし）だったこと
が判明。addr_match自体は正しく0のままなので、これは実際の誤ACKでは
なく**プローブタイミングが早すぎた計測アーティファクト**と判断。

`script/gen_irsim_cmd_v9.py`の`gen_negative()`を修正：該当箇所の
`g.s()`を`g.s(SDA_RELEASE_SETTLE_NS)`に変更し、`.cmd`ファイルを再生成
（ノード参照の再検証：63個中未定義0件、変わらず）。

### 89.5 run_batch.sh の軽微な修正

ログ末尾で`quit`コマンドが`(tty,4): unrecognized command: quit`と
なっていたが、それより前の全シミュレーションは正常完了しており実害
なし（stdinがEOFに達した時点でirsimは正常終了していた）。
`irsim/run_batch.sh`から`quit`行を削除（ヒアドキュメント終端＝EOFの
みで十分）。

### 89.6 残課題

- READトランザクション（2回目以降のSTART）のアドレス不一致問題：
  `force_release_gated()`の要否・妥当性を再検証する実験が必要
  （ユーザーの判断待ち）。
- test_negative修正版の再実行待ち（89.4の仮説が正しいか確認）。
